<!-- document-role: active-plan -->

# Scene 当前断点修复队列

> 核对：2026-09-16，依据全量工具/台账审计调整入口，未重跑样本。文件名日期仅保留链接身份。
> 本文是[兼容路线](scene-compatibility-roadmap.md)的短队列，不建立第二套阶段；工程性能工作按[重构计划](engine-refactor-program.md)执行。

## 1. 当前证据边界

样本身份只用于复现，不能进入产品分派。旧 corpus 计数和人工 verdict 不代表当前 HEAD 已重新验证；技术修复不能自动改写人工验收。当前能力查[能力台账](semantics/coverage-ledger.md)，运行身份查[运行证据](semantics/runtime-evidence-current.md)。

## 2. 现役执行顺序

### Q0 — 先完成兼容路线 P0 的全量关联与采集校准

按[兼容路线 §3.2](scene-compatibility-roadmap.md#32-p0-的四个有界批次)依次交付：盘点/fingerprint/守恒校验 → 既有表的声明/公共合同/代码 owner/场景/证据关联 → 普通产品启动采集校准与 fresh 全集基线 → 共享依赖及首批修复集合。工具扩展是待执行工作，不因本次计划修改视为完成。

每一步只改其完整职责；Q1 保留已有反馈作为复现候选，实际修复顺序由 fresh 首断点、公共依赖和影响面确定，不继续按旧报告的集群数字或以下样本顺序硬排。紧急可见回归可提前修复。

当前用户仅有现成截图或视频，先登记可用参考与控制变量缺口。固定官方客户端同输入动态对照是 P4 关闭依赖；缺环境不阻塞公共实现，但不能记官方一致性通过。

P0 续跑状态（2026-09-17，HEAD `39826fa8` + resident-budget 安全复核/证据未提交批次）：

- P0.1 校准盘点：census 漂移已诊断并刷新（快照内容原定格 `a7b0111a`；`7a4cb4b4` 改生成器模板未重生成 → 源码自哈希漂移；corpus 实变＝45 份 `用户观察说明.md` 消失、`3477054430` 新增截图、32 样本被注入 root 属主 `shaders/blobsSM40/*.dxs` 树共 +592 文件、34 目录新增已排除的 `.mywallpaperx-scene-*` 派生文件）。`verify` passed（payload `9839015b…`），守恒全平衡（64816 occurrence、159/159、orphan 0），census 测试 9/9，独立子代理审查通过（P3 叙述类）。改动未提交：`script/scene_capability_census_snapshot.json`、`docs/scene/semantics/scene-corpus-capability-inventory.md`。
- 待用户裁决：45 份观察说明内容去向不明（本机常规缓存无副本）；样本根 root 属主注入的写入者未查。
- 生成器候选缺陷（另批处理）：md 证据列二元口径（census.py `render_markdown` 附近，仅有截图无说明的样本显示"无"）。
- 下一动作：P0.2 声明→能力/合同/owner→场景→证据关联，schema/migration 与引用完整性门先行。
- P0.2 引用完整性门已落地（2026-09-17 晚，本批）：census 新增 `validate_repair_references(ledger, known_sample_ids, repository_root)`——此前 census 只校验 repair ledger 的**字段与枚举**（状态契约、bounded-verified 必备项、event 形状、family_key 是否存在），完全不校验引用是否指向真实对象。新门检查：`targeted_samples ⊆ corpus 样本`、`regression_gates[].reference` 的 kind 在合同内且 repo 内目标文件与 `#symbol` 存在、`roi_evidence`/`events[].evidence_refs` 的 repo 内路径存在、`commit` 与 `events[].commit` 形状合法（占位符 `uncommitted-working-tree` 允许）。**repo 外路径（典型为 `/private/tmp` 运行产物）按产物治理视为瞬态，只计数不失败**；commit 只做形状校验、不查本地历史，以保持 census 的确定性 payload。首次运行抓出 **13 处真实悬空引用**并已逐条处置：3 处 `dynamic-input/scenescript@…` 的 `synthetic-positive` 指向 `5d7bb4a5` 改名前的 `test_unproven_display_scripts_suppress_layers_and_descendants` → 重指到同一文件内的后继 `test_visible_scripts_keep_previous_current_while_alpha_stays_suppressed`；`shader/frag@60ed3f1823e61bb5` 的 `synthetic-negative` 指向被 `87d254c8` 随专用 effect runtime 一并退役的 `test_launch_precompiles_the_static_texture_readiness_envelope`，`resource/model@9d3474c9219422b7` 的 `synthetic-negative` 指向**任何提交中都不存在**的 `test_resolved_material_runtime_bridge_compiles_and_executes` → 两处**移除**并在各自 `remaining_boundaries` 记录移除理由（同 family 另有覆盖拒绝合同的 negative gate，未下调保护等级）；8 处事件证据指向不存在的 `docs/scene/semantics/runtime-evidence-index.md` → 更正为 `docs/history/scene/runtime-evidence-index.md`（4 个锚点均已在该文件核实）。**独立复审（3 个并行只读验者）判定前两条成立、第三条部分不成立**：门在畸形输入上会抛异常而不是返回失败记录（`regression_gates`/`targeted_samples`/`roi_evidence`/`events` 为真值非列表、`kind` 为不可哈希值、非 dict ledger、路径含 NUL 等），且逃逸守卫用字符串前缀、证据分支缺包含检查、`path_part` 为空时静默跳过、docstring 声称「只计数」但实现未计数——已按处方修正：所有容器访问走形状守卫、包含检查改 `Path.is_relative_to`、证据分支同样拒绝逃逸、docstring 与实现一致、符号检查改为**整词匹配**（原先「必须是 Python 定义」的严判会误伤 8 处指向测试文件内嵌 Swift harness 符号的合法引用）。修后 15 个畸形用例 **0 抛异常**，逃逸/空引用/NUL 路径均产出 typed failure。另按复审记录一处**台账内部张力**：3 个重指 family 的 `synthetic-positive` 后继测试在 `5d7bb4a5` 中同时把 visible 字段断言由「抑制 + unproven-inline-scenescript」改成「previous-current + awaiting-scenescript-publication」，即门认证的是**当前**拒绝语义、而 family 的 `public_fix` 散文仍描述最初的抑制行为——已写入这 3 个 family 的 `remaining_boundaries`，不改写历史修复叙述。门现在 0 失败；新增 **6** 个测试（正例并钉住 ledger 自声明 gate kind、每类反例、默认 ledger 无悬空引用、畸形输入不抛异常、逃逸引用被拒、符号须整词），census **22 tests OK**、inner 门 focused-tests `closure complete: yes`、generate+verify passed（payload `c0d5e13a8c11ed67040330f56000697b1d3f9aff18f99942c7784f0c2a7f787d`）。
- P0.2 `sample → verdict` 关系 schema 迁移 + 引用门（2026-09-17 晚，本批）：`scene_sample_acceptance_ledger.py` 的裁决覆盖层原先只记 `verdict/note/reviewedOn`，缺路线 P0.2 要求的**观看者、关联 run/scenario 集、截图/视频身份、剩余差异、官方对照状态**。现按「旧 schema 可读、缺新字段则 unknown」迁移：`load_verdicts` 接受 `reviewer`/`runs`/`evidence`/`remainingDifferences`/`officialComparison` 五个可选字段，缺省归一为 `unknown`/空列表，**不改写人工裁决文件**（`scene_sample_acceptance_verdicts.json` 由观看者维护、agent 不动，并有专门测试守住「构建前后字节与 mtime 不变、条目键集合不变」）；类型错误（`officialComparison` 不在 `unknown/not-run/blocked/compared`、`runs`/`evidence` 非字符串列表、`reviewer`/`remainingDifferences` 非字符串）fail closed，**空白的 `reviewer`/`remainingDifferences` 是占位而非记录，归一为 `unknown`**，避免占位串把「缺观看者」缺口计数抹平。新增 `validate_verdict_references` 覆盖该关系的另一半，规则按「引用意图」而非书写形式划定：**相对引用视为仓库内**，逃逸仓库报 `verdict-evidence-reference-outside-repository`；**绝对引用落在仓库内同样受检**（不再因为以 `/` 开头就放行），落在仓库外（`/private/tmp`、`~`、`$HOME`）才是瞬态、不检查；**书写在仓库内、解析后落在仓库外**（`..` 或仓库内 symlink 指向仓库外）无论相对还是绝对写法都算逃逸。仓库内目标必须是**文件**，目录报 `verdict-evidence-not-a-file`（目录名不构成证据身份）；空串/纯锚点报 `verdict-evidence-reference-empty`。引用拼写只做**尾部**剥离：字面拼写 → 去尾部空白 → 反复去掉**已识别的**锚点（`#L12`/`#10` 形态）与**已识别的**校验注解（`sha256=`/`size=` 等，空格或 tab 分隔，可连续多段，有界 8 次），**绝不按首个空格截断、也不剥离无法识别的 `#...`**：含空格、`#`、`=` 的文件名不会被截断成另一个（可能存在的）路径，同前缀兄弟文件与其它拼写都不会替一个不存在的引用背书；前导空白不剥离，因此 ` lead.md` 不会借用 `lead.md` 的身份。`Path.resolve()` 的 `OSError`/`RuntimeError`/`ValueError` 按拼写不可解析跳过，存在性检查的 `OSError`（超长路径等）转 `verdict-evidence-reference-unresolvable` 失败记录而非崩栈；校验器对顶层容器、条目与引用都有形状守卫（`verdict-container-shape-invalid`/`verdict-entry-shape-invalid`/`verdict-evidence-reference-invalid`）且不抛异常，满足「畸形输入只产失败记录」的兄弟门契约。`build_ledger` 新增可选 `repository_root`，测试得以在临时仓库内自洽验证而不绑定真实 checkout 布局；失败消息带上失败总数（原先只印前 5 条、无处得知总量）。**未知样本 id 仍由 `build_ledger` 既有检查唯一持有，新门不重复持有该规则**。台账生成物新增「官方对照状态」小节与两条缺口计数，使缺口可见而非隐藏：当前 159 样本的官方对照状态**全为 `unknown`**（这是裁决条目自报字段的缺省归一，不是对 159 个样本目录是否自带作者对照材料的普查），19 条历史裁决**全部缺观看者与截图/视频身份**；生成页写明该计数只指**裁决自己引用的** run/截图身份，与 corpus 清单（`docs/scene/semantics/scene-corpus-capability-inventory.md`）的「人工对照」列（样本目录自带的用户截图与观察说明）不是同一事实。逐样本的观看者/run/截图身份/剩余差异只进 `--json` 载荷，页面不逐条渲染，`docs/scene/semantics/README.md` 的角色行按此收窄。补齐这些字段是 P4「全样本官方视觉复核」前的人工工作，不属 P0.4 的依赖汇总批次。本模块 9 tests OK（字段缺省/空白观看者与空白剩余差异/markdown 四行含 0 值/六类类型与引用错误 fail closed/仓库内绝对路径、含空格含 `#` 含 `=` 路径及其锚点注解写法/同前缀兄弟文件与无法识别的 `#` 后缀不背书/前导空白不借用身份/相对与绝对写法的 symlink 与 `..` 逃逸/瞬态外部路径与空引用/不改写人工文件/校验器畸形输入不抛异常）。
- P0.2 粗映射已落地（2026-09-17 凌晨）：新增 `script/scene_capability_family_map.json`（19 能力词表锚定台账 §3 行名 + 70 规则）；census 扩展结构门/锚点门/映射守恒；2507 family = 2500 mapped + 7 显式 unknown（1 effect/unresolved、6 project-property kind 空/unknown），多对多、快照内可反查（`families[].capability_refs`、`summary.capability_mapping`、inventory §4.1）。独立子代理审查两轮：P2×2（override 引用完整性）已修，P3×3（类型门/sorted key/测试唯一性）已按处方修正；16 tests OK。verify 最终态以本块记录时的最后一次 generate+verify 为准。
- P0.3 全量基线状态与身份边界（2026-09-17 晚，只整理既有证据、未新增运行）：全量 sweep**已存在**——`/private/tmp/mwx-fullset-baseline` 覆盖 159/159，**sweep 本身由单一二进制跑完**（CDHash `918475e7…`、executable SHA-256 `78bfa77e…`），运行窗 2026-09-17 05:41–08:18，`final-verdicts.json`（08:18）记录 **157 PASS / 2 FAIL**。**采集模式是混合的**：先 4 路并行 worker（记录 159 条、分片 56/35/34/34，`06:41:53 all workers complete`），再对 chunk 0 的 70 个样本单实例重跑（约 07:06–08:15）；**现存 159 份 report = 88 份并行段 + 69 份串行段 + 2 份单独调用**（08:18 `run-3738202317`、09:08 `run-3754630802`）。因此身份与结构/首断点结论可用，**并行段与并行遥测受限 report 的 performance/presentation 数字不可作性能基线**。两个 FAIL：`2959875782` 仅 4 条 accepted-layer 证据缺失（GPU completion / compositor consumption / next-frame / exact backend），阻塞原因是 layer 520 的 graph/gpu/compositor/next-frame 事件缺失，`admitted-fallback` 计数 11；`3754630802` 13 条（presentation stream 不匹配 + accepted-layer 4 条 + `successful transaction count below two` + `executor reported failures` + claimed/encoded/GPU-encoded 计数为 0 + observation diagnostic + utility capture below planned + non-black window missing）。归档已按这 159 个 report 重建：`script/scene_sample_debug_archive.json` SHA-256 `8215416a…`，状态 `127/30/2`，取代 2026-09-12 旧归档（仅 5 个 report、`112/44/3`）；**归档含两个执行身份**（158 个 `918475e7…` + 1 个 `6e870cf9…`），`run-3754630802` 在 09:08 被另一次调用以身份 `6e870cf9…`（executable `93e4c3bc…`）重建，且发生在 `d50610b0`、`ed216562`、`61a69b1a`、`09dfbe58` 之后，report `passed=true`——**归档对它的状态既不是 sweep 判定也不是当前 HEAD 判定，单样本结论以该样本自身 report 身份为准**；该边界与「`2959875782` 在归档里仍是 `blocked`」都已写进验收台账与调试台账。当前身份的部分复核：二进制构建/签名于 2026-09-17 21:46:53（构建时工作树 HEAD `dc147773`，Developer ID 签名），CDHash `a31bd68a…`、executable SHA-256 `34bdca21…`；复核会话在 HEAD `4cdda661` 下复用同一 CDHash（其间未重建），完成 **76/159、76 个 report 全 PASS** 后中止（现场无完成记录）。此前并行尝试在未覆盖样本上留下 45 份 stdout 日志（22:18:53 起的串行会话另写 77 份），未覆盖样本合计 46 份日志 = 20 PASS / 6 presentation-only FAIL（并行遥测限制）/ 10 空 / 10 traceback，另有 37 个样本无任何痕迹；这些日志不带 App 身份、对应 report 已不存在，不能当证据。与基线逐样本比对只有一处变化：`2959875782` matrix FAIL→PASS、当前身份 report 归一为 `degraded`（`admitted-fallback` 11→1、`secondaryEvents` 14→0），**但归档仍记 `blocked`、首断点仍是 `effect-admission / admitted-fallback`，不得写成断点已修复**；两次运行之间产品提交不止两个（`8cb2c343`、`a8fea925`、`07133cbf`、`aca7da40`、`cc69da74`），该变化不能唯一归因。**未完成边界**：P0.3「与当前 HEAD 同身份的全量基线」尚未完成，剩 83 个样本（含基线上 FAIL 的 `3754630802`）需在当前身份下定向补齐，**不整体重跑**（用户决定）。（证据包：[E-2026-09-17-P03-FULLSET-BASELINE-AND-SPLIT-IDENTITY](semantics/runtime-evidence-current.md#e-2026-09-17-p03-fullset-baseline-and-split-identity)）
- P0.3 当前身份（CDHash `a31bd68a…`）未覆盖清单（83 个，按驱动 chunk 顺序，供定向补齐；其余 76 个已有该身份的 PASS report）：`3470948192`、`3509243656`、`3609108600`、`3629927359`、`3690859128`、`3721456868`、`3747492842`、`3750813609`、`3763323436`、`3766387484`、`3767343314`、`3768724269`、`3769761761`、`3775373546`、`3780119725`、`3782740481`、`3787355076`、`3788645041`、`3789316755`、`3790956325`、`3792817546`、`3472940912`、`3554161528`、`3610154602`、`3655958892`、`3699213569`、`3738202317`、`3748311238`、`3754630802`、`3763428294`、`3766403294`、`3767460992`、`3768903841`、`3770444459`、`3777761326`、`3780391264`、`3784012236`、`3787382101`、`3788698200`、`3790631363`、`3791905266`、`3793328876`、`3477054430`、`3585875739`、`3612199597`、`3662790108`、`3703104370`、`3742133044`、`3749463715`、`3754639143`、`3765760121`、`3766415113`、`3768020435`、`3769364482`、`3770462923`、`3779026256`、`3780940857`、`3786185473`、`3788066613`、`3788734811`、`3790726145`、`3792249095`、`3793978239`、`3487629864`、`3601964477`、`3612795410`、`3665307769`、`3712499998`、`3743305891`、`3750342273`、`3757555836`、`3765904723`、`3767232084`、`3768229922`、`3769688830`、`3775355045`、`3779904456`、`3781307553`、`3786641495`、`3788467391`、`3788897599`、`3790806929`、`3792400801`。
- 更正（2026-09-17 晚，针对下方第 36 行的「两者现已分别由 … 定向复跑关闭」与第 41 行的「该样本已由 … 定向关闭，不再是现役开放首断点」）：这两处只对它们当时关闭的那条链成立——`2959875782` 关闭的是 visible effectful Puppet → typed geometry publication 链，`3754630802` 关闭的是 resident-target budget 链。按 2026-09-17 的 159 样本 sweep 与后续复核，`2959875782` 的**现役首断点**仍是 `effect-admission / EffectStageAdmission / admitted-fallback`（`admitted-fallback` 计数 11→1，当前身份 report 归一为 `degraded`，归档仍记 `blocked`）；`3754630802` 在 sweep 里 FAIL（13 条），其归档状态来自 09:08 另一次身份 `6e870cf9…` 的重跑（该次发生在 `d50610b0`/`ed216562`/`61a69b1a`/`09dfbe58` 之后，report `passed=true`），**既不是 sweep 判定也不是当前 HEAD 判定**。因此这两个样本都不能读成「已无开放首断点」，`3754630802` 还需在当前身份下定向复核。
- P0.2 引用门遗留（2026-09-17 记录，均在已提交的 `97a179a9`，属 P3、未修、需要新开小批次）：①`#` 开头的文件名直接书写会被判 `verdict-evidence-reference-empty`（可用 `./#name` 绕过）；②已识别的校验注解后缀（`sha256=`/`size=` 等）在「同前缀文件恰好存在」时会替一个不存在的引用背书（属注解语义，需在门文档写明或在解析时要求注解前必须有空格分隔的真实路径）；③`_MAX_SUFFIX_STRIPS = 8`，9 段以上连续注解会被判 `verdict-evidence-file-missing`；④用「另一种仓库根拼写」（例如另一个指向同一仓库的 symlink）书写的绝对 `..` 逃逸会被当作仓库外瞬态放行；⑤引用解析用 `os.path.expandvars`，结果依赖进程环境变量（建议只保留 `~` 与 `$HOME`）。这些缺口都在引用门的作用域内，当前 corpus 的裁决覆盖层还没有 `runs`/`evidence` 引用，故线上影响为零。
- 门禁口径提醒（2026-09-17）：`--phase inner` 会按 `INNER_HEAVY_MODULES` 跳过 `test_scene_semantics_coverage`（本批文档改动因此不会被 Markdown 链接检查覆盖），本批已显式补跑该模块（Markdown 链接全部存在）；后续任何新增/改写文档链接的批次都要显式跑它。另外验收台账页↔归档/裁决的重新生成比对已由 `test_scene_sample_acceptance_ledger.test_committed_ledger_page_is_regenerable_from_its_inputs` 覆盖，但调试台账的归档 SHA 与计数仍只靠人工核对，没有自动门。
- P0.3a 采集校准（DEBUG 隔离路径，App 身份 = `.codex/DerivedData` 2026-09-17 00:17 arm64 Debug ad-hoc 签名，隔离副本 `/private/tmp/mwx-p03-calibration`）：三代表 `3766415113`（纯静态）/`3767343314`（多 pass+feedback）/`2067939514`（脚本动态，45 层 72 效果）均真实运行，submitted/completed 全量 0 failed，截图/执行观测/route 证据可采。**采集缺口（阻塞，非样本失败）**：本机当前会话环境下 `presented=0 / presentStreams=0`（CAMetalLayer presentedHandler 未带有效 presentedTime），performance presentation 门三样本同因 FAIL；需在显示器活动会话复采后才能区分环境与产品缺口。首轮 CPU 分位数受并行构建/审查负载污染，不得当基线。
- 三元 stage-link 断点已修复（2026-09-17 本批，owner `SceneGenericShaderTernaryScalarConditionNormalizer.swift`）：`3767343314` layer 17 `ui_editor_properties_opacity_mask` 材质 shader 的 stage-link 拒绝（`author.frag:59: '' : boolean expression expected`）消失，复跑 `stage-link:rejected` 0 处、shader request `published/accepted`；benchmark PASS failures=[]，3060 callbacks / 3059 completed / 0 failed，60.0 FPS，presented=3058——前次记录的 `presented=0 / presentStreams=0` 为瞬时环境态，本轮同环境实测正常，非持续阻塞，修正此前记录。证据：`docs/scene/evidence/20260917-ternary-scalar-condition/`。**无迁移断点（修正此前两次误判）**：复跑日志中的 `launch-program-failure slot=1 material=mask` 是 launch 时 slot 未就绪→被 `presenceIndependentDefault`（作者声明 `default:util/white`）救援的**前置 trace**，非失败——graph-execution 证据显示 `17#effect#32`（即带 mask 的 sharpen）自 frame 0 起每帧 `program=5bad29f6… outcome=succeeded gpuCompletion=completed`，`17#effect#38/#234` 同样全 succeeded，publication/compositor 消费正常。该样本无残留断点。诊断改进候选（P3，另批）：该 NSLog 名为 "failure" 但在救援成功时也打印，误导归因，建议改名或补充 disposition 字段。
- P0.4 回写与影响面（2026-09-17 凌晨）：三元修复事件已按 schema 追加至 repair ledger `shader/frag@436c998ccdbec6c2`（state=implemented、commit=uncommitted-working-tree、证据指向 evidence 目录与 fixed 复跑报告）；family 的 corpus 共享集合 = {3290491250, 3767343314, 3789316755}。全 corpus 形状扫描（只读，2242 个 shader 文本，1415 个 `.dxs` 二进制正确排除）：**精确形状（整条件裸字面量）除 3767343314 外为 0**；26 处粗命中全部是合法比较条件（`< 0.5 ?` 等），非本缺口。修复的静态受益集合 = {3767343314}（已恢复）。census 重新 generate+verify passed（payload `7afb971b…`）。
- **全集 identity-only 基线完成（2026-09-17 06:43，单二进制干净重跑，4 路并行 62 分钟）**：159/159 report，0 超时 0 崩溃。verdict 原始值 87 PASS / 72 FAIL；70 个 FAIL 的主因是显示器锁屏/非活跃，后续串行重扫关闭了其中的 presentation 环境噪声。当时稳定重现的产品失败只有 `2959875782` 和 `3754630802`；两者现已分别由 GeometryProduct typed named-provider publication 和 resident-target budget 定向复跑关闭。**这是“旧全集基线 + 两个 fresh 定向关闭”，不是当前未提交候选的 fresh 159/159 全集重跑，因此不作 159/159 现役候选声明。**基线数据：`/private/tmp/mwx-fullset-baseline/`。
  - `2959875782` 定向关闭：真实首断点为 visible effectful Puppet 1413 → hidden Puppet graph provider 134 → four-slot composition aggregate 520 缺少 typed geometry publication。现役公共链在计划阶段接纳 placement-exact `.geometryLayer`，把 atlas 或 graph-final 经原 GeometryProduct 栅格化为 authored-local named publication；作者 `2667×1500` 尺寸等比受限为 `2048×1152`，但投影仍使用作者坐标。最终 Developer ID Debug 定向报告 `PASS / failures=[]`，GraphExecutor `838/838/838`、0 failure、0 fallback，layer 134/520 named binding、layer 520 utility capture、layer 130/1413 visible geometry publication 和 exact backend 链闭合。完整证据与限制见 [E-2026-09-17-GEOMETRY-NAMED-PUBLICATION](semantics/runtime-evidence-current.md#e-2026-09-17-geometry-named-publication)。
  - `3754630802` 由 commit `8cb2c343` 修复：必需 target `411,292,672 B` 高于旧预算 `397,284,864 B`，每 surface 自动份额由 `/32` 改为 `/16`。最终 fresh 串行复跑 `PASS / failures=[]`，target pool 峰值 `484,508,416 B` 位于 `794,569,728 B` 预算内；低内存、多 surface 聚合与长稳仍未验证。完整身份与边界见 [E-2026-09-17-RESIDENT-TARGET-BUDGET](semantics/runtime-evidence-current.md#e-2026-09-17-resident-target-budget)。
  - **首断点 No.1 已修复（2026-09-17 04:2x 批次，复审通过）**：model-backed（Puppet）image layer 的 layer-source launch 未就绪，字节级根因 = `3767232084` 的 `models/e00207ce…_puppet.mdl`（MDLV0023，13361B，无 MDLS）使用 **48 字节顶点 stride**（offset=106：vb=10752=224×48，索引 1242 个 min=0/max=223 全覆盖、224 顶点全部有限、位置域恰为层声明 1920×1080 之半、UV∈[-0.004,1.005]），而 `SceneMdlPuppetMeshReader` 的 MDLV0023 stride 表只有 [80,84] → 全覆盖检查（maxIdx==vb/stride−1）永不成立 → `meshBlockNotFound` fail closed → 整层永久 previous-current。修复 = stride 表加 48（真实资产证明路径，覆盖检查天然消歧；复审确认对既有 80/84 资产零回归）。**受益集合实测 = {3767232084}（PASS failures=[]，claimed/encoded/gpuEncoded 从全 0 恢复 + 14 次成功图执行）**。曾误判为受益者的 `3743305891` 实际**不含任何 .mdl**，其 layer 23 是 mp4 payload 视频源的 frame-0 瞬态 not-ready（后续帧已恢复、compositor 消费成功），与 stride 修复无关。该时点的剩余列表随后已由全集串行复扫、恢复感知门和 resident-budget 修复重新裁决；当前只按上方现役残留 `2959875782` 排序。遗留 P3：`test_scene_puppet_mesh.py` 补 stride-48 合成 fixture（已补，20 tests OK）；frame-0 瞬态 not-ready 是否应豁免 observation 门属 benchmark 期待问题，待裁决。
  - benchmark 观测门已恢复感知（2026-09-17 05:3x 批次，复审通过，`scene_wallpaper_graph_output_metrics.py`）：原"任何 layer-local-fallback 诊断 → FAIL"改为按既有合同分类——`layer-source-not-ready` 瞬态若每个引用层都在严格更晚帧取得 next-frame trigger 的 succeeded+compositorConsumed 成功，计为 recovered 数据字段不再失败；未恢复/其他诊断照旧 FAIL（不放松）。验证：3585875739、3743305891（mp4 视频层 frame-0 瞬态）复跑 PASS failures=[]；`3754630802` 在预算修复前仍 FAIL，现已由上方最终候选复跑关闭。新分支入仓单测已补并纳入正式聚合；六个视频瞬态样本复跑全部 PASS。
  - 更正：上条 stride-48 记录中“当前只按 `2959875782` 排序”只是当时时点语句；该样本已由本页上方 GeometryProduct publication 批次定向关闭，不再是现役开放首断点。
  - `3780119725` 的 actual-present intervals 仍不可用，保持为采集边界，不与已关闭的 `2959875782` 产品链合并。
  - typed publication outcome 批次（2026-09-17 傍晚，独立复审驱动，已提交 a8fea925/07133cbf）：`SceneDependencyFrameRuntime` 的 provider publication 返回值由 `Bool?` 改为 `SceneGraphOutputPublicationResult`（`published / unavailable / invalid`）。普通颜色 product 编码/registry 暂不可用只登记 typed reason 并保留主 compositor 输出，typed `.data` 缺 publication 与任何 reservation/epoch/object/registry 漂移以 `published: false` fail closed；geometry pose-not-ready/encode 失败为 ordinary `unavailable`，source/generation/placement/registry 漂移为 `invalid`；coordinator 只允许 exact `named-provider-publication-unavailable` 进入 local discard（其余返回 `named-publication-local-reason-rejected`）。**中间候选曾把 graph-output provider 的 raw-capture 路由不适用误判为 `invalid`**，导致每帧在 `graph-provider-raw-capture-invalid` 处停止 layer loop（报告 `/private/tmp/mwx-2959875782-typed-outcome-20260917-1759/report.json`，15 项失败、无 publication/截图）；该类已按 ordinary `unavailable` 归类，caller 继续 previous-current。同批把 `SceneDependencyFrameRuntime.swift` 的 consumer-side input resolution 拆入新 owner extension `SceneDependencyFrameRuntime+EffectInputResolution.swift`，主文件 845→659 行，不再越过 800 行硬限；source layout 与 harness 源清单同步登记。最终签名 Debug 候选（CDHash `968bef713ed64cfeba619eb82a2a4218ef1e5f52`）fresh 定向复跑 `PASS / failures=[]`（433/432/0、432 presented、executor 634/634/634、0 failure/0 fallback、exact backend 37/37、publication failed 集合全空），inner focused-tests 73 模块 ALL OK、checkpoint `BUILD SUCCEEDED`；全仓 code-health 仍只报本批外 ratchet 漂移与既存超限文件。证据见 [E-2026-09-17-GEOMETRY-NAMED-PUBLICATION](semantics/runtime-evidence-current.md#e-2026-09-17-geometry-named-publication)。 **复审 follow-up 已闭合（2026-09-17 晚，未跑新真实运行）**：源 extent 大于 named target 的 publication 以 `named-provider-publication-target-extent-invalid` fail closed（二轮复审确认为 defense-in-depth 不变量、当前不可达，非已复现的越界）；`SceneUtilityPlanFrameRenderer` 迁移到 typed aggregate resolution（`.invalid` fail closed 并传播 typed stop，ordinary miss 保持局部）；两个 raw-capture 调用点消费 `.invalid`；prepass route operation 改按 contentKind 归因；local discard 保留具体 ordinary reason；消融两个已无消费者的 legacy `aggregateEffectInputs` overload。门禁：checkpoint Debug build `BUILD SUCCEEDED`（首轮 unittest 对这些消费点只有源码字符串断言、无编译证据，二轮 P1 已以构建补齐）、定向 unittest 28 / 82 tests OK、census `generate`+`verify` passed（payload `a6db048c…`）。**未闭合偏差债务**（各需自己的正反运行证据，不得当已修）：`captureForwardGraphSourceFallback` 与 `layerSourceGraphFallbackPublisher` 仍按 `== .published` 布尔粗分类，ordinary `*-unavailable` 会在该路径停帧；`named-provider-publication-target-extent-invalid` 命中即整帧 previous-current，半径大于最小 unsafe unit；本批自身的 repair-ledger 事件待下一次 census bookkeeping 追加。
  - text host 合同缺口修复（2026-09-17 晚）：对象 text 原先不是共享 wrapper/host 合同的 host kind，导致「脚本 + scriptproperties + 用户属性」四键包装的整条脚本没有 owner，且 `text/scriptproperties/<name>` 下的用户属性绑定全部落入 unsupported。修复把 `objectText` 加入 `SceneScriptDynamicProviderHostContract`（准入集合 = 原三条键集 + 四键集），并让字符串投影与 `sceneScriptHostKind` 都走该共享合同，没有新增 registry/owner/route。真实 `3747492842` 修复前后对照（修复前 CDHash `968bef71…`、修复后签名 Debug 候选 CDHash `ab539e4a5db944a99235624aa1d3012c4a11ed8d`）：`propertyBindingProgram` 的 `scriptInstanceProperty` 9→12（新增 layer 59/191/264 的 `text/scriptproperties/allowTilt`），`phase=owner-effects … totalLayers` 与 `phase=owner-effects-admission … admittedOwners` 6→7，layer 191 的 text content 回调首次出现；两次运行都严格 `1/1 PASS / failures=[]`。只读全 corpus 扫描（159 样本）：四键 text 包装 1 处，`text/scriptproperties/<name>` 用户属性引用 189 处 / 40 样本 / 39 个属性名。inner focused-tests 24 模块 ALL OK、checkpoint `BUILD SUCCEEDED`。该批没有可见像素断言（脚本返回值未变、angles 写入不体现为独立 layer mutation，且 after 证据窗口无指针事件），下一轮可证伪门是让指针离中心并对 `.layer(191, .angles)` 断言。证据见 [E-2026-09-17-TEXT-HOST-CONTRACT](semantics/runtime-evidence-current.md#e-2026-09-17-text-host-contract)。
  - 数据：`/private/tmp/mwx-fullset-baseline/`（final-verdicts.json + run-*/report.json + worker logs）；analysis：`/private/tmp/mwx-fullset-summarize.py`。并行经验：4 路并发 Metal 实例 55 分钟跑完全集，但 presentation 遥测仅前台窗口有效——身份/route 数据不受影响，presentation 门必须串行。
- P0.3b 产品路径采集（情报已齐，待选型）：普通链路为 UI 设为壁纸 → SteamWorkshopService.setAsWallpaper → SceneDaemonClient.requestLaunch → daemon 子进程。现有 typed 命令**无截图/证据通道**；`--mwx-debug-scene-evidence-dir` 仅 DEBUG 透传且会切换 floating 证据窗（改变产品行为，验收语义失效）；`MWX LAUNCH-STAGE`/route NSLog 在 Host 公共代码无条件输出（普通路径可用）。最小缺口 = 普通路径截图 + 身份/事件落盘 + 批量编排。方案：(a) UI 自动化驱动真实 App（零产品风险、取证弱）；(b) client/daemon 增加显式可取消 evidence 命令（中大型，须保持"诊断非播放前置"边界）；(c) 拆分 DEBUG 帧捕获与窗口行为解耦开关（中小，依赖 DEBUG 构建）。建议：identity-only 批量先用现有 benchmark 通道（已验证可行）；普通路径截图捕获按 (b) 立项，须独立设计审查。并行跑批经验：4 路并发 Metal 实例 55 分钟完成全集，但 presentation 遥测仅前台窗口有效——身份/route 数据不受影响，视觉/presentation 门必须串行。

### Q1 — 作者参数、视觉验收与 tracked matrix

| 复现索引（非修复优先级） | 尚未关闭的问题 | 下一次操作与关闭条件 |
|---|---|---|
| 1 | `3747492842`：**两个报告症状都已归因，均非引擎缺陷**——「文字裁切」是参考图视口不可控，「多出缩小版 Leon」是用户 Picture 属性状态差异；「额外闪烁」仍未归因 | 文字裁切：用户截图第 0..65 行纯黑 → 墙纸区域 3024×1898、cover 尺度 0.878704、水平裁切 175.1px，而证据窗口 3024×1964 / 0.909259 / 233.78px（相差 58.7px）；作者 layer 152 左端在我方落 −12.9（被裁）、参考图 +38.4（可见）。该参考图登记 `reference-uncontrolled`，不作绝对位置 oracle；两模型只在尺度上一致（0.878704 vs 0.878799，差 0.01%），平移差约 8px，故绝对落点只到 ±10px 量级。小 Leon：作者 `preview.gif` 含它，它由 **layer 186**（「Picture Import」槽位，`visible.user=visible` 默认 true）绘制；受控运行 `visible=false` 把 ROI（屏幕 (330,820)-(950,1330)）亮像素从 `1.879%` 打到 **`0.000%`** 并目视确认整块消失，与用户关闭或替换 Picture 一致，属属性状态差异。**同批撤回**：早先版本据同一 ROI 断言「嵌套复合层 `visible=false` 不抑制其子树」，该断言已证伪——该 ROI 落在 186 的包围盒（x 473..1451、y 627..1181）内，测的是 Picture 而非 273；隔离 186 后翻转 `leonmovement`，全幅差异 `>60` 为 0.207% / 0.224%，而两次同配置重复的噪声地板为 0.019%，即 **273 的可见性正常**；帧内 DEBUG 观测也显示 `frameVisibleLayerIDs` 确实排除 273 及其全部 7 个子层（layers=21 visible=13）。相关 corpus footprint 数字已作废。关闭条件：本条不再有产品缺陷待修；「额外闪烁」需固定 phase 的动态对照才可裁决。见 [E-2026-09-17-REFERENCE-VIEWPORT-UNCONTROLLED](semantics/runtime-evidence-current.md#e-2026-09-17-reference-viewport-uncontrolled) |
| 2 | `1315486372` 水波纹位置异常 | 先复现与归因，再修公共坐标/采样 owner；不从症状直接指定实现 |
| 3 | `2684431262` 紫色块 | 先复现与归因，验证 source→effect→output；不得按样本特判 |
| 4 | Scene Bloom enable/threshold 尚无完整 live consumer | 沿属性 producer→typed channel→全场 post consumer 闭合；若仍 unsupported 则保留待解决边界，不据此关闭全集目标 |
| 5 | 全 corpus identity-only matrix 与人工视觉复核 | 按路线 P0/P4 维护；人工重新观看后才改 verdict；每批先定向，公共能力关闭复测全部影响集合，最终候选全集复测 |

已关闭的 Puppet、双视频、Water Waves mask、TextureAnimation 等断点不再占队列行；新回归必须建立当前复现后重新入队。其余 authored target 按实际 consumer 缺口拉入，不再把已接通的整个 script/camera/particle family 标为缺失。

### Q2 — 稳定帧性能、长稳与发布

性能与资源批次排在架构完整性和真实样本画面正确性之后；只有崩溃、OOM、预算硬拒绝或已测明显阻塞正确运行时才提前。其余按重构计划 E0 基线与测量进入 E1/E3/E4，P5 只拥有最终验收。维持作者分辨率和正确构图，不能恢复缩小纹理等错误行为换取帧率。

## 3. 观察项与能力边界

异步 provider 的短暂 not-ready 按局部 previous-current 后自然恢复；持续不恢复才登记缺口。Puppet 跨层 geometry provider、IK、完整 3D 等能力按专项合同和真实需求拉入，不恢复压平纹理捷径。

## 4. B1–B9 退役索引

已完成批次的证据只按需从[历史索引](../history/README.md)追溯，不再作为当前任务。这里不复制 PASS 数、旧命令或退役 owner 清单。

## 5. 队列维护与批次门

每项只保留问题、下一操作和关闭条件；完成后从表中移除，证据写回唯一台账。落代码遵守[开发工作流](development/development-workflow.md)，一次闭合一个完整职责并完成相称验证；提交仍需用户授权。

当公共首断点关闭、仅剩路线系统性验收时，将本文归档，取消派生队列。
