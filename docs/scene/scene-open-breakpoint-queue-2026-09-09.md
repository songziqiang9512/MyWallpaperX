<!-- document-role: active-plan -->

# Scene 当前断点修复队列

> 核对：2026-09-16，依据全量工具/台账审计调整入口，未重跑样本。文件名日期仅保留链接身份。
> 本文是[兼容路线](scene-compatibility-roadmap.md)的短队列，不建立第二套阶段；工程性能工作按[重构计划](engine-refactor-program.md)执行。

## 1. 当前证据边界

样本身份只用于复现，不能进入产品分派。旧 corpus 计数和人工 verdict 不代表当前 HEAD 已重新验证；技术修复不能自动改写人工验收。当前能力查[能力台账](semantics/coverage-ledger.md)，运行身份查[运行证据](semantics/runtime-evidence-current.md)。

## 2. 现役执行顺序

### Q0 — 先完成兼容路线 P0 的全量关联与采集校准

按[兼容路线 §3.2](scene-compatibility-roadmap.md#32-p0-的四个有界批次)依次交付：盘点/fingerprint/守恒校验 → 既有表的声明/公共合同/代码 owner/场景/证据关联 → 普通产品启动采集校准与 fresh 全集基线 → 共享依赖及首批修复集合。工具扩展是待执行工作，不因本次计划修改视为完成。

每一步只改其完整职责；Q1 保留已有反馈作为复现候选，实际修复顺序由 fresh 首断点、公共依赖和影响面确定，不继续按旧报告的集群数字或以下样本顺序硬排。紧急可见回归可提前修复。

当前用户仅有现成截图或视频，先登记可用参考与控制变量缺口。固定官方客户端同输入动态对照是 P4 关闭依赖；缺环境不阻塞公共实现，但不能记官方一致性通过。

P0 续跑状态（2026-09-22 更新，事实覆盖至 `8f4dc46d`；历史过程记录查 git log 与运行证据页的 P03 证据包，不再在本块累积流水账）：

- **2026-09-22 SteamKit 安装身份批已收口**：current HEAD 的隔离 Debug 构建和 helper 直接匿名趋势查询均通过，但在 `env -u MWX_STEAM_HELPER_COMMAND` 的 bundled route 下，本机 `/Applications/MyWallpaperX.app` 仍为 2.0.8 (269)，`Contents/Resources/SteamService` 缺失，该安装 identity 会在 `executableMissing/notReady` 首断点停止。现役源码的 SteamKit 查询、helper locator、QR 入口和公开 staging→Video/Web/Scene owner 已由历史实现批覆盖，本批没有可安全归因的产品代码断点，未覆盖安装包；用户报告本身因当时没有冻结 bundle identity，不能由此单独归因。证据与命令见本机忽略 `docs/scene/evidence/2026-09-22-steam-installed-bundle-recheck/manifest.md` 和 [SteamKit 专项计划 §9.1](scene-steamkit-migration-plan.md#91-当前-helper-与产品-接缝观测-2026-09-21)。下一动作是用户授权后安装签名 2.0.9 (277) 候选，冻结普通 App child helper identity，再执行真实 QR／授权下载／三引擎播放门；不把匿名 helper 结果冒充账号或下载通过。

- **现役 Goal（2026-09-18 用户授权设定）**：遵照现役 Scene 兼容执行路线，从 P0.3 收尾态持续推进——修复剩余 blocked 样本（现役唯一 `3792249095` terminal divergence；已于本批按 reference-uncontrolled 豁免，见开放项 2）与 `2959875782` 残留的 `1315#effect#1326` effect-local-passthrough 降级链、按 P0.4 依赖排序进入 P1 公共能力修复主循环、P2 逐样本剩余缺口、P3 参数与条件闭环；每批改动经独立子代理审查，保持唯一运行主链并持续消融相关冗余。遇到外部依赖记录阻塞并继续不依赖它的工作；只有真实满足整体完成条件才标记完成。提交已获用户授权（单职责分批、不推送）。

- **当前身份覆盖只覆盖语料的一部分（2026-09-23 更正分母）**：语料根现有 **172** 个样本目录 = 159 个 legacy bare-id + **13 个 `<workshopId>-<uuid>` v2 布局**（App 自身的安装布局，`SteamWorkshopLibraryTransaction` 写入）。159 个有当前身份（CDHash `a31bd68a…`、executable `34bdca21…`）的 fresh 运行证据（2026-09-17 的 76 个 + 2026-09-18 串行补齐的 83 个，全部 PASS），**13 个 v2 布局样本既无运行证据也无身份证据**；identity-only PASS ≠ 视觉验收完成。归档 `script/scene_sample_debug_archive.json`（SHA-256 `49c14bc7…`）状态 `128 structural / 31 degraded / 0 blocked`，其行集仍是 159——该归档的 `--report` 输入（`/private/tmp/mwx-fullset-baseline` 的 159 份 report，2026-09-17 身份）已随临时目录清理消失，**本批无法重建**，重建条件=全样本重跑（见下一公共首断点）。验收台账已按 172 行重生成。
- **已落码修复（全部已提交、各含独立审查与真实样本验证）**：
  - `SceneGenericShaderTernaryScalarConditionNormalizer`：WE 方言数值三元条件 stage-link 拒绝 → 可编译（`3767343314` 恢复）。
  - `SceneMdlPuppetMeshReader` stride 表 +48：Puppet 网格解析失败 → 角色恢复（`3767232084` 恢复）。
  - resident-target 预算公式 `/32→/16`、顶 512MB→1.5GiB（`a8fea925` 抽为 policy owner）：重型 4K 场景整帧硬拒 → 正常渲染（`3754630802` 恢复）。
  - typed geometry named-provider publication（`07133cbf`/`aca7da40`）：`2959875782` 的 publication 链闭合（定向 PASS）。
  - text host 合同补 objectText（`cc69da74`）：`3747492842` 属性绑定 9→12。
  - benchmark 观测门恢复感知（`61a69b1a`）：frame-0 layer-source 瞬态+已恢复不再误判 FAIL。
- **现役开放项（按序，各含下一步）**：
  1. `2959875782` **观测缺口已闭合（2026-09-18）**：归档 blocked 的根因是基线跑用了**旧二进制 `918475e7…`（不含 typed geometry publication 修复 `07133cbf`）**——缺观测的 4 层（19/134/138/520）正是该缺口依赖链；当前身份 `a31bd68a…` fresh 运行 47 层全观测、PASS failures=[]，归档状态随之 blocked→degraded-runtime。**现役残留（降级）**：`1315#effect#1326` 仍走 effect-local-passthrough（radius=effect、severity=degraded），是新的更小待修缺口（P2 逐样本层）。probe（2026-09-18 首轮）=已定位为 layer 1315 的 X-Ray 效果（effects/xray/effect.json），该层为用户属性隐藏层（dynamic layer visibility: value=false effective=false），fresh 运行的 admission records 恰 1 条 admitted-fallback；app.log 无该效果的显式失败诊断（sticky 日志未捕获）。归因已闭合（2026-09-18 02:5x）:该效果是**用户属性门控的条件内容**——作者声明 `visible={user:{condition:"0", name:"xraylayer"}}`（默认关、用户可切换），其依赖引用（前一效果 1324 的输出）在依赖所有权编译器中无 binding 合同 → `dependency-stage-reference-unsupported` → effect-local-passthrough 保留 previous-current。按路线图 §2.3/§3.3 属条件内容缺口（P3 范围）：用户切换 xraylayer 后该效果不会执行。**probe 完成（2026-09-19）**：`unsupportedReferenceEffectKeys` 判定链已读通——引用经 `ownerRequiringReferences` 过滤后**未被 shadow 消化**（`isShadowedInputProvenance` 拒绝：该具名引用不是 1326 的链输入 provenance），且 1326 满足 `exactSinglePassPassthroughEffectKey`（单 pass、visible、单节点）→ 进入 unsupported 降级保留 previous-current。**根因定性**：X-Ray shader 对 1324 输出做**辅助采样**（显式纹理槽，非链输入）——同层前一阶段输出在执行器 prepare 的 `publications` 映射中已存在，但当前无"同层前阶段具名引用从阶段序 publication 解析"的通路（跨层引用走 reservation，同层具名采样无合同）。**修复方向（D3 级执行器能力，需独立 fixture+门）**：prepare 的 material intent 对同层前阶段 output identity 的具名绑定，从 `publications[identity]` 解析（替代 unsupported 降级）。**P3 关闭门备选**：以固定 toggle（xraylayer=true）的条件场景验收按 previous-current 保留的现状（登记为该条件内容的当前执行边界）。**下一切片决定**：执行器能力实现 vs P3 登记验收。 

**XRPROBE 实测定案（2026-09-19，探针已移除）**：真实样本插桩推翻了"前一效果 1324 输出"的原归因——引用实为**跨层**：`provider=813`（层 813：visible=False 的隐藏 image 层，含 blend×3+shake×2 共 5 效果的链），slot=1315#effect#1326 pass0/slot1（X-Ray 效果本身 effect.visible=true；隐藏的是层）。**根因（条件提供者链激活缺口）**：813 的输出是其 5 效果链的合成物，作为 provider 需要 graph-output-provider 准入；但 813 隐藏且无激活通路 → 从未准入/准备 → binding nil → 1326 降级 previous-current。**这是新能力切片（conditional provider chain activation）**：消费层（1315）与提供层（813）均为条件内容，需随消费者可见性一起激活提供链——触及准入乐观可见（批 G 机制可复用于 provider）与 graph-output-provider 按需准入，需独立设计审查。**P3 备选**：按 previous-current 保留现状登记为该条件内容的执行边界（用户切 X-ray 时无效果，与默认关的作者意图一致度待官方对照）。探针代码已移除。拒绝层级在**准入分类**而非执行器——`unavailableDependencyStageReasons` 在 admission compile 期标记（per-layer 闭包调用 `unsupportedReferenceEffectKeys`），被标记 stage 在 `+ProgramFirstStages.swift:48-66` **先于 compileStages** 降级为 visualFailurePassthrough——stage 从未编译、无 resolved product，执行器 publications 通路无从参与。**修正修复层级**：解法在 ownership 编译器——同层 effect-to-effect 具名引用（consumer==provider==layer）当前编译无合同。**候选修复（需独立审查）**：编译为 `.graphInternal` 支持形态（finalize 已接受同层自引用 deps），stage 保持 resolved、执行器经既有 publications/pair 解析。**前置核实**：`compile` 对同层具名引用返回 nil 的原因，及 1324→1326 是否紧邻链。 **实现批进展（同日，carve-out 可达种子已落地）**：`reachableConsumerLayerIDs` 种子扩展（声明依赖引用的隐藏消费层入可达集）后，2959875782 回放实证：`dependency-stage-reference-unsupported` 归零、binding 编译成功、1315 获得动态可见路由（frame=0 userProperty 行）且 X-Ray（1326）入计划为 resolved 阶段、167/166/0 帧零失败。**剩余精确缺口**：运行时 1326 仍经 `visual-failure-passthrough` 降级（admitted-fallback=1，`utilityDependencyIssue: kind=forwardUtilityProvider layer=1315 provider=813`）——隐藏效果链 813 作为 **forward-utility provider** 的准入/发布仍未闭合（其 graph output publication 不可用）。**下一切片**：forward-utility provider 对隐藏效果链的准入与按需发布（813 的 5 效果链 graph 执行 → target 发布 → 1326 采样），需独立设计审查（涉及隐藏层执行成本与激活门控的取舍）。 **WIP（2026-09-19 深夜，工作树含未完成实现——下会话第一动作）**：`.resolvedMaterial` 的 providerKindIsSupported 已扩展（隐藏 image provider + 可见效果链 + 无依赖/无 puppet/utility）+ visibilityShape 同步扩展，**但回放显示 binding 仍未编译**（utilityDependencyIssue forwardUtilityProvider 1315←813 依旧、admitted-fallback=1、XGATE 日志消失=流程在第二门前更早退出）。**下会话第一动作**：给 executableBinding 的早期退出点（尤其 :524 order 门与后续 return nil）加插桩，定位扩展后仍失败的精确检查；移除全部临时插桩后提交。 **已完成（2026-09-19 续，定案提交）**：早期退出插桩定位到**最后一个阻塞子条件 = `puppetMeshPath != nil`**——813 是 Puppet 层（模型 rboob_puppet.mdl）。移除该限制后回放（12s）：**admitted-fallback=0**（X-Ray 的 effect-local-passthrough 降级完全消失）、utilityDependencyIssue=0、167/166/0 帧零失败、零新增拒绝码。**剩余验证**：xraylayer toggle 场景的 1326 执行证据（P3 关闭门，需 runner 用户属性切换）；1315 基线隐藏时 813 链不执行（正确条件语义，无空转成本）。 **toggle 场景取证进展（2026-09-19）**：runner 注入路径确认 = `--mwx-debug-scene-live-properties-json '{"xraylayer":true}'`；回放日志抓到 **`phase=live-property-update accepted=false keys=xraylayer`**——注入被 `applyUserPropertyValues`（SceneDesktopWallpaperHost.swift:230-249）拒绝。候选拒绝分支：①`recordID` 不匹配（runner 调用传的 recordID vs context.recordID）；②`liveState.apply` 的 replacements 校验拒绝（xraylayer 键不在有效 userProperty 集？）；③`soundPlaybackRegistry.canApply`。**下轮**：读 runner 调用点的 recordID 传参与 liveState.apply 对未知键的行为，定位后修复注入（或 runner 侧传参错误），重跑 toggle 场景拿 1326 执行证据。 **注入路径定案（2026-09-19 续）**：三个候选分支逐一排除/定案——①recordID 一致（双方均为 "debug-scene-playback"）；②`liveState.apply` 的拒绝是**合同正确行为**：`xraylayer` ∈ `rebuildRequiredPropertyKeys`（toggle 需重建拓扑），live 路径按守卫拒绝；③布尔类型不匹配也是事实（作者值是字符串 "0"，须注入字符串）。**正确注入路径 = 启动时覆盖** `--mwx-debug-scene-properties-json '{"xraylayer":"1"}'`。**启动覆盖回放结果**：effectiveValues 已含 xraylayer="1"（字符串生效），但 **1315 仍 visible=false / effective=false**——最后未闭合环节 = **用户条件求值消费**：作者声明 `visible={user:{condition:"0", name:"xraylayer"}}`，条件语义（visible 当 xraylayer=="0"？还是用户 UI 布尔 checkbox 把 "0"/"1" 当 false/true？）与 1315 的 prepared visible 之间的映射需读条件求值 owner（`SceneUserPropertyBindings` 的 user-condition 求值）。**下批第一动作**：读该求值逻辑对字符串 "1" 的解释，修正条件消费或确认作者语义（condition:"0" 或为 UI combo 默认索引），随后 toggle 场景闭环。**边界**：Puppet provider 的 graph 输出服务是新增支持形态，不宣称官方 parity；整帧对照未做单层 ROI。回滚点 = 0d426477。**P3 备选维持**：按 previous-current 保留现状登记为条件内容执行边界。 **outfit toggle 定案与进行中修复（2026-09-19 深夜，未提交——用户纪律：代码完成前不提交）**：1315 的可见条件实为 **outfit 组合 "4"**（队列原记 xraylayer 是误归因；evaluate 语义 visible=(值==条件)，`ScenePropertyBindingProgram.swift:81-85`）。outfit="4" 回放：1315 effective=true、813 提供链执行，但同组的 **1299**（X-ray 版 Puppet，deps=[130]，130=同模型正常版、outfit "0" 下隐藏）逐帧 `frame-preparation-request-invalid` → 全帧停。**修复进行中（工作树含未完成改动，回滚即 `git checkout SceneDependencyFrameRuntime+Geometry.swift`）**：`+Geometry.swift` captureExtent 的 `.resolvedMaterial` 分支已加隐藏 Puppet 提供者 extent 路径（无 utility + puppetMesh 非空 → 走 imageLayerBlend 同源纹理 extent）——拒绝码从 `resolved-material-geometry-invalid` 前移到 `image-provider-invalid`，即 **providerTexture 在该校验点为 nil**（隐藏层基础纹理未准备）。**下批动作**：①找 effect-input 解析处 providerTexture 的来源（prepared base-image registry / puppet atlas），为隐藏 Puppet 提供者供给；或②extent 用描述符 authored 尺寸（layer sizeWH，如 130 的 2667×1500）做纹理缺席回退——须核对 named-target publication 的 extent 一致性后再选。完成后门：outfit=4 回放 1299/1315/1326 全链执行 + submitted 恢复 + 默认 outfit=0 无回归 + 独立审查，一次性提交。 **extent 修正两轮实测（2026-09-19 深夜，工作树未提交）**：①preflight 的 `.resolvedMaterial` 路径对 providerTexture **恒传 nil**（设计如此，:611-616 注释）——修正分支改用 `preparedOutputExtent`（= 提供者 fullFramePair extent，preflight :551 已备）。②实测链推进：preflight 拒绝清零、1299/1315(X-Ray)/130 的 graph 全部开始执行（ev6：4/1/2 条执行日志）——但停在新点 `named-provider-publication-target-extent-invalid`（813 的 publication）。③再修正为恒等 extent（blit 路线 extent≡source 合同）后：`target-pool-unavailable`。**根因定案=架构边界**：提供者 full-frame pair extent（本屏 3024×1964）**超过命名目标池上限 2048**（SceneNamedRenderTargetPool.maximumDimension）——归一化破坏 blit 的 extent≡source；恒等又超池。现有路线：blit（超限不可用）、geometry 栅格化（仅 geometry kind）、source fallback（发布的是源不是效果输出）。**需要设计裁决的缺口：超上限全区域命名发布的栅格化颜色路线**（把 graph output 画进受限 named target，等价于 geometry 路线的缩小语义，或评估该角色池上限提升的内存代价）。此为独立能力切片，须设计审查后实施。**已按设计裁决落地（2026-09-19 末批）**：栅格化颜色发布（方案 A，独立设计审查推荐）——captureExtent 隐藏提供者分支超上限走 normalizedExtent、publishGraphOutputIfRequired 三分支（blit 恒等 / 栅格化 isColorContent+pipeline+精确归一化匹配 / 其余 fail-closed）、pipeline 穿参四调用点。**验证门全过**：outfit=4 回放 submitted 250/249/0（此前 0）、零停帧拒绝、X-Ray（1315 effect=4）succeeded+GPU completed、1299(8)/130(4) 全链执行；outfit=0 无回归（166/165/0、零降级）；依赖四套件 31/31（含三处 render_plan 预期更新=已提交可达性语义的补跑同步，各带注释）；终 diff 独立审查无 P1。**登记后续验证补齐（审查 P2-1）**：.resolvedMaterial 隐藏提供者 extent 分支的 Python 桩覆盖（带 effects 的 resolvedMaterial fixture + 错误 extent 负例）——当前正路径由 2959875782 回放覆盖。 **根因定案（2026-09-19 深夜，时间线日志）**：130/813 不对称 = **binding kind 差异**——1315→813 编译为 graph-output provider 绑定（813 进入 requiredGraphOutputProviderLayerIDs → 帧内 provider prepass 主动执行其 graph 并发布 named target，21:49:57-58 两条 policy+执行）；1299→130 被我的隐藏 provider 扩展准入为 **.resolvedMaterial**（raw-capture 路线，130 不入 prepass，帧内从未 claim——130 仅编译期 21:49:50 一条 policy 行）→ 消费侧惰性捕获得 nil 纹理 → 整帧停。**修法方向（下批）**：隐藏效果链提供者的绑定应与 813 同路——依赖计划编译时对"隐藏 provider + authored 可见效果链"的引用选 **graph-output provider binding kind**（而非 .resolvedMaterial raw capture）；即修 `Self.contract`/binding kind 选择或在我扩展的 case 里改走 graph-output 形态。`+Geometry.swift` 的 extent 分支可能随之可撤（raw capture 路线不再被走到）。验证门不变。 **实现批定位（同日续）**：`.forwardUtilityProvider` issue 的产生门在 `+BindingCompilation.swift:520-528`——`guard let providerOrder = order[provider.id], let consumerOrder = order[layer.id], providerOrder != consumerOrder`。order 映射由 `descriptor.renderOrderLayerIDs` 全量构建（:12），理论上 813/1315 都应在内；issue 仍触发说明二者 order 值相等（同一 fallback 位？）或其一缺失——需下一步打印 `order[813]/order[1315]` 实值定位。**若为相等**：两隐藏层在 author 序中相邻或同位时 forward/backward 判定失效，需为条件提供者对定义确定性序规则。**亦注意 :600/:807 有第二处同型判定**（static-model 与另一 contract 路径），修复需覆盖全部分支。 **插桩轮结果（同日）**：:520 门插桩未命中（该 issue 来自 :600/:807 的另一同型判定路径），且排除后 `dependency-stage-reference-unsupported` 保持 0、admitted-fallback=1（X-Ray 仍 effect-local-passthrough）。**下一步**：对 :600/:807 两站点插桩定位，或直接读该路径的 provider 可执行判定对隐藏效果链的完整条件。 **插桩定位（同日再续）**：author 序实测 813@26 < 1315@73（backward 引用），:520 门插桩未命中 → issue 来自 :600/:611 的第二站点。该站点 guard = `providerOrder < consumerOrder || supportsForwardCapture`（backward 应通过）——但 issue 仍触发，说明实际运行路径的 order 值/contract.kind/supportsForwardCapture 子条件与静态推演不符（或存在第三路径）。**下轮第一动作**：对 :608-615 guard 的三个子条件逐一插桩（providerOrder 值、supportsForwardSourceCapture/GraphExecution 值），一次回放定位失效子条件。 **子条件定位完成（同日再续，XRPROBE 实测）**：失效子条件 = **kindSupported=0 且 visibilityShape=0**（noChildren/activeDepsOk/orderGate 均=1，pOrder=26 < cOrder=73 backward ✓）。即 `providerKindIsSupported` 对 **`.resolvedMaterial` 合同 + 隐藏 provider（visible=false）带可见效果链**（813：contentKind=image、3 个可见效果、deps=[]）没有支持 case，且 visibilityShape 的析取支（`!providerHasVisibleEffects || (imageLayerBlend && visible==false) || geometry || visibleImageGraphOutput || solidLayer+requires`）同样不含该形态。**实现方案（下批）**：为 `.resolvedMaterial` 合同新增 provider 支持形态——contentKind=image、无 puppet/utility、provider.visible==false（隐藏条件提供者）、providerHasVisibleEffects（链产出合成物）、provider.dependencyLayerIDs.isEmpty（813 ✓），并同步扩 visibilityShape 析取支。**语义**：隐藏条件提供者的 resolvedMaterial 引用 = 消费者 X-Ray 采样其效果链合成输出——provider 链在 author 位次（26）先于消费者（73）正常执行并发布，消费者在任何位次采样，backward 无需 forward capture。**验证门**：2959875782 回放——admitted-fallback 归零、1326 进 resolved 执行（graph-execution 日志出现真实 program 键）、xraylayer toggle 场景 X-Ray 渲染可见；基线无回归（1315 隐藏时该链是否空转的成本评估随批记录）。 **前置核实完成（再下一段 probe）**：ownership 编译器的 `.graphInternal` 分支只覆盖**层合成引用**（"层自身 base source 发布 composite"），不覆盖**前效果输出引用**（1326 采样 1324 的 effectOutput identity）——后者无归属 → `unsupportedReferenceEffectKeys` 标记 1326 → stage 先于编译降级。**修复方案定稿**：在 `unsupportedReferenceEffectKeys`（或其前）把"引用 provider 为同层 graph 内**更早 stage** 的 effectOutput"的引用从 unsupported 集合排除——该引用由**执行器顺序 prepare 的 publications 映射**天然服务（stage 1324 prepare 时发布 output identity，stage 1326 prepare 时 `validate()` 经 `publications[identity]` 解析，时序由阶段序保证）；排除后 stage 保持 resolved + 激活策略（xraylayer 的激活门控语义不变）。**实现要点**：①graph 内 effectIndex 序判定"更早 stage"；②执行器 `validate` 的 generation/texture 一致性检查依赖顺序 prepare 的既有行为（1324 先于 1326 prepare）✓；③fixture = 2959875782 回放（layer 1315 xraylayer toggle 场景）断言 1326 的 graph-execution program 键变为真实 program 而非 effect-local-passthrough；④前置：`SceneNamedTextureDependencyReferenceAnalysis` 的 visible-only 引用收集若导致该引用无 slot 定义，需同步放宽（与 D2b'' 审查 P2-1 同源）。**工作量**：分类排除 + 回放门，一个有界批次；经独立审查后交付。
  2. `3792249095`：terminal divergence 已按 reference-uncontrolled 豁免（实时时钟内容 vs 固化预览，oracle 不适用，登记于 `scene_sample_preview_oracle_registry.json`）——现役 **0 blocked**。若该样本再现非 oracle 类失败，按新证据重新归因。
  3. `3747492842`「额外闪烁」：需固定 phase 的动态对照（文字裁切=参考视口不可控、多余 Leon=用户 Picture 属性状态差异，均已归因，见 Q1 行 1）。
  4. P0.3b 产品路径采集：选型 (b)（client/daemon 显式可取消 evidence 命令，保持"诊断非播放前置"边界），须独立设计审查。
  5. **SceneScript host 对象模型面已补齐（2026-09-18 批 A）**：`thisObject` 对 `.layer`/`.text` 属性 = 层自身对象（同一 JS 对象、写入走既有 mutation journal）、层句柄 `getChildren()`、own-layer 句柄补 `alpha`/`color` 写与 `getAnimation`（官方 `ILayer extends IObject`）。验证=host harness 新断言全绿 + 注册门 `test_scene_property_vector_script` 20/20（含反例先行：临时移除自身层 `getAnimation` 即 FAIL）+ 隔离真实回放（`3788066613` 三次 PASS，异常从 `not a function` 推进到 authored `sortLayer` 拒绝）。证据见 [E-2026-09-18-SCENESCRIPT-OBJECT-MODEL-LAYER-OBJECT](semantics/runtime-evidence-current.md#e-2026-09-18-scenescript-object-model-layer-object)。
     **批 B → 生产者侧已落地（2026-09-18）**：按精化方案实现——①`effectVisibilityProjection` 候选准入（`.effect` owner + `visible` + 布尔 + `["script","value"]` + targetPath/种子核对，`requiresStatefulOwner`）；②C `configure_effect_visibility_target` + 属性对象句柄可写 `visible`（暂存）+ `update_primitive_callback` 直通消费（读后清，缺 update 与正常 update 两路径都覆盖）；③`validateBooleanFrame` 的 effectVisibility 分支。**验证**：harness 暂存链断言 + 三套件 56 项 + 三媒体样本 3/3 PASS（owner 按作者种子值逐帧评估，失败 0）。证据见 [E-2026-09-18-EFFECT-VISIBILITY-LIVE-CHANNEL](semantics/runtime-evidence-current.md#e-2026-09-18-effect-visibility-live-channel)。**审查定界（P2，诚实记录）**：本批是**生产者侧**——stage activation 只为用户属性直通布尔目标接线，纯脚本效果发布到快照的值暂无 per-frame 读者，可见行为与基线一致（审查确认）。**后续切片 = 消费侧接线**（activation 纳入 sceneScript 生产者 + initiallyInactivePassthrough 激活路径 + 权威分歧消解 + 文本匹配器退役评估）。**消费侧诊断定案（2026-09-18 续）**：策略构造（c0f05e76 已纳 sceneScript 生产者）加 nil 原因启动诊断后回放**零触发**——缺口在**准入结构**：matcher 命中的 startup-inactive 效果经 `unifiedExecutionSubjects` 被赋予 `initially-inactive-passthrough` subject 族（`SceneEffectStageAdmission.swift:150`），以直通形态准入、**不产生 resolved-material product**，`compileStages`/`stageActivationPolicy` 不被调用。**消费侧已闭环（2026-09-19，D1→D2a→D2b''→D3 四切片，经纠正门一次回退后完成）**：种子权威消解（快照初始值=加载准备态）、D2a fixture 证明（终审可闭合、降级可行、fail-closed 码证）、carve-out 重接（script 车道豁免依赖四项+passthrough-blocked，provider 保留；并集遍历修正；:155 豁免为证据驱动修正——媒体家族消费层全部在 blocked 集内）、逐效果预证降级（deps ⊄ 绑定或 FBO → 编译期降级）、根因修复（降级码入激活直通白名单）。**纠正门三条款回放通过**（唯一拒绝=基线 layer-hidden、6 个 initiallyInactive sceneScript 阶段入计划、帧 345/344/0）。**D3 激活端到端**：媒体夹具驱动下 20 次派发、`output=bool(true)`、blendgradient 阶段 frame-0 直通→事件后真实 program 的转换被逐帧日志捕获、可见粉色渐变输出。**登记边界**：FBO 形态运行时激活直通、具名提供者消费型 fail-closed、非 externalPrimary 预证收窄（.externalAggregate 层级等价裁决）。证据见 runtime-evidence-current.md 的 D2a/D2b''/D3 行。原切片设计目标被本实现取代。**设计精化（2026-09-18 探明，实施前必读）**：缺口有三层，单点改动不足以贯通——①**规划器排除**：`SceneAuthoredEffectRenderPlanner` 的 propertyInactive 候选要求目标 ∈ `startupInactiveEffectVisibilityTargets`，而该集合只来自用户属性直通布尔目标（`SceneRuntimeInput.swift:21-26`），纯脚本的效果可见性目标不在其中 → 效果被排除出渲染计划（实证：2938612768 的 1509/1、1509/2 无渲染计划）。修法：SceneRuntimeInput 构造时把脚本拥有的 `.effectVisibility` 目标（来源=向量投影 targets 过滤，批 B 生产者已填充）并入候选。②**直通安全检查不放行该形态**：`effectLocalPassthroughIsSafe` 要求节点全部绑定 texture == effect.input——媒体效果的第二输入 `$mediaThumbnail` 系统纹理 ≠ 层源 → 不安全 → 仍被排除（惰性半步已验证不足并回退）。修法：为 sceneScript 门控效果引入**激活门控可执行阶段**类别（非直通安全、非永久直通）：计划中携带效果图，阶段能力为 `.resolved` + activation policy，逐帧策略判定激活（缩略图已发布 → 纹理就绪 → 执行）或停用（previous-current）。③**执行器**：per-frame 对该类阶段评估 policy（`SceneResolvedMaterialGraphExecutor+Preparation` 的机制现成，需确认对新增类别生效）。**权威分歧**：以实时快照为逐帧权威、prepared 为初始种子（激活门控语义天然消解——停用即 previous-current，无"错误显示"窗口）。**验证口径**：三媒体样本+夹具回放需出现 event=mediaThumbnailChanged → 暂存 → `.resolved` 阶段激活决策 → 效果执行四步链。**前置**：设计审查（触及准入安全边界：直通形态存在的理由是纹理未就绪时的执行保护——激活门控语义恰好以"事件已证明纹理就绪"为激活前提，论证方向成立但需审查确认）。诊断（`MWX stage activation policy: ...`）已留在策略构造处服务设计。**边界**：`["script","user","value"]` 家族维持 load 期独占；`thisObject.solid` 仍为显式边界。
     **下一步（按影响面排序）**：(a) authored `sortLayer`（18 样本使用，需渲染顺序权威设计）；(b) 批 B（上述设计草案，8 样本）；(c) `thisObject.solid` / `parallaxDepth` / audio `play/pause/stop` / `alignment` / `getEffect` / 子对象 `instance` / `volume` 等无消费方成员的显式边界登记。
  6. **benchmark 观察门对时间门控层的误报（2026-09-18 新记录）**：`2959875782` 三次同二进制运行 PASS/FAIL/PASS，FAIL 的机制是 layer 1654 的 authored `visible` 脚本按墙钟秒数门控（`new Date().getSeconds() ∈ (8,40)`）——7 秒观察窗被该区间截断时该层有 first-frame 无 next-frame 证据，`resolved material graph accepted layer next-frame evidence missing` 随即误报；三次运行的层观察覆盖为 34/43/41（本身非确定）。关闭门候选：把时间门控层纳入 reference-uncontrolled/oracle 登记（与 `scene_sample_preview_oracle_registry.json` 同源），或把 next-frame 证据要求限定为"窗口内复现"语义；须独立设计审查。
- **P1 下一批选定（2026-09-18，按公共集群影响面）**：`resource-load:particle-layer-load-incomplete` 集群——9 个样本（`2974757317`、`2986218263`、`3396722575`、`3665307769`、`3690859128`、`3712499998`、`3779904456`、`3780119725`、`3788467391`）的粒子层加载不完整共享同一机制。首轮 probe（2026-09-18，样本 2974757317）：particle_candidates=3、loaded=2（`particle_loaded_layer_ids=[5944,31057]`）、skipped_hidden=1（隐藏层 1991 按设计跳过）、未加载的可见层=**85705**。即集群机制=每个样本恰有一个**可见粒子层加载失败**（非按设计跳过）。首轮代码追踪（2026-09-18）：85705 的定义 `renderers=[]` → 走 child-only 容器路径（rootRender=nil、childRuntime 注册、`admitsChildOnlyContainer` 未拒绝——诊断列表无 missingSpriteRenderer）；childRuntime 有模板（43×matrix_code + eventfollow trails，genericparticle shader 受支持、sprite 贴图在包内、深度 2 ≤ 上限）。**drop 点在 child runtime 的 advance/batch 创建段**：该层注册后从未产生 draw batch。**census 仪器化已实施并定案（2026-09-18）**：child runtime 的 advance 路径加了 DEBUG census（`phase=particle-child-census`：templates/systems/systemParticles/spawnedTotal），运行证实**粒子系统正常 spawn**——5s 时 86 系统/385 实例、14s 时 214，sticky 证据（运行中曾产出 batch 的层集合）已加入 playback state 并入档。**根因定案**：preview log 的粒子摘要是 launch 时刻（advance by 0）写入的，child-only 容器在该时刻零粒子 → `2/3 + 85705: unavailable` 是启动时点测量伪影，非粒子系统失效。**修复已实施并验证（本批）**：①Host DEBUG 访问器 `debugParticleLoadReportLines()`（surface→metalView→particlePlayback）；②runner 的 after 点经 FileHandle **追加**当前 `loadReportLines(descriptor:)` 到 preview log（首版覆写 bug 已修）；③benchmark 粒子/sticky 解析改为 last-match（快照时刻状态）；④playback state 增加 `stickyBatchLayerIDs`（任意 advance 产出过 batch 的层 ID 并集）+ loadReportLines 的 sticky 行 + child runtime 的 expansionSummary/census（templates/systems/systemParticles）。**验证**：2974757317 重跑 PASS、preview log 含 sticky 行、sticky=2/3（85705 snapshot 瞬时无 batch——census 证明 5-27s 有 214-419 活跃粒子，快照时刻的瞬时缺位是 bursty 粒子系统的测量特性）。**遗留 probe → 已定案（2026-09-19，census 升级为 ~1s 节拍 + batches 计数）**：2974757317 的 25s 回放 13 个 census 节拍显示 85705 的 batch 空窗**只存在于启动首秒**（t0=0 系统/0 粒子/0 batch → t1=86 系统/43 粒子/43 batch），此后 24s 稳态 41-44 batch、370-408 活跃粒子、bufferFailures=0——**无 batch 创建路径缺口、无稳态 bursty 空窗**；此前观察的 snapshot 瞬时缺位 = 快照落在发射爬坡窗口（<1s）的测量时序，本轮回放 sticky=3/3 复证。**结论**：该集群的粒子加载/batch 链健康，首秒爬坡属 bursty 发射的启动语义；若 benchmark 期待需要，可将粒子 loaded 计数的测量点推迟过爬坡窗或登记 warmup 语义（低优先，PASS 门不受影响）。其余集群按规模排序：scenescript 异常族（type-error 7 + bad-return 2 + reference 1 + range 1）、layer-source-not-ready 5（视频瞬态已豁免，归档仍记首帧断点）、base-image-texture-load-incomplete 3。
- **P2 视觉保真缺口（2026-09-18 用户观察）**：粒子集群样本中**流星/拖尾效果的样式与官方不符**（用户实机观察）。已知线索：集群样本含 eventfollow trail 子粒子链（如 2974757317 的 matrix_code→matrix_trail，type=eventfollow）。视觉核验（2026-09-18 09:2x，serial probe）：已查看的集群样本 after 快照渲染完整——3396722575（闪电人物场景）、3779904456（星空水体）、3665307769（光翼魔法少女）、2974757317（matrix 字符雨+音频响应）、3792249095（时钟）、3743305891/3788467391 等均完整渲染。**样式差异需用户指认具体样本与时刻**（哪次播放、哪个效果、与官方的差距形态），拿到后按 sprite 形态/运动轨迹/生命周期/发射密度归因。
- **P1 下一批选定（2026-09-18，type-error 集群归因）**：scenescript type-error ×4（layer:alpha/visibility 子簇）的异常消息已捕获——
  ①`cannot read property 'scale' of undefined`（layer 332 visibility 脚本）
  ②`cannot set property 'text' of null`（layer 344 origin 脚本）
  ③`cannot read property 'getTextureAnimation' of null`（layer 344 alpha 链脚本）。
  **归因修正（2026-09-18 复核，撤回原“对象模型缺口”结论）**：三条异常逐条对回作者数据后，全部是**作者脚本引用本样本不存在的层**，不是 VM 缺口——
  ①visibility 异常来自 `thisLayer.getParent().getParent()`：layer 332 的作者父链只有两级（332 → 316「Drag & Drop (Left)」→ 顶层），第二级 `getParent()` 官方契约为 **undefined**（[ILayer 文档](https://docs.wallpaperengine.io/en/scene/scenescript/reference/class/ILayer.html)：getParent“returns undefined if the layer is not parented”），故 `.scale` 抛错；我方实现返回 `JS_UNDEFINED`，与文档一致。
  ②origin 异常来自 `durationLayer = thisScene.getLayer("playerdurationexception"); durationLayer.text = ""`：`playerdurationexception`/`playerbackgroundprogbarexception`/`playeroutlineanim` 在 `3601964477` 的 scene.json 中**只有脚本里的字面引用、没有层定义**（raw 计数 = 1，即引用本身）。
  ③alpha 异常来自 `appearAnim = thisScene.getLayer("playeroutlineanim").getTextureAnimation()`：同上，层不存在 → `getLayer` 返回 null → 成员访问抛错。
  五个同族样本（`3601964477`/`3665307769`/`3610154602`/`3612199597`/`3612795410`）都是同一作者的曲库模板被裁剪后脚本未同步：任何引擎（含官方）都无法为缺失层返回值，故这些单元是**作者内容缺陷、参考引擎同样失败**，不是我方兼容缺口；`shared` 状态对象、`thisScene.getLayer(name/id)`、`getParent()` 在失败脚本中均已实际工作（异常发生在 shared 赋值之后的语句）。
  **同簇真正的产品缺口（本批修复，2026-09-18）**：`3788066613`（EasyTransitions+ v2.1.0 幻灯片脚本，报 `TypeError: not a function`）调用 `thisObject.getChildren()`；我方 `thisObject` 此前只是仅含 `getAnimation` 的桩对象 → 调用即抛。corpus 证据：`thisObject.getChildren` ×1 样本、`thisObject.visible` ×8 样本（effect 绑定，见批 B）、`thisObject.solid` ×1 样本（无消费方，边界）。**落地内容**：`thisObject` 对 `.layer`/`.text` 属性 = 层自身对象（与 `thisLayer` 同一 JS 对象）、`getChildren()` 返回 authored 渲染顺序子层句柄、own-layer 句柄补 `alpha`/`color` 写与 `getAnimation`。**准确结论（勿写成"样本已修复"）**：对象模型面缺口已闭合，该样本的 `init` 仍中止——断点推进到 `thisScene.sortLayer(authoredChild, index)` 被拒（`sortLayer target is not an owned dynamic layer`），幻灯片过渡仍不运行。
  **下一断点 → 批 C 已落地（2026-09-18）**：authored 层重排。①C `sort_layer` 解除作者层拒绝（动态层原路径不变；作者层 mark_dirty 携带新 orderIndex，无 dynamic journal——排序幂等且 Swift 拥有准入）；②桥接放行 `(.upsert, 0, 0)`；③`SceneScriptDynamicLayerRuntime.apply` 新增作者排序分支（`order` 数组重排 + bump topologyRevision）。**验收**：`3788066613` 幻灯片 **PASS** 且 layer 18 owner `callback=completed`（getChildren→排序→子层编排全链）、全样本唯一剩余失败为已归因作者缺陷；harness 形状断言 + 四套件 60/60。证据见 [E-2026-09-18-AUTHORED-LAYER-SORT](semantics/runtime-evidence-current.md#e-2026-09-18-authored-layer-sort)。**边界**：轮播画面切换未做逐帧 ROI；跨 owner 同帧排序按准入序 last-write。**审查（无 P1）两个 P2 → 后续切片**：①同回调多排序的 Swift 应用按流序取绝对索引，可偏离 C 终序（反例成立，继承自 dynamic 路径）；②作者排序无 journal——owner 被拒时 C/Swift 分叉，且同帧 dynamic 拓扑重建会静默回退作者排序。**修复方向**：作者排序入既有拓扑 journal（baseline+ops 重推同时消除两 P2）。→ **已落地（2026-09-18 续）**：`sort_layer` 作者分支改走同一 journal（自动开事务、baseline 含作者层；rebuild SORT 分支对作者记录放宽 owner 检查——任何 owner 可排作者层，与 C 准入一致）；Swift 应用改**收集-交错**（先收集全部作者排序的最终绝对位，主循环后统一 remove+insert，与 C 终序精确一致）。回放 `3788066613` PASS（层 18 完成、唯一失败为已归因作者缺陷）。**批 C 设计侦察（2026-09-18 续，实现前必读）**：①渲染顺序权威链**已存在**——`SceneScriptDynamicLayerRuntime.order`（起始于 `descriptor.renderOrderLayerIDs`）是唯一顺序权威，拓扑快照携带它，每帧投影在 `renderOrderLayerIDs != descriptor.renderOrderLayerIDs` 时经 `dynamicLayerTopologyCache.resolve` 重建描述符（`SceneMetalRenderer+FrameWorldProjection.swift:15-40`）——动态层排序已走通全链；②C 侧 `sort_layer` 的既有实现（journal `MWX_SCENE_QUICKJS_DYNAMIC_LAYER_SORT` + 全域 `order_index` 平移 + `mark_dirty`）对作者层**结构上可复用**，缺的只是 `!target->dynamic` 拒绝与 journal 回放对作者层的处理；③Swift 应用侧 `SceneScriptDynamicLayerRuntime` 的 apply 需新增"作者层排序"操作（改 `order` 数组 + bump `topologyRevision` 使 prepared 投影失效）。**实现清单**：C 拒绝解除（作者层 + `order_index` 平移 + 新 journal op 或 SORT 带作者标记）→ journal 回放（Swift apply 作者排序，含回滚）→ 预算/冲突（跨 owner 同轮排序的固定顺序）→ 夹具（3788066613 幻灯片为天然验收样本）→ 回放取证。**风险**：prepared 投影失效频率（排序不抖动则一次性）；与 dynamic 层混排的顺序语义（作者层与动态层在同一 `order` 数组中平移）。
  同簇收尾（2026-09-18 批 D）：scenescript 异常族在档案中的其余单元全部归因——
  ①`3122339805` text:content bad-return ×2（layer 628/675 世界时钟）：脚本在"日期≥10"时 `return day`（**Number**），我方按"非字符串"拒绝；官方 JS 属性赋值对 String property 做 ToString 强转 → **已修**（字符串回调返回 Number 走 `JS_ToString` 强转；对象/布尔等仍拒绝）。
  ②`3470948192` effectConstant type-error（layer 113 godrays 颜色常量）：`scriptProperties.minColor.mix(maxColor, sv)`——官方 Vec2/Vec3 契约的 `mix(other, amount: Number|Vec)` 未实现 → **已修**（corpus `.mix(` 331 次/13 样本，远超单样本）。
  ③`3779026256` reference-error ×3（layer 33/248/322）：作者把 `createScriptProperties` 写成了 `export function` 却从未赋值给 `scriptProperties` 变量 → `scriptProperties is not defined`；任何 JS 引擎同样抛错，**作者内容缺陷（parity），不修**。
  ④`3470948192` layer 115 scale 的 `non-finite Vec3 output` 仅 frame-0 一次：`shared.vvv` 由 layer 181 的 text 脚本同帧稍后写入，frame-0 顺序窗口内为 undefined → NaN 被有限门拒绝（正确的 fail-soft），但 **bad-return 会永久禁用 owner**（`owner->disabled = true`），瞬态失败变成永久冻结 → 新产品语义缺口，见批 E。
  ⑤新家族：`ownerConstruction=failed ... invalidSource` 的事件-only 脚本（只导出 `applyUserProperties` 无 update/init，用户属性切换因此静默失效）共 **13 个样本**（`2902406982`/`2938612768`/`2959875782`/`3122339805`/`3211615441`/`3238423642`/`3448845950`/`3477054430`/`3509243656`/`3767460992`/`3768724269`/`3779026256`/`3780391264`）→ 见批 F。
- **批 E → 已落地（2026-09-18，切片 1：数据型 bad-return 逐帧重试）**：C 侧 `failure_permanently_disables()`（仅 BAD_RETURN 豁免）应用于 primitive/string/vec3 三个 update 包装的最终检查；Swift 侧 `permanentlyDisablesOwner` 属性 + 三个 program 的 24 处失败点条件化插入 `disabledTargets`（2 处内联 `.badReturn` 初始化校验点不再插入）。**效果**：`3470948192` layer 115 的 scale 动画从"frame-0 NaN 永久冻结"恢复为"帧 0 失败、帧 1 起真实动画值"。init/timer/media-dispatch 失败仍熔断（切片边界）；重试无连续失败上限（成本有界：per-owner 预算不变；取舍经审查）。证据见 [E-2026-09-18-SCENESCRIPT-BADRETURN-RETRY](semantics/runtime-evidence-current.md#e-2026-09-18-scenescript-badreturn-retry)。
- **批 F 候选 → 已落地（2026-09-18，事件-only owner）**：vector runtime 的 bool 守卫分叉——有 value hook 的 owner 保持原严格守卫；纯事件-only owner（只导出 `applyUserProperties`/`media*Changed`/cursor）允许构造（要求有事件钩子、无 destroy、无 audio、`requiresStatefulOwner`）；`requiresFrameEvaluation = handlesUpdate || (handlesInit && !hasInitialized) || timers`，program 在无 pending 事件且不需评估时整帧跳过（避免 C 侧缺 update 禁用 owner）；事件-only 的 evaluate 不进 C，发布输入值并收割事件 mutation。**13 样本回放 12/13 PASS**（其余样本行为不变）；`3122339805` 的 FAIL 为新暴露的**既存**死单元——478「Eyes」tint（`478#effect#480`）在批 D 二进制与本二进制都是 0 次执行（worktree 对跑判定），基线是启动期静默拒绝，现在是帧 3 编码的 degraded passthrough——见 [E-2026-09-18-SCENESCRIPT-EVENT-ONLY-OWNERS](semantics/runtime-evidence-current.md#e-2026-09-18-scenescript-event-only-owners)。
- **批 G → 已落地（2026-09-18）**：`SceneLayerVisibility.hasCurrentSourceDisplayAuthority` 在 snapshot==nil 且可见性被脚本拥有时按乐观可见处理；`SceneUtilityLayerRuntimePlan` 新增 `utilityNamedBindingConditionalCount`（沿父链判定的条件化绑定数）；基准门对条件化绑定豁免必需成功数（总数强制；逐层成员契约仍由 `required_named_target_binding_succeeded_layer_ids` 承担）。**20 样本全量回放 20/20 PASS**；`3122339805` 的 478 tint 从"双二进制 0 执行"恢复为完整执行链（帧 3 用户属性激活 → 通用着色器 → CPU invocation encoded-output）。证据见 [E-2026-09-18-LAUNCH-VISIBILITY-OPTIMISTIC](semantics/runtime-evidence-current.md#e-2026-09-18-launch-visibility-optimistic)。**遗留**：①~~按成员豁免~~ **已落地（2026-09-18 续）**：计划报告新增 `utilityNamedBindingConsumerLayerIDs`/`utilityNamedBindingConditionalLayerIDs`，基准门改为按成员豁免（条件化消费者从必需集合中扣除，剩余消费者逐一强制成功；无成员数据时回退计数豁免）——`3122339805` 无命名绑定（空清单正确），`2959875782` consumers=[134,801,1299] 全条件化、required=∅、PASS；②语料全量复跑时 `expected_utility_named_binding_planned`（45 样本中 4 个非零）与 particle visible 计数需按新语义重生成（失败响亮，属预期重生成触发非回归）；③与 X-Ray 条件内容 P3（现役项 1）同族，其关闭门可复用本机制验证。
- **基础设施与边界（操作纪律）**：基线数据 `/private/tmp/mwx-fullset-baseline/`（瞬态目录，关键结论已抄录运行证据页 P03 证据包）；presentation 遥测必须单实例串行+前台+caffeinate（并行/锁屏/遮挡都会全灭）；`launch-program-failure` NSLog 在默认救援成功时也打印，勿据此归因；新 Swift 文件须同步测试源清单与 `scene_swift_source_sets.json`；新 python compile+run 测试会被 Mimosa hook 误拦，走 evidence 手动门；`--phase inner` 跳过 semantics_coverage，文档批须显式跑。
- **残留小项**：样本根注入的 `shaders/blobsSM40` 写入者未查（低优先）；P0.2 引用门 5 条 P3（`97a179a9` 批记录，线上影响为零）；`3780119725` actual-present intervals 偶发（复检 PASS）。

### Q1.4 — 音频频谱与采集生命周期（2026-09-20 当前态）

**本批首断点已修复：**当前提交前二进制从已有 `exclude-current-process` tap 切到 Scene Sound 所需 `include-current-process` 时，同步 destroy→create 会先触发一次 CoreAudio `OSStatus 560947818 / !obj`，等待 retry 后才恢复，造成约 2.39 秒无 Scene publication。`SystemAudioSpectrumService` 现在按 IOProc→aggregate→tap 检查每个销毁结果；失败 identity 保留并由同一服务重试，全部成功或明确已不存在后才记录 monotonic 1 秒 retirement deadline。IOProc 尚存时不再清空 callback-owned stream format/buffer/throttle state，只有 IOProc 已销毁或确认不存在后才在 capture queue 重置。scope 换代、configuration invalidation 与 demand stop→快速 restart 统一走一个 `startCaptureIfNeeded` gate，并以 restart sequence、scope epoch、process scope、active consumer 共同拒绝 stale closure。overlay 柱数变化只换队列内 analyzer，不再替换服务或撤销 Web/Scene consumer；无旧资源的初次启动仍立即执行。旧 restart wrapper 已由公共 gate 取代，未保留 scope 专用 wrapper；没有第二 tap、FFT、provider 或 registry。

**当前验证与证据：**最终音频相关 8 模块 **84/84 PASS**，含 Web+Scene 活跃时柱数切换、stop/IOProc/aggregate/tap 四类部分失败、callback state 不早退役及 stale generation 不读 buffer 的反例；checkpoint 与 Developer ID Debug build 均成功。同批前一产品 identity `CDHash=519c64b9a8baa3e616da70987d40daf7608d30bb` 实际完成 exclude→retirement wait→include generation 2、全程无 `!obj`；随后 callback 并发加固使二进制 identity 变化，因此该 transition 不冒充最终构建 fresh 证据。最终 App `CDHash=c91a01c63e2cef52b09909baa20984497d2d8736` 对真实只读 `3780119725` 定向 **1/1 PASS / failures=[]**，隔离 HOME 无旧 consumer，直接启动 include generation 1；32-band layers `436/594/666`、64-band layer `380` 与 layer `378` generic SceneScript particle rate 均消费同一非零 generation，195/194/0 frame、0 drawable miss，原分辨率 after 帧可见底部频谱条。transition/final report 与双运行 manifest SHA-256=`782533e15f5d69cf193aacb02cdf178722121c72468274d88afe605f47e845a2 / e75f573d52b92a90f17ff79120e06f1088bafeb903bbd8cb2d3d20998757c34b / 58086f8ac90afe77bb111f10078805c6eaf430719585fcec7ee267beb6df97ac`；详见 [current evidence](semantics/runtime-evidence-current.md#e-2026-09-20-audio-capture-resource-retirement)。

**仍开放而不得算通过：**上述 retirement 证据仍是隔离 direct-host Debug benchmark；Q1.4b 闭合一个真实样本的共享 `SceneDaemonClient→daemon→requestLaunch` 下游，Q1.4c 闭合 `SteamWorkshopService.requestSceneRender→.steamWorkshopSceneReadyToRender→MainWindowCoordinator→multiplexer` 的程序化产品入口，Q1.4d 建立了120个声明关系样本的历史70/50首断点基线，Q1.4g再以当前HEAD和持续外部控制声源复核为71个capture publication、49个no-demand，但这些运行都没有经过人工UI卡片选择，visual/consumer validated仍均为0。1 秒是本机可复现 retirement 边界，不是 Apple 固定时序。最终 identity 下的真实 exclude→include transition 与可控 teardown error尚未执行；系统 tap 可能混入其他进程声音；wallpaper-local 独立幅度、官方 FFT bins/窗口/归一化/平滑数值、固定同输入官方对照、长稳、多屏、暂停/锁屏/睡眠和设备热插拔恢复均未验证。后续音频批只在 fresh 首断点或上述场景证据出现时继续扩实现，不能因任一单样本或publication分桶宣称频谱兼容完成。当前71/49身份与上限见[当前HEAD受控全集证据](semantics/runtime-evidence-current.md#e-2026-09-21-audio-current-head-controlled-corpus)。

**用户复核追加（2026-09-20，后续音频两批）：**先用现有 corpus/capability 工具枚举全部声明音频采集、频谱 provider、Audio Bars/Enhanced Audio Bars 与 SceneScript audio consumer 的样本和条件场景，关联到普通 App→client→daemon→`requestLaunch`→单一 `SystemAudioSpectrumService`→typed publication→GPU consumer 的首断点；Debug 构建身份本身不能解释采集失效，也不能把 benchmark 单样本外推为全集。随后以同一可控输入检查 band/bin 映射、频率尺度、窗口、归一化、平滑与左右/声道合并，专门复现“左侧柱集中偏高、右侧长期低振幅”以及“播放一段时间后右侧冻结、只剩左侧第一柱活动”；修复必须落在共享 analyzer/publication/lifecycle owner，并验证所有受影响样本、不同柱数、长稳和重新采集，不得按样本重排柱子或用随机抖动掩盖冻结。

**Q1.4a 全量声明→consumer 意图关系基线已闭合（2026-09-20，本批）：**既有 `scene_capability_census` 现以独立 `audio-declaration` occurrence 关联 editor 标记、raw material host array、effect `AUDIOPROCESSING`、particle `audioprocessing*` 与 SceneScript `registerAudioBuffers` 到原 owner occurrence、可见性和触发形态；仍不保存 shader/脚本 payload，也未新增平行清单。独立审查推翻了首版“79 个静态 consumer candidate”的过强结论：raw shader 文本不能代替 launch-envelope active variant/host ABI，SceneScript 也必须分别证明 global scope 与 resolution。修正后当时语料（159/159）只读 corpus 仍为 **66,244 occurrence / 2,535 family**（payload SHA-256 `49f2202aad8bbf501e334144f1a19938cc385160a646d66124fd444de0cc40e2`，零守恒/映射失败），音频关系仍为 **1,428 occurrence / 120 声明样本**；其中 editor `supportsaudioprocessing=true` 为 **79** 样本，非 project 声明关系为 **120** 样本（41 个没有 editor 标记），保守静态 consumer 意图只有 **52** 样本（27 个 editor 标记样本仍没有静态可确认意图），本批 runtime-confirmed 为 **0**。**2026-09-23 按 172 语料重算（同一 `generate` 命令、同一关系 schema）**：**68,762 occurrence / 2,607 family**（payload SHA-256 `c29a6e9608abdb1804dcbeb214bf936b4eabb22b4887e18d82940ffc405c93cf`，零守恒/映射失败），音频关系 **1,499 occurrence / 129 声明样本**；editor 标记 **84** 样本，非 project 声明关系 **129** 样本（45 个没有 editor 标记），保守静态 consumer 意图 **56** 样本（28 个 editor 标记样本仍没有静态可确认意图），runtime-confirmed 仍为 **0**。material-host 934 处/113 样本全部保持 `launch-envelope-unjoined`；其 raw uniform 只记录 52 个 exact-shape 与 2,554 个 preprocessor-conditioned 声明，不再从 shader combo 名猜 `implicit-disabled`。particle 70 处/14 样本仍为 51 enabled、18 缺 mode、1 disabled。SceneScript 261 处/50 样本均证明为 global scope，其中 192 个调用的 16/32/64 resolution 可静态准入，69 个表达式保持 `resolution-unresolved`；注释、字符串、regex 不计，template interpolation 与 computed member 保留，callback/arrow 调用不得冒充 module evaluation。完整集合、差集和逐类分布固定在 [生成台账 §3.3](semantics/scene-corpus-capability-inventory.md#33-音频声明与-consumer-意图关系) 与 snapshot `summary.audio_declarations`。公共执行 owner 仍只有 `SceneDesktopWallpaperHost.updateAudioSpectrumDemand` → `SystemAudioSpectrumService` → `SceneAudioSpectrumInbox` → material/particle/QuickJS consumer；census 只闭合 P0 关系，不升级运行支持。该冻结 snapshot 的 runtime-confirmed 仍为 0；后继 Q1.4b 的单样本 DEBUG 共享产品下游证据不反向改写 census snapshot，也不替代普通产品入口或 fresh 全集基线。

**Q1.4b 跨进程采集桥的共享产品下游已闭合一个代表样本，普通产品入口仍开放（2026-09-20，本批）：**根因是 Host 与 `SceneAudioSpectrumInbox` 已迁入 daemon，而唯一 `SystemAudioSpectrumService` 仍在主 App；两进程各自的 singleton inbox 不共享内存，daemon 的需求既未回传，App 的 FFT 结果也只发布到 App 本地 inbox，因此 direct-host DEBUG 可以工作而产品daemon链恒静默。现役链由 daemon 发出完整 typed demand（含 daemon-local scope epoch / 是否包含 daemon 输出），client 用 session generation 接收并驱动 App 唯一采集 owner；App 以独立 route epoch拒绝旧 tap callback，把同一 16/32/64×L/R 快照经 generation-bound client 送回 daemon，daemon 再用原 local token发布到原 inbox。overlay/Web/Scene capture回调各自以单槽latest-only进入App主线程，App→pipe与daemon reader→主线程都以普通控制命令为顺序屏障并只在屏障间保留一个高频帧；writer/reader两侧的控制backlog均限256条/8 MiB，拒绝普通控制时失败关闭endpoint而不静默丢弃。进程重启/停止先撤销generation需求，client只接受active且非retiring transport的事件，不存在双 FFT、第二 provider或静默双发布；graceful retiring stdout拒绝由行为测试与代码审查覆盖，不能由SIGKILL运行外推。daemon 是独立进程，所以“包含 wallpaper-local Sound”在 App tap 上转换为 `exclude-current-App`；`include=false` 时没有 admitted Sound binding 的不变量已记录，但尚缺切换运行反例。现有55/65秒证据由 `--mwx-debug-scene-daemon-client` 提前接管AppDelegate并直接dispatch `.loadScene`，只证明共享client以后下游，不计普通产品入口通过。

首轮冻结diff SHA-256 `cd5f50f2f00f24302ffb476f0d2499e8854f73347f4286be81fb6d987e800d26`被独立只读审查以CHANGES REQUIRED阻断：停止/退休stdout可复活旧demand，capture→main与公共pipe存在无界积压，`A1/control/A2`会跨屏障重排，新增源码形状断言违反仓库规则，`reset()`还在解锁后读取可变demand。第二轮冻结diff SHA-256 `40ce42ecbd98da6d38e7a067d20154a98534d8ba0198bd3e4609d26a16024441`复审继续阻断Video/Web控制准入失败被忽略、daemon reader→main控制队列无界、DEBUG runner证据被误写为普通产品入口、overlay/Web callback→main仍无界，以及selector没有把transport实现绑定到行为门。现役修正把overlay/Web/Scene拆为三个独立latest-only handoff，writer/reader控制backlog同时限256条/8 MiB，App/daemon两侧控制拒绝均失败关闭，并删除最后一个engine源码形状断言。第三轮冻结diff SHA-256 `85042975554a4ac79242e3c35e829c15a604ae7344b7e2cc24d6d21cacc96d22`复审又阻断四项证据上限：首帧截图早于非零publication、runner仍误名为stable product path、通用handoff门被写成三条实际callback均已执行，以及SIGKILL恢复不能证明graceful retiring stdout。最终实现把runner/result字段改为`stableDaemonClient`，DEBUG证据只覆盖共享client后的产品下游；通用handoff行为门与三实例构建接线分开登记；强杀只登记crash-generation恢复，graceful retiring admission保留为行为测试与代码审查。最终unsigned Debug launcher/debug dylib SHA-256为`4eceed7901cf20186a2ad822cda506362614a6721aca84665e01221ec743aa11 / 158fa6be037c6ec34c74dbceed9fb2c43c670d43c9758a3e8d0432c20de87be0`。隔离真实`3780119725`的55秒稳定DEBUG harness共享下游为`failures=[]`、scope epoch 1、peak `0.474654764`、789 rendered/0 dropped/0 fallback；32-band三层左右分别22/19个非零bin、64-band layer 380左右39/35个，SceneScript particle rate消费同一generation 2。首帧截图只证明完整构图；非零发布后两张3024×1964桌面帧及频谱ROI明确显示底部柱条并改变高度分布，稳定切片据此为`S4 visible`。65秒强杀场景在PID 85811→85821、两个request间恢复，唯一预期failure=`daemon-exited-9`，第二daemon重新非零发布并完成705 rendered/0 dropped/0 fallback；这只证明`S3 executable` crash-generation撤销与新代重放。稳定result/log/两张post-nonzero全帧/两张ROI SHA-256=`05255c3b80ea91d46f17336ca37e5132d34bdc133b2256f8fd15987e8f97b18c / bd9326919044185592f3f8fb606f4d317636cdcadda3109a9b962b9849ae2039 / cc9e2fe133c9fa5ce8b4788635646c0d4264518c169a20eb2fdf599e83cc6e7e / 7d139e5f4fcf7ed30f1a1f0e324eea0a1e6b3a38675f0e5bfef9572eeebf94d0 / 362ddf65eb185464c00a43e98605e748431a39ac90d49708e4927c4c7c0ec74c / 295413b53617db6043bfe48077211d0a1b03b714a41bc70e84876bf5634a2db6`，恢复result/log/两张first-present安全帧 SHA-256=`2073dbc7b1241efe59aa5ff5e7010530f08b2f918a9ea8e9b4c084bfd429f6b5 / cb1ca071c591b60533d3d6baf20cc52cf703e36d68b90e39980e691b11b223bd / 9610ca45b8ef05b08411fc2f9971d7f1aa63d450f255eb0c953e51dd0871538a / b8073550bd175ff0f6534722b04cfb82834da5386f480259a2e1b91099c30744`。两张post-nonzero截图仍复现“左侧柱高、右侧逐段趋平”的下一断点；本批不修改 FFT/bin/平滑算法，不把可见但失真记成频谱正确。最终相关6模块58项、全部20个`test_steam_*`模块和统一selector的19个focused模块通过；code-health只被本批外既有ratchet/超行债务阻断，随后单独运行同一隔离checkpoint build成功，JSON语法和`git diff --check`通过。详见[跨进程音频桥证据](semantics/runtime-evidence-current.md#e-2026-09-20-audio-daemon-product-bridge)。第四轮冻结diff SHA-256 `8f53635ac8c18f2ecde1f603021c189626425e581157adfbe5d651300dcfb177`经同一独立只读审查者以 **CHANGES REQUIRED** 阻断，唯一P2是最终门仍沿用旧的5模块47项，漏记本轮实际验证的`test_system_audio_spectrum`；统一纠正为6模块58项后的第五轮冻结diff SHA-256 `02f927f651fff9e776954a37dc6bd0d87eb562641b53b24c0e98be4b79157b20`复核为 **APPROVE / P0–P3无发现**。审查者确认冻结补丁与实际diff一致，普通入口/全集/FFT算法/graceful retirement等证据限制保持不变。**下一动作**：本批提交后，按用户顺序先复核SteamKit当前安装/构建身份的远程列表、登录和搜索断点；随后回到音频，以普通产品入口覆盖120个声明样本，并对固定输入的频段分布、左右声道和长稳冻结做共享analyzer数值批。

**Q1.4c 程序化普通产品入口已闭合一个代表样本，全集与算法仍开放（2026-09-20，本批）：**既有 `DebugSceneDaemonClientRunner` 增加互斥 `--mwx-debug-scene-product-entry` 模式，但不新增第二分发器：它只在 stable daemon、无 switch、私有 defaults suite、显式 `CFFIXED_USER_HOME` 以及 Workshop/样本/HOME 都实际存在且与 POSIX 账户真实 HOME/Workshop 不重叠时构造 `SteamWorkshopDownloadRecord`，调用现有 `SteamWorkshopService.requestSceneRender`，随后完全复用 `.steamWorkshopSceneReadyToRender→MainWindowCoordinator→PlaybackCommandMultiplexer→SceneDaemonClient→daemon requestLaunch`。首两次探针没有任何 Scene 活动，定位到既有 DEBUG 分类错误：共享的 `--mwx-debug-scene-root` 使 App 跳过 `MainWindowCoordinator.configure`。首轮冻结 diff SHA-256 `e0349c3b9279cb87f09c479cb379067d8d0a902aede8cee191f96f3b3a414479` 的独立只读审查又以两项 P1 阻断：若整体排除 daemon-client runner，会令既有 direct/recovery/switch 模式加载 coordinator 并可能污染产品状态；仅检查 Workshop 路径“不等于”真实根也无法拒绝父子目录、符号链接和 prefs 落入真实 HOME。现役实现因此把 coordinator 准入收敛为同时具备 daemon-client+product-entry+root 的纯策略，旧 direct 模式保持原隔离启动；隔离路径策略要求 existing directory 且双向 disjoint，真实 HOME 用 `getpwuid(getuid())` 取得，不受 `CFFIXED_USER_HOME` 影响，并由实际编译该 Swift 策略的反例测试锁定。

最终 Developer ID Debug App（launcher/debug dylib SHA-256 `9396a9a07d9f4e0f5ed2617b87a32148e9138c20a160a768a460140247a73f16 / 23c27d35d819c7461a0f1f81e621bf40d9a31f3b2c6cc772ab673dcfa8789c57`，CDHash `72d9c49f14decb524cb2b0cc28e2f004cddd5065`）在 `/private/tmp/mwx-audio-product-entry-final2.KupjNk` 隔离 HOME/Workshop 下，对只读副本 `3780119725` 的 35 秒 fresh 运行记录 `launchEntry=steam-workshop-product`、`launchPhase=launched`、活动 record `debug-scene-daemon-client`、daemon PID 96576、1 次 first-present、scope epoch 1、1 次非零 publication peak `0.19248007237911224`、352 rendered / 23 busy / 0 dropped / 0 fallback、52,129 draw/pipeline bind、21,505 geometry draw、`failures=[]`。result / first-present window / app log SHA-256=`12fb6f9ecb542e79a5c1e188962e986f1fe25e9b4608514c1cee9a6c5efe3abc / 4d86f0dae0a41da03fa9db23f34d360b8de8d202bf01d2479416e93f945810a8 / 0d9fa7aff569450c81945cbff5f20c9636f1f6a42a4be40b18887e8763576216`。同一最终二进制另以 direct stable 模式运行 20 秒，得到 `launchEntry=direct-client`、1 次 first-present、1 次非零 publication peak `0.51332050561904907`、68 rendered / 15 busy / 0 dropped / 0 fallback、`failures=[]`，且日志没有 DEBUG defaults、Steam library 或 product-entry 标记；result / app log / first-present SHA-256=`b6a198190a62f5ec55023e88be88a56f09a77e2f8a1790d841d91add087b313f / d0149fc625d541e35dd8101787db786816b66ef7304e8e57e02efc71be18815c / de8023fe2844962ca1284e8fc2623e7c5525c64c8a9db008085b73818c6954c3`。first-present 仍只证明完整构图，非零 publication 后的旧全桌面截图被前台窗口遮挡并明确作废，不计视觉验收。统一 inner selector、补充音频 6 模块、4 项策略反例与 Developer ID Debug build 的最终结果见本批 current evidence；code-health 仍只被批外既有 ratchet/超行债阻断。修正后冻结候选经同一独立只读审查者复算 diff 并以 **APPROVE / P0–P3 无发现**通过；审查确认两项 P1 均已关闭、公共链仍唯一，且以下证据上限不变。该证据最高为 `S3 executable programmatic product entry`；不等于人工 UI 选择、频谱可见/样式正确、120 样本全集、固定同输入数值、长稳或官方 parity。详见 [程序化产品入口证据](semantics/runtime-evidence-current.md#e-2026-09-20-audio-programmatic-product-entry)。**下一动作**：本批提交后，把同一产品入口模式接入既有全集 benchmark，按 120 样本 fresh 首断点分桶，再进入共享 analyzer 的固定输入频段分布、左右声道和长稳冻结批。

**Q1.4d 120 个音频声明样本的程序化产品入口基线已建立（2026-09-20，本批）：**没有复制 snapshot 的 ID 清单或另建 runner；既有 `scene_wallpaper_benchmark.py` 新增互斥 `--product-entry-audio-baseline` 模式，直接读取冻结 census snapshot 的 `summary.audio_declarations.relationship_sample_ids`，要求 120 个 ASCII 数字 ID 有序、唯一且与登记计数完全相等，然后逐样本复制到隔离 runtime、HOME 与 Workshop，调用 Q1.4c 的同一 `SteamWorkshopService` 产品入口。每项只保留 app log 与 daemon result，派生截图/fragment等可重建文件当场删除；同批让现役证据提升器认识这两类产品入口路径字段，未引入第二统计 owner。25 秒全量运行精确覆盖 **120/120**，首次分桶为 65 个 `capture-publication-observed`、49 个 `audio-demand-missing`、1 个 `first-present-missing`、2 个 `launch-not-complete`、3 个 `nonzero-publication-missing`；对后六项同二进制补跑 60 秒后，5 项转为非零发布，`2959875782`转为 demand missing。最终有效基线为 **70 个 S3 capture-publication / 50 个 audio-demand-missing**，无样本从分母删除。全量与补跑 report SHA-256 分别为 `7274201c74437e64b7035719f48fc905754dbed8b05041092fe85b43bb758618 / 213e39f96860f621d463921add264f0017d18721426f573633fbd3938e028101`；snapshot SHA-256 为 `e32998934156d93b637006a0a66468edd42660bc1b0e6309b6df648193295894`。126 份 result/log 已提升到 ignored 本机证据缓存，manifest SHA-256=`95b99dc61d36e3d889958ca426a0f38c02eb00bdea7463f9c06a6af52c4a8eb5`，共 44,973,622 bytes；另删除 5,999 个可重建运行产物 / 685,324,768 bytes，runtime 全部退役。

静态 consumer 意图 52 个样本中 48 个在本基线取得非零发布；另有 22 个未被保守静态意图确认的样本由运行链补足 publication。四个同时具备明确静态 consumer 意图、已 launched/first-present/rendered、但没有 audio demand 的首断点在 Q1.4e 已逐项校准：`3754639143` 的 Pulse consumer 只在非默认 `newproperty=0` 分支，`3777761326` 的三个 Pulse effect 作者均隐藏，`3788066613` 的 64-band consumer 由默认 false 的 `show1` 控制；三者要保留为条件场景，不得用常开 demand 伪造。`3779026256:206` 则是 stateful Bool owner 同时导出 media hook 时被准入误拒的公共缺口，现已恢复。其余 46 个 no-demand 项继续保留为 conditional/hidden/launch-envelope 未 join 的未知项，不标成 unsupported。该基线使用环境系统音频而非固定输入；正 publication 在整轮前后持续出现，足以排除“全局静音”解释，但不证明某个作者 consumer 已执行、频谱可见、柱形分布/冻结正确、UI 卡片选择、固定输入数值、官方 parity 或 120 个样本通过。共享 analyzer 的 band/bin、窗口、归一化、平滑、左右声道与长稳修复仍在本队列，不能被 70 个 S3 publication 取代。详见 [全集产品入口证据](semantics/runtime-evidence-current.md#e-2026-09-20-audio-product-entry-corpus-baseline) 与 [组合 media+audio Bool owner 证据](semantics/runtime-evidence-current.md#e-2026-09-21-audio-stateful-bool-media-owner)。

**Q1.4e 组合 media+audio Bool owner 准入缺口已修复（2026-09-21，本批）：**`SceneScriptVectorOwner` 不再无条件排斥 init/update 与 media hook 的组合，只有原本已获 stateful layer side effects 的 Bool owner可保留 media/user/audio/mutation；继续使用 authored-order `SceneScriptMediaFrameCoordinator`，media target 从剩余 vector path 排除，不引入第二 owner 或双执行。destroy、非 stateful media、cursor 冲突与 event-only audio 仍失败关闭。raw 上界是 16 owner / 9 样本，当前唯一确认同时消费 media+audio 的是 `3779026256:206`。五个行为模块 **52/52 PASS**，当前最终源码的 Developer ID Debug build/验签成功；35 秒程序化产品入口由 no-demand 推进到 `capture-publication-observed`（peak `0.3908659816`、1072 rendered、0 dropped/fallback），25 秒 media-playing+确定性音频条件场景 1/1 PASS，日志确认 12 个动态层、media generation 1、非零 audio generation 2 与 owner 原子提交。原分辨率帧仅证明完整动态输出，不足以单独识别频谱 ROI，故本批最高是 S3 consumer event execution，不宣称视觉样式/parity；产品入口分类器字段仍保守为`consumer_execution_validated=false`。最终证据 manifest SHA-256=`d51ee37c502fd3f5cdd4a101da8078632933303c3cfbc85f0e17c81a4f35af0c`；详见 [current evidence](semantics/runtime-evidence-current.md#e-2026-09-21-audio-stateful-bool-media-owner)。首轮冻结 diff `d8fc1deb149d890d02902071b69e88ddb89c5063c0705f203182099d0b8d80da` 被独立只读审查以一项 P1 和一项 P2 阻断：签名运行二进制早于最终行数收缩却被误写为当前身份，且残留作者 ReferenceError 漏记 layer 112。现役修正已用最终源码重建并重跑两条证据链，且把残留集合纠正为 `33/112/248/322`；第二轮冻结 diff `5c56f4f47935e0b38d8ab76cca197da78b334bd077e94bd5772046d18e6fbdd3` 经同一独立审查者复核为 **APPROVE / P0–P3无发现**，证据上限与未验证边界不变。**下一动作：**进入共享 analyzer 固定输入批，优先复现并纠正左右/频段集中与长时间后右侧冻结；同时补跑上述三个隐藏 consumer 的明确开启场景，不能把默认隐藏当失败或通过。

**Q1.4f 共享 analyzer 频段集中与长时右半冻结已修复（2026-09-21，本批）：**公开作者合同只固定 16/32/64 档、左右声道、低频到高频和正值形状，没有公开可移植的 FFT 窗、精确频率边界、归一化或平滑公式；基线 `311115e3` 却把未公开的 sqrt-like bin map、640-bin 截断和约 0.501 窗中心写成“官方 identity”，再从 64 档平均得到 16/32 档。固定输入门实际证明 440 Hz / 5 kHz 在 16 档只落到 `2/9`，带 `+0.5` DC 偏置的 440 Hz 主峰被推到第 0 档；该错误同时解释了真实样本“左侧高、右侧逐段趋平”，且平均降采样会继续稀释窄带能量。现役唯一 `SystemAudioSceneSpectrumAnalyzer` 对每声道各做一次滚动窗 FFT，16/32/64 三档复用该声道magnitudes；先去除每声道窗口均值、应用标准 Hann 与 coherent amplitude scale，再按项目明确声明的 `32 Hz...16 kHz` 对数边界分别直接求三档。三档各自保留同一快起慢落包络，不新增 provider/producer/registry，也不按样本重排。高采样率窗口有界截到 4096，避免旧实现因 `windowCount > fftSize` 把整链归零；声道数或采样率改变时滚动窗与全部包络原子撤销，避免旧L/R或旧bin identity泄漏。确定性 440 Hz / 5 kHz 现在分别落在 `16:6/13、32:13/26、64:27/52`；固定 DC 前后主峰不变，30 Hz 的 1600-frame 分块连续跑 192 帧，第二个完整扫描周期右半 8 档仍全部变化且第 0 档峰值 `<0.15`。音频 analyzer 31项与粒子响应4项共 **35/35 PASS**；被删除的粒子第5项只检查源码字符串与内部常数，不是行为门。inner 16 模块全通过，checkpoint 与 Developer ID Debug build成功；code-health仍只被批外既有ratchet/超行债务阻断。

同一最终源码的签名 Debug App（CDHash `bfdce4ea405996f55eda89d57a8b457d215e4438`，launcher/debug-dylib SHA-256 `524f64b7d4c4dce047a488c836bf92cce060d5c3021bb55a2080fd83344f41f4 / 653cd374b0e40baaf179f344c51182904a188c4df27aa869c683bb55429e0712`，运行前后deep/strict verified）以确定性 PCM 经过同一产品 analyzer、shared inbox 和作者 32/64-band shader，对真实只读副本 `3780119725` 定向 **1/1 PASS / failures=[]**：406 submitted / 405 completed / 0 failed。frame 30...810 的 27 个每30逻辑fixture帧采样点中，右半 `upperPeak=0.372...0.691`、`upperShapeDelta=0.0016...0.0145`、右声道 `rightPeak=0.422...0.741`，均未归零或冻结；相隔约 20 秒的两张 3024×1964 原帧及 1824×520 ROI 显示左右横轴柱条都持续换形。report/app-log/两张ROI SHA-256=`525842f3f51f2c91bd338a1bcc6c18ba8302bc5e5f97eaff3c9a62be29963876 / b7665143b683b49676fccb1afe633e6bea16e46bbb50a168cc6dd7e9f100c544 / 289d4d21af787e81e8cacf0958b875596cecefe1d98584e855787c5646d8b16a / 3ab07cf739cd25cde483e27864416a5d3905cede72414e7b6a55d5811f0609cc`，ignored最终本机证据 manifest SHA-256=`9b555595047817b8070297205d4ed81629bd60069ee625892e12119b72a173f0`；详见 [current evidence](semantics/runtime-evidence-current.md#e-2026-09-21-audio-spectrum-band-distribution)。该证据最高为 `S4 deterministic-fixture-visible`，不是普通系统 tap/真实音乐、120 样本视觉全集、作者不同 bar count/shape、重新采集/暂停恢复、多屏/热插拔/长稳或官方固定同输入 parity。首轮冻结diff `fd92c6873d8a8aad35d574deb1c852d4df9971b48fa3daa471a35a2b6acf60a0`经独立只读审查为 **CHANGES REQUIRED**：采样率/声道数切换未撤销旧包络、长时门只累计全程可能漏掉后半冻结、能力表仍把已闭合固定输入项写成缺口、Debug夹具残留源码形状断言，以及模块数和采样措辞不准确。上述项已分别以格式切换行为门、第二完整扫描周期门、现役能力行修正、删除形状测试及证据措辞校准闭合；最终产品身份、可见运行与载荷已重建。第二轮冻结diff `8b219b5258f2aaf3bf27a9a2228ab6b27162eba3a1a7295a5517ed0a67146257`经同一独立审查者复核为 **APPROVE / P0–P3无发现**；审查者严格只读，批准不提升现有S4证据上限。**下一动作：**提交本批；随后按用户现役优先序审计并进入下一个仍开放的公共断点。

**Q1.4g 当前HEAD普通system tap与受控声源全集复核已闭合（2026-09-21，本批）：**在不修改产品代码的前提下，从HEAD `31c4d648`新建并验签Developer ID Debug App（2.0.9 (277)、Team`H9QWU9XN8R`、CDHash`6a6ad75bb8d952534206745921547b047f7f7a43`）。七个音频模块全通过；普通产品入口对`3780119725`的35秒fresh运行不使用PCM fixture即取得scope epoch 1、peak`0.3503055871`、375 rendered、0 dropped/fallback。随后按操作记录以独立低音量`ffplay`循环同一macOS系统音作为外部控制声源，25秒重跑冻结120关系样本，再对六个已知慢启动项补跑60秒；最终合并为 **71/120 capture-publication、49/120 no-demand、0个未完成/零publication残留**。52个保守静态consumer intent中49个已到publication；剩余`3754639143/3777761326/3788066613`的默认no-demand分别由非默认combo、作者固定隐藏effect和默认false布尔开关解释。其余46项仍是conditional/hidden/launch-envelope-unjoined unknown，不计unsupported或通过。相较Q1.4d的70/50，唯一净变化是Q1.4e已修复的`3779026256`；因此“Debug构建全局禁用音频采集”不成立，当前无需重写tap/FFT/IPC owner。该外部声源的PID/起止时间没有独立封存进manifest，样本日志只证明tap收到外部非零PCM及退休后恢复，不能证明每帧仅来自该系统音。full/supplement report SHA-256=`da8f218faf4ec5752972cd879473c49f8b677c9135b9c03c86f08693422daed7 / 501f2ff31e3964729aed0fefc6105e214e50fa947cfccf8e25c9cb275a2e1810`，本机ignored证据manifest SHA-256=`71462bffbbf1659baf4bcf8ddeccb95f9bb89188bae51bb62488d63d69a54240`；最高仍为S3 publication，visual/consumer validated均为0，不能外推人工UI、频谱可见或官方parity。详见[当前HEAD受控全集证据](semantics/runtime-evidence-current.md#e-2026-09-21-audio-current-head-controlled-corpus)。**下一动作：**沿同一产品入口为`3754639143 newproperty="0"`和`3788066613 show1=true`增加通用typed启动override证据；`3777761326`只作作者固定隐藏负例。只有开启后仍no-demand才修公共admission/launch-envelope首断点，不把project标记或raw shader声明改成常开采集。

首轮独立只读审查以一项P1 **CHANGES REQUIRED** 阻断现役队列/能力表仍把历史70/50写成当前事实，并同时提出一项P2证据上限问题：外部控制声源措辞超过未封存PID/起止生命周期的可复现记录。两项均修正后冻结三文件diff SHA-256=`18af43df9c40bda73e7044e5ae35f0518678ecf371ea41093d4c75c855dd85e3`，同一审查者独立复算全集、静态intent、manifest载荷及唯一产品链后给出 **APPROVE / P0–P3无发现**；审查者未编辑、构建、运行、生成缓存、暂存或提交，批准不提升S3上限。

**Q1.4h 两个作者条件音频consumer的typed启动场景已闭合（2026-09-21，本批）：**既有产品入口基准新增一个通用、有界的typed启动属性参数，只允许一个明确样本与string/finite number/bool map；App端再次严格解析，结果回显不一致即identity-invalid。DEBUG-only helper不建立第二属性owner：它只在隔离产品入口把现役`SteamWorkshop.scenePropertyOverrides.<recordID>`临时置于同一`UserDefaults`实例的易失argument domain，现役`scenePropertyOverrides(for:)`读取后仍调用普通`requestSceneRender`，随后恢复完整旧domain；Release不含该helper。最终签名App CDHash=`08fc8db98ce5dbb124a89569f47a16844e944c44`。默认/启用配对实测：`3754639143`默认30秒no-demand，`newproperty="0"`后45秒publication peak=`0.1382659972`，layer122/effect0的16-band左右在generation13变为非零并沿effect1 suffix完成frame0/1 GPU/publication/terminal compositor；`3788066613`默认30秒no-demand，`show1=true`后45秒publication peak=`0.0158740319`，layer25/effect0的64-band左右在generation2/20变为非零并于frame0/1 GPU/publication/terminal compositor闭合。四次均0 dropped/fallback；两份隔离Debug suite运行前后无plist残留。最终四report SHA-256=`caaa3d503360e0242caa94a37f23fc06043228ccbb85a28e3ca85c0434da24e2 / 4714a27baa487db3639638272fe4016cbe97a482cc8f54f1c8279394864f9cdf / 037a547f4875dc49fe476035b95a5eb5f5b63aa347eeea3742ba8a23fd120aa9 / a4984e4a14d664e6811b383adff3a6ea9e0e758854876738d2fed1cb73336b79`，ignored证据manifest SHA-256=`62c0613609c5534e9eee0c0cb20981960f363544665f0913475c624e36c0c452`；详见[条件consumer证据](semantics/runtime-evidence-current.md#e-2026-09-21-audio-conditional-consumers)。上限为两个明确作者条件的S3 consumer-executed，不是频谱ROI、人工属性UI、官方parity或全集完成；`3777761326`继续是作者固定隐藏负例，余下46个默认no-demand仍保持unknown。**下一动作：**从这46项逐样本连接声明→作者可达场景→公共consumer→代码owner→执行证据，选择第一个可重现的`launch-envelope-unjoined`公共首断点；没有首断点证据不得扩写tap/FFT/provider。

**Q1.4h 验证与独立审查闭环：**仓库checkpoint选择的25个关联模块全部通过；`verify_scene_change --phase checkpoint --run`随后只被批外既有OnlineLibrary/Steam/Video ratchet和多份既有超800行文件挡在code-health，触达的三个Swift文件仅有review-limit warning，未新增hard-limit/ratchet错误；验证器因此未自动进入构建，单独执行其标准`script/run_checkpoint_build.sh`为`BUILD SUCCEEDED`。首轮冻结diff `cf1a1d27b57bacfc135ac18f681483aa504044d8fdb8c1c2eb1fca2c1b5d4087`经独立只读审查为 **CHANGES REQUIRED**：P1指出Python中bool与number相等会绕过typed identity；P2指出巨大integer与lone surrogate会从输入precondition逸出为traceback。修正为string/bool/number三类精确匹配、全部相关异常归一到ValueError并补受控exit2反例后，第二轮diff `4f8e5ed7f1b6f843dab7b0aa0120faf144ba84e0feaafd678a71ae1268ed7005`又暴露一项P2：被污染result里的超大integer可在回显比较时Overflow。该出口现失败关闭为identity-invalid并补classifier反例；产品入口单测11/11、受影响3模块ALL OK、py_compile/diff-check通过。最终代码/证据冻结diff `2820df9f61c56a7d139ef1f1d094f3516ccaf4cce6b2e6ca810a6d9fdd9edb6b`经同一审查者复审为 **APPROVE / P0–P3无剩余发现**；审查者自行核对唯一产品链、volatile domain同步捕获/恢复、四份report/log/manifest和consumer终端事件，全程未编辑、构建、运行产品、生成缓存、暂存或提交。批准不提升S3证据上限。

**Q1.4i SceneScript `WEVector`与Vec3公开构造首断点已闭合（2026-09-21，本批）：**`3712499998:66`的可见性owner在global scope注册64-band AudioBuffers，并用`WEVector.angleVector2`、`new Vec3(Vec2)`建立63个环形动态柱；旧module loader拒绝`WEVector`，补module后又暴露旧`Vec3`默认参数把Vec2对象转成`z=NaN`，最终被`layer origin expects finite Vec3`拒绝。现役修正只扩展同一QuickJS allowlisted C module owner：`WEMath.deg2rad/rad2deg`与`WEVector.angleVector2/vectorAngle2`按公开degree合同导出；同一value host把`Vec3(Vec2)`和`Vec3(x,y)`的z补0，保留既有单Number复制、三Number、string及本地`Vec3(Vec2,z)`扩展。unknown module、错参数和非有限输入继续局部失败关闭，没有新增module loader、property/audio owner或样本分支。修复前行为门精确出现`SceneScript module is not allowlisted: WEVector`；模块恢复后真实样本又精确复现非有限origin，二次修正后同一owner创建63个动态层且`vectorFailures=0`。

最终Developer ID Debug App（2.0.9 (277)、Team`H9QWU9XN8R`、CDHash`464decdbd5f5ccb9414a19b63b0d62697c62d10a`）在普通程序化产品入口、持续低音量外部`afplay`声源下，对真实只读`3712499998`运行35秒为`capture-publication-observed`：唯一request/first-present、scope epoch 1、peak`0.2793599069`、2110 rendered / 0 dropped/fallback、`failures=[]`；日志确认layer66完成、64个owner侧layer/63个dynamic、generation2非零AudioBuffers事件。另以确定性PCM经过同一analyzer/inbox的direct benchmark严格 **1/1 PASS**，1245/1244/0 submitted/completed/failed、63个dynamic layer逐帧编码；3024×1964 ready/after原帧人工确认洋红频谱环存在，但环形轮廓变化较小，故不宣称幅度/样式正确或官方parity。product/visual report SHA-256=`9cb4fac006624d9c3e53830b9ed1e0dccc80f62ae30038c75ba48e10738316f3 / caa5c8e1aa7a5ebbd71dda7341a49873a7e1bed71220591a1a7f15f16ff395fc`；ignored证据manifest SHA-256=`d6716c5bb85ff0448adae50f13334e006c7e717c64fc9a10c952c6c08aa0d221`、载荷17,550,209 bytes。当前最高是普通产品入口`S3 consumer event-executed`加受控`S4 ring-visible safety`；重复文字/装饰、频谱幅度与作者样式、普通UI选择、长稳、完整WEVector/Vec3 edge和官方同输入仍开放。首轮冻结diff `33fe2a00e65540d0a8def58829cb7c072e1405ee4e47a65c1cb73e4a247b78c8`被独立只读审查以 **CHANGES REQUIRED** 阻断：P2指出同一能力表仍把批前已实现且有正反门的`WEColor.normalizeColor`写成L0，P3指出轴向+135°门被过写为“四象限”。修正并删除不可充分复算的ROI精确百分比后，第二轮diff `0d06d0a2ca38574b1800cd760ee2abb82f57474d35cc4209aa9446b610778aad`又暴露一项P3：Generic VM总览漏列WEVector。最终冻结diff `c78b11af15c0b11b6bcde84d6eae90db5ce93113af868f68cc9d3ab942bcd821`经同一审查者全量复核为 **APPROVE / P0–P3无遗留 finding**；审查者确认唯一loader/value/audio/compositor链、异常失败域、两个非有限入口和证据上限，且全程未编辑、构建、运行、生成缓存、暂存或提交。批准不提升上述S3/S4上限。

**Q1.4j `WEVector` sibling cohort与当前no-demand守恒已闭合（2026-09-21，本批）：**旧Q1.4g全集中的`3721456868:97`、`3763428294:226/373`、`3779904456:353`都是默认可达的Circle Audio Visualization owner，批前日志分别精确停在同一个`SceneScript module is not allowlisted: WEVector`；因此它们与Q1.4i的`3712499998:66`属于同一公共首断点，不应继续留在launch-envelope unknown。当前HEAD `3fbd7c5a`新建并验签Developer ID Debug App（CDHash`4a51a532475cb47902d8f9513a7616c297e813f6`）后，以持续低音量外部Glass系统声沿普通程序化产品入口逐项运行35秒，三项均为`capture-publication-observed`、唯一request/first-present、scope epoch 1、0 dropped/fallback、`failures=[]`；peak/rendered分别为`0.0801128522/1191`、`0.1387264132/735`、`0.1024030447/1754`。frame0同一QuickJS owner链分别准入`1/2/1`个owner和`63/126/63`个dynamic layer，`vectorFailures=0`，generation `2/20/9`取得`audioValuePublished`；没有新增样本分支、tap、FFT、provider或第二owner，本批也没有产品代码变更。

将Q1.4g冻结的`71 publication / 49 default no-demand`按后继真实运行精确覆盖`3712499998`与上述三项后，当前默认场景守恒为 **75/120 capture-publication、45/120 no-demand**；其中`3754639143/3777761326/3788066613`仍分别是已验证的非默认作者条件、作者固定隐藏负例和默认false条件，故未解释unknown从46先经Q1.4i降到45、本批再降到 **42**，没有删除样本或把unknown算通过。剩余42项由41个material-host raw/preprocessor-only关系与`3750813609`一个particle missing-mode关系组成，这只是声明→运行尚未join的分类，不是unsupported。三项report SHA-256=`e3d4b111c06304be3214b9e6aa7836c04ef8680786fdf41ca64d6f0f1b8261df`，ignored证据manifest SHA-256=`5da488b1a0254d6b2f5423296757461678ad7db035c1e8bd47ac47e0e9090c7a`、380,372 bytes；详见[WEVector音频环证据](semantics/runtime-evidence-current.md#e-2026-09-21-scenescript-wevector-audio-ring)。最高仅为三项`S3 consumer event-executed`，没有频谱ROI、幅度/方向/位置/样式、人工UI、长稳或固定官方同输入parity；外部声源生命周期也仍未独立封存。冻结diff `efaf39538ed68ced6d206a34d86977978759901c030c8c91a56537b09ef7b3d0`经未参与实现的独立只读审查为 **APPROVE / P0–P3无发现**：审查者独立复算manifest/report/六个载荷与三份真实包身份、默认属性可达性、旧失败和新owner/audio publication事件、`75+45=120`及42项构成，并确认没有样本dispatch或第二tap/FFT/provider/property/compositor；其未编辑、构建、运行、生成缓存、暂存或提交，批准不提升上述证据上限。**下一动作：**先对剩余集合中唯一仍有editor音频支持标记的`3765760121`执行默认`newproperty7=false`与作者可达`newproperty7=true`的typed启动配对，沿真实`Simple_Audio_Bars` material owner核对active variant/host-uniform admission；只有开启后仍no-demand才把它定为公共launch-envelope首断点，不得扩写tap/FFT/provider。

**Q1.4k `3765760121`作者条件与material消费链已闭合（2026-09-21，本批）：**真实project把bool属性`newproperty7`标为“音频感应”、默认`false`，同时声明`supportsaudioprocessing=true`；同一Q1.4j签名App（CDHash`4a51a532475cb47902d8f9513a7616c297e813f6`）与同一低音量Glass外部声下，默认35秒按作者条件把layer763拒绝为`execution-route-layer-hidden`，得到`audio-demand-missing`、1887 rendered / 0 dropped/fallback。typed启动`{"newproperty7":true}`后45秒为`capture-publication-observed`，peak`0.1301648021`、2470 rendered / 0 dropped/fallback、`failures=[]`；`Simple_Audio_Bars`的layer763/effect0/descriptor764以`genericCompilerArtifact`进入唯一resolved-material graph，64-band左右uniform均从frame0 generation0静音推进到frame75 generation4非零（L/R peak=`0.130164802/0.112227999`，两侧`first=0`且各2个非零band），frame0完成GPU/publication/terminal compositor，frame1取得next-frame。本轮operator观察到外部`ffplay`控制声源PID `64051`从`2026-09-21T06:40:51Z`持续到`06:43:03Z`（132秒）且停止后进程不存在；该生命周期没有独立封存，不能与report/hash等同，Glass文件SHA-256仍为`b337d8e01066c30de2f296763609b7e488bb3c6ea817aa875ee999d708433cf9`。default/enabled report SHA-256=`5d5c6c1df9e92446a5cd4c2aae850523ccf5b39fb09b8259beabc0b9dad7bb19 / e7be2f96331241cb4ceb9cafd216358675853e24cb8d09fc75a9225ac0d72e2b`，ignored证据manifest SHA-256=`66a5c850dcacf47b76be4bdda6f11284b60fb0c6ed0653cb7b7394f8cc898721`、206,917 bytes；详见[作者条件material音频证据](semantics/runtime-evidence-current.md#e-2026-09-21-audio-3765760121-condition)。本批不改产品代码：默认守恒仍是`75 publication / 45 no-demand`，但四个默认no-demand已有作者条件/固定隐藏解释，故unknown从42降到 **41**（40个material raw/preprocessor-only + `3750813609`一个particle missing-mode）；不得据raw声明、project支持标记或默认关闭条件常开采集。最高为一个作者条件下的`S3 publication + 保存日志中的bounded consumer-executed`，不是频谱ROI、人工属性UI、长稳或官方parity。

**Q1.4l 余下41项声明→no-demand关系已闭合为“未作者激活”，不是采集断点（2026-09-21，本批）：**没有新建第二个统计工具；既有`scene_product_entry_audio_baseline`只增加一个纯关系join，要求输入sample identity有序唯一、declaration occurrence identity唯一，遇到缺声明、混合owner、exact active ABI或畸形字段均失败关闭，输出上限固定为`declaration-reconciliation-only`。以Q1.4g full/supplement和Q1.4i/j四个后继WEVector结果重算，默认场景继续严格守恒为`75 publication + 45 no-demand = 120`；从45项中只扣除已有独立证据的三个作者可达条件和一个作者固定隐藏负例，余下41项经159样本live census（1,428条音频声明、0 validation failure、payload SHA-256=`49f2202aad8bbf501e334144f1a19938cc385160a646d66124fd444de0cc40e2`）得到 **40样本/401处 `material-host-spectrum-not-authored` + 1样本/1处 `particle-audio-mode-missing` + 0 unresolved**。

40项的401个owner全部是`material/effect-pass`，shader为`effects/shake` 382处、`effects/pulse` 11处、Workshop pulse revision 8处；owner的`audio_processing_state`全部`absent`、owner `combo_keys`全部为空、exact active resolution全部为空，只有802个preprocessor-conditioned left/right原始声明，visibility为147 visible / 65 dynamic / 189 hidden。官方公开[Shake](https://docs.wallpaperengine.io/en/scene/effects/effect/shake.html)与[Pulse](https://docs.wallpaperengine.io/en/scene/effects/effect/pulse.html)均把`Audio response=None`定义为普通定时模式，只有从None改成其他选项才响应系统音频，故不能从raw shader schema反向制造demand。`3750813609`唯一关系是visible `sphererandom` emitter只写`audioprocessingbounds`、没有`audioprocessingmode`；直接适用的官方[Emitter](https://docs.wallpaperengine.io/en/scene/particles/component/emitter.html)合同把配置Audio response（如Center）作为Sphere/Box emitter进入音频响应且随音频active的条件，[Operator](https://docs.wallpaperengine.io/en/scene/particles/component/operator.html)和[Initializer](https://docs.wallpaperengine.io/en/scene/particles/component/initializer.html)又明确Mode是启用开关，现役[source index](semantics/source-index.md#15-particles)也已将mode缺省关闭登记为有界合同，因此typed particle owner把missing mode保持关闭是正确负例。reconciliation JSON SHA-256=`2cb60ce2263df42f0560e15a0575dbf12c0f072401d8533e96ad15c16037872f`，ignored manifest SHA-256=`0a61cb808719e3184c7d87fc316177027d51d5a88190dc534c44993e0b088cf9`；详见[no-demand声明关系证据](semantics/runtime-evidence-current.md#e-2026-09-21-audio-no-demand-declaration-reconciliation)。

这只把Q1.4“声明存在却为何不demand”的关系unknown从41降为0，不把41项删出分母、不标unsupported或通过，也不证明其普通时间型Shake/Pulse视觉正确。当前可达音频consumer候选全集因此为 **78样本（75默认 + 3作者条件）**；`3777761326`固定隐藏与上述41项没有作者可达音频激活。下一批先在既有表内把78项按consumer kind、作者条件、16/32/64分辨率、可见层/动态层和保存的consumer execution/ROI证据拆开，得到真正“频谱柱/环/粒子/光效”子集并逐项补visual与长稳；不得把publication或本次静态负例分类冒充全部频谱显示正确。

**Q1.4l 独立审查闭环：**首轮冻结tracked diff `3c9c7b7d8f03467ac31fbce32394c35a2684df14be54f4a3c684f30b4e7fb170`被未参与实现的只读审查以 **CHANGES REQUIRED** 阻断：P2指出material正类没有要求source显式audio combo与唯一preprocessor-conditioned ABI，particle空key也可能被误归missing-mode；P3指出`sphererandom`结论应直接引用Emitter合同，而不能只外推Operator/Initializer。分类器现对缺失/false combo、缺失/其他ABI、bool伪整数、空/无关key和显式mode全部保留unresolved，文档补直接Emitter与现役source-index合同；helper 14/14、统一inner 9模块、语义覆盖7/7、py_compile/diff-check及最终live-census逐值重放均通过，结果仍为40 + 1 + 0。修正冻结diff `a774383fa26c4352019220340f266e677cf77bcbcfb968c5403b90817ef1f375`经同一审查者全量复审为 **APPROVE / P0–P3无遗留 finding**；其独立复算48→49→45→41、`75+45=120`与分类并集/唯一性，并确认没有样本特判、第二CLI/tap/FFT/provider/statistics owner、常驻成本或证据越级。审查全程未编辑、构建、运行测试/产品、生成缓存、暂存或提交；批准不提升本批declaration-only证据上限。

**Q1.4m 78个作者可达consumer的跨已登记构建保存日志执行关系已闭合，粒子component事件成为首个公共观测门（2026-09-21，本批）：**没有新建CLI或第二统计表；既有`scene_product_entry_audio_baseline`只增加纯函数，严格接收有序唯一的78个ASCII样本identity与同集合保存日志，复用renderer的`MWX typed input consumption: channel=audio-spectrum consumer=material-uniform`和QuickJS的`audioValuePublished`事件，按现役`String(describing:)`形态约束effect/layer/particle/text target identity，同一target的value type漂移、uniform边/长度不一致、silent/nonzero状态矛盾、非有限值、非静音material或已发布SceneScript事件的零generation、非`generic-only`脚本route均失败关闭；generation 0的初始silent material事件合法但不提升consumer。分母由Q1.4l的75个默认publication与`3754639143 newproperty="0"`、`3788066613 show1=true`、`3765760121 newproperty7=true`三个作者条件组成；这些保存运行分属 **4个** 已登记Debug executable identity，没有单一候选二进制重跑78项，故本批是跨构建关系盘点而不是当前候选全集验证。生成器逐项核对report与当前census的project/package SHA-256，**78/78双fingerprint一致**后才允许join；当时live census（159 语料）为159样本、66,244 occurrence、2,535 family、1,428条音频声明/120样本（2026-09-23 按 172 语料重算：68,762 occurrence / 2,607 family、1,499 条音频声明/129 样本，见 Q1.4a 与“全样本鼠标/指针交互 census”），payload SHA-256=`49f2202aad8bbf501e334144f1a19938cc385160a646d66124fd444de0cc40e2`且零validation failure。

保存日志中 **77/78** 有非零consumer事件：33项仅material、19项仅SceneScript、25项二者都有；58样本共有129个material consumer（16/32/64分别55/33/41个consumer，覆盖19/23/35样本），127个左右两边均非零，`3780391264`的两个consumer按作者形态仅左边非零，不能通过伪造right uniform把它“校正”为全局双边。44样本共有225个SceneScript target（45 effectConstant / 145 layer / 35 particle；17 bool / 72 scalar / 7 vector2 / 129 vector3）。129个material事件全部精确join到唯一`material/effect-pass` owner与其`material-host-spectrum`声明；225个脚本target中221项同时精确join静态owner与registration，`3448845950`一项目标owner存在但压缩脚本以计算成员名注册，保守lexer正确保持registration未知，另有`3509243656 visibility`、`3743305891 color`、`3792817546 color`三项只可连接到同层其他脚本owner，登记为`runtime-borrowed-owner-target`，不伪造同字段声明也不把census扩成JS求值器。

唯一没有material/SceneScript事件的是`2131872317`：live census精确记录两个enabled、dynamic的Sphere Random emitter `particle-audio-response`，11个material host声明均为`not-authored`，且没有SceneScript registration。该批只把首断点收窄为**粒子component执行观测缺口**，没有从场景级demand外推粒子响应已执行；后继Q1.4n已在当前候选上关闭此观测缺口。直接频率数组消费只表示frequency-addressable material consumer，不自动等于“可见频谱柱”；Q1.4m自身最高仍为跨构建`S3 saved-log consumer event`。详见[作者可达consumer执行盘点](semantics/runtime-evidence-current.md#e-2026-09-21-audio-author-reachable-consumer-inventory)。

**Q1.4m 独立审查闭环：**首轮冻结diff SHA-256=`12f9687e987d1d684a5ebbcd4ff57805c448febd17e22d4ad3478b65a4477d70`被未参与实现的只读审查以 **CHANGES REQUIRED** 阻断：两项P2指出SceneScript target body/value type漂移未由公共helper失败关闭、运行report与live census只按sample ID连接而未机械守恒project/package fingerprint；另一项P2指出78项跨4个executable身份却可能被误读为单一当前候选；P3指出“零generation失败”误伤合法初始silent material事件。现役修正增加target语法/field owner/type稳定性负门、78/78双fingerprint逐项核对、4个runtime identity与`single_candidate_runtime_validated=false`，并把generation口径限定到非静音material/已发布SceneScript。最终代码/文档冻结diff SHA-256=`713816f7d695151a08ae58d82eb330b16ac729f81f0c829391215611071985a5`、ignored inventory/manifest SHA-256=`2abcb724579ca635942ba0fbce2dd3989a087beaed3dcc41da1b8f6f9c208bb2 / a8b4c56d70b068a5c5b95f4dda75f50f8ac9cb81864456abffe37521ba992e1a`；统一inner 9模块、census 24项、py_compile、JSON与diff-check通过。审查者全量复核为 **APPROVE / P0–P3无遗留 finding**，确认没有样本dispatch、第二owner/统计链、静默双执行、常驻帧成本或证据越级；其全程未编辑、构建、运行测试/产品、生成缓存、暂存或提交，批准不替代主Agent测试，也不提升跨构建S3上限。

**Q1.4n 粒子音频component提交后执行观测已闭合（2026-09-21，本批）：**现役`SceneParticleAudioInput`随共享16-band值携带同一snapshot generation；emitter、Turbulent Velocity Random、Turbulence和classic Vortex只在各自真实调用共享evaluator时产生typed候选，要求generation非零且该component选中的声道/频段含正值。候选identity与pending事件跟随simulator/root/child既有frame snapshot：surface拒绝即restore，重试仍可产生；只有`commitPreparedFrame`存在真实transaction且全部surface已提交后才发布payload-free `generic-only`事件，child系统退休前先收集，playback再按layer/path/component/index去重。root与child template各自保存可回滚sticky identity，新child simulator advance前即抑制已报告component，避免短寿命child churn重复频段扫描／候选分配；已抑制长寿命child在Set合并前退出。非零generation但选定频段全零时，响应与positive count以同一次作者顺序遍历求得，不再为观测重复扫描。没有从声明或场景级demand反推执行，也没有新增tap、FFT、provider、simulator、graph或compositor。既有保存日志纯函数扩为schema 2，作者path以`pathBase64`无损传输，并用行尾锚定与strict Base64/UTF-8解码拒绝伪字段、坏identity/generation/range/count/route与配置漂移；旧日志不会自动取得particle execution。

最终Developer ID Debug App（2.0.9 (277)、Team`H9QWU9XN8R`、CDHash`6dac50ed57cf48bd83d8abb18aa870c2d75bbb56`；launcher/debug dylib SHA-256=`0bf1b61021a8d1efa5a6b528b3d59af589bf6a9981a97f8e259bb2cccc7587f2 / 8947cff01d40ba32bb20e2c755343d400bc47ea78f470dfdb62b1d7941c35b0e`）沿普通App → client → daemon → requestLaunch对真实只读`2131872317`运行25秒：唯一request/first-present、scope epoch 1、peak`0.1500010192`、1056 rendered / 0 dropped/fallback、`failures=[]`；generation 3实际驱动layer `529/832`同一`fireworks1.json`的emitter index 0，channel 3、频段`0...10`各有6个正值，schema-2 inventory为1/1样本、2个emitter consumer、`particle_component_execution_validated=true`。frame0 21/21 effect invocation与GPU/publication/terminal compositor完成，frame1有next-frame；粒子自身是draw batch，不把material graph事件伪装成粒子GPU节点。另存ready/after原分辨率帧可见动态，但direct-host运行因旧matrix utility/effect/graph expected-count漂移为NON-PASS，且无静音配对，故只作画面安全/动态上下文，不宣称音频因果或官方样式。product/dynamic report SHA-256=`bfa92f1b8a543709c170b3c0cbb89f91b9ec341b4d9c7546863724f063c6f934 / 5ba2ac267f8b437a0f55105fe8122b4037103086abf016c89d02f57991a63f14`，consumer inventory/ignored manifest SHA-256=`28f647d328d417c954f9b02d60b4671aef9acbf7a845d2457d6201694cbfbc7f / dbe46e612199fcef273d328cd56ef72d0b8f27b165eeae796244f4b75696d2e9`；三模块55项（10项既有平台跳过）、inner 11模块、diff-check与checkpoint/Developer ID build通过，code-health仍只被批外既有ratchet/超行债务及wrapper识别限制挡住。最高为当前候选`S3 particle consumer executed`加非归因动态安全帧，未闭合输入→count/trajectory因果、其他component真实样本、长稳、78项同构建全集或官方parity；详见[粒子component执行证据](semantics/runtime-evidence-current.md#e-2026-09-21-particle-audio-component-execution)。按用户最新顺序，提交后先复核SteamKit实际启动身份的远程列表／登录／搜索，不重复实现既有公开查询和入库能力。

**Q1.4n 独立审查闭环：**首轮冻结diff `dcc2a90ef8416f697312da37ab521c5c8dba54c6f44989008c63489484433fed`被只读审查以两项P2、一项P3阻断：child churn仍在playback去重前重复扫描／分配，raw path与非行尾锚parser允许字段注入，文档模块数错误。修正template事务sticky、Base64严格grammar和11模块口径后的第二轮冻结diff `2967e9d2c16f8582fc955ee88ca3ec60f6a0d3f01b519a9d1348158a936f3e1b`又因全零选频的双重扫描及已抑制child重复Set工作被阻断。最终融合响应/positive count单遍求值、subset快返并增加非零generation全零频段与60粒子initializer哨兵；最终冻结diff `07c739ab7a3afe1caa905b4d4b1005758fa9cb233e244336fa718f2dbd12d98c`经同一审查者全量复核为 **APPROVE / P0–P3无遗留 finding**。审查者确认四类调用点、事务回滚、all-surface commit、child retirement、Base64 parser、最终运行identity与证据上限，全程未编辑、构建、运行或提交；批准不替代主Agent验证。

**Q1.4o cursor AudioBuffers demand、同帧刷新与typed owner去重已闭合（2026-09-21，本批）：**开工先核对Q1.4现役记录、`6ee39ea9/dacb796d/6d3d9983/d4d4d5bf/b8ab6106/22e6835f/f39767be/e2a32b5e/b8816ee8/8c868c72/622ab31b`音频历史与当前代码，确认不是重复实现tap、FFT、daemon bridge或既有QuickJS AudioBuffers，而是上一批明确登记的cursor owner时序缺口。现役demand聚合现把`SceneScriptCursorProgram.hasAudioConsumers`纳入同一个`SceneAudioSpectrumInbox`需求；host把本帧冻结snapshot传给cursor dispatch，dispatch只对带注册的现有QuickJS owner调用同一个`refreshAudio`，失败局部撤销该owner的cursor输出，永久错误才禁用owner，后续vector/media阶段由owner内`lastAudioGeneration`去重，不新增tap、provider、registry或第二audio owner。vector只借出实际导出cursor event的owner；standalone projection又排除vector已持有的精确typed target，避免同一visibility binding被重复构造，但不同typed target的真实双owner仍由原collision门失败关闭。

只读census当时（159 语料）为 **66,244 occurrence**，因`scenescript-registration`加入cursor/audio交叉形状后为 **2,536 family**，payload SHA-256=`e93e6f348c60e930f81bab6f10ca93aa972af4573f462eda1553e3c291a84385`；**2026-09-23 按 172 语料重算为 68,762 occurrence / 2,607 family，payload SHA-256=`c29a6e9608abdb1804dcbeb214bf936b4eabb22b4887e18d82940ffc405c93cf`**；保守lexer只找到 **1** 个同owner静态候选：`3238423642:917`，16-band AudioBuffers并导出`cursorEnter/cursorLeave`。实际编译Swift/C/QuickJS行为门覆盖standalone cursor-only demand、borrowed/standalone callback读取当前generation、随后vector同代去重、已由vector持有visibility时不重复构造，以及不同owner碰撞继续拒绝。最终源码重新构建的签名Debug App（Team`H9QWU9XN8R`、CDHash=`89ec47c09c84da6ea171b4f9c6bde55f8aead398`）对真实只读副本`3238423642`用确定性频谱和media-playing运行：修复前日志精确出现`identity=917 ... cursor owner collision`，最终日志无该失败，layer917的scale/visibility都消费media generation 1，scale owner再消费非零audio generation 2（peak `0.472441226`），424 submitted / 423 completed / 0 failed / 0 drawable miss，78 owner teardown零失败。四点轨迹只命中layer2190，**没有**触发layer917的enter/leave，故真实运行只证明collision消除、owner激活和非零audio publication；callback读取当前generation由行为门证明，不升级为layer917真实cursor事件或视觉正确。matrix仍因批外既有named-target 348期望失败，整次运行不记PASS；本批最高为`S3 bounded owner activation/audio refresh wiring`，不是普通UI、完整样本、频谱/hover视觉或官方parity。report/app-log/runtime-evidence/ignored manifest SHA-256=`a429c6df6438a83e6b73a71fc8e54d7b6ed77e5500e14025f5a8ea5997899dcb / 77f458d6ffc9a19276bac29597701676e2a57b9ca516321671ae9b53b6a2ba67 / f09ad595edf750e974737b4299286e2794074c6318b044699e4ebf386598bdcc / 5ec95d164b8ca782471896bdaaf8dffb52b0dd04b19c729f2b6624f58193170b`；详见[cursor音频owner证据](semantics/runtime-evidence-current.md#e-2026-09-21-cursor-audio-owner-refresh)。

**Q1.4o 独立审查修正：**首轮冻结diff SHA-256=`5fd6c9f1420b710e5d23f852be36ba9e9c8dfda3ffac255d04b893b946977b2a`被未参与实现的只读审查以两项P2阻断：`family_key`已受cursor交叉状态影响但family摘要没有解释拆分字段；export扫描又只覆盖`export function`且未证明模块级，可能把嵌套／模板伪代码误记为事件，同时漏掉合法箭头函数与export-list alias。现役修正把proven/unresolved cursor exports及consumer state写入occurrence、family摘要和全集聚合；lexer以brace/paren/bracket/template-expression深度要求模块级，只把可证明的function/arrow/export alias登记为事件，动态initializer单列`cursor-export-unresolved`，不降格成`no-cursor-event`。嵌套块、模板文本/表达式、direct/async function、const arrow、function alias和动态initializer均有反例门；159样本重建后occurrence/family守恒不变、仍只有上述1个proven候选，payload更新为本节所列身份。

第二轮冻结diff SHA-256=`b6974d0beb9546df290780a81afd76b3c2a02e54625fa8d10e04c1239aa1ec01`继续被三项P2阻断：generator/destructuring/可变或重写binding仍会漏为`no-cursor-event`或假proven；同generation门用相同payload且vector不读audio，删掉去重也会通过；`AudioDemand.swift`单独回退不会自动选中新门。现役修正把exported name关联到模块级binding及最终可证明状态：只承认未重写的function/generator声明与完整immutable function/arrow initializer，其余destructuring、mutable、dynamic/IIFE、re-export和wildcard均显式unresolved；vector门改用同generation但不同payload，并由update回读cursor阶段留下的`0.75/0.8`，因此重复刷新会直接失败；selector把AudioDemand精确接入该门并有选择器反例。全集守恒和唯一proven候选仍不变。

第三轮冻结diff SHA-256=`22da1f02da1c0e49e859d949f61789865d5b09773d2b3409874b54360eaae3d8`再被一项P2阻断：prefix update、destructuring assignment和for-in/of写入仍可制造假proven，class export被静默漏掉，解构属性键被误作binding，named async function expression又被误认第二份声明。现役修正把非export-list的嵌套同名引用保守视为可能写入，并显式识别prefix/postfix/compound写；class owner保持unresolved；object/array binding pattern只提取真正绑定侧，computed/property key不伪造关系；async declaration还必须回溯到合法模块边界。上述四组反例均已加入静态门；全集仍只有原1个proven候选。

最终代码／测试／证据冻结diff SHA-256=`c4b51b7a1a0bddba1416b6a6a8bb41695266951e1864718be99c918c41686769`经同一审查者全量复核为 **APPROVE / P0–P3无遗留 finding**。最终census 26项、核心4模块和统一inner 33模块通过，py_compile、两份JSON、census verify与diff-check通过；code-health仍只被批外既有ratchet/超行债务阻断，触达Swift只有review-limit warning。审查者确认唯一QuickJS owner、共享snapshot、同代去重、typed target去重、真实collision失败关闭、局部失败域与S3证据上限，且全程未编辑、构建、运行测试/产品、生成缓存、暂存或提交；批准不替代主Agent验证。lexer仍是冻结corpus的保守扫描器而非完整ECMAScript AST，未来新语法必须保持unresolved或由更强parser接管。

**Q1.4o 音频→真实particle-only切换需求连续性已闭合（2026-09-21，本批）：**进入本项前先核对Q1.4现役记录、`6ee39ea9/d4d4d5bf/22e6835f/cf73d568`等相关提交史和当前代码，确认daemon bridge、频段分布／右半长时更新、粒子consumer执行与cursor+audio owner已经解决，未重复修改；仍开放的首断点是`activate`在surface rebuild前把尚未知的particle结果当`false`，令旧音频场景→particle-only场景形成`true→false→true`。现役helper把particle状态收敛为三态：material／SceneScript／cursor已知consumer仍可在rebuild前直接开启；没有这些已知consumer且particle尚未知时保留inbox的**完整旧demand与声源scope**；surface成功后用实际`surfaces.values`结果提交精确true/false。启动／切换与显示拓扑重建都调用同一可执行lifecycle seam，任一rebuild失败均沿唯一`stop→teardownSurfaces(clearContext:true)`撤权。没有新增tap、FFT、provider、registry、第二owner或样本分支；旧“rebuild前必须传false”的源码形状断言已删除，实际编译生产helper与lifecycle seam的行为门锁定audio→particle-only零事件、audio→none仅在结论后一次撤权、初始particle-only一次开启、已知material+Sound在rebuild前开启并保留scope，以及rebuild失败exact-once stop/revoke。

**既有运行与当前源码的身份边界：**上面的App、result、log和manifest运行发生在本轮lifecycle seam与DEBUG control-policy改动之前；它们仍是旧构建的S3证据，不是当前未提交源码的fresh runtime。当前冻结源码追溯为tracked implementation diff SHA-256 `4f1b6cd8e3fbb1be27fc15decbac9d68d185ef02a4d13dd03b510c2df976470e`，另含`DebugSceneDaemonSwitchControlPolicy.swift` blob SHA-256 `4769ec3f1598ab87f376ea76659cd912649ebd7a361078b1b7cb3cc252e03a49`。

最终Developer ID Debug App（2.0.9 (277)、Team`H9QWU9XN8R`、CDHash=`1ef4e6678fd08119ca5bc4c2078d6be8bac11237`；launcher/debug dylib SHA-256=`f3b32a62f23e58612c22ca364f88444e4a9dc5ac8627be993e95a15469badea6 / cc8af730026059d7e09690290edfbd56a13ea9529d21a483d6f2bf358bab4619`）用只读副本`3780119725 → 2131872317`完成32秒同daemon切换：两个request/first-present同属PID `24508`，`switchCompletedInSameDaemon=true`，demand事件从头到尾只有`requiresSpectrum=true / includesDaemonProcessOutput=true / scopeEpoch=1`，没有中间false，`failures=[]`。隔离进程启动时已有非Scene consumer先形成generation 1；Scene route正式准入触发的既定scope retirement发生在切换前。专用DEBUG证据模式仍发送请求的property update与性能档，只省略会按合同暂停tap的pause/mute；它使用固定2秒延迟而不是capture-ready保证，因此每次证据必须由日志另行核对generation-before-switch。本次日志精确证明generation 2于`23:45:24.776`启动，随后`23:45:25.701`才发出切换，跨过`23:45:32.541`第二首帧直至runner正常结束才停止，故没有把runner自身控制或前置route换代冒充切换失败／成功。result/app-log/build-log/manifest SHA-256=`6ba4c8ed91af3ec4c1fbe72abf741a09c2fd410d230a74340ed9e3607f449ec1 / 17e45387a75aec0703f27ade314dde32d641a27ff5995267cd3e1af0b3618aac / 56bc887b00551ea821227181fbe4dd44fe8e596a05cc5f556dbd6cd982a27616 / d6d292153ff4008eb7cae803d19495657428bdf7dd8b23dd7b3cb049af01dcc8`，详见[同daemon切换连续性证据](semantics/runtime-evidence-current.md#e-2026-09-21-audio-demand-switch-continuity)。最高为`S3 direct-client shared downstream`；不是普通UI入口、频谱／粒子视觉因果、其他切换组合、真实rebuild failure运行、长稳或官方parity。下一音频lifecycle项只在`include=false`声源scope切换、真实rebuild failure或暂停／睡眠／设备热插拔出现fresh反例时继续，不再重做本项。

**Q1.4k 独立审查闭环：**首轮冻结diff `335efd6df4d3decacf2f887675829e2d48780eacaf54f26a8e689737756e2760`被未参与实现的只读审查以 **CHANGES REQUIRED** 阻断：P2指出cursor场景级demand与owner refresh被合并成过强结论，P3指出两个particle关联候选尚未证明particle-only，另一P3指出未留存的声源PID/UTC不能与manifest证据同级。按上述三项收窄后，冻结diff `f686fbbbd8926dd82e9e4335753ba6dc25f9e9ba4772882b2b87884960bb10a4`经同一审查者复核为 **APPROVE / P0–P3无遗留 finding**；审查者独立核对真实包、六个留存载荷、作者条件、material uniform/graph终端事件与`75/45/41`守恒，全程未编辑、构建、运行产品、生成缓存、暂存或提交。批准不提升证据上限，两项后续时序缺口仍只属静态反例/候选。

**Q1.4d 历史基线工具批审查记录：**首轮冻结组合 diff SHA-256 `51bdd0c9c4320592db110a6db0c3a85f87c97dedcce046c22410a8b0c13b8f48` 被独立只读审查以 **CHANGES REQUIRED** 阻断：证据提升器未约束 sample ID，可能目录逃逸或重复覆盖；进程/runner/identity失败可被后续产品断点误桶；批级验签被写成逐样本验签；重复同request的first-present未被拒绝；非保留子目录或symlink不会清理。现役修正把promoter sample identity收紧为ASCII数字且per-run唯一并在copy前再做run-root containment；分类优先级改为process→runner/schema→result identity→可信payload阶段；first-present要求exact-once且record identity精确；artifact递归计数和清理不跟随symlink；文档改为两轮各自批前/批后验签，并明确旧report生成早于最终`matrix_source`序列化字段。修正后126份留存结果由最终分类器重算仍零差异，3个定向模块及统一inner selector的13个模块全部通过，py_compile、JSON与`git diff --check`通过；本批没有Swift改动，因此没有把旧build冒充新构建。第二轮冻结组合 diff SHA-256 `1994ad132bd7c013f0c3fcc16d52fd9a71ea7e583fbbdddc574c0f84b78eeb1f` 经同一审查者完整复审为 **APPROVE / P0–P3无发现**；审查者未编辑、构建、运行、生成缓存、暂存或提交，且批准不提升上述S3证据上限。

**Q1.4p 三引擎音频采集与频谱 producer 已统一（2026-09-22，本批，代码已验证，真实音乐/官方 parity 未验证）：**交接审计确认 CoreAudio process tap、aggregate device、PCM `SystemAudioCaptureBuffer` 本来只有一个采集 owner，但 `SystemAudioSpectrumService` 随后分别持有 Scene 4096-FFT、Web 4096-FFT、Overlay 1024-FFT 三套 analyzer，导致三种播放引擎的窗口、频段、归一化、时序和右半冻结边界分叉。本批以现役 `SystemAudioSceneSpectrumAnalyzer` 为 Video/Web/Scene 共用的唯一 PCM→16/32/64×L/R canonical producer；Web/Video 只接收同一份 64-band typed snapshot，Overlay 只做双声道合并、有限柱数投影、显示风格与本地包络，不再接收 PCM、创建 `FFTSetup` 或运行第二套 FFT。Scene endpoint 撤销改为只清自身 publication，仍在工作的 Web/Video 不丢共享滚动窗；只有 tap 停止、采集失败或真实资源代际切换才重置 canonical producer。2026-09-22 当前执行的 `test_system_audio_spectrum`、`test_system_audio_spectrum_recovery`（含 revoke→reenable）、`test_scene_audio_spectrum_input`、`test_scene_audio_capture_scope`、`test_scene_audio_demand`、`test_scene_audio_response`、`test_scene_particle_audio_response` 七组门全部通过；第一次 checkpoint 因 Embed SteamService 的 .NET restore SIGSEGV 失败，隔离重试后同一 `script/run_checkpoint_build.sh` 为 `BUILD SUCCEEDED`，仅有批外既有 C/Swift warnings。恢复反例的 revoked-frame 只证明旧 Scene token 通过共享 processing-admission，未注入 PCM，不提升为 Web/Video analyzer 或 callback 输出证据。当前证据仍不包括普通系统音乐的完整全集 ROI、Video/Web/Scene 实机三引擎逐样本视觉验收、固定官方同输入、长稳/设备切换，因此不记为全量频谱兼容完成；`test_scene_cursor_audio_consumer` 在本机历史上超过30秒无输出，本批保留未验证，不以超时冒充通过。独立只读复审已完成，最终冻结候选为 **APPROVE / P0–P3 无发现**；提交后继续音频真实输入/均衡与其他断点。

**Q1.4q 同构建普通系统声音入口复核（2026-09-22，证据批）：**已提交 `93ec811b` 的当前 Developer ID Debug App `CDHash=9ab9e0b11ffb8b7053a47331fc51e3005641dfa4` 在隔离 HOME/Workshop 中，以持续外部 `Glass.aiff` 声源运行真实 Scene `3754639143` 的作者条件 `newproperty=0`，取得产品入口 demand、一次非零 capture publication、1732 rendered / 0 dropped / 0 fallback、`failures=[]`，最高 `S3`；同一构建运行真实 Web `923576681`，**未传** `--mwx-debug-web-audio-spectrum-fixture`，作者 listener 注册、generation 2 的真实 tap peak `0.6239`、`audio.spectrum.changed meanDelta=0.0323`，原分辨率两帧可见柱条改变，最高为代表样本有界 `S4` 可见而非官方一致性。两者不填充120项全集的缺口或Video结果；现有 `--mwx-debug-play-workshop-id` 仅触发 `setAsWallpaper`，没有提供本次验证所需的显式/受控 `systemAudioSpectrumEnabled=true`，它会按持久化设置调用 `applySystemAudioSpectrumToEngine`，本批未取得可归因的Video overlay结果；当前真实Video目录为空，因此Video普通入口/开启条件仍未验证。Web启动前的generation 1来自既有设置/恢复状态，曾被Web runner关闭；本条只认作者demand后的generation 2。第一次Web手动命令的隔离defaults suite名称不符合现役`Debug.`前缀，DEBUG precondition崩溃，已修正命令并成功重跑，不计产品故障。具体命令、identity、原始哈希与未验证项见[普通Scene/Web取证](semantics/runtime-evidence-current.md#e-2026-09-22-audio-ordinary-scene-web)。下一步用正常设置入口在隔离Video内容上启用overlay、取typed消费与可见序列；再覆盖三引擎受影响集合、长稳/设备恢复与官方固定同输入。

**Q1.4r 跨引擎音量/静音控制闭环（2026-09-22，本批，代码与行为门已验证；真实 UI/三引擎手动验收未完成）：**此前设置页的音量滑块直接调用 `WallpaperEngine.setVolume`，只覆盖当前 Video/Web 内容；Scene 只有独立 `setMuted`，作者 Sound 的 `baseVolume` 没有公共主增益，导致滑块对 Scene 无效。本批沿现役 `WallpaperEngineCommand → PlaybackCommandMultiplexer` 增加一个百分比 `setVolume(0...100)` 命令和归一化 `PlaybackVolumeState`。Video/Web 仍由同一个 `VideoPlaybackCommandHandler → WallpaperEngine` 投影；Scene 由 `SceneDaemonClient → setVolume(0...1) JSON → SceneDaemonRuntime → SceneSoundPlaybackRegistry.masterVolume` 投影，最终增益严格为 `muted ? 0 : authored/user baseVolume * masterVolume`。`PlaybackMuteState` 现在是唯一静音意图 owner，并以 `UserDefaults` key `PlaybackMuteState` 持久化；启动只恢复该 key，旧版本 `settings.volume == 0` 仅在 key 缺失时迁移一次，不再从正音量反推静音；`resetToFreshInstallState()` 同步清除该 key，确保全新安装级重置恢复可听默认态。Scene load/reconnect 在现有 request replay 后重放当前音量与静音，daemon 激活时再从同一状态初始化新 Sound registry；未新增 tap、FFT、provider、clock、registry 或 compositor，也没有按 sample/layer/path 分支。设置滑块移除重复的直接引擎写入，只保留设置持久化、公共音量命令和 0 边界静音命令。

本批 `test_playback_command_multiplexer`、`test_scene_daemon_protocol`、`test_scene_sound_playback_program` 共 **12/12 PASS**；广播门验证 Scene/Video 都收到 37.5%，协议正例覆盖有限 `0.375`、越界 `1.01` 拒绝，现有 multiplexer harness 另覆盖静音持久化恢复与旧 `volume=0` 一次性迁移；最终隔离 `xcodebuild -project MyWallpaperX.xcodeproj -scheme MyWallpaperX -configuration Debug -derivedDataPath /private/tmp/mwx-audio-controls-final4.o8KAfe build CODE_SIGNING_ALLOWED=NO` 为 **BUILD SUCCEEDED**（仅既有 SteamService CS8603 warning）。独立只读复审冻结本批实际 diff（基线 `4fbc11e5`，当前列出的 owned paths 含 `WallpaperEngine` 直接接入点、Video/Web handler、Scene IPC/host/registry、设置/启动/重置静音迁移、测试与权威文档；不含自动改写的 `project.pbxproj`）后需确认 nested mute→volume 投影、持久化/迁移、重置语义、重连顺序和 Web 路由；本批未执行设置窗口手动滑动、静音前后真实音频电平、三引擎同输入端到端或视觉取证，不能把代码/行为门升级为 UI/实机全引擎验收。

下一批仍按现役队列处理 `systemAudioSpectrumEnabled` 的三引擎策略统一、canonical producer 的左右频段视觉均衡/2241938645 单柱首断点，以及 `2849382252`、`3789316755` 的作者条件 demand 与 `2134765860` Audio Ring 的脚本/属性首断点；不以本批音量闭环替代频谱开关或样本兼容结论。

**Q1.4s 三引擎系统频谱策略门已统一（2026-09-22，本批，代码/测试/build 已闭合；真实 UI 与视觉全集仍未验证）：**当前开关原先只影响 Video overlay，Web listener 与 Scene daemon authored demand 可在设置关闭时继续保留 producer/旧快照。本批新增公共 `WallpaperEngineCommand.setSystemAudioSpectrumEnabled`，设置详细配置仍由 `WallpaperEngine` 唯一保存，随后通过 `PlaybackCommandMultiplexer` 广播同一个策略门；Video/Web 的 overlay、Web listener 和 Scene route 均要求该门为 true，Web/Scene 仍必须有各自作者 demand，不因开启开关而无条件采集。关闭时清空 Video/Web 当前 projection，撤销 App tap 的 Web/Scene consumer；Scene client 通过版本化 daemon `setSpectrumEnabled` 重放，daemon 侧拒绝后续旧 publication 并清空 `SceneAudioSpectrumInbox`，重连/replay恢复同一设置。没有新增 tap、FFT、provider、registry、clock、compositor 或样本分支，也未改变 canonical producer 的频段算法。

本批 owned paths 为 `WallpaperEngineCommand`、Video handler/设置接入、`WallpaperEngine+SystemAudioSpectrum`、Web projection、Scene `SceneAudioSpectrum`/daemon protocol/client/runtime，以及三项既有 focused tests。`test_playback_command_multiplexer`、`test_scene_daemon_protocol`、`test_system_audio_spectrum`、`test_scene_audio_spectrum_input`、`test_scene_daemon_client_wiring`、`test_system_audio_spectrum_recovery` 共 **57/57 PASS**；其中 `test_scene_audio_spectrum_input` 新增可执行 publication-gate 过渡门，验证关闭时清零并拒绝旧帧、重新开启只恢复既有 demand，不制造新 demand；`test_system_audio_spectrum` 的全引擎门检查仍含少量源码接线断言，不能单独替代运行时 UI 取证。隔离 Debug checkpoint 命令为 `xcodebuild -project MyWallpaperX.xcodeproj -scheme MyWallpaperX -configuration Debug -derivedDataPath /private/tmp/mwx-spectrum-policy-checkpoint.RKZ7zp build CODE_SIGNING_ALLOWED=NO`，结果 **BUILD SUCCEEDED**，仅有既有 SteamService/Swift warnings；`git diff --check` PASS。当前证据只到公共策略/协议/Inbox/daemon gate 行为门和编译，不包含设置窗口手动开关、三引擎同源真实电平、Video overlay 实机 ROI、长稳、设备恢复或官方固定同输入。

本批从 `057296b4` 冻结的最终 diff SHA-256 `59ecc69a6a7e3dc3daa47aa865427b05fae138839d4a5bb777190bbf783f07c2` 经独立只读复审 **APPROVE / P0–P3 无发现** 后提交为 `b2f1a61d`；首轮审查发现的重复 Video daemon 命令与非行为门已修正，复审发现的证据摘要 52/57 不一致也已修正。详细配置仍是唯一 Video daemon `setSpectrumEnabled` 发送点，coarse policy 不重复 IPC；审查只覆盖代码、协议、Inbox 行为门与 Debug build，不提升 UI/视觉证据。下一职责仍是 canonical producer/投影左右频段均衡与 `2241938645` 单柱首断点，再进入 `2849382252`、`3789316755`、`2134765860` 的作者条件/脚本首断点。

**Q1.4t Video overlay 投影的首帧饱和首断点（2026-09-22，历史前置批；当前投影已由 Q1.4v supersede）：**共享 `SystemAudioSceneSpectrumAnalyzer` 的 16/32/64×L/R producer 不变；真正可复现的后续首断点在唯一 `SystemAudioOverlaySpectrumAnalyzer.styledLevels`：强输入的新峰只让旧 adaptive ceiling 向上移动 20%，第一帧多根柱被 `min(1, …)` 一起夹到 1，失去真实频段形态。确定性多频 PCM 经当前 producer 得到 16 档 `0.2695…0.8362`，旧 overlay 16 柱全部 `0.6600`；修正为上升峰值立即更新 ceiling、衰减仍按原速率后，同输入 overlay 为 `0.3636…0.6340`，左右段保留不同形态。已有 `test_system_audio_spectrum` 加入 fresh 高电平分段输入正反门，禁止全柱同高饱和；与 `test_scene_audio_spectrum_input`、`test_system_audio_spectrum_recovery` 合计 38/38 PASS。该批只改 Video overlay 的 typed projection，不改 FFT、Scene/Web作者频段或需求，也不把本机确定性 PCM 提升为真实音乐/Video ROI。`2241938645` 的 raw 声明另有 layer 282 可静态准入64档注册、layer 68 分辨率动态/未解析注册与四个未作者激活的 material schema；单柱不能由 overlay 饱和直接归因，需单独跟进其脚本/条件/产品消费首断点。

**Q1.4u 动态 SceneScript image layer 的逐帧 transform publication 修复（2026-09-22，本批，代码/行为门/新 binary 回放已完成；样本视觉关闭仍待）：**交接审计复核了 `8c78e8a2` 及前序频谱、策略门和音量提交，未重复修改 PCM/tap/FFT。对真实只读 `2241938645`，原始 SceneScript layer 282 的 64 档脚本在实际 QuickJS/Swift vector harness 中以渐变 64-band 输入产生 63 个动态柱，全部 `scale.y=1.5625…50.0`、origin 递增且 owner 无失败；原隔离产品日志也记录 `descriptor=63 visible=63 sourcePublications=63 encoded=63`。因此“只剩最左一柱”不是 `registerAudioBuffers` 只发布第0档。首断点在公共 `SceneScriptDynamicLayerRuntime.snapshot()`：`topologyRevision` 只在动态层结构变化时递增，但旧缓存同时保存完整动态层 descriptor；结构不变的逐帧 `origin/scale/angles/visible` upsert 被旧缓存永久冻结。现役修正保留拓扑和 material-color target cache，并以独立的 bounded value revision 只在 accepted dynamic mutation 后刷新动态 layer records；quiescent snapshot 不重建，仍未新增第二 provider/property/clock/graph/compositor，也未按样本分支。测试先 warm snapshot 再做 value-only upsert，锁定旧 stale-cache 实现不能通过；三个 focused test 共 **30/30 PASS**，`git diff --check` 通过。

新签名 Developer ID Debug binary（CDHash `4aa623e8b0b4af3518ae2383a2ff886e050ad67d`，executable SHA-256 `0b8845d73137260b3988d46b9a8fa1f2de30c6d6246dd8e40375d6bf0d129e8c`）经隔离 direct-host 回放命中 `2241938645`：启动 `14,809 ms`，787 submitted / 786 completed / 0 failed，`dynamic-layer-render` 两个取证帧均为 `descriptor=63 visible=63 sourcePublications=63 encoded=63`，GPU completion status=4/error=none；统一 PCM 日志从 frame 30 到 720 持续同时出现右端峰值（例如 `0.704/0.680/0.573/0.684`），不再是只动左端的采集证据。新序列原分辨率截图的对角柱条 ROI 中性白亮像素计数随帧为 `6202,5583,6801,7751,7780`（series 0000/0010/0020/0030/0045），证明画面序列发生变化，但尚未按作者预登记 ROI 完成人工视觉/官方对照；matrix report 的 16 项失败按原始报告拆为 15 项 authored effect graph expectation mismatch（Opacity、Workshop Shadow、Procedural Noise、Film Grain、Light Shafts、Shake、Water Flow、Water Waves、Foliage Sway、Water Ripple、Blend、Transform、Transform static fallback count、Transform static fallback diagnostics、authored shader count）及 1 项 resolved material graph succeeded layer IDs mismatch，不能概括为单一旧 effect-count 期待，也不能记为样本通过。详细输入、report 与截图身份见 [E-2026-09-22-SCENESCRIPT-DYNAMIC-LAYER-FRAME-VALUES](semantics/runtime-evidence-current.md#e-2026-09-22-scenescript-dynamic-layer-frame-values)。

本批下一步冻结最终 diff 交未参与实现的独立只读审查；修正发现后提交。剩余同簇首断点仍为 `3789316755` 动态层预算溢出后的作者空句柄、`2134765860` 可见 ROI/连续变化、`2849382252` exact fixture/demand，以及三引擎真实均衡分布；不以本批 224 回放替代这些条件场景。

**Q1.4v 统一频谱包络与投影的参考合同纠偏（2026-09-22，已提交 `8f4dc46d`）：**用户复核指出柱条“软绵绵、像波浪”且左右/右半长期失真，不能继续靠投影参数猜测。本批先固定第三方 MirageWallpaper revision `443777e29a8046615db6275f80ff816a4bad444b` 的中性可观察结构：唯一 stereo 64-band producer 负责窗化/FFT、连续频带聚合、一次幅度映射和每带快起慢落；binder 只做 64→16/32/64 的连续块平均；失联进入静音，消费端不叠加邻域/全局平均或第二时间包络。该第三方资料仅作 clean-room 结构交叉检查，不把其私有表达、常数或算法实现写入产品合同，也不替代官方固定同输入对照。

现役实现已收敛为 `SystemAudioSceneSpectrumAnalyzer` 单一 64-band/L/R 包络 owner：16/32 与任意柱数只做无重叠、有界连续块平均，Video overlay 删除 adaptive ceiling、邻域/全局混合、floor lift 和 previous-frame envelope，仅保留静态 style/sensitivity 增益与噪声门；Web dispatch 删除二次幂压缩/时间平滑，节流期间保留最新 typed snapshot，窗口解除时发布，静音及 active malformed 输入立即发布清零。测试覆盖 64→28/48/96 非标准投影、producer release、overlay 无二次尾巴、Web throttle/latest/silence/invalid 行为；当前 focused 六模块合计 72/72 PASS，checkpoint Debug build `BUILD SUCCEEDED`（仅既有 SteamService CS8603/deprecation warnings）。本批仍未声称真实系统音乐三引擎 ROI、全集样本、官方 FFT 数值/parity、长稳或设备切换关闭；capture service 自身失联回静音的 owner 继续作为独立后续断点。

> Q1.4p 的 Overlay“本地包络”描述及其七组前序 gate 计数属于历史前置证据；Q1.4v 已撤销该 temporal envelope，当前 projection 批以六个模块的 72/72 gate 为准，当前行为以静态 style/sensitivity/噪声门和无状态 typed projection 为准。

**Q1.4w 命名音频样本产品入口复核（2026-09-22，证据批，未产生公共代码改动）：**以当前 HEAD 的 Developer ID Debug 候选包（2.0.9/277，Team `H9QWU9XN8R`，CDHash `18899e39287f7b9d526fa059e810aaa6b9554373`，executable SHA-256 `bf2f0af6b1a58105633bb46c76593d13ea54bd022bf09e59fe19fb010ef4fe44`）分别经普通 App→client→daemon→`requestLaunch` 运行 `2134765860` 与 `3789316755` 35 秒；命令、package/project/report/log SHA-256 固定在本机忽略证据 manifest `docs/scene/evidence/2026-09-22-audio-named-samples-product-entry-recheck/manifest.md`（权威文档不依赖该缓存）。两项均 `audio_demanded=true`、非零 publication、runner `failures=[]`、报告状态 `capture-publication-observed`，但均只到 `S3-capture-publication`，未取得原分辨率 ROI、连续可见变化或官方同输入对照。

`2134765860` 的本次报告为 762 rendered frames、最大 publication peak `0.0944732055068016`；当前候选 app.log 记录 frame 0 的 16/32-band L/R typed material uniform（state=silent）、frame 0/1 的 GraphExecutor/GPU completion 与 terminal compositor（Audio Ring 节点本身非terminal，Shake suffix 与 Visualizer32 有 terminal consumption），以及 frame 94 的非零 L/R generation。已有同 fingerprint saved-log inventory 与此 fresh log 相互印证。因此当前不应再改 shared producer 或凭静态柱条外观重写 Audio Ring；剩余是可见 ROI/连续变化和作者参数人工验收。`3789316755` 的 frame 0 首断点为 `TypeError: cannot set property 'alignment' of null`（layer 147）：作者 `createLayer` 循环超过既有 `MWX_SCENE_QUICKJS_MAX_DYNAMIC_LAYERS=64`，第 65 个可选创建返回 null 后被脚本解引用；粒子 layer 25 的 audio consumer 仍有非零发布。已有 `test_scene_script_quickjs.py` 的 64-budget fixture 覆盖“overflow 返回 null、脚本可判空”的 fail-soft 合同，不能未经官方合同把预算扩大为修复；静态 `average[dataIndex+1]` 末档边界仍只是潜在问题，尚未成为 runtime 首断点。

`2849382252` 已取得 exact corpus identity，但本轮真实运行仍为 **failed/open**：报告 `/private/tmp/mwx-audio-system-live.0DSnYn/results-2849382252-live/report.json`，report SHA-256=`714bd7093c8cecbfc24ae5ffd264970fb3c0b3744f4b2e9b890038ed4618ba23`，app.log SHA-256=`7fe6ccc9c944062fe48313f73485763fe0e29b1e3da30fe175b20952b6ce1f19`，project SHA-256=`d466d48ee794beeb7f5a975dc80c028729f5c3a87b0f9aee3078c793527b86e5`，package SHA-256=`d925888a0de49cbe756f290f907347ddc1721539edb9eb28f82005649a340c40`；runner 首断点为 `effect execution CPU invocation failed` 与 `resolved material graph unexpected effect-local passthrough`。同一日志已证明真实音频 capture peak=`0.6740`、inbox peak=`0.492443` 及多项 SceneScript 音频/粒子回调，因此不能再写成 unknown，也不能计入通过或从分母移除。下一可执行项是沿同一产品入口拆分其 effect 首断点与作者条件；不得把该失败归因于频谱 producer、Audio Ring 或凭此扩大 SceneScript dynamic-layer budget。

本证据批的独立只读审查先以 P2 指出 `2134765860` typed event 的 frame 0/1 叙述超出日志；已按日志改为“frame 0 静音 typed L/R、frame 0/1 graph/GPU/compositor、frame 94 非零 L/R”。复审前冻结 tracked diff SHA-256 `ed8fa162f7ea1059d7c4d663b7cafbd690edafa85a6c25f8bc5275cd2cb7dbbf` 与 ignored manifest SHA-256 `080ef46598657473c38b705b5abcf9d189b2f7e8a90f9cf98fae70c2a524d02e` 经同一审查者复核为 **APPROVE / P0–P3 无遗留**；审查者未编辑、构建、测试、运行产品、暂存或提交。主 Agent 的现役文档治理/语义门 32/32 PASS、`git diff --check` PASS，审查与测试均不提升 S3 上限。本段审查记录属于后续文档同步，随最终 diff 再次只读复核。

**Q1.4x 频谱首档比例与节奏感复核（2026-09-22，开放、未提交）：**用户连续反馈从“左端首柱持续偏高”演变为“首柱可归零但比例突兀”，再到“整排乱动、缺乏音乐节奏感”。当前工作树包含 analyzer、两项频谱测试及本条/运行证据文档的实验性 diff，不能把成功构建或 direct-host matrix PASS 视为修复。实验候选 v4（signed Developer ID Debug executable SHA-256 `b24e4e1ea6181235ad0426e3c8be0b62388c20031f62ede526ba10e5e94c7b89`）在外部系统音乐下运行隔离 `3780119725/3789316755/2134765860/2241938645` 为 4/4 benchmark PASS，`3780119725` frame 6 的 32 档左首值 `0.3552`、峰 `0.4210`，但首档视觉仍不对。后续 v5 对低频 shelf 的另一实验候选同样 4/4 benchmark PASS，`3780119725` frame 7 的 32 档左首值 `0.0669`、峰 `0.4243`，仍被用户判为无节奏；两次未固定相同 PCM/播放相位，不能把数值差直接解释为改动收益，也不能用非黑截图或 PASS 代替视觉验收。证据身份与上限见 [当前音频节奏断点证据](semantics/runtime-evidence-current.md#e-2026-09-22-audio-rhythm-open).

官方公开 [Web Audio Visualization](https://docs.wallpaperengine.io/en/web/audio/visualizer.html)、[SceneScript AudioBuffers](https://docs.wallpaperengine.io/en/scene/scenescript/reference/class/AudioBuffers.html)、[音频处理教程](https://docs.wallpaperengine.io/en/scene/scenescript/tutorial/audio.html) 与 [shader audio uniforms](https://docs.wallpaperengine.io/en/scene/shader/variables.html) 只确认左右声道、每声道 16/32/64 个低→高频段、低频第 0 档常用于 bass/beat、Web listener 约 30 Hz、原始值可逐帧跳变且作者可在具体视觉绑定中按 frametime 平滑；shader uniform 为正值且**非归一化**。它们没有公开 FFT bin 边界、频率加权、窗函数或全局 envelope 数值。现役 [官方客户端高层取证](semantics/client-runtime-static-forensics.md#59-particle-audio-response-归一化结构复核) 仅覆盖 particle 字段归一化和 SceneScript audio array 更新时序，明确不提供可移植的全局频谱数值/平滑策略；第三方固定 revision 资料只作职责交叉参照，不是官方真值。**下批首动作**：停止无同输入依据的 gain/warp/门限试探，冻结可重放 PCM 与声音/画面时间轴，分开量化 producer 每档能量、低频节拍起音、作者 shader/SceneScript 的局部平滑、最终柱条 ROI；用同一 PCM A/B 验证“首档不过强、真实 bass 不被切掉、静音归零、左右/16/32/64 顺序不变”，再取得固定官方同输入对照。未取得这些证据前，本项保持开放，实验 diff 不提交、不将 4/4 记为视觉正确。

**研究裁决（不作为算法验收）：**官方 Web 示例直接按 64 档绘柱，只把插值列为作者可选处理；SceneScript 教程用低频第 0 档驱动 beat 响应，并在具体绑定中按 `frametime` 平滑。两者均未要求公共 producer 使每根柱等幅、随机跳动或注入节拍；开源 [projectM](https://github.com/projectM-visualizer/projectm) README 同时列出 PCM、beat detection 与 FFT 能力，仅作第三方职责案例，不能反推 Wallpaper Engine 的 producer、bin 或 envelope。当前 Q1.4x 的 per-bin 单归属、算术均值、低频 gate/shelf、凸形 warp、对比与 release 同时变动，且没有同 PCM/时间轴证据；独立只读首审因此阻断把实验提交为修复。旧测试强制“纯低频要有右半轴合成活动底”不是官方合同，会复制无关柱；新的项目实验也不得反过来把“纯 bass 右半必须精确全零”或私有 `1.75` band identity 当官方标准。先用固定 PCM 的 bass、弱 bass、混合乐音、起音/静音序列验证真实频带与时间响应，再分别检查作者绑定与 ROI；若证据不支持，应回退本实验的全局 producer 变更。

**Q1.4y 频谱低频映射与能量起音候选（2026-09-22，本批，代码已审查、视觉仍开放）：**在 Q1.4x 的未提交实验之上，唯一 `SystemAudioSceneSpectrumAnalyzer` 将会吞并左侧的 `1.75` 凸形 log warp 改为有界 `0.78` 凹形 warp；FFT bin 仍单归属 canonical 64-band，16/32/64 仍只做同一数组的连续块平均。每个声道从同一 rolling PCM 计算 RMS，以独立 bounded frame-wide gain/onset pulse 调节已有非零 band；不合成跨带活动底，fresh/reset 静音严格为零，空 band target 不注入 floor，已有 previous 值只按有界 release 衰减；attack/release 收紧到 `0.82/0.68`，减少软尾拖影。测试移除未公开私有 band index 与“纯 bass 必须填满右半轴”期待，新增固定四频 PCM 强度脉冲的重复 rise/fall 门。冻结基线 `9e98043c`，owned paths 为 analyzer 与两项频谱测试，代码/测试 diff SHA-256=`381e8880baf61fe7f0fc24f7a1b9bbdeee812b7bbe48517fa64d972123ef1588`；103 项定向测试 PASS，`git diff --check` PASS，`bash script/run_checkpoint_build.sh` **BUILD SUCCEEDED**（仅既有 SteamService/Swift 隔离兼容 warnings）。独立只读复审按该 SHA **APPROVE / P0–P3 无发现**，确认无第二 FFT/provider、reset/声道/采样率失效域清零、测试未把私有常数当合同。

确定性四频强度脉冲的两个重复周期均有 `0.6796→0.2552` rise/fall、峰值范围 `0.6829`、活动 band 数随能量变化 `16` 档；这只证明 producer 对同一 PCM 能量序列有可重复的共同起落，不证明真实音乐节拍、作者柱条 ROI 或官方数值。当前仍未取得同一段真实 PCM/播放时间轴的 A/B、原分辨率连续截图/ROI、官方固定同输入对照、全集或长稳；因此 Q1.4x 保持开放，Q1.4y 也不计入视觉通过。提交后下一批按现役队列转入 Q1.5/Q1.6：先处理 `3769761761/3766387484/1315486372` 丁达尔光线和 `3554161528/3790726145/3792817546` 粒子/轨迹鼠标跟随，复用已关闭的公共 pointer/X-Ray/particle owner 证据，不能把已闭合门重复实现。

### Q1.5 — 指针/鼠标交互缺陷簇（2026-09-19 用户实机观察，P2 优先）

用户实机反馈聚成一类：**指针坐标管线**（mouse → typed frame update → effect 消费）的方向/偏移缺陷。

| # | 症状 | 样本 | 初步定位 |
|---|---|---|---|
| 1 | X-Ray 遮罩中心点与鼠标不同步、有偏移；上下方向反转 | `2959875782`（xraylayer 条件层，D3 后已能渲染） | 2026-09-20已修公共effect-texture inverse的半幅局部坐标缺陷并拆分raw output MVP；合成四象限达到有界S4，真实AppKit鼠标/官方同输入仍待验 |
| 2 | 拖动可移动窗口图层：向上拖、窗口向下跑（方向反转） | `3122339805` | 拖拽 delta 的 Y 方向符号或 origin 映射反转 |
| 3 | **保护约束**：`2163522240`（竖长图）拖拽方向当前**正确**（鼠标上移图片向下，内容跟手）——修复 #2 时不得破坏 | `2163522240` | 两样本的拖拽实现/坐标系可能不同，需逐一定位各自消费链 |
| 4 | 粒子/轨迹等效果跟随鼠标不完善 | 用户复核样本 `3554161528`、`3790726145`、`3792817546`；当前 census 唯一显式 Sphere/Box pointer-emitter 为 `3238423642:984`；另有 4 资产/4 样本出现 CP0 operator/initializer 形态，但按官方合同不是有效 pointer | 先逐样本确认真实声明是 pointer provider、cursor event、SceneScript dynamic layer 还是粒子 control point，不把外观看似轨迹或 CP0 当同一算法；随后沿公共 typed pointer/event→particle/graph consumer 修复并回归全集。既有 984 四点 S4 只作 sentinel，不能替代三项新样本验收 |
| 5 | 所有作者鼠标点击事件必须送达正确脚本 owner，不能只证明移动坐标 | 全 corpus 的 `cursorDown/cursorUp/cursorMove`、button/inside/previous-current consumer；已知事件脚本含 `3122339805:167` | 建立声明→实例化 owner→AppKit hit/input→事件队列→下一帧 mutation/publication→compositor 的全集关联；覆盖按下、释放、移出、窗口焦点/多屏和条件隐藏。禁止新增第二事件总线，丢失或 stale 事件只局部失败 |

**排查入口与关闭门：**指针位置从 App 输入 → frameInputs（pointer provider）→ typed update → 各消费效果；#1/#2 修复后需真实鼠标操作录屏，#3 在修复前后保持正确，#4 独立按粒子公共链验收。`SceneMetalView+SceneScriptCursorInteraction.swift` 的场景转换已含 y=0 顶部映射；`3122339805:167` 的作者脚本消费 `cursorDown/cursorMove/cursorUp` 的 `event.worldPosition`。runner 的合成 `setPointer` 只驱动 provider，不经过 AppKit cursor 事件分发，故 #1/#2 的真实拖拽方向取证仍需要用户鼠标交互；这不阻塞其他不依赖人工鼠标的批次。禁止为修 #1/#2 全局翻转 pointer：`2163522240` 是必须保持的正确方向 sentinel。

**#1 X-Ray专项（2026-09-20 当前态）：**首断点不是全局pointer provider或Y翻转，而是stock X-Ray vertex已经在`g_EffectTextureProjectionMatrixInverse`后执行`*0.5`，host裸`outputMVP.inverse`导致centered local再减半。公共helper现返回`S(2,2,1) * inverse`且不加translation；raw output MVP独立喂给`g_EffectModelViewProjectionMatrix`，避免破坏Shine/COPYBG。独立审查又关闭两个共享回归：恢复layer/particle倾斜平面哨兵，并把model-only consumer从texture inverse requirement移除，避免无关计算和奇异矩阵扩大失败域。真实`2959875782/outfit=4`四象限合成轨迹中三层X-Ray完成binding/GPU/compositor/next-frame并可见方向变化；matrix仍只因既有consumer 79三项失败。`3767343314` Cursor Ripple严格PASS；最终review-fix签名App上的`3113287126` Shine/COPYBG也严格PASS，20个graph layer均GPU/next-frame、0 GraphExecutor failure。剩余门是自动mask中心oracle、真实AppKit鼠标录屏、Puppet screen→atlas及crop/fit/rotation/perspective、previous/button和官方固定同输入；详见[X-Ray projection证据](semantics/runtime-evidence-current.md#e-2026-09-20-xray-effect-projection)。

**#4 粒子/轨迹专项（2026-09-20 当前态）：**公开合同与158个可解析包的census确认CP0是system origin，pointer consumer必须是CP1+；当前唯一显式Sphere/Box pointer-emitter为`3238423642:984`。公共prepared identity、root/strict-child provenance、missing-pointer fail-closed、optional-normal REFRACT与`.0004s`同回调birth/death Sprite sample均已闭合；persistent lifecycle/event/下一advance仍死亡，有限child在新事件准入前退出live budget，Rope/RopeTrail topology不变。连续轨迹批次又修正DEBUG override携带完整`SceneSurfacePointerState`并逐帧覆盖`previous`的第二owner：取证producer现在只提交current/inside/button，previous仍只由产品提交帧推进。真实`cursortrail=2`四点回放中layer 984完成41/41 particle/refraction batches、269/269 instances；四个3024×1964 ROI红色分量质心距注入点`4.18 / 8.42 / 16.62 / 12.99 px`，before/after均无25 px内同类分量，因此current-pointer连续位置与后继显示帧达到有界S4。matrix仍只因既有named-target 348失败；剩余门是previous-pointer作者效果、官方亚帧采样、固定官方同输入与轨迹/密度parity，不宣称完整粒子或S5。详见[trajectory evidence](semantics/runtime-evidence-current.md#e-2026-09-20-particle-pointer-trajectory)、[subframe evidence](semantics/runtime-evidence-current.md#e-2026-09-20-particle-subframe-lifetime)、[pointer evidence](semantics/runtime-evidence-current.md#e-2026-09-20-particle-pointer-emitter)与[optional-normal evidence](semantics/runtime-evidence-current.md#e-2026-09-20-particle-optional-normal)。

**全样本鼠标/指针交互 census（2026-09-22，本批静态证据，未关闭运行缺口）：**当时 159 个 Scene 根（158 个可解析 `scene.pkg`）已按官方输入合同重新盘点；`scene_capability_census.py query --live --domain dynamic-input` 得到 7,733 条动态 occurrence、2,856 个脚本 owner（kind 含 `scenescript` 的 occurrence），快照 payload SHA-256=`cd2f36d4c982fde39711128953ac56f0d9cb31ddb425326e1870ba6ac0abb5b9`。**2026-09-23 按 172 语料重算**：7,921 条动态 occurrence、2,904 个脚本 owner、**351** 个 family key，payload SHA-256=`c29a6e9608abdb1804dcbeb214bf936b4eabb22b4887e18d82940ffc405c93cf`。六个正式 callback 按 172 语料重算为 `cursorEnter` 201/12、`cursorLeave` **208/13**、`cursorMove` **109/24**、`cursorDown` 335/29、`cursorUp` 148/29、`cursorClick` **136/23**（格式均为 occurrence/sample；合计 **467** 个 proven owner、**42** 个样本），`cameraparallax=true` **36** 样本、非零 `parallaxDepth` **82** 样本（2026-09-23；159 语料值为 207/12、108/23、135/22、466/41、30、75）。重算用同一 lexer/遍历口径，且该口径已逐一复现 159 基线的全部 12 个已发布数字后才用于 172（本机忽略证据缓存 `docs/scene/evidence/v1/sample-identity-172-2026-09-23/`：`scan.py` SHA-256 `53e16c0ad0d16972c3b69f0ea0770693cc067943175216b32ba560d0e0a33c05`、`cursor-parallax-bare-id-only.json` `ea1d4b78e8d4fff5e1f5d04b8adfdb0be3639c23bc7e11c7025ab24b93d29472`、`cursor-parallax-172.json` `f5ed849f0248b72445a50f6b1199982163890d549ff4e4ad044c3ad5d470f9bc`）。计数只在进程内存在、未入仓（一次性探针；重建条件=口径变更时重跑同一命令）。旧的“五个 callback”计数已从能力表和 API 表纠正。

静态直读入口另得 `event.worldPosition` 72/25、`input.cursorWorldPosition` 86/11、`input.cursorScreenPosition` 1/1，共 155 个脚本 owner、31 个样本；这些只证明作者声明，不证明 AppKit 命中、事件顺序或视觉执行。gesture 方面没有第七种官方 drag event：拖动是 `cursorDown → cursorMove* → cursorUp` 加 capture/命中状态，`cursorClick` 只在同一 Solid owner 按下并在释放时仍命中才生成。Camera Parallax（30 个 scene-level 开启样本、75 个非零 layer depth）和 Oversized Image scroll 是独立的位移合同，不能与 drag delta 共享一个全局 Y 翻转。

组件/特效分类为：合法显式粒子 locked CP1+ 52 definitions/25 样本；CP0 flags=1 26/14 按官方合同无效；control-point operators 83/35；Rope/RopeTrail renderer 138/41（仅形状，不代表 pointer）；X-Ray 30/29、Cursor Ripple 4/4、Depth Parallax 10/10、Advanced Fluid 1/1。`3554161528` 有完整 cursor down/move/up 与 enter/leave 但另有无效 CP0 资产；`3790726145`、`3792817546` 的轨迹声明都不能由 CP0 推出鼠标跟随；`3769761761` 的 click 与 shine/godrays 是独立声明；`1315486372` 是 Water Ripple，不属于丁达尔 ray。完整表与官方链接见[运行输入与属性覆盖表 §鼠标/指针交互全样本盘点](semantics/runtime-input-property-coverage.md#鼠标指针交互全样本盘点2026-09-22)。

**下一动作：**以该 census 为基线，先追踪一个公共 first-breakpoint（surface pointer snapshot → hit/projection → 六事件队列 → typed mutation/publication → compositor/next-frame），再处理 effect/particle consumer；不按样本、路径、hash 或截图分派，不把静态计数提升为运行通过，不重复已经闭合的 X-Ray inverse、CP1+ current-pointer 或音频 producer 门。

**Q1.5 公共 pointer button-state seam（2026-09-22，本批实现，尚未关闭真实运行）：**`SceneSurfacePointerState` 新增 `setOutside(primaryButtonIsDown:)`，AppKit outside/exit 与带按键的 captured drag 不再把产品态强制清为 `false`；真实 release sample 才清除 primary button。SceneScript event snapshot 与产品 effect 共用这一 typed state，未新增事件总线、provider 或样本分支。`script.tests.test_scene_surface_pointer_event_buffer` 的新反例验证 inside-press → outside-held 保持 `[isInside=false, productButton=true, sceneScriptButton=true]`，outside-release 后三者一致归零；cursor continuity/frame routing/QuickJS 相关 19 项回归通过。该门只证明状态一致性和失败半径，不证明真实 AppKit 命中、拖动方向、视觉 ROI 或官方同输入；下一动作是用同一 public seam 复现 `3122339805` 与保护样本 `2163522240` 的 drag delta/scroll 方向，再决定是否需要投影或作者 transform owner 修复。

**Q1.5a SceneScript author-space world-position seam（2026-09-22，本批实现，尚未关闭真实运行）：**静态读取真实只读包确认 `3122339805` 的两个拖动 owner（`objects` 数组下标 167/180 = 层 id 207/144）把 `CursorEvent.worldPosition` 直接与 `thisLayer.origin` 合成；`2163522240` 不含 SceneScript cursor owner，仍是 oversized/X-Ray 独立消费路径。官方 [CursorEvent.worldPosition](https://docs.wallpaperengine.io/en/scene/scenescript/reference/class/CursorEvent.html) 定义为 absolute world coordinates，当前 renderer 的正交 Scene world frame 为 Y-down，旧出口把内部值直接发布给作者，造成拖动 delta 与作者 origin 的纵向合同不一致。现已在 `SceneLayerCursorGeometry.authoredWorldPosition` 建立唯一出口：显式 `orthoHeight` 时仅把内部 Y 映射为 `orthoHeight - rendererY`，native-perspective 不翻转；`sceneScriptSurfaceInput` 与 owner hit event 共用该转换，不修改 shared pointer、Camera Parallax 或 Oversized Image。新增 `test_scene_layer_cursor_geometry` 正交/透视反例，回归命令与 checkpoint build 待本批冻结后登记。该门只证明 typed 坐标合同和静态作者链，不能代替真实 AppKit drag、原分辨率 ROI、保护样本滚动方向或官方同输入对照；下一步在真实鼠标可用时对 `3122339805` 与 `2163522240` 做同输入 before/drag/after 证据。

### Q1.6 — 光影/光线：共享light intensity publication与真实directional可见链（2026-09-20 当前态）

light专项核对的当时语料（159 目录 / 158 可解析包，2026-09-22）共得15个声明/6样本：9个spot、4个directional、2个point；**2026-09-23 按 172 语料重算为 17 个声明 / 7 样本（9 spot、4 directional、point 类 4 = `lpoint` 2 + 默认项目别名 `point` 2）**，口径=`objects[].light` 字段（该口径已先复现 159 的 15/6；探针见本机忽略证据缓存 `docs/scene/evidence/v1/sample-identity-172-2026-09-23/` 的 `probe.py`/`effect-rows-172.json`）；未知项仍为缺`scene.pkg`的`3766415113`。旧代码只解析spot/directional，`3662790108`与`3287715210`的point声明在document入口即丢失；同时directional与spot各自`.prefix(4)`会让共享模型ABI实际接收超过公开四灯预算的集合。现役修复把Workshop `lpoint`和默认项目`point`别名保真进入document/descriptor，directional/point/spot按作者layer顺序共用总计4灯snapshot，并把point position/radius/color/intensity送入既有static-model Metal diffuse consumer；旧count夹带在ambient/material flags的打包也已撤除，三类计数统一为typed `uint4`。没有按样本/路径分派，也没有新增renderer、light registry或compositor。

独立审查修正后的静态链让三种灯候选统一来自当前作者render order并消费canonical visible set；脚本隐藏、隐藏父级、混合排序/overflow和三个parser严格反例均有执行门，Swift/Metal uniform ABI漂移也在pipeline preparation失败关闭。真实`3662790108`曾证明point连续frame 0...2驱动14个`receivesLighting` static-model draw且同command-buffer completed/error=none，但画面仍停在既有错误启动页，只到`S3 executed`；`3287715210`的point没有static-model consumer，只到typed declaration/wiring。该基线见[点光静态证据](semantics/runtime-evidence-current.md#e-2026-09-20-point-light-static-model)。

后继共享intensity批次已把exact point/spot/directional object的user/SceneScript wrapper统一投影到`.layer(...,.intensity)`，由同一`SceneDynamicSnapshot`发布；启动期对全部light candidate预留color/intensity consumer，真正的灯选择与draw仍逐帧服从canonical visibility，避免作者隐藏或隐藏父链在live显示后失去资格。direct-static snapshot与standalone volumetric spot cone共用同一typed color/intensity helper，旧launch-frozen spot值和renderer第二可见性owner已撤销；standalone plan又以实际实例化的scalar target和穷尽source evidence共同准入，只接受唯一顶层、小写exact `intensity`，大小写变体、额外nested source、`color/visible`和其他host均失败关闭。负数/非有限/超Float输出只拒绝该producer并保留下层fallback。最终候选签名App上真实`3437487219`同一surface/window live切`backlight 0→0.5`严格1/1 PASS，1064/1063/1061 submitted/completed/presented、0 failed；右下地球ROI luma`28.01→44.53`且顶部背景近稳，达到有界`S4 visible`。同App重跑`3662790108`仍有point的3组snapshot/completion和420/419/0 failed frame，但SceneScript owner先以`candidate aggregate source exceeds UTF-8 byte budget`失败关闭、teardown owners=0；该matrix因不要求motion而显示PASS，不能记作动态point或太阳系通过。真实`3768903841`的4个standalone spot均`succeeded`且711/710/709 submitted/completed/presented、0 failed，原分辨率序列可见动态光锥；整份matrix仍因7项已撤权effect专用计数过期而NON-PASS，不能记作整样本通过。

**aggregate-source 审计结论（2026-09-22）：**该预算并非可以直接放宽的真实源总量，而是把同一可见性 binding 同时列入 vector 与 cursor 的互斥候选，造成预检重复计费。现役构造顺序以 vector 的 claimed visibility 集合扣除 cursor 候选；事件型源在 vector 尝试失败后重建为 cursor owner 时只占一份预算，不同真实 owner 仍逐 owner 计费。新增的互斥 vector/cursor 夹具、精确 owner/aggregate 边界、重复源反例保持通过/拒绝分离；14/14 owner-admission tests、`git diff --check` 与 checkpoint Debug build 均通过。真实只读 `3662790108` 在新候选 App 上已不再报告 `candidate aggregate source exceeds UTF-8 byte budget`，但随后命中独立的 `candidate aggregate construction work exceeds 4096`，所以 point SceneScript owner 仍未准入，旧 source-budget 证据不能冒充动态 point 通过；完整运行证据见[本批审计记录](semantics/runtime-evidence-current.md#e-2026-09-22-scenescript-aggregate-source-budget)。

**aggregate construction-work 审计结论（2026-09-22）：**旧实现按每轮全部剩余 owner 预收费，已改为在各 vector/cursor/scalar/string/pass owner 构造即将开始时消费共享 4096 预算；未开始的后续 family、borrowed cursor registration 和 identity collision 不再计费。保留 per-domain `expectedOwnerCount > 4096` 的快速 fail-closed 与 4097 个真实 owner 反例；新增“前 20 个 owner 逐轮早失败、后续 240 个成功”的 260-owner 夹具，16/16 owner-admission、28/28 关联测试和 checkpoint build 均通过。真实 `3662790108` 当前日志显示 7 个 vector visibility owner 各触发一次 fresh-domain 重建后，实际 construction work 到 `consumed=4096/4096`，该次候选每个 domain 的 `plannedOwnerUpperBound=923`；因此这已不是 source-budget 或未执行 family 的重复预收费，仍是失败 owner 污染域后重建 surviving owners 的真实累积成本，point SceneScript owner 继续 fail-closed，不能记动态 point 通过。完整运行与 hash 见[本批证据](semantics/runtime-evidence-current.md#e-2026-09-22-scenescript-aggregate-construction-work)。

**下一公共首断点（2026-09-23 更新）：**同层多目标 cursor owner 已按精确 `SceneDynamicTarget` 准入（8 个同层 collision 归零），借用 owner 的作者 `init` 顺序与 init 值交接已闭合（证据身份、回归、多轮冻结 diff 独立审查记录见[本批记录](semantics/runtime-evidence-current.md#e-2026-09-23-scenescript-cursor-typed-target-ownership)）。真实 `3662790108` 在未命中下述 `3692` 路径的 PASS 运行中复跑为 0 collision、0 `event=cursor failure`、`665/3/341/15` 与批前基线一致（该链为偶发触发，复现身份与计数见 (c)），但 frame 0 的 `cursorOwners=0` 只是该帧 owner-effects 指标，截图仍是启动页，**不能算动态 point、鼠标拖动链或视觉恢复**。下一批顺序：(a) **已闭合（2026-09-23，受控 hover A/B）**：受控对照需要两件 DEBUG 仪器，已落地并各带退役条件——`--mwx-debug-scene-hover-pointer-from-launch`（把合成指针在首帧之前放在请求点；既有行为在 launch 前无条件冻结 outside，受控悬停最早只到 ready 后约 1.4 s，此时 vector pass 已跑过）与一次性探针 `phase=cursor-normalized-bounds`（各 cursor owner 的本地命中盒 `|local|≤0.5` 经同一 `modelViewProjection` 投影回归一化坐标，每 owner 每进程一次）。**标定**：from-launch 固定在 `(-0.2766,-0.2885)` 时 978 首帧盒 `(-0.288408,-0.306510)..(-0.264703,-0.270404)`（与其"info-bar 按钮约 0.024×0.036 归一化、随作者脚本化的信息栏状态移动"的性质一致；24 点盲扫未命中的原因即尺寸）。**A/B**（同 matrix、同参数、同一隔离副本）：消融腿（HEAD − cursor 侧 init 预运行，cdhash `e8d3e1a9…`）978#scale 抛 `multiply of undefined`→`owner is disabled`、指标 664/4；HEAD 腿（cdhash `b175d29f…`）completed 且 enter 命中、指标 665/3。判别只取日志/指标级事实（两腿 report 的像素门差异不可归因）。证据见[批 1 条目](semantics/runtime-evidence-current.md#e-2026-09-23-scenescript-cursor-typed-target-ownership)与证据缓存 `docs/scene/evidence/v1/cursor-hover-978-controlled-2026-09-23/`。**仪器退役条件**：三件（bounds 探针、from-launch 开关、`hover_pointer_from_launch` 矩阵字段）与链 3692 的两条诊断在 (b)(c) 收口时同批删除，不得成为常驻 DEBUG 机制。(b) **capture/inside 与 click 已取到；typed 腿已在另一只读样本闭合（2026-09-23，`3122339805`）；compositor 腿未取得**。用 `--mwx-debug-scene-hover-pointer-from-launch` + 命中盒探针做受控运行（本批 5 次，cdhash `29bcf6b0…`，隔离只读副本；归档 5 次（matrix/report/日志摘录 + manifest，共 16 个文件））：①`layer(978,.scale)` 受控 click（hover `(0.6570,-0.2200)`）取得 `cursorEnter → cursorClick captureActive=1 currentHit=1 → cursorLeave`，作者 `cursorClick` 实际执行、零失败、report PASS；②捕获链在 dock 图标 owner `1481` 上取得 `move(0,1) → down(0,1) → move(captureActive=1,currentHit=0) → up(captureActive=1,currentHit=0)`，即**被 capture 后离开命中区仍收到 move/up**；同窗口 `527` 为 `down(0,1) → up(1,1)`。③反向对照：hover 在 978 idle 盒中心时 press 落到 `527`（无 click，因其未导出 `cursorClick`）；该 run 中 978 的 leave 由 `pointer-state outside` 造成（非作者滑动），"盒已移开"的证据是其后指针回到同一点仍无 re-enter/down。**typed 腿已在另一只读样本闭合（2026-09-23，`3122339805`）**：其 `Numbers Window`(207)/`Eyes Window`(144) 的 `.origin` 绑定在 `cursorMove` 写 `thisLayer.origin = event.worldPosition.add(dragOffset)`（Q1.5a 原记的 "object 167/180" 是 `objects` 数组**下标**：`objects[167].id=207`、`objects[180].id=144`，原记录正确）。受控 drag（hover = 探针盒中心 `(0.5551,0.4537)`，from-launch → drag `(0.35,0.30)`）取得 `cursorDown(0,1) → cursorMove mutation=origin value=1079.470459,717.001953 → 同采样 cursorMove(1,0) local=(-0.497,-2.767) → 0.31 s 后 cursorUp(1,1) local≈(0.0001,-0.0001)`：同一指针位置在突变采样内落在盒外、在后续帧回到盒心，即 typed mutation 经 publication 落到后续帧几何；零 `event:cursor failure`，帧级 `rendered=381 attempts=481 busy=100 dropped=0`、快照非黑。**仍未取得**：compositor/next-frame 的**窗口切片**（只有自 launch 累计计数）；**多步连续 move**（现仪器 drag = press + 单次 move + release，`pointer_trajectory_normalized` 不能带按钮）；原分辨率 ROI、人工视觉验收与官方同输入对照。本样本（`3662790108`）自身在 down/move/up 体内直接写 typed 属性者为 0（typed 写在其同 owner 的 `update()` 内、由回调写入的 `shared` 驱动），故其 3 次拖拽 FAIL 仍只作阳性线索。**工具限制**：命中盒探针每进程只落一次，落点相对作者脚本首轮写入的先后决定采到 pre-script 盒或脚本盒（投影本身无指针依赖），实测差异约 0.48 屏宽，故"按探针盒选 press 点位"不可靠。证据 `docs/scene/evidence/v1/cursor-interaction-capture-chain-2026-09-23/`（manifest + 五运行 matrix/report/日志摘录（含 pointer-state 与探针行）、各 SHA-256）。
(c) `3692` 非有限 world-transform 链：**已定位并由本批收敛第一个 producer**。复现（保留证据，逐目录）：`output-single-owner-final`(cdhash c8fe1567…)/`output-batch-final`(8518db1b…)/`output-grid1`(37d8053f…)/`output-probe1`(27a0624b…) 分别在帧 64/71/138/132 起出现；每次 burst 的 distinct cursor target 为 247（`output-single-owner-final` 为 239），`event=cursor failure` 4845–10994 行（`world transform is non-finite` 22344–53360 行）。根因：层 `1824`（`sykmlogo底层`）的作者一阶平滑 `progress += (target - progress) * speed` 在 `speed*engine.frametime > 2` 时发散（实测 `scriptProperties.speed ∈ {10,15,20,50}`，host 已把 `simulationFrameTime` 夹在 0.25 s，故 speed=15 的倍率饱和于 |1−15×0.25|≈2.75；**慢帧即可触发，不是一次性事故**），约 60 帧后产生有限 Double（批前探针冻结值 `4.67278316e+38`；修复后运行首值 `3.5494512e38`、末值 −2.49479502e46），`SceneLayerDynamicWorldFrameResolver.value` 只校验 Double `isFinite`，`Float()` 上溢成 `±inf` → 该层世界矩阵 NaN → 域级 previous-current。本批修复：该处转换后加 finite 门，超范围即返回 nil（该层回退 authored/静态变换），并留 `phase=dynamic-transform-out-of-range` DEBUG 诊断（退役条件=(c) 关闭时）。**同类点已补齐**：动态层 descriptor 路线的同一收窄 `SceneScriptLayerTopologyProjection.swift` 已在 `dynamicImage`/`dynamicText` 加同一值域门（超范围即返回 nil，runtime 以既有 `invalidArgument("dynamic layer is invalid")` 本地拒绝该 mutation），由真实类型 harness 的前/后对照取证（`docs/scene/evidence/v1/dynamic-layer-transform-range-2026-09-23/`，1e39 从 accepted→nil；authored 不变）。JS ABI 边界（`SceneScriptLayerHandleBridge` 的 mutation 抽取）刻意**不**加门，理由是：①若在源侧**不按 `raw.dynamic` 分流**地设门，会把 authored 的 origin/scale/angles 写入从批 2 已落地的**字段级**读侧回退硬化为 owner 拒绝；②Float 可表示性是渲染 ABI 约束、收窄点就在投影 builder，源侧按 `raw.dynamic` 设门还会多拒绝"携带完整记录但只做销毁"的 `.destroy`（`SceneScriptLayerHandleBridge.swift:555` 允许 `(.destroy, 1, 0)`，而 `SceneScriptDynamicLayerRuntime.swift:310-314` 的 destroy 不构建 descriptor），拒绝面比消费侧大；源侧既有的 `raw.dynamic == 1` 域门约束的是作者可见 API 值域（color 0…1、pointSize 1…1024），不是本约束。**三条批外同类 producer 的可达性判定已完成（2026-09-23，本批）**：全语料扫描（`docs/scene/evidence/v1/authored-numeric-range-2026-09-23/scan.py`，输出 `scan-output.txt`）按 `floatVector` 的接受形态遍历 **171** 个样本（扫描规则=目录含 `scene.pkg`；语料 2026-09-23 实为 **172** 个样本目录 = 159 bare-id + 13 个 `<id>-<uuid>` v2 布局，另 1 个 `3766415113` 的入口是 `gifscene.json`/`gifscene.pkg`、不在该规则内；本批复核其 1 object / 59 数值 / 最大量级 1e4 / 无 cursor 脚本引用，故结论不受影响）scene.json 的全部数值与**全数值字符串**（含 `"x y z"` 多分量）：**超出 Float 范围者 0，但余量为零**——最大作者量级 **3.40282e38**，恰等于 `Float.greatestFiniteMagnitude`，位于 `1300076567/objects[3]/instanceoverride/controlpoint1`（粒子控制点，非世界变换字段）；`>1e19` 者 **1**（即该值）；世界变换相关字段的逐字段上界为 origin 4.3e4、size 3e4、radius 1e5、farz 1e4。据此外推：①矩阵乘积溢出与 ③Camera fov 在当前语料**不可由作者内容触发**；语料最大分量恰等于 `Float.greatestFiniteMagnitude`（粒子 `instanceoverride.controlpoint1`，走 `SceneParticleDefinitionParser` 的 NSNumber 路径、**不经 ② 的收窄点**），但足以否证"作者量级远低于 Float 上限"的假设，故 ② 采取**防御性**落码；② 自身输入面的实测上界为 origin 4.3e4 / size 3e4。②的收窄点（`SceneDocumentLoader.floatValue`/`floatVector`，同时服务作者 vector 字段与 `general.camera.fov`）已加饱和门 `saturatingFloat`（超出可表示范围的有限值饱和到 `±Float.greatestFiniteMagnitude`；真正非有限输入保持原义），行为门 `test_authored_numbers_beyond_the_float_range_saturate`（计数区分饱和与拒绝、JSON/NSNumber 真实通路、in-range 精确、非法 token 与 NaN 反例；去掉饱和即干净断言失败），10 个编译该文件的模块全绿。**取舍登记**：饱和使既有 `isFinite` 消费者（如 `SceneDocument+General` 的 cameraShake 标量、`SceneScriptLayerHandleBridge.validOrigin`）由"拒绝"改为"接受 FLT_MAX"——这正是设计中想要的局部 fail-soft（避免启动级半径），但这两个消费者尚无独立测试。**③ 仍是候选**：`cameraPath.fov` 不经 `floatValue`（走 `SceneDocumentObject` 的内部 `number(_:)` 与 `SceneMetalRenderer+Camera` 的 `map(Float.init)`），本批未触碰，故 ③ 保持未收敛登记。**仍未收敛**：另 `SceneScriptLayerRuntimeDescriptorBridge` 的 16 分量门仍是唯一收敛点，有限 Float 巨值经 `parentWorld*local` 乘积溢出等其它 producer 仍走域级 previous-current；②已由 `saturatingFloat` 加门，残余=饱和使既有 `isFinite` 消费者（cameraShake 标量、`validOrigin`）由拒绝改为接受 FLT_MAX、无独立测试（见证据条目取舍登记）；`SceneMetalRenderer+Camera` 的 `cameraPath.fov.map(Float.init)` 收窄后未查 finite（候选）。**该链会摧毁 hover/拖动证据，因此仍排在 (a) 之前**：本批已落两条 DEBUG 定点诊断（`phase=dynamic-transform-out-of-range`、`phase=non-finite-world-transform`）并把上溢 fail-closed 到 authored 值；未收敛项见上一句。独立门无需新增（本文件 16 分量门 + C 侧 `SceneQuickJSLayerSnapshotHost.c` 的 `!isfinite || fabs > FLT_MAX`）。(d) **已闭合（2026-09-23，官方合同核对）**：官方 Cursor Events 原文 "All mouse cursor events will only work on objects marked as Solid in the layer settings"；effects/passes 非 layer object，现役 `cursorOwnerRegistrations` 只放行 `.layer`/`.text`，**不存在 effect/pass 目标进入 cursor owner 的通路**，与文档一致；新增机器门 `test_pass_owned_cursor_callback_never_becomes_a_cursor_owner`（精确 `passTargets` + pass 值 owner 1 + cursor owner 0 + cursor failure 0；并含 cursor-only pass 变体），effect 侧由既有断言钉住。**同批登记两项**：①`SceneScriptVectorRuntime` 对 bool effect 目标 + cursor 导出**整只本地拒绝**（`invalidSource`，既有 `771d10e4` 引入）——客户端特有 fail-loud 取舍、非文档可推出，语料仅 `3211615441` objects[22] effects[3]/[4] 两处（`effects[3]` 的 `cursorClick` 与 `effects[4]` 的 `init` 写值均与该效果 authored `visible=false` 同值，暂无视觉分歧）；②现役准入**不实施**文档的 Solid 限制，按我方分类器复算 465 个 cursor-owning 层中 97 solid（19 路径 + 78 模型标志）/ 303 non-solid 标志 / 65 无图，**字面实施会让 368 个作者 owner 失声**（含已受控观测事件的非 Solid 层 `3662790108#978` 与 `#1481`），故维持现状并登记分歧，重估条件为双向的固定官方同输入对照。证据 `docs/scene/evidence/v1/cursor-solid-restriction-2026-09-23/`（scan.py SHA-256 `0d7a287d…`、scan-output.txt `90b96415…`）。（此前误举 `3122339805#207/#144` 为失声例证——实为 plain-solid，已更正。）
**Solid 分类事实（2026-09-23 更正，取代原"446 失声"口径）：**`contentKind == "solid"` 的判定是**路径等于 `models/util/solidlayer.json` **或**命中 `solidModelPaths`**；`solidModelPaths` 来自 `SceneAssetCatalog` 读取模型 JSON 的 `solidlayer` 标志，该标志对**内置**模型（`MyWallpaperX/Resources/SceneStockAssets.bundle/assets/…`，如 `solidlayer_depthtest.json` 声明 `"solidlayer": true`）与**工坊内嵌**模型（如 `3662790108` 包内 `models/solid_instance_model_d8460aba.json`）**同样生效**，故 depth-test 与内嵌 instance-solid **都**计为 solid：首版"depth-test 变体被当作 image"及其推出的 446 失声数是分类口径错误，正确值为 368（97 solid = 19 路径 + 78 模型标志 / 303 模型标志 false / 65 无 image）。内置 `models/util/*` 本身不在工坊包内（实测样本 pkg 内 0 条 `solidlayer*` 条目），故模型标志是权威分类，且该分类当前只在实施 Solid 限制时才成为前置。

**(e) 13 个 v2 布局样本的缺口（2026-09-23 新登记，本批）**：语料 172 = 159 个 bare-id + 13 个 `<workshopId>-<uuid>`（App 自身的 v2 安装布局，`SteamWorkshopLibraryTransaction` 写入）。修复发现规则后这 13 个样本已进入 census/能力表/验收台账分母（172/172 可解析、68,762 occurrence、台账 172 行），但它们**仍然没有**：①身份与 fresh 运行证据（当前 **159/172**）；②`scene_sample_debug_archive.json` 归档行（归档仍 159 行，且其 `--report` 输入已随 `/private/tmp` 清理消失，本批不可重建）；③全样本矩阵条目（matrix 仍 45、`added_since_matrix` 127）。**合并条件=全样本以当前身份重跑**（重建 report→archive→matrix 并补齐 13 个样本的身份证据）；在此之前 §六 的全样本验收分母按 **172** 计、13 个按 0 计，不得从分母移除。**跨轨观察（不在 Scene 范围）**：`script/web_system_state_benchmark.py:60` 对 sample id 仍只接受数字，而 App 对 web/video 同样用 v2 布局写公共类型目录；当前 `创意工坊/Web` 根无 v2 目录故未触发，登记给 Web 轨。


**(f) 既有红测（2026-09-23 定位，与本批改动无关）**：`test_scene_material_program_census.test_owned_graph_uses_material_id_preserves_holes_and_skips_program` 在 HEAD `931e3aa4` 上即失败——`scene_material_program_census.py` 的 harness stub `SceneRenderDescriptor.Layer` 停在 `50672fda`（2026-09-19）之前的成员集（只有 `id`/`effects`），而生产 `SceneAuthoredEffectRenderPlanner.swift:50` 自该提交起读 `layer.parentID`/`childLayerIDs`/`contentKind`，故 Swift 切片编译报 `no member 'parentID'`。已由 HEAD 隔离 worktree 复跑同一模块消融确证为既有失败（非本批引入）；隔离 Debug 构建 0 error 说明生产代码本身可编译。**已闭合（2026-09-23）**：stub `Layer` 补上切片所需成员 `parentID`/`childLayerIDs`/`contentKind`/`utilityLayer`（后三者 `decodeIfPresent` 默认值，另加只与 nil 比较的 `SceneUtilityLayer` 替身），该模块由 FAIL 转为 `OK 13.0s`（4 tests）。
**(g) 声明面两处口径缺口（2026-09-23 登记）**：①`runtime-input-property-coverage.md` 的 `Particle control point / trail` 行仍为当时 159 语料值，其中 `Rope/RopeTrail renderer 138 / 41` 用本批两种候选口径（可达粒子文档内 `renderer[].name` 前缀；PKGV 全 `particles/*.json`）分别复算为 46/24 与 47/25，**未能复现已发布值**，故该行不改数、只登记为重测缺口（探针 `rope-renderers.py` 与输出 `rope-renderers.json` 在本机忽略证据缓存 `docs/scene/evidence/v1/sample-identity-172-2026-09-23/`）；②同表 `control-point operators 83 / 35` 与 `CP1+ 52 / 25`、`CP0 flags=1 26 / 14` 同样未按 172 重算（同缺口）。

**用户复核追加（2026-09-20，丁达尔射线专项；2026-09-22 复核）：**静态 corpus 盘点确认 `3769761761` 是 `shine + godrays` graph，`3766387484` 是 `lightshafts` linear `DIRECTDRAW=1`，而 `1315486372` 只声明 `waterripple`（其水波纹 D1 已闭合，不能再归为丁达尔射线）。旧 direct-draw 几何门把 `3766387484:80` 的 `point0.x=-0.00204` 当非法并退回 centered carrier，隔离回放日志为 `normalizedTopInset=0`；现已在共享 `ScenePreparedDirectDrawOutputGeometry` 以水平 `[-0.01,1.01]` 有界 overscan 准入，重新回放同一层得到 `normalizedTopInset=0.219220`，frame0/next-frame GPU completion、publication、terminal compositor 均闭合。该批只关闭 linear/direct-draw authored border-overscan 首断点，不宣称 Light Shafts/Godrays 全部样式、Corner/RAYMODE 1/2、mask/non-direct-draw、官方固定同输入或原分辨率 ROI parity。`3769761761` 的 Godrays/Shine 当前 graph 5-node stages 已执行成功但视觉 parity 未验证；`1315486372` 继续沿 Water Ripple 证据，不作为 ray 样本。下一项仍是按 source-derived shape 盘点 remaining RAYMODE/mask/ordered forms，不能按 effect 名或 sample ID 加分派。
**RAYMODE 形态复核（2026-09-22）：**上一条首断点已进一步在共享 geometry compiler 中闭合：作者 `lightshafts.vert` 对 RAYMODE 0/1/2 共用同一 `inverse(squareToQuad(point0…point3))` prepared source，因此只要 `DIRECTDRAW=1`、RAYMODE 属于 `0...2`、四点 finite/static 且处于既有水平边界合同，就统一准备 half-canvas carrier；不按 radial/corner 名称、样本或 layer 分派。当前签名 App 的隔离 matrix `3287715210`（径向 layer 470）、`3768724269`（角向 layer 100）、`3768903841`（径向 layer 162）三样本均 `PASS`，分别取得 `normalizedTopInset=0.188290 / 0.062380 / 0.250000`，frame0/next-frame GPU completion、publication、compositor 消费闭合。点集超出规范化合同的 RAYMODE 1/2 仍 centered fail-soft；Godrays/Shine 多 pass、mask/non-direct-draw、官方固定同输入与 ROI parity 仍未关闭。

### Q1 — 作者参数、视觉验收与 tracked matrix

| 复现索引（非修复优先级） | 尚未关闭的问题 | 下一次操作与关闭条件 |
|---|---|---|
| 1 | `3747492842`：**两个报告症状都已归因，均非引擎缺陷**——「文字裁切」是参考图视口不可控，「多出缩小版 Leon」是用户 Picture 属性状态差异；「额外闪烁」仍未归因 | 文字裁切：用户截图第 0..65 行纯黑 → 墙纸区域 3024×1898、cover 尺度 0.878704、水平裁切 175.1px，而证据窗口 3024×1964 / 0.909259 / 233.78px（相差 58.7px）；作者 layer 152 左端在我方落 −12.9（被裁）、参考图 +38.4（可见）。该参考图登记 `reference-uncontrolled`，不作绝对位置 oracle；两模型只在尺度上一致（0.878704 vs 0.878799，差 0.01%），平移差约 8px，故绝对落点只到 ±10px 量级。小 Leon：作者 `preview.gif` 含它，它由 **layer 186**（「Picture Import」槽位，`visible.user=visible` 默认 true）绘制；受控运行 `visible=false` 把 ROI（屏幕 (330,820)-(950,1330)）亮像素从 `1.879%` 打到 **`0.000%`** 并目视确认整块消失，与用户关闭或替换 Picture 一致，属属性状态差异。**同批撤回**：早先版本据同一 ROI 断言「嵌套复合层 `visible=false` 不抑制其子树」，该断言已证伪——该 ROI 落在 186 的包围盒（x 473..1451、y 627..1181）内，测的是 Picture 而非 273；隔离 186 后翻转 `leonmovement`，全幅差异 `>60` 为 0.207% / 0.224%，而两次同配置重复的噪声地板为 0.019%，即 **273 的可见性正常**；帧内 DEBUG 观测也显示 `frameVisibleLayerIDs` 确实排除 273 及其全部 7 个子层（layers=21 visible=13）。相关 corpus footprint 数字已作废。关闭条件：本条不再有产品缺陷待修；「额外闪烁」需固定 phase 的动态对照才可裁决。见 [E-2026-09-17-REFERENCE-VIEWPORT-UNCONTROLLED](semantics/runtime-evidence-current.md#e-2026-09-17-reference-viewport-uncontrolled) |
| 2 | `1315486372` 水波纹位置异常 | 先复现与归因，再修公共坐标/采样 owner；不从症状直接指定实现。**复现+归因进展（2026-09-19，队列记录暂不提交）**：15s 回放复现——涟漪以画面中心（人物裙摆）为圆心而非水面区域；效果经 source-proven-graph-input-stage-uniform-passthrough（generic-only）执行、帧零失败。shader 源已从 pkg 提取（shaders/effects/waterripple.frag/vert）：①mask（g_Texture2=masks/waterripple_mask_d057…，slot 2）是涟漪位置权威——texCoord.xy += normal.xy * strength^2 * mask，mask≈0 区域无涟漪；②g_Texture2 注解 default:util/white——加载失败即白即全域涟漪（与症状吻合：涟漪覆盖人物全身而非水面）；③mask UV 为子矩形映射：vert 里 v_TexCoord.zw = texCoord * (g_Texture2Resolution.z/x, .w/.y)——WE 的 masks/ 打包子矩形约定（z/w=子矩形尺寸）。下批二选一定位：(a) 查 mask TEX 是否真实加载（asset state / 回退 util/white 的诊断行）；(b) 查 generic compiler 对 g_Texture2Resolution.zw（非 xy 分量）的 uniform 推导——子矩形缩放缺失时 mask 采样错位/退化。归因落点候选 = mask 加载或 TEX resolution uniform 的公共 owner。探针(b)进展（同日续）：运行时把 g_Texture2Resolution.zw 编码为 candidate.mappedSize（SceneResolvedMaterialUniformEncoder:107-112——xy=physicalSize、zw=mappedSize），与 WE 的 mask 子矩形语义（z/w=子矩形尺寸）对应；candidate 的 mappedSize 来自 SceneTextureLoader+Candidate.swift:154-157（outputGeometry.mapped 或退化为 physicalSize）。下批最终探针：打印 slot2（waterripple_mask）candidate 的 physicalSize/mappedSize 实值——若相等（退化）则查 TEX 解码的 outputGeometry/mapped 对 masks/ 约定的推导；若 mask 未加载（slots[2] 无 resource）则走加载/回退 owner。pkg 侧进展（同日再续）：mask TEX 条目已定位（offset 0x4d3f42、size 0xc42c，TEXV/TEXI/TEXB 三段俱全、约 50KB）——mask 确实打包在 pkg 内；TEXI 字段手工解析未果（词序非 [fmt,mips,w,h]），下批改用生产 SceneTexContainer 解码器或临时 DEBUG 打印 slot2 candidate 的 physical/mapped 实值完成最后一环。生产解码器字段序已核对（SceneTexContainer.swift:180-186：format@18/flags@22/textureWH@26,30/imageWH@34,38，条目以 TEXV0005\0TEXI0001\0 开头）；mask 条目内 TEXV 实际位于切片偏移 681（TEXI@690），按此基址的词序列 (0,512,262144,262144,245760,138240,…) 与字段序不吻合（数值像字节偏移而非尺寸）——疑切片基址仍偏。下批最省路径：不再手解 pkg，直接在 SceneTextureLoader+Candidate 的 mask 加载点加一行 DEBUG NSLog 打印 physicalSize/mappedSize（或 slots[2] 是否 nil），一次回放即得实值并定 owner。**实值已取得（2026-09-19 末批，探针已移除）**：mask candidate physical=1024×1024、mapped=960×540——**加载成功且子矩形不退化**（mapped=TEXI imageWH，masks/ 约定推导正确），两个登记候选根因（加载回退、geometry 退化）**双双排除**。对照官方 preview.jpg：官方涟漪细密、限于水面（人物下半周围）；我方环纹更大、扩散越过衣物——症状实锤但 owner 改指向 **UV 方向/缩放**：嫌疑=mask/normal 采样的 V 翻转（flipY 约定差异会把水面 mask 镜到上方）或 g_Texture0Resolution/g_Ratio 参与的 ripple UV 缩放推导。下批探针：side-by-side ROI 对照（官方 vs 我方）+ 对 v_TexCoordRipple 的 g_Ratio=0.5 与 rippleTextureAdjustment 的 uniform 实值打印。uniform 编码侧事实（同日续）：基图 slot0 candidate physical=mapped=1920×1080（preview log 实证）→ rippleTextureAdjustment=1.777 正确；mask slot2=(1024,1024,960,540) → mask UV 缩放 (0.9375, 0.5273) 正确。两个 resolution uniform 都健康——**rippleTextureAdjustment/g_Ratio 推导排除**。剩余唯一嫌疑：**mask 纹理的内容行序/V 方向**（TEX 解码上传时 row0 约定与 WE 打包 mask 的行序是否一致；水面 mask 若在纹理下半而采样按上半读即越过衣物）——下批探针：解码 mask TEX 后 dump 其 alpha/亮度行分布（上/下半均值对比），与'水面=画面下半'对照；若行序颠倒则修 TEX 上传行序 owner（影响面=所有 pkg TEX，需审查）。TEXI 已按生产偏移净解（缓存文件直接可读：fmt=0、flags=0x2、texture 1024×1024、image 960×540，TEXB0002@46）；TEXB mip 表词形 (256,256,262144,262144,…) 中'offset'量纲超文件大小（50KB），疑为 [w,h,uncompressedSize,?] 或位偏移——像素行分布 dump 未完成。下批省路径：改在生产解码器加临时 DEBUG（mip0 解码后 dump 上/下半均值一行 NSLog），一次回放即得行序结论，避免继续手解二进制。回归二分第一轮（2026-09-19 末批，用户关键线索：位置曾正常、某提交后错位）：9c7b7886（09-12）隔离构建+回放——涟漪仍在躯干（与 HEAD 同错位）→ 回归早于 09-12，mip/上传链四提交（9c7b7886/e277a078/6ce0d7be/970a5645）整窗排除；行序探针在两个 upload 分支均未命中（mask 或走 cachedTexEmbeddedImages 缓存/另一 decode 入口）。下批窗口：08-26→09-12 或改查 effect 槽 purpose/cached 路径。**行序结论已修正（2026-09-20 设计审查推翻）**：此前判定自底向上是通道误读——A 通道全图均匀 255（239=255*960/1024 padding 稀释），作者 mask 内容在 R 通道（blob 行 199-467=37%-87%、峰 68%，shader 采 .r）；基图（同 fmt=0 LZ4 格式）亮斑与官方 preview 三指标吻合证明 top-down 行序。与官方 preview 对照完全吻合，也解释'曾正常'（回归=翻转丢失）。**嫌疑链（下批收口）**：编译器把每个 texSample2D 包成 per-slot UV 变换（MWX_TEXTURE_UV 宏，TextureSamplingNormalizer:109-115）；运行时在 UniformEncoder:113-146 按同一槽位映射事实（sameSlotMappedCoordinateFacts）选 identity 或 candidate.materialProgramUVTransform——mask 槽的 V 翻转归属此决策。下批：打印 slot2 的 xAxis/yAxis 变换实值与 sameSlot 事实归属 → 定翻转丢失提交（git log -S sameSlotMappedCoordinate / materialProgramUVTransform）→ 修复 → 回放验证 → 审查提交。二分第二轮（2026-09-20）：d1b08eac 的父 35d1cea0（08-30）回放——涟漪同样在躯干（与 HEAD 同错位）→ 回归早于 08-30，d1b08eac（identity 决策）排除；更早候选锚点：762f2ebd（08-22 UV ABI 引入 materialProgramUVTransform）、a1d469f3（08-03）、8f4cd308（07-29 uploadRaw 引入）。下批从 08-22 或 08-03 继续二分；亦需考虑 35d1cea0 时代可能本就未正常（用户记忆的'正常'或早于 08 月）。二分第三轮（2026-09-20）：762f2ebd~1=11d6e103（08-22 前，UV ABI 引入前）回放——涟漪同样在躯干错位 → **翻转在 UV ABI 之前就未实现**；'回归丢失翻转'假设对 08-22 后窗口不成立。新假设：用户记忆的'正常'≈更早期该效果未执行/无涟漪状态，效果接通时 mask 行序问题一并显现。收口路径改为直接修复：mask 槽 V 翻转（候选 owner：①per-slot UV 变换的 yAxis 负向——但 materialProgramUVTransform 守卫 yAxis.y>0 需扩合同；②fmt=0 raw RGBA 上传时对 masks/ 形态翻转行序）——须独立设计审查选定并评估影响面（全部 pkg TEX vs 仅 mask 用途）。**D1 已修复（2026-09-20）**：真实根因=SameSlotMappedCoordinateAnalyzer 的 exactVertexOwnership 过度拒绝（waterripple.vert 的 coordsRotated=v_TexCoord.xy 只读引用在映射语句外 → identity 事实不成立 → 编码器二次套用 0.9375/0.5273 子矩形缩放，V 压到 0-0.278）——放宽为只约束写引用（读不改采样值语义），加 readOnlyRippleReuse 正例与 strayWrite 负例 fixture。修复后 15s 回放：涟漪回到水面（人物下半周围）与官方 preview 一致、603/603/0——D2（effect 链 V 反转）不存在，结案。回归 4 组 9/9 绿。影响面=所有复用 v_TexCoord.xy 做涟漪坐标的 stock waterripple 族。 |
| 3 | `2684431262` 紫色块 | **复现+初步归因（2026-09-20，未提交）**：15s 回放复现——画面左右两侧边缘（列 0-10%/70-95%、行 0-75%）出现**纯品红 (255,0,255) 实心块**，ready 窗口即存在（14.5% 面积）。排除项（全部实测）：全部效果 admitted-generic/complete、零路由失败、全部纹理加载 OK（含 2000×2000 底层环/代码层）、authored tint 颜色全为白/青/橙（无品红）、底层环纹理与代码层纹理（27612767，1920×1080 matrix 代码雨，**含 authored 紫色字形**）本身无异常。**定位**：代码层 239（全屏 1920×1080 matrix 雨纹理，唯一效果=effects/perspective）——紫色实心块=代码字形被错误几何拉伸/合并的产物（字形密度高，拉伸后 merge 成实心品红）。**嫌疑=perspective 效果的顶点位移在我们 generic 管线的处理**（官方 preview 中代码雨经透视铺满全屏且字形分离）。下批（已细化，2026-09-20 续）：**shader 源已提取入 evidence（docs/scene/evidence/2684431262-perspective/，本机忽略缓存）**——层 239 perspective 参数 MODE=1、top=0.08/left=0.26/right=-0.49/bottom=0.2；vert 机制：①MODE=1 顶点位移用 `g_Texture0Resolution.z/.w`（mapped 尺寸）+ `mix(-1,1,step(uv,0.5))`（顶点阶段 step/三元）；②**单应 q 无条件施加**（`v_TexCoord.xy*=q; v_TexCoord.z=q` 不在 `#if MODE==0` 内）；③frag 做 `uv.xy/uv.z` 透视除 + REPEAT 时 `frac`。三个具体探针（下批按序）：(a) 顶点阶段 step/mix/数值三元在我们的 frontend 规范化是否覆盖（ed216562 三元规范化是否仅 fragment）；(b) `g_Texture0Resolution.zw`（mapped 尺寸分量）在顶点 uniform 集是否编码（encoder textureResolution case 是否进 vertex 变体）；(c) MWX_TEXTURE_UV 宏包裹透视除后 UV 的相互作用（identity 事实对 vec3 varying + projective 是否成立）。探针(a)已答（2026-09-20）：ScalarArithmeticNormalizer.rewrite 对 vertex/fragment 两阶段同链执行（SourceNormalizer:66-90，TernaryScalarConditionNormalizer 为阶段无关子步），step 在 builtin 白名单（SceneAuthoredShaderVectorConversion:4）且层 239 实际编译执行成功（profile=ordinary-shader、593 帧）——顶点三元/step 覆盖无缺口，(a) 排除。剩余探针 (b) 顶点 uniform 的 mapped 分量、(c) 透视除+frac 与 UV 宏互作。探针(b)进展（同日续）：NeutralTextureResolutionAnalyzer 的两类 fact（packed/separate coordinate）都是**坐标映射专用形状**（要求 resolution 4 引用集中在单条赋值、且是 varying 坐标赋值）；perspective 顶点的位移用法（z/w 参与 position.x/y +=）不匹配 → fact=nil。fact=nil 本身合法（可选），关键剩余问题=编译产物的 vertex uniform 布局是否仍收录 g_Texture0Resolution 且运行时按 (physical,physical,mapped,mapped)=(2048,2048,1920,1080) 编码 z/w——下批从层 239 的 Metal 产物（或 DEBUG 打印顶点 uniform 实值）验证；若 z/w 错成 physical 或缺 uniform，位移幅度错 ~6.7%（2048/1920）——不足以解释整块实心品红，更可能是 (c) 透视除/frac 与 UV 宏互作（q 极值下 frac(REPEAT) 把 dense 字形 merge 成实心）。探针(c)部分完成（2026-09-20 续）：①frac 有 #define frac fract 宏（SourceNormalizer:339）——非缺口；②slot0 双重缩放假设弱化——代码层纹理加载时已裁剪为 1920×1080（preview log 'OK 27612767.tex → 1920×1080'），physical=mapped → UV 变换≈identity，且 same-slot fact 因 .z=q（projective）写入本就不成立但无子矩形映射语句也不需 identity。剩余疑点：(i) 品红是否=**正确数学下的 authored 表现**（official preview 的屏幕宽高比 1.778 vs 我方屏幕 1.539，cover-fit 裁边后强透视边缘的字形 merge 可能本来就接近实心，需与 official 同比例对照）；(ii) 若非 (i)，则需单层隔离渲染对照。下批：用 official preview（1920×1080）等比窗口重放或直接对比我方 ready 快照中心区域与 preview 的同一 ROI 字形密度。定性完成（2026-09-20 续）：**真缺陷**——同分数 ROI（右缘带 x 0.78-0.95、y 0.05-0.70）对照，official preview 0.0% 纯品红 vs 我方 46.2%；细字形在任何宽高比下不可能合成 46% 实心覆盖，宽高比假设否定。且实心（每片元同色）意味着 UV 常量/quad 折叠（MODE=1 位移 529px@right=-0.49 + 单应 q 折叠），而非 frac 噪声。下批：单层隔离渲染层 239（最小化变量），检查 quad 几何是否折叠——候选 owner：MODE=1 顶点位移的 step 分支方向或 mul(x,y) 行主序在我们编译中的展开。微核对（2026-09-20 续）：mul 宏 #define mul(x,y) ((y)*(x)) 与基图 vert 同宏且基图正确——非嫌疑；step(a,b) GLSL/HLSL 语义一致（b>=a?1:0）——非嫌疑；lerp→mix 转换参数序一致——非嫌疑。剩余唯一未验=单层隔离渲染的 quad 几何实测。CPU 仿真定案（2026-09-20 续，authored 参数数值代入 vert 源）：MODE=1 位移量为**纹理像素单位**（texZ=1920/texW=1080 × authored 值）——四角位移 (±154,±529)px；若 a_Position 不是同尺度（WE 惯例=像素空间 quad + 像素单位 MVP），位移主导位置 → **四角 (153,-281)/(-153,529)/(-153,-529)/(153,281) 自交叉（bowtie 折叠）**——与我方实心品红形态吻合；单应 q（1.24-5.13）本身良性。**owner 线索：我们 generic 管线对该 pass 的 a_Position 尺度/MVP 约定 vs WE 像素空间假设**——下批核对 SceneImageLayerPipeline/pass encoder 的顶点装配（像素 or NDC）与 g_ModelViewProjectionMatrix 的尺度补偿。owner 定位（2026-09-20 末批）：**generic pass 的 a_Position 尺度**——SceneMetalPipeline quad=±0.5 单位空间（SceneMetalPipeline.swift:42-45），graph executor 的 authored pass 用 fullTargetMVP=diag(2/W,2/H)（+Validation.swift:432-436，按像素空间位置设计）；像素空间四角（±960/±540）+ 位移(±154,±529) = 无自交叉的剪切梯形（authored 正确形态），而 ±0.5 基座 + 像素位移 = bowtie。**修复方向=authored effect pass 的顶点装配需用像素空间 quad（±W/2,±H/2）匹配 fullTargetMVP，或在 MVP 里补偿尺度**——下批读 SceneGraphResourcePassEncoder 的实际顶点绑定确认哪侧错，修复后回放验证。owner 假设修正（2026-09-20 末批，重要）：读编译器注入的 vertex main（SourceNormalizer:430-452）——authored shader 使用 g_ModelViewProjectionMatrix 时注入 **像素空间** a_Position=（mwxPosition-0.5)*mwxRenderSize（:437-438），authored pass 的 MVP=fullTargetMVP diag(2/W,2/H)——**像素空间自洽**，此前±0.5 quad owner 假设对 authored 路径不成立（那是 SceneMetalPipeline 的 generic 捕获 quad，两套管线不同）。且像素基座+位移仿真无自交叉。剩余未验=①mwxRenderSize 的实值（effect target 尺寸 vs 纹理 mapped 尺寸不一致时位移比例错）②注入 a_TexCoord 的 y 翻转（mwxCoordinates (0,1),(1,1),(0,0),(1,0)）与 authored step 分支的组合③单层回放实测。下批从①打印 mwxRenderSize/uniform 实值开始。①已答（2026-09-20 末批，代码级）：mwxRenderSize=**effect target 尺寸**（Preparation:211 renderSize: CGSize(target.width, target.height)），位移=g_Texture0Resolution.z/w=**源纹理 mapped 尺寸**（1920×1080）——**目标/源两套尺寸确实并存**。位移以源像素为单位作用在目标空间 quad 上：若 target≈3024×1964（全帧 pair），位移相对比例偏小（不折叠但透视过弱）；若 target≈1920×1080 则与 WE 一致。剩②（a_TexCoord y 翻转×step）与③（单层回放）未验——下批一次性回放取 effect target 实尺寸（preview log 或 graph-execution 日志的 targetDescriptors）即闭合。③已答（2026-09-20 末批）：graph-execution 日志实证 layer 239 inputWidth=1920 inputHeight=1080——**effect target 与源纹理一致**，①尺寸错位正式排除（位移比例与 WE 一致）。authored 形态=右缘收束成 ~22px 细条（±(540-529)）的强透视梯形——**唯一剩余嫌疑=②注入 a_TexCoord 的 y 翻转**（mwxCoordinates (0,1),(1,1),(0,0),(1,0) 把 authored step 的 y 分支上下对调 → 位移 dy 符号翻转 → 上下缘互换 → 收束缘出现在错误一侧/或与单应 q 失配）。下批：对比注入坐标系与 WE a_TexCoord 约定（y=0 顶 or 底），修 mwxCoordinates 或 mwxPosition 的 y 映射。②已排除（2026-09-20 末批）：注入 mwxCoordinates=(0,1),(1,1),(0,0),(1,0) 与 generic 管线 SceneMetalPipeline quad 的 texcoord 逐角**完全一致**（(0,1)/(1,1)/(0,0)/(1,0)，SceneMetalPipeline:42-45），且基图 authored shader（genericimage2）走同一注入渲染正确——注入 a_TexCoord 约定与全引擎自洽且被基图证实为 WE 约定。**至此全部命名嫌疑（三元/step、frac、slot0、mul/step/lerp、±0.5 quad、尺寸错位、y 翻转）均排除**——剩余唯一工具=单层隔离渲染（fixture 只留层 239）直接观察 authored 位移路径的输出，下批执行。单层 fixture 已建成并完成参数扫描（2026-09-20）：原地改包（pkg 内 scene.json 替换为 159+239 两对象派生版、表内 size 字段同步、绝对偏移不变）——两层 fixture 173/173/0 正常运行且紫色块单独复现（排除层间互作）。参数对照（免构建改包重放）：authored(top=.08,bottom=.08,left=.26,right=-.49)→两侧大块品红；right=0→品红移到左侧 20.9%（跟随 left=.26）；all-zero→品红 0.0%。MODE=1 顶点位移为唯一触发器实锤。下批两个收口探针：①查编译后 Metal 源的 a_Position 来源（注入 prologue mwxCoordinates 与 SceneAuthoredShaderMetalSource:145 的 vertexID 生成两套 quad 机制是否叠加/未生效）；②宽松滤镜测源纹理字形色（若 (200,50,255) 级则品红=放大字形、bug=几何过大；若纯 255,0,255 则另有来源）。②已答（2026-09-20 续）：27612767.tex 字形主色为 (192,192,48)黄白/(192,48,96)品红系/(192,96,144)，宽松滤镜 r>200,g<70,b>200 计数 **0**——**纯 (255,0,255) 不可能源自纹理采样**（源里最饱和品红仅 (192-223,48,96-144)）；all-zero 对照运行画面=正确多彩代码雨（灰/橙/绿/紫字形）——渲染链本身健康。纯 255,0,255 且跟随位移参数出现 ⇒ 指向**几何折叠区域的越界/未初始化读取或渲染器生成色**（非 authored 内容）。①成为唯一剩余收口探针：读该 pass 编译后 Metal 顶点源验证两套 quad 机制的叠加/未生效——下批第一动作（从 artifact 缓存或 DEBUG dump 取 Metal 源）。①部分完成（2026-09-20 续）：Metal 路径只有**一套** quad 机制——vertexWrapper（MetalSource:143-165）按 vertexID 查同一张坐标表 (0,1),(1,1),(0,0),(1,0)，vertex 引用 g_MVP 时 a_Position=(position-0.5)*mwxRenderSize（targetPixels 分支，emitter:141-144 判定）——GLSL 注入与 Metal wrapper 是等价两路径非叠加；用 wrapper 实际 texcoord 重算 MODE=1 四角=（-806,-259)/(806,-1069)/(-806,259)/(806,1069)——**非交叉的剪切平行四边形**（y 超出目标被裁剪，正常）。spin_mask 纹理（1000×1000 灰度 192/0）也零品红。**剩余唯一未验=g_Texture0 在该 pass 的实际绑定与 MWX_TEXTURE_UV 路由**——需读编译后 Metal 源（artifact dump 或缓存）看 slot0 绑定对象与 same-slot fact 不成立时套用的 UV transform 实值；纯 (255,0,255) 仍无来源解释，最可能=该 pass 绑定的纹理/变换把采样引到未初始化区域。下批：跑一次带 execution-observations 的 fixture 回放让 evidence 目录写出 .metal/.glsl 编译产物，读该 pass 的绑定与 UV 宏展开。①完成（2026-09-20 续，产物已归档 docs/scene/evidence/2684431262-perspective/，等长字节微扰强制 fresh compile）：编译 Metal 证实——单 quad 机制（mwxGenericVertex + _19 坐标表）、a_Position=(mwxPosition-0.5)*mwxRenderSize 像素空间、MODE=1 位移逐行忠实、gl_Position=M*v（与基图一致的列向量约定）；**fragment 采样链=mwxTexture0Coordinate（Transform0/1 仿射）→ fract(透视除后 UV) → g_Texture0.sample**。剩余最后一步=**Transform0/1 的运行时实值与 slot0 实际绑定纹理**——same-slot fact 对该 shader 不成立（vec3 projective 写入），编码器将套 candidate.materialProgramUVTransform()；若 slot0 candidate 是 effect 输入 framebuffer（尺寸=目标 1920×1080 或全帧）而 transform 非恒等，fract 后 UV 进入未采样区/越界。下批：DEBUG 打印该 pass Transform0/1 实值+slot0 绑定身份（一次回放）→ 归因 → 修复。实值已取得（2026-09-20 续，T0PROBE 留在树）：transform=**恒等**（t0=(0,0,1,0) t1=(0,1,0,0)）、phys=mapped=1920×1080、purpose=premultipliedColor、sourceOwns=0——**UV 仿射链排除**。至此 authored 顶点/采样/变换/纹理内容全部干净，唯余**slot0 绑定纹理的实际内容/尺寸**：pass encoder 绑定的 g_Texture0 若是共享 pair/全帧纹理（尺寸≠1920×1080），shader 按 0..1 UV 采样会读到层内容之外的区域（未初始化 pool 数据=纯品红垃圾的可能来源）。下批最终探针：在 pass encoder 打印 slot0 绑定纹理的 width/height 与内容首像素（或对比 pool 纹理分配路径的初始化），一步定品红来源。B0PROBE 定案（2026-09-20 续）：slot0 绑定=SceneSharedPairB sharedPair **1920×1080**——尺寸正确、非全帧纹理，越界读假设排除。至此**整条运行链全部实证干净**（几何/UV/transform/纹理尺寸/源内容），纯 (255,0,255) 只能来自 **pair 纹理内容本身未被写入的区域**（共享 pair 为 pool 回收纹理，源捕获若未覆盖全 pair，残留=先前内容的纯品红像素）。下批收口方向：①读回 pass 时刻 pair 内容（readback 或再探针）确认未写入区=品红；②或直接审 pool/pair 分配与源捕获覆盖策略——若捕获 quad 未覆盖全 pair（如裁剪后 quad 只写部分），修 source capture 覆盖或 pair 分配 clear。结构性证据已闭合（2026-09-20 末批）：sharedPair 由 device.makeTexture 直接分配（SceneOffscreenTexturePool+PersistentGraphTargets.swift:196-201，storageMode=.private、**无任何 clear/初始化**）——pair 未写入区=任意显存残留（本机呈现纯品红）。因 fract 保证 UV∈[0,1)，品红⇒pair 的 [0,1) 域内有未写入子区（源捕获未全覆盖或写入了子矩形）。**修复方向（需设计审查二选一）**：A) pair 分配时一次 clear（简单但每对加一次 pass 开销，且治标——依赖未写区被读的形态不再出现）；B) 修正源捕获/基图写入使覆盖全 pair（治本——找到为何 1920×1080 autosize 基图未覆盖 1920×1080 pair 的子区，可能是捕获 quad 的 UV/几何映射只写部分）。下批先做一次 pair readback 定位未写子区的形状（左侧/环形/子矩形），据形状直接指向写入方。再核对（2026-09-20 末批）：encodeSourceCapture 的 drawLayer 用 fullTargetMVP=diag(2,2)+±0.5 quad=全目标覆盖、基图 autosize 1920×1080=同尺寸——**理论全覆盖**，与'未写子区'矛盾；另一可能=**A/B 双成员错绑**（捕获写 A、pass 读 B，首帧 B=未初始化）或基图 launch-prepared 走了不同写入路径。all-zero 位移时 q 恒等（全零参数下 p0-p3=单位方形）uv 不动=正确——与'pair 已含正确源'一致，与'有未写区'并存只能解释为**写入覆盖是时序/成员相关**（帧内先读后写或成员错配）。下批唯一决定性测量：pass 前 pair readback（两成员各一次，首帧）+对照捕获写入成员。成员映射事实（2026-09-20 末批，代码级）：allocator 把 zeroSlot→pair.first(A)、oneSlot→pair.second(B)（ScenePersistentGraphTargetAllocator.swift:121-122）；B0PROBE 实测 pass slot0 绑定=**SharedPairB（one 成员）**；pair plan 的 current 成员从 .zero 起（transitionCount 偶数，SceneLayerFullFramePairPlan.swift:108）——**首帧源捕获写 A(zero)、pass 读 B(one) 的成员错绑高度可疑**。下批决定性测量简化为：打印捕获写入的目标纹理 label（一次回放）——若=A 而 pass 读=B 即坐实错绑；修复=pass 输入绑定取 current 成员（或捕获写 opposite）。CAPPROBE 定案（2026-09-20 续，**成员错绑证伪**）：捕获写入目标=**SceneSharedPairB** 与 pass 读的成员**相同**——同帧同成员，错绑假设不成立。剩余两个假设：①**帧内编码顺序**（pass 编码在捕获之前→读未初始化/上一帧内容；两探针 NSLog 同毫秒无法从日志序判定，需序列计数器）；②**捕获源本身含品红**（CAPPROBE 显示捕获 from source=**unlabeled** 1920×1080——非带 label 的代码纹理，其身份与内容未验，若为另一中间纹理可能携带品红）。下批：探针加全局序列号区分编码序 + 打印 unlabeled 源的 identity/label 归属。编码序定案（2026-09-20 续，**时序假设证伪**）：CAPPROBE seq=1（.527 捕获）先于 B0PROBE seq=1（.528 pass）——捕获先编码先执行，pass 读的是本帧已写入的 pair。成员错绑与时序双双证伪后，**唯一剩余=unlabeled 捕获源（1920×1080）本身含品红**——最可能是 launch-prepared 静态基纹理（同为 makeTexture 无 clear）：若基图准备只写了部分区域，未写区品红经捕获复制进 pair，位移后的 UV 读到即显色。这也与 all-zero 干净一致（恒等 UV 只读已写区）。下批最终探针：给捕获源加 label/或读回源纹理若干像素验证品红 → owner=基图准备的覆盖/clear → 修复同源捕获策略。追踪进展（2026-09-20 末批，未闭合）：unlabeled 捕获源的归属链=ScenePreparedBaseImageResources（ScenePreparedDeviceResources.swift:25/206-229，entry.outcome 经 textureLoader 常规加载——若为常规 load 产物则纹理内容=已初始化资产数据，'部分写入品红'假设可能也证伪）。**诚实状态**：本切片取证已横跨 12+ 探针/实验、全部命名假设证伪或排除，唯一未做的决定性测量=直接读回 unlabeled 源或 pass 输入纹理的像素（全链静态分析已到极限）。下批建议：改用 GPU readback 一次性测量（pass 前对 pair 做 blit readback 数像素）或提交当前探针链交独立审查（审查可能从静态分析发现被忽略的环节）。GPU readback 探针已加（2026-09-20 末批，RBPROBE：pass 编码点对 pair 8 行 blit readback + completedHandler 打印首/中像素）但首轮回放**未发射输出**——completedHandler 异步打印可能晚于进程退出或被 blit-in-front 编码序影响，需一轮修正（改为下一帧编码点同步打印或 waitUntilCompleted）。同步化尝试失败并已回退（2026-09-20 末批）：scheduledHandler 内同步 wait 死锁帧提交（submitted=0），readbackSample 已整体移除（B0/CAP 探针保留无害）。**结论：编码线程内做同步 GPU 读回不可行，剩余路线=独立审查取证链**（把 12+ 实验/证伪/矛盾数据交审查代理，从静态分析找被忽略环节），或离线诊断工具（专用 DEBUG runner 在帧间 GPU 空闲时读回）。**已收口（2026-09-20 末批，修复+验证+提交）**：独立只读审查定案根因——**此前 12+ 探针全部盯输入侧，输出侧从未被检查**：material pass 的输出 target 是 pair 成员 A（fixedTerminalMember=.zero→pairTexture=.first；B0PROBE 实测的 slot0=B、CAPPROBE 捕获写 B 全是输入侧），`SceneResolvedMaterialPassEncoder.encode` 对输出 target `loadAction=.dontCare`，其合法性建立在 `supportsResolvedMaterialFullscreenOverwrite`（SceneMaterialRenderState.swift:97-103，只验 blend/depth/cull/alphaWrite 管线状态）**不可证明的"几何覆盖 100%"假设上**——authored 顶点位移（perspective）使 quad 覆盖<100%，未覆盖区=未定义附件内存（Apple GPU 确定性填充纯品红）；成员 A 由 makeTexture(.private) 分配且无任何 clear，首帧即暴露。全部实证吻合：all-zero 覆盖 100%→干净；right=0 品红跟随 left（左缘两角竖直收缩 26% 的未覆盖楔形）；捕获全覆盖的是 B 与 A 无关；读写同纹理 hazard 被双重 ObjectIdentifier 守卫（PassEncoder:376/396）排除；品红位于 fragment 根本不执行的 quad 外区域（非采样色）。**修复**：color 内容 pass `loadAction .dontCare→.clear(0,0,0,0)`（premultiplied 透明，合成透出底层；data/scalar 附件保持 .dontCare 原语义；tiler 上 clear 无额外带宽）；T0/B0/CAP 三探针同批移除。**验证（隔离回放，3024×1964）**：fixture authored 品红 14.5%→**0.0%**，中心覆盖带与修复前**逐位相同**（diff=0.0，证明修复只影响未覆盖区）；all-zero 对照不变；全样本 2684431262 20.8%→**0.0%**，右缘收窄的透视代码雨显现（官方 preview 形态）；回归 2163522240（竖长图正常）、2998757800（水波纹涟漪正常）均 0.0% 无异常。 |
| 4 | Scene Bloom enable/threshold 尚无完整 live consumer | 沿属性 producer→typed channel→全场 post consumer 闭合；若仍 unsupported 则保留待解决边界，不据此关闭全集目标 |
| 5 | 全 corpus identity-only matrix 与人工视觉复核 | 按路线 P0/P4 维护；人工重新观看后才改 verdict；每批先定向，公共能力关闭复测全部影响集合，最终候选全集复测 |

已关闭的 Puppet、双视频、Water Waves mask、TextureAnimation 等断点不再占队列行；新回归必须建立当前复现后重新入队。其余 authored target 按实际 consumer 缺口拉入，不再把已接通的整个 script/camera/particle family 标为缺失。

### Q2 — 稳定帧性能、长稳与发布

性能与资源批次排在架构完整性和真实样本画面正确性之后；只有崩溃、OOM、预算硬拒绝或已测明显阻塞正确运行时才提前。其余按重构计划 E0 基线与测量进入 E1/E3/E4，P5 只拥有最终验收。维持作者分辨率和正确构图，不能恢复缩小纹理等错误行为换取帧率。

## 3. 观察项与能力边界

异步 provider 的短暂 not-ready 按局部 previous-current 后自然恢复；持续不恢复才登记缺口。Puppet 跨层 geometry provider、IK、完整 3D 等能力按专项合同和真实需求拉入，不恢复压平纹理捷径。

## 4. B1–B9 退役索引

已完成批次的证据只按需从[历史索引](../history/README.md)追溯，不再作为当前任务。这里不复制 PASS 数、旧命令或退役 owner 清单。

## 5. 队列维护与批次门

每项只保留问题、下一操作和关闭条件；完成后从表中移除，证据写回唯一台账。落代码遵守[开发工作流](development/development-workflow.md)，一次闭合一个完整职责并完成相称验证；提交仍需用户授权。

当公共首断点关闭、仅剩路线系统性验收时，将本文归档，取消派生队列。
