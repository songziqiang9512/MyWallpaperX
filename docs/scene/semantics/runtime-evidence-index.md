# Scene 运行证据索引

> 状态：现役证据入口
>
> 最近核对：2026-07-29
>
> 实现基线：`e315743`

本页给覆盖表中的 `L3` 子集提供可追溯证据包。每个证据包至少包含代码、自动测试和真实运行或 GPU 证据；缺少任一项的能力只能标 `L0-L2`，或在专项表中明确写 `gate incomplete`。`.codex` 报告是本机隔离运行产物，不提交 Git；报告路径、App 身份和摘要写入现役文档，避免将其误当源码 fixture。

## 1. 当前统一门

| 项目 | 当前证据 |
|---|---|
| 当前完整快照门 | `e315743` 的最终签名 Debug App 使用 2026-07-29 从真实只读目录重新构建的 45 样本隔离副本，跑 `.codex/scene-full45-audio-bars-fisheye-20260729-v1/report.json`，**45/45 PASS**；基础 image texture `451/451`、particle `130/130`、REFRACT root batch 14、strict stage 294、chain 53、Audio Bars 4、Fisheye zero-distortion 1，graph failed/failed frames/drawable miss 均为 0。报告 SHA-256 `5c6b58f6ac017ea40b5ef0a9389d1024b25677c01f6ac3942ec9e852dbfb19fe`，full matrix SHA-256 `697414b052c26afdb26af76cbaa1f9a0873a18e5b423756d17aa3227b40ac6f7`；没有统一性能阈值，最低 driver FPS 为作者/宿主目标约 30 FPS 的 `3769688830`，故 PASS 不代表全量性能健康。 |
| 最近固定回归门 | 同一 RopeTrail 共享渲染构建跑 `.codex/scene-fixed13-ropetrail-20260729-v1/report.json`，**13/13 PASS**，image texture `242/242`、particle `76/76`、REFRACT root batch 7、strict stage 133、chain 18、graph failed/failed frames/drawable miss 均为 0；报告 SHA-256 `84e9d44e56c839705ffc737d59d1cb5b4834e1d78000c87d236a3a7f72c944bb`，fixed matrix SHA-256 `a06aa14ad42abf6f27d1578e1bf053b79f7386f95370f6ee28cbf89a9de297f0`。fixed13 不含三个 RopeTrail 目标样本，只证明共享 Sprite/Trail/viewport 路径在另一组样本未回归；不能替代 full45。 |
| Clipping Mask 定向门 | `27a5664` 的签名 Debug App 跑 `.codex/scene-clipping-mask-targeted-20260728-v2/report.json`，`2902406982`、`2938612768`、`2974757317`、`3768229922` **4/4 PASS**；前两者 Clipping Mask 分别为 7/1，后两者因 composition consumer 边界保持 0。报告 SHA-256 `5149fae35232bf8a84654983de59ec65ff8ed730394ec3c50449075aa51c0a31`，full matrix SHA-256 `7ee346bd5d227f14dfc55778eff6638895bfcc23f5b817fe25fd7827ed3ccc8f`。 |
| Cursor Ripple 定向门 | `95bd0c0` 的最终签名 Debug App 跑 `.codex/scene-cursor-ripple-targeted-20260729-v5/report.json`，`2974757317`、`3299228616`、`3767343314` **3/3 PASS**；每个样本 Cursor Ripple 1、isolated 1、GPU succeeded 1、failed 0，报告逐层锁定 omitted sibling。报告 SHA-256 `b2b4bd134cb5f8ccfe12a809483096a51d8ef21cbe96fbbded598e10bec61949`，matrix SHA-256 `691b7e2fb1711351f51a93223e881977926ef36ea05f6f7e3a4df3ec1f29467b`。 |
| Shine 定向门 | `78b30cd` 的最终签名 Debug App 跑 `.codex/scene-shine-targeted-20260729-v3/report.json`，`2241938645`、`3767232084`、`3769364482`、`3769761761` **4/4 PASS**。legacy `2241938645` 保持 Shine 0；三个 modern 样本合计 Shine 6、isolated 3，`3769364482` 的 Timeline 动态实例与所有 unsupported sibling 逐项保留 omitted diagnostics。报告 SHA-256 `6f908d78e8b2f96524808abfa5c617ec69b0c27c81b25cfa5a3f0f96d7aaf71a`，matrix SHA-256 `d5d81fa0abd919fa3390df1b529ddbe8a672b62edddb08fed6b6fc43512ba66a`。 |
| Image/Solid alignment 定向门 | `88510a6` 的签名 Debug App 跑 `.codex/scene-image-alignment-targeted-20260728-v1/report.json`，`3122339805` 与 `3767460992` **2/2 PASS**，均 loaded 1.0、failures 空、sample-root residue 0、签名前后 identity 不变；报告 SHA-256 `f66cb394e9172fd7ce00126ada36af6bcd5a54b8603ca4d7e4f7cd85912722a8`，full matrix SHA-256 `f198ab106341a46530806e40219181176ac61d98edbdd7bff2d1f3032071c998`。人工对官方截图核对：`3122339805` 的左侧背景与窗口框从整体向内/向下错位恢复到作者边界，`3767460992` 的 X 面板从左下偏移回到父内容中央；这只验证可见方向与锚点几何，不证明两个样本完整还原。 |
| Light Shafts 定向门 | `c47fe22` 的签名 Debug App 跑 `.codex/scene-light-shafts-continuity-targeted-20260729-v3/report.json`，`3299228616`、`3766387484` **2/2 PASS**；报告 SHA-256 `cc2cb3bbc74620253c9dd7168ab1bd94e47e2fa359b5a6f3fca1a1cb9e10209b`，matrix SHA-256 `21b4de5142f36708e02018c799ee0495518aa55dfaf2eb2f11ff85cc296cdb97`。两者各执行 1 个 strict Light Shafts；前者 chain/stage `1/4`、succeeded `[246,303,601]`，后者 direct-draw chain/stage `0/1`、succeeded `[80]`，failed layers/frames 与 drawable miss 均为 0、loaded 1.0。另以 6 秒 after delay 对 `3299228616` 跑 `.codex/scene-light-shafts-continuity-longphase-3299228616-20260729/report.json`，**1/1 PASS**，报告 SHA-256 `02661e6d0bddd206d757f0281c24a1f3787803323589f230e8a07c7e00933624`，after 帧仍有多束双侧光带。`38cd1d3` 的旧 5/5 门仍提供 Color/Corner/Radial 三个运行负例，但没有在本构建上重跑；当前自动门继续锁定这些路径 fail closed。 |
| Shake/Godrays/长链定向门 | `f26aae5` 的最终签名 Debug App 跑 `.codex/scene-shake-godrays-targeted3-20260729-v3/report.json`，`1937925563`、`2067939514`、`3767232084` **3/3 PASS**，报告 SHA-256 `063c0531f877442166b53af427aadc6edd2b6bfc79eb3eb83e68854d50ab4846`。三者 strict stage/chain 分别为 `48/12`、`11/3`、`7/1`，failed frames 均为 0；第一轮完整门发现的 206 透明 Tint 后接 Blur 白块已由 premultiplied-safe Tint 修复，最终截图恢复黑色圆形主体。 |
| Pulse/Water/Iris/频谱定向门 | `8f4cd30` 的签名 Debug App：`.codex/scene-193-spectrum-detail-targeted-20260729-v5/report.json` 的 `1937925563` 执行 `Pulse -> Water Ripple -> Godrays` 后接既有频谱链，报告 SHA-256 `0d1e5f9841af16137577b5dcae20bd69c129a511adfa5c61d11cca6535f0c5d3`；`.codex/scene-2470144420-water-targeted-20260729-v2/report.json` 锁定该样本旧版 Water Ripple 动态，报告 SHA-256 `359c7ac72700005649a954dac893a8124c051b1150ce167e97bdb8472ce0ceff`。最终 full45 中 `1937925563` 为 Pulse 1、Water Ripple 1、Godrays 13、chain/stage `13/51`；`2470144420` 为 Water Waves 1、Foliage 3、Water Ripple 1、Iris suffix 1、chain/stage `1/5`；`3742133044` 为 Water Flow 1、Foliage 3、Iris suffix 1、chain/stage `1/4`，三者 failed layers/frames 均空。 |
| Particle Turbulence 定向门 | `7b1d36f` 的签名 Debug App 运行 `.codex/scene-turbulence-targeted-20260729-v1/report.json`，`2131872317`、`3299228616`、`3757555836`、`3769688830`、`3769761761` **5/5 PASS**，texture `loaded_ratio` 均为 1.0、failed frame 0、签名前后 verified；`3299228616` 的 particle root 仍为既有 `6/7`，RopeTrail layer 275 未加载，本批不把 5/5 写成粒子全载。报告/full matrix SHA-256 为 `8a713218c73a264a8a15ee9dd7460136d37201444a5f1751552aafe7355b67dc` / `21b4de5142f36708e02018c799ee0495518aa55dfaf2eb2f11ff85cc296cdb97`。旧 full45 的 8 样本/19 个当前加载图层中，本门覆盖 5 样本/14 个 Turbulence 图层且新日志不再含 `unsupportedOperator:turbulence`；另 3 样本未在新实现上复跑。 |
| Particle 分布 / sampler / mip 定向门 | `d19e76b` / `d792296` / `f4e8a0c` 的公共实现分三层核验。TEX sampler 中间构建 `.codex/scene-particle-sampler-after-20260729/report.json` **4/4 PASS**（报告 SHA-256 `c5f76eb9a3ab51178162ecfa576d450a873b0accd6094252a2786e9f2a759fea`）；分布/振荡构建对 `2470144420`、`1937925563`、`3742133044` 分别 **1/1 PASS**（报告 SHA-256 `dd5f246c7c8d37f715460ae603a72ed690c08ae3e9cf3076af024e51a4bcbc88` / `e769a4df2be4965dead48cf741bb809f2de7aabcada43b66443cceebb28bb4ce` / `551cb1b7a3e5c2e90aaf2cac13b3465f9494d32eaca8f7fbd1befacacb129b4a`）；最终 mip 构建对 `2470144420`、`3742133044` 分别 **1/1 PASS**（报告 SHA-256 `e87263966e9c7a9907b01c951c4d981e77ebb29e5a410df6623989d35683f537` / `231574a173c27152befc7189885964084e05f657ed1815a1048a63aad3da0332`），两次均 texture loaded ratio 1.0、failed frame 0。没有重跑 fixed13/full45。 |
| Particle Position oscillation 定向门 | `5911049` 的签名 Debug App 运行 `.codex/scene-particle-position-oscillation-targeted-20260729-v1/report.json`，`2067939514`、`3088601835`、`3299228616`、`3742133044` **4/4 PASS**；texture loaded ratio 均为 1.0、failed frame/drawable miss 均为 0，particle root 分别 `1/1`、`19/19`、`6/7`、`1/1`，其中 `3299228616` 的既有 RopeTrail layer 275 仍未加载。报告/full matrix SHA-256 为 `aab1936e4f37df31659801b66064ab8c8f3a40a759e046caa82d0b457eeacffb` / `21b4de5142f36708e02018c799ee0495518aa55dfaf2eb2f11ff85cc296cdb97`；该批没有重跑 fixed13/full45。 |
| Particle RopeTrail 定向门 | `aa68bb8` 最终签名 Debug App 运行 `.codex/scene-particle-ropetrail-targeted-20260729-v5/report.json`，`3299228616`、`3743305891`、`3770444459` **3/3 PASS**；particle 分别 `7/7`（新增 layer 275，ready initial 20）、`1/1`（layer 26，ready initial 116）、`5/5`（新增 REFRACT layer 48，ready initial 0），failed frame/drawable miss 均为 0。报告 SHA-256 `e87b1f56498aa5e9c02040f6acc44c1ca758c3ab98bbd531f4b21d6c341fffd8`，正式 full matrix SHA-256 `433570bb2554b10c034e638a4086d94f90d04d1bb4099f00dafa17bcb4c6ce6b`。这是最终 parser fail-closed 收窄后的当前二进制证据；whole-scene 单帧不能证明 initial 0 瞬态层、死亡尾迹或官方动态等价。 |
| Audio Bars/Fisheye 329 定向门 | 实现批次的签名聚焦构建在隔离 Workshop root 与临时 HOME 运行 `.codex/scene-audio-bars-329-default-20260729-v1/report.json`，默认属性 `3299228616` **1/1 PASS**；utility layer `151` capture planned/succeeded `1/[151]`，graph succeeded `[151,246,303,601]`，Simple Audio Bars 1、Fisheye zero-distortion 1、chain/stage `2/6`、failed frame/drawable miss `0/0`。报告 SHA-256 `16a1a02eea6d9d4972164ae573ab079218ea3a9c9bbc92f3aa4201943d77a0a8`，定向 matrix SHA-256 `910713aa662c38bf1ed6fe2e834717f12ad8079c723fb75c51ad657b0648797c`，executable SHA-256 `787f048202764939d50ece0c5640c1f8eec79f1388e64f6d1d0468588efeeabd`，CDHash `0093a7c4f89392c3b52f78012abd7ac43cdda77d`，签名前后 verified；同一执行合同随后由当前最终 full45 再次覆盖。系统输入为静音，截图不能证明动态条形或 WE 视觉等价；非零左右不对称、峰值大于 1 与混合语义由确定性 GPU 门覆盖。 |
| 历史 static-origin 定向门 | `.codex/scene-static-origin-targeted-20260725/report.json` 为 1/1、particle `17/19`（当时口径）；`3088601835` 在当前门下为 `19/19`，matrix-code 两层随 `b86db59` 解除 nested 阻断；`snowstormfog` child 仍由真实缓存门确认执行 |
| 最新合同门 | 生产播放为 raw project root -> in-memory `SceneRuntimeInput`，Debug runtime evidence schema 1。`e315743` 的 relocated stereo Audio Bars / Fisheye 聚焦与共享门纳入完整 Scene discover，**693 项通过、2 项按既有环境条件跳过**；代码健康 640 Swift files、44 locked legacy files、400-line limit 通过，签名 arm64 Debug build 与严格 codesign 通过。当前 full45 是该最终提交态二进制的重建隔离快照门；历史 fixed13 未在本构建上重跑，继续只作独立旧门。 |
| ShaderContract | 173 contracts = 143 authored + 30 host built-in；286 stages、0 diagnostics；source/IR include 155、annotation 1523、declaration 2660，见 E-SHADER-CONTRACT |
| Live property | layer alpha、solid color、strict Local Contrast/Opacity、direct text content/point-size/color、exact Simple Audio Bars `Bar Color` 与 relocated 16-band profile `Opacity` 都有 typed per-surface consumer；前述既有项有 accepted 且 surface/window identity 不变的运行门，Audio Bars 当前只有 compiler/snapshot/GPU 自动门，尚无真实 live override 门；见 E-LIVE-PROPERTY / E-DYNAMIC-TEXT / E-WORKSHOP-SIMPLE-AUDIO-BARS |
| Provider 双代 | `2938612768` static image blend 5/5；`2902406982` named capture 6/6、binding 7/7；报告见 E-PROVIDER |
| Puppet/BC 合成正确性 | bind-pose/静态 MDAT 继续有效；`f1ee79b` 新增严格单 clip MDLA/full-TRS/LBS 播放，`3747492842` 六层与完整门中的其他受限层执行，unsupported mixing/visibility/profile 回退 bind pose；`18d0056` 为超预算/多 image BC1/2/3 增加 GPU premultiply，并为跨 image sprite 提供静态 authored 首帧 fallback；完整动画未实现；见 E-PUPPET-BC |
| Particle runtime | 既有 strict child、有限 static origin、22-key fallback、strict REFRACT、静态 world-space、非音频 Turbulence、TEX sampler/mip 与 normalized-lifetime oscillation 边界不变；`aa68bb8` 新增严格单 renderer/Screen RopeTrail，要求 finite `length`、有界 `segments/maxcount/instance`，用 fixed-step snapshot 和 live particle ID 保存轨迹，并复用 TEX/blend/REFRACT。当前 full45 particle `130/130`、初始 REFRACT root batch 14；Rope 和非严格 RopeTrail 仍拒绝。见 E-PARTICLE。 |
| 方向性视觉证据 | 样本自带 preview 和用户放置的官方客户端截图是当前第一视觉依据；运行门的 `preview_visual` 仍是 advisory。RopeTrail 后 `3743305891` 当前帧出现连续斜向轨迹，方向接近官方，但底部音频频谱、文字/日期排版仍缺；`3770444459` 静态构图接近官方，水滴/水花/折射密度仍不足，且目标 layer 48 ready initial 为 0；`3299228616` layer 275 ready initial 为 20，但 whole-scene 单帧不能隔离证明其淡斜线动态。Light Shafts、`1937925563` 频谱与动态文字的既有方向性结论继续有效。以上只证明公共能力方向改善，不证明精确节奏、生命周期或 Windows 动态/像素等价。 |
| 签名 App | 当前 full45 使用 Debug App `2.0.8 (268)`，Bundle `com.songziqiang.MyWallpaperX`、Team `H9QWU9XN8R`、CDHash `2ecdc5fe1589a6fa9c30fd285a1fe32ddd756bf9`，报告记 source/staged App 在 benchmark 前后均 verified。 |
| executable SHA-256 | 当前 full45 最终构建为 `c2dbcdb0ce2ea9f343ee105175e073d1f1a983dd23a2a4110dceaa62eca510df`。 |
| 样本边界 | 真实 Workshop root 只读；报告均来自隔离 sample root 与临时 HOME |

当前真实目录有 45 个数字目录；最新 source manifest 为 45 样本、`skipped=[]`，且 project/package 哈希与现役矩阵逐项一致。当前完整门为 strict graph 294 stage/53 chain、Audio Bars 4、Fisheye zero-distortion 1、graph failed 0、particle `130/130`、REFRACT 14；历史 fixed13 为 133 stage/18 chain、particle `76/76`、REFRACT 7，没有在当前构建上重跑。两门互不替代，也都不证明完整兼容、性能健康或 WE parity。报告内 `preview_visual` 仍是 advisory、`comparison_scope=same-sample-change-only`、`cross_sample_ranking=false`、`absolute_threshold=null`，因此用户提供的官方截图和本轮帧图只用于同样本方向性检查，不是 Windows 像素 golden。历史资源/Particle 与旧门计数保留在各证据包，不再作为当前聚合口径。

## 2. 证据包

<a id="e-ingest"></a>
### E-INGEST: Scene / PKG / TEX / resource ingest

- 代码：[SceneProject.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Format/SceneProject.swift)、[ScenePkgReader.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Format/ScenePkgReader.swift)、[ScenePkgCacheExtractor.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Format/ScenePkgCacheExtractor.swift)、[SceneResourceView.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/SceneResourceView.swift)、[SceneTextureLoader.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/SceneTextureLoader.swift)、[SceneTextureMipUploader.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/SceneTextureMipUploader.swift)、[SceneTexturePathResolver.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/SceneTexturePathResolver.swift)、[SceneResourceReferenceIndex.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/SceneResourceReferenceIndex.swift)
- 自动门：[test_scene_pkg_cache_extractor.py](../../../script/tests/test_scene_pkg_cache_extractor.py)、[test_scene_resource_view.py](../../../script/tests/test_scene_resource_view.py)、[test_scene_user_property_textures.py](../../../script/tests/test_scene_user_property_textures.py)、[test_scene_resource_reference_index.py](../../../script/tests/test_scene_resource_reference_index.py)、[test_scene_raw_texture_uploader.py](../../../script/tests/test_scene_raw_texture_uploader.py)、[test_scene_wallpaper_benchmark.py](../../../script/tests/test_scene_wallpaper_benchmark.py)。raw uploader GPU 门锁定静态 raw RGBA mip 按作者 image extent 逐级裁剪与 premultiply、动画 atlas 保持物理布局、非法作者尺寸整组回退物理 extent。
- 运行门：当前 full45 记录 raw-root 启动、runtime evidence/resource diagnostics、loaded/candidate counts、sample-root residue 0 和 stop lifecycle；`1937925563` 的 base/mask 从物理 padding 裁到作者 extent 后，频谱遮罩不再被压缩到纹理一角。裁剪只对单图非动画 raw RGBA 链生效，不能外推到压缩 TEX、动画 atlas 或通用格式兼容。

<a id="e-runtime-input"></a>
### E-RUNTIME-INPUT: 原始项目目录到内存运行输入

- 代码：[SteamWorkshopSceneService+ScenePlayback.swift](../../../MyWallpaperX/Modules/SteamWorkshop/Scene/SteamWorkshopSceneService+ScenePlayback.swift)、[SceneDesktopWallpaperHost+Launch.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Runtime/SceneDesktopWallpaperHost+Launch.swift)、[SceneRuntimeInput.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Runtime/SceneRuntimeInput.swift)、[SceneRuntimeModel.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Runtime/SceneRuntimeModel.swift)、[DebugScenePlaybackRunner.swift](../../../MyWallpaperX/App/DebugScenePlaybackRunner.swift)。
- 自动门：[test_scene_runtime_input.py](../../../script/tests/test_scene_runtime_input.py)、[test_scene_frame_context.py](../../../script/tests/test_scene_frame_context.py)、[test_scene_wallpaper_benchmark.py](../../../script/tests/test_scene_wallpaper_benchmark.py)。当前完整 Scene/Web suite 131 个模块全部通过，benchmark 50/50；代码健康 581 Swift files、44 locked legacy files、400 行上限。
- 运行门：本批完整 45 样本 45/45；每个宿主从隔离 sample root 解析并直接启动，Debug evidence 只落在显式 evidence directory，benchmark 锁定旧文件 residue=0、schema 1 和 stop 后 surface=0。13/13 仅为 `ee69fc4` 的历史门，不验证当前实现。
- 参考边界：只读核对 `linux-wallpaperengine-reference` 的 observable architecture（项目目录/VFS -> `project.json` -> 内存 Scene model）与 WaifuX 的原始 wallpaper path 宿主参数；未复制 GPL 代码、命名、payload 或错误文本。官方 Scene overview/publishing 文档没有公开 Windows player 的内部解析/序列化链，因此不能据此断言官方内部完全没有 IR。
- 边界：必要的 Project/PKG/scene/asset/descriptor 解析仍存在，但全部在宿主启动调用内完成；“原样传给宿主”不表示 renderer 直接消费未经解析的字节。官方文档未公开 Windows 内部 IR，因此本证据只证明移除了 MyWallpaperX 自建的 write-then-read 交换层，不证明官方实现内部不存在 IR。

<a id="e-base"></a>
### E-BASE: 基础 layer、层级、cover 与合成

- 代码：[SceneDocument.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Format/SceneDocument.swift)、[SceneRenderDescriptor.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Runtime/SceneRenderDescriptor.swift)、[SceneImageLayerPivot.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Rendering/SceneImageLayerPivot.swift)、[SceneMetalRenderer+LayerTransforms.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Rendering/SceneMetalRenderer+LayerTransforms.swift)、[SceneImageLayerCompositor.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Rendering/SceneImageLayerCompositor.swift)。
- 作者合同与语料：[官方本地声明](../reference/official/lib.sceneScript-v2.8.d.ts) 的 `IImageLayer.alignment` 支持 center/top/right/bottom/left 与四角。44 份已解包真实 `scene.json` 中 16 个样本共 135 次 image/solid alignment 声明，center 83、非中心 52；非中心分布为 left 22、right 2、bottom 1、topleft 24、bottomleft 3，其中 35 次位于 parented layer。修复前 parser 丢弃该字段，renderer 因而把所有 image/solid origin 当作几何中心；`88510a6` 将字段完整保留到 layer descriptor，并在 `sizeScale` 后、layer rotation 内应用独立 quad pivot，缺省或未知值退回中心，text layer 继续只用自己的 horizontal/vertical alignment。
- 自动门：[test_scene_image_layer_alignment.py](../../../script/tests/test_scene_image_layer_alignment.py)、[test_scene_solid_layers.py](../../../script/tests/test_scene_solid_layers.py)、[test_scene_capture_geometry.py](../../../script/tests/test_scene_capture_geometry.py)、[test_scene_wallpaper_benchmark.py](../../../script/tests/test_scene_wallpaper_benchmark.py)。alignment 门锁定全部九种官方取值、大小写归一、缺省/未知负例、Scene JSON 到 descriptor round-trip、text/image 路由隔离、pivot 位于 `sizeScale` 之后，以及 root/parented 两种几何 fixture。当前 Scene suite 共运行 622 项并通过，2 项按既有环境条件跳过。
- 运行门：alignment 定向 `.codex/scene-image-alignment-targeted-20260728-v1/report.json` 为 2/2，fixed13 `.codex/scene-image-alignment-fixed13-20260728-v1/report.json` 为 13/13；签名 App identity 为 team `H9QWU9XN8R`、CDHash `2db431064c38159153fde7486147134a8eaa4ae9`、executable SHA-256 `bbbdba7831c8dc8f919061c62619aeda2b8a1d0ee112ff5a26dd37a383bf2821`，运行前后验证不变。当前 `e698c18` full45 也包含该实现并 45/45 通过，但完整门只证明矩阵合同没有回归，不替代两样本人工锚点核对。
- 视觉边界：`3122339805` 仍缺作者脚本驱动的窗口标题、日志/时钟/日历内容和部分窗口显隐；`3767460992` 的动态背景相位不能用单帧逐像素对齐。当前证据证明公共锚点字段被接受并产生正确方向的共享可见修正，不证明 Windows 像素等价或两个样本完整兼容。

<a id="e-preview-visual"></a>
### E-PREVIEW-VISUAL: Steam preview 方向性视觉证据

- 实现提交：`3194ac5`。代码：[scene_preview_visual_evidence.py](../../../script/scene_preview_visual_evidence.py)、[scene_wallpaper_benchmark.py](../../../script/scene_wallpaper_benchmark.py)、[web_benchmark_capture.py](../../../script/web_benchmark_capture.py)。
- 自动门：[test_scene_wallpaper_benchmark.py](../../../script/tests/test_scene_wallpaper_benchmark.py) 新增 5 项，覆盖 preview 路径逃逸拒绝、按参考比例中心裁切、分项指标、拼图生成、缺失 preview 非阻断和汇总合同；聚焦 benchmark/capture 测试 43/43，宿主 GPU Scene 全量 297/294/3。
- 固定运行门：`.codex/scene-preview-visual-20260724/fixed13-final/report.json` 为 13/13，13 个样本视觉证据全部 available，9 个 GIF 使用 `first-frame-via-sips`，4 个 JPG 使用静帧；报告 SHA-256 `9ccfb3a8c760ebb8f868d489471e78ddc83a862e59e41105566130a9f38fbf62`。该报告及同阶段 final-v2 只作为历史 preview 证据；当前 full45 继续逐样本生成 preview 并排图。
- 人工校准门：`.codex/scene-preview-visual-20260724/calibration6-final/report.json` 为 6/6，报告 SHA-256 `3f9f9f08d641a6f27b2e43bdb9a11380c54dd55423a047e31952b61ce31830f2`。并排图直接暴露 `2902406982` 的构图错乱、`2938612768` 的过度水波形变和 `3750813609` 的字体/颜色差异；`3742133044` 画面完整但因作者 preview 采用不同近景裁切取得较低分项值，证明数值不能跨样本排序。
- 合同边界：benchmark 不改变播放 viewport 或 cover 投影，只把运行截图中心裁到 preview 比例。报告明确 `gating=false`、`comparison_scope=same-sample-change-only`、`cross_sample_ranking=false`、`absolute_threshold=null`；分项色彩、亮度、直方图和显著性中心仅用于同一样本跨提交对照，拼图仍需人工复核。Steam preview 不能验证动画速度、Shake 相位、粒子轨迹、音频响应、字体像素或 WE parity，缺失/解码失败只记 `unavailable`，不改变样本 PASS。

<a id="e-frame"></a>
### E-FRAME: Frame Context

- 代码：[SceneDesktopWallpaperHost.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Runtime/SceneDesktopWallpaperHost.swift)、[SceneDesktopWallpaperHost+FrameDriver.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Runtime/SceneDesktopWallpaperHost+FrameDriver.swift)、[SceneFrameContext.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Runtime/SceneFrameContext.swift)、[SceneFramePerformanceTelemetry.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Runtime/SceneFramePerformanceTelemetry.swift)、[SceneMetalView.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Rendering/SceneMetalView.swift)。
- 自动门：[test_scene_frame_context.py](../../../script/tests/test_scene_frame_context.py)、[test_scene_dynamic_snapshot.py](../../../script/tests/test_scene_dynamic_snapshot.py)、[test_scene_performance_telemetry.py](../../../script/tests/test_scene_performance_telemetry.py)、[test_scene_wallpaper_benchmark.py](../../../script/tests/test_scene_wallpaper_benchmark.py)。当前 benchmark 对 startup/performance 事件缺失、重复、负值和非有限值失败关闭；full45 的 45 个事件全部可解析。
- 运行门：`.codex/scene-transform-full45-c81a-final-20260728/report.json` 为 45/45；同一 host frame 采样后广播，stop 后 surface=0。`performance` 只记录 callback/drawable/encode/CPU/GPU 的分位数和失败计数，当前没有长期预算或 PASS 阈值，不能据此升级 Performance budgets。

<a id="e-pipeline-lifecycle"></a>
### E-PIPELINE-LIFECYCLE: launch-owned lazy effect pipeline repository

- 实现提交：`fde0951`。代码：[SceneImageEffectPipelineRepository.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Rendering/SceneImageEffectPipelineRepository.swift)、[SceneAuthoredEffectPipelineSet.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredEffectPipelineSet.swift)、[SceneDesktopWallpaperHost+Launch.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Runtime/SceneDesktopWallpaperHost+Launch.swift)、[SceneAuthoredShaderPipelineCache.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredShaderPipelineCache.swift)、[SceneFilmGrainPipeline.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneFilmGrainPipeline.swift)。
- 生命周期合同：一次 launch 创建一个 device/repository，所有 surface 与同 launch 的 screen-topology rebuild 复用它；每种 effect pipeline 由独立加锁 slot 首次请求时构造，并同时缓存成功或失败。Film Grain state 在 slot 内一次性不可变构造，authored shader cache 的 lookup/compile/counter/reset 同锁；layer Color Blend 的可变 background scratch 仍由每个 compositor 独立持有，只共享不可变 effect pipeline。
- 自动门：[test_scene_pipeline_repository.py](../../../script/tests/test_scene_pipeline_repository.py)、[test_scene_frame_context.py](../../../script/tests/test_scene_frame_context.py)、[test_scene_authored_shader_execution_planner.py](../../../script/tests/test_scene_authored_shader_execution_planner.py)。覆盖 repository 构造时 factory 调用为 0、128 路并发同 slot 只构造一次且 identity 相同、失败只尝试一次、未请求 slot 保持 0、两个 launch 各自构造一次，以及 screen rebuild 复用同一 repository。
- 性能边界：同机同样本的两组探索性顺序 A/B 中，eager/lazy 启动分别为 `1785.598/1824.913 ms`（`+39.315 ms`）与 `1761.746/1787.525 ms`（`+25.779 ms`）。样本仅各 2 次，且启动口径包含固定延迟、包加载、首帧和证据落盘，因此既没有证明单屏启动加速，也不足以断言稳定回退；steady-state completed FPS、GPU p95 与 pre-encode p95 也没有一致方向。成立的结论是按需失败隔离和同 launch 复用的结构合同；真实双屏并发与 topology rebuild 尚无运行门，Performance budgets 继续为 `L0`。

<a id="e-timeline"></a>
### E-TIMELINE: 作者 Timeline IR、绝对 scene-time 求值与 typed 写回

- 代码：[SceneTimelineAnimation.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Format/SceneTimelineAnimation.swift)、[SceneTimelineEvaluator.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Format/SceneTimelineEvaluator.swift)、[SceneDocument+Timeline.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Format/SceneDocument+Timeline.swift)、[SceneTimelineTargetCompiler.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Properties/SceneTimelineTargetCompiler.swift)、[SceneTimelineRuntime.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Properties/SceneTimelineRuntime.swift)、[SceneDesktopWallpaperHost+TimelineReport.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Runtime/SceneDesktopWallpaperHost+TimelineReport.swift)
- 自动门：[test_scene_timeline_ir.py](../../../script/tests/test_scene_timeline_ir.py)、[test_scene_timeline_evaluator.py](../../../script/tests/test_scene_timeline_evaluator.py)、[test_scene_timeline_document.py](../../../script/tests/test_scene_timeline_document.py)、[test_scene_timeline_target_compiler.py](../../../script/tests/test_scene_timeline_target_compiler.py)、[test_scene_timeline_runtime.py](../../../script/tests/test_scene_timeline_runtime.py)
- 只读语料 census（44 个已解包 `scene.json`）：48 处作者 Timeline 全部解析成功、0 诊断；按真实 JSON 路径分布为 effect constant 23、layer transform 10、粒子 `instanceoverride` 7、layer `alpha` 5、`maxwidth` 2、`zoom` 1。**必须按完整路径归类**——`alpha` 一名三用，三者走不同 target 与 consumer。
- 编译结果：28 条 binding（effect constant 23 + layer alpha 5）、诊断 `relativeUnsupported` 10 / `unsupportedHost` 3 / `wrapLoopIgnored` 7。`wrapLoopIgnored` 比 census 的 9 处 `wraploop` 少 2，差额是 `2902406982` 落在 `unsupportedHost` 的两个 `maxwidth`——阻断项不再记降级。
- 运行门：`.codex/scene-timeline-t2-20260727/report.json` 5/5，preview 日志同目录。确定性求值证据（首帧与半程各一次，不依赖像素归因）：`3769688830` 六条 loop effect alpha 均 `value@0=0.00000 → value@half=0.59646`；`3769364482` 的 `rayintensity` `-0.02653 → -0.08842`，与作者关键帧 `(0, -0.026534296)`、`(189, -0.10451264)` 的线性插值手算吻合（负值是作者数据本身）；`3028090166` 三条 layer alpha 生效且 layer 23 的 `scale` 因 `relative` 留诊断；负门 `2067939514` 的 `startpaused` 动画 `value@0 == value@half == 1.00000`，确实停在首帧。
- 边界：真实执行只覆盖 single 21 + loop 7，**Mirror 真实样本 0 处执行**（6 处全在被 `relative` 拒绝的 layer transform 上）；tangent 只保真不消费、插值走线性；`relative`、Combined Animation、Animation Event 与粒子 `instanceoverride` 均未执行；无 Windows golden。

<a id="e-parallax"></a>
### E-PARALLAX: Camera Parallax 与受限 pointer 投影

- 代码：[SceneLayerParallax.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Rendering/SceneLayerParallax.swift)、[SceneMetalView.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Rendering/SceneMetalView.swift)
- 自动门：[test_scene_layer_parallax.py](../../../script/tests/test_scene_layer_parallax.py)、[test_scene_wallpaper_benchmark.py](../../../script/tests/test_scene_wallpaper_benchmark.py)
- 运行门：`.codex/scene-parallax-correction-formal13-20260723/report.json` 与 `.codex/scene-property-parallax-off-gate-20260722/report.json`；显式开启和 author-off 分开验证。

<a id="e-property"></a>
### E-PROPERTY: User Properties 与受限 target

- 代码：[SceneUserProperty.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Properties/SceneUserProperty.swift)、[SceneUserPropertyBindings.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Properties/SceneUserPropertyBindings.swift)、[SceneUserPropertyResolver.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Properties/SceneUserPropertyResolver.swift)
- 自动门：[test_scene_user_properties.py](../../../script/tests/test_scene_user_properties.py)、[test_scene_user_property_textures.py](../../../script/tests/test_scene_user_property_textures.py)
- 运行门：`.codex/scene-property-text-day-gate-pass-20260722/report.json`、`.codex/scene-user-texture-final13-r2-20260723/report.json`；layer alpha/solid color 的 live 子集见下一证据包，其余 target 仍按 consumer 能力回退整场重建。

<a id="e-live-property"></a>
### E-LIVE-PROPERTY: B0 binding program 与已注册 live consumers

- 代码：[ScenePropertyBindingProgram.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Properties/ScenePropertyBindingProgram.swift)、[ScenePropertyLiveUpdateState.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Properties/ScenePropertyLiveUpdateState.swift)、[SceneSurfaceEvaluationTransaction.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Properties/SceneSurfaceEvaluationTransaction.swift)、[SceneDynamicLayerValues.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Properties/SceneDynamicLayerValues.swift)、[SceneDesktopWallpaperHost.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Runtime/SceneDesktopWallpaperHost.swift)
- 自动门：[test_scene_property_binding_program.py](../../../script/tests/test_scene_property_binding_program.py)、[test_scene_runtime_input.py](../../../script/tests/test_scene_runtime_input.py)、[test_scene_surface_evaluation_transaction.py](../../../script/tests/test_scene_surface_evaluation_transaction.py)、[test_scene_property_live_update_state.py](../../../script/tests/test_scene_property_live_update_state.py)、[test_scene_dynamic_layer_values.py](../../../script/tests/test_scene_dynamic_layer_values.py)、[test_scene_property_live_routing.py](../../../script/tests/test_scene_property_live_routing.py)、[test_scene_framebuffer_capture.py](../../../script/tests/test_scene_framebuffer_capture.py)、[test_scene_wallpaper_benchmark.py](../../../script/tests/test_scene_wallpaper_benchmark.py)
- 运行门：alpha/solid color/Local Contrast/Opacity 沿用既有定向报告；direct text 的 `2134765860:customtext/textcolor/textsize` 见 `.codex/scene-dynamic-text-targeted-213-final-20260723-1907/report.json`。各项均 `accepted=true` 且 surface/window identity 不变。
- Audio Bars 新合同：`715e078` 为 exact `effects/workshop/2084198056/Simple_Audio_Bars/effect.json` pass 0 的 `Bar Color` 注册 vector3/color target；当前可重定位 16-band profile 沿用同一结构化 path matcher，并为 `ui_editor_properties_opacity` 注册 scalar target，两者都由 strict plan 每帧从 snapshot 消费。自动门证明 path/pass/name/type 变异失败关闭、动态 color/opacity 进入 GPU；尚未运行真实 `basecolor`/`newproperty11`/`musicbar` live override，因此旧 `.codex/scene-live-solid-color-312-20260723-1006/report.json` 只能说明当时 target 未注册，不能继续作为当前 `basecolor` 负向证据。
- 边界：运行门已证明 layer alpha、solid color、有效可见 direct text 三字段，以及 strict Local Contrast/Opacity；Audio Bars `Bar Color`/`Opacity` 当前仅到 compiler/snapshot/GPU 自动门。SceneScript、hidden/no-consumer text、particle、container、non-solid color、其他 effect constant 或 unsupported mixed key 必须返回整场重建。program 可编译本身不构成 live 运行证据。

<a id="e-text"></a>
### E-TEXT: 静态文字

- 代码：[SceneTextTextureLoader.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Text/SceneTextTextureLoader.swift)
- 自动门：[test_scene_text_geometry.py](../../../script/tests/test_scene_text_geometry.py)、[test_scene_user_properties.py](../../../script/tests/test_scene_user_properties.py)
- 运行门：`.codex/scene-text-fixed-20260722/report.json` 和正式矩阵的 text loaded/candidate 结构门；没有 Windows 字体/基线 golden。

<a id="e-text-anchor"></a>
### E-TEXT-ANCHOR: text layer screen anchor

- 代码：[SceneLayerScreenAnchor.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Rendering/SceneLayerScreenAnchor.swift)、[SceneMetalRenderer+LayerTransforms.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Rendering/SceneMetalRenderer+LayerTransforms.swift)、[SceneTextDescriptor.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Text/SceneTextDescriptor.swift)
- 作者合同：`lib.sceneScript.d.ts` 的 `ITextLayer.anchor` 给出 none/center/top/topright/right/bottomright/bottom/bottomleft/left/topleft 十个取值，`locale/ui_en-us.json` 的 `ui_editor_properties_screen_anchor` 为 "Screen anchor"；随包只有 5 个 text layer，`dino_run` 的 231/177 为 `topright`，`previewcountdown`/`previewclock`/`preview3dclock` 为 `none`。
- 自动门：[test_scene_layer_screen_anchor.py](../../../script/tests/test_scene_layer_screen_anchor.py)、[test_scene_text_geometry.py](../../../script/tests/test_scene_text_geometry.py)。锁定十个取值的符号表、大小写不敏感、缺省/未知/退化输入零偏移、`center` 锚点等于相机偏移，以及真实 `coverHalfExtents` 下 16:9/16:10/21:9 的锚定边距恒为作者画布上的 1.57/7.871 世界单位（不锚定时 16:10 的右边距为 `-15.53`、21:9 的上边距为 `-16.28`，即整块移出可见矩形）。
- GPU 门：同一自动门用真实 `SceneImageLayerPipeline` 与真实 `SceneCameraProjection.viewProjection` 把 `dino_run` 的 `label_coins`（size `780x291`、scale `0.057`、origin `341.42999 185.129`、作者对齐 `right`/`center`）画进离屏纹理：作者宽高比 16:9 下锚定前后完全相同（396 px、x`[222,254]`、y`[0,11]`）；21:9 下不锚定为 0 覆盖像素，锚定后回到同一组 396 px。
- 边界：`anchor` 只在 text layer 上存在，CoreText 栅格化不消费它。当前 full45 运行在该实现之后，但没有为 `anchor` 设置专项截图/像素断言；历史 fixed13 也不覆盖它。没有 Windows 像素标定。layer pivot 见 [E-TEXT-PIVOT](#e-text-pivot)。

<a id="e-text-pivot"></a>
### E-TEXT-PIVOT: text layer alignment pivot

- 代码：[SceneTextLayerPivot.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Rendering/SceneTextLayerPivot.swift)、[SceneMetalRenderer+LayerTransforms.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Rendering/SceneMetalRenderer+LayerTransforms.swift)、[SceneTextTextureLoader.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Text/SceneTextTextureLoader.swift)
- 作者合同：`lib.sceneScript.d.ts` 的 `ITextLayer` 给出 `horizontalalign` 为 left/center/right、`verticalalign` 为 center/top/bottom，`locale/ui_en-us.json` 对应 `ui_editor_properties_text_horizontal_alignment`/`ui_editor_properties_text_vertical_alignment`。这两个字段同时决定框内排版（CoreText 段落对齐，早已消费）和框相对 layer origin 的位置：origin 落在被命名的那条边上。
- 几何判据：随包 `dino_run` 的 `label_coins`（size `780x291`、pointsize 64）与 `label_top`（size `390x145`、pointsize 32）内容同为 `"00000"`、scale 同为 `0.057`、`horizontalalign` 同为 `right`，且共用同一个 `origin.x = 341.42999`（画布宽 343），但世界宽相差一倍（44.46 与 22.23）。几何中心 pivot 下两者右边缘相差 22.23 并分别越过右边界 20.66 / 9.55；取右边缘 pivot 后两者右边缘齐平在 341.43，边距同为 1.57，`label_top` 四边零溢出。三个 preset text layer（`previewcountdown`/`previewclock`/`preview3dclock`）都是 `center`/`center` 且 `origin.x` 正好是 256 画布的中心 128，取中心 pivot 时摆位不变。垂直方向随包只有 `center`，两个标签的 origin 间距 12.245 与半高之和 12.427 相符，也就是竖向仍是中心 pivot。
- 自动门：[test_scene_text_layer_pivot.py](../../../script/tests/test_scene_text_layer_pivot.py)。锁定 left/center/right × top/center/bottom 的单位 quad 平移表（`±0.5`，`top` 因 `sizeScale` 的 `-size.y` 翻转与 `left` 同号）、大小写不敏感、缺省与未知取值退回几何中心，以及 renderer 必须把 pivot 排在 `sizeScale` 之后、同一组字段仍继续喂 CoreText。
- GPU 门：同一自动门用真实 `SceneImageLayerPipeline` 与真实 `SceneCameraProjection.viewProjection` 在 256x144（作者 16:9）离屏纹理上画两个标签。几何中心 pivot 下 `label_coins` 216 px、x`[238,255]`，`label_top` 54 px、x`[247,255]`，两者右边缘都被裁在最后一列且可见宽度不同；作者对齐 pivot 下 `label_coins` 396 px、x`[222,254]`（33 列全可见），`label_top` 102 px、x`[238,254]`（17 列），右边缘齐平在 `origin.x` 对应的第 254 列。竖向 `top` 把 `label_coins` 从 y`[0,11]` 推到 y`[6,17]`（半高 8.2935 世界单位 = 6.19 px），`bottom` 推到 y`[0,5]`。
- 边界：静态文字仍以作者 `size` 加 `padding` 作为固定外框；动态文字在 `limitwidth=false` 时会扩到不小于 CoreText intrinsic size，并把同一尺寸传给 provider capture、effect/FBO 与最终 quad，`limitwidth=true` 则保持作者限制。`padding` 对称扩框后 pivot 取扩后的边，随包全部 `padding: 0`，没有反例可校。`blockalign` 未解析，baseline 本身仍未处理。动态扩框见 [E-TEXT-SCRIPT](#e-text-script)，没有 Windows 像素标定。

<a id="e-text-pointsize"></a>
### E-TEXT-POINTSIZE: 300 DPI point size 换算

- 代码：[SceneTextGeometry.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Text/SceneTextGeometry.swift)、[SceneTextTextureLoader.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Text/SceneTextTextureLoader.swift)
- 作者合同：`ui/dist/monaco/autocomplete/lib.sceneScript.d.ts:1604` 给 `ITextLayer.pointsize` 的注释是 "Size of the font in points for 300 DPI."，因此像素字号 = `pointsize * 300 / 72 = pointsize * 25/6 ≈ 4.16667`；此前实现用的 `round(pointsize * 4)` 比它低 4%。
- 数值判据：历史探针用 `assets/fonts/Segment7Standard.otf`（unitsPerEm 1000）在 CoreText 里排 `dino_run` 两个记分标签的同一内容 `"00000"`，该串 advance 恒为像素字号的 2.925 倍。`pointsize 64 -> 266.667 px` 时排版宽正好 780.000、ascent+descent+leading 290.67（取整 291）；`pointsize 32 -> 133.333 px` 时排版宽正好 390.000、ascent+descent+leading 145.33（取整 145）。作者 size 分别是 `780x291` 与 `390x145`，四个数字逐位相符。反解同样落在 25/6：让排版宽等于作者宽所需的像素字号是 266.667 与 133.333，倍率都是 4.1667；`x4.0` 只能给出 748.80 与 374.40。
- 自动门：[test_scene_text_geometry.py](../../../script/tests/test_scene_text_geometry.py)。锁定 `32 -> 400/3`、`64 -> 800/3`、两者严格 2 倍线性、包裹式 property 值 `42 -> 175`，以及 `0 -> 1`、`300 -> 1024` 两端夹取。
- 边界：数值判据来自历史手动探针，不是仓库内自动门——自动门只校验算术，字体度量没有进 CI。字号仍夹在 `[1, 1024]` px，作者值 ≥ 245.76 磅起偏离官方换算，这是本地纹理保护不是官方合同（超大外框另由 `SceneTextGeometry.rasterLayout` 整块等比缩小）。静态作者框与动态 intrinsic 扩框边界见 [E-TEXT-PIVOT](#e-text-pivot)；换算准确只保证字号，不证明 Windows 排版等价。

<a id="e-text-limits"></a>
### E-TEXT-LIMITS: text layer 行数与宽度溢出限制

- 代码：[SceneTextRowLimit.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Text/SceneTextRowLimit.swift)、[SceneTextDescriptor.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Text/SceneTextDescriptor.swift)、[SceneTextTextureLoader.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Text/SceneTextTextureLoader.swift)
- 作者合同：`ui/dist/monaco/autocomplete/lib.sceneScript.d.ts` 的 `ITextLayer` 给出 `limitrows`（"Enable row limit."）、`maxrows`（"Max number of rows."）、`limitwidth`（"Enable width limit."）、`maxwidth`（"**Max width in pixels**."）、`padding`（"Padding in pixels."）。`limituseellipsis` 不在 typings 里，但编辑器字符串有 `ui_editor_properties_overflow_ellipsis`（"Overflow ellipsis"），语料里与 `limitrows` 同时出现。
- 语料判据：452 个不同 text layer（29 个不同 scene 指纹，来自 `.codex/*/runtime-homes/*/Library/Caches/MyWallpaperX/SteamWorkshopScene/*/scene.json` 与历史客户端快照）里 `limitwidth` False 393 / True 58 / 缺省 1，`limitrows` False 392 / True 59 / 缺省 1，`limituseellipsis` False 437 / True 14，`maxrows` 为 1 的 434 个、2 的 6 个、3 的 5 个、22 的 5 个、21 的 1 个。**开关语义由语料反证**：393 个 `limitwidth: false` 的 layer 全带编辑器默认 `maxwidth: 500`，其中 79 个去重后的作者宽度已经大于 500——若无条件套用就会把它们错误压窄，所以两个数值必须被开关门控。反向的 45/47 个 `limitwidth: true` 去重行满足 `size.x <= maxwidth`；剩下 2 个（653、624 对 `maxwidth: 620`）是 property 驱动 layer，作者 `size` 已过期。
- 自动门：[test_scene_text_row_limit.py](../../../script/tests/test_scene_text_row_limit.py)。走真实生产路径（`SceneDocumentLoader` → `SceneRenderDescriptorBuilder` → `SceneTextTextureLoader`）栅格化自建 fixture 后逐像素读回 `MTLTexture`：`maxrows 1` 让两行内容只剩首行（覆盖像素 4910→2190，保留的行区间与基线首行 `[8,44]` 逐位一致）；硬换行 `"A\nB\nC"` 在 `maxrows 2` 下只剩前两行（1767→1229，行区间等于基线前两段）；`limituseellipsis` 把末行右边界从 133 推到 177 且覆盖像素上升；`limitwidth + maxwidth 100` 把 400 px 框里 274 px 的单行折成两行、右边界 ≤ 100；**负例**同一个 `maxwidth 100` 在 `limitwidth: false` 时仍是 274 px 单行；三开关全开且全为 property 包装（含 `animation` 字典）时末行从 `"AA…"` 回退成 `"A…"`（77 px ≤ 100）。另有 `wrapWidth` 的五个纯函数断言：关闭 → 653、`maxwidth 620` → 620、`maxwidth 1942.6299` → 以外框 653 为上界、`scale 0.5` → 310、`maxwidth 0` → 653。
- 边界：fixture 只用 macOS 系统字体别名 `systemfont_arial` 与自建 A/B 内容，不含任何官方 payload；断言是本项目自己的 CoreText 结果，不是 WE 像素等价——官方省略号的回退粒度、字符和断点都没有 Windows 对照。`maxwidth` 仍被夹在作者内容宽以内；`limitwidth=true` 的动态内容保持受限框，`limitwidth=false` 才按 intrinsic size 扩框。`blockalign`（语料 False 257 / 缺省 192 / True 3）仍未解析。

<a id="e-text-fontref"></a>
### E-TEXT-FONTREF: 字体引用分类（别名 / 包内 / 客户端自带）

- 代码：[SceneTextFontResolver.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Text/SceneTextFontResolver.swift)
- 作者合同：官方只读安装目录里 `systemfont_*` 恰好 8 个（`arial` 8 次，`verdana`/`segoe`/`sansserif`/`consolas`/`comicsans`/`cambria`/`calibri` 各 6 次），`assets/fonts` 恰好 19 项 = 15 个字体文件 + 4 个 license `.txt`。作者写成 `fonts/X` 时官方先在包内找同名文件，找不到再用客户端自带的 stock 字体——语料闭合反证见下一条。
- 语料判据：655 个 `scene.json`（`.codex/*/runtime-homes/*/Library/Caches/MyWallpaperX/SteamWorkshopScene/*/scene.json`）里 244 个去重 text layer（按 font+text+size）的字体引用归属为**包内文件 184 / stock 41 / alias 19 / 未知 0**。"未知 0"是这套两层解析模型的闭合证据：所有非包内、非别名的引用都恰好落在客户端 `assets/fonts` 的 15 个名字里。stock 分布为 Atami-Regular 17、Alcubierre 7、8bitOperatorPlus8 6、spincycle_3d_ot 5、summer85 2，NotoSans-Regular / Segment7Standard / Blackout 2 AM / kust 各 1；alias 分布为 arial 8、verdana 4、comicsans 3、consolas 3、cambria 1。按原始出现次数 `systemfont_consolas` 480 次、`fonts/Atami-Regular.otf` 403 次，都不是边角样本。
- 类别判据：15 个 stock 字体的类别来自历史 CoreText 实测，不按字体名猜。emoji 看 `traitColorGlyphs` 与 COLR/sbix/CBDT 表；mono 看 fixed-pitch trait 或数字与字母 advance 是否一致；NotoSans-Regular 归 sans，其余 display。类别只决定 app 包缺失时的兜底家族（Menlo / Noto Sans / Apple Color Emoji / Helvetica）和诊断后缀，不再决定正常命中时的字形。
- 字形来源判据：15 个官方名字全部物理位于 `MyWallpaperX/Resources/SceneStockAssets.bundle/assets/fonts`，其中 8 个为原版、7 个为替代字体；两类都按官方物理文件名读取。
- 替代选择判据：替代不是按名字或类别硬凑，而是把每个原版与候选逐个并排渲染 "Hamburg 0123" 比对字形后选定——Atami→Poppins-Medium、Alcubierre→Poppins-ExtraLight（同为几何无衬线，单层 a、圆形 O、线宽与字重吻合）、spincycle_3d_ot→BungeeShade-Regular（同为宽扁立体描边大写，比对中 RubikIso 的小写更杂乱被否）、summer85 与 Lazer84→PermanentMarker-Regular（同为粗马克笔 display，KaushanScript 过轻被否）、kust→Bangers-Regular、CursedTimerUlil-Aznm→复用已随包的 Segment7Standard（同为七段数码管，不新增文件）。
- 自动门：当前没有字体引用的自动门。
- 边界：7 个替代字形的轮廓、advance、kerning 与原版不同，命中时带 `stockSubstituted` 诊断。8 个原版仍无 Windows 栅格化逐像素对照；`weight`/`italic`/CJK 缺字回退链仍缺，见 [E-TEXT](#e-text)。

<a id="e-dynamic-text"></a>
### E-DYNAMIC-TEXT: direct property 动态文字与 generation lifecycle

- 代码：[SceneDynamicTextTextureStore.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Text/SceneDynamicTextTextureStore.swift)、[SceneDynamicTextGenerationState.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Text/SceneDynamicTextGenerationState.swift)、[ScenePropertyBindingCompiler+TargetMapping.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Properties/ScenePropertyBindingCompiler+TargetMapping.swift)、[SceneTextTextureLoader.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Text/SceneTextTextureLoader.swift)
- 自动门：[test_scene_dynamic_text_binding.py](../../../script/tests/test_scene_dynamic_text_binding.py)、[test_scene_dynamic_text_generation.py](../../../script/tests/test_scene_dynamic_text_generation.py)、[test_scene_frame_context.py](../../../script/tests/test_scene_frame_context.py)、[test_scene_wallpaper_benchmark.py](../../../script/tests/test_scene_wallpaper_benchmark.py)。覆盖三类 typed value、错误 kind/conditional fail closed、重复值去重、A->B->C stale completion 拒绝、失败保留 last-ready 和 reset。
- 运行门：`.codex/scene-dynamic-text-targeted-213-final-20260723-1907/report.json`；隔离 `2134765860` 先以 `text=2` 启用作者 Custom 模式，再 live 更新 `customtext/textcolor/textsize`，surface `1 -> 1`、window `[94398] -> [94398]`、changed ratio `0.003541478640192539`、text `6/6`。正式回归 `.codex/scene-dynamic-text-final13-20260723-1915/report.json` 为 13/13、sample root residue 0。
- 边界：本包只证明 direct user property；有效可见且有 text/style 的 layer 才是活动 consumer。exact native clock/date/day 复用同一 consumer，但证据单列在 [E-TEXT-SCRIPT](#e-text-script)。通用 SceneScript、media、hidden/no-consumer layer、conditional binding 和 Windows font/layout parity 均不在本证据包内。

<a id="e-text-script"></a>
### E-TEXT-SCRIPT: exact native clock / spaced-day / date profiles

- 实现提交：`8740737`。代码：[SceneTextScriptDefinition.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Text/SceneTextScriptDefinition.swift)、[SceneTextScriptCompiler.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Text/SceneTextScriptCompiler.swift)、[SceneTextScriptRuntime.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Text/SceneTextScriptRuntime.swift)、[SceneDynamicDefinitionMerger.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Properties/SceneDynamicDefinitionMerger.swift)、[SceneDynamicTextTextureStore.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Text/SceneDynamicTextTextureStore.swift)、[SceneTextTextureLoader.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Text/SceneTextTextureLoader.swift)。
- 准入合同：只接受 Workshop `2981960200` 的三份已独立复核 raw source SHA-256，并要求完整 `scriptproperties` key set、类型和值域同时匹配。clock 生成 `-HH<delimiter>mm-` 与可选 seconds；day 生成作者约定的 spaced weekday；date 生成 day/month/year 与可选 weekday/newline。测试 fixture 只保存 source hash 和项目自建参数/期望值，不复制第三方脚本源码。未知 source、缺失/额外/变异 property、隐藏或祖先隐藏 layer 均不执行，保留作者 fallback。
- 自动门：[test_scene_text_script_runtime.py](../../../script/tests/test_scene_text_script_runtime.py)、[test_scene_text_row_limit.py](../../../script/tests/test_scene_text_row_limit.py)、[test_scene_dynamic_text_generation.py](../../../script/tests/test_scene_dynamic_text_generation.py)、[test_scene_wallpaper_benchmark.py](../../../script/tests/test_scene_wallpaper_benchmark.py)。锁定固定 UTC 日期下 24/12 小时、seconds、spaced weekday、padded month、wrapper unwrapping、unknown/mutated fail closed、`SceneScript > Timeline > user > authored` 优先级、definition 去重，以及 `limitwidth=false` 扩框 / `true` 保持作者框。完整 Scene suite 648 项通过、2 项按既有条件跳过。
- 真实运行门：`.codex/scene-text-script-3765760121-20260729-v3/report.json` 为 **1/1 PASS**，binding 3、diagnostic 0，报告 SHA-256 `4c05731fd083e7a76a4b7ff0bd534e969f42e472a2498e482ba5457e5e583410`；其余五个样本 `.codex/scene-text-script-shared6-20260729-v1/report.json` 为 **5/5 PASS**，合计 binding 13、diagnostic 0，报告 SHA-256 `903e4dc18120ddd663166e3827b6df17cf45aebc11d6f66248d277ae8fd07a0e`。full45 为 45/45、16 bindings；其余 14 个样本共 38 条未知 text-script diagnostic，均未误执行。
- 视觉证据：`3765760121` 的最终帧 `.codex/scene-text-script-3765760121-20260729-v3/results/3765760121/scene-after-window.png` 对比样本官方截图，日期、时钟与完整星期已替代三个占位符；动态 render size 经 project capture、两段 Foliage 和最终 quad 后仍保持完整内容。当前 Wednesday 比官方截图中的 Tuesday 更长，作者右边缘位置使末端贴近或略越屏幕；这是内容长度与作者布局的剩余差异，不是脚本值被旧占位符纹理截断。背景切换是用户 combo，不能归因于时间脚本。
- 边界：这是 native formatter，不解析或执行 JavaScript，不开放官方 `Date`/`engine`/lifecycle/handle/event/module API，也不证明三份脚本以外的同类字符串实现。wall clock 采用系统 Gregorian calendar 与当前时区；时区、locale、DST 跳变、暂停/唤醒、跨午夜原子帧和 Windows 字形/像素 parity 尚无专门门。

<a id="e-particle"></a>
### E-PARTICLE: 2D Particle 子集

- 实现提交：strict eventspawn `f4173ea`，birth/natural-death event queue、Sprite Trail child 与 1,024 粒子预算 `7d53c10`，Event Follow parent-ID owner/逐帧 follow/parent-death 回收 `928acca`，持续/混合/duration child lifecycle 与 root aggregate budget `2f897bc`，strict static/default-static child `899704b`，有限 static origin translation `678a052`，strict depth-two nested child、per-depth 64/跨两层 128 systems 聚合预算与 depth-three/nested-static fail closed `b86db59`，22-key 程序纹理 registry 与非音频 Turbulent Velocity Random `4a17ee6`，strict REFRACT `e698c18`，static General/Movement/Renderer world-space `54a6ebc`，非音频 Turbulence operator `7b1d36f`，TEX sampler `d19e76b`，Box/random exponent/lifetime Alpha/Size oscillation/initializer offset `d792296`，实际 mip 链及 RG88 全 mip 颜色适配 `f4e8a0c`，lifetime-normalized Position oscillation `5911049`，strict RopeTrail `aa68bb8`。RopeTrail 重点代码为 [SceneParticleRopeTrailPlan.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Particles/SceneParticleRopeTrailPlan.swift)、[SceneParticleStepSnapshotRecorder.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Particles/SceneParticleStepSnapshotRecorder.swift)、[SceneParticleDefinitionParser.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Particles/SceneParticleDefinitionParser.swift)、[SceneParticleRuntime.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Particles/SceneParticleRuntime.swift)、[SceneParticleShaderSource.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Particles/SceneParticleShaderSource.swift) 与 [SceneMetalRenderer+Particles.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Rendering/SceneMetalRenderer+Particles.swift)。
- 自动门：[test_scene_particle_rope_trail.py](../../../script/tests/test_scene_particle_rope_trail.py)、[test_scene_particle_definitions.py](../../../script/tests/test_scene_particle_definitions.py)、[test_scene_particle_simulator.py](../../../script/tests/test_scene_particle_simulator.py)、[test_scene_particle_rendering.py](../../../script/tests/test_scene_particle_rendering.py)、[test_scene_particle_runtime.py](../../../script/tests/test_scene_particle_runtime.py) 的 RopeTrail 聚焦集合 **54/54 PASS**；完整 Scene discover **681 项通过、2 项按既有环境条件跳过**。门锁定轨迹/fade、prewarm/outer-delta partition、非等比 transform、wide viewport、REFRACT 切线/UV span，以及 malformed/multiple/animated/advanced-field/超预算 fail-closed；这些均为项目 clean-room 数值与 GPU 门，不是 Windows fixed-seed/pixel parity。
- World-space/REFRACT 历史门：`.codex/scene-particle-worldspace-targeted-20260729-v3/report.json` 5/5 PASS（报告 SHA-256 `8eb370ecbcf1065f05185b43b2f691f4e587eed22504d197e8fd88b18a91eb94`）；当时 fixed13 为 13/13、particle `76/76`、REFRACT root batch 7，full45 为 45/45、particle `127/130`、REFRACT root batch 13。`2998757800` layers 840/214034、`3299228616` layers 248/263、`3750813609` layers 523/530、`3769688830` layer 967 从 World-space 拒绝转为执行；`3770444459` layers 239/245/248 原已加载，该批修正 Movement world direction。
- RopeTrail 当前真实门：最终 parser 收窄后的 `.codex/scene-particle-ropetrail-targeted-20260729-v5/report.json` 为 **3/3 PASS**（SHA-256 `e87b1f56498aa5e9c02040f6acc44c1ca758c3ab98bbd531f4b21d6c341fffd8`）。`3299228616` 为 particle `7/7`、layers `[593,244,248,253,258,263,275]`、ready initial 合计 30，其中目标 layer 275 为 20；`3743305891` 为 `1/1`、layer 26、ready initial 116；`3770444459` 为 `5/5`、layers `[48,239,245,248,285]`、REFRACT 4，目标 layer 48 ready initial 0。三者 failed frame/drawable miss 均为 0；当前完整门为 particle `130/130`、REFRACT 14，fixed13 为 `76/76`、REFRACT 7。
- RopeTrail 官方与实现边界：官方公开资料只确认 Rope 连接每个 spawned particle，RopeTrail 沿每个粒子的自身路径画线，并公开 Length、Segments、Subdivision、UV scale/scrolling 与性能取舍；没有公开数值默认值/范围、Length 单位、节点/插值/接缝、精确 UV、fadealpha、死亡尾迹或 pause/seek/reset。当前实现据此只准入单 RopeTrail、Screen、renderer world-space 关闭、finite `0 < length <= 4`、`segments` 缺省 4/范围 1...8、`maxcount <= 512`、`maxcount × segments <= 4096`、可选 `fadealpha`，其余字段/组合 fail closed；这些限制是项目预算和 clean-room 解释，不是官方边界。
- RopeTrail 视觉与残余：`3743305891` 当前帧已有连续斜向轨迹，较接近官方，但底部音频频谱和文字/日期排版仍缺；`3770444459` 的水滴/水花/折射密度不足，目标层 initial 0，整场截图不能证明动态轨迹；`3299228616` 目标层 initial 20，但现有整场单帧不能隔离归因。残余诊断包括 329 的 `audioResponseIgnored`，374 的 child transform/control point，377 的 Boids 与滴水/splash children。缺少隔离层、alpha、周期帧/视频、死亡尾迹与内存时间序列，因此只到 `L3 executed-degraded`。
- World-space 性能与视觉边界：full45 五样本 Debug driver FPS 为 59.901/60.000/59.990/29.971/60.029，GPU p95 为 9.941/7.510/8.520/10.974/7.855 ms，CPU p95 为 1.054/1.457/0.482/2.265/2.041 ms；没有 A/B 基线或统一阈值，`3769688830` 约 30 FPS 是既有作者/宿主目标，不能归因成回归。人工 after 帧确认雨、星点、雾/落叶、花瓣、水面主体仍可见，新粒子多为细小或动画相位相关，不能从单帧归因像素差异或宣称 Windows parity。
- Turbulence 真实门：旧 full45 日志中 `unsupportedOperator:turbulence` 为 8 样本/19 个当前加载图层；`7b1d36f` 定向选择其中 5 样本/14 图层，报告 `.codex/scene-turbulence-targeted-20260729-v1/report.json` 为 5/5，全部新日志不再含该诊断。五样本 initial live particle 分别为 10/10/264/3,878/514；CPU p95 为 0.640...3.077 ms、GPU p95 为 5.123...13.079 ms、driver FPS 为 59.973...60.136，failed frame/drawable miss 均为 0。`2131872317` 仍有两条既有 event child `particleBudget` 截断诊断，与 Turbulence 无关；跨运行环境和动画相位不同，且没有统一性能阈值，因此这些数字只记录本次运行状态，不构成性能改善或长期预算结论。
- Turbulence 视觉边界：人工对比五个样本的官方截图与新 after 帧，主体构图、既有光照/水面和火花、星点、花瓣或流动微粒均保持可见，未见明显公共回归；动画时相、用户属性、裁切和文字日期不同，单帧无法把局部差异独立归因于 Turbulence。没有 Windows 固定 seed 轨迹或像素 golden，不能声明 WE 数值、动态或视觉等价。
- Sampler / 分布真实门：TEX sampler 中间构建 `.codex/scene-particle-sampler-after-20260729/report.json` 为 4/4；分布/振荡构建中，`.codex/scene-particle-exponent-box-oscillation-247-after-20260729-v2/report.json`、`.codex/scene-particle-shared-193-after-20260729-v1/report.json`、`.codex/scene-particle-shared-374-after-20260729-v1/report.json` 各 1/1，三者使用相同 executable SHA-256 `e735ed60c66036ac62c054e81394e62e48982314d0210ebee6df709fc8f64171`、CDHash `2282a33ab2a7f968e071a880de82df3ea022c191`。`2470144420` 的 layer 106 Sakura 命中 repeat sampler、Turbulent Velocity offset=3 与 Size Random exponent=2，layer 128 Stars 命中 zero-min centered Box 与 Oscillate Alpha；日志 initial live 48 只来自 Sakura prewarm，Stars `starttime=0` 的 initial=0 不能用于推断其稳态数量。`1937925563` 只作共享 Box/oscillation/exponent 回归，既有三角/频谱/水波保持可见；没有围绕其细微残差继续拟合。`3742133044` 默认只加载雪花 layer 196，隐藏樱花 layer 189 未执行，不能把该门写成隐藏叶片/花瓣 exponent 证据。
- Mip 真实门：最终 `f4e8a0c` 构建在 `.codex/scene-particle-mip-247-after-20260729-v1/report.json` 与 `.codex/scene-particle-mip-374-after-20260729-v1/report.json` 各 1/1，使用 executable SHA-256 `2ea566444267a6eef5d7f83da2148c27f9bb9c8f96301ea0407ba8932f1fe820`、CDHash `9de152b091d8d932510e51bb3f35fdd42c8e368f`，texture loaded ratio 1.0、failed frame 0、driver FPS 59.844/59.913。`2470144420` 的 `rosepetals.tex` 为 7-level mip，`3742133044` 可见雪花为 RG88 多 mip；两者 after 帧主体与粒子保持可见。Stars 的 `halo_6.tex` 只有一个 mip（sidecar `nomip:true`），因此该层不应因 mip 修复改变，也不能用其星点残差否定/证明 mip 能力。
- Position oscillation 真实门：保留 full45 提取中的 `Oscillate Position` 命中 16 个样本、38 个 root layer usage、24 个 sample-definition path，包含 snow、wind、dust、fireflies、bird 与 falling hearts。`5911049` 的 `.codex/scene-particle-position-oscillation-targeted-20260729-v1/report.json` 为 4/4；四样本 initial live 为 3,891/2,161/10/219，CPU p95 0.829...1.356 ms、GPU p95 6.470...12.570 ms、driver FPS 60.01...60.23，均 failed frame/drawable miss 0。人工 current/reference comparison 未见公共静态构图回归；单帧不能验证轨迹相位，语义判据以数值门为主。`2470144420` 的 Sakura/Stars 都不使用 Position oscillator，因此本门不用于宣称其花瓣/星点残差被修正。
- 方向性视觉：对比用户放置的官方客户端截图，`2470144420` 的动态花瓣从右上/边缘集中恢复到上半屏广域，exponent=2 后典型尺寸向较小值收敛；Stars 也从正象限扩展为全宽覆盖。当前帧仍比官方截帧有更多中大近景花瓣，动态 halo 星点更亮、亮核更大、视觉密度更高；圆形小点中还混有背景静态资产，扁平点受 layer 非等比缩放，不能把两种形态当成两个粒子算法。作者 Stars rate=40、lifetime=4...8、无 prewarm，单帧时刻不能独立推出实例数或授权全局降 alpha/rate。
- REFRACT 当前 full45 初始计数为 `2131872317=0`（仅晚生成 child）、`2419444134=1`、`2998757800=6`、`3768229922=2`、`3769688830=1`、`3770444459=4`，合计 14；旧 World-space 批次的性能数字只保留为历史观测。`3769688830` 作者/宿主目标表现约 30 FPS，且当前没有统一性能阈值或 A/B 基线，不能把 PASS 写成性能健康。
- 运行门：`c654571`/`f02f41d`/`a5a951f` 依次把 registry 扩到 15-key，`9748a8c` 扩到 18-key，`8a27089` 扩到 20-key，22-key 批次新增 `particle/halo_6`、`particle/star` 并按官方 alpha 场重拟合八个静态 key。`b86db59` 批次以真实目录重建 45 样本隔离副本：固定 13 样本门 PASS 13/13、particle `66/76`；完整 45 样本门 44/45、particle `110/131`（`.codex/scene-nested-child-full45-20260727/report.json`，SHA-256 `3723abdb…`），唯一失败为 `3743305891` 的 masked Opacity layer 23 `authored effect graph layer failed`——该缺陷与 particle 无关，根因是 mp4 payload 视频层从不加载 per-layer effect 资源，已在 opacity 视频层批次修复：定向复跑 PASS、固定 13 样本门 PASS 13/13（隔离临时根，报告 SHA-256 `5b6ad5f4bc82653da0ecc20be5d2bdfd3626ca1bc213acc494bbf5705f756e5e`，PASS 后按 `.codex` 治理清理临时目录），完整 45 样本门未在该修复后复跑。`3088601835` 升至 `19/19`（matrix-code 两层解除 nested 阻断），Snow layers `513/534` 继续以 `64x64 additive` 执行；`test_scene_particle_runtime.py` 的真实缓存门确认 `2974757317` 43 列 matrix head 全部创建、trail 单 template 共享且 instance ≥ 43、author-off 的 `2938612768` 不创建，`snowstormfog` 两层 child instance 与纹理宽 128 继续成立。合成门锁定 nested 跟随链（trail origin 逐帧等于 head 位置、head 死亡即回收）、depth-three `nestedDepthUnsupported`、nested static `nestedStaticChildUnsupported` 与 depth-two 80-follower 截断到 64 的 `nestedAggregateSystemBudget` 正反例；origin `1 2 3` 正例，angles、scale、event origin、`nan` origin、probability 反例；continuous、instantaneous+rate、有限/无限 duration、完成回收、Event Follow parent-death 回收与 per-depth 64/跨两层 128-system aggregate budget 均通过。程序纹理不是官方资产，也不是 WE pixel parity。默认隐藏的 `3742133044:189` 与 `3765760121:161` 仍不创建。真实缓存门中 `3768903841` layer 264 执行 eventspawn child；`2131872317` layer 529 在 fixed 60 Hz 第 233 帧生成 1,024 个 eventdeath trail instance，离屏 Metal 覆盖超过 1,000 像素。隔离 45-home 缓存 census 确认语料最深 authored 链两层、depth-two 声明全部 identity eventfollow probability 1；26 样本 census 门锁定 maximumChildDepth 2、63 条 nested 声明，collision/delete、inherit-value-from-event、非空 child CP mapping 均为 0。
- 内置纹理形态判据来自 2.8.42 的历史 `.tex` 解码和逐像素测量。尺寸对齐 `imageWidth`/`imageHeight`；八个静态 key 的公式经三轮参数网格搜索，引用次数加权 RMSE 从 0.0878 降至 0.0373。`beam_1` 是横向椭圆径向光斑，`halo_4` 带全幅低幅外晕，两族光柱均上宽下窄。RGB 三通道与 alpha 同值，匹配 `SceneParticleMetalPipeline` 的预乘白色输入。
- 内置纹理缺口口径：接 bundle 前的历史 census 必须按 slot 0 且排除 `REFRACT` 统计，否则会高估；当时 663 包 / 2,362 个 particle definition 的真实缺口为 3 名 / 78 次（`debris/debris1` 53、`fire/fire2` 17、`beam/hose_4` 8）。当前 resolver 不依赖 catalog，按官方相对路径查找 bundle 资源；Particle slot 0 可直接消费对应纹理。本机固定矩阵只有 6/13 样本可用；当前签名 App 重跑六个样本均完成、image 82/82、particle 11/13、sample-root residue 0，且无 `builtInTextureUnavailable`/`textureLoadFailed`。整门仍因 7 个样本缺失、interpretation v25 与当前 v29 不符，以及 `2902406982` strict graph 旧断言而未成立，不能记 PASS。
- 内置纹理边界：产品 App 当前优先读取 bundle 内 311 个同路径真实 TEX（`TEXB0001`–`TEXB0004` 容器，格式、尺寸与 mip 链逐资产各异）；particle 子树 164/164 个 TEX 均有同路径 sidecar，整个 bundle 的 298 个 `.tex-json` 均可解析，其中 52 个 particle sidecar 声明单一 `spritesheetsequences`。particle runtime 已从 TEX container flags 选择 filter/address 并消费实际 mip levels，RG88 颜色适配也不再丢级。这证明 identity、decode/upload 与项目 sampler 合同，不证明逐资产官方通道/颜色空间/atlas/像素 parity。runtime 仍不读取 sidecar 的 nominal frame geometry；raw TEX frame axes 虽已保留，也尚未改变 Sprite quad aspect，rotated/trimmed atlas 与完整序列播放仍缺。22-key 项目自建程序纹理只在 bundle 缺失时回退，不是 Windows parity。
- 视觉门：eventdeath 证据仍为 `.codex/scene-eventdeath-current-v25-delayed-20260725/report.json` 的 1/1 与 4.5 秒可见烟花。当前 247/374 方向性证据见上；whole-frame motion 同时包含其他动态层，只用于证明画面活跃，不归因成单一粒子字段。所有报告 sample-root residue 0、preview 对照 available；当前 App 身份见统一门。
- 边界：TEX bit mapping 来自项目 clean-room container/sidecar corpus，flag 8 的 border 语义当前只降级 clamp-to-edge；没有 Windows sampler golden。Random exponent、centered Box、normalized-lifetime oscillation 与 Turbulence 都是项目 clean-room 数值合同；audio-enabled profile 继续零执行并留诊断。Rope 仍 unsupported；RopeTrail 仅严格单 renderer/Screen 子集，Length 时间解释、segments 默认值/范围、线性插值、quad 接缝、fadealpha、死亡尾迹与暂停/seek/reset 均未获官方或 Windows golden 证实。REFRACT、World-space、Child、Lighting/Collision 等既有有限边界不变。

<a id="e-puppet-bc"></a>
### E-PUPPET-BC: Puppet bind-pose/静态 attachment/严格 MDLA 播放、BC premultiplied 解码与 Puppet 材质边界

- 实现提交：mesh/预算内 BC/REFRACT `8bac86e`，超预算/多 image BC GPU 归一 `18d0056`，静态 attachment `49ee89a`，MDLA IR/rig/full-TRS `ca6d841`/`56f92a2`/`2be2b44`，严格播放 `f1ee79b`。代码：[SceneMdlPuppetAnimationReader.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Format/SceneMdlPuppetAnimationReader.swift)、[SceneMdlPuppetRigReader.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Format/SceneMdlPuppetRigReader.swift)、[ScenePuppetAnimationEvaluator.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Rendering/ScenePuppetAnimationEvaluator.swift)、[ScenePuppetPlaybackState.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Rendering/ScenePuppetPlaybackState.swift)、[SceneCompressedTextureUploader.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/SceneCompressedTextureUploader.swift)，以及既有 mesh/attachment/BC/particle asset readers。
- 自动门：[test_scene_puppet_animation.py](../../../script/tests/test_scene_puppet_animation.py)、[test_scene_puppet_mesh.py](../../../script/tests/test_scene_puppet_mesh.py)、[test_scene_puppet_playback.py](../../../script/tests/test_scene_puppet_playback.py)、[test_scene_layer_world_frame.py](../../../script/tests/test_scene_layer_world_frame.py)、[test_scene_bc_texture_decoder.py](../../../script/tests/test_scene_bc_texture_decoder.py)、[test_scene_bc_texture_uploader.py](../../../script/tests/test_scene_bc_texture_uploader.py) 与 [test_scene_particle_assets.py](../../../script/tests/test_scene_particle_assets.py)。播放门锁定 full TRS、`T * Rz * Ry * Rx * S`、hierarchical world、`animatedWorld * inverse(bindWorld)`、normalized four-weight LBS、source-FPS 离散 loop，以及 mixing/dynamic visibility/malformed declaration 的 bind-pose 回退；BC uploader 门锁定超预算、多 image BC3 的 GPU straight->premultiplied 像素、静态首帧裁切与旋转 frame fail-closed；缺失可选数字的 parser 栈溢出也有回归。
- 定向运行门：`.codex/scene-puppet-animation-20260725/targeted-v1/report.json` PASS，SHA-256 `d656addbbdec26d70a4e3b57c9c1195b042701a10fce51f1eb421f8fa073fbce`；`3747492842` 六层记录 `puppet animation OK`，interpretation v25，whole-frame `changed_ratio=0.238523`。同一样本旧 v24 无 hover 对照 `.codex/scene-puppet-animation-20260725/control-v24-v1/report.json` SHA-256 `75ab9cde8df0d9ededa4e13534f550f2834d928e2d8252f7c22c837de11406f0`，既有 effect 仍产生 `0.177934` 运动；因此约 `0.060589` 的增量只作方向性证据，不能把全帧变化全部归因于 Puppet。
- BC 真实证据：完整 45 样本 TEX census `.codex/scene-bc-census-full45-20260725.json`（SHA-256 `29decdec2e3de8acb1c31e4a9ecba8b2e759288d16ade43f517a78fcb6237268`）只发现一个超预算/多 image BC 载荷：`3768903841` 的 BC3 `7680x7560`、5 images、140 sprite frames，由 image layers 195/198/199/200/201 共用；每 image 是 4x7 atlas、28 帧。`18d0056` 后这些层各加载 `1920x1080` authored 首帧，不再绘制整张 atlas 或 straight-alpha matte。定向 `.codex/scene-bc-targeted-20260725/report.json` 1/1（SHA-256 `8f747d699c7ad006c62b981fc8d0510bc6826d8eecddfba5dbcd7d53164a8107`）、固定与完整两层统一门均通过。
- 正反运行门：完整 45/45 与固定 13/13 均为 v25、surface `1 -> 0`、sample-root residue 0。`3768229922` 两层进入 30 FPS animation，blend 1.3 层回退；`3769688830` 一层进入 30 FPS animation，additive/property-bound/multi-layer 声明回退。首轮完整门暴露 `doubleValue(nil)` 递归崩溃；修复后 `2998757800`/`3028090166` 定向 2/2 与完整门均通过，旧 MDLV0016 仍 fail closed。
- 边界：当前是 `L3 executed-degraded` 的严格单 clip CPU LBS，只接受已验证的 MDLV0023/MDLS0004/MDLA0006、loop、静态 visibility、non-additive、blend/rate=1 且无 blend-in/out。没有插值、rate/blend/mixing、动态 attachment follow、constraint/IK/physics/channels/clipping、GPU skinning 或 Windows WE golden。Particle strict `genericparticle` REFRACT 已由 E-PARTICLE 独立执行；Puppet/其他材质的 REFRACT 不因此获得执行能力。BC 解码性能门为未优化 Debug 构建 4K BC1 约 0.3 s（行级并发）。跨 image BC sprite 当前只提供可表达的 authored 首帧静态 fallback，5-image/140-frame 动画播放、旋转 frame 与完整 sequence 生命周期均未实现。

<a id="e-provider"></a>
### E-PROVIDER: Texture provider、named target 与静态 dependency blend

- 代码：[SceneFrameTextureRegistry.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/SceneFrameTextureRegistry.swift)、[SceneDependencyRenderPlan.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneDependencyRenderPlan.swift)、[SceneImageBlendRuntime.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Rendering/SceneImageBlendRuntime.swift)、[SceneImageBlendPipeline.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Rendering/SceneImageBlendPipeline.swift)
- 自动门：[test_scene_frame_texture_registry.py](../../../script/tests/test_scene_frame_texture_registry.py)、[test_scene_dependency_render_plan.py](../../../script/tests/test_scene_dependency_render_plan.py)、[test_scene_image_blend_plan.py](../../../script/tests/test_scene_image_blend_plan.py)、[test_scene_user_property_textures.py](../../../script/tests/test_scene_user_property_textures.py)
- 运行门：fallback/file 证据为 `.codex/scene-texture-fallback-293-v3-20260723/report.json`、`.codex/scene-user-texture-final13-r2-20260723/report.json`；双代修复后的定向证据为 `.codex/scene-provider-generation-293-20260723-1035/report.json` 与 `.codex/scene-provider-generation-290-20260723-1036/report.json`。
- 边界：连续帧同 identity、同 `MTLTexture` 的 layer/property/system publication 保持 resource generation；替换或缺席后重现会换代。named target 是当帧重写资源，继续使用 frame epoch。system/media/video 的显式内容 generation、metadata、异步取消和通用 consumer 尚未完成。

<a id="e-utility"></a>
### E-UTILITY: Utility capture 与 named primary target

- 代码：[SceneUtilityLayer.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Rendering/SceneUtilityLayer.swift)、[SceneNamedRenderTargetPool.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneNamedRenderTargetPool.swift)、[SceneDependencyFrameRuntime.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneDependencyFrameRuntime.swift)
- 自动门：[test_scene_utility_layers.py](../../../script/tests/test_scene_utility_layers.py)、[test_scene_named_render_target_pool.py](../../../script/tests/test_scene_named_render_target_pool.py)、[test_scene_framebuffer_capture.py](../../../script/tests/test_scene_framebuffer_capture.py)
- 运行门：正式矩阵和 `2902406982` 定向报告记录 utility capture、named producer/consumer 与 GPU completion。

<a id="e-effect-ir"></a>
### E-EFFECT-IR: EffectDefinition / Material / authored graph

- 代码：[SceneEffectDefinition.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneEffectDefinition.swift)、[SceneAuthoredEffectRenderPlan.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredEffectRenderPlan.swift)、[SceneAuthoredEffectRenderPlanner.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredEffectRenderPlanner.swift)
- 自动门：[test_scene_effect_definition.py](../../../script/tests/test_scene_effect_definition.py)、[test_scene_effect_render_graph.py](../../../script/tests/test_scene_effect_render_graph.py)、[test_scene_authored_effect_execution.py](../../../script/tests/test_scene_authored_effect_execution.py)
- 运行门：`.codex/scene-effect-ir-formal13-final-20260723/report.json` 与 `.codex/scene-effect-graph-canonical-final-20260723/report.json`；只证明结构和 canonical identity，不证明 GPU execution。

<a id="e-shader-contract"></a>
### E-SHADER-CONTRACT: Shader source contract IR v1

- 代码：[SceneShaderContract.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneShaderContract.swift)、[SceneShaderContractLoader.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneShaderContractLoader.swift)、[SceneAssetCatalog.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/SceneAssetCatalog.swift)、[SceneRuntimeInput.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Runtime/SceneRuntimeInput.swift)
- 自动门：[test_scene_shader_contract.py](../../../script/tests/test_scene_shader_contract.py)、[test_scene_runtime_input.py](../../../script/tests/test_scene_runtime_input.py)、[test_scene_wallpaper_benchmark.py](../../../script/tests/test_scene_wallpaper_benchmark.py)。loader 门覆盖 absolute/`..`/root-stage-include symlink escape、invalid UTF-8、missing/unreadable stage、malformed annotation 和 duplicate identity；benchmark 门覆盖 evidence 必填字段、source/raw hash、stage kind/path/nested arrays、diagnostic schema、host built-in 空 stage 与 canonical SHA。
- 运行门：`.codex/scene-shader-contract-final13-v2-20260723/report.json` 为 13/13；173 contracts（143 authored + 30 host built-in）、286 stages、0 diagnostics。独立 source/IR 复核中 include、annotation、declaration 数完全一致，分别为 `155/1523/2660`。
- 构建门：Scene tests 223 total / 222 pass / 1 skip；签名 App `2.0.8 (268)`，Team `H9QWU9XN8R`，CDHash `033bc40a5ee8dbf0d6bd0e478e9a8c5a875b90b4`，executable SHA-256 `2b6a8d6ee9863de41fd91792f682c2ff0c49ecf1dd15a9909d3d6e924f76bab8`。
- 边界：完整语料的 source/include/annotation/declaration 仍只由本证据证明保真；后续 E-AUTHORED-SHADER 只消费其中严格无 include、framebuffer-only 单 pass 子集，不能反向把其余 contract 升级为可执行。

<a id="e-authored-shader"></a>
### E-AUTHORED-SHADER: bounded framebuffer-only authored source execution

- 代码：[SceneAuthoredShaderExecutionPlanner.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredShaderExecutionPlanner.swift)、[SceneAuthoredShaderFrontend.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredShaderFrontend.swift)、[SceneAuthoredShaderRenderer.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredShaderRenderer.swift)、[SceneOffscreenResolutionPolicy.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneOffscreenResolutionPolicy.swift)、[SceneCaptureGeometry.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Rendering/SceneCaptureGeometry.swift)。
- 准入：单 effect/单 material/零 authored FBO，source contract 必须只有 vertex+fragment 且无 include；material texture/combo/user shader value/alpha writing 为空，render state 为 normal/no-depth/no-write/no-cull；sampler 必须全部声明为 framebuffer。frontend 只接受静态有界控制流、可链接 varying、受支持 typed uniform/常量和 `g_Texture0...7`，其他情况失败关闭。
- 自动门：[test_scene_authored_shader_frontend.py](../../../script/tests/test_scene_authored_shader_frontend.py)、[test_scene_authored_shader_execution_planner.py](../../../script/tests/test_scene_authored_shader_execution_planner.py)、[test_scene_capture_geometry.py](../../../script/tests/test_scene_capture_geometry.py)、[test_scene_offscreen_texture_pool.py](../../../script/tests/test_scene_offscreen_texture_pool.py)、[test_scene_framebuffer_capture.py](../../../script/tests/test_scene_framebuffer_capture.py)、[test_scene_debug_frame_capture.py](../../../script/tests/test_scene_debug_frame_capture.py)。其中投影 extent 门锁定非方形 drawable 像素，分级门锁定普通 effect 2048 与 authored shader 4096 上限，GPU 门锁定 solid 的 1x1 provider 不会再把 effect target 缩成 1x1；调试证据门锁定输出 PNG 保留 drawable 原生像素，不再强制缩到最长边 1280。
- 真实门：`/private/tmp/mwx-314-native-capture.2qGFdQ/report.json` 为 `3141421197` 1/1 PASS，layer 20 authored shader stage 1、failed 0；3024x1964 after 帧与样本目录同尺寸官方客户端截图对拍，恢复连续红色链条、动态旋转和暗部细节，不再由证据层缩图制造模糊。相关公共回归 `/private/tmp/mwx-314-native-fixed13-v3.C0d26W/report.json` 为固定矩阵 13/13 PASS。
- 尺寸与预算：solid effect 请求图层在当前 drawable 上的投影像素；shader uniform 的 physical framebuffer 与 authored mapped size 分开保留。普通 effect 最长边仍为 2048，受限 authored shader 最长边为 4096，pool 驻留预算 128 MiB；这是一项按能力成本分级的公共合同，不含样本 ID 分支。
- 边界：没有 include/preprocessor/combo/material texture、任意 FBO/multi-pass/render state、动态 user/timeline uniform、完整 GLSL 或 Windows 像素 golden。`3141421197` 的 preview 与用户提供的官方引擎截图是视觉依据；错误的 WaifuX bake 不覆盖它们。该子集是 `L3 executed-degraded`，arbitrary custom shader 仍为 `L0`。

<a id="e-mask-slot-combo"></a>
### E-MASK-SLOT-COMBO: texture-slot combo 由贴图存在自动设置

- 2.8.42 取证快照显示：59 份 shader 各有 1 处 `sampler2D` 注解声明 `"combo":"MASK"`，例如 `assets/effects/opacity/shaders/effects/opacity.frag` 的 `g_Texture1` 与 `assets/effects/blurprecise/shaders/effects/blur_precise_gaussian.frag:10` 的 `g_Texture2`。后者同一个槽位同时带 `"combo":"MASK"` 与 `"require":{"ENABLEMASK":1}`，把两类 combo 并列在一行里。
- 语料证据：52 个包、54 份去重 `scene.json`、2140 个 effect pass 中，`MASK` 被显式声明 **0 次**；同一批语料里 `[COMBO]` 块声明的 `ENABLEMASK` 出现 54 次、`BLENDMODE` 251 次。798 个 pass（37.3%）在 `textures` 数组的槽位 1 及之后绑了贴图。
- 结论：texture-slot combo（写在 sampler 注解里的 `"combo"`）不进 `scene.json`，由编辑器按该槽位是否绑图在编译期自动设置；只有 `[COMBO]` 块 combo 才会被写进 `scene.json`。因此「等 `combos["MASK"] == 1` 再启用遮罩」永远不成立，判据必须是槽位有没有绑图。
- 口径提醒：本页其他小节写的「1013 个 `scene.json`」是**文件级**口径——`.codex` 下 1013 份 scene.json 只来自 52 个包（同一包被多次 runtime-home 缓存），实例/pass 计数被按运行次数重复放大约 17 倍。跨小节比较数字前先确认口径。
- 边界：只证明 `MASK` 这一个 texture-slot combo 从不出现在语料里，并不证明所有 texture-slot combo 的默认值语义；也没有官方编辑器行为的直接抓取。

<a id="e-effect-inline"></a>
### E-EFFECT-INLINE: 受限手写 Effect profiles

- Water Waves 顺序边界提交：`1766c76`。
- 代码：[SceneEffectRuntimePlan.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneEffectRuntimePlan.swift)、[SceneInlineEffectRuntime.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneInlineEffectRuntime.swift)、[SceneMetalPipeline.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Rendering/SceneMetalPipeline.swift)
- 自动门：[test_scene_effect_mask_gating.py](../../../script/tests/test_scene_effect_mask_gating.py)、[test_scene_framebuffer_capture.py](../../../script/tests/test_scene_framebuffer_capture.py)
- 运行门：`.codex/scene-waterwaves-ordered-chain-targeted-20260724/report.json` 为 5/5，`1766c76` 阶段固定门 `.codex/scene-waterwaves-ordered-chain-fixed13-final-20260724/report.json` 为 13/13。Water Waves 精确计数为 `3722933264=4`、`3723344874=2`、`3724553795=0`、`2902406982=0`、`2938612768=0`；矩阵同时锁定最小值和最大值。它们是该能力落地阶段的历史门；当前统一门见页首。
- legacy Water Waves 边界：共享 inline uniforms 只能表达一个 effect，不能保持 Blend/Water Flow/Iris 等前序 stage、多个 Water Waves 的独立参数或作者顺序，因此只有唯一可见 Effect 为 Water Waves 时才准入；隐藏 sibling 不阻断，缺失声明 mask 继续 fail closed。`2938612768` 从 legacy count 1 降为 0 后不再全屏夸张扭曲，主要人物构图恢复。`31ae557` 后 exact stock profile 已改走 strict authored-chain backend，但不符合精确指纹的其他声明仍受本边界约束。
- 总边界：当前只是 Foliage/Iris/Water/Chromatic/Perspective-Opacity 等表内子集，不执行 authored shader；Cursor Ripple 已移出 inline 路径，见 [E-EFFECT-CURSOR-RIPPLE](#e-effect-cursor-ripple)。Steam preview 指标没有可靠反映 293 的几何改善，人工 montage 仍是本次视觉判定依据。

<a id="e-effect-cursor-ripple"></a>
### E-EFFECT-CURSOR-RIPPLE: strict 3-pass persistent wave field

- 实现提交：`95bd0c0`。代码：[SceneCursorRippleShaderProfile.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneCursorRippleShaderProfile.swift)、[SceneAuthoredCursorRipplePlanner.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredCursorRipplePlanner.swift)、[SceneCursorRipplePipeline.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneCursorRipplePipeline.swift)、[SceneCursorRippleRenderer.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneCursorRippleRenderer.swift)、[SceneCursorRippleEffectTextureLoader.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/SceneCursorRippleEffectTextureLoader.swift)、[SceneAuthoredEffectCursorRippleIsolation.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredEffectCursorRippleIsolation.swift)。
- 准入与语义：只接受完整 stock 或完整 legacy 两组三 shader canonical/raw SHA-256 配对、exact 3 material node / 2 `rgba8888 fit 256|512` target、精确参数/slot/render-state 与 effect-instance `descriptorID`。项目自有 Metal 实现将高度/速度编码进 RGBA8，按前一帧到当前帧的 effect-local pointer 线段注入，四邻域传播并跨帧保留；`rippleScale/decay/speed/strength` 逐实例消费，slot 1 collision mask 为白色阻断、黑色可传播。官方 [Cursor Ripple 文档](https://docs.wallpaperengine.io/en/scene/effects/effect/cursorripple.html) 只提供编辑器参数和交互用途，不公开 Windows 数值算法；实现不复制 authored shader。
- 自动门：[test_scene_cursor_ripple_pipeline.py](../../../script/tests/test_scene_cursor_ripple_pipeline.py)、[test_scene_graph_render_target_plan.py](../../../script/tests/test_scene_graph_render_target_plan.py)、[test_scene_authored_effect_execution.py](../../../script/tests/test_scene_authored_effect_execution.py)、[test_scene_effect_mask_gating.py](../../../script/tests/test_scene_effect_mask_gating.py)、[test_scene_wallpaper_benchmark.py](../../../script/tests/test_scene_wallpaper_benchmark.py)。真实 Metal 三帧门锁定移动注入产生非零 history、下一静止帧继续传播、全白 collision mask 将 history 压回零；RT 门锁定 `fit 512` 在 16:9 输入下为 `512×288`，且只允许严格 Cursor Ripple 的 `_rt_EightBuffer2` 首读 history。
- 真实门：`.codex/scene-cursor-ripple-targeted-20260729-v5/report.json` 为 3/3，最终提交态 `.codex/scene-cursor-ripple-full45-20260729-v2/report.json` 为 45/45。`2974757317:1200`、`3299228616:246`、`3767343314:17` 各执行 Cursor Ripple 1、isolated 1、GPU succeeded、failed 0；完整门聚合 stage 207、chain 30、Cursor Ripple 3、Clipping Mask 8、graph failed 0。三个正例所在链都含当前 unsupported sibling，所以 planner 先用原 input role 严格验证 Ripple stage，再只把该 stage 重基到 layer source；omitted Tint/Blend/Blend Gradient/Local Contrast/Pulse、Water Ripple/Reflection/Shake 或 Sharpen 逐样本进入报告，不能写成整链兼容。
- 生命周期与性能边界：history table 首次/重建时清零，pool/table 随 surface resize/reset/scene switch/stop 丢弃；单实例是两个最长边 256 或 512 的 RGBA8 state target，加已有全尺寸 chain input/output，继续受 128 MiB transaction 预算。当前没有 pause/seek 专项事件、点击注入、六实例同时可见压力门或统一 GPU p95 阈值。
- 视觉边界：动态波场由 synthetic GPU 门证明，真实 benchmark 只证明三个 stage 在签名 App 内稳定执行并输出非黑帧，没有脚本化鼠标轨迹或锁相截图；因此不宣称波纹半径、传播速度、衰减、折射强度、collision 边缘或 Windows 像素/动态等价。样本目录官方客户端截图和“用户观察说明”继续作为下一轮人工交互对照依据。

<a id="e-effect-water-motion"></a>
### E-EFFECT-WATER-MOTION: exact stock Foliage Sway / Water Ripple / Water Waves / Water Flow profiles

- 实现提交：Water Waves `31ae557`，Water Flow `94aebc5`，Foliage Sway / Water Ripple strict profile `3baf1fc`，effect-instance 资源身份与 Foliage noise consumer `27040be`，exact Foliage typed utility capture `5f3e543`。代码：[SceneAuthoredWaterWavesPlanner.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredWaterWavesPlanner.swift)、[SceneAuthoredWaterFlowPlanner.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredWaterFlowPlanner.swift)、[SceneAuthoredFoliageSwayPlanner.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredFoliageSwayPlanner.swift)、[SceneFoliageSwayShaderProfile.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneFoliageSwayShaderProfile.swift)、[SceneFoliageSwayEffectTextureLoader.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/SceneFoliageSwayEffectTextureLoader.swift)、[SceneWaterRippleEffectTextureLoader.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/SceneWaterRippleEffectTextureLoader.swift)、[SceneFoliageSwayPipeline.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneFoliageSwayPipeline.swift)、[SceneFoliageSwayRenderer.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneFoliageSwayRenderer.swift)、[SceneWaterRipplePipeline.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneWaterRipplePipeline.swift)、[SceneMetalRenderer+EffectMasks.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Rendering/SceneMetalRenderer+EffectMasks.swift)。
- 自动门：[test_scene_effect_instance_texture_loader.py](../../../script/tests/test_scene_effect_instance_texture_loader.py)、[test_scene_authored_effect_chain_planner.py](../../../script/tests/test_scene_authored_effect_chain_planner.py)、[test_scene_authored_effect_execution.py](../../../script/tests/test_scene_authored_effect_execution.py)、[test_scene_framebuffer_capture.py](../../../script/tests/test_scene_framebuffer_capture.py)、[test_scene_utility_layers.py](../../../script/tests/test_scene_utility_layers.py)、[test_scene_opacity_planner.py](../../../script/tests/test_scene_opacity_planner.py)、[test_scene_waterflow_builtin_phase.py](../../../script/tests/test_scene_waterflow_builtin_phase.py)、[test_scene_wallpaper_benchmark.py](../../../script/tests/test_scene_wallpaper_benchmark.py)。loader 门锁定两份遮罩按 `descriptorID` 分离、共享 noise/normal 复用、未计划实例不加载和缺失资源 fail closed；utility 门锁定 content/typed-kind 成对、只有 planned capture 加载资源、其他 backend 默认拒绝。本批完整 Scene suite 644 项通过、2 跳过，当前统一门见页首。
- 运行门：`.codex/scene-waterflow-20260724/full26-final/report.json` 与 `.codex/scene-waterflow-20260724/fixed13-final/report.json` 分别为 26/26、13/13，报告 SHA-256 分别为 `7753412293b17b71a1db966910ed3e2dbbcc4b7fd66d342d5d316f8731f16cf6`、`7488830cc5df8b2012cee10893b162427e64104075ac3045e32f827e6f8ceb74`，矩阵 SHA-256 分别为 `5bc2300aca22a32d31b64d9d19c2d6619a841eeaba672aae5a3e05768143f2e2`、`8e0dd0aa296be3130ea5f9313194c71d76885165dc24938af415ce6eecf25ce5`。签名 App 为 `2.0.8 (268)`、Team `H9QWU9XN8R`、CDHash `a37d51608519fed9d5ae86307654f9aba590357c`、executable SHA-256 `f6ae1d08a294e0e19221537b5864d23157032f3ebd763dbc94dc9db49624ffe4`，staged App 运行前后签名均验证。
- 该批关键样本：`3028090166` 为 succeeded `[86,129]`、Water Flow 2、Opacity 1、chains 1、stages 3；`3768903841` 为 succeeded `[33,37,41,181,188,202,211,220,232]`、Water Waves 4、chains 1、stages 10；当时 `3768229922` 的六段 Water Waves 链按 12 个全尺寸 texture unit 计费，超过默认 96 MiB 池保证的 6 unit，规划阶段保持 stages 0 而非运行时分配失败。当前实现已改为按实际 target extent 计费并使用 128 MiB/8-unit 规划保证，c81a full45 中该样本为 succeeded `[171,231,234]`、Water Waves 6、chain 1、stages 8、failed `[]`。
- 当前 Foliage/Water Ripple 资源语义：planner 从 graph effect key 精确选中实例，不再要求整层只有一个 Foliage；loader 只为会执行的 stage 建 `descriptorID -> resources`，Foliage 分别消费 slot 1 mask 与显式/默认 `util/noise`，Water Ripple 分别消费 slot 1 mask 与 slot 2 normal，renderer 在路径和纹理同时匹配 plan 时才执行。Foliage 的 project-owned MSL 继续使用既有 sway 参数，但 noise 从 SceneStockAssets/样本资源真实采样，不编译或翻译 authored shader。
- 当前运行门：最终 full45 **45/45 PASS**，Foliage Sway 16、Water Ripple 9、Water Waves 20、Water Flow 13、stage 292、chain 52、graph failed/failed frames 0。`2998757800:13` 继续以完整四段链和按 descriptorID 分离的资源执行；`2470144420:13` 的严格终止 Iris 后缀不独立绘制，前五段 Water Waves 1、Foliage 3、Water Ripple 1 原子执行；`3742133044:17` 同理执行 Water Flow 1、Foliage 3。`2473638329` 的后续 Shake 仍不满足同一后缀合同，保持失败关闭。
- profile 边界：stock 路径只接受 exact stock definition/material/raw shader fingerprint、单 material stage、精确 slot/constant/render-state 合同，并执行项目内 MSL，不编译 authored shader。Foliage legacy 只接受一组 exact canonical/raw shader fingerprint、显式 `util/noise` 和已知常量键子集；缺省值由注解默认补齐，其他指纹继续失败关闭。Water Flow 缺失 phase 时只为精确 `particle/normal_ring_smooth` 生成 64x64 R8 项目自有径向纹理；其他缺失路径继续失败关闭。同链上的 stock Opacity 读取 `[nil, mask]` 槽位 1 的遮罩（见 [E-EFFECT-OPACITY](#e-effect-opacity)）。
- legacy 指纹白名单（2026-07-27 批次）：Water Waves 增加 [SceneWaterWavesShaderProfile](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneWaterWavesShaderProfile.swift) 5 指纹（stock + `legacyReversedDirection`/`legacyDirectV1` ×2/`legacyV2`），Water Ripple 增加 [SceneWaterRippleShaderProfile](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneWaterRippleShaderProfile.swift) 3 指纹。逐版本 diff 证明 legacy 数学与 stock 的差异可折算：v1 基向量 `(0,-1)` 与逆向 scroll 均按 `rotate((0,-1),θ)==rotate((0,1),θ+π)` 在 plan 侧 +π 归一（pipeline 零改动）、legacy 无 `g_Exponent`（执行恒 1，`pow(|sin|,1)*sign==sin` 精确等价）、标量 `perspective` 语料全 0（只接受 0，执行端无需修正项）。legacy 允许常量子集缺省回填注解默认、mask 可缺省（v1 无条件采样 default `util/white`=白=1、v2 MASK combo 未启用，均等价无遮罩，pipeline 以 `maskUVScale=0` 哨兵跳采样）、v1 族 definition 无 gizmos；range 按 Float 精度比较（`2131872317` 的 `scale` 0.01 序列化为 `0.009999999776…`）。自动门：[test_scene_waterwaves_profile.py](../../../script/tests/test_scene_waterwaves_profile.py)（fixtures 为 pkg 提取真实字节 [waterwaves_legacy_shaders.json](../../../script/tests/fixtures/waterwaves_legacy_shaders.json)）、[test_scene_water_ripple_planner.py](../../../script/tests/test_scene_water_ripple_planner.py)。第六种 waterwaves 指纹（`1553008362`/`1636394814` label-as-key 语料，0 可执行实例）不纳入白名单。
- 视觉边界：`3028090166` 与 WaifuX SceneBake MP4 的同时间帧 SSIM 约为 `0.798/0.786`，构图和主体位置大致一致，云层、龙身和水面扰动的相位/强度仍有差异。`3765760121` 的 layer 187 现在执行两段 Foliage；日期/时间/星期已由 [E-TEXT-SCRIPT](#e-text-script) 替代占位符，但 benchmark 静态截图仍没有锁定动画相位，不能证明叶片/头发振幅、方向或局部边界与 Windows 相同。该 MP4、Steam preview 和官方截图都只是方向性参考，不是 Windows Wallpaper Engine pixel golden；四类 effect profile 均为 `L3 executed-degraded`。

<a id="e-effect-xray"></a>
### E-EFFECT-XRAY: pointer-driven exact stock X-Ray profile

- 实现提交：`3baf1fc`。代码：[SceneAuthoredXRayPlanner.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredXRayPlanner.swift)、[SceneXRayRuntimePlan.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneXRayRuntimePlan.swift)、[SceneXRayPipeline.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneXRayPipeline.swift)、[SceneXRayEffectTextureLoader.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/SceneXRayEffectTextureLoader.swift)、[SceneMetalView+Pointer.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Rendering/SceneMetalView+Pointer.swift)。
- 自动门：[test_scene_xray_planner.py](../../../script/tests/test_scene_xray_planner.py)、[test_scene_layer_cursor_geometry.py](../../../script/tests/test_scene_layer_cursor_geometry.py)、[test_scene_framebuffer_capture.py](../../../script/tests/test_scene_framebuffer_capture.py)、[test_scene_property_binding_program.py](../../../script/tests/test_scene_property_binding_program.py)、[test_scene_wallpaper_benchmark.py](../../../script/tests/test_scene_wallpaper_benchmark.py)。覆盖 exact fingerprint、layer-local pointer、组合区域 source UV、blend/halo/opacity texture、property live target、pointer outside/资源缺失失败关闭及受限前缀。
- 运行门：`.codex/scene-xray-composition-20260724/results-pass3/report.json` 为 3/3；用户在真实桌面壁纸路径手动确认 `2998757800`、`3747492842`、`3757555836` 三个鼠标遮罩均正常。该 3/3 是能力落地阶段门；当前统一门见页首。
- 关键修复：X-Ray 不再直接把整屏 framebuffer 当作 layer source，而是先按组合区域和原 `sourceUV/sourceUniforms` 捕获到 chain input，再以局部 pointer 执行。`3747492842` 因此不再把 authored 3840x2160 下层图错投到屏幕坐标；TEXB0004 metadata 读取也让 400x400 halo 使用作者映射尺寸。
- 边界：`3747492842` 只执行 X-Ray 受限前缀，后续 Film Grain/Bloom/Iris 等 unsupported effect 明确省略并保留诊断；没有 Windows pointer/像素 golden，不能宣称通用 mixed chain、完整 sampler/aspect 或 WE parity。

<a id="e-effect-tint"></a>
### E-EFFECT-TINT: exact stock/legacy Tint profile 与 32 模式 `ApplyBlending` 表

- 代码：[SceneAuthoredTintPlanner.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredTintPlanner.swift)、[SceneTintExecutionPlan.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneTintExecutionPlan.swift)、[SceneBlendModeShaderSource.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneBlendModeShaderSource.swift)、[SceneTintPipeline.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneTintPipeline.swift)、[SceneAuthoredEffectChainRenderer.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredEffectChainRenderer.swift)。
- 自动门：[test_scene_blend_mode_table.py](../../../script/tests/test_scene_blend_mode_table.py) 在 GPU 上跑 33 个 dispatch 分支 × 64 个源色 × 3 个 tint 色 × 2 个 opacity = 198 draw，逐像素对照独立参考；[test_scene_tint_planner.py](../../../script/tests/test_scene_tint_planner.py) 覆盖 exact profile、默认值、binding 与拒绝矩阵；[test_scene_framebuffer_capture.py](../../../script/tests/test_scene_framebuffer_capture.py) 锁 blendMode 从 plan 进入真实 Metal chain，并以 premultiplied BGRA 输入断言 masked mode 0 的 straight-RGB 混合值；[test_scene_tint_rendering.py](../../../script/tests/test_scene_tint_rendering.py) 另把 Tint 接到 Gaussian Blur，断言透明角在前后两段都保持零 RGB/零 alpha，且所有像素 `RGB <= alpha`。
- 语料证据（去重口径：52 个包、54 份 `scene.json`）：16 份含 stock Tint，共 224 个 pass。203 个（90.6%）不声明 `BLENDMODE`，12 个省略 `alpha` 常量（`color` 常量 224 个全带），`MASK` combo **一次都没有**显式声明，也没有出现表外 combo 或表外常量。
- 遮罩执行（2026-07-27 落地，原 30/224 欠账清账）：槽位 1 遮罩按 [E-MASK-SLOT-COMBO](#e-mask-slot-combo) 证据执行——官方语义 `mask = g_BlendAlpha; #if MASK mask *= tex(g_Texture1).r`（stock 相乘；legacy 指纹为 `mask = tex.r` 覆盖，按 [SceneTintShaderProfile](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneTintShaderProfile.swift) 分流）作 `ApplyBlending` 混合权重，不改 alpha 通道，UV 修正 `g_Texture1Resolution.zw/.xy`。遮罩按 descriptorID 由 [SceneTintEffectTextureLoader.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/SceneTintEffectTextureLoader.swift) 装载，取不到贴图整段拒绝；text layer 的 per-effect 资源装载缺口（text 纹理走 CoreText 路径、从不装 effect 贴图）一并补上，`3122339805` 的 8 个 text 层遮罩 tint 因此解锁。可执行面 194/224 -> 224/224（显式 `MASK` combo 声明仍拒绝，语料 0 次）。GPU 门：framebuffer 的 masked case 断言半灰遮罩（0.502）作 mix 权重的逐像素结果。运行门：`.codex/scene-tint-mask-20260727/` 定向 4 样本（`3122339805` TintCount 0->11、stage 3->11、全 succeeded 0 failed；`2974757317`/`2134765860`/`3769688830` 各按 sibling 边界保持并 PASS）与固定 13 门（唯一 mismatch 为 `3122339805` 正向计数，合同刷新后定向 PASS）。已知口径瑕疵：preview 的 route-only 计数不感知 authored chain 接管（text/image 层均虚计，`3122339805` 合同值 11 含 8 个已被 chain 执行的 tint），修正属后续报告口径批次。
- premultiplied 修复与边界（`f26aae5`）：chain source 先按 alpha 解预乘到 straight RGB，执行 `ApplyBlending` 后再预乘；mode 0 按作者语义输出 opaque。首轮 full45 暴露 `2067939514:1063` 的透明 Tint RGB 被 Blur 扩散为白块，修复后定向与第二轮 full45 均通过，人工帧恢复黑色圆形主体。该 A/B 是项目回归门，不是 Windows 像素 golden；半透明边缘的 WE 精确舍入仍未标定。
- legacy 指纹（2026-07-27 建账，`f26aae5` 更新运行事实）：[SceneTintShaderProfile.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneTintShaderProfile.swift) 双指纹白名单（stock canonical `606ea00a…` + legacy canonical `3c418471…`）。`1937925563` 的 12 条频谱链现合计执行 Tint 24；显式 `MASK=1` 的 stock/legacy 分歧仍按既有边界失败关闭。

<a id="e-effect-pulse"></a>
### E-EFFECT-PULSE: 时间驱动多指纹 Pulse profile

- 代码：[ScenePulseShaderProfile.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ScenePulseShaderProfile.swift)、[ScenePulseExecutionPlan.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ScenePulseExecutionPlan.swift)、[SceneAuthoredPulsePlanner.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredPulsePlanner.swift)（+Constants 扩展）、[ScenePulsePipeline.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/ScenePulsePipeline.swift)、[SceneAuthoredEffectChainRenderer+Pulse.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredEffectChainRenderer+Pulse.swift)、[ScenePulseEffectTextureLoader.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/ScenePulseEffectTextureLoader.swift)。
- 官方语义：pulse.frag `AUDIOPROCESSING == 0` 分支 `pow(smoothstep(bounds.x, bounds.y, sin(t*speed + phase + Δ)*0.5+0.5)*amount + noise, power)`，`PULSECOLOR` 走 `ApplyBlending(BLENDMODE, albedo*tintLow, albedo*tintHigh, pulse)`（`BLENDMODE` `[COMBO]` 默认 9），`PULSEALPHA` 乘 alpha，`MASK` 按 `mix(sample, albedo, mask.r)`、UV 修正 `g_Texture2Resolution.zw/.xy`。语料 5 种 shader 指纹中 4 种可执行、逐指纹绑定语义：stock 2.8.42 `Δ=-π/2`、noise UV `(1/12,1/36)`、`noisespeed` 默认 0.5/range [0,1]、输出 `max(0,rgb)`；legacy 三变体 `Δ=0`、noise UV `(1,0.333)`、`noisespeed` 默认 0.1/range [0,0.5]，输出分 `saturate(rgba)` 与 `max(0,rgb)` 两形态（后者含 `CAST3(0)` 与字面 `0` 两个 fragment 指纹）。第五种（`3088601835` 的 `#ifndef AUDIOPROCESSING` 变体）生效公式取决于 WE combo 注入行为，无证据判定 vert/frag 哪条路径生效，fail closed。
- 自动门：[test_scene_pulse_planner.py](../../../script/tests/test_scene_pulse_planner.py) 用 stock bundle + 内嵌 legacy 源现场加载 4 指纹并断言逐 profile 语义参数、canonical/raw SHA-256 变异拒绝、`AUDIOPROCESSING!=0`、SceneScript 绑定、未知常量键（label-as-key）、bounds `x>=y`、越界值、`noisespeed` 逐 profile range 分流、槽位形态（slot 0 绑图/非 `util/noise` 覆盖/空白遮罩/多余槽/paths 不一致/user texture 全拒）、scalar/vec2/vec3 direct binding 的 snapshot 取值与越界回退、graph/definition/material 变异矩阵。[test_scene_pulse_rendering.py](../../../script/tests/test_scene_pulse_rendering.py) 在 GPU 上断言：pulse=0 时像素恒等、同参数不同 `g_Time` 出不同像素、stock 与 legacy 相位语义在同参数下分流（`sin(-π/2)` vs `sin(0)`）、`PULSEALPHA` premultiplied 四通道乘、遮罩 0/1 端点分别回原图/全效果、bounds/blendMode/尺寸非法输入拒绝。
- 语料证据（45 样本目录）：12 个样本 30 个 pass；23 个绑 slot 2 遮罩（`masks/pulse_mask_*` 包内资产）、19 个显式 slot 1 覆盖全部仍是 `util/noise`、7 个 audio 驱动（`AUDIOPROCESSING=3`）、1 个 user binding（`tinthigh`）、1 个 SceneScript 绑定、4 个把编辑器 label 当常量键。census 入口 [scene_effect_census.py](../../../script/scene_effect_census.py)。
- 运行门：最终 full45 Pulse 8；既有 `2131872317` 为 7，`1937925563:13` 新增 1，并与 Water Ripple/Godrays 后缀组成 3-stage chain。用户运行观察确认中央三角随时间呼吸；full45 报告只证明阶段执行与帧成功，不把该观察升级成锁相数值门。
- 边界：stock `stock2842` Pulse 的 audio 驱动已接入 [E-AUDIO-EFFECT](#e-audio-effect)；同一 exact legacy profile 可省略旧编辑器 `replacementkey`，但 definition/material/shader/slots/constants 其余合同仍须全部匹配。其他 legacy Pulse 指纹与 unsupported sibling 继续 fail closed。32/64 档当前只由 exact Simple Audio Bars 消费，不外推到 Pulse；无 Windows 像素 golden，不能宣称 WE parity。

<a id="e-effect-godrays"></a>
### E-EFFECT-GODRAYS: exact stock Radial / legacy Directional God Rays 5-pass profile

- 实现提交：stock `0ff96e0`，legacy Directional `f26aae5`。代码：[SceneAuthoredGodraysPlanner.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredGodraysPlanner.swift)（+Constants 扩展）、[SceneGodraysShaderProfile.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneGodraysShaderProfile.swift)、[SceneGodraysPipeline.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneGodraysPipeline.swift)（+Encoding 扩展）、[SceneGodraysRenderer.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneGodraysRenderer.swift)、[SceneGodraysEffectTextureLoader.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/SceneGodraysEffectTextureLoader.swift)。
- 静态 stock authored shader 证据：downsample2（premultiply + luma `step(raythreshold, ...)`；`util/clouds_256` 双层 noise UV，并以 `smoothstep(0.5±noisesmoothness, noise)` 调制 alpha）→ cast（Radial 30/50 samples 沿 `uv→center` 加权积分）→ gaussian X/Y（`KERNEL` 默认 1=blur7a、显式 0=blur13a）→ combine（`ApplyBlending(BLENDMODE, source.rgb, rays.rgb, rays.a)`，alpha 相加并 saturate；`BLENDMODE=0` 直接返回 rays）。双 half RT `_rt_halfcompobuffer1/2` 为 `scale=2`、`rgba_backbuffer`；combine slot 1 是 graph 的 `previous` source 绑定，不是实例遮罩。Metal 路径是项目自有等价实现，不编译 authored shader；这些静态公式和指纹不证明 Windows runtime 像素语义。
- legacy exact profile：四个 authored shader 指纹必须成组匹配；`CASTER=1` 按旧版定向基向量旋转，Gaussian X/Y 使用旧版原始 13-tap 权重，两个 half RT 为 `rgba8888`。cast/Gaussian 越出 offscreen extent 时采透明，不把边缘 texel 延展成硬直末端；downsample 仍按边缘夹取。combine pass 的实例 named-capture 只允许精确 `_rt_imagelayercomposite_<当前 layer>_a`，解析后仍绑定 graph previous source，不开放通用 named-RT 消费。
- 自动门：[test_scene_godrays_planner.py](../../../script/tests/test_scene_godrays_planner.py) 同时锁 stock 与 legacy 四 shader raw/canonical 指纹、definition/material/RT/combo/常量/named-capture 结构和变异拒绝；[test_scene_godrays_rendering.py](../../../script/tests/test_scene_godrays_rendering.py) 用项目自有 impulse fixture 锁定两个方向、legacy 13-tap Gaussian 对称扩散、cast/Gaussian 透明边界与非有限方向拒绝。
- 语料与运行门：19 个可见 Godrays 实例，现役 full45 执行 15 个；既有 stock 2 个，`1937925563` 的 13 个频谱 bar 全部执行。该样本 succeeded 为 `[13,555,590,625,660,695,730,808,843,878,913,948,983]`，Godrays 13、Shake 12、Tint 24、Pulse/Water Ripple 各 1、chain/stage `13/51`、failed frames 0。
- 边界：只接受两组完整 exact profile；其他 Directional/legacy 指纹、`COPYBG`、未知 RT/combo/material/state 与 unsupported sibling 继续失败关闭。GPU 门证明算法分支、方向响应和边界采样；官方截图对照仍显示条末渐隐、条间幅度和青/紫占比有残差，不证明实时系统音频运动或 Windows 像素等价。

<a id="e-effect-shine"></a>
### E-EFFECT-SHINE: exact modern stock Shine 5-pass profile

- 实现提交：`78b30cd`。代码：[SceneAuthoredShinePlanner.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredShinePlanner.swift)、[SceneShineExecutionPlan.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneShineExecutionPlan.swift)、[SceneShinePipeline.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneShinePipeline.swift)、[SceneShineRenderer.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneShineRenderer.swift)、[SceneShineEffectTextureLoader.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/SceneShineEffectTextureLoader.swift)、[SceneAuthoredEffectShineIsolation.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredEffectShineIsolation.swift) 与共享 [SceneAuthoredEffectStageRebase.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredEffectStageRebase.swift)。实现没有样本 ID 分支；pipeline repository 按需创建并跨 surface/rebuild 复用。
- 准入与执行：只接受 modern stock definition/material/shader exact fingerprint、5 material pass、两张 `scale=2` `rgba_backbuffer` half RT，以及静态有限的 threshold/noise/edge/sample/direction/speed/length/intensity/color/kernel/scale/blend 常量。项目自有 Metal 依次执行 threshold + `util/clouds_256` noise、multi-edge ray cast、Gaussian X/Y 和 authored blend；可选 slot-1 实例遮罩按 descriptorID 装载。graph 中 downsample 与 combine 的 prior-input binding 都经公共 stage rebase 重写，避免 isolated stage 继续读取原混合链 target。fingerprint 只作准入，不编译或复制 authored shader。
- 自动门：[test_scene_shine_planner.py](../../../script/tests/test_scene_shine_planner.py) 锁 exact graph/definition/material/shader、默认值、常量/slot/RT/状态正反例和 legacy/dynamic/`COPYBG` 拒绝；[test_scene_shine_pipeline.py](../../../script/tests/test_scene_shine_pipeline.py) 在 GPU 上锁 threshold identity、方向/颜色变化与非法输入；[test_scene_authored_effect_stage_rebase.py](../../../script/tests/test_scene_authored_effect_stage_rebase.py) 锁同一 stage 的所有 prior-input binding 都重基；framebuffer、pipeline repository、benchmark 与 full matrix 门另锁实际调度、生命周期、计数和 omitted diagnostics。本批聚焦 104 项通过，完整 Scene suite 640 项通过、2 项按既有条件跳过。
- 语料与运行门：45 样本中共 8 个 Shine 声明；`2241938645` 的 1 个 legacy definition 保持拒绝，modern 7 个中 6 个静态执行，`3769364482` 的 1 个 Timeline 动态实例保持拒绝。定向 `.codex/scene-shine-targeted-20260729-v3/report.json` 为 **4/4 PASS**；`3767232084` 为 Shine 1/isolated 1，`3769364482` 为 Shine 4/isolated 1，`3769761761` 为 Shine 1/isolated 1。完整 `.codex/scene-shine-full45-20260729/report.json` 为 **45/45 PASS**、strict 216 stage/33 chain、Shine 6/isolated 3、graph failed 0；三个 `[Blur Precise, Shine]` text chain 完整执行，另三个 isolated stage 明确列出所有 omitted sibling。三个目标样本 GPU p95 分别为 `8.859`、`11.447`、`7.287 ms`，failed frames 均为 0；这是一次运行观测，不是统一性能预算。
- 边界：legacy definition、Timeline/user binding、`COPYBG`/full-frame variant、未知 shader/material/state 和包含多个合格 Shine 的降级形态继续 fail closed。isolated 正门只证明一个严格静态 Shine 可从 layer source 执行，不证明被省略的 Shake、动态 Shine 或其他 sibling；真实样本截图只用于方向性核对，尚无固定时钟、Windows 像素 golden 或 WE parity。

<a id="e-effect-light-shafts"></a>
### E-EFFECT-LIGHT-SHAFTS: strict linear/direct-draw/gradient quad

- 实现提交：strict profile `49a5878`，几何/UV 修正 `38cd1d3`，连续多光束近似 `c47fe22`。代码：[SceneAuthoredLightShaftsPlanner.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredLightShaftsPlanner.swift)、[SceneLightShaftsExecutionPlan.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneLightShaftsExecutionPlan.swift)、[SceneLightShaftsPipeline.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneLightShaftsPipeline.swift)、[SceneLightShaftsQuadGeometry.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Rendering/SceneLightShaftsQuadGeometry.swift)、[SceneLightShaftsEffectTextureLoader.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/SceneLightShaftsEffectTextureLoader.swift)、[SceneMetalRenderer.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Rendering/SceneMetalRenderer.swift)。parser 保留 `shape:"quad"`，runtime descriptor 不再把这种 effect-only 对象误归为普通 container；直接绘制复用公共 layer world/parallax/alpha 路径，不含样本 ID 分支。作者 `point0...3` 只构成 effect-local perspective UV，不再扭曲世界几何；退化、奇异或单位 quad 内分母跨零的映射在 planner 阶段失败关闭。planner 不再校验后丢弃 `rayradius`、`noiseamount`、`noisescale`，三者进入 execution plan 和 Metal uniforms。
- 准入：只接受 exact stock definition/material/vertex+fragment raw/canonical SHA-256、单 effect/单 material/无 authored RT 的 standalone quad、默认 linear `RAYMODE=0`、`DIRECTDRAW=1`、Gradient `RENDERING=1`、normal/no-depth/no-write/nocull、完整静态常量集合；任何实例纹理/user binding、child/dependency、inline script、prior-effect input、unknown field/state/combo/hash 均拒绝。pass 未显式携带 shader 默认贴图时，资源按 sample/project-local 优先、项目自有 `SceneStockAssets.bundle` 回退，严格请求 `materials/util/noise` 与 `materials/gradient/gradient_iridescent`。
- 官方与 clean-room 边界：[Wallpaper Engine Light Shafts 文档](https://docs.wallpaperengine.io/en/scene/effects/effect/lightshafts.html) 只用于确认 Linear/Radial/Corner 与 Color/Gradient 这组模式边界；其中 Linear 是直线型动态光束，方向、角度与长度由作者域控制。官方文档未给 standalone quad 的精确局部 bounds 或私有 shader 公式；3 个 stock preview preset 与 5 个真实实例跨 256/2560/3840 authored canvas 均未声明 `size`，常见 scale 约 2，当前据此采用 unscaled half-canvas extent 的有界推断。Metal 路径是项目独立实现的六条纵向 Gaussian 光带：stock noise 只为每束提供稳定中心/宽度/相位种子，固定错相让左右光束交替但保留非零底亮，作者 radius/noise amount/noise scale/smoothness/speed/scale/intensity/exponent、feather、gradient 与 perspective UV 均实际参与；gradient 只在这一 exact profile 内降低色度并保持 premultiplied 半透明输出。它不复制官方私有 shader/算法，也不把束数、交替节奏或颜色写成 Windows renderer 语义证明。
- 自动门：[test_scene_light_shafts_geometry.py](../../../script/tests/test_scene_light_shafts_geometry.py)、[test_scene_light_shafts_planner.py](../../../script/tests/test_scene_light_shafts_planner.py)、[test_scene_light_shafts_rendering.py](../../../script/tests/test_scene_light_shafts_rendering.py)、[test_scene_light_shafts_texture_loader.py](../../../script/tests/test_scene_light_shafts_texture_loader.py)、[test_scene_authored_effect_chain_planner.py](../../../script/tests/test_scene_authored_effect_chain_planner.py)、[test_scene_authored_effect_execution.py](../../../script/tests/test_scene_authored_effect_execution.py)、[test_scene_framebuffer_capture.py](../../../script/tests/test_scene_framebuffer_capture.py)、[test_scene_pipeline_repository.py](../../../script/tests/test_scene_pipeline_repository.py)、[test_scene_wallpaper_benchmark.py](../../../script/tests/test_scene_wallpaper_benchmark.py)。当前聚焦集合 **51/51 PASS**：除既有四角 perspective UV、固定 unit-quad、半画布 extent、exact 正反准入、资源失败关闭与 premultiplied/alpha 门外，新增锁定 radius/noise amount/noise scale 不能丢失且会改变 GPU 输出、四个时相都有连续光带、左右两侧始终有能量、至少两束分离光带、整体不会熄灭且 alpha 保持半透明。
- 运行门：`c47fe22` 的 `.codex/scene-light-shafts-continuity-targeted-20260729-v3/report.json` 为 **2/2 PASS**（SHA-256 `cc2cb3bbc74620253c9dd7168ab1bd94e47e2fa359b5a6f3fca1a1cb9e10209b`，matrix SHA-256 `21b4de5142f36708e02018c799ee0495518aa55dfaf2eb2f11ff85cc296cdb97`）。`3299228616:601` 为 Light Shafts 1、chain/stage `1/4`、succeeded `[246,303,601]`、failed frames/drawable miss `0/0`、CPU/GPU p95 `1.421/8.960 ms`、driver `60.228 fps`；`3766387484:80` 为 Light Shafts 1、chain/stage `0/1`、succeeded `[80]`、failed frames/drawable miss `0/0`、CPU/GPU p95 `1.553/8.759 ms`、driver `59.880 fps`。另以 6 秒 after delay 对 329 跑 `.codex/scene-light-shafts-continuity-longphase-3299228616-20260729/report.json`，**1/1 PASS**（SHA-256 `02661e6d0bddd206d757f0281c24a1f3787803323589f230e8a07c7e00933624`），Light Shafts 1、failed frames/drawable miss `0/0`、GPU p95 `10.098 ms`；性能数字只作本机观测。`38cd1d3` 的旧 5/5 门仍记录 Color/Corner/Radial 三个运行负例；本批没有扩大准入，自动门已直接覆盖 fail-closed 与四时相 GPU 风险，故未重跑 fixed13/full45。
- 视觉边界：第一轮 `.codex/scene-light-shafts-continuity-targeted-20260729-v1/report.json` 虽为运行 PASS，但形成单条偏右、近不透明的蓝紫光柱并在短 after 帧整体熄灭，按用户观察明确否决。最终 v3 人工并排检查中，`3299228616` 顶部为左右都有的多条青白半透明下照光带，ready、短 after 与 6 秒 after 都保留光带且亮度/分布变化；`3766387484` 同一公共 profile 也由碎点转为较自然的中央浅色雾光。当前只证明共享能力对方向、连续性、覆盖、半透明与偏色的方向性改善，不证明 exact half-canvas extent、束数、交替节奏、颜色、强度或 Windows 像素/动态一致。Radial/Corner、Color、mask、non-direct-draw、ordered-chain 输入和其他历史指纹继续 fail closed。

<a id="e-effect-fisheye"></a>
### E-EFFECT-FISHEYE: exact stock zero-distortion/background-off profile

- 实现提交：`e315743`。代码：[SceneAuthoredFisheyeZeroDistortionPlanner.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredFisheyeZeroDistortionPlanner.swift)、[SceneFisheyeZeroDistortionPlan.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneFisheyeZeroDistortionPlan.swift)、[SceneFisheyeZeroDistortionShaderProfile.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneFisheyeZeroDistortionShaderProfile.swift)、[SceneFisheyeZeroDistortionPipeline.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneFisheyeZeroDistortionPipeline.swift)。准入要求 stock definition/material/raw vertex+fragment SHA、单 effect/node/pass、normal/nocull/depth-disabled、无纹理/RT/command/condition、无 user binding/Timeline/诊断，以及 exact `center=(0.5,0.5)`、`distortion=0`、`size=1.2`、`BACKGROUND=0`；没有样本或 layer ID 分支。
- 官方与 clean-room 边界：[Wallpaper Engine Fisheye 文档](https://docs.wallpaperengine.io/en/scene/effects/effect/fisheye.html) 只确认 Background 控制鱼眼范围外图像是否保持可见，以及 Center/Distortion/Size 的作者含义；没有公开私有 shader 或零 distortion 的像素公式。当前项目独立 Metal backend 在该唯一 profile 内保留半径内原像素，并按 premultiplied alpha 把半径外裁为透明；这实现了 `BACKGROUND=0` 的可见边界，不把私有变形算法写成已知事实。
- 自动门：[test_scene_fisheye_zero_distortion_planner.py](../../../script/tests/test_scene_fisheye_zero_distortion_planner.py) 锁定 exact asset/constant/combo/state 正门及非零 distortion、其他 center/size/Background、动态值和指纹变异负门；[test_scene_fisheye_zero_distortion_rendering.py](../../../script/tests/test_scene_fisheye_zero_distortion_rendering.py) 在 GPU 上断言中心/边内像素保留、角落透明、premultiplied 输出和非法尺寸拒绝，并由 framebuffer/pipeline repository/chain 门覆盖有序调度。
- 运行门：`.codex/scene-audio-bars-329-default-20260729-v1/report.json` 为 `3299228616` **1/1 PASS**；layer `151` 的 Audio Bars -> Fisheye 两 stage 完整执行，preview 日志记 Fisheye zero-distortion 1，utility capture succeeded `[151]`、graph succeeded `[151,246,303,601]`、chain/stage `2/6`、failed frame/drawable miss `0/0`。当前最终构建 full45 继续锁定 Fisheye 1、Audio Bars 4、stage/chain `294/53`、graph failed 0。该真实门只覆盖上述唯一零变形/背景关闭 tuple；非零 distortion、其他 center/size/Background、动态值、aspect/edge sampling 与 Windows pixel golden 继续 fail closed。

<a id="e-effect-blur"></a>
### E-EFFECT-BLUR: strict Blur graph profiles

- 代码：[SceneAuthoredEffectExecutionPlan.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredEffectExecutionPlan.swift)、[SceneAuthoredStandardBlurPlanner.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredStandardBlurPlanner.swift)、[SceneOffscreenEffectRenderer.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneOffscreenEffectRenderer.swift)、[SceneStandardBlurRenderer.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneStandardBlurRenderer.swift)
- 自动门：[test_scene_authored_effect_execution.py](../../../script/tests/test_scene_authored_effect_execution.py)、[test_scene_framebuffer_capture.py](../../../script/tests/test_scene_framebuffer_capture.py)、[test_scene_offscreen_texture_pool.py](../../../script/tests/test_scene_offscreen_texture_pool.py)、[test_scene_wallpaper_benchmark.py](../../../script/tests/test_scene_wallpaper_benchmark.py)
- 运行门：`.codex/scene-graph-target-positive-final-20260723/report.json` 为 3/3，6 个 graph layer GPU succeeded、0 failed；`.codex/scene-graph-target-negative-final-20260723/report.json` 为 3/3，unsupported graph 未误执行。旧 formal13 报告仍是历史全矩阵证据。

<a id="e-effect-local-contrast"></a>
### E-EFFECT-LOCAL-CONTRAST: strict stock Local Contrast graph profile

- 实现提交：`136d35c`。代码：[SceneAuthoredLocalContrastPlanner.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredLocalContrastPlanner.swift)、[SceneLocalContrastPipeline.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneLocalContrastPipeline.swift)、[SceneLocalContrastRenderer.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneLocalContrastRenderer.swift)、[SceneAuthoredEffectExecutionPlan.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredEffectExecutionPlan.swift)。
- 自动门：[test_scene_local_contrast_planner.py](../../../script/tests/test_scene_local_contrast_planner.py)、[test_scene_local_contrast_rendering.py](../../../script/tests/test_scene_local_contrast_rendering.py)、[test_scene_property_binding_program.py](../../../script/tests/test_scene_property_binding_program.py)、[test_scene_framebuffer_capture.py](../../../script/tests/test_scene_framebuffer_capture.py)、[test_scene_wallpaper_benchmark.py](../../../script/tests/test_scene_wallpaper_benchmark.py)。固定 stage golden 覆盖 alpha-weighted 4-tap downsample、13-tap X/Y Gaussian、BGRA source/output、RGBA quarter targets、original alpha 与 strength `0/0.32/1`。
- 运行门：定向报告 `.codex/scene-local-contrast-targeted-20260723/report.json` 为 2/2；正式报告 `.codex/scene-local-contrast-final13-20260723/report.json` 为 13/13。`2902406982` layers `167/177` 执行 Local Contrast，graph succeeded 为 `[167,177,530]`、failed 为空、Local Contrast count 为 2；`2938612768` 的可见 4/5-effect mixed chains 保持 count 0，既有 image blend 5/5。两样本退出均为 `surface 1 -> 0`。
- profile 边界：每个 stage 只接受 exact stock 4 material pass、两个 FBO `scale=4` non-unique `rgba8888` RT、KERNEL0/GREYSCALE0/MASK0、Gaussian `scale=(1,1)`（含官方缺省）和三份 exact authored shader contract fingerprint。mask、greyscale、KERNEL1/2、非默认 Gaussian scale、包含 unsupported stage 的 mixed chain、未知/变更 shader、condition/function 均 fail closed；fingerprint 只用于准入，authored source 不被编译或执行，也没有 generic shader translation。
- parity 边界：Metal backend 采用 stock shader 的非 `HLSL_SM30` 采样语义，没有该分支的 `0.75 / resolution` 偏移；没有 Windows golden，因此状态为 `L3 executed-degraded`，不能宣称跨后端逐像素一致。

<a id="e-effect-workshop-shadow"></a>
### E-EFFECT-WORKSHOP-SHADOW: exact Workshop single-pass Shadow profile

- 实现提交：`809b75e`。代码：[SceneAuthoredWorkshopShadowPlanner.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredWorkshopShadowPlanner.swift)、[SceneWorkshopShadowPipeline.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneWorkshopShadowPipeline.swift)、[SceneWorkshopShadowRenderer.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneWorkshopShadowRenderer.swift)。
- 自动门：[test_scene_workshop_shadow_planner.py](../../../script/tests/test_scene_workshop_shadow_planner.py)、[test_scene_workshop_shadow_rendering.py](../../../script/tests/test_scene_workshop_shadow_rendering.py)。planner 门锁定完整 definition/material/vertex+fragment ShaderContract fingerprint、单 material stage、零 authored FBO/bind/slot、`MASK=0`、`BLENDMODE=0`、normal/nocull/depth disabled、black color、静态有限 alpha/border/offset，并验证 prior-effect input role；未知 hash/combo/state/constant/user binding 均失败关闭。独立 Shadow GPU 门覆盖 premultiplied alpha、border/offset、clamp-to-edge、零 alpha identity 与非法资源/常量失败关闭。
- 运行门：定向 `.codex/scene-workshop-shadow-targeted-20260723-1536/report.json` 为 1/1；正式 `.codex/scene-workshop-shadow-final13-20260723-1540/report.json` 为 13/13。`3724289844` 为 stages 4/chains 1/Shadow 1、succeeded `[20,28,36]`、blocked/failed 为空；全矩阵为 10 stage、1 real chain、Shadow 1、failed 0、legacy blocked 2、route-only 34。
- target/FBO 边界：该 definition 没有 authored FBO；executor 复用 `SceneGraphRenderTargetTable` 的 synthetic full-size input/output texture，并在 ordered chain 中把 precise Blur 输出作为 Shadow 输入。这只证明零-FBO strict profile 与既有 table/transaction 能协作，不升级 authored extent/format/clear/history。
- parity/产品边界：执行器运行仓内手写 MSL，不预处理、翻译或编译 authored shader。它是 Workshop `3488490208/shadow_____________` 的 exact profile，不是官方 45 项 Effect、generic Shadow 或 lighting shadow；`BLENDMODE=0` 没有合法官方 Windows 像素 oracle，因此仍是 `L3 executed-degraded`，不等于 WE 像素等价。当前 GPU 单测使用整数 X offset，尚未像素锁定样本实际 `(2,-2)` 的亚像素插值/Y 方向，也没有 Blur -> Shadow compositor 像素 golden；定向真实样本矩阵是当前集成门。Shadow 常量保持静态，没有新增 live target。

<a id="e-effect-workshop-clipping-mask"></a>
### E-EFFECT-WORKSHOP-CLIPPING-MASK: exact named-provider Clipping Mask profile

- 实现提交：`27a5664`。代码：[SceneClippingMaskContract.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneClippingMaskContract.swift)、[SceneAuthoredClippingMaskPlanner.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredClippingMaskPlanner.swift)、[SceneAuthoredEffectChainRenderer+ClippingMask.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredEffectChainRenderer+ClippingMask.swift)、[SceneDependencyRenderPlan.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneDependencyRenderPlan.swift)、[SceneImageLayerCompositor.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Rendering/SceneImageLayerCompositor.swift)。旧路径只把 `clipping_mask` 当 dependency shortcut，并在完整 authored chain 之后追加 blend；当前把 named provider 在作者原始 stage 消费，chain 成功后不再重复应用。
- 准入合同：只接受 `effects/workshop/2800594362/clipping_mask/effect.json` 的精确 definition/material/raw+canonical ShaderContract 指纹、单 material/零 authored RT、normal/nocull/depth disabled、slot 1 的单一 primary named reference，以及已观察到的 classic `BLENDMODE=0/5` 静态实例形状。另一组 weighted-neutral 指纹也被严格登记，但当前真实声明只位于 composition consumer，不构成执行证据。缺 provider、前向/循环依赖、secondary reference、user texture、未知 slot/combo/常量/state/hash 或不支持 sibling 都整链失败关闭；无样本 ID 或路径 contains 分支。
- 自动门：[test_scene_clipping_mask_contract.py](../../../script/tests/test_scene_clipping_mask_contract.py)、[test_scene_dependency_render_plan.py](../../../script/tests/test_scene_dependency_render_plan.py)、[test_scene_authored_effect_chain_planner.py](../../../script/tests/test_scene_authored_effect_chain_planner.py)、[test_scene_authored_effect_execution.py](../../../script/tests/test_scene_authored_effect_execution.py)、[test_scene_framebuffer_capture.py](../../../script/tests/test_scene_framebuffer_capture.py)、[test_scene_wallpaper_benchmark.py](../../../script/tests/test_scene_wallpaper_benchmark.py)。聚焦 6 模块 104 项通过；GPU 门覆盖 mask 0/0.5/1、`BLENDMODE=0/5`、premultiplied alpha、作者顺序和 dependency 只消费一次，planner/benchmark 锁定精确正负合同。完整 Scene suite 627 项通过，2 项按既有环境条件跳过。
- 语料与运行门：4 个样本共 23 个声明。`2902406982` 有 8 个 image 声明，其中 7 个可见层精确执行、1 个隐藏；`2938612768` 有 3 个 image/solid 声明，其中 layer 322 执行，layer 299 因前置 unsupported `gradient_color` sibling 保持整链关闭，layer 250 隐藏。`2974757317` 的 3 个和 `3768229922` 的 9 个声明都属于 composition consumer，当前保持 Clipping Mask 0。定向 4/4、fixed13 13/13、full45 45/45；完整门合计 Clipping Mask 8、stage 204、chain 30、graph failed 0，精确报告与哈希见本页统一门。
- 视觉与边界：人工对官方截图核对，`2902406982` 左侧三角标题/副标题更清楚，`2938612768` 的歌曲作者文字下方静态黑条残留消失，均为方向性改善。项目复用现有 dependency blend Metal 数学，不编译或复制 authored shader；没有 Windows 逐像素 golden，不能宣称算法等价。composition provider/consumer、weighted profile 的真实执行、`gradient_color -> clipping` mixed chain、动画/SceneScript 常量与其他历史指纹仍未实现。

<a id="e-effect-opacity"></a>
### E-EFFECT-OPACITY: exact stock Opacity profile 与 per-instance 遮罩

- 实现提交：`b8842d8`（exact profile）。遮罩执行代码：[SceneAuthoredOpacityPlanner.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredOpacityPlanner.swift)、[SceneOpacityEffectTextureLoader.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/SceneOpacityEffectTextureLoader.swift)、[SceneOpacityPipeline.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneOpacityPipeline.swift)、[SceneOpacityRenderer.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneOpacityRenderer.swift)、[SceneAuthoredEffectChainRenderer.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredEffectChainRenderer.swift)。
- 官方语义：`assets/effects/opacity/shaders/effects/opacity.frag` 的 MASK 分支是 `float mask = texSample2D(g_Texture1, v_TexCoord.zw).r;` 后 `albedo.a *= mask * g_UserAlpha;`，非 MASK 分支 `mask = 1.0`。遮罩 UV 由 `opacity.vert` 用 `g_Texture1Resolution.zw / .xy` 修正到 mapped/physical 比例。项目侧对应 `SceneLayerEffectTextureLoader.mappedUVScale(for:texture:)`，源纹理是 premultiplied，等价形式为四通道同乘 `mask.r * alpha`。
- 遮罩归属：遮罩挂在 **effect 实例**而不是 layer 上——52 包语料里 108 个 stock opacity pass 绑了槽位 1，分布在 78 个 layer；其中 30 个 layer（38.5%，集中在 `3299228616`）在同一层挂了多个绑不同遮罩路径的 opacity 实例，因此按 layer 绑定会错绑。实现按 `descriptorID` 建 per-effect 字典，与 Shake / Water Flow / Water Waves 同构。槽位是否绑图即遮罩开关，依据见 [E-MASK-SLOT-COMBO](#e-mask-slot-combo)。
- 自动门：[test_scene_opacity_planner.py](../../../script/tests/test_scene_opacity_planner.py)、[test_scene_opacity_rendering.py](../../../script/tests/test_scene_opacity_rendering.py)、[test_scene_property_binding_program.py](../../../script/tests/test_scene_property_binding_program.py)、[test_scene_framebuffer_capture.py](../../../script/tests/test_scene_framebuffer_capture.py)、[test_scene_wallpaper_benchmark.py](../../../script/tests/test_scene_wallpaper_benchmark.py)。planner 锁定 stock definition/material raw SHA、vertex/fragment ShaderContract fingerprint、单 material stage、无显式 `MASK` combo（或显式 `MASK=0`）、零额外资源/命令/condition、有限静态或 direct-binding alpha，并断言槽位 1 的遮罩路径被记入 plan、未绑图时为 `nil`。GPU 门用四个 `mask.r` 互不相同的 texel 锁定逐 texel 乘法（排除「丢遮罩」与「按常量取一次」），另有一条断言换 `maskUVScale` 必须改变采样结果。framebuffer 门让同一层既带 layer 级 opacity 遮罩又带 authored chain，量出 authored 与 legacy 两条路径都得到源 `×mask×alpha`（不是 `×alpha` 也不是 `×mask×alpha²`），第三条用例声明遮罩但不给贴图时整段拒绝。
- 修正记录：authored chain 路径的 capture uniforms 是 `.neutral`（`SceneImageLayerCompositor.swift` 的 `sourceEffectInputs`），所以 `EFFECT_OPACITY_MASK` 不会在 capture 里生效；旧实现的 opacity 后端又不绑遮罩，结果是 authored 路径把遮罩整个丢掉，实测像素为源 `×alpha`。planner 里旧名 `validInactiveMaskTexture`（"inactive"）正是这个误解的痕迹，已改名 `validMaskTextureBinding`。声明了遮罩却取不到贴图时现在整段返回 nil，不静默降级成无遮罩。该 fail-closed 又暴露出第二个既有缺口：`SceneMetalView.loadImageLayers` 只在 solid 分支和 `.loaded` 分支加载 per-layer effect 资源，mp4 payload 视频层在 `makeVideoTextureSourceIfNeeded` 分支直接 `continue`，`SceneLayerEffectTextureStore` 里从来没有它们的条目，于是视频层上的 opacity 遮罩（以及 shake flow、waterwaves 遮罩等同类 per-effect 资源）在执行期必然取不到。`3743305891` layer 23 的 `authored effect graph layer failed` 就是这条路径：遮罩 TEX `materials/masks/opacity_mask_3e77cf22.tex` 物理存在且可解码 1920×1080，路径解析与 descriptorID 绑定都正确，唯一原因是这一层压根没进加载分支。修复把三处加载收敛成 `loadEffectTextures(for:)` 并补进视频分支，属于资源加载缺口而不是 planner 语义或状态通道问题。
- 正向运行门：`.codex/scene-opacity-targeted-290-final-20260723-1722/report.json` 中 `2902406982:[365,372,647,664]` 精确执行，`newproperty50=0.2` accepted 且 surface/window identity 不变。负向 `.codex/scene-opacity-failclosed-293-final-20260723-1725/report.json` 中 candidates `[165,454,626,629,924]` 因 SceneScript 保持 Opacity/stages 0。正式 `.codex/scene-opacity-final13-20260723-1730/report.json` 为 13/13，矩阵 SHA-256 `47d01b05a60368cddc679393fb5dd11d562cd4b69cc9d4bd1f559c179fff4e23`，合计 14 stage、Opacity 4、failed 0、legacy blocked 2、route-only 30。这三份报告都早于遮罩修正，只覆盖无遮罩实例。遮罩执行现在有第一条整包运行门：`3743305891` layer 23 是 mp4 payload 视频层上的带遮罩 stock Opacity，视频层资源加载缺口修好后 `phase=authored-effect-graph layer=23 status=succeeded`，preview 报告出 `opacity effect mask OK opacity_mask_3e77cf22.tex → 1920×1080`，完整矩阵的 `expected_authored_effect_graph_succeeded_layer_ids` 已从 `[54,60,66]` 收紧到 `[23,54,60,66]` 锁定它；其余四项 opacity/stage/graph 计数不变。
- 边界：显式 `MASK` combo 声明（语料 0 次）、SceneScript 计算值、Workshop variants、额外纹理/命令/condition、未知 hash/combo/state 和 unsupported mixed chain 均失败关闭。手写 Metal backend 没有 Windows pixel golden，不等于 generic authored shader 或 WE parity；遮罩采样用 linear/clamp-to-edge，官方 sampler state 未验证。

<a id="e-effect-color-key"></a>
### E-EFFECT-COLOR-KEY: stock-equivalent Color Key 与 relocated 作者副本

- 实现提交：准入 `ee69fc4`，premultiplied-alpha 修正 `0fb0669`。代码：[SceneAuthoredColorKeyPlanner.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredColorKeyPlanner.swift)、[SceneColorKeyPipeline.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneColorKeyPipeline.swift)、[SceneAuthoredEffectChainRenderer+ColorKey.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredEffectChainRenderer+ColorKey.swift)。
- 准入合同：definition 的 replacement key、单 pass 形状和依赖必须闭合到同一 material/shader identity；material/instance/resolved 三层都要求零纹理槽、只允许 INVERT/FLATTEN、normal/nocull/depth disabled。vertex/fragment 路径从作者 identity 推导，raw SHA 必须与实际 source 自洽，可执行源码去掉逐行注释后必须与已验证 stock 2.8.42 指纹相同。因此路径重定位、material JSON 字段顺序和 annotation JSON 键顺序不影响准入，任一可执行 token、texture slot、combo、常量或 render state 改动仍失败关闭；没有 workshop ID、layer ID 或样本 ID 分支。
- 自动门：[test_scene_colorkey_planner.py](../../../script/tests/test_scene_colorkey_planner.py)、[test_scene_colorkey_rendering.py](../../../script/tests/test_scene_colorkey_rendering.py)、[test_scene_wallpaper_benchmark.py](../../../script/tests/test_scene_wallpaper_benchmark.py)。planner 独立构造 relocated identity/material/shader fixture，并分别锁定注释序列化变化接受、可执行 token/material texture/additive state 拒绝；benchmark 把 Color Key count 从只读指标升级为矩阵精确门。
- 运行门：`/private/tmp/mwx-376-committed-matrix.zrMYm2/results/report.json` 为 `3767460992` 1/1 PASS，报告 SHA-256 `9b2e02398dffedbf67af364a4683381e96a8791f69c44ad679cd210f89e2af0b`，对应提交态 full matrix SHA-256 `441e900f128177b4e7dbff657ac1a23b7b2af1cf8dd4b344c0dddbbd2c9ad158`。loaded ratio 1.0，succeeded layers `[17,20,79,945,994]`、Color Key 4、chain 5、stage 24、route-only 3、graph failed 0；该批另有 13/13 历史固定门。
- 视觉边界：用户提供的官方客户端截图与当前原生 3024x1964 帧都保留中心 `X + 半透明矩形` 的无音频唱片盒设计；放射圆点属于 Audio Bars 并随系统音频输入变化，不是贴图黑点。两次帧未锁定同一场景时刻、音频输入和作者属性，不能作为像素 golden。当前只执行静态 alpha/fuzziness/tolerance/color 与 INVERT/FLATTEN；dynamic binding、纹理输入、未知 shader/state/combo、非等价 Workshop variants 和 Windows 同步像素门仍未闭合。

<a id="e-audio-input"></a>
### E-AUDIO-INPUT: Scene 16/32/64 频段音频频谱输入管线

- 实现提交：底座 `eccb36a`，求值器 `72420f0`，接入 surface 求值链路与 consumer 驱动 `72ad176`，32/64 Workshop 档位 `715e078`，可重定位 16-band stereo consumer `e315743`。代码：[SceneAudioSpectrum.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Runtime/SceneAudioSpectrum.swift)、[SceneAudioResponse.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Runtime/SceneAudioResponse.swift)、[SystemAudioSceneSpectrumAnalyzer.swift](../../../MyWallpaperX/Core/Playback/SystemAudioSceneSpectrumAnalyzer.swift)、[SceneDesktopWallpaperHost+AudioDemand.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Runtime/SceneDesktopWallpaperHost+AudioDemand.swift)。
- 自动门：[test_scene_audio_spectrum_input.py](../../../script/tests/test_scene_audio_spectrum_input.py) 覆盖三档 snapshot 形状、非法值归零、inbox 代际与需求生命周期、静音稳定零输入、低到高顺序、同输入同输出、非法采样率/空输入 fail closed，以及同一次左右声道 FFT 同时产生 16/32/64 档；[test_scene_audio_response.py](../../../script/tests/test_scene_audio_response.py) 锁 stock 16 档求值；[test_scene_audio_demand.py](../../../script/tests/test_scene_audio_demand.py) 锁 consumer 驱动、每帧单次采样广播和原 32/64 Workshop Audio Bars 频谱接线，新 16-band profile 的左右通道接线与无 unit clamp 由 [test_scene_workshop_simple_audio_bars_rendering.py](../../../script/tests/test_scene_workshop_simple_audio_bars_rendering.py) 锁定。
- 官方合同来源：stock `shake.vert` / `pulse.vert` 的 `CreateAudioResponse` 逐字一致（测试锁定该事实）。三处按原样保留：官方算出 `audioFrequencyEnd = max(min, max)` 后从未使用；除数是未经 `max()` 修正的 `frequencyMax - frequencyMin + 1`，`min > max` 时循环不执行、除数为负，结果是 `smoothstep(bounds, 0)` 而非非法输入；`saturate` 只作用于 `pow` 结果，`multiply` 在其后相乘，因此响应可大于 1。
- 公开作者合同：[IEngine.registerAudioBuffers](https://docs.wallpaperengine.io/en/scene/scenescript/reference/class/IEngine.html) 只接受 16/32/64，[AudioBuffers](https://docs.wallpaperengine.io/en/scene/scenescript/reference/class/AudioBuffers.html) 的等长 left/right/average 数组每帧更新、从低频排到高频且通常 0...1 但允许超过 1；[shader audio globals](https://docs.wallpaperengine.io/en/scene/shader/variables.html) 也明确三档左右声道为正值且不归一化。当前新 16-band profile 因而不对峰值作 1.0 上限裁剪；这不意味着项目 FFT 的频段映射或数值尺度与 Windows 相同。
- 边界：host snapshot 现承载 16/32/64 档；stock effect 与 [E-WORKSHOP-SIMPLE-AUDIO-BARS](#e-workshop-simple-audio-bars) 的 strict relocated stereo up/down profile 消费 16 档，另两个 exact bottom profile 消费 32/64 档。其他 Workshop shader 与 SceneScript `registerAudioBuffers` 仍未接入，host snapshot 也没有 SceneScript `average` object。频段划分（32 Hz→16 kHz 对数）、-60 dB 归一化和平滑是本项目工程选择，官方未公开，**不构成数值 parity**；与 Web 侧 64+64 是两套独立合同。采集 30 Hz、渲染 60 Hz，不插值，同一快照可在两帧间重复使用并带 generation。
- 生命周期：采集由 consumer 存在性驱动（stock Shake/Pulse 与三个 exact Workshop Audio Bars plan），launch 时声明、teardown 时撤销；`playbackPaused`/`screenLocked`/`systemSleeping`/`displaysSleeping` 沿用既有 `captureAllowed`，不采集时立即归零。macOS < 14.2、无权限或设备失败输出稳定全零并出诊断，不产生假波形。

<a id="e-workshop-simple-audio-bars"></a>
### E-WORKSHOP-SIMPLE-AUDIO-BARS: exact 2084198056 bottom 与 relocated stereo up/down profiles

- 原 32/64 bottom profile 实现提交为 `715e078`；严格可重定位 16-band profile 与 Fisheye ordered follow-up 提交为 `e315743`。代码：[SceneAuthoredWorkshopSimpleAudioBarsPlanner.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredWorkshopSimpleAudioBarsPlanner.swift)、[SceneAuthoredWorkshopSimpleAudioBarsPlanner+Profile.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredWorkshopSimpleAudioBarsPlanner+Profile.swift)、[SceneWorkshopAudioBarsExecutionPlan.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneWorkshopAudioBarsExecutionPlan.swift)、[SceneWorkshopSimpleAudioBarsPipeline.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneWorkshopSimpleAudioBarsPipeline.swift)、[SceneAuthoredEffectChainRenderer+SimpleAudioBars.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredEffectChainRenderer+SimpleAudioBars.swift)。
- 准入合同：original fragment-C 继续要求 exact definition 字段、material raw SHA、vertex/fragment raw SHA、canonical ShaderContract SHA 与 `32+CLIP_LOW` / `64+CLIP_HIGH` bottom/replace tuple。relocated revision 只从 `effects/workshop/<digits>/workshop/2084198056/Simple_Audio_Bars/effect.json` 的数字 relocation 结构派生所有路径，要求相同 raw shader 指纹、完整 material 语义、自洽 canonical contract 与 exact `RESOLUTION=16, SHAPE=9, ANTIALIAS=1, BLENDMODE=31, TRANSPARENCY=4`；未知路径、内容或 combo 变异全部拒绝。三者都要求单 effect/node/pass、normal/nocull/depth-disabled、无纹理/RT/command/condition，并只接受 solid 或无 child/dependency 的 composition current-frame capture；没有 sample/layer ID 分支。
- 参数与 GPU：original profile 保留 slot-origin 相邻 band 插值、居中 spacing、bottom bounds/clip 与 replace premultiplied 输出。relocated profile 精确锁定 Bar Count 35、spacing 0.5、bounds `0...0.5`、AA `0.05,0.05`，left/right 分别形成上/下条，source alpha 作为 intersect 权重后执行 additive premultiplied 输出；`Bar Color` 与 `Opacity` 走 typed binding/snapshot consumer。公开 AudioBuffers/shader 合同允许音量峰值超过 1，因此该 profile 不做错误的 unit clamp。自动门 [test_scene_workshop_simple_audio_bars_planner.py](../../../script/tests/test_scene_workshop_simple_audio_bars_planner.py)、[test_scene_workshop_simple_audio_bars_rendering.py](../../../script/tests/test_scene_workshop_simple_audio_bars_rendering.py) 与 [test_scene_property_binding_program.py](../../../script/tests/test_scene_property_binding_program.py) 锁 exact relocated 正门、路径/指纹/combo/常量负门、左右不对称、峰值大于 1 延伸、intersect alpha/add 色彩、静音透明和 typed color/opacity 目标；original 32/64 像素门继续保留。
- 运行门：原最终二进制定向 `.codex/scene-simple-audio-bars-targeted-algo-final-20260728/report.json` 为 **2/2 PASS**，SHA-256 `865b875ce715fea7f7179cd187e98f662030cb673a9cdae687fed3e7e0eaf3eb`。当前最终 full45 中 `2938612768:563` 以 composition capture 执行、`3122339805:64` 以 solid source 执行，连同既有第三个 profile 与新增 `3299228616:151` 合计 Audio Bars 4。`.codex/scene-audio-bars-329-default-20260729-v1/report.json` 为 329 默认属性 **1/1 PASS**：layer `151` capture succeeded，Audio Bars 1 后接 Fisheye 1，chain/stage `2/6`、graph succeeded `[151,246,303,601]`、failed frame/drawable miss `0/0`；最终 full45 为 stage/chain `294/53`、graph failed 0。
- 视觉边界：三份真实门的系统输入均为静音，因此只证明 exact planner、utility capture、GPU stage、稳定静音和无回归，不证明非零波形在真实桌面的可见动态。329 官方客户端截图显示时间文字上、下各有一组独立白色条，而本次静音截图看不到条形，不能做动态视觉对齐结论；确定性 GPU 门只证明当前 clean-room 算法确实区分左右声道、允许峰值大于 1 并执行所述 alpha/add 数学。没有同步音源/时刻/属性或 Windows 像素 golden，不能宣称 WE parity；其他 revision/shape/transparency/blend/AA、Scroll/SceneScript sibling、partial chain 与 project/fullscreen 继续 fail closed。

<a id="e-audio-effect"></a>
### E-AUDIO-EFFECT: stock/old-editor Shake 与 stock Pulse 的 AUDIOPROCESSING 执行

- 实现提交：stock Shake `72ad176`，Pulse 与共享准入层 `5451cb1`，old-editor Shake audio `f26aae5`。代码：[SceneAudioResponseAdmission.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAudioResponseAdmission.swift)、[SceneAuthoredShakePlanner+Audio.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredShakePlanner+Audio.swift)、[SceneAuthoredPulsePlanner+Constants.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredPulsePlanner+Constants.swift)。
- 官方分支语义：Shake 的时间驱动整段包在 `#if AUDIOPROCESSING == 0` 内，启用后 speed/friction/bounds/flowPhase 均不参与，`DIRECTION == 0` 下 `offset = v_AudioPulse` 再乘 `strength² * flowMask`；Pulse 先无条件 `pulse = v_Pulse`，再由 `#if AUDIOPROCESSING == 0` 分支整体覆盖，启用后 speed/phase/amount/bounds/noise/power 均不参与。两者的项目实现均在 CPU 侧按帧求出标量随 uniform 上传，与官方 `varying`（每 draw 常量）等价而非同址实现。
- 常量默认值：五个 audio 常量在语料中部分可选（`2419444134` 缺 frequencymin/max、`1937925563` 还缺 audioexponent），缺省按 shader annotation 回填。`audiobounds` 默认值两 effect 不同——shake `0.0 1.2`、pulse `0.5 1.0`——由各 planner 传入，共享层不内置（测试锁定）。
- profile 准入：Shake 为 `whitePhaseFallback`/`timeOffsetCombo` 与 exact `legacyUnconditionalPhase`，Pulse 仅 `stock2842`。legacy Shake 只接受完整已登记 shader profile、`DIRECTION=0`、`AUDIOPROCESSING=1...3` 与既有 audio 常量边界；省略 phase 或显式 `util/white` 均归一为内置 R8 白相位。
- 自动门：[test_scene_shake_planner.py](../../../script/tests/test_scene_shake_planner.py)、[test_scene_shake_rendering.py](../../../script/tests/test_scene_shake_rendering.py)、[test_scene_audio_demand.py](../../../script/tests/test_scene_audio_demand.py) 与 Pulse 既有门覆盖常量默认、old-editor definition、profile/slot/combo 变异、静音与非零 pulse；注入非零 pulse 时 GPU 输出相对静音改变超过 20 个像素，NaN 输入失败关闭。
- 边界：其余 legacy 指纹、Shake `DIRECTION` 1/2、material 层 audio combo、未启用 audio 却声明 audio 常量和其他 Workshop shader 继续拒绝。真实 benchmark 使用稳定静音，只证明 runtime chain 与零输入；尚无同步非零系统音频截图或 Windows 数值/像素 golden。

<a id="e-effect-shake"></a>
### E-EFFECT-SHAKE: exact stock / old-editor flow-map Shake profile

- 实现提交：stock `e505a9e`，old-editor definition/audio `f26aae5`。代码：[SceneAuthoredShakePlanner.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredShakePlanner.swift)、[SceneShakePipeline.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneShakePipeline.swift)、[SceneShakeRenderer.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneShakeRenderer.swift)、[SceneAuthoredEffectExecutionPlan+Backend.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredEffectExecutionPlan+Backend.swift)。
- 自动门：[test_scene_shake_planner.py](../../../script/tests/test_scene_shake_planner.py)、[test_scene_shake_rendering.py](../../../script/tests/test_scene_shake_rendering.py)、[test_scene_authored_effect_chain_planner.py](../../../script/tests/test_scene_authored_effect_chain_planner.py)、[test_scene_wallpaper_benchmark.py](../../../script/tests/test_scene_wallpaper_benchmark.py)。覆盖 exact definition/material/raw shader fingerprint、RG8 flow、R8 phase 或 authored white fallback、scene time、Blur 前后顺序、未知 fingerprint 和动态 audio/speed 失败关闭。
- 运行门：现役定向与 full45 见页首；`1937925563` 的 12 个 `legacyUnconditionalPhase`、`AUDIOPROCESSING=3` Shake 全部执行，Shake 12、failed layers 空。既有 `2802243144` stock 正门与 `2134765860` unsupported 变体边界由 tracked full matrix 继续保护。
- 边界：只接受单 pass `MASK=0/NOISETEXTURE=0`、三个 exact shader profile、已验证纹理槽与 `DIRECTION=0`；dynamic speed/noise/direction、遮罩槽绑图、未知 shader/combo/state 和 unsupported sibling 整链失败关闭。当前执行项目内 MSL，不编译 authored shader，也没有 Windows pixel golden。
- legacy 指纹：`legacyUnconditionalPhase`（frag `c4911d58…`、canonical `af9b4c97…`）允许老编辑器省略 `replacementkey`，但仍要求 version/name/group/pass/material/shader 全部精确；phase 可省略或显式 `util/white`，flow 只接受已验证 RG88/ARGB8888。`1636394814`/`1553008362` 的缺 version、label-as-key 常量等其他差异仍失败关闭。

<a id="e-effect-blend-transform"></a>
### E-EFFECT-BLEND-TRANSFORM: exact single-texture Blend 与 identity-only Transform

- 实现提交：`fde0951`。代码：[SceneAuthoredBlendPlanner.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredBlendPlanner.swift)、[SceneBlendPipeline.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneBlendPipeline.swift)、[SceneBlendEffectTextureLoader.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/SceneBlendEffectTextureLoader.swift)、[SceneAuthoredTransformPlanner.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredTransformPlanner.swift)、[SceneAuthoredEffectChainRenderer+Blend.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredEffectChainRenderer+Blend.swift)、[SceneAuthoredEffectChainRenderer+Transform.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredEffectChainRenderer+Transform.swift)。
- 自动门：[test_scene_blend_planner.py](../../../script/tests/test_scene_blend_planner.py)、[test_scene_blend_effect_texture_loader.py](../../../script/tests/test_scene_blend_effect_texture_loader.py)、[test_scene_blend_resource_routing.py](../../../script/tests/test_scene_blend_resource_routing.py)、[test_scene_transform_planner.py](../../../script/tests/test_scene_transform_planner.py)、[test_scene_authored_effect_chain_planner.py](../../../script/tests/test_scene_authored_effect_chain_planner.py)、[test_scene_framebuffer_capture.py](../../../script/tests/test_scene_framebuffer_capture.py)、[test_scene_wallpaper_benchmark.py](../../../script/tests/test_scene_wallpaper_benchmark.py)。覆盖 exact definition/material/raw shader 指纹、常量/combo/render state、asset/property texture fallback、作者顺序、alpha 只应用一次、未知指纹/非 identity/dynamic user 或 timeline binding 失败关闭，以及 45 个样本的 Transform 零值/正值矩阵合同。
- Blend 边界：只接受单 pass、无 authored RT、slot 1 单纹理、`BLENDMODE=0`、`NUMBLENDTEXTURES=1`、`TRANSFORMUV=0`、`TRANSFORMREPEAT=0`、`WRITEALPHA=0`、`OPACITYMASK=0`、中性 alpha/angle/offset/scale 和有限 multiply；asset texture 可由已声明的 property texture 覆盖。旧 static dependency/provider blend 是独立路径，不能作为官方 Blend Effect 的执行证据。其他 blend mode、多纹理、T2-T6、UV transform/repeat、opacity mask、write alpha 和动态 shader 值继续拒绝。
- Transform 边界：只接受 stock 2.8.42 单 material、无 authored RT、`MODE=1`、`CLAMP=1` 且 `angle=0`、`offset=(0,0)`、`scale=(1,1)` 的 identity profile；执行复用 framebuffer capture/copy，不实现 transform 数学。`2067939514` 两个 scale 的 `valueKind=binding` 没有可执行 user/timeline binding，fallback components 均为 `[1,1]`，因此只按静态 identity fallback 执行并逐项输出 `unsupported-dynamic-binding-static-fallback`；这不代表 dynamic scale 或 live update 已支持。非 identity center/offset/rotation/scale、repeat/`CLAMP=0`、Vertex/UV variants 与未知 hash 继续拒绝。
- 运行门：full45 报告 `.codex/scene-transform-full45-c81a-final-20260728/report.json` 为 **45/45 PASS**。206 的 succeeded layers `[65,161,342]`、failed `[]`、Blend 2、Transform 2、static fallback 2、chain 2、stage 7、route-only 1；两条 fallback diagnostics 精确落在 layer 65/161 的 effect 2 scale。定向长窗口 `.codex/scene-transform-2067939514-c81a-final-20260728/report.json` 为 **1/1 PASS**，报告 SHA-256 `d47f87ad1a4c35af29d0102d23395b68e5a2f345df05e3c245e8105b3cf9c8cd`，matrix SHA 与 full45 相同；startup `1769.806 ms`，测量 `7.093 s`，driver `60.059 fps`、completed `59.917 fps`、pre-encode p95 `7.734 ms`、GPU p95 `5.515 ms`、failed 0、drawable miss 0。这是一次定向观测，不是长期性能预算。
- 视觉边界：新帧人工可见山景、雪粒子、中心黑圆/白边和 Windows 标志，与用户提供的同样本官方客户端截屏方向一致；viewport/crop、粒子时相和精确像素未锁定，不能写成 Windows golden、logo parity 或视觉等价。

<a id="e-effect-chain"></a>
### E-EFFECT-CHAIN: ordered strict effect-chain scheduler

- 实现提交：scheduler `b541867`，各 backend 见对应专项；按真实 texture extent 的预算准入为 `f26aae5`，严格终止 Iris 内联后缀为 `8f4cd30`。代码：[SceneAuthoredEffectExecutionChain.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredEffectExecutionChain.swift)、[SceneAuthoredIrisInlineSuffixPlanner.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredIrisInlineSuffixPlanner.swift)、[SceneAuthoredEffectChainRenderer.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredEffectChainRenderer.swift)、[SceneOffscreenTexturePool.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneOffscreenTexturePool.swift)、[SceneImageLayerCompositor.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Rendering/SceneImageLayerCompositor.swift)。
- 自动门：[test_scene_authored_effect_chain_planner.py](../../../script/tests/test_scene_authored_effect_chain_planner.py)、[test_scene_offscreen_texture_pool.py](../../../script/tests/test_scene_offscreen_texture_pool.py)、[test_scene_framebuffer_capture.py](../../../script/tests/test_scene_framebuffer_capture.py)、[test_scene_blend_planner.py](../../../script/tests/test_scene_blend_planner.py)、[test_scene_transform_planner.py](../../../script/tests/test_scene_transform_planner.py)、[test_scene_wallpaper_benchmark.py](../../../script/tests/test_scene_wallpaper_benchmark.py)。planner/pool 门覆盖 identity、连续输入、prior-effect role、整链预算/LRU 原子回滚；synthetic GPU 门覆盖实际 stage texture transfer、末段合成、Blend 与 identity Transform、alpha 只应用一次和失败不泄漏。
- 集成门：首段只应用一次 layer masks/UV/alpha，后段使用 neutral uniforms 并仅保留 strict effect 自有资源。严格终止 Iris 只有在所有前缀 stage 可执行、Iris 自身与已验证的 inline identity 后缀完全匹配且位于链尾时才允许前缀原子执行；不产生伪 Iris draw，也不按样本 ID 分支。由此 `2470144420:13` 执行五个 Water/Foliage stage，`3742133044:17` 执行四个 Water/Foliage stage。
- 运行门：当前 full45 为 294 stage/53 chain、Pulse 8、Godrays 15、Foliage Sway 16、Water Ripple 9、Water Waves 20、Water Flow 13、Iris suffix 2、Audio Bars 4、Fisheye zero-distortion 1、graph failures 0；历史 fixed13 为 133 stage/18 chain、Iris suffix 1、graph failures 0。两门分别 45/45 与 13/13，fixed13 没有在当前构建上重跑，互不替代。
- 边界：planner 不再用假设的 2048² 输入提前拒绝长链；默认 128 MiB pool 在拿到真实 extent 后按精确字节数原子准入，超预算仍在分配前整条拒绝且不污染 resident state。Iris 非终止、带未知 stage/资源/state、需要独立视觉输出或后接其他 effect 时继续整链失败关闭；其余 backend/profile、generic compose/condition/function、通用 history、SceneScript 与动态 profile 边界不变。真实链没有逐 stage Windows readback，矩阵 PASS 不是视觉等价。

<a id="e-effect-rt"></a>
### E-EFFECT-RT: effect-instance render-target foundation and strict consumer

- 代码：[SceneGraphRenderTargetPlan.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneGraphRenderTargetPlan.swift)、[SceneGraphRenderTargetTable.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneGraphRenderTargetTable.swift)、[SceneOffscreenTexturePool.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneOffscreenTexturePool.swift)、[SceneImageLayerCompositor.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Rendering/SceneImageLayerCompositor.swift)
- 自动门：[test_scene_graph_render_target_plan.py](../../../script/tests/test_scene_graph_render_target_plan.py)、[test_scene_graph_render_target_table.py](../../../script/tests/test_scene_graph_render_target_table.py)、[test_scene_authored_effect_chain_planner.py](../../../script/tests/test_scene_authored_effect_chain_planner.py)、[test_scene_offscreen_texture_pool.py](../../../script/tests/test_scene_offscreen_texture_pool.py)、[test_scene_framebuffer_capture.py](../../../script/tests/test_scene_framebuffer_capture.py)；覆盖 identity/extent/lifetime、history-required、预算/别名、cache/LRU/resize/format/reset、整链 allocation transaction，以及 precise/standard/Local Contrast staged GPU pixels。
- 运行门：旧 graph-target 正向/负向报告各 3/3；最新 `.codex/scene-opacity-final13-20260723-1730/report.json` 为 13/13，继续验证签名、BGRA/RGBA target consumer、零 authored FBO Shadow/Opacity、真实整链 exact count 与失败关闭。
- 边界：resident budget 只限制 cache transaction 提交后的驻留记账，候选创建时瞬时驱动分配可更高。真实样本报告不输出通用 `historyRequired` 原因；该分类由 plan 单元门证明。RGBA8888 已由 stock Local Contrast 与 Cursor Ripple strict profile 消费；Workshop Shadow 只复用 synthetic BGRA input/output，不升级其他 authored target。只有 Precise Blur 两种白名单 material-command 拓扑和 Cursor Ripple exact history 形态进入 scheduler；其他 history consumer、compose、condition/function 仍未执行。

<a id="e-graph-command"></a>
### E-GRAPH-COMMAND: same-frame copy/swap foundation

- 实现提交：`f1c6a10`。代码：[SceneGraphRenderTargetPlan.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneGraphRenderTargetPlan.swift)、[SceneGraphRenderTargetTable.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneGraphRenderTargetTable.swift)、[SceneGraphCommandRuntime.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneGraphCommandRuntime.swift)。
- 自动门：[test_scene_graph_render_target_plan.py](../../../script/tests/test_scene_graph_render_target_plan.py)、[test_scene_graph_render_target_table.py](../../../script/tests/test_scene_graph_render_target_table.py)。plan 按作者顺序保留 command，要求 source/target 已声明且已写入、descriptor 完全一致，并拒绝 condition/compose、读前写、未知 command 与整数/预算错误；Metal runtime 验证 logical map 完整、物理纹理不 alias，copy 后字节一致，swap 后 logical identity 映射交换。
- 运行门：定向 `.codex/scene-copy-swap-targeted-372-20260723-1945/results-pass/report.json` 为 1/1；正式 `.codex/scene-copy-swap-final13-20260723-1955/report.json` 为 13/13，报告 SHA-256 `1399d3ea4a4c00bb5529dc6db7b0fd80e810003c945e04d6a26200944f18fe74`。`3723344874` 记录 copy definition 1、swap definition 2、graph swap node 2，但 function 1/condition 4 继续阻断，stage/chain 仍为 0。
- 边界：这只证明同帧 RT 命令的计划、Metal copy 和身份交换合同；现有 strict material scheduler 尚未消费 command list，copy/swap 不跨帧保留资源，也不意味着 Fluid/Motion Blur、compose、condition/function 或 generic authored shader 可执行。

<a id="e-graph-history"></a>
### E-GRAPH-HISTORY: persistent history render-target seed

- 实现提交：foundation `dcedc2e`，Cursor Ripple 正向 consumer `95bd0c0`。代码：[SceneGraphRenderTargetPlan.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneGraphRenderTargetPlan.swift)、[SceneGraphRenderTargetPlan+Extent.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneGraphRenderTargetPlan+Extent.swift)、[SceneGraphRenderTargetTable.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneGraphRenderTargetTable.swift)、[SceneAuthoredEffectChainRenderer.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredEffectChainRenderer.swift)、[SceneCursorRipplePipeline.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneCursorRipplePipeline.swift)。
- 自动门：[test_scene_graph_render_target_plan.py](../../../script/tests/test_scene_graph_render_target_plan.py)、[test_scene_graph_render_target_table.py](../../../script/tests/test_scene_graph_render_target_table.py)、[test_scene_cursor_ripple_pipeline.py](../../../script/tests/test_scene_cursor_ripple_pipeline.py)。通用非 unique FBO 的读前写仍返回 `historyRequired`；显式 `unique:true` 或严格 Cursor Ripple `_rt_EightBuffer2` 白名单才通过并记录 `requiresHistorySeed`。Metal table 门验证首次 seed clear 为全零、command buffer 完成前不误标初始化、GPU 失败可重试；Ripple 门验证跨帧非零续波和 collision mask。
- 真实隔离门：历史 foundation 门 `.codex/scene-history-full26-final-20260723-2332/results-pass/report.json` 为 26/26、当时没有正向 consumer；当前提交态 `.codex/scene-cursor-ripple-full45-20260729-v2/report.json` 为 45/45，三个真实 Cursor Ripple layer 均执行、graph failed 0。这是第一个真实 persistent history consumer，但三个 layer 都是明确省略 unsupported sibling 的 isolated degraded stage。
- 边界：pool 的 effect/plan cache 负责跨帧 table 复用，reset/resize/switch/stop 会丢弃并重建；当前不保存通用跨帧 logical swap 映射，不实现 seek/pause/fixed-step history、Motion Blur/Fluid 等其他 consumer、generic compose、condition/function 或通用 authored shader。Cursor Ripple clean-room 波场不证明 Windows 数值/动态等价。

<a id="e-graph-interleave"></a>
### E-GRAPH-INTERLEAVE: bounded material-command scheduling

- 实现提交：`ebf44a9`。代码：[SceneGraphNodeScheduler.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneGraphNodeScheduler.swift)、[SceneAuthoredPreciseBlurPlanner+Topology.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredPreciseBlurPlanner+Topology.swift)、[SceneOffscreenEffectRenderer.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneOffscreenEffectRenderer.swift)。
- 自动门：[test_scene_authored_effect_execution.py](../../../script/tests/test_scene_authored_effect_execution.py)、[test_scene_graph_render_target_table.py](../../../script/tests/test_scene_graph_render_target_table.py)、[test_scene_framebuffer_capture.py](../../../script/tests/test_scene_framebuffer_capture.py)、[test_scene_authored_effect_chain_planner.py](../../../script/tests/test_scene_authored_effect_chain_planner.py)。定向 graph/runtime/Metal/semantics 共 72/72；GPU 门锁定 pure material、copy interleave 和 swap interleave 输出差异在阈值内、非空且保持 premultiplied alpha。
- 真实隔离门：2026-07-24 批次以当时目录重建的 26 个副本运行 `.codex/scene-command-interleave-20260724-003051/full26-results-pass2/report.json`，结果 26/26、residue 0、strict stage 24、failed 0；同批 fixed13 `.codex/scene-command-interleave-20260724-003051/final13-results-pass/report.json` 为 13/13、residue 0、stage 14、chain 1、failed 0。`3723344874` 仍为 stage/chain 0，`unsupportedFunctions:1` 与 `unsupportedCondition:4` 继续阻断。
- 边界：Precise Blur 只接受 `material0 -> copy -> material1` 或 `material0 -> swap -> material1`。copy 要求两个 descriptor 匹配的非 unique BGRA target；swap 要求 destination `unique:true`。当前真实 Workshop 集合没有合法正向 interleave 样本，因此 `L3 executed-degraded` 来自 synthetic GPU 正反门；generic FBO command graph、跨帧 mapping/history、Motion Blur、Fluid、generic compose、condition/function 与 authored shader 仍未执行。

<a id="e-graph-legacy-compose"></a>
### E-GRAPH-LEGACY-COMPOSE: exact Blur Precise legacy two-pass normalization

- 实现提交：`4f13daf`。代码：[SceneAuthoredPreciseBlurPlanner+Topology.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredPreciseBlurPlanner+Topology.swift)、[SceneAuthoredEffectRenderPlanner+Resolution.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredEffectRenderPlanner+Resolution.swift)、[SceneAuthoredEffectExecutionPlan.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredEffectExecutionPlan.swift)。只接受 exact `KERNEL=0` Blur Precise 的 `[{"material":"...gaussian_x.json","compose":true},{"material":"...gaussian_y.json"}]`，合成 `_rt_FullCompoBuffer1`，让横向 pass 读取 `previous@0` 并写入该 target，纵向 pass 从该 target slot 0 写 effect output。4K legacy profile 可使用 2048 offscreen cap，blur offset 仍按原始 source extent 归一；显式 authored FBO profile 继续要求 exact extent。
- 自动门：[test_scene_authored_effect_execution.py](../../../script/tests/test_scene_authored_effect_execution.py)、[test_scene_effect_render_graph.py](../../../script/tests/test_scene_effect_render_graph.py)、[test_scene_framebuffer_capture.py](../../../script/tests/test_scene_framebuffer_capture.py)。定向 planner/GPU 共 34/34；generic compose、非 Blur Precise、非 `KERNEL=0`、mixed shape、arbitrary kernel、Refraction 和 scene-background capture 均保持失败关闭。
- 原始样本门：`2067939514` 的 graph target `0 -> 16`、blocker `32 -> 0`、unblocked layer `12 -> 28`，最终 graph SHA-256 `332256f9c917608413e6a56453e36422c99f3e1bd874f833bf46553b53a7cba2`；`2802243144` 分别为 `0 -> 3`、`6 -> 0`、`1 -> 4`，SHA-256 `a2c038386e38447162f17c00c147f475782d2f93a84e448280164b889fd81923`。两者 strict stage 都仍为 0，完整 effect chain 继续被 Shake 等未支持 sibling 阻断，因此不能宣称原始画面已改善。
- 正向隔离 GPU 门：仅在 `2802243144` 副本中关闭 layer 41 的无关 Shake sibling，保留原 Blur definition/material/shader/参数不变；`.codex/scene-legacy-compose-20260724/positive-results-pass/report.json` 为 1/1，报告 SHA-256 `d09b531ba06d185c39ebabb6aa5d81069bc1b05e991aac9f3f49b6ba6f049546`，layer 41 precise Blur stage 1、failed 0，layers 64/115 继续 legacy blocked。该门证明 executor 可运行，不是未修改样本的视觉通过证据。
- 边界：generic `compose` 整体仍为 `L2`。本 profile 不提供 scene-behind-layer identity，不支持 Refraction、背景捕获、任意 compose 链或 WE 像素等价。

<a id="e-video"></a>
### E-VIDEO: 内嵌 MP4 image-layer 子集

- 代码：[SceneVideoTextureSource.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/SceneVideoTextureSource.swift)、[SceneTextureLoader.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/SceneTextureLoader.swift)
- 自动门：[test_scene_wallpaper_benchmark.py](../../../script/tests/test_scene_wallpaper_benchmark.py) 与现有 frame-context tests
- 运行门：固定矩阵中的 embedded video frame/candidate 计数和共享 host-time 运行日志；seek/pause/loop 精确门仍缺失。

<a id="e-lifecycle"></a>
### E-LIFECYCLE: switch / stop / diagnostics

- 代码：[SceneDesktopWallpaperHost.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Runtime/SceneDesktopWallpaperHost.swift)、[SceneDiagnostics.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Runtime/SceneDiagnostics.swift)
- 自动门：[test_scene_wallpaper_benchmark.py](../../../script/tests/test_scene_wallpaper_benchmark.py)
- 运行门：所有正式矩阵样本退出后 `surface=0`，并记录 timeout、resource、graph、GPU 和 unsupported diagnostics；尚无 30 分钟交互或 2 小时 soak。

## 3. 使用规则

1. 专项表的 `L3` 行必须直接链接本页某个证据包，或自行给出代码、自动测试和运行证据三联。
2. 一个证据包只能证明标题和边界中写明的子集；不能横向给同名但未消费的官方能力升级。
3. 更新运行实现后先生成新的隔离报告，再更新本页、本专题表、总台账和现役计划。
4. 报告丢失、样本集变化或 App 身份变化时，保留历史数字但不得写成当前已复核事实。
