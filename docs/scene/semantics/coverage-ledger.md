# Scene 官方语义与实现覆盖台账

> 状态：现役系统汇总；逐项等级以各专项能力表为准
>
> 最近核对：2026-08-12
>
> 当前系统等级和主要缺口见本页总表；精确 App、矩阵、报告与逐批运行身份统一见 [运行证据索引](runtime-evidence-index.md)。此前逐批 checkpoint 已迁移到 [覆盖台账批次历史附录](coverage-ledger-history.md)，不得用历史批次覆盖当前结论。

本表把已收集的 Wallpaper Engine 作者语义逐项映射到 MyWallpaperX 当前代码、运行证据和下一道验收门。详细语义仍以同目录专题文档为准；这里回答三个问题：官方是否有这项能力、当前播放器走到哪一级、下一步补什么公共能力。

## 1. 口径

> 最新 R4-2 modern Procedural Noise Program checkpoint：`workshop/2906937488/effects/procedural_noise` 的四个当前active subject现由统一Program进入GPU。公共链只跳过两条声明外、注释独占行的畸形`[COMBO]` record：真实形态是flat `options`标签缺opening quote但保留closing quote，每条都必须有词法无条件的合法root-stage counterpart，combo identity相同，且除唯一坏的顶层`options`外所有顶层字段完全相同；declaration-attached、nested伪装、conditional/conflicting/missing counterpart或identity不确定形态继续拒绝。comment中的伪directive不参与schema；uniform-bound loop只能由同stage唯一`staticExact` producer证明并受256次/4096嵌套预算约束，metadata `range/int`不作为运行时clamp或展开依据；signed static initializer、conditional alpha-preserving join、program-scope `constant`、有界compound `mix/lerp`浮点向量缩窄，以及unconditional fixed-point seed的typed资源预载都保持通用且失败关闭。`.codex/scene-procedural-noise-program-r4-2-3767460992-20260809-v14-top-level-options-proof/report.json`因正式full45 entry仍断言旧owner/count/hash而整体**FAIL**，但现场四个Procedural Noise subject均为`admitted-generic / resolved-material / program / encoded-output`，resolved `[17,20,79,945,994]`，executor claimed/encoded/GPU `90/90/90`、failure/deferred 0，48次成功transaction覆盖terminal/compositor/next-frame，graph diagnostic/GPU failure/failed frame均为0，截图非黑且changed ratio `0.91154`。中央仍是`X`占位，画面不代表视觉等价；该checkpoint当时独立可达的legacy `2924967132` backward-dependency owner现已由B13迁入统一GraphExecutor，见下方现役权限合同与[E-R4-B13-EXTERNAL-PRIMARY-PROCEDURAL-NOISE](runtime-evidence-index.md#e-r4-b13-external-primary-procedural-noise)。

> R4-A+C、R4-B1 至 R4-B17 当前静态权限合同：四类 partial recovery 仍保持删除，Program-first、typed pair/logical-target/full-frame/external-primary 与 B10-B13 的生命周期/依赖接管不变；B14-B16依次删除resolved-program legacy yield、旧whole-chain产品调用和Light Shafts独立main-pass fallback。B17再删除compositor、utility planner与text loader默认路径中的四个产品planner引用：任何可见effect若没有resolved claim、authored execution plan或明确suppression，产品draw以`legacy-effect-product-authority / unclaimed-visible-effects`失败关闭，不再由旧planner/offscreen renderer恢复；结构性source-copy与layer color blend仍保留，standalone authored helper只剩R5删除面。utility只允许完整resolved/authored capture，legacy-only effect不再取得capture；text loader默认也不再持有planner，正式diagnostics仍可显式注入summary。manifest因而把`legacy-product-authority` **4→0**、R5 `legacy-authority-total` **7→3**，保留的3处全是observation而不是产品执行；R4 retirement总量 **57→53**。generic named texture、secondary/multiple/mismatch/current-frame缺失继续失败关闭；fixed Text/Texture/Media/AudioBars selector、numeric Workshop dispatch与独立ImageBlend仍待撤权，因此状态继续是 `R4=partial / R5=not_started`。

> R4-A+C 当前运行门：精确补丁快照的 checkpoint **88 modules ALL OK**、Scene 全量 **183 modules ALL OK**、code health 与签名 build verify 通过；`.codex/scene-r4-ac-fixed13-20260810-v1/report.json` 为 **13/13 PASS**，loaded ratio 全 1，failed frame/drawable miss/residue 全 0，executor `1004/1004/1004` / failure 0，graph terminal/transaction `126/126`且无 diagnostic/failed/GPU-failed。`2938612768` 保留独立 ImageBlend `5/5` 和 typed named target/binding `2/2`，证明 rejected authored graph 的 legacy suppression 没有错误吞掉这两类独立 owner。corrected full matrix 已写回，但最终二进制没有运行 fresh full45；fixed13 不能替代 full45，也不改变 R4/R5 状态。精确身份见 [运行证据索引](runtime-evidence-index.md)。

> R4-B1 当前运行门：精确产品快照 code health 为 **852 Swift / 44 locked legacy / 400 行**，Developer ID 签名 build verify 与 deep strict codesign 通过。`2131872317` 使用正式 full matrix 的首轮 v1 只因旧 whole-chain owner/count/hash 预期而 FAIL；解释后只更新这些字段与 fixed base digest，v2 为 **1/1 PASS**。layers `[13,23,74,78,82]` 全部由 `resolved-material-graph` 完成，19 个 exact effect 均 observed、gap/failure 0；executor claimed/encoded/GPU `140/140/140`、failure/deferred 0，五层都有 compositor 与 next-frame，legacy conflict、failed frame、drawable miss为0。ready/after非黑且 changed ratio `0.050386`；该方向门不证明Windows像素等价，也不替代fixed13/full45。精确证据见 [E-R4-B1-PROGRAM-FIRST-PAIR](runtime-evidence-index.md#e-r4-b1-program-first-pair)。

> R4-B2 X-Ray 当前运行门：`3757555836:69` 的 `X-Ray -> Water Flow -> Water Ripple -> Water Flow -> Shake` 五段链现由 `resolved-material-graph` 唯一持有，五段 exact effect 全部 observed，GraphExecutor claim/encode/GPU、compositor 与 next-frame 均闭合，旧 whole-chain count 归零。正式定向报告 `.codex/scene-r4-b2-xray-3757555836-20260811-v2/report.json` 为 **1/1 PASS**；fixed13 `.codex/scene-r4-b2-xray-fixed13-20260811-v1/report.json` 为 **13/13 PASS**，executor `2863/2863/2863`、failure 0，failed frame/drawable miss 0。before/hover/after 均非黑，hover 前后 changed ratio 为 `0.12177/0.11468`；这只证明同样本方向变化和当前 owner contract，不证明 full45、Windows 像素等价或其余 X-Ray topology。精确证据见 [E-R4-B2-XRAY-IDENTITY-OUTPUT](runtime-evidence-index.md#e-r4-b2-xray-identity-output)。

> R4-B6 stock Radial God Rays 历史运行门：`3028090166:13` 的 Water Flow → Water Waves → Standard Blur → Water Ripple → God Rays 五段原子链现由 `resolved-material-graph` 唯一持有；God Rays 保留5个author节点与两张half RT，15/15 exact effect observed，30/30 graph transaction覆盖GPU/compositor/next-frame，legacy conflict、graph failure、failed frame与drawable miss均为0。正式报告`/private/tmp/mwx-r4-b6-godrays-3028090166-20260811-v2-formal.YqUn1D/output/report.json`为 **1/1 PASS**；ready/after非黑、changed ratio `0.376551`，人工核对龙、人物、云层与桥面完整。该门当时只迁移stock Radial/non-legacy形态；其`1937925563` legacy Directional旧owner边界现已由B9取代。B6当时未运行fixed13/full45，不证明性能或Windows/Wallpaper Engine像素等价。精确证据见 [E-R4-B6-GODRAYS-LOGICAL-TARGET](runtime-evidence-index.md#e-r4-b6-godrays-logical-target)。

> R4-B7 modern static Shine 当前运行门：正式full matrix中的旧Shine正执行计数已从4归零；`3767232084:17`七段ordinary-image链与`3769364482:115/122/130`三条CoreText Precise Blur→Shine链现由`resolved-material-graph`唯一持有。正式报告`/private/tmp/mwx-r4-b7-shine-3767232084-3769364482-20260811-v3-formal.Z1V9xd/output/report.json`为 **2/2 PASS**；两样本exact effect分别`7/7`与`6/6`，graph transaction分别`14/14`与`12/12`并覆盖GPU/compositor/next-frame，executor failure/deferred、legacy conflict、failed frame与drawable miss均为0。ready/after均非黑，人工核对人物、动物/火焰、背景与三条动态文字完整可见。legacy definition、Timeline/user binding、COPYBG/full-frame、unsupported sibling及Windows/Wallpaper Engine像素等价仍未证明；fixed13/full45未运行。精确证据见 [E-R4-B7-SHINE-LOGICAL-TARGET](runtime-evidence-index.md#e-r4-b7-shine-logical-target)。

> R4-B8 exact raw Precise full-frame compose pair 历史运行门：`2802243144` 的 raw pair 已从旧whole-chain owner `[41,64,115]` 迁入 `resolved-material-graph`，现resolved layers为`[41,58,64,115]`，16个成功transaction覆盖四层GPU/compositor/next-frame。B8当时只对该raw重建范围保留`2067939514`的dynamic effect visibility旧owner `[65,1063]`；这两层现已由B10迁移。同时收窄B1遗留的utility captured-main回归：只有typed pair leaf且backend显式`supportsUtilityCapture`才可恢复，现役正例为`2902406982:257`、`3299228616:151`与`3768903841:263`；B8 full-frame compose本身仍拒绝captured-main。最终fixed13与full45分别为 **13/13 PASS**、**45/45 PASS**，但这只证明正式owner/admission/disposition/execution矩阵在该App身份上闭合，不证明视觉parity；`3768229922`既有近黑视觉债务仍保留。精确证据见 [E-R4-B8-PRECISE-FULL-FRAME-COMPOSE](runtime-evidence-index.md#e-r4-b8-precise-full-frame-compose)。

> R4-B9 legacy Directional God Rays logical-target 当前运行门：`1937925563` 的13层/51 effect完整链已从旧whole-chain原子迁入`resolved-material-graph`，旧chain/stage/God Rays均归零。13个God Rays stage保留五material、两张scale-2 non-unique `rgba8888` half RT与finite direction/legacy Gaussian；其他38个stage走Program，13个typed God Rays走dedicated logical-target，51/51 exact effect守恒。正式报告`.codex/scene-r4-b9-legacy-godrays-1937925563-20260812-v2-formal/report.json`为 **1/1 PASS**，executor claimed/encoded/GPU `1365/1365/1365`、failure/deferred 0，102个成功transaction覆盖13层GPU/compositor/next-frame。ready/after非黑且changed ratio `0.302184`；这是同样本方向证据，不是Windows像素等价。精确证据见 [E-R4-B9-LEGACY-GODRAYS-LOGICAL-TARGET](runtime-evidence-index.md#e-r4-b9-legacy-godrays-logical-target)。

> R4-B10 user-property effect visibility lifecycle 当前运行门：user-property只在exact typed consumer存在时live；generic visibility或同一property混合active/inactive target会由live-state在提交前整体拒绝，service随后按已保存override重启active Scene。Admission只继续拒绝Timeline与generic SceneScript的逐帧visibility。`2067939514:65/1063`与`2998757800:23`共 **3 layers / 13 stages** 因而从旧whole-chain迁入统一resolved owner；正式报告`.codex/scene-r4-b10-user-visibility-206-299-20260812-v2-formal/report.json`为 **2/2 PASS**。206与299的executor分别为`36/36/36`和`80/80/80`、failure/deferred均0，成功transaction分别`22/22`和`44/44`，GPU/compositor/next-frame集合闭合、legacy conflict为空。正式运行未注入property override，只证明启动后的owner与执行合同；live/relaunch原子性由自动门证明，不据此宣称视觉parity、性能闭环或完整矩阵刷新。精确证据见 [E-R4-B10-USER-PROPERTY-VISIBILITY-LIFECYCLE](runtime-evidence-index.md#e-r4-b10-user-property-visibility-lifecycle)。

> R4-B11 captured-main Standard Blur logical-target 当前运行门：utility captured-main只在execution plan显式`supportsUtilityCapture`且stage topology为`logicalTarget`时进入统一owner，full-frame compose即使声明utility support也继续拒绝。`2902406982:530`的strict四pass/two-quarter-RT Standard Blur已从旧whole-chain迁入`resolved-material-graph`，utility capture成功层为`[257,530]`且failure为空。正式定向报告`.codex/scene-r4-b11-captured-standard-blur-290-20260812-v2-formal/report.json`为 **1/1 PASS**，fresh fixed13 `.codex/scene-r4-b11-captured-standard-blur-fixed13-20260812-v1-formal/report.json`为 **13/13 PASS**；本批未运行full45，也不宣称像素parity。精确证据见 [E-R4-B11-CAPTURED-STANDARD-BLUR-LOGICAL-TARGET](runtime-evidence-index.md#e-r4-b11-captured-standard-blur-logical-target)。

> R4-B12 exact external-primary Clipping Mask 当前运行门：launch capability只接纳单一primary、slot 1、精确provider/dependency/graph的Clipping Mask binding；frame reservation、provider current-frame capture、same-object ready publication与late execute形成一个typed时序合同，ticket阻止compositor再次混入dependency。`2902406982:[70,182,217,245,265,285,791]`、`2938612768:322`、`2974757317:[956,57098]`共 **10 layers / 11 stages**（`:57098`为Clipping Mask→static Opacity）从旧whole-chain迁入`resolved-material-graph`。正式定向报告`.codex/scene-r4-b12-external-clipping-290-293-297-20260812-v3-formal/report.json`为 **3/3 PASS**，fresh fixed13 `.codex/scene-r4-b12-external-clipping-fixed13-20260812-v1-formal/report.json`为 **13/13 PASS**；三样本旧whole-chain均归零，GPU/compositor/next-frame集合闭合。B12当时未运行full45，其后续余量现已由B13接管；两批都不据此宣称视觉parity或R5准入。精确证据见 [E-R4-B12-EXTERNAL-PRIMARY-CLIPPING-MASK](runtime-evidence-index.md#e-r4-b12-external-primary-clipping-mask)。

> R4-B13 exact external-primary Procedural Noise 当前运行门：`3768903841:49`只在active effect `49#effect#54`为exact `legacyWorleyColor`、单node/pass 0、0 authored/logical RT、primary provider `42`、slot 3、blend 0且完整dependency/graph identity闭合时进入统一owner；launch reservation与hidden solid provider的current-frame capture/ready复用同一texture，late execute再次核对consumer/provider/variant/slot/blend/epoch/object。正式定向报告`.codex/scene-r4-b13-external-procedural-noise-3768903841-20260812-v2-formal/report.json`为 **1/1 PASS**，fresh fixed13 `.codex/scene-r4-b13-fixed13-20260812-v1-formal/report.json`为 **13/13 PASS**；layer 49的旧authored owner/Procedural/stage计数归零并进入resolved，exact effect `14/14`、executor `195/195/195`、28个transaction及GPU/compositor/next-frame集合闭合。同一签名App的`.codex/scene-r4-b13-full45-20260812-v1-formal/report.json`为 **45/45 PASS**，旧owner layer/failed/chain/stage、executor failure、GPU failed/failed outcome、timeout/nonzero exit/residue全部为0。该单例合同和full45都不开放其他Procedural Noise variant、generic named texture或视觉parity；whole-chain corpus owner归零也不等于R4完成或R5准入。精确证据见 [E-R4-B13-EXTERNAL-PRIMARY-PROCEDURAL-NOISE](runtime-evidence-index.md#e-r4-b13-external-primary-procedural-noise)。

> R4-B14 resolved-program legacy yield撤权是纯静态架构批次：删除`compileStages`永远为空的三组dedicated参数及其不可达分支，并删除backend的`yieldsToResolvedMaterialProgram`二次裁决；Program编译失败后的typed pair/logical-target/full-frame/external-primary fallback仍由既有Program-first admission处理。manifest同名R4退役门由1降为全局0，总R4 retirement occurrence由60降至59。整合5模块 **31 tests / 99.143 s / OK**、semantics **7/7 PASS**、code health与Developer ID签名build verify通过；产品owner、矩阵与像素路径不变，所以本批不运行benchmark。精确证据见 [E-R4-B14-RESOLVED-PROGRAM-LEGACY-YIELD-REMOVAL](runtime-evidence-index.md#e-r4-b14-resolved-program-legacy-yield-removal)。

> R4-B15 whole-chain产品撤权删除compositor最后一个`renderLegacyAuthoredChain`调用：authored chain只有取得resolved claim才可进入产品执行；否则立即记录`legacy-authored-chain-product-dispatch / resolved-material-claim-unavailable`并返回false，不进入旧planner、base draw、offscreen或telemetry callback。旧helper和stage renderer暂时只作为R5待删surface及像素单测存在。R4同名gate由1降至0，R5 helper surface由2降至1，总R4 retirement occurrence由59降至58。checkpoint **71 tests / 48.507 s / OK**、code health及Developer ID签名build verify通过；本批不改变正式矩阵owner，故不运行benchmark。精确证据见 [E-R4-B15-LEGACY-WHOLE-CHAIN-PRODUCT-RETIREMENT](runtime-evidence-index.md#e-r4-b15-legacy-whole-chain-product-retirement)。

> R4-B16 Light Shafts main-pass fallback撤权删除quad层缺少resolved frame plan时的旧plan/resource/pipeline恢复与direct main-pass encode；只有`transparentDirectDraw` resolved frame plan可进入产品绘制，unsupported/no-claim形态保持不绘制。旧`SceneLightShaftsLayerRenderer.draw`仅保留为R5删除面与测试oracle，不再拥有产品调用。R4同名gate由1降至0，总R4 retirement occurrence由58降至57；本批不改变正式矩阵、shader math或resolved像素路径，因此不运行benchmark，也不新增视觉parity声明。精确证据见 [E-R4-B16-LEGACY-LIGHT-SHAFTS-MAIN-PASS-RETIREMENT](runtime-evidence-index.md#e-r4-b16-legacy-light-shafts-main-pass-retirement)。

> R4-B17 legacy product authority撤权把compositor、utility与text默认路径中的`SceneEffectRuntimePlanner`产品引用由4降至0：无claim可见effect现在失败关闭，旧offscreen执行不再可达；requires-source-copy与layer color blend仍是结构性合成，standalone authored helper只保留为R5 surface。legacy-only utility不再capture，text默认summary为nil；正式diagnostics显式注入的3个planner观察调用仍属于R5 observation面，不拥有产品执行。R4 retirement总量由57降至53，R5 legacy authority总量由7降至3。本批不改变B13正式矩阵owner，不运行benchmark/fixed/full，也不新增视觉或性能结论。精确证据见 [E-R4-B17-LEGACY-PRODUCT-AUTHORITY-RETIREMENT](runtime-evidence-index.md#e-r4-b17-legacy-product-authority-retirement)。

| 等级 | 含义 | 允许的结论 |
|---|---|---|
| `L0 absent` | 当前 Scene 管线没有结构或运行入口 | 未实现 |
| `L1 recognized/preserved` | 能识别声明、保留部分字段或输出诊断 | 已识别，不能宣称可播放 |
| `L2 wired/routed` | 已进入 IR、资源或 Render Graph 路由，但没有可靠执行结果 | 已接线，不能宣称有效果 |
| `L3 executed-degraded` | 有受限执行器和正反测试，仍有明确语义或视觉偏差 | 可用子集，必须同时写明边界 |
| `L4 semantics-verified` | 作者启用、输入、顺序、生命周期和视觉/数值门均与 WE 基准核验 | 该合同可宣称语义兼容 |

系统等级取 schema 保留、作者启用、运行消费、生命周期和语义/视觉门中的最低值，不做平均。某一子集达到 `L3` 不会把同名系统整体升级；`45/45` 当前完整快照门和 `13/13` 历史 fixed13 都只代表各自矩阵在对应实现上通过运行门，不是样本兼容率或 WE 还原率。当前真实目录有 45 个数字目录，本轮 source manifest `skipped=[]`，全部进入可运行矩阵。

当前没有任何完整系统达到 `L4`。官方公开的作者能力已经建立完整索引，但 Wallpaper Engine 没有公开完整稳定的私有 Scene/PKG/TEX 序列化规范，也没有公开 Windows 渲染器实现；未知字段仍须用合法样本、官方 assets 或 Windows golden 继续核验。

> R4-2 Film Grain stock/no-mask owner migration：官方职责路径保持普通 image effect → ordered material pass → authored source/include/helper → framebuffer + stock noise → built-in uniform → Program → layer-local graph execution → final composition。closed registry 只登记 exact `util/noise -> .noise`，使旧式未标注 `g_Texture0` 在 typed auxiliary default 存在时投影为 implicit framebuffer；未知 default、purpose 冲突、mask 与 unsupported topology继续失败关闭。Program 成功时该 stage 由`resolved-material-graph`持有；Program不适用时，即使旧strict compiler能识别完整graph，B15产品门也会失败关闭而不再调用whole-chain renderer或legacy planner。旧helper只保留像素单测。`3767460992:20#effect#702` 的Program正例继续成立；`3747492842:434#effect#545` 的`prefix-omitted`只描述R4-A前的历史报告，现役静态结果是整图`rejected-chain`。2026-08-08的最后一份pre-R4-A+C full45仍为 **14 个样本 / 57 个 resolved layer / 75 个 exact subject**、**44/45，非 PASS**，唯一红项为`2067939514`既有粒子偏差；这些数字没有被本轮刷新。精确历史报告、矩阵与App身份见 [E-EFFECT-FILM-GRAIN](runtime-evidence-index.md#e-effect-film-grain)。这不开放mask、其他Film Grain revision、任意path purpose、Windows动态/像素、R4完成或R5。

> 最新 R4-3 authored Program / combo-default checkpoint：共享frontend继续以静态可证、失败关闭的HLSL分量规则处理浮点向量乘除链，并只为`mat3 inverse(...)`与简单value-type `inout`参数提供typed Metal translation；其他矩阵宽度、数组/畸形`inout`、歧义表达式和未知helper继续拒绝。官方2.8.42 normal material-pass clean-room证据已推翻v42采用的“require-inactive ordinary-code zero”：material显式combo优先，缺值时active annotation的有效`default`进入同一compile map；JSON `require/requireany`在已证player路径不剪枝，项目只把它保真为authoring/editor关系metadata。现役Template保持explicit-only，resolver在active schema验证所有同名default/options后统一供给条件、definedness、普通source与variant/prepared identity；missing-needed、conflict、malformed继续失败关闭，sampler/format resource require仍走独立fixed point。45样本preparation census中只有7个`COPYBG`无显式值且无annotation default的pass由旧接受改为`missing-default`拒绝，其余721个pass不变，没有新GPU owner。fresh `.codex/scene-visible-r4-combo-default-3766387484-20260809-v48-full-system/report.json`为**1/1 PASS**，报告SHA-256`c5d79938ccb104f218b929be1f74ffad57fcc2c4b63f0746ebcc2e4cf3af3768`；两层accepted，executor claimed/encoded/GPU/failure为`170/170/170/0`，20次terminal/成功transaction覆盖`[17,80]`的GPU/compositor/next-frame，graph diagnostic/failed outcome/GPU failure为0，三相截图非黑，before→hover changed ratio`0.503683`。真实`:80`只写`DIRECTDRAW=1, RENDERING=1`且`RAYMODE`取default0，所以是Linear/Gradient而非此前误记的Radial/Color；本次`BLENDMODE 31`默认修正对该direct-draw零base分支与旧值0代数等价，不能解释或宣称修复光效。用户观察的正确验收对象是整幅作者运动系统，而不是孤立“头发整块移动”：双眼、人物各区域、叶片/粒子、Water/Shimmer/Depth/Twirl、光线和相对幅度仍未与原定义闭合。v48只证明Program结构执行，不证明视觉正确或Windows同相位像素等价。精确证据见 [E-R4-3-AUTHORED-PROGRAM-CHAIN](runtime-evidence-index.md#e-r4-3-authored-program-chain)。本批未改正式矩阵、未跑fixed13/full45；其他Light Shafts形态、logical target/multi-pass/history/compose及旧产品owner仍未清零，R4-3/R4未完成，R5未准入。

> 最新 R4-3 Water/Shimmer stage-pixel checkpoint：隔离 `3766387484` v51 在同一帧、同一实际 scene time比较 stage 0 Foliage、stage 1 Water、stage 2 Shimmer与terminal stage 8。Water 的 stage-local `rgb Δ≥2` 在七个目标秒均为约`1.0–1.22%`并逐秒改变；Shimmer只在其活跃相位于右腕/饰带 ROI 出现局部正向高光，第2…5秒的黑色/平台相位保持零贡献。两者不再只是resolved-material的结构准入：executor claimed/encoded/gpuEncoded=`268/268/268`、failure=0，20次成功terminal/transaction具有GPU、compositor与next-frame证据，graph diagnostic/failed outcome/GPU failure=0且终端/截图非黑。真实透明度因底图opaque而不变，项目自有二维GPU门另锁Water位移后的alpha与Shimmer alpha preserve。该证据排除“Water/Shimmer从未进入GPU”，但不证明Windows parity、全景视觉闭合、其他variant，或双眼、光线、粒子、Depth/Twirl、头发的相对幅度已正确；R4仍未完成，R5未准入。精确报告、像素指标和自动门边界见 [E-R4-3-WATER-SHIMMER-STAGE-PIXELS](runtime-evidence-index.md#e-r4-3-water-shimmer-stage-pixels)。

> 最新 Scene Camera Shake bounded checkpoint：官方2.8.42 clean-room复核把它界定为每帧一次的Scene级相机位移，不是对象`Shake` effect、随机噪声或按particle flags分派。现役实现只准作者显式开启、有效正交投影与editor范围内有限参数，以absolute `sceneTime`生成确定性XY位移；timeline/path origin与shake先形成唯一working camera，shared view、parallax、pointer和particle都只消费这一状态。真正perspective Scene继续失败关闭。`3766387484` author-on v49与同素材property-off v50均为**1/1 PASS**，报告SHA-256分别为`c8319a9be8f3a7ef8d9990b253f1f4d117ca29b2ea45c6def9224c572940d9d4` / `ed5812c2ca7db1e9b59c24fee2dd60f88399df9b7b3b4c96815f770f476f49cb`；camera分别为`executable true 0.23/1/1.25`与`disabled false`。两次完整Program链各有20次成功transaction、terminal/compositor/next-frame、零graph diagnostic/GPU failure/failed frame且截图非黑；这些graph数字只证明对照时其他作者链保持健康，不是shake invocation count。10秒ready→after的鲁棒边缘配准在开启态四象限一致为约`(-4,+1)`像素，关闭态四象限均为`(0,0)`；changed ratio为`0.65801`对`0.45965`，证明新增的是全景共同相机位移而非头发专修。它不证明Light Shafts、Water/Shimmer/Depth/Twirl、Iris、粒子子语义或整幅相对幅度已经正确；正式矩阵未改、fixed13/full45未跑，R4仍未完成，R5未准入。精确证据见[E-CAMERA-SHAKE](runtime-evidence-index.md#e-camera-shake)。

> 最新 R4-2 stock standalone Blend owner migration：`.blend`现遵守共享Program-yield合同，只有完整Program成功时才让旧dedicated plan撤权；`2067939514:342#effect#344`已由`resolved-material-graph / program`唯一持有，property slot 1候选、authored asset fallback、static multiply与premultiplied state/color在同一snapshot闭合。定向v3为**1/1 PASS**，executor claimed/encoded/GPU `100/100/100`、failure 0，两次成功transaction均有terminal/compositor/next-frame证据，graph diagnostic/GPU failure/failed frame为0且截图非黑；v2因预期owner/count迁移和该样本既有particle ratchet而非PASS并保留失败现场。本批未改正式矩阵、未跑fixed13/full45。其他Blend shape、Transform非identity数学与SceneScript audio scale仍未迁移；Transform所在stage的user-property effect visibility owner blocker已由B10以typed-live或原子拒绝后整景重启的公共lifecycle移除。R4-2/R4未完成，R5未准入。精确边界见[E-EFFECT-BLEND-TRANSFORM](runtime-evidence-index.md#e-effect-blend-transform)与[E-R4-B10-USER-PROPERTY-VISIBILITY-LIFECYCLE](runtime-evidence-index.md#e-r4-b10-user-property-visibility-lifecycle)。

> R4-4 utility composition Program owner migration checkpoint：公共 graph-input sampler 与straight-output合同不变；resolved source route新增有界main-target capture，只接受可见、无child、dependency ownership为显式`.none`，且每个resolved material都有active audio-spectrum host consumer的`composition/project/fullscreen`完整Program，并在作者层执行点读取此前已绘制的drawable。`2938612768:563#effect#565` 省略`RESOLUTION`、实际使用默认32-band bottom/replace Audio Bars，已从`.workshopAudioBars` dedicated owner迁到`resolved-material-graph / program`；capture geometry、resolved frame target plan与最终utility alpha分别复用现有统一合同，未知shape/dependency/dedicated leaf与非audio utility Program在claim前失败关闭。最终源码隔离v3 **1/1 PASS**，executor claimed/encoded/GPU `142/142/142`、failure 0，6个成功transaction覆盖terminal/compositor/next-frame，graph diagnostic/GPU failure/failed frame均为0；ready/after非黑，肉眼可见音乐面板15根黑条从短横线增长为竖向柱，主图crop/placement正确。`3299228616:387`曾因仅按shape准入active Scroll而出现深色矩形拷贝，现以非audio负门拒绝；v3负门隔离**1/1 PASS**，layer387不在resolved集合且错误矩形消失。ordinary `3122339805:64`的v6正例仍成立。正式矩阵未改，未跑fixed13/full45。在该checkpoint时relocated 16-band尚未迁移，现役follow-up见下一段；带child/dependency的utility、未证project/fullscreen与其他dedicated/legacy/utility owner仍可达，R4-2/R4-4/R4未完成，R5未准入。精确边界见 [E-R4-2-AUDIO-ARRAY](runtime-evidence-index.md#e-r4-2-audio-array) 与 [E-WORKSHOP-SIMPLE-AUDIO-BARS](runtime-evidence-index.md#e-workshop-simple-audio-bars)。

> 最新 R4-2 relocated Audio Bars Program链迁移：`3299228616:151`的exact 16-band stereo up/down作者Program已由`resolved-material-graph`唯一持有，随后strict zero-distortion Fisheye作为统一链dedicated leaf顺序执行。公共frontend只为静态可证的简单float `%`标量赋值显式生成`fmod`，公共color analyzer只为精确root additive helper、同一sample/weight与`sample.a * weight`证明straight输出；complex/vector modulo、其他alpha关系、非root helper继续失败关闭，不按sample/layer/effect/path/hash选择算法。隔离v3 **1/1 PASS**：resolved `[151,303,601]`，executor claimed/encoded/GPU `114/114/114`、failure 0，10个terminal success/成功transaction覆盖compositor与next-frame，graph diagnostic/GPU failure/failed frame/drawable miss均为0；ready/after非黑，上排条向上、下排向下逐条增长，无深色整块矩形。共享premultiply边界的`2938612768`/`3122339805`回归另为**2/2 PASS**且两者既有条形方向/颜色未回退。正式矩阵未改，未跑fixed13/full45。其他Audio Bars combo/topology、非零Fisheye、带child/dependency utility与旧owner仍可达，R4-2/R4-4/R4未完成，R5未准入；精确报告身份与失败边界见 [E-WORKSHOP-SIMPLE-AUDIO-BARS](runtime-evidence-index.md#e-workshop-simple-audio-bars)。

> 最新 R4-2 Simple Audio Bars专用owner撤权：original默认32/64与relocated 16-band三个旧专用profile已有统一Program正证据后，现役Simple planner/profile、Metal pipeline、renderer dispatch及只服务旧链的测试已删除；未知Simple形态必须形成完整Program，否则整链失败关闭。增强版Audio Bars仍保留独立backend，故probe/compiler `32→31`而runtime backend `31→31`。删除后的三样本门 **3/3 PASS**：executor claimed/encoded/GPU依次`148/148/148`、`996/996/996`、`159/159/159`，failure均0，成功transaction `6/24/10`，terminal/compositor/next-frame齐全，graph diagnostic/GPU failure/failed frame/drawable miss全0，旧Simple执行计数全0；默认32档黑条、64档紫条与16档上下双向条均保持非黑、局部独立变化，无整块矩形。Scene Swift `588→584`、LOC `104565→103404`，小于3 KiB/1 KiB文件分别`191→192`、`53→54`；受影响自动门、代码健康与签名build verify通过。报告SHA-256 `ecc320b016d461a487778190a913c9f9e7962f3da1b04532abba67ea8ebe4211`，正式矩阵未改，未跑fixed13/full45。该撤权只闭合Simple Audio Bars家族旧owner；其他音频、utility、dedicated/legacy owner仍可达，R4-2/R4未完成，R5未准入。

> Simple撤权后的cross-family安全修正：增强版Audio Bars继续使用同名`.workshopAudioBars` backend，现已明确保留为统一mixed chain的dedicated leaf而不是Program-yield family；公共mixed capability又恢复“首个active stage必须resolved”的失败关闭合同。Spin试验迁移v1虽有resolved `[17,20,79,994]`、GPU `24/24/24`、34个成功transaction且diagnostic为0，画面却只剩中央占位图标，changed ratio 0；该全绿黑屏现场保留。修复后`3767460992` **1/1 PASS**：layer17/79可靠回退、layer945增强版Audio Bars恢复为resolved mixed leaf，resolved `[20,945,994]`、GPU `18/18/18`、32个成功transaction、failure/diagnostic为0，彩色旋涡ready/after肉眼方向变化恢复，changed ratio `0.90495`。报告SHA-256 `d7e501c3d6eb95b5734ff68f456468c9a45805b24c56667101ef2d0c5fa571f1`；未跑fixed13/full45。该门修复owner边界，不迁移Spin，也不提高增强版Audio Bars或R4等级。

> 最新 R4-2 Workshop Audio Hue owner迁移：公共frontend现只把preprocessor后active source仍有引用的非sampler uniform放入Program ABI，避免combo-disabled cursor分支留下无消费者的inverse matrix；只为built-in `mix/lerp`的独立浮点向量参数做共同最小宽度缩窄，user-defined/复合/歧义形态失败关闭。color analyzer只开放保持输入alpha、以同一mask混合原RGB与新RGB的root写入。`3767460992`的Workshop `2193274282/hue_shift`在layers `[17,20,79,945]`使用同一Program hash `5680eddd…`，专用planner/plan/renderer/backend随后删除；probe/compiler/runtime backend `31→30`，hardcoded expected SHA `237→233`，numeric Workshop selector `57→50`，Scene Swift `584→582`、LOC `103404→103271`。删除后v6 **1/1 PASS**：resolved `[17,20,79,945,994]`，claimed/encoded/GPU `30/30/30`、failure 0，48个terminal/成功transaction覆盖GPU/compositor/next-frame，graph diagnostic为0，ready/after非黑且changed ratio `0.88681`。但中央仍是`X`占位资源、背景全局强扭曲，用户指出的眼睛不动、头发非局部、光效错误均未闭合；该门只证明Audio Hue作者Program接线和色相变化，不代表样本视觉正确、R4-2/R4完成或R5准入。精确证据见 [E-R4-2-AUDIO-HUE-PROGRAM](runtime-evidence-index.md#e-r4-2-audio-hue-program)。正式矩阵未改，未跑fixed13/full45。

> 最新 R4-2 Spin Program owner撤权：现役资料库中的官方stock `spin/effect.json`、material和vertex/fragment source已经完整界定当前样本的单pass职责与combo；旧`SceneAuthoredSpinPlanner`实际只接受这一个固定形态，`SceneSpinPipeline`则是项目手写近似。统一Program现可完整编译同一作者source，故无需新增Ghidra或参考实现取证，旧planner/plan/pipeline/renderer/backend与专用测试一并删除，未知Spin形态不再回退到指纹近似。dedicated probe/compiler/runtime backend `30→29`，hardcoded expected SHA `233→229`，Scene Swift `582→578`、LOC `103271→102664`。最终`.codex/scene-spin-program-r4-2-3767460992-20260809-v6-owner-retired/report.json`为**1/1 PASS**：layers 17/20/79/945的Spin均为`admitted-generic / resolved-material / program`，resolved `[17,20,79,945,994]`，claimed/encoded/GPU `30/30/30`、failure/deferred 0，48个terminal/成功transaction覆盖五层compositor与next-frame，graph diagnostic/GPU failure为0；ready/after非黑，changed ratio `0.86204`。但画面仍是整幅液化、中央`X`，人物各区域、双眼、粒子与光效的完整相对运动未闭合；这证明Spin专用owner已被通用Program替代，不证明整景正确。正式矩阵未改，未跑fixed13/full45；R4-2/R4仍未完成，R5未准入。精确证据见 [E-R4-2-SPIN-PROGRAM](runtime-evidence-index.md#e-r4-2-spin-program)。

> Puppet disjoint-additive 公共门：`0892e74b` 在既有 MDLV0023/MDLS0004/MDLA0006、loop、blend/rate=1、无 blend-in/out 边界内，允许全部 clip 标记 additive、首帧逐 bone 与 bind pose 差值不超过 `0.001`，且实际驱动 bone 集两两不相交的组合；重叠 bone、重复 animation、非 bind reference 或混合 opaque/additive 继续整层 fail closed。property-bound `animationlayers[].visible` 编译为 typed bool target 并由每帧 snapshot 消费，不执行 SceneScript。Puppet/property/benchmark 等 **180 tests**、代码健康（744 Swift files / 44 locked legacy / 400-line limit）与签名 Debug verify 通过。最终签名 App 对隔离 Workshop 副本中的 `3769688830` 运行 `.codex/scene-puppet-disjoint-3769688830-20260802-v2/report.json` **1/1 PASS**：Puppet layer IDs `[21,44,50,53,56,59,157]`、disjoint-additive `[21,44,50,53,56,59]`、17 clips，submitted/completed/failed `107/106/0`、drawable miss/residue 0，ready/after `mean_delta=0.055279`、`changed_ratio=0.564666`；人工方向性核对确认头/发/脸/肩、手臂和尾巴均有骨骼动态。报告/App executable SHA-256 为 `e6c6cffa706a4051fd7cee8d35b3ee8d3e76236882db3009c53c5e1ca9b60ae6` / `896fdeea9c4c52e0e5f0c10bfaa6b32a144c08cd722d9979133cf2d65972c29c`，CDHash `e5120b4ad2d0b473297df646ff766a7ed4c1f031`、team `H9QWU9XN8R`，签名前后 verified。未跑 fixed13/full45；这不开放重叠骨骼冲突、权重/插值、independent rate、动态 attachment follow、constraints/IK/physics 或 Windows 数值/像素等价。

## 2. 官方资料覆盖

| 资料面 | 收集状态 | 权威入口 | 仍未知 |
|---|---|---|---|
| Scene 官方页面 | 179 个 `/en/scene/` 页面已建目录并逐页映射到唯一合同 anchor，2026-07-22 经代理核验 | [官方页面全目录](official-page-catalog.md)、[逐页映射](official-page-map.md)、[分组映射](official-page-crosswalk.md) | 官方未来页面变化 |
| 官方 Effect | 45 个用户可见 effect + `_empty` 内部占位已整理 | [内置 Effects 语义全集](effects-reference.md)、[执行覆盖表](effect-execution-coverage.md) | 私有 shader 精确算法和全部历史版本 |
| SceneScript | 官方 `lib.sceneScript.d.ts` VERSION 2.8 已逐 API 建账 | [SceneScript API 覆盖表](scenescript-api-coverage.md) | Windows VM 的非公开行为和预算细节 |
| Scene/Effect/Material/Shader/RT | 作者合同与 IR/executor 状态已拆分记录 | [场景格式与 Render Graph](scene-format-and-render-graph.md)、[Graph/Shader 覆盖表](render-graph-shader-coverage.md) | 完整私有 schema、默认值和所有 combo |
| Particle | 官方组件、子类型和执行阶段已逐项建账 | [粒子组件覆盖表](particle-component-coverage.md) | 未公开 preset 资产内容和平台精确视觉 |
| Timeline/属性/输入/Provider | 官方求值顺序与 target 状态已逐项建账 | [运行输入与属性覆盖表](runtime-input-property-coverage.md) | 私有序列化字段和 Windows 精确数值 |
| Puppet/3D/Lighting/性能/RGB/离线 | 高级对象与产品策略已逐项建账 | [高级对象覆盖表](advanced-object-coverage.md) | 私有资产格式、设备和渲染器内部行为 |
| 资料来源 | 官方、样本、WaifuX、linux-wallpaperengine、当前代码分级 | [资料来源与证据索引](source-index.md) | 第三方实现不能替代官方真值 |

因此，后续一般不再猜“这个能力是什么”；实现前先查上述合同。仍需研究的部分应明确标为私有格式/算法未知，不能用视觉近似反向定义官方语义。

## 3. 系统总表

| 系统 | 当前级别 | 当前真实能力 | 主要缺口 / 升级门 | 批次 |
|---|---|---|---|---|
| PKG/TEX/资源索引 | `L3` | loose/PKG/stock 查找、常见 TEX、内嵌 MP4、诊断式失败；PKG extraction 以 package/content SHA 和 exact file set 校验损坏/陈旧 cache，单次读取包体；同一 surface 复用 TEX parse 与 device texture，本机普通文件以 canonical path/size/mtime/device/inode/ctime 区分 generation，并在 image/TEX/video 读取或发布前后复核同一 revision；metadata 不可得或非普通文件失败关闭；PNG/JPEG、raw/embedded format 0 与 BC1/2/3 的九类 2D typed purpose（premultiplied color、straight albedo、generic preserved、mask、noise、flow、phase、normal、depth）进入独立 cache/upload identity，另有严格 format-0 vertical embedded-PNG `.lookupTable` volume purpose 保留 header/mip depth 并上传为 `rgba8Unorm` Metal `type3D`；普通 2D consumer 明确拒绝 volume。全部 33 个 Effect 辅助纹理调用已显式选 2D 用途：mask 16、noise 6、flow 2、phase 2、normal 2、depth 1、generic preserved 2、X-Ray straight albedo 1、premultiplied Blend color 1；data 图片只接受原尺寸、无 decode 映射、integer packed、明确非预乘 alpha 或无 alpha（skip）且使用受控 32-bit byte order 的 RGB(A/X) provider，并归一为 RGBA；只有 CGImage 明确无 alpha 的 straight albedo 可 bounded raster/resize 并固定 alpha=1，alpha-bearing/premultiplied 与其他 data purpose 仍严格；Water Flow flow/phase 与 Standard Blur mask 已从同一 source generation 原子取得 identity、purpose、实际 physical/已验证 mapped size、UV transform、sampler 和 pixel format；Shake flow/phase/mask、Foliage Sway mask/noise、Water Ripple mask/normal、Depth Parallax R8 depth 与 exact legacy Blend overlay 共九槽进一步经共享 authored `0...7` binding，把每个 texture 与各自 candidate 的 generation/metadata 原子绑定；普通 PNG/JPEG property color 与 authored TEX 均发布 typed candidate，非法尺寸/变换、错误 slot/purpose/format 与 clamp-border 均失败关闭；BC1/2/3 多 image color sprite 新增 axis-aligned、整数像素、同 frame extent 的 bounded native autoplay：source 按 file generation/device 跨 surface 去重，RGBA target 按实例分配，straight alpha 经 GPU 转预乘，launch-scoped 384 MiB 预算使用设备 texture size/alignment 且随 playback/Scene 生命周期释放，文件 revision 在分配前后复核，GPU 失败允许同帧重试；R3 launch VFS catalog 又为 purpose-proven static asset 形成 exact demand/publication，并把固定八槽及 hole 纳入 atomic Program | LUT effect/material consumer、3D sampler/颜色空间与 Windows sampling golden；跨进程 extraction lock、跨网络文件系统或粗粒度/陈旧 ctime 下的强内容证明（当前无 content digest）、旋转/trimmed/fractional/异尺寸跨-image frame、dynamic replacement、SceneScript `ITextureAnimation` detach/join/command/timer 生命周期、alpha-bearing straight source resize 与 Windows 通道 golden、BC5 consumer/encoding、未证 regular asset purpose、尚未准入 Program 的 slot/purpose/readiness、system/media/variant 动态 generation、case/symlink/duplicate/多格式边界及完整 VFS golden | B0 |
| Scene IR 与基础层级 | `L3` | 基础对象、顺序、父子 transform/visibility、受限 Puppet 静态 attachment frame 与 animation layer 声明、常见 image/text/solid/particle；Scene loader 与 descriptor admission 双重拒绝重复 authored object ID，把排序后的冲突 ID 作为 blocking 诊断并沿 launch error 保留具体原因，避免下游 `Dictionary(uniqueKeysWithValues:)` runtime trap；image/solid 作者 `alignment` 随 Scene JSON 进入 layer IR，并以独立于 text alignment 的 quad pivot 放进 `sizeScale` 之后，支持 center/top/right/bottom/left 与四角、缺省/未知值保持中心。44 份已解包真实 Scene 中 16 个样本声明 135 次，52 次为非中心；作者 `color` 与 `brightness` 由普通 image/solid 在 color blend/effect 前共同折进 `u.tint`，GPU 门覆盖暖色、`brightness > 1`、mode 31 与 SDR 裁剪；`contentKind == "text"` 跳过 compositor 的同名颜色/亮度，交回 CoreText 栅格化阶段消费；作者 text layer `anchor`（编辑器 Screen anchor）随 interpretation v28 进 text IR，并与 parallax 折进同一个 layer 平移（随包 5 个 text layer 中 `dino_run` 的 231/177 为 `topright`，其余 3 个为 `none`），有 CPU 几何门与离屏 GPU 覆盖门；作者 `horizontalalign`/`verticalalign` 除了继续喂 CoreText 框内排版，还作为 text layer 的 quad pivot 折进同一个 model 矩阵（排在 `sizeScale` 之后），随包 5 个 text layer 中 `dino_run` 的 231/177 为 `right`/`center`，其余 3 个为 `center`/`center`；作者 text layer 的 `limitrows`/`maxrows`/`limitwidth`/`maxwidth`/`limituseellipsis` 随 interpretation v29 进 text IR，并在 CoreText 栅格化阶段决定换行宽度、行数与省略号（两个数值只在对应开关打开时生效），有逐像素正反门 | effect/pass/material/target duplicate、模型、动态 attachment follow、复杂 object component 和全部动态字段；image/solid alignment 尚无 Windows 逐像素 golden；`brightness` 超过 1 的部分只由 render target 精度裁剪，没有 WE 像素标定；image color 的动态 binding 仍未开放 | B0 |
| Utility composition | `L3 bounded` | typed composition/project/fullscreen、完整 bounded current capture 与 `_a` named target；exact composition Clipping Mask、`Clipping Mask -> static Opacity`及exact legacyWorleyColor Procedural Noise共用完整 chain/单 backward dependency 门。B12/B13让这些exact utility consumer与普通image consumer共用统一external-primary owner：launch冻结typed binding，frame预留与provider capture复用同一texture，ready后按完整identity晚绑定执行，ticket保证只消费一次；Clipping Mask固定slot 1，legacyWorleyColor固定slot 3/blend 0。无child/dependency且Program每个material都有active audio host consumer的`2938612768:563`仍以显式main-target source route执行。X-Ray partial-prefix child capture已撤销；B17后只有完整resolved/authored execution可授权capture，legacy-only effect不再通过旧planner取得capture，受支持但未接管的形态降为`partialEffects`并由产品draw失败关闭 | effectful/nested/child provider、project/child/multi-dependency consumer、SceneScript/dynamic alpha、weighted hidden profile、generic named texture、secondary/multiple/mismatch/current-frame缺失、`_b`数据流与RGB语义继续失败关闭；ImageBlend仍是独立owner，一个Clipping Mask、Procedural Noise或Audio Bars正例不外推其他utility | B2/B3 |
| 画布/cover/背景 | `L3` | cover 投影和作者声明视差门，未覆盖区域不再暴露灰底 | 多比例、多屏和 Windows 像素基准 | B5 |
| Frame Context | `L3` | host 单一 60 Hz driver；共享 clock 发布 raw、最大 0.25 秒的 simulation、dropped delta 与 discontinuity，particle/parallax 消费 simulation，shader/video/Timeline 保持 raw/absolute time；Scene pause 冻结 scene time 和 frame index，resume 首帧 delta 为 0 且不补暂停长帧；Debug benchmark 结构化记录 discontinuity/dropped/max-raw、startup、callback、drawable、encode、CPU/GPU 分位数，缺失/非法事件失败关闭 | 真实系统 pause/sleep、其他 simulation consumer、seek/history、目标 FPS/不同 FPS、长期阈值、离线 adapter 与 Windows timing golden | B0 |
| Dynamic target/value 与 property binding program | `L3` | 六类 value、主要 target、固定优先级；v22 保存 layer alpha/color、direct text 三字段、strict Local Contrast/Opacity 与 root Particle 八字段 definitions/instructions/effective values；bounded text `maxwidth` Timeline 与 time-of-day Blend 分别发布 typed text-width/multiply definition；mixed/invalid/未知 SceneScript key 标记重建 | direct text 已有 per-layer generation/coalescing/last-ready；hidden/no-consumer text 与其他 effect constant 仍重建 | **B0/B4** |
| Per-surface dynamic snapshot | `L3` | host 共享 property 输入，每个 surface 独立 evaluation transaction、snapshot 与 generation；相同 payload 不增 generation；root Particle 按 layer 提取七个 scalar 与 normalized color typed values，bounded text width 按 layer 提取 scalar | pointer/provider/SceneScript 等 local producer 接入后继续扩充隔离门 | **B0/B4** |
| Atomic live property state/routing | `L3` | layer alpha、纯 solid color、strict Local Contrast strength、stock Opacity alpha、bounded text width 与 root Particle 八字段先原子求值并直接供 renderer/simulator 消费；粒子真实门证明四键更新不替换 surface/window | mixed、SceneScript、unsupported、无 consumer 与粒子 child target 仍保留整场重建或 fail-closed fallback | **B0/B4** |
| Timeline runtime | `L3` | 作者 `animation` 无损进 IR（lane/keyframe/tangent/options/combined 分组/`relative`/`previewvalue`），44 个真实 `scene.json` 的 48 处全部解析成功、0 诊断；纯函数 evaluator 按**绝对 scene time** 求值 Loop/Mirror/Single、`startpaused`、端点保持、作者 cubic Bézier handle 与 Loop wrap 闭合段，双侧禁用严格线性。编译成 typed `SceneDynamicTargetDefinition` 后经既有 per-surface transaction 写回，`SceneDynamicSource.timeline` 优先级高于 userProperty。现役 Workshop **48/48** authored host 均形成 typed binding：effect constant 23、layer alpha 5、relative layer transform 9、root particle scalar override 7、bounded text `maxwidth` 2、bounded Combined 2D camera `origin/zoom` 2；实际视觉消费仍受对应 backend/chain 准入约束，`3769688830:157` 是首条已闭合的 Tint alpha Timeline。9 条 wrap 声明消费末 front/首 back，当前 fixed13 25 bindings / 0 diagnostics。camera origin 不与 layer transform 重复计数；两成员共用 owner clock并原子进入共享 projection | 48/48 只表示现役 authored host 已编译为 typed binding，不是所有 target 都已由可执行 renderer 消费，也不是 generic Timeline：非 layer-transform/particle scalar 的 `relative`、普通 Combined、multiple camera path/3D camera 与 Animation Event 继续拒绝或未执行。handle/wrap wire 单位是合法语料 clean-room 合同，无 Windows 同相位/像素 golden；absolute CP position 另有 stock-wire/project-fixture consumer，angle 只保真 | **B4** |
| SceneScript property binding IR | `L1` | 文档级 IR 无损保存 inline `source`、owner、完整 JSON target path、`scriptproperties`、authored fallback 与 JSON value type；覆盖正式取证的 scene general、object transform、layer visibility、effect visibility、effect-pass constant 五类位置，单 wrapper 的 `script + user` 冲突诊断并拒绝；既有 layer 顶层 binding 继续随 descriptor/cache 传递，旧 cache 缺字段仍可解码 | nested/未知 owner 不提升；file/module、schema-resolved Vec/value type、handle、VM 与 lifecycle 仍缺 | **B4** |
| Exact native text-script profiles | `L3 bounded` | 七个 source SHA + 完整 property shape 双重准入；原三 profile 的 6 样本/16 bindings 之外，`3768903841` 新增 compact day、long-month date、12/24-hour clock 与共享 time-of-day greeting 四 profile，每帧进入 typed snapshot 与动态文字 consumer，未知/变异 profile fail closed | exact profile 本身不执行 JavaScript、不开放 engine/lifecycle/handle/event；公共 Date/string AST 子集单列下一行，greeting 仍必须且只能消费唯一已验证 day/night schedule，见 [E-TEXT-SCRIPT](runtime-evidence-index.md#e-text-script) | **B4** |
| Bounded property-bound text `update(value)` subset | `L3 bounded` | inline source 解析为项目自有、无循环 AST；支持 `new Date()` 与七类 calendar getter、primitive script property、局部变量/顶层未初始化声明槽、块/`if`/`else if`、有限 primitive 数组与整数索引、`&&`/`<`、严格相等和 number↔numeric-string 松散相等、`+`/`%`、单元素/primitive 数组字符串转换与 string `slice`；按 token/statement/evaluation budget 执行并写入 typed text snapshot，不看样本、layer 或 source hash | 只解析唯一 exported `update`，不执行顶层 builder/initializer；块没有独立 scope，也没有完整 coercion/exception/locale、init/module/engine/owner/handle/event/timer/任意调用；未知语法或非法值继续 fallback，见 [E-TEXT-SCRIPT](runtime-evidence-index.md#e-text-script) | **B4** |
| Bounded property-bound Blend time-of-day subset | `L3 bounded` | 完整 wrapper 下的唯一 exported `update` 只准常数、有限四则算术、`engine.timeOfDay`、`WEMath.smoothStep` 与 `Math.max`；结果经 typed Blend multiply target 进入 per-surface snapshot，按本地 wall clock 逐帧更新，不看样本、layer 或 source hash。exact 单纹理 Blend 另支持静态 `WRITEALPHA=1`，但 alpha 本身不接受 binding | 没有 engine/WEMath module object；其他 statement/function/API/target、动态 alpha、named RT/media、多纹理、时区/DST/跨午夜长稳与 Windows 数值/像素 golden 仍缺；见 [E-EFFECT-BLEND-TRANSFORM](runtime-evidence-index.md#e-effect-blend-transform) | **B4** |
| Exact native SceneScript 64-band Audio Bars profiles | `L3 bounded` | 两个 raw source SHA 与完整 binding/property/layer/asset/shader/render-state 合同双重准入；复用 host 64 档左右声道，逐 bin 求算术平均并以单纹理 64 draw 执行；未知或变异 profile 整体 fail closed | 不执行 JavaScript，不开放 `registerAudioBuffers`、`AudioBuffers`、`createLayer`、`ILayer` 或 dynamic-layer lifecycle；renderer instance 不等于 Scene layer；无同步非零音频或 Windows golden，见 [E-SCENESCRIPT-AUDIO-BARS](runtime-evidence-index.md#e-scenescript-audio-bars) | **B4** |
| Exact native texture-animation profiles | `L3 bounded` | 两个精确 source/profile：delayed-loop 继续以实例级 scene-time 状态机执行 stop/play 与确定性独立等待；time-of-day profile 以完整 wrapper/property shape 准入并用 wall clock 驱动三帧 toggle，与 greeting 共享同一 schedule。未知/变异脚本维持原生 autoplay | 不执行 JavaScript，不开放通用 `ITextureAnimation` handle、detach/join、rate/pause/seek/frame mutation、owner timer、cursor click 或 persisted manual override；见 [E-PUPPET-BC](runtime-evidence-index.md#e-puppet-bc) 与 [E-TEXT-SCRIPT](runtime-evidence-index.md#e-text-script) | **B4** |
| Generic SceneScript VM/API | `L0` | 无 ECMAScript VM、通用 file/module loader、host object/API bridge 或事件/lifecycle runtime | 完整 source/binding/value-type IR、安全 ECMAScript、生命周期、API/events、预算隔离 | **B4** |
| Current pointer | `L3` | view-normalized -> scene world 与 axis-aligned authored layer UV 极窄子集；parallax/受限 effect 消费 | parent/rotation/scale/parallax 逆变换与 effect/control-point/script golden | B0/B4 |
| Previous pointer | `L2` | Frame Context 保存，但 renderer 未消费 | shader built-in 与事件 delta | B0/B4 |
| Pointer buttons/events | `L0` | 无 button/down/up/click snapshot 或 dispatch | 同帧输入队列与 author-off | B0/B4 |
| Audio declarations | `L3` | effect 侧 `AUDIOPROCESSING` combo 与五个 audio 常量已由共享准入层消费；exact Workshop `2084198056/Simple_Audio_Bars` 的 16/32/64 数组与三个严格 combo tuple 有独立 consumer；粒子侧 audio schema 保真解析并由 bounded emitter/initializer/operator 子集消费 | 其他 Particle profile、Workshop audio shader、SceneScript `AudioBuffers` bridge 和未准入 combo/指纹继续失败关闭 | B0/B4 |
| Audio frame input | `L3` | 16/32/64 频段 × left/right host-shared 快照：系统音频 tap -> 单次 FFT 的 `SystemAudioSceneSpectrumAnalyzer` -> `SceneAudioSpectrumInbox` -> `SceneFrameContext`，host 每帧采样一次广播给所有 surface；采集由 consumer 存在性驱动，无 consumer/暂停/锁屏/休眠即停采并归零 | stock/effect profiles 消费 16/32/64；两个 exact native property-script profile 消费 64 档；bounded Particle Audio Response 消费 16 档且同帧 fixed steps 复用同一 snapshot。频段边界、归一化和平滑是工程选择，非官方合同；native average 不等于 SceneScript `AudioBuffers` bridge/object | B0/B4 |
| Resolved-material authored audio arrays | `L3 bounded` | 公共 frontend/Program ABI 保留 `float g_AudioSpectrum{16|32|64}{Left|Right}[N]` 的数组 shape/byte size，并从同一 FrameContext snapshot 经 host schema/finalizer 编入 uniform bytes；Program variant 的实际 host-uniform consumer驱动音频 demand。`3122339805:64`以ordinary source、`2938612768:563`与relocated `3299228616:151`以有界composition main-target source进入resolved-material GPU/compositor/next-frame事务；前两者证明bottom柱高，后者证明16-band left/right上下双向逐条增长及Program后zero-distortion Fisheye有序消费。未知数组、非法长度/类型、跨stage冲突、不完整envelope与不确定utility shape保持失败关闭，见 [E-R4-2-AUDIO-ARRAY](runtime-evidence-index.md#e-r4-2-audio-array) | 只证明当前可静态准入的Simple Audio Bars Program子集；不支持任意uniform array、通用audio shader、任意utility/dependency或SceneScript `AudioBuffers`，也不证明官方FFT数值或Windows像素等价。其他dedicated audio profile owner尚未全部撤权 | B0/B4 |
| 内嵌视频纹理 | `L3 bounded` | TEX 内嵌 MP4 image-layer 以共享 `SceneFrameTiming` 映射 item time；launch-scoped registry 按 layer/source/device 复用 provider，surface rebuild 保留连续 item time，pause 保帧、resume 不补暂停时长，按成功 publication 增 content generation，同 frame 去重并拒绝 stale/mismatched publication，stop 释放 player/output/cache/临时文件；真实样本证明启动、连续非黑画面和 stop | seek、真实系统 pause/screen hot-plug 运行门、loop 首帧/首帧黑场策略、codec/device-loss 恢复、variant/generic material consumer 与 Windows A/V parity | B1 |
| 系统媒体 identity | `L3 bounded cover` | `$mediaThumbnail` / `$mediaPreviousThumbnail` 独立 identity；current 有显式 generation publication 与严格 Blend consumer，previous 有原子 history 和一个内容/结构严格准入的 authored gradient transition consumer | live platform producer、generic event/SceneScript、其他 transition/variant 与 metadata/status | B1/B4 |
| Particle runtime | `L3` | 作者 Sprite、常见组件、bounded 16-band Audio Response、bounded classic Vortex、bounded Cap Velocity、最多 64 粒子的 zero-separation Boids alignment/cohesion、root/child 共用的 bounded Sprite Trail、static/spawn/death/follow Sprite profile 共用的 bounded uniform screen-plane child scale、严格单 renderer/Screen 的 bounded Rope 与 RopeTrail、静态同空间 plain-image Layer Image、Sphere/Box/Layer Image 共用的 bounded initial emitter delay、root Sphere/Box 的仅 rate Random periodic active/delay schedule、有限非负有序的 emitter speed range、bounded Directions/Sphere Sign、root Sphere/Box emitter 的 bounded static CP-angle frame，以及 exact pointer-lock CP 1...7 驱动的 bounded local Control Point Force。Particle audio 共用 frame snapshot 与 evaluator：rate-only Sphere/Box 乘 rate，Turbulent Velocity Random/Turbulence 乘 phase，底层已准入的 classic Vortex 乘 speed；invalid/unsupported form fail closed。root `instanceoverride` 另可由 direct User Property 驱动 `alpha/size/lifetime/rate/speed/count/brightness/colorn`，并由 absolute scalar Timeline 驱动前七项；每步 snapshot 覆盖 static fallback，只影响发射与新生粒子。initial delay 在 fixed-step 边界只消费剩余 active slice，duration 从 delay 后计时；classic Vortex 按 v1 axis/distance/speed 与 standard/infinite-axis flag 做项目自有 fixed-step 切向执行；Cap Velocity 按作者 max speed 与 normalized-age blend 移除超速比例且保持方向；Boids 用不可变 fixed-step 快照避免数组顺序偏差；Sprite Trail 允许省略/`null` length 并使用项目默认 `1`；child scale 只准正有限 X=Y 与三轴 `1/1024...1024`，同步缩放 local position/size/velocity/trail stretch；Rope 支持 `0...7` subdivision、有限 UV scale、equal-lifetime smoothing、时间 scrolling 与隔离 child topology。普通 Sprite 消费可信单 sequence sidecar nominal frame aspect，缺失时按 raw TEX axes × physical pixels 回退，root/child 共用；custom/missing shader、显式空 renderer 与未准入声明继续 fail closed | dynamic override 的 script、source conflict、direct color、relative/Combined、child inheritance 与存量粒子追溯更新仍缺；child/Layer Image/未准入 operator audio 与 emitter delay/duration/periodic/burst audio 仍缺；child 非零 angles、event offset、XY 非均匀/镜像、Rope/nested/world-space-movement scale 与 Windows transform golden 仍缺；initial delay 没有 Windows fixed-step/组合 timing golden；classic Vortex 的 CP/center force/`vortex_v2` ring 与 Windows audio/数值/轨迹 golden 仍缺；Cap Velocity 的 Windows blend/clamp 数值和轨迹 golden 仍缺；Boids separation、speed clamp、大群 spatial index 和 WE 轨迹公式仍缺；periodic burst/max-per-period、Layer Image 动态/text/puppet source、dynamic CP angle、previous pointer、其他 CP consumer、adjusted/world/perspective、Rope animated texture/root world/multiple renderer、精确 ribbon join、通用 RopeTrail subdivision/UV、Lighting、custom shader executor 和 Windows golden 仍缺失 | **B4** |
| Text/Font runtime | `L3` | CoreText 静态栅格、direct property 动态重栅格、七个 exact native profile、bounded property-bound `Date` text update subset 和部分 font/pointsize/padding/scale；动态内容在 `limitwidth=false` 时按 intrinsic size 扩框并贯穿 capture/effect/final quad；结构门 `79/108` | 完整 SceneScript/system/media text、Windows baseline/fallback、outline/shadow/effect；动态扩框无 Windows 像素 golden | B4/B5 |
| Camera Parallax | `L3` | 仅作者开启且非零 depth 时启用，含层级传播/阻断；与Scene Camera Shake共用同一帧相机身份但保持独立算法 | WE 数值/时序 golden、zoom、3D camera | B5 |
| Scene Camera Shake | `L3 bounded` | 作者显式enable、默认值与有限editor-range参数进入独立descriptor/evaluator；当前只准有效2D orthographic Scene，以absolute scene time生成确定性XY camera displacement。base/path与shake形成唯一frame camera，shared view、parallax、pointer、正交层和下游perspective particle都消费该状态；User Property通过rebuild生效。见[E-CAMERA-SHAKE](runtime-evidence-index.md#e-camera-shake) | 真正perspective Scene的XYZ分支、3D camera、SceneScript same-frame setter、无重建live property、官方pause/seek事件策略、Windows同相位数值/像素、多屏长稳与整景视觉等价 | B5 |
| User Properties | `L3` | 独立窗口、条件、持久化、PNG/JPEG `sceneTexture`；layer alpha、纯 solid color、direct text、strict Local Contrast/Opacity 与受限 X-Ray target 已无重建 live 更新；同一 property texture 可按消费者建立 premultiplied generic/Blend candidate，以及相互独立的 X-Ray straight-albedo blend 与 preserved-channel halo identity；X-Ray opacity 是 authored `.mask` texture，没有 property map。exact legacy Blend slot 1 的 property candidate 优先，缺失时回退 authored asset，候选与 UV/sampler/format 同代进入 renderer；Simple Audio Bars `Bar Color` 与relocated 16-band `Opacity`已由typed binding/snapshot进入统一Program GPU consumer，旧Simple专用compiler已删除 | Audio Bars 尚无真实 live override 门；unsupported/mixed/SceneScript bindings、Texture Variants、shortcut、跨重启 UI 门；精确 census 见 runtime-input 专项表 | **B0/B1** |
| Typed texture provider | `L3` | layer/named/property identity、status/fallback；静态 resource generation 与 named frame epoch 已分离；本机普通文件 generation 还携带 device/inode/ctime，cache hit、candidate、TEX 与 video source publication 均复核完整 revision，metadata 不可得时不发布；direct text、embedded MP4 与 bounded current/previous media cover 使用显式 content generation publication，frame registry 拒绝 stale generation、同代换纹理和 publication/texture 不匹配；media cover 新 generation 还会协作取消旧 request，在 current/previous decode/upload 边界停止过时工作；previous transition 的 gradient mask 另按 typed `.mask`、slot 2、format 与 authored path 校验。Water Flow flow/phase、Standard Blur mask、static plain/effectful base image、bounded REFRACT normal 与 Depth Parallax R8 depth 已用共享 typed candidate 原子携带 generation、purpose、actual physical/mapped、UV、sampler 与 pixel format；静态 base candidate 无论 direct、inline 还是 offscreen/authored-effect 路由，都在最终 GPU split 复核 texture identity/purpose/UV/sampler/format；其上新增公开 authored `0...7` 的共享 slot-binding 原子，Shake 1/2/3、Foliage Sway 1/2、Water Ripple 1/2、Depth Parallax 1 与 exact legacy Blend 1 共九个 bounded 槽进一步原子携带 slot、identity/generation、purpose、完整 UV、sampler、format、mip/resolution/texel；Blend 的 PNG/JPEG property 与 authored TEX 共用最终 ready candidate 选择，已选 candidate 不合合同时失败关闭；TEXB0003 embedded image 按 payload-class 尺寸合同准入 | live platform media producer、其他 media transition/variant、其余 provider 的通用异步取消、单次 ImageIO 调用抢占、跨网络/粗粒度 metadata 的 content digest、其余 Effect 与 generic material 的 slot population/readiness、effectful/nested/child provider、sprite rotation、BC5 与 clamp-border parity | **B1** |
| EffectDefinition/Material IR | `L2` | definition/pass/RT/material/slot hole/combo/constant 可保留并建图；ShaderContract 保存 source identity；material render state 已把兼容拼写归一到同一 IR，pass 级 `alphawriting` 与 `usershadervalues` loss-preserving 进入 IR。中性 shader preparation/frontend 消费 source、constant、host/dynamic uniform 与 hidden-slot schema，固定 8 槽 Template 再把同代 provider、active reflection、uniform bytes、typed state 与 color contract 原子形成 Program；R4 对完整守恒的 bounded Program 发布唯一 `resolved-material` owner 并交统一 graph executor 编码，旧 independent plan/pipeline/renderer 不再参与产品准入 | 完整 schema、typed shader defaults、condition/function 与通用 Metal state translator/cache 仍缺；当前只覆盖 analyzer 证明的 framebuffer/单源颜色与 bounded dynamic-uniform 形态，未证 purpose/state/color、复杂 sampler、多 pass 与 `alphawriting=default` 通用 color write mask 仍缺 | B2 |
| Effect execution evidence | `L3` | descriptor admission、static runtime disposition、exact/aggregate CPU invocation、route operation 与 shared-frame command-buffer status 分轴；完整 `EffectKey`、owner/route、sticky success/failure 和 exact/aggregate eligible-observed-gap 守恒进入 Debug report。execution demand 由同代有效 disposition 推导，真正空 effect/纯 inactive 可合法无日志，missing/invalid/unknown 继续失败关闭；矩阵只锁 7 个跨运行稳定字段 | shared command-buffer 完成不是逐 stage GPU 成功；未观察 gap 不伪造成失败或成功；该合同不证明视觉、官方算法、长期性能或未准入 stage | B3 |
| Bounded effect executors | `L3` | complete-chain-only基础上逐stage Program-first；Program失败段只有typed pair、logical-target或full-frame-compose adapter完整闭合时才可接管。B8接管exact raw Precise pair；B9让strict stock Radial与legacy Directional God Rays共享typed logical-target adapter；B11开放bounded captured-main；B12/B13再让exact Clipping Mask与legacyWorleyColor Procedural Noise的单一external-primary binding以frame reservation→provider capture/ready→late exact execute进入同一owner，并由ticket保证单次dependency消费。B15/B16删除旧whole-chain与Light Shafts独立main-pass产品调用；B17把剩余产品planner引用清零，无claim可见effect原子失败关闭 | generic compose、Refraction/scene background、generic/full-frame-compose captured-main、unique/history、Cursor Ripple history、generic named texture、secondary/multiple dependency、其他Procedural Noise与其他未迁移shape失败关闭；Light Shafts只有resolved `transparentDirectDraw`可写main pass；requires-source-copy与layer color blend仍是结构性合成，standalone authored helper只剩R5 surface，3处legacy planner观察调用不构成产品owner；独立ImageBlend仍可达；formal owner门不是Windows visual golden | B3/B4 |
| Legacy Workshop Procedural Noise | `L3 executed-degraded` | exact `2924967132` definition/material/raw+canonical shader、静态常量、`COMBINE=0`/`WAVE=0`、`legacyWorleyColor`与稀疏 slot 3 typed texture；B13把单 backward dependency composition的primary provider冻结为typed external-primary binding，provider current-frame capture/ready后由统一GraphExecutor执行项目独立的时变横向暗云带近似，旧authored whole-chain owner归零。详见 [Effect 执行覆盖表](effect-execution-coverage.md)、[E-EFFECT-PROCEDURAL-NOISE](runtime-evidence-index.md#e-effect-procedural-noise) 与 [E-R4-B13-EXTERNAL-PRIMARY-PROCEDURAL-NOISE](runtime-evidence-index.md#e-r4-b13-external-primary-procedural-noise) | 其他 Workshop ID/profile、dynamic binding、多 dependency、generic Procedural Noise 数学与 Windows 像素/动态等价；B13单例不升级整族 | B2/B3/B4 |
| Current-frame capture | `L3` | bounded完整utility capture；普通 capture 仍要求无 child/dependency，`2938612768:563`已把该source按作者触发点交给统一resolved executor。B12/B13的exact Clipping Mask与legacyWorleyColor先为provider预留同一texture，provider在作者顺序内完成current-frame capture后才发布ready，consumer late execute再次验证同一frame epoch/object；普通image与已证exact composition共享该合同。旧`captureAfterChildrenXRayPrefix`及其partial graph触发面已删除 | 通用 composition/nested capture、未证project/fullscreen、extent/format/mask、SceneScript/dynamic alpha、scene-background，以及缺失/过期/错object的current-frame publication | B2/B3 |
| Named primary target | `L3` | bounded `_a` producer/consumer可执行；B12/B13把exact Clipping Mask与legacyWorleyColor primary reference提升为统一execution ownership，binding固定consumer/provider/variant/完整slot/blend，reservation与ready publication保持同一texture identity，ticket禁止最终compositor二次消费dependency | generic named texture、secondary/multiple reference、provider/slot/blend/epoch/object mismatch、effectful/nested/child provider与更多dependency topology | B2 |
| Named secondary identity | `L2` | registry 区分完整 variant；无 `_b` producer/consumer flow | secondary 数据流、copy/swap/history | B2 |
| Generic FBO command graph | `L2` | target/bind/compose/copy/swap/condition/function 可保留或 blocker；FBO extent、clear、target/lifetime table、bounded copy/swap/history与Precise Blur material-command interleave已有各自严格门。B8的exact raw Precise `compose:true`不是FBO：它保留0 authored/logical RT并在统一executor内部使用layer-local two-member pair | 上述exact profile为受限`L3`；generic compose、Refraction/scene-background capture、condition/function、通用render-state/cache、跨帧logical swap和完整生命周期仍缺失；不得从B8推导generic FBO或background支持 | B2 |
| History RT | `L3 executed-degraded` | 既有 `unique:true` history seed/clear 仍成立；Cursor Ripple 严格白名单允许 exact `_rt_EightBuffer2` 非 unique 读前写，两个 `fit 256|512` RGBA8 target 在跨帧 table 中轮转；`c73aa0b0` 将 authored transparent-zero clear 与 history seed 合并为同一只执行一次的初始化事务，重建新 table 后重新初始化，普通下一帧不重复 clear；项目自有 GPU 门验证移动注入、静止续波与白色 collision mask阻断。三个真实样本的isolated执行只属于R4-A前历史证据，不能证明现役产品会执行 | Cursor Ripple不是统一 GraphExecutor typed leaf；B15后完整准入也不再取得旧whole-chain产品路由，保留helper只作历史像素/history单测。点击注入、pause/seek固定步进、通用跨帧copy/swap、其他history effect与Windows动态/像素golden仍缺 | B2 |
| Scene compatibility/source preparation | `L2` | project/scene version 分别以 declared/missing/invalid/out-of-range fact 与 provenance 保存，不合成 effective version；`SceneResourceView` 之上建立 package/loose/stock 候选 source graph、include edge/content digest、active source map/schema 与 prepared/variant identity。preparation 作为中性 Program 前置能力，不独立授权 GPU owner；package -> loose -> stock 是项目 VFS policy，不是已证明的官方全版本默认 | compatibility default/patch rule、verified backend/version/platform/texture-format provider、跨语言 prelude、完整 annotation/default，以及更多能与 slot/state/color 一起进入 bounded Program 的 source 形态 | B2 |
| Authored shader path/source identity | `L3 executed-degraded` | material path、stage source/raw/canonical 与 ShaderContract identity 保真；中性 preparation/frontend 对 bounded 单 effect/单 material 形态形成 active source/reflection、static/host/dynamic uniform、texture schema 与 color/state contract，只有完整原子 Program 才能由 `resolved-material` owner 准入并经统一 graph executor 执行。三组 exact Scroll 与早期单 pass GPU fixture 只保留为历史 oracle，不再推断产品 owner 或授权旧 plan/pipeline/renderer 可达 | include/combo 仍主要是 `L2` preparation；未证 sampler purpose、复杂 render state、FBO/多 pass、更多语法、arbitrary shader 与 Windows golden 继续关闭 | B2 |
| Shader source/include/annotation/declaration contract | `L2` | 完整 source/include/annotation/declaration 保真；独立 source graph、有界 include/directive、object/function-like macro evaluator、typed `[COMBO]` 与 sampler readiness/requirement schema、有界 fixed-point、active source/schema/source map/dependency/variant digest 和 unresolved input/output color carrier 已落地；已证 slot/provider、active reflection、host/static/dynamic uniform、state 与保守 color transfer 原子形成 Template/Program，bounded 子集由 resolved executor 消费 | verified environment provider、跨语言 macro/prelude、完整 annotation default/editor schema、未证 purpose/state/color、任意 sampler/render-state/multi-pass consumer 仍缺；preparation success 不能单独升级 GPU 准入 | B2 |
| Arbitrary custom shader execution | `L0` | 上述严格 framebuffer-only 单 pass 子集不等于 arbitrary shader | 通用受控翻译/映射、安全、资源分级与产品门 | P3 |
| Puppet asset identity | `L2` | MDLV0021/0023 mesh、受限 MDLV0023 + MDLS0004 + MDAT0001，以及三来源 MDLA0006/full-TRS/80+84-byte skin weights 已交叉核验并 fail closed；v25 保存 authored animation layers | 更多 MDL/MDLA 版本、完整辅助轨道与超出已验证形状的数据 | P2 |
| Puppet runtime | `L3 executed-degraded` | bind-pose 重组（`8bac86e`）、静态 attachment（`49ee89a`）、严格单 clip（`f1ee79b`），以及 bind-referenced、驱动 bone 集两两不相交的 bounded additive 组合与 typed visibility（`0892e74b`）；冲突或畸形声明整层回退 bind pose | 插值、非 loop mode、非 1 rate/blend、重叠 bone mixing/权重、动画 attachment follow、constraint/IK/physics/events、Windows golden | P2 |
| Bounded standalone volumetric `lspot` | `L3 bounded` | 无 parent/child/effect/dependency/script、`castvolumetrics=true`/`solid=true`、单 relative Mirror angles Timeline 的 2D projector cone；作者 color/intensity/radius/cone/density/exponents 进入 premultiplied additive direct draw | 不执行 normal/PBR lighting、shadow、projection image/video/effect provider、point/tube/directional、动态脚本/audio/cursor 或通用四灯合成；见 [E-SPOT-LIGHT](runtime-evidence-index.md#e-spot-light) | Advanced |
| 2D lighting/Scene HDR | `L0` | 上述 standalone `lspot` 只画投影体，不使 image material 响应光照；layer Bloom 近似也不等于官方 lighting/HDR pipeline | PBR maps、author-enabled lit material、ambient、shadow/reflection、scene post | P2 |
| 3D model/camera/physics | `L0` | 无 Scene 3D runtime | model/node/material/skeleton/attachment/camera/physics | P3 |
| RGB device integration | `L0` | 无 Scene RGB provider/output | macOS 策略、授权和 fail-closed | P3 |
| Debug PNG readback | `L2` | benchmark 可生成 GPU 截图，并按 Steam preview 比例中心裁切、输出非阻断分项指标和并排图 | 只允许同一样本跨提交比较；不能跨样本排名、替代 Windows golden 或标成 offline bake | P3 |
| Offline bake | `L0` | 无固定步进产品 adapter | fixed clock/seed、provider replay、编码器 | P3 |
| Stop/switch lifecycle | `L3` | surface=0 和部分资源释放门 | VM/provider/GPU 细粒度计数、系统暂停与长稳 | B0-B4 |
| Performance budgets | `L0` | 无统一 CPU/GPU/显存/帧时长期阈值 | 30 分钟交互、2 小时 soak、多屏和压力门 | B0-B4 |

R3 起，本表旧 bounded executor 摘要中“property 缺失时作者 fallback”的简写统一收窄为：只有对应 exact provider identity 被显式发布为 `absent` 才表示用户未选择并允许作者候选；字典 missing、pending、unavailable、incomplete ready 或 publication identity mismatch 均失败关闭。该修正不把 R3 Program 抬为 GPU consumer。

## 4. 官方 Effect 覆盖摘要

45 项的作者条件、执行通道、公共依赖、当前证据和下一验收门统一维护在 [官方 Effect 执行覆盖表](effect-execution-coverage.md)。按逐行单值现状汇总为 `L1=16`、`L2=3`、`L3=26`、`L4=0`；Fisheye 的 `L3` 只指 zero-distortion/background-off exact profile，Transform 的 `L3` 只指 identity-only exact profile，Depth Parallax 的 `L3` 只指 exact stock 单 image/effect/pass 与 R8 depth 子集，所有 `L3` 都是表内明确限定的子集，不是 WE parity。内部 `_empty`、Workshop `gradient_color` 和 layer Bloom 另列，不能替代任何官方 Effect。

## 5. Particle 子系统覆盖

逐 initializer、operator、renderer、control point 和 child 类型的证据见 [粒子组件覆盖表](particle-component-coverage.md)。本节只保留系统级摘要，不能替代逐项表。

| 官方组件 | 当前级别 | 当前边界 | 升级门 |
|---|---|---|---|
| General 字段 IR | `L2` | 常见 material、maxcount、starttime、world-space/perspective/frame-blend 与五种 author allow-override system flags 可保留 | 其余 General/material variant 与动态字段 |
| General 执行子集 | `L3` | max count、prewarm、perspective、frame blend 与 color/speed/count/lifetime/size override gate 可消费；root direct User Property / absolute Timeline 可按受限字段逐步覆盖作者 fallback | script/conflict/direct color/relative/Combined、child/存量粒子追溯、prewarm cap 与 Windows 数值门 |
| Emitter schedule | `L3` | rate、instantaneous、duration；raw bit 2 的 rate emission 按每个 emitter、每次 render callback 最多一个；Sphere/Box/Layer Image 共用 bounded initial delay；严格 `flags=4`、仅 rate Sphere/Box 的确定性 Random periodic active/delay 窗口可与合法 initial delay 组合 | one-per-frame 的 30/60/120 FPS Windows count/Rope topology、initial-delay Windows timing、periodic burst/max-per-period、audio/override 组合和 WE 时间/RNG golden |
| Emitter launch speed | `L3` | finite nonnegative ordered `speedmin...speedmax`；max-only 以 0 为下界，上界项目预算 1,000,000；无效范围零发射并诊断 | 零半径、更多 emitter/consumer 与 Windows fixed-seed velocity/trajectory golden |
| Emitter shape controls | `L3` | Directions 只准 scalar 或完整 finite 三轴，绝对值不超过项目 multiplier 预算 1,000,000；零轴停在 origin。Sign 只准 Sphere 且每轴为 `-1/0/1`；其余声明零发射并诊断 | 更多轴组合、负 Directions 官方语义与 Windows fixed-seed position distribution golden |
| Sphere Random emitter | `L3` | 可执行子集 | 全参数、distribution golden |
| Box Random emitter | `L3` | zero/absent `distancemin` 的各轴按 `±abs(max)` 围绕 origin 采样；非零 min/max 保留作者区间路径；bounded Directions 可逐轴乘法，Box Sign 失败关闭 | 更多轴组合、反向范围、负 Directions 官方语义与 Windows distribution golden |
| Layer Image emitter | `L3 bounded` | 仅唯一 typed `emitterimage` index 0、静态同 parent/同 transform 的 center-aligned plain image；shared/managed RGBA8/BGRA8 单层纹理按非零 alpha 像素中心采样 | text/puppet、动态/effect source、颜色/运动继承、bitmap 更新与 Windows golden |
| 其他 emitter | `L1` | 名称可诊断，字段和执行不足 | 独立 fixture 和作者条件 |
| 已接 initializer 子集 | `L3` | lifetime/size/velocity/color/alpha/angular velocity 常见路径；finite nonnegative exponent 以 `pow(U,e)` 施加到 scalar、vector 各轴和 color 共用插值因子；event spawn/death/follow child 的省略/`setcolor` Inherit Initial Value 可复制有限 parent color snapshot | 其他 event channel、默认/非法/极值分布、WE RNG/seed golden |
| Bounded HSV Color Random | `L3 bounded` | 七个显式 direct-number wire、normalized ordered HSV ranges 与 `1...1024` hue steps；创建时按 authored order 执行项目自有离散 hue、连续 saturation/value 和 HSV→RGB 近似，有效 instance color override 冲突失败关闭 | 官方 default、instance-color 依赖、RNG/端点、linear-vs-sRGB 与 Windows 数值/像素 golden |
| Rotation Random initializer | `L2` | 字段与 simulation 分支已接线；无最终 rotation 断言 | renderer orientation 数值门 |
| 其他未接 initializer | `L1` | control-point/remap 等可诊断或字段不足 | 逐项 fixture 与创建时语义 |
| 已接 operator 子集 | `L3` | movement/angular、alpha/size/color change、部分 oscillate、非音频 Turbulence、event-follow 的省略/`setcolor` Inherit Value，以及最多 64 粒子的 zero-separation Boids alignment/cohesion；event color 每步读当前 parent state且不重启 child emission，Boids 使用不可变 fixed-step snapshot，其他声明继续 fail closed | 其他 event channel/type、Boids separation/speed clamp/spatial index、默认 phase/system seed、curve 与 Windows 数值/轨迹门 |
| Turbulence operator | `L3 executed-degraded` | 非音频 profile 按作者顺序消费 mask/phase/scale/time scale/speed/blend/fixed dt/static speed override，非有限写入失败关闭；audio profile 零执行 | Windows 固定 seed 轨迹/像素 golden；audio phase 调制公式 |
| 其他未接 force/operator | `L1` | maintain/reduce/vortex/remap/collision 等明确 unsupported | control-point/world-space 力场、typed channel 与 collision solver |
| Sprite renderer | `L3` | 作者纹理和程序化静态遮罩子集；TEX filter/address/mip 进入真实 Metal sampler，UV>1 repeat、minification 与单 mip 均有 GPU 门 | clamp-border 精确值、全 material/blend/lighting/atlas 与像素 golden |
| Sprite Trail | `L3` | root/strict child 共用单 quad stretch；显式 length 按有限非负值执行，省略/`null` 采用项目默认 `1`，min/max 缺省为 `1/1`；malformed、非有限、负值与反序范围失败关闭 | 默认 `1` 是 stock corpus + 公开 orientation-only 语义约束下的 clean-room 合同；orientation/单位/atlas/曲线与 Windows 像素精度仍缺 |
| Rope declaration | `L3` | typed kind/字段进入严格 profile；单 renderer、Screen、静态纹理、2...512 maxcount，准入 subdivision `0...7`、有限 UV scale、smoothing/scroll；strict child 另允许 bounded bit-2 one-per-frame emitter 与透明 unused parent CP mapping | `segments`、length/fades、root world-space、animated texture、multiple renderer、active/unknown CP mapping 继续拒绝 |
| Rope execution | `L3` | 按单调 particle ID 的出生顺序连接相邻 live 粒子；位置用独立 Catmull-Rom/Hermite 曲线细分，端点 size/color/alpha 线性插值，支持 UV scale、equal-lifetime smoothing 和 simulation-time scrolling；child system 独立建链并保留 birth-time world origin，root/child 生成段预算分别为 4,096/8,192 | 独立 quad 的急弯接缝仍可见；scroll rate、curve/join、死亡拓扑/暂停恢复没有 Windows trajectory/pixel golden，root world-space 与 animated texture 未开放 |
| Rope Trail strict execution | `L3` | 严格 Screen 单 renderer 子集以有界历史生成 quad 段；非准入字段、组合和预算全部 fail closed | 官方数值/UV/接缝/死亡尾迹/暂停恢复语义与 Windows golden |
| Static control-point subset | `L3` | root Sphere/Box emitter 只按 authored ID 0...7 绑定 static definition offset/instance position；完整 finite static definition/instance angle 以项目自有 X→Y→Z frame 旋转 local origin、shape 与 initializer velocity，instance 当前值替换默认角。显式 declaration identity 缺失/重复/越界、malformed/动态 angle 或引用越界即零发射。depth-one static child 可在 exact raw-copy profile 下复制 finite static parent CP position | dynamic angle、adjusted/world/operator/child angle consumer、angle copy 与 Windows golden |
| Dynamic control-point declaration | `L2` | CP position/angles nested Timeline 进入 typed IR/target；user/script/pointer 仍只诊断 | 其他 binding producer 与坐标空间 IR |
| Dynamic control-point execution | `L3 bounded` | 仅 absolute Timeline position 经 per-surface snapshot 每帧驱动 root emitter；缺值回退 static | angles、relative、property/script/pointer、operator/child 与 cross-space golden |
| Child asset graph | `L3` | 可递归发现并为 strict child 建立 runtime template 至层级 2：depth-one 每声明一个 template，depth-two 仅 event 触发且按 parent asset path 去重展开一次；missing/cycle/depth-three/nested-static fail closed | runtime cycle lifecycle、depth-two static 语义与 teardown 压力门 |
| Child execution/events | `L3` | deterministic birth/natural-death queue；strict static/default-static child 在有限 authored local origin 创建一次，允许 depth-one exact raw parent CP position copy与 bounded uniform screen-plane scale，仍要求零 angles、probability=1，且目标定义自身含 event children 时不再被 nested 阻断；`eventspawn`、natural-`eventdeath` 与 no-CP `eventfollow` strict Sprite child 独立模拟、绘制、跟随和回收，并共用相同 bounded scale frame；event child 可在创建时复制有限 parent color snapshot，follow 还可逐步跟随当前 parent color，depth-two 复用 typed parent state；depth-two event child 以 (parent system, 粒子) 为 owner、事件跨层按帧串行传播；spawn/death 系统的 rate 发射窗口有界（authored duration 优先，否则以 child 粒子最大寿命为本地近似窗口），rate-only event child 不再无限累积；每 system 1,024 粒子，每 root runtime 每深度 64 systems、跨两层 128 systems/131,072 capacity | nonzero angles、event offset、XY 非均匀/镜像、Rope/nested/world-space-movement scale、adjusted/dynamic CP copy、collision/delete、size/alpha 等其他 value channel、depth-two static、stop/switch 压力门 |
| Built-in textures | `L3` | 20 个精确 key；`particle/fire/fire1`、`particle/light/light_shafts_0`、Flare 三纹理、`particle/nature/snow` 与 `particle/smoke/smoke2` 等为项目自建确定性遮罩，不等于官方资产 | `rain_drops_sheet` 等剩余高频 key、atlas metadata、多纹理 material |
| World-space declaration | `L3` | General、Movement 与 Renderer flag 分开保真和准入；不再因 renderer-only world flag 拒绝整层 | Control Point world-space 仍只有声明 |
| World-space execution | `L3` | 静态可逆 layer/ancestor world frame 下，General 固定 birth origin、Movement 逆变换作者 velocity/gravity、Renderer orientation 不继承 system rotation/scale；inline script、transform Timeline/诊断、有效 parallax、循环/奇异 frame 失败关闭 | 动态 parent/Timeline、multi-screen/camera 与 Windows 数值/像素 golden |
| Collision declaration | `L1` | 可诊断但无 solver | shape/depth/response/event IR |
| Collision execution | `L0` | 无 solver | fixed step 与 event dispatch |
| Audio-response declaration | `L1` | 五字段按粒子 schema 保真进 IR，emitter/turbulent velocity/operator 三类共用同一声明类型；启用后一律报 `audioResponseIgnored`（operator 此前无诊断、会静默按无音频路径模拟） | 声明保真不等于可执行；默认值官方未公开，IR 层不内置 |
| Audio-response execution | `L0` | frame snapshot 已可用，但**求值公式与调制目标无证据**：粒子侧无 shader 源码，第三方参考实现对应代码是 `audioAmplitude = 0.0` 的 TODO 占位，其默认值在 emitter/initializer 两处自相矛盾 | 需 Windows golden 或官方公开算法；在此之前不得按推测实现 |
| Sprite Sheet | `L3` | Sequence/Random frame/frame blend 子集可执行；repeat sampler 不再把超 1 UV 拉成边缘条；可信单 sequence sidecar nominal aspect 优先，raw TEX axes 像素长度回退，并随 current/next frame blend 插值普通 Sprite 几何 | trim pivot、多 sequence/multi-image 异尺寸、loop/edge、多纹理 material 与 Windows atlas golden |
| Static instance overrides | `L3` | alpha/size/lifetime/rate/speed/count/brightness/normalizedColor 有运行断言，五种 General gate 有逐项 allow/deny 门 | direct color、完整类型/range 与 Windows 状态门 |
| Direct color/control-point override | `L2` | CP position 的 static/absolute-Timeline root-emitter 子集已有数值门；static CP angle 的 bounded root Sphere/Box emitter consumer 也有正反门。本聚合行仍受 direct color、dynamic angle 与其他 consumer 无执行门限制 | direct color、dynamic CP angle 与其他 consumer 断言 |
| Dynamic instance overrides | `L3 bounded` | root direct User Property 已执行七个 scalar + `colorn`，absolute Timeline 已执行七个 scalar；每步按 layer 取 typed snapshot 并覆盖 static fallback，CP absolute position consumer 保持；static CP angle 只消费静态当前值。真实 live/Timeline 两门均无 sample/layer 分支 | script、冲突来源、direct color、relative/Combined、dynamic CP angle consumer、child inheritance、存量粒子追溯语义与 Windows golden |
| Material/blend | `L3` | `genericparticle` slot 0、additive/translucent 子集；共享 typed state 只准入 `disabled/disabled/nocull` 与 alpha unspecified/default，未知 blend/state 产生 `unsupportedBlendMode`/`unsupportedRenderState`，root 不执行、child graph 拒绝，不再静默回退 translucent；color TEX sampler 显式传到 renderer，strict REFRACT 的 slot 0 使用 `.straightAlbedo`、slot 1 使用 `.normal` typed identity，各自使用独立 sampler，background 固定 linear-clamp；同一 authored 文件只有在已上传 texture 的宽高和 mip 数仍与 container 完全一致时才可跨两种 purpose 复用，发生 albedo raster/resize 时拒绝把颜色纹理静默当 normal；普通 color/additive 的既有 premultiply 合同不变。项目自建 GPU 门已锁定 format-4 packed normal 的 CPU decode/native BC 两路径与 byte-equivalent format-0 RGBA 位移一致，并锁定 translucent/additive 的 fractional albedo × particle alpha coverage 只作用一次 | `alphawriting=default` 目前只是 bounded admission，不控制固定粒子 pipeline 的 Metal write mask；unsupported custom shader 仍可能误走自有 pipeline；官方/Windows DXT5n 恢复公式与法线数值/像素 golden、多纹理/combo、其他 render state、HDR/Lighting/Cutout、clamp-border 与 Windows 像素标定仍缺 |
| Fixed step/seed/maxcount | `L3` | particle fixed simulation step、deterministic seed、maxcount 有运行门；共享 clock 已把受控 simulation delta 送入 particle runtime 并记录 dropped-time | 真实 pause/sleep、seek/history、离线 fixed-clock 等价与 WE 数值 golden |
| Turbulent velocity initializer | `L3` | 消费 forward/right/up、phase、scale、time、speed range；finite offset 作为 forward→tangent 平面方向旋转，`scale=0` 仍保留 offset；合法 Audio Response 以 `1 + response` 乘 phase，作者 phase 0 仍无效，非法 profile 继续 fail closed | right/up/noise mapping、offset 弧度与 audio 幅度均属 clean-room；Windows 固定 seed/audio 数值、轨迹与视觉 golden |
| Delta clamp/prewarm cap | `L2` | delta 子项已由共享 clock 提供 raw/simulation/dropped/discontinuity、1 秒长帧数值 fixture 与 telemetry；聚合等级仍受 prewarm cap 缺定向预算断言限制 | 真实长卡顿/不同 FPS 与高 prewarm 压力门 |

最近完整矩阵的可见 root 粒子为 `129/130`；唯一未加载 root 是 `2974757317:85705` 的显式空 `renderer: []`，它沿 `missingSpriteRenderer` 失败关闭。初始 REFRACT root batch 为 14；RopeTrail 三个目标样本为 `3299228616=7/7`、`3743305891=1/1`、`3770444459=5/5`，后者 REFRACT 为 4。这不等于粒子组件完整兼容。晚于该完整门的公共能力分别由独立定向门证明：`5cc7ee37` 对 `2131872317` / `2419444134` / `3299228616` 执行 bounded Particle Audio Response；`2419444134` 缺底层参数的 Vortex 仍失败关闭；`3770444459` 的 zero-separation Boids 由 `1fd08811` 执行、bounded static CP-angle emitter frame 由 `7cce5fde` 执行，两条 dripping-water splash/refract Rope child 由 `319214c5` 执行，三组 water-impact droplets child 的 omitted-length Sprite Trail 诊断由 `36b9775a` 消除，root direct User Property / absolute scalar Timeline instance override 由 `32e3a928` 对 `2902406982` / `2998757800` 执行。未准入 dynamic source、Boids/Rope/Trail/audio 形态、动态 world-space、Lighting 等 unsupported component 仍在。`2131872317` 的 event-death child REFRACT 继续由独立 7 秒延迟 runtime 门证明，child 不增加 root loaded-layer 计数。上述数字只度量对应矩阵实际加载的 root layer，不代表后续粒子扩展已经进入 full45，也不代表 Windows 视觉等价。

`1fd08811` 把 Boids 从纯名称诊断升级为 typed、预算化的公共执行：只接受最多 64 粒子、flags 0、finite bounded neighbor/alignment/cohesion、显式 `separationfactor=0` 且无 audio/malformed/未知字段；每步从不可变状态快照计算邻域均值，数组倒序不改变按-ID 结果。真实 `3770444459` 的 layer 48 与官方 stock `previewdrippingwater` 都记录 `boidsBounded:boids`，最终隔离报告分别为 particle `5/5` 与 `1/1`、failed frame/sample residue 均为 0；stock ready/after 可见双侧滴水与连续下落相位。分离力、250 粒子官方预览、speed clamp 与 spatial index 继续失败关闭；没有 Windows fixed-seed trajectory/pixel golden，当前数值只能表述为项目 clean-room 近似。

`36b9775a` 修复 Sprite Trail 把省略 `length` 的合法 stock 形态误判为 unsupported：共享 plan 对 omitted/`null` 使用项目默认 `1`，root 与 child 都传入 parser 的 malformed 状态，已填写但无效的字段继续沿 `trailRendererUnsupported` 失败关闭。`3770444459` 的 8 秒固定步长 harness 在 layers 239/245/248 均观察到 `water_impact_droplets.json`，各层峰值 9 instances，stretch `1.384857...2.0`，对应 child diagnostic 为 0。最终签名 App 的真实报告 `.codex/scene-particle-spritetrail-default-child-20260802-v1/report.json` 与官方 stock `previewwaterimpact` 报告 `.codex/scene-particle-spritetrail-default-stock-20260802-v1/report.json` 均 **1/1 PASS**：前者 particle `5/5`、changed ratio `0.340743`，后者 particle `1/1`、changed ratio `0.738958`，两者 failed frame/drawable miss/sample residue 均为 0。stock 多相位截图可见水花轨迹运动；没有 Windows 同相位 golden，不能据此宣称官方缺省数值、密度、轨迹或像素等价。本批未跑 fixed13/full45。

`8b06538d` 修复 strict REFRACT 的 BC3 purpose 丢失；`d432d4d5` 再把 slot 0/1 分别绑定 `.straightAlbedo` / `.normal`，并以自建 GPU fixture 锁定单 image CPU BC3 decode、多 image native BC3 直传与 byte-equivalent format-0 RGBA 的位移一致、G/A swapped 与 neutral normal 负对照，以及 translucent/additive 的 coverage 单次作用。当前工作区又要求同源复用前 texture 宽高/mip 数与 authored container 完全一致，避免 X-Ray 所需的 opaque albedo resize 扩散为 normal data resize。`3770444459` 的 8 秒固定步长历史门确认 layers 239/245/248 分别有 72/77/56 个非空 REFRACT 活动帧；当前隔离签名 App 门仍为 5/5 particle、REFRACT 4、failed frame/drawable miss 0。等级保持 `L3`：这只闭合项目内部存储/合成合同；当前没有官方/Windows DXT5n 恢复公式、法线位移 golden，也没有证明水花密度、生命周期或像素等价。

`40768dfc` 为 child definition 保留 `parentcontrolpoint` 与 raw-copy flag，并只准 depth-one static、CP `0...7`、唯一 finite static root source、零 child offset/angles 的 exact raw-copy profile。动态、event/nested、adjusted、malformed 与越界形态均 fail closed。隔离真实 `3770444459` 的 layer 48 记录 `rawParentControlPointCopyBounded:mappings=2`，5/5 particle、425/425 completed frame、failed frame/drawable miss 0；全景截图正常且有运动，但不能隔离滴水位置、angles 或证明 Windows parity。

`c6fc9c24` 修复 root emitter 把 authored CP identity 误当数组下标优先的问题，并在 emitter consumer 边界只准所有显式 declaration 都有唯一 0...7 identity；合法但未声明的 CP 仍使用 system zero，缺失/重复/越界 identity 与越界 source 均零发射并记录 `controlPointEmitterUnsupported`。隔离真实 `3770444459` 的 layer 48 记录 `controlPointEmitterBounded:sources=2`，particle 5/5、submitted/completed/failed `426/425/0`、drawable miss/sample residue `0/0`；ready/after 为完整彩色场景、changed ratio `0.301361`。这只证明真实多 emitter 链路与整景稳定，不隔离 CP angles、坐标转换、滴水轨迹或 Windows parity；未跑 fixed13/full45。

`938630a0` 把 emitter speed 从会静默交换反向边界的通用 range helper 收窄为有限、非负、有序且上界不超过 1,000,000 的项目准入；max-only 以 0 为下界，正 min-only、负值、反向、非有限与超预算声明均零发射并记录 `emitterSpeedUnsupported`。隔离真实 `3290491250` 的 layer 49 记录 `emitterSpeedBounded:sources=1`，particle 1/1、submitted/completed/failed `425/425/0`、drawable miss/sample residue `0/0`；灰色是 authored 背景，人物、标志和移动星粒子可见。该门证明真实 speed declaration 进入运行链且整景稳定，不证明 WE 的缺省值、随机序列、轨迹或像素等价；未跑 fixed13/full45。

`ce16de08` 把 emitter shape 从“解析到就执行”的分支收窄为独立准入：Directions 只接受 scalar 或完整 finite 三轴且绝对值不超过 1,000,000 的项目 multiplier 预算，Sphere Sign 只接受每轴 `-1/0/1`；malformed、短向量、非有限、超预算、非法 Sign 与 Box Sign 均零发射并记录 `emitterShapeUnsupported`。隔离真实 `3750342273` 的 layer 52 `Snow perspective` 记录 `emitterShapeBounded:sources=1`，particle 1/1、submitted/completed/failed `420/420/0`、drawable miss/sample residue `0/0`；黑白山景、时钟与多尺度雪粒子完整可见，ready/after 雪点位置和密度变化，changed ratio `0.092006`。该门证明真实 `directions="1 0.03 1"` / `sign="0 0 1"` 进入运行链且整景稳定，不证明 WE RNG、分布、轨迹或像素等价；未跑 fixed13/full45。

`7b1d36f` 的 Turbulence 定向门为 5/5，覆盖旧 full45 日志中 19 条 `unsupportedOperator:turbulence` 的 14 条；另 3 个命中样本没有在新实现上复跑。该批没有改变 particle root loaded 计数，也没有重跑 fixed13/full45。

`d19e76b` / `d792296` / `f4e8a0c` 依次补齐 TEX sampler、常见随机分布/Box/lifetime oscillation 与实际 mip 链。分布构建对 `2470144420`、`1937925563`、`3742133044` 各 1/1；最终 mip 构建对 `2470144420`、`3742133044` 各 1/1。`2470144420` 的 Sakura 命中 repeat、Turbulent Velocity offset、size exponent 2 与 7-level mip，Stars 命中 centered Box/Oscillate Alpha、但其 `halo_6` 是作者单 mip，因此 mip 修复不应改变星点。`3742133044` 的默认可见雪花命中 RG88 多 mip 保留；隐藏樱花不计入该门。该批没有样本 ID 分支，也没有重跑 fixed13/full45。

`5911049` 把 Oscillate Position 从按秒龄逐帧积分的 velocity-like 增量改为每个粒子 normalized lifetime 的精确波形差值，并让 mask 的非 0/1 数值成为轴向幅度权重。保留 full45 提取中该 operator 命中 16 样本、38 个 root layer usage、24 个 sample-definition path；定向 `2067939514`、`3088601835`、`3299228616`、`3742133044` 为 4/4。数值门锁定 lifespan independence、1 秒与 1/60 秒 fixed-step 同时相一致、完整周期无漂移和半权重 mask；没有 Windows fixed-seed trajectory golden，不能升级为 `L4`。`2470144420` 的 Sakura/Stars 都不使用该 operator，因此本批不宣称修正其剩余花瓣/星点残差。

## 6. 动态运行系统覆盖

### 6.1 Timeline 与 SceneScript

| 能力 | 当前级别 | 升级门 |
|---|---|---|
| Particle `animation` wrapper presence | `L2` | CP position/angles 的 nested wrapper 已进正式 Timeline IR；其他 particle instance field 仍只保留 presence/诊断 |
| Timeline object/property target | `L3` | 现役 Workshop census 为 effect constant 23、layer alpha 5、relative layer transform 9、root particle scalar override 7、bounded text `maxwidth` 2、bounded camera origin/zoom 2，共 **48/48** 个 authored host typed 执行；5 条普通 transform 走共享 world-frame，4 条 `lspot` angles 保持 strict spotlight consumer，camera 两成员共用 owner clock 并原子进入 projection。CP position/angles 另有 stock-wire/project-fixture typed target，只有 absolute position 的 root-emitter consumer；不得外推 generic target |
| Keyframe/value/tangent | `L3 bounded` | 帧号/值/双侧 handle/`lockangle`/`locklength` 无损保真；缺失/`null` handle 合法，非 object 报 `invalidTangent`。evaluator 以 segment-normalized X 与 property-offset Y 消费 `start.front` / `end.back`，enabled X 越出各自 segment 或已消费 control value 非有限时整条 fail closed；一侧禁用退化到端点、双侧禁用严格线性。44 场景 178 个 enabled front/back 均满足范围，四个样本含自定义 handle；单位与曲线是 clean-room 合同，仍无 Windows 同相位 golden |
| Loop/Single | `L3` | 按绝对 scene time 求值，真实执行 loop 7、single 21；single 到末帧保持末值不回绕，loop 跨周期同相位，有单测与真实样本双证 |
| Mirror | `L3` | evaluator 与单测覆盖三角波折返；随包 6 处 relative Mirror layer transform 已形成 additive binding，其中 `2938612768` 的普通 origin 走共享 world-frame，`3768903841` 的四束 `lspot` 继续走 strict spotlight consumer；端点采样仍无 Windows 定标 |
| start paused | `L3` | 6 处执行且恒停首帧；`2067939514` 为负门——无 VM 时不存在能调 `play()` 的主体，自动播放会偏离作者意图 |
| wrap-loop | `L3 bounded` | 9 条 Workshop 声明与 2 条随包 stock 声明共同约束跨周期闭合段；只准 Loop 与 `[0,length)` keyframe，按末 `front` / 首 `back` 求值并进入所有现役 typed consumer。官方只定义编辑器自动创建平滑过渡，不公开私有 wire/数值；无 Windows 同相位 golden |
| `relative` 合成 | `L3 bounded` | 9 处 layer transform 均编译为 typed additive binding；5 条 ordinary `origin`/`angles`/`scale` 走共享 hierarchy-aware world-frame，4 条 `lspot` angles 保持 strict spotlight consumer。camera origin 另按相同 additive 合同进入 bounded Combined camera target；非 layer-transform relative 继续 fail closed。公开官方页未定义私有 wire/合成公式，当前“作者基值 + 动画偏移”合同仍是 clean-room 校准，无 Windows golden |
| Combined Animations | `L3 bounded` | 双向 key 引用已保真；唯一真实 `default` 2D camera `origin` owner + `zoom` child 组共用 owner clock、同帧求值并原子提交。坏引用、多 path、未知 camera/queue、非 reciprocal 组及普通 Combined 整组 fail closed；不代表 generic Combined |
| Animation Events | `L0` | frame crossing、loop、同 layer script dispatch |
| Property binding IR | `L1` | 文档级保存 inline source、scene/object/effect/pass owner、完整 target path、properties、authored fallback 与 JSON value type；已证五类 target 有 13-shape 自建门；nested/未知 owner 不提升，`script + user` 冲突 fail-closed |
| Exact native text profile | `L3 bounded` | 七个 exact source/property profile 直接编译为 typed text target；不执行 JavaScript |
| Bounded text `update(value)` subset | `L3 bounded` | 唯一 exported update 的无循环 AST 直接求值 primitive properties、`new Date()` getter、条件与字符串装配；三层预算，未知语法/值失败关闭 |
| Exact native 64-band Audio Bars profile | `L3 bounded` | 两个 exact source/profile 直接编译为 64 个 renderer draw；不执行 JavaScript，也不创建动态 layer |
| Generic file/module/schema-type/handle IR | `L0-L1` | inline binding 的 owner、完整 path 与 JSON value type 为 `L1`；file/module dependency、schema-resolved Vec/value type、runtime handle identity 仍为 `L0` |
| ECMAScript VM | `L0` | 安全隔离、确定性 budget、异常处理 |
| `init`/通用 `update` 生命周期 | `L0` | bounded text subset 只纯函数求唯一 text update，不建立脚本实例；每屏实例、同帧 mutation、stop teardown 仍缺 |
| `engine` globals/Math | `L0` | 无 host bridge 或标准 Math runtime |
| bounded text `new Date()`/getter | `L3 bounded` | 同帧 wall date 注入纯 Swift AST evaluator；Gregorian 当前时区，尚无 locale/DST/离线 clock adapter/Windows golden |
| user/cursor/audio/media events | `L0` | generation queue、顺序、异常隔离 |
| component/object/particle API | `L0` | typed handles、只允许作者目标、失效语义 |
| timer/timeout/interval | `L0` | scene-time scheduler、pause/resume/预算 |

### 6.2 User Properties

完整控件和 target 计数见 [运行输入与属性覆盖表](runtime-input-property-coverage.md)。2026-07-22 的 21 样本 census 为 424 definitions、952 bindings、195 条 conditional bindings；本轮 26 样本完整运行门没有重做这项专项 census，因此不把旧计数冒充当前全集。layer alpha 73 条已编译并由当前 image/solid/text consumer live 执行；layer color 73 条全部指向 solid，其中 25 条属于纯 color key 可 live，`3122339805:basecolor` 的 48 条因同键还含未支持目标继续重建；exact Local Contrast strength 1 条与 `2902406982` 的 stock Opacity direct binding 已由 strict consumer live 执行。SceneScript Opacity candidates 继续计入 unsupported/fail-closed，不冒充 direct binding。

| 类型/行为 | 当前级别 | 当前边界或升级门 |
|---|---|---|
| Catalog/bindings | `L3` | 2026-07-22 的 21 样本专项 census、format 22 binding program 与受控 fallback；direct text content/point-size/color、Local Contrast/Opacity live，mixed/hidden/no-consumer/SceneScript fail closed |
| `color` | `L3` | UI/持久化/solid-only live consumer；补颜色空间、non-solid 与全部 target |
| `slider` | `L3` | min/max/default/step/fraction/precision UI；layer alpha、exact Local Contrast strength 与 exact stock Opacity alpha 已 live，其他 target 依 consumer 决定重建 |
| `bool` | `L3` | 条件/部分 target；不得按名称自动启用 effect |
| `combo` | `L3` | option value/条件；补全部 authored target |
| `textinput` | `L3` | 可编辑/持久化；有效可见 direct text consumer 可无重建更新，其他 target 仍重建 |
| `texture`/`scenetexture` | `L3` | PNG/JPEG picker/bookmark/static consumer 极窄子集；补 video/variant/general material |
| `usershortcut` | `L0` | parser 当前归为 unsupported；需 macOS 授权和安全降级 |
| group/order/condition | `L3` | 独立窗口已支持；补嵌套/全条件和负向门 |
| reset/default/override | `L3` | layer alpha reset/override 可原子 live 提交；texture bookmark 或非 live key 仍重建；补跨重启 UI 门 |
| Texture Variants | `L0` | 补 schema、checkbox/combo 选择和 provider identity；脚本不得切换 |
| property update event | `L0` | typed snapshot 后再派发给 SceneScript |

### 6.3 Text、Audio、Media 与 Provider

| 能力 | 当前级别 | 当前边界或升级门 |
|---|---|---|
| 静态文字内容 | `L3` | CoreText 可见；结构样本 `79/108` |
| 字体解析/fallback | `L3` | 解析顺序 alias → 包内文件 → 客户端自带（stock）→ 缺失，路径安全判定优先于后三层。官方 `systemfont_*` 别名共 8 个（`arial`/`verdana`/`segoe`/`sansserif`/`consolas`/`comicsans`/`cambria`/`calibri`），`consolas` 缺失时退到 Menlo 保住 fixed-pitch。客户端 `assets/fonts` 的 15 个官方名字全部物理位于 `SceneStockAssets.bundle/assets/fonts`：8 个原版报 `stockBundled`；7 个替代字体按官方文件名读取，报 `stockSubstituted` + `stockFontSubstituted:<category>`。包缺失或损坏才报 `stockApproximation`。**等级仍为 `L3`**：7 个替代字形的轮廓、advance 与 kerning 不是官方字形，8 个原版也无 Windows 栅格化逐像素对照，见 [E-TEXT-FONTREF](runtime-evidence-index.md#e-text-fontref) |
| point size | `L3` | 官方 `pointsize` 是 300 DPI 磅值（`lib.sceneScript.d.ts` 的 `ITextLayer`："Size of the font in points for 300 DPI"），栅格像素字号取 `pointsize * 300 / 72`，随包 `dino_run` 两个记分标签的作者 size `780x291`/`390x145` 四个数字逐位复现（此前的 `round(pointsize * 4)` 低 4%）。字号仍被夹在 `[1, 1024]` px，作者值 ≥ 245.76 磅起偏离官方换算，这是本地纹理保护不是官方合同；数值判据来自本机只读探针而非仓库内自动门，也无 Windows 逐像素对照，见 [E-TEXT-POINTSIZE](runtime-evidence-index.md#e-text-pointsize) |
| baseline/alignment | `L2` | 基线（baseline）本身未处理，`blockalign` 未解析；对齐字段的执行边界见下一行，溢出裁剪见 overflow limits 行 |
| text alignment pivot（`horizontalalign`/`verticalalign`）与 padding | `L3` | 官方取值域 left/center/right × center/top/bottom 全部参与 quad pivot；作者 `size` 是已含 `padding` 的外框，边对齐把去掉 padding 后的内容边钉到 origin，center 保持几何中心，缺省与未知值退回中心；同一组字段继续喂 CoreText。动态内容仅在 `limitwidth=false` 时取不小于作者外框的 intrinsic+padding size，并把同一 render size 传给 provider capture、effect/FBO 与最终 quad，见 [E-TEXT-PIVOT](runtime-evidence-index.md#e-text-pivot) / [E-TEXT-SCRIPT](runtime-evidence-index.md#e-text-script) |
| screen anchor（text layer） | `L3` | 官方 10 个取值全部解析并每帧折进 layer 平移，`none`/缺省/未知取值不偏移；离屏 GPU 门下 21:9 的 `dino_run` 记分标签从 0 覆盖像素回到与 16:9 完全一致的 396 px 右上角；只有 text layer 携带该字段，Windows 像素标定见 [E-TEXT-ANCHOR](runtime-evidence-index.md#e-text-anchor) |
| overflow limits（`limitrows`/`maxrows`/`limitwidth`/`maxwidth`/`limituseellipsis`） | `L3` | 五个字段随 interpretation v29 进 text IR 并由 CoreText 栅格化消费；两个数值只在对应开关打开时生效。`limitwidth=true` 的动态文字继续遵守作者/`maxwidth` 限制；`limitwidth=false` 的动态内容按 intrinsic size 扩到不小于作者框，避免 clock/date/day 被占位符框裁断。静态文字布局不变；官方省略号和动态外框无 Windows 逐像素对照，见 [E-TEXT-LIMITS](runtime-evidence-index.md#e-text-limits) / [E-TEXT-SCRIPT](runtime-evidence-index.md#e-text-script) |
| outline/shadow/text effects | `L1` | 字段或 effect 可保留，未形成完整绘制链 |
| property-driven text | `L3` | direct user property、exact native profile 与 bounded text update subset 都经 per-frame typed snapshot、按 layer generation/stale cancellation 和动态纹理 consumer；其他 target 仍依 consumer 决定重建 |
| SceneScript clock/text | `L3 bounded` | exact clock/day/date/greeting 七 profile之外，唯一 exported `update(value)` 的公共无循环 Date/string 子集可按语法准入；`2134765860` 两层与 `2802243144` 一层为真实正门。完整 VM、timeOfDay/engine、init/event/handle/module 仍为 `L0` |
| Audio declaration | `L3` | effect 与粒子两套 schema 分别保真解析；两者字段名不同，粒子无 `audioamount` |
| Audio 16 bins | `L3` | left/right host-shared 快照，静音/无权限/停采集稳定归零，可注入；当前 Scene 分析器以 -60 dB 下限扩大相邻条幅度差，频段、下限与归一化仍为工程选择 |
| Audio 32/64 bins | `L3` bounded | left/right host-shared snapshot 与同次 FFT 已接；供统一Program中的Simple Audio Bars 32/64-band作者shader及两个exact native property-script 64-band profile。当前`2938612768:563`默认32档与`3122339805:64`显式64档数组均由统一Program实际编码并取得非零可见柱高变化；旧Simple专用consumer已删除，其他Workshop/SceneScript不外推 |
| Audio effect consumer | `L3` | stock Shake（`whitePhaseFallback`/`timeOffsetCombo`）、exact old-editor Shake（`legacyUnconditionalPhase`）、stock Pulse（`stock2842`，并接受同一精确旧版 profile 缺省 `replacementkey`）继续使用各自bounded consumer；可静态证明的`3122339805:64` ordinary、`2938612768:563` composition与`3299228616:151` relocated Simple Audio Bars正例均由Program消费16/32/64 host数组，后者保留上下双向及后续zero-distortion Fisheye顺序。旧Simple专用consumer已删除，其他Simple combo/topology必须形成完整Program或失败关闭。其余legacy指纹、其他Workshop audio shader、未支持combo与SceneScript binding fail closed |
| Audio particle consumer | `L0` | 声明已保真但求值公式无证据；见 5 节 Audio-response execution |
| Exact native property-script audio bars | `L3 bounded` | 两个严格 source profile；64 档逐 bin average、单纹理 64 draw、完整准入与失败关闭；不产生 JS object 或 Scene layer handle |
| Generic SceneScript audio API | `L0` | 无 VM、`registerAudioBuffers`、`AudioBuffers` object/array identity 或订阅 lifecycle |
| Sound layer | `L0` | `3743305891` authored FLAC 尚未解码/播放，也未成为 Scene 频谱输入；当前 system tap 排除本进程。需补 sound content IR、提取/播放、volume、生命周期与自有频谱合同 |
| Embedded MP4 frame | `L3` | image-layer 子集；不同于系统媒体 provider |
| Media status/metadata/timeline | `L0` | injectable snapshot 和 lifecycle |
| Media thumbnail identity | `L3 bounded cover` | current/previous identity 已分离并经 frame registry 显式 generation 消费；replacement pending 保留 last-ready generation，ready 后原子换代；previous 另有一个内容/结构严格准入的 transition consumer；见 [E-MEDIA-THUMBNAIL](runtime-evidence-index.md#e-media-thumbnail) |
| Media thumbnail producer/consumer | `L3 bounded cover` | producer-agnostic inbox、16 MiB 输入预算、最长边 256 异步 decode、旧 request 协作取消/stale completion 拒绝、pending last-ready、clear/decode-failure 最终作者 fallback、严格 normal/full-strength Blend current consumer，以及 per-surface pause-safe previous gradient transition；单次 ImageIO 调用不可抢占，且无 live platform producer、generic event/SceneScript、generic Blend Gradient 或其他 variant |
| Layer/named target provider | `L3 bounded；generic carrier L2` | bounded current-frame graph；B12/B13让exact Clipping Mask与legacyWorleyColor的普通image/composition consumer以launch-time binding声明单一hidden static provider，frame reservation和provider capture复用同一MTLTexture，只有成功capture才发布ready，late execute要求同一epoch/object。`3768903841`的provider 42→consumer 49已走该合同。R3 registry继续保留exact graph identity与ready/incomplete/absent/pending/unavailable/missing区分；effectOutput/FBO仍未形成通用publication |
| Named secondary identity | `L2` | registry 已区分完整 `_b` variant |
| Named secondary producer/consumer | `L1` | executor 识别后拒绝；需真实数据流 |
| Property file provider | `L3 bounded` | PNG/JPEG 静态 consumer 极窄子集；R3 material/Blend 只在 exact identity 被显式发布为 absent 时回退作者候选，missing/pending/unavailable/incomplete 全部失败关闭；仍缺通用 GPU consumer、video/variant |
| Video/system provider identity | `L2 carrier` | R3 Template/registry保留exact system demand/publication state；purpose冲突、publication不完整或lifecycle未证时为unavailable，不伪造absent |
| Video/system generic producer | `L0` | 尚无通用 provider lifecycle |
| Generic material slots 0...7 | `L3 bounded` | Template 固定保留 8 项及 hole，Program 在同一 snapshot 原子携带 provider publication、active variant/reflection、uniform/state/color 与 exact/semantic identity；R4 bounded 子集已由 resolved pass encoder 和统一 graph executor 完成 GPU/compositor/next-frame 闭环，未证 slot/purpose/readiness 仍失败关闭 |

## 7. 高级对象与输出覆盖

逐 Puppet、Model、Lighting、产品预算、RGB 和 offline 项见 [高级对象覆盖表](advanced-object-coverage.md)。

| 能力族 | 当前级别 | 最小可用门 |
|---|---|---|
| Puppet asset identity | `L2` | 更多 MDL/MDLA 版本、辅助轨道和完整资源图 |
| Puppet mesh/bones/weights runtime | `L3` bind pose + 静态 attachment + 严格单 clip / disjoint-bone additive CPU LBS | 插值、冲突 mixing/权重、GPU skinning、动态 attachment follow、层级/遮罩 |
| Puppet spring/rigid/rope/wind | `L0` | fixed timestep solver、events、确定性 golden |
| 2D PBR maps | `L0` | normal/roughness/metalness/emissive slot 与 color space |
| Bounded standalone volumetric `lspot` | `L3 bounded` | 四束 authored cone 已按 source order、world origin 与 relative Mirror angle Timeline direct draw；无 lit-material interaction |
| Generic point/spot/tube/directional light | `L0` | typed 四灯 IR、author-enabled material、ambient、provider 与完整排序/坐标链 |
| Generic lit-material shadow/reflection/light volume | `L0` | standalone `lspot` cone 不含 material interaction；仍需 RT graph、depth/occlusion、预算和像素门 |
| Official Scene Bloom target identity | `L1` | typed scene target 已定义；binding 尚未分类 |
| Official Scene HDR/Bloom post runtime | `L0` | HDR targets、tone mapping、scene ordering |
| Workshop layer Bloom approximation | `L3` | 受限 layer effect pipeline；不得冒充 Scene post |
| 3D model/node/material | `L0` | asset loader、scene graph、camera、PBR material |
| Skeleton/attachment/animation | `L0` | animation evaluator、skin/attachment 生命周期 |
| 3D physics | `L0` | fixed timestep、collision、determinism |
| Custom shader reference/path/source identity | `L3 executed-degraded` bounded subset | material path 与 authored stage source/raw/canonical identity 已保存；独立 source graph 保留候选、依赖与 provenance，active source/frontend、slot/provider、uniform/state/color 再原子形成 Program identity。bounded Program 只由 resolved-material graph executor 执行；三组 exact Scroll 与早期 GPU fixture 仅是历史 oracle，不再形成第二个产品 owner，也不代表任意路径/source可执行 |
| ShaderContract/source preparation | `L2` | source/include/annotation/declaration/stage已保留；bounded include/directive evaluator、typed combo/readiness schema、active source map/digest与unresolved color carrier已落地。R3的Template/Program进一步原子承载8槽provider/default、reflection、uniform/state与保守resolved color子集；跨语言prelude、verified environment provider、未证purpose/state/color与GPU consumer仍缺 |
| Resolved material Template/Program | `L3 bounded` | production raw graph 形成固定 8 槽 Template；同代 frame/resource/dynamic snapshot 形成 exact/semantic Program identity。R4-1/2 对 analyzer 证明的 straight-alpha + implicit framebuffer 子集完成 capability admission、dynamic effect-constant binding、Program GPU 编码、resolved owner publication、compositor 与 next-frame observation；only-explicit-absent fallback 与所有 unknown fail-closed 继续有门 | 任意 sampler/多 pass/复杂 state、通用 condition/function、未证 color transfer 与更广泛 graph resource 仍缺；旧 owner 尚未全部撤权，不能写成唯一 graph executor 或完整兼容 |
| Custom shader execution | bounded subset `L3 executed-degraded`；arbitrary `L0` | 现有 bounded single-pass framebuffer subset可执行；2026-08-08 pre-R4-A+C full45曾记录14样本 / 57 resolved layer / 75 exact subject，其中普通Opacity、Color Key与41个Tint subject由统一Program owner持有。该历史聚合没有被本轮刷新。R4只通过公共frontend/binder/color/capability合同准入，不按sample/layer/path/hash选择；旧`.authoredShader` backend/pipeline cache/Scroll profile已撤权，三组exact Scroll只作历史oracle | include/combo、未证sampler/purpose、多pass、复杂render state、任意source/GLSL、Opacity/Tint未迁移形态、其他dedicated/legacy/utility/audio owner撤权与Windows golden仍缺；bounded Program证据不等于通用shader兼容 |
| RGB device | `L0` | macOS 产品策略、授权、设备 adapter |
| Debug PNG readback | `L2` | 可生成 benchmark 截图证据；不得标成 offline bake |
| Offline bake | `L0` | 与实时共用 IR/evaluator/render graph，固定时钟和编码输出 |

## 8. Coverage-first 实施批次

| 覆盖批次 | 开发计划映射 | 目标 | 完成判据 |
|---|---|---|---|
| **B0 Contract/Runtime Kernel** | `S3 第 1-4 项` | v22 binding program、per-surface transaction、atomic state、alpha/solid-color/direct-text/strict Local Contrast/Opacity consumer 与 rebuild fallback 已闭合 | 新 live target 继续要求 compiler、consumer、原子失败与不换 surface/window；pause/fixed-time 单列 |
| **B1 Provider Core** | `S2 第 5 项 + S3` | dynamic text、bounded media cover与embedded MP4保留各自已证lifecycle；R3把frame registry扩为exact layer/named/graph/asset/property/system identity，publication atom保存request/candidate/content generation及完整candidate metadata，immutable snapshot区分ready/incomplete/absent/pending/unavailable与missing。Blend与Program只有explicit absent可回退；launch VFS catalog可发布purpose-proven static asset | 把主动cancel/teardown推广到Texture Variants、live system media与动态provider；补effectOutput/FBO command publication、单次decode抢占、media多surface/hot-plug与video真实pause/hot-plug/seek/loop；不含generic GPU consumer或nested/effectful provider |
| **B2 Graph Resource Runtime** | `S2 第 1-5 项` | 既有strict target table、copy/swap/history/clear、Precise Blur interleave与typed utility/dependency capture保持；R2/R3建立preparation与Program，R4-A至B13持续把Program、pair、logical-target、exact raw full-frame pair与external-primary dependency迁入统一executor。B9闭合legacy Directional God Rays，B10收敛visibility lifecycle，B11迁移exact captured-main Standard Blur，B12迁移10层/11段exact Clipping Mask，B13使正式矩阵旧authored whole-chain corpus owner归零；B14删除resolved-program legacy yield双入口，B15删除旧whole-chain compositor产品调用，B16删除Light Shafts main-pass fallback，B17把legacy product planner authority由4降为0 | R4仍有manifest retirement gates 53 occurrences及独立ImageBlend owner：fixed Text/Texture/Media/AudioBars selector与numeric Workshop dispatch必须继续在R4撤权。R5 legacy authority只剩3个observation调用，待R4完成后与standalone helper等不可达脚手架一并删除；里程碑full45只验证当前矩阵，不可越过这些产品门直接进入R5。算法/视觉债务不阻塞架构主线，但若R5发现仍需产品行为迁移必须退回R4 |
| **B3 Provider-Graph Integration** | `S2 第 5-6 项` | nested/effectful/scene-background source、通用 material consumer、45 Effect 严格 profile family | B1+B2 均完成后接入；不得新增 effect-name 视觉旁路 |
| **B4 Feature Breadth** | `S3-S4` | direct dynamic text、七个 exact native clock/day/date/greeting profile、公共 bounded Date/string text update、bounded Blend time-of-day subset、exact day/night texture toggle、两个 exact native 64-band property-script Audio Bars、Timeline **48/48** 现役 authored-host 子集（含 bounded 2D camera Combined）、root Particle direct User Property 八字段、bounded Particle Audio Response、bounded classic Vortex、16/32/64 档 host audio、stock Shake/Pulse 与 exact Workshop Simple Audio Bars consumer 已完成；通用 SceneScript core、Sound、system/media text、cursor/media、generic Combined/multi-path/3D camera、其余 Timeline/audio 与按依赖排序的 particle breadth 仍待推进 | 每族正向、默认关闭、unsupported、determinism 和 lifecycle 门 |
| **B5 Fidelity** | `S2-S4` 广度完成后 | 字体、视差、粒子、常用 Effect 与 WE Windows golden 对齐 | 固定输入逐像素/数值阈值、性能预算、长稳和多屏门 |
| **Advanced** | `S5` | Puppet、2D light/HDR、3D、arbitrary custom shader、RGB、offline bake | 每个系统有完整 IR/runtime/lifecycle/product gate 后再升级 |

研究可以并行，产品执行不能倒置：B0 live-property、direct dynamic text generation、七个 exact native text profile、bounded Date/string text update、bounded Blend time-of-day expression、两个 exact native 64-band property-script Audio Bars、Timeline 受限 typed target、Scene audio 和既有 bounded effect/particle/resource runtime 已形成各自正向门；unsupported Effect/Timeline/粒子/通用 SceneScript 形态继续 fail closed。当前完整门与聚合计数见页首及运行证据索引；`3299228616` 的新增两 stage与`1937925563`的Pulse/Water Ripple/Directional Godrays只属于exact profile。`2470144420`与`3742133044`的严格Iris终止后缀是R4-A前历史证据，现已被complete-chain-only合同取代。bounded text update不能外推engine/timeOfDay；独立Blend子集也只开放已列expression与multiply target，不能外推为通用ECMAScript、engine object、event/module或其他target。两个audio profile不外推为`AudioBuffers` API或动态layer。其他可见未知script继续诊断；任何可见effect若没有resolved/typed产品claim就失败关闭，不再以旧planner恢复执行。requires-source-copy与layer color blend只是结构性合成，不授权effect；正式text diagnostics显式注入的planner summary也只是观察面。官方图与当前帧只作方向性检查，没有同步Windows golden时不宣称视觉等价。下一代码批须从专项缺口重新选择，不从任一bounded正门外推通用兼容。

## 9. 更新规则

1. 每次 Scene 能力提交必须更新本表对应行和精确边界；只更新开发流水账不算完成。
2. 升级到 `L2` 必须有结构/路由测试；升级到 `L3` 必须有实际执行正例、作者关闭反例、失败降级和生命周期门；升级到 `L4` 必须有官方行为或 Windows golden。
3. 新发现的官方能力先补 [官方页面全目录](official-page-catalog.md)、[页面能力映射](official-page-crosswalk.md) 和专题语义，再进入本表；私有字段按 [资料来源与证据索引](source-index.md) 标证据等级。
4. 当前 baseline、矩阵报告、测试总数和签名 App 身份只以 [运行证据索引](runtime-evidence-index.md) 为主答案；本表只维护系统摘要与专项入口，带日期的计划和路线图是批次快照。语义自动门校验布局合同与相对链接，不假装同步这些动态事实。
5. 开发开始顺序：先看本表选择最低公共依赖，再查专题合同和 source index，最后查看样本命中；不得先凭截图写视觉特判。
6. 总表只允许单一 `L0` 到 `L4` 等级；若同一能力同时存在 IR 与 executor 子集，必须拆成两行或下沉专项表。
7. 每行至少要能追溯到专项表中的代码、测试和运行证据；只有 parser 或结构时不得写成执行支持。
