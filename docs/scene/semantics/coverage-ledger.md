# Scene 官方语义与实现覆盖台账

> 状态：现役 current capability 唯一系统摘要；逐项合同与专题内等级由各专项能力表维护
>
> 最近核对：2026-08-21
>
> 当前系统能力、主要缺口和明确待办只由本页汇总；唯一执行顺序见 [Scene 兼容执行路线](../scene-compatibility-roadmap.md)，精确 App、矩阵、报告与运行身份统一见 [运行证据索引](runtime-evidence-index.md)。路线只拥有 V0-V5 顺序，不复制或改写 current capability；逐批迁移过程只从[统一历史索引](../../history/README.md)追溯，不得覆盖当前结论。

本表把已收集的 Wallpaper Engine 作者语义逐项映射到 MyWallpaperX 当前代码、运行证据和下一道验收门。详细语义仍以同目录专题文档为准；这里回答三个问题：官方是否有这项能力、当前播放器走到哪一级、下一步补什么公共能力。

## 1. 口径

现役产品只保留 admission → Program/typed stage → GraphExecutor → compositor 主链；`source-proven-scalar-color-interpolation`、`source-proven-opaque-scalar-output`、`source-proven-straight-alpha-r8-signal`、`source-proven-independent-premultiplied-output`、`source-proven-graph-target-passthrough`、`source-proven-normalized-sample-sum`、`source-proven-graph-input-alpha-attenuation`、`source-proven-graph-input-color-blend`、`source-proven-graph-input-stage-uniform-passthrough`、`source-proven-graph-input-stage-uniform-straight-alpha-preserving`、`source-proven-graph-input-stage-uniform-straight-alpha-preserving-no-auxiliary`、`source-proven-graph-input-audio-stage-uniform-straight-alpha-no-auxiliary`、`source-proven-graph-input-conditional-straight-union`、`source-proven-graph-input-single-sampler-alpha-mutation`、`source-proven-graph-input-same-slot-channel-reconstruction`与`source-proven-graph-input-auxiliary-rgb-blend-alpha-preserving`十六个已登记 profile 当前缺省 `generic-only`。其中 independent-premultiplied-output 必须由 prepared source 证明预乘输出，active sampler 不得包含 previous-current slot 0，也不得含 graph target/input、external provider 或 scalar output；它不按 Light Shafts/effect/material/sample/layer/path/hash 准入。color-blend 仍只接受 prepared source 证明“一个 exact previous-current graph input 与一个 uniform RGB 做 `ApplyBlending`，权重为有限 scalar、至多再乘或替换一个非 graph red-only opacity mask，mode 0 明确写 opaque alpha，其余 mode 保持 source alpha”；source slot、active sampler schema、graph role 与 optional readiness envelope 必须完全一致。真实 mixed-chain 反证表明更宽的 `source-proven-graph-input-straight-alpha` 与 `source-proven-graph-input-straight-alpha-preserving` 尚未覆盖完整 compiler language envelope，因此两者仍为 `prefer-generic`。Film Grain stock/no-mask、严格 alpha-attenuation、source-proven color-blend、source-independent premultiplied output、conditional straight-union、stage-uniform straight-alpha-preserving与无auxiliary stage-uniform straight-alpha-preserving cohort 已从旧专用产品 owner 撤权；对应共享 MaterialProgram/GraphExecutor 持有产品输出，旧 Opacity/Tint/Light Shafts/Workshop Shadow/Workshop Shift Hue/Workshop Gradient/Enhanced Audio Bars planner、profile、backend、pipeline、像素renderer与texture loader及实现耦合测试已删除。仍存在的`SceneLightShaftsLayerRenderer.swift`只做quad模型放置并把resolved frame plan交给唯一compositor，不生成Light Shafts像素；这个effect命名carrier仍是后续通用化/清理债务。默认坏 artifact 以 `artifact-contract-rejected` 撤销当前 effect 的 generic claim，再由共享 GraphExecutor 发布 exact previous-current 并继续 suffix；它不会恢复已删除 owner，也不把失败 effect 记为成功。只有显式 `disable-generic` 才请求 shared `boundedSwift` frontend；该共享frontend仍无法编译时继续收窄为effect-local previous-current。Color-blend optional mask直接消费MaterialProgram的typed asset publication；pending、unavailable、wrong-purpose、wrong-content与sampling-unresolved只撤销当前eligible effect并保留suffix，generation/request identity等完整性错误继续硬拒绝。eligible launch-time视觉合同失败对无 clear、无 target/history/dependency/blocker 的单 material leaf和普通多material full-frame compose effect均已收窄为 effect/stage-local previous-current passthrough；多节点形态必须全部读取effect input、写effect output、非末node均`compose:true`且末node不compose，任何partial Program都不发布。V1现又对两类exact single-effect ordinary FBO topology开放同一局部视觉降级：无command的两material `effect input → FBO → effect output`；以及由先前authored write支配每次target read、material ordinal递增、所有target初始化/消费、唯一terminal full-frame output的ordinary material/copy/swap链。eligible失败时丢弃全部prepared material/command prefix和未提交target/mapping/resources、发布effect-entry previous current并在下一帧重建普通graph；format仍由既有target/attachment admission决定，fresh App可见证据只覆盖RGBA，项目Metal门另覆盖已准入R8无command形态与未见mixed command rollback。clear、unique、read-before-write history、condition/function、dependency、blocker、其他target/command topology及target/ABI/generation/epoch/hazard/lifecycle/runtime encode完整性错误仍硬拒绝。`MWX_SCENE_GENERIC_SHADER_PROFILE_ROUTES` profile-local registry 已通过隔离 harness 正反门，route decision 现在在 preparation 生成 immutable snapshot 并随 compiled Program 进入 execution telemetry。固定 glslang → SPIR-V → SPIRV-Cross MSL backend 在签名 bundled worker/cache 中预算化编译并原子发布，launch envelope 在首帧前预热或负缓存 Metal pipeline；公共 lowering/frontend 没有 effect/sample/path/hash matcher。V0 首个可见完成门已达到 `slice-visible`，当前主线继续 V1 graph/FBO/command 与剩余 owner debt。

> 纠偏记录（2026-08-20）：前两个过早删除 owner 的提交已经回滚并恢复能力后，Film Grain stock/no-mask 与严格 alpha-attenuation 分别完成共享接管。Opacity 本次是在 mask-present、texture-absent、坏 artifact 局部 passthrough、未见命名组合与 `disable-generic` 回滚均闭合后才删除旧产品 owner；Tint/ColorBlend cohort 随后先建立 source-derived shared profile、optional-mask局部失败、mapped/mip R8实际采样、未见命名组合与shared bounded frontend回滚门，再成族删除旧planner/profile/pipeline/renderer/loader。Light Shafts 初次候选删除时新增profile没有命中真实stage，因此没有直接提交；纠正为由prepared颜色合同与active sampler/graph facts准入后，三个既有真实正例均以`generic-only`完成GPU/publication/compositor/next-frame，`disable-generic`又只回滚shared `boundedSwift`并保留输出，才删除整套专用leaf。这些有界profile均不外推宽泛 straight-alpha、任意Tint/Light Shafts family、完整`common_blending.h`语言面或官方像素parity。

当前Film Grain stock/no-mask、严格alpha-attenuation、source-proven color-blend与`source-proven-independent-premultiplied-output`均不再有可恢复的旧effect产品fallback；显式`disable-generic`只进入共享`boundedSwift` frontend。Light Shafts 的旧 main-pass、typed planner/plan/compiler backend/pipeline/resource loader及耦合测试已全部删除；真实`3299228616:601`、`3766387484:80`与`3768903841:162`由同一公共profile进入generic artifact、MaterialProgram与GraphExecutor。该结论只完成这个source-derived cohort的owner migration，不升级Corner、mask、non-direct-draw、ordered input、其他revision或官方视觉等价。

同日的 conditional straight-union owner cohort 把已有源码结构证明从过宽的`source-proven-graph-input-straight-alpha`中拆成`source-proven-graph-input-conditional-straight-union`：只接受同一唯一 graph-input slot 的两个RGBA sample、两个whole-color fallback、完整RGB+alpha分支写入与唯一terminal return，且无external provider、scalar output或authored graph target。该profile缺省`generic-only`；默认坏artifact只让各个实际consumer执行`material-generic-owner-revoked` previous-current，只有显式profile-local`disable-generic`回滚shared`boundedSwift`。真实`3767460992:945#effect#956`默认与回滚均闭合GPU/publication、后缀effect、terminal compositor与next-frame；旧Workshop Shadow planner/plan/compiler backend/pipeline/renderer及耦合测试已删除。这个source-derived合同不按Shadow、effect、sample、layer、path或hash准入，也不升级其他conditional、其他Shadow revision或官方像素parity；精确证据见[E-EFFECT-WORKSHOP-SHADOW](runtime-evidence-index.md#e-effect-workshop-shadow)。

同日的 typed Timeline effect-vector 原子又纠正了公共 producer 把所有 effect constant 压成 scalar 的偏差：`SceneTimelineTargetCompiler` 现在从完整作者 raw value 严格形成 1–4 维 scalar/vector definition，缺失、空、畸形、非有限、超过四维或 animation lane 数不一致时不生成 target；合法普通值沿唯一 per-surface snapshot 进入 MaterialProgram uniform，值变化不改变 Program semantic identity 或 prepared shader cache key。真实资源派生的 representative Tint color vector3 已经由 shared Program/GraphExecutor 产生红降绿升的 next-frame 可见变化，lane mismatch 只让该 effect 做 previous-current passthrough，`disable-generic` 回滚到 shared `boundedSwift` frontend；真实 `3748311238:728` 的两个 Shake `friction` vector2 consumer 也进入普通 Program/GPU/publication/compositor，frame-1 dynamic snapshot 故障只局部复制 previous current 并在 frame 2 恢复原 Program。该 Timeline 原子本身没有撤销任何旧 owner；Opacity与Tint分别由后续独立alpha-attenuation和color-blend owner门撤权，Shake仍保持原结论。Timeline 证据不能外推其余 vector consumer、整个 effect family、owner migration 或 parity。

2026-08-19 的 graph-input straight-alpha 原子证明了一个不按 Fisheye、definition/material path、sample/layer/hash 准入的 bounded 形态：prepared source 必须证明 `.straightAlpha(sourceSlot)`，输出为 color、无 external provider，且 active slot 由 typed graph binding、material graph-input alias 或既有 `implicitFramebufferSlots` 结构规范化证明；真实 `3765760121:96` 曾以 `genericCompilerArtifact` 闭合 GPU/publication/compositor/next-frame，旧 Fisheye 专用产品链也已删除。2026-08-20 的更广真实 mixed-chain 随后反证该 profile 的默认 `generic-only` 范围过宽：同一 profile 内既有 Color Key/Shadow 的合法 generic artifact，也有当时尚缺 mutable fragment varying、straight-output lowering 与 exact uniform-key 支持的 Enhanced Audio Bars。现役宽泛 route 因此仍为 `prefer-generic`，不撤销上述 bounded 正证，但撤销 profile-level owner-migration 完成声明；Enhanced Audio Bars 后续只通过更窄的 source-derived audio/stage-uniform profile 完成 owner 迁移，不能反向扩大这个宽泛 profile。精确恢复证据见 [E-V1-ORDINARY-SHADER-REGRESSION-RECOVERY](runtime-evidence-index.md#e-v1-ordinary-shader-regression-recovery)与[E-EFFECT-WORKSHOP-ENHANCED-AUDIO-BARS](runtime-evidence-index.md#e-effect-workshop-enhanced-audio-bars)。

2026-08-21 的窄 graph-input alpha owner cohort 没有重新放宽上述两个宽 profile，而是从 prepared source 另行证明两个互斥子集：`source-proven-graph-input-single-sampler-alpha-mutation`只接受唯一 regular graph-input sampler、同一 source slot、无 auxiliary/provider/target/scalar output、sampled alpha 在 terminal return 前被明确改写；`source-proven-graph-input-same-slot-channel-reconstruction`只接受同一 slot 的 base sample 加按 RGB 单分量写回的 shifted samples，alpha 必须保持 base sample 且不得有隐藏 sample、alpha write 或 control flow。运行审计发现 Color Key 也合法命中前一 source shape 后，没有把结构 PASS 直接升级为 owner 完成：共享 artifact v2 先纠正旧 lowering 只在 alpha factor 与 RGB 无关时才成立的 whole-premultiplied-vector 快捷式，现对整个 source-proven single-sampler alpha-mutation cohort统一执行 sample unpremultiply、作者 RGB/alpha 数学、terminal premultiply，并以 backend identity v2 使旧 cache 失败关闭。两 profile 缺省`generic-only`，坏 exact artifact 在 claim 前以`artifact-contract-rejected`撤权并只让当前 effect 发布 previous-current，显式`disable-generic`才回滚 shared `boundedSwift`。真实`3765760121:96`、真实 mixed-chain `3767460992`中的四个 stock/Workshop Color Key consumer，以及从当前真实`3779026256`资源树派生、仅去掉未证明 visibility script 的隔离`9000000622:112`均闭合各自默认 generic artifact、profile-local rollback、GPU/publication/compositor/next-frame；坏 artifact 报告按预期 NON-PASS，但安全 transaction、后缀输出与唯一 compositor 继续。Chromatic 派生 fixture 不是原始真实样本、Fast/fixed/full 或官方对照；这些证据不外推宽 straight-alpha profile、整个 Fisheye/Color Key/Chromatic family 或 parity。精确证据见[E-V1-NARROW-GRAPH-INPUT-ALPHA-OWNERS](runtime-evidence-index.md#e-v1-narrow-graph-input-alpha-owners)。

同日的 auxiliary RGB blend owner cohort 也没有放宽`source-proven-graph-input-straight-alpha-preserving`：新profile只由prepared source证明“唯一graph-input source carrier + 一个distinct auxiliary RGB slot + direct uniform mix或登记的双sample/product/pow/ApplyBlending preprocessing + source alpha原样terminal output”，并排除全fragment隐藏sample、source/auxiliary同slot、alpha write、非uniform blend、provider/target/scalar output。改名direct-mix未见正例与五类更宽反例锁定它不按Film Grain/effect/sample/layer/path/hash准入。真实`3767460992:20#effect#702`由宽profile的`prefer-generic`迁到该窄profile的`generic-only / genericCompilerArtifact`；坏exact artifact以`artifact-contract-rejected`撤销当前effect，GraphExecutor发布`visual-failure-passthrough:material-generic-owner-revoked`并继续GPU/compositor/next-frame，显式profile-local`disable-generic`才回滚shared `boundedSwift`。这只减少真实mixed-chain的bounded frontend产品owner债务，不恢复或新增专用Film Grain renderer，也不外推其他noise/mask/blend/revision、独立Film Grain ROI或官方像素parity。精确证据见[E-V1-AUXILIARY-RGB-BLEND-OWNER](runtime-evidence-index.md#e-v1-auxiliary-rgb-blend-owner)。

同日的 stage-uniform graph-input owner 原子修复 linked reflection 把vertex/fragment共享UBO超集误当成两个stage都主动消费的ABI问题：artifact现在为每个field保存实际stage，Program只要求对应stage声明/使用的作者uniform，共享host `mwxRenderSize`仍保持跨stage。`source-proven-graph-input-stage-uniform-passthrough`只由prepared passthrough、无external provider、active typed graph input以及vertex/fragment均存在source-proven非sampleruniform事实准入，不含Foliage、definition/material path、sample/layer/hash。真实`3765760121:187`两个连续Foliage effect默认均为`generic-only / genericCompilerArtifact`并闭合GPU/publication/compositor/next-frame；损坏exact artifact时以`artifact-contract-rejected / bounded-frontend-owner-revoked`在claim前失败关闭；同一坏artifact下`disable-generic`只回滚共享`boundedSwift` frontend。旧Foliage dedicated planner、pipeline、renderer、texture loader与专用测试owner已经删除，生产Scene树无`SceneFoliageSway*`引用。精确证据见[E-V1-STAGE-UNIFORM-GRAPH-INPUT](runtime-evidence-index.md#e-v1-stage-uniform-graph-input)。

同一公共profile随后撤销Water Waves `stock2842`的旧产品fallback，而没有扩大source准入：当前corpus中该source profile覆盖11个样本、62个可见 authored instance；最终App把其中49个active exact subject全部以`generic-only / genericCompilerArtifact / resolved-material-graph`编码，分布于27层，另13个保持inactive，Water Waves dedicated/fallback为0。显式profile-local `disable-generic`只把`3768903841`四个stock stage回滚到共享`boundedSwift`，不恢复旧renderer。`reversedDirectionV1`、两个`directV1` source fingerprint与`reducedV2`仍保留原dedicated fallback；因此这里只完成stock profile的`owner-migration`，不表示整个Water Waves family、V1或视觉parity完成。精确证据见[E-V1-WATER-WAVES-STOCK-OWNER](runtime-evidence-index.md#e-v1-water-waves-stock-owner)。

本页是系统级 current capability 的唯一摘要入口；专项表拥有逐项字段、边界、代码/测试证据和下一门。各专项沿用的 `L0-L4` 只在该专题自己的定义下解释：不同专项的同名 `L` 不可横向比较、求和、排序。`S0-S5` 只由[运行证据索引](runtime-evidence-index.md)定义，表达精确 code/App/fixture/ROI identity 的证据闭合深度，不表达能力宽度；`S0` 只是无已保存证据，不是优先级。任何 `L` 都不得批量映射为 `S`，一条 `S4` 也不得外推整个 family 已可见。需要系统结论时读取本页，需要逐项结论时沿本页链接进入对应专项；路线不得复制一份平行 current 表。

本页表格中的等级只概括本行写明的 bounded 范围与证据强度，不表示开发优先级或用户收益，也不能反向统一各专项的本地等级。真实样本已有缺图、错误合成、黑窗或交互失败时，先修该样本第一个共享断裂边，并把同一可见结果需要的 texture selection、owner、Program、GPU、publication、compositor 与 next-frame 闭合；一个 `L1/L2` 基础项或某行“下一门”不能取代这条链，也不能据此把相互依赖的 consumer 拆成多个“已完成”批次。

真实 corpus 的 authored occurrence、公共结构 family、参数 profile 和修复事件统一见[全样本能力分类与修复事件台账](scene-corpus-capability-inventory.md)。该 census 用于回答“样本声明了什么、某个修复事件影响哪些样本”；本页继续回答“当前实现到什么等级”。两者不可互相推导：静态高频 family 不是开发优先级，`resolved/observed/repair_state` 也不是运行支持。

R0-R5、B0-B25 与旧 Coverage-first 的逐批迁移过程不参与现役路线；需要追溯时只从[统一历史索引](../../history/README.md)进入。表中仍出现的旧标签只用于定位既有证据包，不能解释为下一批。

2026-08-16 的后续公共切片又把 exact current-pair leaf 的 launch-time `material-variant-envelope-color-contract` 收窄为 typed previous-current passthrough；派生真实内容从整层无执行恢复为 `Program → fallback → Program`、GraphExecutor `7/7/7` GPU 完成、terminal compositor 与 next-frame，末段截图与既有 `.35` suffix 基线同 hash。失败 effect 仍记为 typed CPU failure；sampler、dynamic/frame-time uniform、texture/resource/target/state/hazard/generation/runtime encode 不在该准入集合内。此切片不改变 owner migration 或 parity-release 的未完成状态。

同日下一公共切片只把 launch-time `material-variant-envelope-uniform-schema` 加入相同 exact leaf 门：malformed active uniform annotation 不再使整个 authored suffix 消失，失败 effect 保持 typed failed，previous-current copy 与后缀 Program 仍取得 GPU/publication/compositor/next-frame。two-node 同源反例继续在 graph admission 硬拒绝；sampler schema、动态 producer、frame-time uniform、resource/state/invariant/runtime encode 均未放宽。该切片不改变 owner migration 或 parity-release 的未完成状态。

随后 frame-time dynamic uniform 公共切片只放宽 `uniform/dynamicUniformBindingInvalid`：缺 dynamic target、producer/source mismatch、值无法编码或 authored-source 与 fallback 编码不一致时，exact single-current-pair leaf 可做 previous-current GPU copy并继续 suffix。隔离真实内容 `9000000550` 的正常门为 1/1 PASS；Debug-only frame fault 只在 frame 1 触发一次该 fallback，frame 2 恢复普通 Program，所有 7 个 transaction 均 terminal success，失败 effect 后的 suffix 与 compositor 同帧继续，正常/恢复截图同 SHA-256。严格 benchmark 因故意 typed CPU failure 把 fault 报告判为 NON-PASS；two-node App 反例在 launch `not-admitted/r5-no-runtime-owner` 且无伪 publication，同一错误码的 executor 层硬拒绝由 Metal harness 另行证明。route 仍是 `prefer-generic`，只达到 `slice-visible`；没有撤销旧 owner，也没有取得 Fast Suite、官方对照或 parity-release。

2026-08-21 的普通 FBO failure-radius 原子把同一 dynamic uniform 视觉失败扩到 exact 两material/一FBO 的单effect graph：第一node只能从effect input写普通nonunique、无clear/condition的FBO，第二node只能从该FBO（可另读effect input）写effect output；任一eligible frontend/uniform/optional texture失败都跳过整个effect、回滚已准备前缀并丢弃未提交target state，然后由GraphExecutor GPU复制previous current。自动Metal门同时覆盖现役RGBA与已准入R8 target合同；隔离真实`3749463715`资源树派生的`9000000800`可见运行只使用RGBA，正常门为1/1 PASS。frame 1故障注入形成`visual-failure-passthrough`、2 authored/0 executed/2 rejected、空mapping/target descriptor、GPU completion/publication/compositor，frame 2重建普通2 material/1 target graph并next-frame成功。故障报告因刻意CPU失败与rejected-node诊断严格保持0/1 NON-PASS；它只证明安全链，不是兼容PASS。route保持`prefer-generic`，没有撤权旧owner，也不开放clear/unique/command/history/dependency、任意FBO topology、Fast/fixed/full或官方parity；精确证据见[E-V1-ORDINARY-FBO-VISUAL-FAILURE-PASSTHROUGH](runtime-evidence-index.md#e-v1-ordinary-fbo-visual-failure-passthrough)。

同日后续普通 command-graph failure-radius cohort 复用同一GraphExecutor事务，把eligible边界扩到一个共享topology predicate：单effect、至少一个普通FBO，节点只能是ordinary material/copy/swap；所有FBO读取都由同effect更早的作者写入支配，无clear/unique/read-before-write history/condition/function/dependency/compose，material ordinal递增，所有target初始化且被消费，只有末node全帧输出。任一eligible visual preparation失败都会丢弃全部prepared material/copy/swap prefix及未提交mapping/resources，再复制effect-entry previous current；target/command/resource/ABI/generation/epoch/hazard/lifecycle与runtime encode错误不进入该门。项目Metal门闭合两material/一copy及未见`material→material→copy→swap→material`回滚，并让clear、unique、read-before-write history继续硬拒绝。隔离真实`3749463715`资源树派生的RGBA fixture `9000000810`正常门为1/1 PASS，2 material/2 copy、2 target、无history，首帧与next-frame均GPU/publication/compositor成功。frame 1 dynamic uniform故障形成typed effect-local passthrough、4 authored/0 material/0 copy/4 rejected、空mapping/target descriptor与安全GPU/compositor输出；frame 2重建2 material/2 copy并恢复。故障报告按严格执行合同保持0/1 NON-PASS；ready/after与正常门同SHA只证明安全构图未丢失。route仍为`prefer-generic`，无owner撤权；history lifecycle、condition/function、dependency/真实依赖子图失败、任意command/FBO topology、Fast/fixed/full与官方parity仍是偏差债务。精确证据见[E-V1-ORDINARY-FBO-COMMAND-VISUAL-FAILURE-PASSTHROUGH](runtime-evidence-index.md#e-v1-ordinary-fbo-command-visual-failure-passthrough)。

随后 dependency-subgraph failure-radius cohort 把一个更窄的运行时断点从整帧失败收敛到真实consumer分支：当 unified preparation 已为classic primary external dependency提交了同epoch、同provider/kind的reservation，但该provider当帧普通视觉capture没有publication时，runtime只把对应无history consumer transaction记为`external-primary-provider-capture-unavailable`，释放其allocation并从最后已composite tail重算尚未执行的独立transaction；不伪造texture、不把失败consumer记为成功，也不提交其publication/compositor。reservation缺失/漂移、frame epoch、provider/kind、publication object不匹配以及history-bearing transaction仍不能进入该局部门。两个独立provider→consumer分支的隔离fixture `9000000820`正常门1/1 PASS；Debug证据只按排序后的provider ordinal注入一次capture miss，不按sample/layer/path/hash选择产品算法。故障帧中consumer `70`局部失败，独立consumer `182`同一command buffer仍GPU completed/publication/compositor；下一帧provider capture恢复后两支都成功，性能采样为110 submitted/110 completed、0 failed且无GPU command-buffer failure。故障报告因预登记的typed failed observation与diagnostic严格保持0/1 NON-PASS；人工核对正向、故障ready与恢复after均为完整可见构图，只证明该fixture的安全连续性，不证明像素等价。classic primary named-provider route保持既有`generic-only / owner-migration-complete`，ordinary shader Program仍报告自己的`prefer-generic` profile；本批没有撤权或删除owner。effectful/nested provider、secondary/multiple dependency、history-bearing consumer、其他capture/resource/lifecycle失败、Fast/fixed/full与官方parity仍是偏差债务。精确证据见[E-V1-DEPENDENCY-SUBGRAPH-CAPTURE-FAILURE-CONTAINMENT](runtime-evidence-index.md#e-v1-dependency-subgraph-capture-failure-containment)。

随后 persistent-history executor-invalidation cohort 闭合的是完整性边界，不是普通视觉降级：共享 coordinator 在 invalidation 时先以 typed `executor-invalidation` 终止旧 reset generation 的未完成transaction、清空旧history residency并推进generation；同一 Program/GraphTargets/GraphExecutor 主链的首个恢复transaction不得把该原因降格为`initial`，必须以零history rehydrate、`history=seeded / reset=executor-invalidation`建立新epoch，下一transaction再进入`history-copy-on-write`。隔离真实`3749463715`资源树派生的单layer Motion Blur persistent graph正常门为1/1 PASS、390/390 terminal success与389次history COW；Debug-only时间触发不读取sample/layer/path/hash identity，失效时只让旧`r4:1:152`形成一个typed failed observation且没有GPU failure，约16 ms后的`r4:2:153`即完成新seed、GPU/publication/compositor，`r4:2:154`恢复COW。故障报告因这一预期完整性拒绝与diagnostic严格保持0/1 NON-PASS，123/122 submitted/completed、0 failed frame、0 drawable miss，ready/after仍为同一完整可见构图；surface在注入前后保持1并于stop降为0。该结果只达到`S4 slice-visible safety / bounded persistent-history executor-invalidation reset/recovery / prefer-generic`，不表示owner migration、device loss、scene switch、surface stop、seek/pause、任意history topology、Fast/fixed/full或官方时序/像素parity完成。精确证据见[E-V1-PERSISTENT-HISTORY-EXECUTOR-INVALIDATION-RECOVERY](runtime-evidence-index.md#e-v1-persistent-history-executor-invalidation-recovery)。

随后 persistent-history scene-switch rebuild cohort 继续闭合正式host生命周期，而不是新增第二套history或renderer：每个 `SceneResolvedMaterialSubmissionCoordinator` 现在创建与可替换texture pool解耦的runtime-instance identity，terminal graph observation携带该identity；benchmark按`runtime + layer`审计generation、mapping与history转换，并继续在单一runtime内严格拒绝generation回退，旧schema-1无runtime字段的历史日志只保留legacy单scope兼容。Debug-only时间触发调用现有正式`SceneDesktopWallpaperHost.launch`，不按内容identity选择route。隔离同输入重启的scene-switch门为1/1 PASS：surface `1→1`，两个runtime分别从`allocation/mapping generation=1 / history=seeded / reset=initial`进入下一transaction COW，两套physical pool UUID不相交；602/602 terminal success、600次COW，diagnostic/failed outcome/GPU failure均为0，最终stop为`1→0`。ready、switch-after与after均为完整可见构图，没有黑屏、整层或整帧丢失。该结果只达到`S4 slice-visible safety / bounded same-process persistent-history scene-switch rebuild / prefer-generic`；同输入重启不证明任意两个不同Scene间的作者语义、surface stop/device loss/seek/pause、多surface、owner migration、Fast/fixed/full或官方时序/像素parity。精确证据见[E-V1-PERSISTENT-HISTORY-SCENE-SWITCH-RECOVERY](runtime-evidence-index.md#e-v1-persistent-history-scene-switch-recovery)。

随后 persistent-history surface-stop/relaunch cohort 把原子替换与真正清空宿主分开验证：Debug-only时间触发只调用正式`SceneDesktopWallpaperHost.stop()`，确认旧surface runtime先以typed `.surfaceStop`失效、窗口与surface降为0、provider registry与launch context清空；0.25秒后再调用正式`launch`从空宿主重建，不新增renderer、pool、history registry或compositor owner，也不按内容identity选择route。隔离同输入正门为1/1 PASS：surface完整序列为`1→0→1→0`，旧/new runtime分别有181/411个成功atom，两者都从`allocation/mapping generation=1 / history=seeded / reset=initial`进入下一transaction COW，两套physical pool UUID不相交；592/592 terminal success、590次COW，diagnostic/failed outcome/GPU failure均为0。ready、relaunch-after与after均为完整可见构图；intentional stop到relaunch之间没有surface，本批不把该间隙表述成无黑屏连续性。该结果只达到`S4 slice-visible safety / bounded same-process persistent-history surface-stop/relaunch / prefer-generic`；不证明任意不同Scene作者语义、device loss、pause/seek、多surface、owner migration、Fast/fixed/full或官方时序/像素parity。精确证据见[E-V1-PERSISTENT-HISTORY-SURFACE-STOP-RELAUNCH](runtime-evidence-index.md#e-v1-persistent-history-surface-stop-relaunch)。

随后 persistent-history pause/resume retention cohort 复用正式全局播放控制，不拆除surface、runtime、pool、history或唯一compositor owner：pause冻结`SceneClock`并停止frame driver，resume恢复同一宿主。隔离正门为1/1 PASS：surface在before/paused/before-resume/resumed均保持1，clock pause状态为`false→true→true→false`，frame driver为`active→inactive→inactive→active`；pause后只允许一个已提交的in-flight transaction完成，此后约4.194秒没有新graph observation/transaction，resume首transaction沿同一runtime与physical pool把allocation/mapping generation从181推进到182并继续`history-copy-on-write`，没有initial seed、reset或第二owner。全程664/664 terminal success、663次COW，diagnostic/failed outcome/GPU failure均为0，resume后截图仍为完整可见烟雾构图。该结果只达到`S4 slice-visible safety / bounded persistent-history pause/resume retention / prefer-generic`；不证明暂停期视觉截图、系统focus/sleep/lock事件、seek/discontinuity、device loss、多surface、任意history topology、owner migration、Fast/fixed/full或官方时序/像素parity。精确证据见[E-V1-PERSISTENT-HISTORY-PAUSE-RESUME](runtime-evidence-index.md#e-v1-persistent-history-pause-resume)。

随后 persistent-history different-scene rebuild cohort 把同输入re-launch扩到“相同layer/effect/pass identity、不同authored state”的更强反例：Debug-only alternate-input只接受隔离root并仍调用正式`SceneDesktopWallpaperHost.launch`；第二Scene保留`533/557` identity与同一普通history topology，但改变camera、clear、layer alpha/color/transform和Motion Blur rate，不按这些值选择产品route。隔离正门为1/1 PASS：surface `1→1→0`，旧/new runtime分别有184/494个成功atom，各自从`allocation/mapping generation=1 / history=seeded / reset=initial`进入下一transaction COW，两套physical pool UUID不相交；合计678/678 terminal success、676次COW，diagnostic/failed outcome/GPU failure均为0。ready与switch-after分别是浅灰和深蓝背景的完整烟雾构图，证明新authored state实际进入产品链；没有切换瞬间截图，故不声称瞬时无黑屏。该结果只达到`S4 slice-visible safety / bounded same-identity different-authored-state scene-switch rebuild / prefer-generic`；不证明任意不同topology/provider/format/SceneScript/particle state、device loss、seek、多surface、owner migration、Fast/fixed/full或官方时序/像素parity。精确证据见[E-V1-PERSISTENT-HISTORY-DIFFERENT-SCENE-SWITCH](runtime-evidence-index.md#e-v1-persistent-history-different-scene-switch)。

随后 launch-time contributor policy 公共切片只把“同一 target 的多个 value contributor 全部有已证明 producer/target 且没有 control attachment”分类为 `material-dynamic-uniform-contributor-policy`；exact single-current-pair leaf 不选择或发明值，只复制 previous current 并继续 suffix。隔离 `9000000550` 以 fallback 1、generic 2 形成 `Program → fallback → Program`，frame 0/next-frame 六条 observation 均 GPU completed，末段 compositor 消费，截图与单 contributor 对照同 SHA-256；`9000000551` two-node 反例 accepted/claimed/encoded/GPU 均为0。删除第二 contributor 后 fallback 归零、generic 3且 targeted benchmark 1/1 PASS。零 contributor、缺 producer/target mismatch、任意 control、FBO/dependency/multi-node与生命周期错误继续硬拒绝；route 仍为`prefer-generic`且只达到`slice-visible`。

随后 launch-time control policy 公共切片只把“target、唯一 value contributor 与 producer 全部已证明，且 control attachment 精确为单个 `.unprovenSceneScript`”分类为 `material-dynamic-uniform-control-policy`；它不执行或解释未知 script，exact single-current-pair leaf 只复制 previous current 并继续 suffix。隔离 `9000000560` 为 fallback 1、generic 2，frame 0/next-frame 六条 observation 均 GPU completed，末段 compositor 两次消费；`9000000561` two-node 反例 accepted/claimed/encoded/GPU 均为0。删除 script 后 targeted 1/1 PASS、fallback 0、generic 3，Timeline 恢复普通 Program 并只在目标灰色 ROI 改变像素。零 contributor、缺失/mismatch producer、重复或混合 control、多 contributor 加 control、已知 control 配错 contributor、FBO/dependency/multi-node与生命周期错误继续硬拒绝。route 仍为`prefer-generic`且只达到`slice-visible`；该包冻结时的下一门是 launch-time `dynamic-uniform-unavailable` producer availability 分类，后续 producer-unavailable 包见下文，不是 SceneScript VM 或 control 语义支持。

随后 launch-time producer availability 公共切片只把“exact target、唯一 value contributor、control为空且对应producer在launch catalog中完全缺席”分类为`material-dynamic-uniform-producer-unavailable`；它不创建provider或动态值，exact single-current-pair leaf只复制previous current并继续suffix。隔离`9000000570`为fallback 1/generic 2，frame 0/next-frame六条observation均GPU completed且末段compositor两次消费；`9000000571` two-node反例accepted/claimed/encoded/GPU均为0。补回精确project property后`9000000572` targeted 1/1 PASS、fallback 0/generic 3，三个普通Program恢复，截图只在目标ROI改变65,792个像素。同user-property key异target、零contributor、multi-contributor缺producer、任何control、FBO/dependency/multi-node与生命周期错误继续硬拒绝。route仍为`prefer-generic`且只达到`slice-visible`；该包冻结时的下一门是zero-contributor control-only policy，后续中性 script-attachment 包见下文，不是通用provider或SceneScript VM支持。

随后 launch-time script-attachment 公共切片纠正旧 control-only 分类：未证明的 SceneScript 可能仍是 value producer，现役 Template/Program 因此使用 `DynamicUniformScriptAttachment`、`scriptAttachments`、`.unproven` 与 `uniformScriptAttachmentUnproven`。只有 exact target、零个已证明 value contributor、authored fallback 存在且 attachment 精确为单个 `.unproven` 时，exact single-current-pair leaf 才以 `material-dynamic-uniform-script-attachment-unproven` 复制 previous current 并继续 suffix；脚本不执行、不解释、不分类，fallback也不冒充动态值。隔离 `9000000580` 的旧 App 基线为accepted/claimed/encoded/GPU全0和全黑；当前App为fallback 1/generic 2，frame 0/next-frame六条transaction全部GPU completed且末段compositor两次消费。`9000000581` two-node反例继续全拒绝；删除script而保留authored value的`9000000582`为targeted 1/1 PASS、fallback 0/generic 3。正门和回滚截图字节相同，只证明owner/path恢复。route仍为`prefer-generic`且只达到`slice-visible`；真实脚本value/control/lifecycle目标合同仍归V2 VM。该包冻结时的下一门为multi-contributor missing-producer policy，后续公共切片见下段。

随后 launch-time multi-contributor producer availability 公共切片只处理“一个或全部 producer 真正缺席”：多个 value contributor、attachment 为空且没有同 key 异 target identity mismatch 时，exact single-current-pair leaf 以 `material-dynamic-uniform-contributor-producer-unavailable` 复制 previous current 并继续 suffix，不选择仍可用 contributor，也不合成缺失 producer 或值。隔离 `9000000590` 的批次前 App 为 accepted/claimed/encoded/GPU 全 0 和全黑，当前 App 为 fallback 1 / generic 2，frame 0/next-frame 六条 transaction 全部 GPU completed 并到达 terminal compositor；`9000000591` two-node 反例继续零执行。只保留一个 Timeline contributor 的 `9000000592` 为 targeted 1/1 PASS、fallback 0 / generic 3，三个普通 Program 恢复，目标 ROI 改变 65,792 个像素。同 key 异 target、任意 script attachment、multi-node/FBO/dependency 仍硬拒绝；route 仍为 `prefer-generic` 且只达到 `slice-visible`。该包冻结时的下一门是 static/host uniform binding provenance 分类；后续 static 与 host declaration 原子见下文。

随后 frame-time static uniform 公共切片把 Finalizer 的 static 与 host 编码失败分别登记为 `staticUniformBindingInvalid` / `hostUniformBindingInvalid`，但 exact single-current-pair leaf 只允许前者以 `material-finalizer-static-uniform-binding` 复制 previous current 并继续 suffix；它不为缺省或畸形 authored static 值选择默认值。隔离 `9000000600` 从批次前 `uniformBindingInvalid` 整图硬拒绝、claimed/encoded/GPU 全 0 和全黑，恢复为一个 typed failed effect 与前后两个普通 Program；frame 0/next-frame 六条 transaction 全部 GPU completed、末段 compositor 两次消费且截图非黑。two-node `9000000601` 保持 claim 0 / 全黑；显式 `alpha=0.8` 的 `9000000602` targeted 1/1 PASS、fallback 0、三个普通 Program 恢复，相对 passthrough 只在目标 bbox 改变 65,792 个像素。缺失/畸形 active schema、duplicate/mixed declaration、host uniform、multi-node/FBO/dependency、resource/target/state/lifecycle/runtime encode 继续硬拒绝。route 仍为 `prefer-generic` 且只达到 `slice-visible`；该包冻结时的下一门是 host binding provenance 子分类，后续结果见下段。

随后 host uniform provenance 公共切片把“作者 material 声明 host-owned field”分类为 `hostUniformDeclarationConflict`，与 `encodeHost` 的真实 frame/host invariant 失败 `hostUniformBindingInvalid` 分离；只有前者可在 exact single-current-pair leaf 以 `material-finalizer-host-uniform-declaration-conflict` 复制 previous current 并继续 suffix。隔离 `9000000610` 从批次前 claimed/encoded/GPU 全 0 与全黑恢复为一个 typed failed effect、前后两个普通 Program、frame 0/next-frame 六条 GPU completed transaction 和 terminal compositor；`9000000611` two-node 反例 accepted/claimed/encoded/GPU 均为 0 且全黑。删除 authored `g_Time=99` declaration 的 `9000000612` 为 targeted 1/1 PASS、fallback 0、三个普通 Program 恢复，正门与回滚 after 图逐像素相同。render/screen size、非有限 matrix/pointer、daytime/frametime、texture metadata、audio snapshot 等 host input 错误，宽泛 schema/declaration `uniformBindingInvalid`、multi-node/FBO/dependency 及 resource/target/state/lifecycle/runtime encode 继续硬拒绝。route 仍为 `prefer-generic` 且只达到 `slice-visible`；现役下一门是拆分宽泛 `uniformBindingInvalid` 的 active schema 缺失与 duplicate/mixed declaration provenance。

随后 non-host uniform declaration provenance 公共切片把 defensive active-schema 缺席分类为 `activeUniformSchemaMissing` 并保持硬拒绝，把同一 reflected field 同时匹配多个 authored key 分类为 `uniformDeclarationConflict`；只有后者可在 exact single-current-pair leaf 以 `material-finalizer-uniform-declaration-conflict` 复制 previous current 并继续 suffix，不从 alias/field declaration 中选择值。隔离 `9000000620` 从批次前 `uniformBindingInvalid`、claimed/encoded/GPU 全 0 与黑窗恢复为一个 typed failed effect、前后两个普通 Program、frame 0/next-frame 六条 GPU-completed transaction 和 terminal compositor；`9000000621` two-node 反例在公共 `graphBlocked` 门 accepted/claimed/encoded/GPU 均为 0 且全黑。删除 `u_Alpha` alias、只保留 `alpha=0.8` 的 `9000000622` 为 targeted 1/1 PASS、fallback 0、三个普通 Program 恢复，正门相对回滚只在目标 bbox 改变 65,792 个像素。`activeUniformSchemaMissing`、真正 host/frame input 错误、multi-node/FBO/dependency 及 resource/target/state/lifecycle/runtime encode 继续硬拒绝。route 仍为 `prefer-generic` 且只达到 `slice-visible`；该包冻结时的下一门是拆分宽泛 `activeSamplerSchemaInvalid`，后续现役结果见下段。

随后 authored sampler schema 公共切片新增 `authoredSamplerSchemaInvalid` 并把 variant-cache construction 改为 typed `launchValidated`，不再由 failable initializer 吞掉 provenance。只有作者 sampler annotation/declaration 或 `formatcombo` 无法形成 launch schema时，exact single-current-pair leaf才以`material-variant-envelope-sampler-schema`复制previous current并继续suffix；宽泛`activeSamplerSchemaInvalid`不在allowlist。隔离`9000000630`从批次前accepted/claimed/encoded/GPU全0与黑窗恢复为一个typed failed effect、前后两个普通Program、frame 0/next-frame六条GPU-completed transaction和terminal compositor；`9000000631`two-node反例在公共`graphBlocked`门保持零执行，harness另锁相同reason的multi-node硬拒绝。删除未知`mode`的`9000000632`为targeted 1/1 PASS、fallback 0、三个普通Program恢复，正门相对回滚只在目标bbox改变65,792个像素，证明没有执行未知mode shader。active `[PASS]`、internal target、binding order/slot/name、cross-variant divergence及frame/resource/target/state/lifecycle/runtime encode继续硬拒绝。route仍为`prefer-generic`且只达到`slice-visible`；其后的重复owner收敛见下段，不改变本切片的视觉边界。

随后 launch-envelope owner 收敛删除 Finalizer/TextureResolver 的隐式 cache 入口，并使 frame/product selection 只消费 launch 已冻结的 reachable sampler schema 与 compiled variants；旧 frame-time `reachable-sampler-schema` 解析、首次 variant compile 和对应宽泛误报已消失。所有 ready variant 若得到同一个 typed frame failure仍原样传播；未预编译 key、非唯一匹配与 launch/frame implicit framebuffer identity 漂移以 `identityInvariant` 硬拒绝且不增加 preparation/frontend/capacity 计数。真实 `3767460992` 批次前后保持 22 个 Program subject 与 7 层 GPU/compositor/next-frame 合同，targeted 1/1 PASS、ready/after 非黑、有运动且 `sample_root_residue=[]`；这只证明 owner consolidation/no-regression，不新增视觉能力。`RuntimeCatalog` 仍只为 launch demand discovery 读取 source reachability；该包冻结时的下一 V0 门是拆分剩余 compound `activeSamplerSchemaInvalid` provenance，后续现役结果见下段。

随后 sampler binding provenance 公共原子从当前产品 writer 删除 compound `activeSamplerSchemaInvalid`，分别登记 `samplerBindingOrderInvalid`、`samplerBindingDuplicateSlot`、`samplerBindingIdentityMismatch`、`samplerInternalTargetUnsupported`、`activePassUnsupported` 与 `samplerVariantSchemaDivergence`；前三类 binding invariant 和后三类 preparation invariant 均映射 `material-variant-envelope-invariant` 并继续硬拒绝，不进入 exact-leaf visual passthrough。launch compile、frame selection 与 texture resolver 共用同一 binding 验证，因此 duplicate slot 会在构造字典前返回 typed failure而非触发进程级 trap。真实 `3767460992` 保持 22 个 Program disposition、52 个稳定 transaction、7 层 GPU/compositor/next-frame 集合，targeted 1/1 PASS、非黑且有运动；只证明 typed provenance/no-regression，不新增视觉能力。route 仍为 `prefer-generic`，仍在 V0。下一门先核实既有隔离 authored `formatcombo` 候选是否真实触发 `samplerVariantSchemaDivergence`；若不命中，继续定位第一个真实 frame/resource/state blocker。active pass/internal target 留给 V1 ordered graph/target，不能因精确诊断而提前准入。

随后Chromatic公共可见原子完成V0首门：真实`2902406982`资源树派生的无脚本visual atom由普通Program实际产生GPU output，22/22预登记帧满足`R < 0 < G < B`；author-disabled control不执行，combo-0的未证明additive coordinate只对当前effect做previous-current passthrough并保留base/compositor/next-frame。该包只记`slice-visible`，route仍为`prefer-generic`；此前sampler divergence的“下一V0门”已封存为历史断点，但该失败本身仍hard reject。当前路线进入V1，不是Fast Suite、owner migration、官方对照或发行完成。

2026-08-17 的首个 V1 公共可见原子使一条普通 `material -> material -> copy -> unique history/FBO -> material` 作者链经同一 Program/GraphExecutor 执行：vertex source 只有实际引用 `g_ModelViewProjectionMatrix` 时才接收 target-pixel `a_Position`，否则接收 NDC/clip-space；公共 color transfer 允许两个或以上直接 sampled color 由 scalar `mix` 组合，并要求所有 slot 的 resolved color representation 完全一致。真实 `3749463715` 资源树派生的单 effect atom 形成 2 个 material node、1 个 copy node、2 个 target，291/291 graph observation terminal success，并到达 GPU completion、publication、terminal compositor 与 next-frame；author-disabled 不执行，非法 copy target 在 admission 前拒绝并保留原始 layer。该包冻结时只达到 `slice-visible`，两个 shader artifact 都以 `compiler-artifact-colortransfer` 回退到 `boundedSwift`；该历史断点已由 2026-08-19 的公共颜色合同 owner 收敛包替代。精确历史证据见 [E-V1-MATERIAL-COPY-HISTORY](runtime-evidence-index.md#e-v1-material-copy-history)。

2026-08-19 的 V1 generic shader 颜色合同原子删除了“作者源码 analyzer 与 SPIRV-Cross MSL spelling 各自决定 color transfer”的冲突：prepared authored source 能证明语义时，共享 `SceneAuthoredShaderColorTransferAnalyzer` 是唯一 material color fact，编译后端只提供语言翻译、reflection 与 MSL；artifact consumer 会拒绝与已证明源码事实矛盾的 carrier，源码暂不可证明的既有 compiler-proven 形态仍保持 fail-closed 后备。改名变量的未见双纹理 scalar-mix 正门进入 `interpolated-color`，vector weight、color mutation、未绑定 slot 与伪造 color fact 拒绝。随后两个独立 owner 批次分别把“不含外部 provider texture + source-proven scalar interpolation”与“active sampled slot + authored internal framebuffer identity + source-proven passthrough”切为默认 `generic-only`：artifact v3不再保存重复`routeState`，环境中的`prefer-generic`也不能重新授权bounded owner；compiler/artifact failure以typed `bounded-frontend-owner-revoked`拒绝，仅`disable-generic`允许回滚。第二批首次宽泛实现曾把layer-source/effect-output graph reference误纳入profile，既有launch envelope正门立即失败；现役边界已收窄为`SceneGraphTextureIdentity.kind == .framebuffer`，并以错误slot、外部provider与非framebuffer反例锁定。fresh signed App中Motion Blur的accumulation与combine node分别以`source-proven-scalar-color-interpolation`和`source-proven-graph-target-passthrough`进入`generic-only / genericCompilerArtifact`，正门481/481 terminal success并闭合GPU/publication/compositor/next-frame；显式`disable-generic`回滚476/476且after PNG与正门逐字节相同。只损坏combine artifact时，该profile以`artifact-contract-rejected / bounded-frontend-owner-revoked`在graph claim前局部关闭，旧owner不接管，安全中性compositor底图继续完成。两个profile均达到`owner-migration`；普通/provider-backed及其他shader profile仍有bounded fallback权，因此不是整个shader family`generic-only`、Motion Blur动态/官方时序完成、V1完成、Fast/fixed/full、官方parity或release。精确证据见[E-V1-GENERIC-SCALAR-INTERPOLATION](runtime-evidence-index.md#e-v1-generic-scalar-interpolation)。

2026-08-19 的下一枚 V1-B 公共 shader owner 原子补齐了 `source-proven-graph-input-straight-alpha-preserving` 的共享 lowering：prepared source 证明同一 typed graph slot 的多次 `float4` sample、alpha 明确来自采样 `.w`，artifact 在统一 straight-color/unpremultiply 与 compositor premultiply 边界执行；不依赖 effect/sample/path/hash。SPIRV-Cross 对音频标量数组生成的多余 `.x` 由反射的 `g_AudioSpectrum16|32|64{Left|Right}[N]` ABI 规则统一清除，动态 `g_AudioFrequencyMin..Max` loop 只有在匹配已声明 16/32/64 音频数组时计入有界 work budget。23 项 generic artifact/compiler 测试、code health、Developer ID Debug build 均通过。真实派生 `9000000620` fresh `generic-only` 报告 `/private/tmp/mwx-v1b-chromatic-runtime.Rjx3Rw/output4/report.json`（SHA-256 `08f3730d0a3ce4e79aa88aec4436a3a9aa40ad57f3326f7fc86ed965a17ea2f4`）为 1/1 PASS：admitted-generic、pipeline warmup ready=1、GraphExecutor frame 0/next-frame GPU completed、publication 与 compositor consumed；同一 exact profile 的 `disable-generic` 报告 `/private/tmp/mwx-v1b-chromatic-runtime.Rjx3Rw/output5/report.json`（SHA-256 `eefb9aa18d7c37e023d6447a503d213c2bf34277f75c68b34a4b351b550aa810`）记录 typed route-disabled fallback 且 PASS。此证据达到 `slice-visible + generic-only`；rollback 已演练，但本批尚未删除全局 dedicated effect 家族，也没有独立 ROI、未见改名组合的 fresh GPU 样本或官方 parity，因此不能宣称 V1-B 全部完成、Chromatic/Fisheye family parity、Fast/fixed/full 或 release。

2026-08-17 的第二个 V1 公共可见原子先让一条作者命名跨层依赖经 reservation 与 typed provider publication 进入普通 MaterialProgram slot，再由 GraphExecutor 到达 layer current/compositor。随后独立 owner 批次把 classic primary route 切为默认 `generic-only`：`BLENDMODE=5` 与新组合 `Opacity=1` 均为 Program 1、dedicated/fallback 0、GraphExecutor `7/7/7`，旧 Clipping planner、contract、execution plan、renderer 和专用测试 owner 已删除；`_b` 仍以 typed `unsupportedVariant` 零执行，非 unit authored constant（真实派生 `Opacity=0.5`）以 typed `unsupportedConsumer` 在 binding 前 fail closed。`MWX_SCENE_NAMED_PROVIDER_ROUTE=disable-generic` 只撤销该 generic binding，产生 typed consumer/provider issue，零 capture/binding/GraphExecutor 且保留安全 compositor output，不恢复旧链。该 bounded owner migration 不表示V1完成、Fast Suite PASS、普通shader compiler已`generic-only`、官方parity或release。精确证据见 [E-V1-CROSS-LAYER-NAMED-PROVIDER](runtime-evidence-index.md#e-v1-cross-layer-named-provider)。

随后 V1 primary named-provider multiple-consumer 原子证明同一个 hidden composition provider 可被两个可见 consumer 共享：真实 `2902406982` 资源树派生 fixture `9000000700` 由 provider `125` 发布 `_rt_imageLayerComposite_125_a`，consumer `70/71` 各自完成 slot-1 binding；provider capture 1 次、binding 2 次、GraphExecutor claim/encode/GPU `2/2/2`，首帧与 next-frame 均完成 publication/compositor。secondary `_b` 与 `disable-generic` 均零 graph 执行并保留安全输出。该 `slice-visible` 只覆盖单一 primary backward provider、两个 consumer、无 user override、blend 0、unit constants；secondary/multiple reference、多个 provider、cycle、nested/effectful provider、非 unit constants、owner migration、Fast Suite、官方 parity 与 release 仍未完成。精确证据见 [E-V1-PRIMARY-NAMED-PROVIDER-MULTIPLE-CONSUMERS](runtime-evidence-index.md#e-v1-primary-named-provider-multiple-consumers)。

随后 V1 two-provider atom 证明两个独立 hidden composition provider 可在同一 frame 各自服务一个普通 consumer：真实派生 fixture `9000000700` 的 provider `125/126` 分别发布 `_rt_imageLayerComposite_125_a/_126_a`，consumer `70/71` 各自完成 slot-1 binding；`utilityNamedTargetPlanned=2`、binding=2，两个 layer 的首帧与 next-frame 共 4 个 terminal graph observations 均 GPU/publication/compositor。缺失 provider `126` 的反门只拒绝 consumer `71`，consumer `70` 仍完成首帧与 next-frame；`MWX_SCENE_NAMED_PROVIDER_ROUTE=disable-generic` 则两个 consumer 零 graph 执行并保留安全输出。该 `slice-visible` 只覆盖两个单一 primary backward provider、各一个 consumer、无 user override、blend 0、unit constants；secondary、multiple reference、cycle、nested/effectful provider、非 unit constants、owner migration、Fast Suite、官方 parity 与 release 仍未完成。精确证据见 [E-V1-TWO-PRIMARY-PROVIDERS](runtime-evidence-index.md#e-v1-two-primary-providers)。

随后 V1 cycle 边界取得真实隔离负证：从同一 `9000000700` 派生 fixture 给 hidden provider `125` 增加反向 `dependencies:[70]`，与 consumer `70/71 -> 125` 形成环。运行时对两个 consumer 记录 `cyclicDependency`，named target planned/binding、capture 与 GraphExecutor 均为 0；`9000000700` 仍完成安全 compositor 帧，ready/after 图同为 `6235cfbba4ae0c84a16bcd0574ed24bccd94863a6bb58b537cb07f29d9216774`。该结果把 cycle 明确登记为硬拒绝边界，不代表 nested/effectful、secondary、multiple-reference 或多个 provider 已完成。

随后 V1 single-consumer multi-reference 原子证明同一 primary provider 可被同一 consumer 的两个普通 authored effect/pass 引用共享：`referenceSlots` 保留两个精确 effect/pass/slot identity，provider reservation/publication 仍只有一个 owner；正门和 secondary 反门、`disable-generic` 回滚均经隔离真实派生 fixture 闭合，见 [E-V1-SINGLE-CONSUMER-MULTI-REFERENCE](runtime-evidence-index.md#e-v1-single-consumer-multi-reference)。该 bounded profile 只覆盖 primary、slot 1、无 user override、blend 0、unit constants；nested/effectful provider、secondary、mismatch、其他 slot 与 owner migration 仍未开放。

2026-08-21 的后继 V1 nested/effectful provider 原子替代了上段“仍未开放”的旧边界，但只开放更窄的共享 closure：从一个可见 image root 沿 primary `_a` backward reference 反向遍历 hidden image provider，每个 effectful provider 必须恰有一个可由同一 dependency compiler 接受的上游 dependency，且整图 acyclic；最终 capability catalog 再级联撤销缺失上游的 orphan graph。隐藏 provider 的 base 不再被误当作 named output，普通 MaterialProgram/GraphExecutor final 先在同一 command order 中 blit 到独立 named reservation，再由现有 publication registry 提供给下一层；中间层 terminal observation 明确 `compositor=false`，只有最终可见 consumer 持有唯一 compositor。真实 `2419444134` 资源派生的三层 fixture `92 -> 104 -> 1104` 正门为 **1/1 PASS**：4 个 effect 全部 `admitted-generic/program`，capability `3/3/0`，GraphExecutor `21/21/21`、failure/local fallback 0，named capture `92/104`、binding `104/1104`、三个 layer GPU/next-frame闭合而 compositor 集合仅 `[1104]`。缺失最终 provider 的反例与 `MWX_SCENE_NAMED_PROVIDER_ROUTE=disable-generic` 回滚均为 **1/1 PASS**、capability `3/0/3`、GraphExecutor 0，并保留安全非黑 previous/current；回滚不恢复旧 owner。该结果只达到 `S4 slice-visible / representative-content / prefer-generic`，不开放 secondary、forward/cycle、child、non-image、multi-dependency/reference、history/resize/device-loss、更深混合链、Fast/fixed/full 或官方像素 parity；精确证据见 [E-V1-NESTED-EFFECTFUL-NAMED-PROVIDER](runtime-evidence-index.md#e-v1-nested-effectful-named-provider)。

2026-08-17 的第三个 V1 公共可见原子把 exact authored `uvs` absent-or-`"repeat"` 降为 typed clamp/repeat target descriptor，并贯穿 allocation/publication sampling identity、`.scalarRedUnorm` Program output 与 direct-red consumer。隔离真实 `3765760121:73` 在属性覆盖下形成两个 ordinary material node 和固定 `256x256 r8` repeat target，经 GraphExecutor 到达 GPU completion、terminal compositor 与 next-frame；预声明 `x=0.30...0.82 / y=0.12...0.78` ROI 呈局部、空间非均匀的周期变化。后续独立 owner 批次让 prepare node 以 `source-proven-opaque-scalar-output`、combine node 以 `source-proven-straight-alpha-r8-signal` 两个非样本 typed profile 默认进入 `generic-only / genericCompilerArtifact`；失败不再回到 bounded frontend，只有显式 `disable-generic` 可原子回滚。该 bounded profile 达到 `owner-migration`，但不表示 Fast Suite、arbitrary R8/Glitter、整个 shader family、官方 parity、V1 完成或 release。精确证据见 [E-V1-FBO-UV-ADDRESS-REPEAT](runtime-evidence-index.md#e-v1-fbo-uv-address-repeat)。

2026-08-17 的第四个 V1 公共可见原子开放一个严格的 layer-local full-frame compose 结构：单一 effect 至少两个 ordinary material node、0 authored FBO/command/history/background/dependency/condition，所有 node 都从 effect input 读取并写 effect output，所有非末 node 为 boolean `compose:true`，末 node 为 nil/false。项目自有 contract representative 由两个非 `dedicated:` Program 经同一 GraphExecutor 轮转 full-frame pair，首帧与 next-frame 都完成 GPU、exact effect-output publication 和 terminal compositor；相对同资源 no-effect control，`2640x1964` 内容 ROI 的像素差异比例为 `1.0`，最终稳定为作者 Program 写出的 `(64,64,64)`。首/次 Program preparation failure 的 Metal 反门都丢弃未编码 prefix、恢复 stage-entry previous current。2026-08-20 又把同一shape的launch-time visual contract/dynamic-owner失败归为whole-effect passthrough，capability继续保留同层suffix；带history/FBO的同构反例仍在claim前硬拒绝。resource/target/dependency/runtime encode/state/lifecycle继续硬拒绝。2026-08-21 对 corpus 中仅有的 `compose:true` revision 完成 owner migration：公共 `source-proven-normalized-sample-sum` 只接受单一输出 helper 返回同一 texture slot 的 `1...32` 个正 literal 权重 sample、权重和为 1、无分支/隐藏 sample/混槽，并默认 `generic-only`。真实 `2802243144` 的三个 Precise identity 全部进入 MaterialProgram/GraphExecutor，旧 raw-compose planner topology、launch allowlist、ProgramFirst fallback 与 dedicated preparation/renderer 支路已删除；显式 `disable-generic` 只回滚 shared `boundedSwift`，不复活旧 owner。`2067939514` fresh probe 仍被既有 SceneScript target conflict 在 launch 前阻断，不能作为本批正证。该 bounded owner migration 不是整个 Precise Blur、compose family、Refraction/scene-background capture、Fast Suite、官方 parity 或 release。精确证据见 [E-V1-GENERIC-LAYER-LOCAL-COMPOSE](runtime-evidence-index.md#e-v1-generic-layer-local-compose)。

同日的显式 authored-intermediate/FBO owner cohort 把同一公共 source proof 扩到一个精确 immutable vec4/float4 局部 alias，并在同 effect 两个 material 间以 typed inactive provider 传递 sibling-only combo schema；当前 material 显式 combo 始终覆盖 inherited inactive，未知 provider 仍拒绝。MaterialProgram 的 texture selection 只有在整个 reachable launch envelope 都无 active sampler 时才忽略候选，任何可达 sampler 仍保留 readiness、purpose、format、publication 与 binding 硬门。当前 corpus snapshot 中三个byte-equivalent Workshop definition共有35个visible authored refs；最终签名App下，真实`3765760121:68/76/82`的`effect input -> full RGBA FBO -> effect output`两pass均为`generic-only / Program`并闭合首帧与next-frame GPU/publication/compositor，`disable-generic`只回滚shared `boundedSwift`。`.preciseGaussian`已从产品ordered compiler撤权，旧planner仅保留无产品执行权的direct-test oracle。两个相邻真实definition的10个visible Precise layer也都进入Program，但整份双样本报告因既有matrix、其他layer/particle断点为0/2，不能称suite PASS。该owner migration不开放真实`MASK`、`BLURALPHA`、dynamic scale、其他FBO/command/history、官方像素parity或V1整体完成。精确证据见[E-V1-PRECISE-AUTHORED-INTERMEDIATE-OWNER](runtime-evidence-index.md#e-v1-precise-authored-intermediate-owner)。

2026-08-17 的第五个 V1 公共可见原子把 exact case-insensitive `_rt_FullFrameBuffer` 降为 renderer-owned `sceneBackground` provider：它表示同帧中受影响 layer 之前的 main target，与 layer source、layer-local current、effect FBO/history、named cross-layer publication 和 final compositor output 分离。当前只接受单 effect、exactly two ordinary material、首 node slot 1 且 `compose:true`、次 node compose nil/false、0 authored FBO/blocker、一次 compose transition、两次 full-frame write、同 pair member in/out、dependency owner `.none` 的 image/solid/text captured-layer shape；资源必须保持 consumer layer、frame epoch、purpose、extent、UV、sampler、premultiplied content 与 texture-object identity。隔离 contract representative `9000000003:38` 由两个普通 Program 读取 same-frame main target，GraphExecutor `7/7/7`、frame 0/next-frame GPU completion、effect-output publication 和 terminal compositor 全部闭合；正门相对 no-effect control 改变 5,220,312 像素（87.896825%），三 node 反例以 typed `scene-background-compose-shape` 零 claim/encode/GPU 并回滚到与 control 完全相同的 PNG。route 仍为 `prefer-generic`，只达到 `slice-visible / contract-representative`；不是 stock Refraction、normal/mask 语义、其他 background/dependency compose、owner migration、Fast Suite、官方 parity 或 release。精确证据见 [E-V1-SCENE-BACKGROUND-COMPOSE](runtime-evidence-index.md#e-v1-scene-background-compose)。

2026-08-17 的第六个 V1 公共可见原子让两个完全同 descriptor 的 declared FBO 执行 authored `swap`：command 只交换 logical identity mapping，不复制像素、不推进 full-frame current，后续 ordinary material 读取交换后的 identity。项目自有正例相对 no-swap control 使整个 `3024x1964` frame 从 `(229,13,13)` 变为 `(13,26,229)`，两路均闭合Program、GraphExecutor、GPU completion、exact effect-output publication、terminal compositor与next-frame。launch admission现按author extent expression及format/address/unique/clear检查swap compatibility，不再让`1x1` probe把不同scale钳成同一pixel extent；extent/format/unique/clear任一不匹配均以typed `swap-target-descriptor-incompatible`在claim、allocation、encode与publication前硬拒绝，layer-source passthrough与author-disabled base逐字节、逐像素相同。route仍为`prefer-generic`，只达到`slice-visible / contract-representative`；不表示跨帧mapping/history、Fluid condition/function/float format、V1完成、owner migration、Fast Suite、fixed/full、官方parity或release。精确证据见[E-V1-SAME-DESCRIPTOR-SWAP](runtime-evidence-index.md#e-v1-same-descriptor-swap)。

2026-08-18 的第七个 V1 公共可见原子只把 exact authored shader schema 中稳定的 `[COMBO] default:0` 投影为缺失 graph condition key 的 implicit-zero evidence；material/instance 显式 combo 继续优先，nonzero、readiness、format、disabled、branch-only、条件冲突、malformed、identity mismatch与audit overflow均失败关闭。false target/pass/binding在allocation、Program preparation、encode与publication前剪枝。项目自有缺省零与显式一正例分别得到红/蓝全帧、GraphExecutor `7/7/7`、GPU/publication/compositor/next-frame；unproven-key反例为零claim/encode/GPU，204/204 frame完成且rollback截图与author-disabled control字节相同。真实Fluid相邻探针仍为NON-PASS，但condition evidence已证明`LIGHTING/LINEEMITTER/OPAQUE/PERSPECTIVE/RENDERING`，当前首断点推进到typed `function-invocation-unavailable`。route仍为`prefer-generic`，证据只到`slice-visible / contract-representative`；不是Fluid、V1、owner migration、官方parity或release完成。精确证据见[E-V1-SHADER-DEFAULT-ZERO-CONDITION](runtime-evidence-index.md#e-v1-shader-default-zero-condition)。

2026-08-18 的第八个 V1 atom 将 definition `functions` 从 admission blocker 收敛为共享 GraphExecutor 的显式 typed request：请求必须匹配 frame epoch、effect identity、唯一 registry function 与当前 lease；clear 在 history rehydrate 后、ordinary graph intent 前，按 function invocation 与 target authored order（包括重复项）推进同一 logical target 的 content generation，并通过现有 publication/rollback 事务交给后续 material。无 request 时 registry 不自动执行；unknown effect/name、stale epoch、target/lease mismatch 和 encode-abandon 分别在 clear/encode/publication 前拒绝或丢弃未提交 command。项目自有 Metal harness 的 control 像素为蓝色、显式 clear 后后续 material 读到透明像素，重复调用产生 4 个有序 clear intent；21 个 focused tests 全部通过。随后 bounded QuickJS-NG scalar owner 已把作者 mutation buffer 接入同一 frame context；shared domain 的多个 scalar owner 现在在 callback 边界动态绑定各自的 `thisLayer` host，双 owner 交叉反例证明 mutation 不串 layer。隔离真实 fixture `9000000800` 的 positive 记录 `SceneScript VM` 的 `clearHistory` mutation 并使 layer `17/100` 完成，`9000000801/0802/0803` 的 unknown-function、no-call、invalid-effect 只在 layer `17` 产生声明内 local fallback，layer `100` 仍闭合 graph/compositor/next-frame。当前最高为 `slice-visible / contract-representative / prefer-generic`，不表示通用 `IEffect` API、Fluid、V1、owner migration、官方 parity 或 release 完成。精确证据见[E-V1-MATERIAL-FUNCTION-INVOCATION](runtime-evidence-index.md#e-v1-material-function-invocation)与[E-V2-SCENESCRIPT-MATERIAL-FUNCTION-LOCAL-FALLBACK](runtime-evidence-index.md#e-v2-scenescript-material-function-local-fallback)。

2026-08-18 的第九个 V1 公共可见原子把显式 typed material/instance condition provider 接入 generic graph admission：false target/pass/binding 在 target allocation、Program preparation、encode 与 publication 前剪枝，raw planner 仍保留 `unsupportedCondition` 作为已处理的 admission-phase diagnostic。隔离真实 `3768724269` 派生 fixture 的正门在 `layer=100` 形成 2 material node + 1 compose node，反门剪掉 false 首节点后只剩 1 material node；两路 frame 0/next-frame 均 GPU completed、publication generation、terminal compositor consume、next-frame 与 teardown 闭合，`sample_root_residue=[]`。该结果达到 `slice-visible / representative-content / prefer-generic`，不是 generic-only、owner migration、Fast Suite、V1 整体或官方 parity；剩余 condition/function/provider、conditional FBO/history/dependency 与其他 graph failure 边界继续登记在专项表和运行证据中。精确身份见 [E-V1-EXPLICIT-CONDITION-PRUNING](runtime-evidence-index.md#e-v1-explicit-condition-pruning)。

随后一个 V1 公共可见原子把同一真实 Motion Blur authored graph 的单 copy 扩展为两个有序 copy command：2 material + 2 copy 节点沿 `SceneGraphNodeScheduler`/`SceneGraphCommandRuntime` 执行，416 个 terminal graph observations 均 GPU/publication/compositor/next-frame 成功；未声明第二 target 的反门在 admission 前以 `unified-capability-unavailable` 关闭，claim/encode/GPU 为 0，安全 layer-source passthrough 继续完成。该结果达到 `slice-visible / representative-content / prefer-generic`，不升级 arbitrary multi-copy、跨 effect/lifecycle history、owner migration、Fast Suite、V1 整体或官方 parity。精确身份见 [E-V1-ORDERED-MULTI-COPY-COMMAND-CHAIN](runtime-evidence-index.md#e-v1-ordered-multi-copy-command-chain)。

随后一个 V1 公共可见原子把同一真实 Motion Blur authored graph 扩展为一个 copy 后一个 swap 的混合 command chain：2 material + 1 copy + 1 swap 节点沿同一 scheduler/target-plan/GraphExecutor 执行，415 个 terminal graph observations 均 GPU/publication/compositor/next-frame 成功；未声明 swap target 的反门在 admission 前以 `unclaimed-effect-product-authority` 关闭，graph claim/encode/GPU 为 0，安全 layer-source passthrough 继续完成。两个 target 的 descriptor（extent/format/address/unique/clear）在正门中保持完全一致；该结果达到 `slice-visible / representative-content / prefer-generic`，不升级 arbitrary mixed command、跨帧 mapping/history lifecycle、跨 effect dependency、owner migration、Fast Suite、V1 整体或官方 parity。精确身份见 [E-V1-MIXED-COPY-SWAP-COMMAND-CHAIN](runtime-evidence-index.md#e-v1-mixed-copy-swap-command-chain)。

随后一个 V1 公共可见原子把同一 mixed chain 的 swap node 接入显式 integer combo 条件：`MIXED=1` 保留 `2 material + 1 copy + 1 swap`，415/415 terminal observations 成功；`MIXED=0` 在 target allocation/encode 前只剪掉 swap，保留合法 `2 material + 1 copy`，423/423 terminal observations 同样 GPU/publication/compositor/next-frame 成功。两路均无 graph diagnostic、failed outcome、failed frame 或 drawable miss，`sample_root_residue=[]`。该结果达到 `slice-visible / representative-content / prefer-generic`，不升级 conditional FBO/binding/function/dependency、任意 condition provider/expression、跨帧 lifecycle、owner migration、Fast Suite、V1 整体或官方 parity。精确身份见 [E-V1-CONDITIONED-MIXED-COMMAND-PRUNING](runtime-evidence-index.md#e-v1-conditioned-mixed-command-pruning)。

随后一个 V1 公共可见原子闭合相反的 mixed command 顺序：真实 Motion Blur 派生 graph 先由首 material seed `_rt_FullCompoBuffer1`，再按作者顺序执行 swap 后 copy，末 material 读取 copy 后仍保存可见内容的 logical target。2 material + 1 swap + 1 copy 在 430/430 个 terminal graph observations 中均 GPU/publication/compositor/next-frame 成功，fresh Metal ready/after 保持非均匀完整纹理；把 swap target 改成未声明 identity 后在 admission 前关闭，graph claim/encode/GPU 与 observation 均为 0，110/110 frame 仍完成并保留安全 layer-source 输出。该结果达到 `slice-visible / representative-content / prefer-generic`，只授权这一 seeded、descriptor-identical、单 effect 顺序；不升级 arbitrary target seed/mixed command、跨帧 mapping/history lifecycle、dependency family、owner migration、Fast Suite、V1 整体或官方 parity。精确身份见 [E-V1-MIXED-SWAP-COPY-COMMAND-CHAIN](runtime-evidence-index.md#e-v1-mixed-swap-copy-command-chain)。

2026-08-18 的第十个 V1 公共可见原子把普通 RGBA FBO 的 authored `clear` 从“只保真、固定透明清屏”推进到共享 GraphExecutor 初始化：四个有限 `[0,1]` 分量（string/array 两种作者写法）进入 typed target descriptor、initialization command、publication 与后续 material read，仍沿同一 target allocation/rollback/compositor 链执行。真实 authored Motion Blur 资源树派生 fixture 只把 `_rt_FullCompoBuffer1` 的 clear 改为 `1 0 0 1`，2 material + 2 copy 正门达到 303/303 terminal observations、GraphExecutor claim/encode/GPU `1/1/1`、GPU/publication/compositor/next-frame，ready/after 非黑；同资源 disabled control 的 after 图与正门 RGB 均值绝对差约 `3.56`、RGB changed ratio `1.0`，证明 clear 不是仅结构通过。非法数值/长度/越界仍在 target admission 失败；R8、条件 clear、其他格式与 reset/resize/跨帧 load/store 仍关闭。未声明第二 copy 的反门在 admission 前零 claim/encode/GPU，`unclaimed-effect-product-authority` 只影响该 effect，安全帧继续完成。该包达到 `slice-visible / representative-content / prefer-generic`，不表示任意 clear、完整 persistent lifecycle、secondary/nested provider、V1 完成、owner migration、官方 parity 或 release。精确证据见 [E-V1-AUTHORED-RGBA-CLEAR](runtime-evidence-index.md#e-v1-authored-rgba-clear)。

随后同一 authored RGBA clear 原子补充了 DEBUG evidence-window 的 surface resize/restore stress：`1.5:0.6,3.5:1.0` 两个请求均被接受，正门窗口快照依次为 `3024x1964 → 1816x1180 → 3024x1964 → 3024x1964`，clear 后的 ready/resize-01/after 图逐像素一致；正门报告为 1/1 PASS，406/406 graph observations 均 `succeeded`、GPU completed、publication、terminal compositor 与 next-frame 存在。相同序列的非法第二-copy 反门也为 1/1 PASS（显式声明 `unclaimed-effect-product-authority` local fallback），admission accepted/claim/encode/GPU 均为 `0`，两次 resize 与恢复帧仍保留安全底图。graph 含 history rehydrate，allocator 每帧产生新 allocation generation；后续 extent atom 已把容易误解为窗口事件的旧 `resize-reprepare` 更名为 `allocation-reprepare`，两者都不能解释为“仅两次 resize allocation”。本批因此只把 surface resize/restore 记为 `slice-visible` stress，不升级 authored target extent、load/store/reset/seek/stop、跨 surface 或完整 persistent lifecycle。精确身份见 [E-V1-AUTHORED-RGBA-CLEAR-SURFACE-RESIZE](runtime-evidence-index.md#e-v1-authored-rgba-clear-surface-resize)。

随后一个 V1 公共可见原子把真实 Motion Blur authored RGBA FBO 的 `scale:2` 从 loss-preserving IR 闭合到实际 graph extent、allocation/publication 与可聚合 descriptor telemetry：3840x2160 内容按现役 full-frame policy 形成 `2048x1152` graph input，两张 target 均解析为 `1024x576/rgbaBackbuffer`；2 material + 3 copy chain 在当前 build 上取得 412/412 terminal success、GPU completion、publication、terminal compositor 与 next-frame，resize/restore 前后 descriptor hash/count 保持一致。`scale:2` 对 `scale:1` 的 copy-connected 反例在 `1x1` launch probe 中仍会同时 clamp 为 `1x1`，但实际 frame plan 以公共 storage contract 返回 typed `frame-target-plan-unsupported-target-descriptor`，只对 layer 533 安装 local fallback；executor claim/encode/GPU 为 `0/0/0`，frame command buffer 和安全 layer-source 输出继续完成。未见组合门另覆盖 `scale:4`、absolute `640x480`、`width+fit+scale`、`scale:0.5` 与 minimum clamp，但这些 harness 不升级 arbitrary fit/absolute/composed extent。route 仍为 `prefer-generic`，最高只到 `slice-visible / bounded authored FBO scale-extent chain`；不表示 owner migration、`generic-only`、persistent load/store/reset/seek/stop、multi-surface/hot-plug、Fast/fixed/full、官方 parity 或 release。精确身份见 [E-V1-AUTHORED-FBO-SCALE-EXTENT](runtime-evidence-index.md#e-v1-authored-fbo-scale-extent)。

紧接的 V1 可见原子不新增产品 owner，而是把现有公共 `fit` resolver 从 contract harness 闭合到 actual allocation 与画面：同一真实 Motion Blur 资源树派生 graph 的两张 RGBA target 改用 corpus 已观察到的 authored `fit:256` spelling，`2048x1152` graph input 按保持比例且只缩小的合同解析为两张 `256x144/rgbaBackbuffer` target；2 material + 3 copy chain 取得 415/415 terminal success、GPU completion、publication、terminal compositor 与 next-frame，resize/restore 前后 descriptor hash/count 稳定。正门 after 图相对 `fit:256`/`fit:128` 不兼容反门的安全 layer-source 底图，RGB 平均绝对差约 `11.18/14.08/11.14`、阈值 `>2` 的 changed ratio 为 `1.0`。反门在 `1x1` probe 仍会同时 clamp 为 `1x1`，actual frame 才以 typed `frame-target-plan-unsupported-target-descriptor` 局部降级，claim/encode/GPU 为 `0/0/0`、failed frame 为 `0`。该批的公共 unseen 门曾只在 harness 证明 `fit:256 + scale:2` 得到 `128x72`；现役真实闭环见下一段。route 保持 `prefer-generic`，最高为 `slice-visible / bounded authored FBO fit-extent chain`；该批本身不开放 arbitrary fit、absolute/composed extent、Fluid、owner migration、persistent lifecycle、Fast/fixed/full、官方 parity 或 release。精确身份见 [E-V1-AUTHORED-FBO-FIT-EXTENT](runtime-evidence-index.md#e-v1-authored-fbo-fit-extent)。

随后一个 V1 可见原子把上述 `fit:256 + scale:2` 从 unseen harness 闭合到 authored parser、actual allocation 与画面：同一 2 material + 3 copy representative 的两张 RGBA target 先按 `2048x1152` input 做不放大的 `fit:256`，再除以 `scale:2`，形成 `128x72/rgbaBackbuffer:2`；当前签名 App 的正门取得 483/483 terminal success、GPU completion、exact publication、terminal compositor 与 next-frame。第二张 target 改为 `fit:256 + scale:1` 时，`1x1` probe 仍会同为 `1x1`，actual frame 才以 typed `frame-target-plan-unsupported-target-descriptor` 局部拒绝，claim/encode/GPU 为 `0/0/0`、local fallback 为 `1`、failed frame 为 `0`。恢复既有 fit-only 输入后重新得到 `256x144/rgbaBackbuffer:2` 与 483/483 terminal success。正门 after 相对安全负门的 RGB 平均绝对差约 `11.16/14.18/11.22`，阈值 `>2` changed ratio 为 `1.0`；正门与 fit-only 回滚也存在可见但较小的 resolution 差异，不能外推为 fidelity 改善。route 仍为 `prefer-generic`，最高为 `slice-visible / bounded authored FBO fit-then-scale extent chain`；不开放 arbitrary composed extent、单轴/absolute 组合、其他 format/topology、owner migration、Fast/fixed/full、官方 parity 或 release。精确身份见 [E-V1-AUTHORED-FBO-FIT-SCALE-EXTENT](runtime-evidence-index.md#e-v1-authored-fbo-fit-scale-extent)。

下一个 V1 可见原子继续复用同一公共链，把 corpus 已观察到的双轴 authored `width`/`height` spelling 闭合到 RGBA actual allocation：Motion Blur 资源树派生 graph 的两张 target 均声明 `width:640 / height:360`，不依赖 `2048x1152` graph input 的比例计算，2 material + 3 copy chain 取得 412/412 terminal success、GPU completion、publication、terminal compositor 与 next-frame；所有 observation 的 descriptor 均为 `640x360/rgbaBackbuffer:2` 且 resize/restore 前后 hash/count 稳定。把第二张 copy-connected target 改为 `320x180` 后，公共 plan 在 `1x1` 与 actual frame 都以 typed `frame-target-plan-unsupported-target-descriptor` 拒绝不兼容 storage；产品 capability 仍被观察为 accepted，但 claim/encode/GPU 为 `0/0/0`、local fallback 为 `1`、failed frame 为 `0`，安全 layer-source 底图继续完成。正门 after 相对反门的 RGB 平均绝对差约 `11.28/14.09/11.15`，阈值 `>2` changed ratio 为 `1.0`。unseen 门只在 harness 证明单轴 `width:640` 会保留另一输入轴为 `1152`。route 仍为 `prefer-generic`，最高为 `slice-visible / bounded authored FBO absolute-extent chain`；不开放 arbitrary absolute、single-axis/composed extent、Fluid、owner migration、persistent lifecycle、Fast/fixed/full、官方 parity 或 release。精确身份见 [E-V1-AUTHORED-FBO-ABSOLUTE-EXTENT](runtime-evidence-index.md#e-v1-authored-fbo-absolute-extent)。

紧接的 V1 可见原子把上述单轴 `width:640` 从 unseen harness 闭合到 actual allocation 与画面：同一 ordinary 2-material/3-copy representative 的两张 RGBA target 只覆盖 input width，保留 `2048x1152` input height，形成 `640x1152/rgbaBackbuffer:2`；签名 App 的正门取得 479/479 terminal success、GPU completion、exact publication、terminal compositor 与 next-frame。第二张 target 改为 `width:320` 时，公共 descriptor contract 以 typed `frame-target-plan-unsupported-target-descriptor` 局部拒绝，claim/encode/GPU 为 `0/0/0`、local fallback 为 `1`、237/237 frame 完成且 failed frame 为 `0`。恢复双轴 `640x360` 后重新取得 497/497 terminal success。正门 after 相对安全负门的 RGB 平均绝对差约 `11.29/14.06/11.12`，阈值 `>2` changed ratio 为 `1.0`；正门与双轴回滚也有较小可见差异，不能外推为 fidelity 改善。route 仍为 `prefer-generic`，最高为 `slice-visible / bounded authored FBO single-axis extent chain`；不开放 arbitrary single-axis/composed extent、height-only、其他 format/topology、owner migration、Fast/fixed/full、官方 parity 或 release。精确身份见 [E-V1-AUTHORED-FBO-SINGLE-AXIS-EXTENT](runtime-evidence-index.md#e-v1-authored-fbo-single-axis-extent)。

2026-08-21 的 non-enlarging extent cohort 继续复用同一 Motion Blur 资源与唯一 target/allocation/publication/compositor owner，把三个共享风险一致的 unseen shape 一次闭合到 actual App：两张 `height:360` target保留`2048`输入宽度并形成`2048x360`；`scale:4`形成`512x288`；`width:1000 + fit:600 + scale:2`按 axis override → fit → scale 顺序形成`260x300`。三正门合并报告为 **3/3 PASS**，各自只有一个 shared graph claim/encode/GPU owner，terminal observations为`431/421/432`，GPU completion、publication、terminal compositor与next-frame均属于layer 533；人工检查三张after均保留完整非黑烟雾构图。三个只改变第二target的descriptor mismatch反门均以`frame-target-plan-unsupported-target-descriptor`在unsafe encode前局部拒绝，claim/encode/GPU为`0/0/0`、local fallback为`1`，failed frame/drawable miss为0且安全after同SHA-256。独立公共测试另锁定三种actual extent与同样的storage rejection，没有增长既有超长测试owner。route保持`prefer-generic`；`scale:0.5`放大、min-1、其他absolute/composed组合、动态fidelity、owner migration、Fast/fixed/full、官方parity与release仍未闭合。精确证据见[E-V1-AUTHORED-FBO-NONENLARGING-EXTENTS](runtime-evidence-index.md#e-v1-authored-fbo-nonenlarging-extents)。

紧接的独立 upscale atom 把风险不同的 `scale:0.5` 放大从 resolver harness 闭合到 actual target budget、persistent history 与可见输出：同一隔离 ordinary Motion Blur graph 的 `2048x1152` input 形成两张 `4096x2304/rgbaBackbuffer` target，逻辑存储为 `75,497,472` bytes，仍在现役 128 MiB transaction contract 内。正门为 **1/1 PASS**，GraphExecutor claim/encode/GPU 为 `1/1/1`、421/421 terminal success、420 次 history copy-on-write，GPU completion、publication、terminal compositor 与 next-frame 均属于 layer 533；205/204 submitted/completed frame、failed frame/drawable miss 为 0，人工检查 ready/after 均为完整非黑构图且局部动态保留。只把第二 target 恢复为 `scale:1` 的反门按预登记执行要求为 **0/1 NON-PASS**，以 typed `frame-target-plan-unsupported-target-descriptor` 在 claim/encode/GPU 前局部拒绝，local fallback 为 1 且安全 compositor 继续。route 保持 `prefer-generic`；min-1、其他 arbitrary extent/format/topology/lifecycle、owner migration、Fast/fixed/full、官方 parity 与 release 仍未闭合。精确证据见 [E-V1-AUTHORED-FBO-UPSCALE-EXTENT](runtime-evidence-index.md#e-v1-authored-fbo-upscale-extent)。

随后独立 minimum-extent atom 把同一公共 resolver 的 floor/min-1 边界从 harness 闭合到 actual allocation/history/publication：同一隔离 ordinary Motion Blur graph 的两张 target 均声明 `scale:10000`，`2048x1152` actual input 在除法与向下取整后钳为 `1x1/rgbaBackbuffer:2`。正门为 **1/1 PASS**，GraphExecutor claim/encode/GPU 为 `1/1/1`、365/365 terminal success、364 次 history copy-on-write，GPU completion、publication、terminal compositor 与 next-frame 均属于 layer 533；205/205 submitted/completed frame、failed frame/drawable miss 为 0，人工只确认完整非黑构图与局部动态存活。第二 target 改为 `scale:1000` 时，launch `1x1` probe 仍把两者都钳到 `1x1`，但 actual frame 形成 `1x1` 对 `2x1`；反门按执行要求为预期 **0/1 NON-PASS**，以 typed `frame-target-plan-unsupported-target-descriptor` 在 claim/encode/GPU 前局部拒绝，local fallback 为 1 且 205/205 安全 frame 继续完成。route 保持 `prefer-generic`；其他 arbitrary extent/format/topology/lifecycle、owner migration、Fast/fixed/full、官方 parity 与 release 仍未闭合。精确证据见 [E-V1-AUTHORED-FBO-MINIMUM-EXTENT](runtime-evidence-index.md#e-v1-authored-fbo-minimum-extent)。

2026-08-19 的 V1 allocation-lifecycle 原子不扩大 authored extent 准入，而是把实际 graph input、complete target plan 与 committed history 的关系变成 fail-closed typed 事实：同 plan 的 fresh writable history generation 记为 `history-copy-on-write`，descriptor-stable 且无 history 的 fresh identity 记为 `allocation-rebind`，input extent 或 target descriptor 改变才记为 `allocation-reprepare`；不一致组合拒绝 candidate commit。真实 `3749463715` 资源树派生 solid capture 以 `1.0 → 0.25 → 1.0` 形成 `2048x1152 → 963x542 → 2048x1152` input。`scale:2` 正门 target 随之为 `1024x576 → 481x271 → 1024x576`，两次 reprepare 都重新 seed、丢弃旧内容且不做 rehydrate；未见的 `640x360` absolute 正门 descriptor 保持不变，两次 reprepare 都复用 history、精确复制 1 个 readable closure、且不丢弃旧内容。mixed scale/absolute 反例只在中间尺寸产生 typed descriptor fallback，恢复 A 后同层 graph 再成功；三份 fresh report 均 **1/1 PASS**，stable frames 分别记录 416/419/297 次 history COW，并到达 GPU completion、publication、terminal compositor 与 next-frame。截图仅为 non-black/advisory，flat-border ratio 为 1.0、无 motion，不能表述为视觉 fidelity、动态或官方 parity。route 仍为 `prefer-generic`，最高为 `slice-visible / bounded authored FBO graph-input extent allocation lifecycle`；没有开放 arbitrary lifecycle/load-store/seek/stop、多 surface/hot-plug、owner migration、Fast/fixed/full、性能/长稳或 release。精确身份见 [E-V1-AUTHORED-FBO-INPUT-EXTENT-LIFECYCLE](runtime-evidence-index.md#e-v1-authored-fbo-input-extent-lifecycle)。

同日的 Shift Hue owner cohort 先以严格、有界的 legacy annotation JSON 恢复作者 `range:[0,01]` 进入同一 ShaderContract：恢复只会在字符串外、JSON value 边界上去掉裸 integer 的冗余前导零，不修复 decimal/exponent/正号/结构错误，也不改写原始 source/annotation text。`source-proven-graph-input-stage-uniform-straight-alpha-preserving`只由 straight-alpha-preserving color fact、唯一 typed graph input、无 target/provider/scalar output、active sampler 全集恰为唯一 regular graph-input sampler 与唯一受 typed schema 约束的 defaulted opacity-mask auxiliary，并要求 vertex/fragment 均有 active non-sampler uniform 准入，不读 Shift Hue/effect/sample/layer/path/asset/hash identity。真实`3767460992` 两个 Shift Hue stage 在默认route下均为`generic-only / genericCompilerArtifact`；坏artifact只让这两个effect执行effect-local previous-current并保留后缀，显式`disable-generic`则只回滚共享`boundedSwift`。旧Shift Hue planner/plan/compiler backend/pipeline/renderer与耦合测试已删除；这只完成该source-derived profile的owner migration，不升级其他mask/resource/state/revision、更宽straight-alpha-preserving profile或官方像素parity。精确证据见[E-EFFECT-WORKSHOP-SHIFT-HUE](runtime-evidence-index.md#e-effect-workshop-shift-hue)。

随后的 Workshop Gradient cohort 不按效果名添加算法，而是在共享compiler input增加保守canonicalizer：双stage同名同型且大于16的varying array，只有在静态`1...16`次loop可安全展开、所有use成为literal、两stage需要同一prefix时才同步收敛；dynamic bound、控制流退出、动态/越prefix引用与shape mismatch都保持失败关闭。prepared authored source继续是loss-preserving truth，canonical pair同时供external artifact和shared bounded rollback。`source-proven-graph-input-stage-uniform-straight-alpha-preserving-no-auxiliary`要求straight-alpha-preserving、唯一typed graph input、无target/provider/scalar output、active sampler全集精确为该regular graph input且双stage均有active uniform；真实`3767460992:945#effect#952`默认`generic-only / genericCompilerArtifact`，坏artifact只保留effect-local previous-current并继续后缀，显式`disable-generic`回滚`boundedSwift`仍PASS。旧Gradient planner/plan/compiler backend/pipeline/renderer与耦合测试已删除；该批冻结时mixed chain为25 shared / 1 dedicated。精确证据见[E-EFFECT-WORKSHOP-GRADIENT](runtime-evidence-index.md#e-effect-workshop-gradient)。

本轮 Enhanced Audio Bars cohort 继续修共享语言/ABI而没有新增效果算法：fragment varying只有在非数组、只在唯一`main`中使用且确实被写时才局部化为mutable copy；helper/global use、数组、名称冲突与多`main`保持失败关闭。共享artifact lowering同时把source-proven straight-alpha输出置于统一unpremultiply/premultiply边界，按反射只清除16/32/64 stereo scalar audio数组动态索引后的SPIRV-Cross伪`.x`，MaterialProgram保留作者uniform key的显著尾随空白并拒绝空白/控制字符键。新的`source-proven-graph-input-audio-stage-uniform-straight-alpha-no-auxiliary`只接受唯一typed graph input、active sampler全集精确为它、双stage active uniform、完整同档left/right音频数组以及至少一个安全局部化mutable fragment varying；默认`generic-only`。真实`3767460992:945#effect#950`以generic artifact闭合GPU/publication/suffix/compositor/next-frame，损坏exact artifact只让该effect发布previous-current并继续后缀；旧Audio Bars planner/plan/compiler backend/pipeline/renderer与耦合测试已删除。现役mixed chain为26 shared Program / 0 dedicated。显式`disable-generic`仅证明安全撤回产品route并局部previous-current，不是视觉等价回滚；宽泛straight-alpha、任意audio shader、SceneScript AudioBuffers、独立ROI与官方parity仍未完成。精确证据见[E-EFFECT-WORKSHOP-ENHANCED-AUDIO-BARS](runtime-evidence-index.md#e-effect-workshop-enhanced-audio-bars)。

## 2. 官方资料覆盖

| 资料面 | 收集状态 | 权威入口 | 仍未知 |
|---|---|---|---|
| Scene 官方页面 | 179 个 `/en/scene/` 页面已建目录并逐页映射到唯一合同 anchor，2026-07-22 经代理核验 | [官方页面全目录](official-page-catalog.md)、[逐页映射](official-page-map.md)、[分组映射](official-page-crosswalk.md) | 官方未来页面变化 |
| 官方 Effect | 45 个用户可见 effect + `_empty` 内部占位已整理 | [内置 Effects 语义全集](effects-reference.md)、[执行覆盖表](effect-execution-coverage.md) | 私有 shader 精确算法和全部历史版本 |
| SceneScript | 官方 `lib.sceneScript.d.ts` VERSION 2.8 已逐 API 建账 | [SceneScript API 覆盖表](scenescript-api-coverage.md) | Windows VM 的非公开行为和预算细节 |
| Scene/Effect/Material/Shader/RT | 作者合同与 IR/executor 状态已拆分记录 | [场景格式与 Render Graph](scene-format-and-render-graph.md)、[Graph/Shader 覆盖表](render-graph-shader-coverage.md) | 完整私有 schema、默认值和所有 combo |
| Particle | 官方组件、子类型和执行阶段已逐项建账 | [粒子组件覆盖表](particle-component-coverage.md) | 未公开 preset 资产内容和平台精确视觉 |
| Timeline/属性/输入/Provider | 官方求值顺序与 target 状态已逐项建账 | [运行输入与属性覆盖表](runtime-input-property-coverage.md) | 私有序列化字段和 Windows 精确数值 |
| Puppet/3D/Lighting/性能/RGB/离线 | 高级对象与产品策略已逐项建账 | [高级对象覆盖表](advanced-object-coverage.md) | 私有资产格式、设备和渲染器内部行为 |
| 资料来源 | 官方、样本、WaifuX、linux-wallpaperengine、当前代码分级 | [资料来源与证据索引](source-index.md) | 第三方实现不能替代官方真值 |

因此，后续一般不再猜“这个能力是什么”；实现前先查上述合同。仍需研究的部分应明确标为私有格式/算法未知，不能用视觉近似反向定义官方语义。

## 3. 系统总表

下表每行的“主要缺口 / 升级门”只定义该能力家族如何升级，不是按行执行的任务队列。批次可为同一用户可见结果跨行、跨 D 节点实施和验收；不得用局部等级提升代替 terminal composition 与目标 ROI。最后一列只是旧证据 provenance，现役路线只看 V0-V5。

| 系统 | 当前级别 | 当前真实能力 | 主要缺口 / 升级门 | 历史证据标签（非路线） |
|---|---|---|---|---|
| PKG/TEX/资源索引 | `L3` | loose/PKG/stock 查找、常见 TEX、内嵌 MP4、诊断式失败；PKG extraction 以 package/content SHA 和 exact file set 校验损坏/陈旧 cache，单次读取包体；同一 surface 复用 TEX parse 与 device texture，本机普通文件以 canonical path/size/mtime/device/inode/ctime 区分 generation，并在 image/TEX/video 读取或发布前后复核同一 revision；metadata 不可得或非普通文件失败关闭；PNG/JPEG、raw/embedded format 0 与 BC1/2/3 的九类 2D typed purpose（premultiplied color、straight albedo、generic preserved、mask、noise、flow、phase、normal、depth）进入独立 cache/upload identity，另有严格 format-0 vertical embedded-PNG `.lookupTable` volume purpose 保留 header/mip depth 并上传为 `rgba8Unorm` Metal `type3D`；普通 2D consumer 明确拒绝 volume。全部 33 个 Effect 辅助纹理调用已显式选 2D 用途：mask 16、noise 6、flow 2、phase 2、normal 2、depth 1、generic preserved 2、X-Ray straight albedo 1、premultiplied Blend color 1；data 图片只接受原尺寸、无 decode 映射、integer packed、明确非预乘 alpha 或无 alpha（skip）且使用受控 32-bit byte order 的 RGB(A/X) provider，并归一为 RGBA；只有 CGImage 明确无 alpha 的 straight albedo 可 bounded raster/resize 并固定 alpha=1，alpha-bearing/premultiplied 与其他 data purpose 仍严格；Water Flow flow/phase 与 Standard Blur mask 已从同一 source generation 原子取得 identity、purpose、实际 physical/已验证 mapped size、UV transform、sampler 和 pixel format；Shake flow/phase/mask、Foliage Sway mask/noise、Water Ripple mask/normal、Depth Parallax R8 depth 与 exact legacy Blend overlay 共九槽进一步经共享 authored `0...7` binding，把每个 texture 与各自 candidate 的 generation/metadata 原子绑定；普通 PNG/JPEG property color 与 authored TEX 均发布 typed candidate，非法尺寸/变换、错误 slot/purpose/format 与 clamp-border 均失败关闭；BC1/2/3 多 image color sprite 新增 axis-aligned、整数像素、同 frame extent 的 bounded native autoplay：source 按 file generation/device 跨 surface 去重，RGBA target 按实例分配，straight alpha 经 GPU 转预乘，launch-scoped 384 MiB 预算使用设备 texture size/alignment 且随 playback/Scene 生命周期释放，文件 revision 在分配前后复核，GPU 失败允许同帧重试；R3 launch VFS catalog 又为 purpose-proven static asset 形成 exact demand/publication，并把固定八槽及 hole 纳入 atomic Program | LUT effect/material consumer、3D sampler/颜色空间与 Windows sampling golden；跨进程 extraction lock、跨网络文件系统或粗粒度/陈旧 ctime 下的强内容证明（当前无 content digest）、旋转/trimmed/fractional/异尺寸跨-image frame、dynamic replacement、SceneScript `ITextureAnimation` detach/join/command/timer 生命周期、alpha-bearing straight source resize 与 Windows 通道 golden、BC5 consumer/encoding、未证 regular asset purpose、尚未准入 Program 的 slot/purpose/readiness、system/media/variant 动态 generation、case/symlink/duplicate/多格式边界及完整 VFS golden | B0 |
| Scene IR 与基础层级 | `L3` | 基础对象、顺序、父子 transform/visibility、受限 Puppet 静态 attachment frame 与 animation layer 声明、常见 image/text/solid/particle；Scene loader 与 descriptor admission 双重拒绝重复 authored object ID，把排序后的冲突 ID 作为 blocking 诊断并沿 launch error 保留具体原因，避免下游 `Dictionary(uniqueKeysWithValues:)` runtime trap；image/solid 作者 `alignment` 随 Scene JSON 进入 layer IR，并以独立于 text alignment 的 quad pivot 放进 `sizeScale` 之后，支持 center/top/right/bottom/left 与四角、缺省/未知值保持中心。44 份已解包真实 Scene 中 16 个样本声明 135 次，52 次为非中心；作者 `color` 与 `brightness` 由普通 image/solid 在 color blend/effect 前共同折进 `u.tint`，GPU 门覆盖暖色、`brightness > 1`、mode 31 与 SDR 裁剪；`contentKind == "text"` 跳过 compositor 的同名颜色/亮度，交回 CoreText 栅格化阶段消费；作者 text layer `anchor`（编辑器 Screen anchor）随 interpretation v28 进 text IR，并与 parallax 折进同一个 layer 平移（随包 5 个 text layer 中 `dino_run` 的 231/177 为 `topright`，其余 3 个为 `none`），有 CPU 几何门与离屏 GPU 覆盖门；作者 `horizontalalign`/`verticalalign` 除了继续喂 CoreText 框内排版，还作为 text layer 的 quad pivot 折进同一个 model 矩阵（排在 `sizeScale` 之后），随包 5 个 text layer 中 `dino_run` 的 231/177 为 `right`/`center`，其余 3 个为 `center`/`center`；作者 text layer 的 `limitrows`/`maxrows`/`limitwidth`/`maxwidth`/`limituseellipsis` 随 interpretation v29 进 text IR，并在 CoreText 栅格化阶段决定换行宽度、行数与省略号（两个数值只在对应开关打开时生效），有逐像素正反门 | effect/pass/material/target duplicate、模型、动态 attachment follow、复杂 object component 和全部动态字段；image/solid alignment 尚无 Windows 逐像素 golden；`brightness` 超过 1 的部分只由 render target 精度裁剪，没有 WE 像素标定；image color 的动态 binding 仍未开放 | B0 |
| Utility composition | `L3 bounded` | typed composition/project/fullscreen、完整 bounded current capture 与 `_a` named target。classic primary Clipping dependency 现由 `.resolvedMaterial` binding 驱动 reservation、provider current-frame publication、普通 MaterialProgram slot 1 与 GraphExecutor，后者是该 bounded profile 的唯一产品 owner；旧 Clipping 专用 planner/contract/execution plan/renderer/test 已删除。准入要求 primary backward reference、composition provider、slot 1、无 user texture override、dependency blend 0，且 authored constant 缺席或所有 component 为 unit；`BLENDMODE=5`、`Opacity=1`、同provider双consumer、同consumer多effect/pass reference与两个独立provider各一consumer有真实正证。另一个 bounded primary closure 允许可见 image root 反向可达的 hidden image provider 先执行普通 Program/GraphExecutor，再把 graph final 复制到独立 named reservation；中间 provider 不占 compositor。worleyColorV1 Procedural Noise仍按其独立 slot 3/blend 0 typed owner执行；`2938612768:563`仍以显式main-target source route执行。X-Ray partial-prefix child capture与独立ImageBlend owner已撤销 | child/non-image provider、project/child/multi-dependency/reference consumer、forward/cycle execution、history/resize/device-loss、SceneScript/dynamic alpha、任意named texture、secondary、mismatch/current-frame缺失与RGB语义继续失败关闭；`_b` 为 typed `unsupportedVariant`，非 unit authored constant（包括 `Opacity=0.5`）为 typed `unsupportedConsumer`，missing provider 只拒绝不可满足的真实依赖 closure，route-disabled 时 generic named closure 零执行且保留安全输出。普通authored Blend与layer color blend保留各自现役合同，一个Clipping、Procedural Noise或Audio Bars正例不外推其他utility | B2/B3 |
| 画布/cover/背景 | `L3` | cover 投影和作者声明视差门，未覆盖区域不再暴露灰底 | 多比例、多屏和 Windows 像素基准 | B5 |
| Frame Context | `L3` | host 单一 60 Hz driver；共享 clock 发布 raw、最大 0.25 秒的 simulation、dropped delta 与 discontinuity，particle/parallax 消费 simulation，shader/video/Timeline 保持 raw/absolute time；Scene pause 冻结 scene time 和 frame index，resume 首帧 delta 为 0 且不补暂停长帧；Debug benchmark 结构化记录 discontinuity/dropped/max-raw、startup、callback、drawable、encode、CPU/GPU 分位数，缺失/非法事件失败关闭 | 真实系统 pause/sleep、其他 simulation consumer、seek/history、目标 FPS/不同 FPS、长期阈值、离线 adapter 与 Windows timing golden | B0 |
| Dynamic target/value 与 property binding program | `L3 bounded；QuickJS scalar S4 bounded visible` | 六类value、主要target、固定优先级；v22保存layer alpha/color、direct text三字段、strict Local Contrast/Opacity与root Particle八字段；bounded text width、time-of-day Blend、placeholder-fade Opacity及launch-origin `object.origin`已有运行子集。`311115e3`又把shared layer alpha、audio-scaled particle rate/layer scale、property slider→Vec3 origin/scale与identity display projection接入同一definition/snapshot链；QuickJS-NG scalar owner 在 authored/user/Timeline 后投影到同一 `sceneScript` channel，并在派生真实 `3768724269` fixture 上改变 pass-owned `strength` | QuickJS visible 只覆盖一个 numeric `constantshadervalues` scalar；generic VM 的非-scalar target、hidden/no-consumer、host API、事件/mutation 与未证明脚本target仍缺或重建；该证据不证明原始 authored corpus 或官方 parity | **B0/B4** |
| Per-surface dynamic snapshot | `L3 bounded；QuickJS scalar S4 bounded visible` | host共享property/audio/media输入，每个surface独立transaction/snapshot/generation；相同payload不增generation。placeholder fade、shared/audio/property projection与QuickJS scalar evaluation在surface循环外计算；launch-origin与hover/click在每surface的camera/world-frame hit-test后进入最终transaction；V2 scalar result 在同一 fixture 的最终 graph snapshot 中被消费 | 当前 VM domain 仍按 scene 共享；per-surface VM、通用event queue与provider实例隔离仍缺；正门只有一个 surface 和一个 scalar target，不能外推多屏或通用事件 | **B0/B4** |
| Atomic live property state/routing | `L3` | layer alpha、纯solid color、strict Local Contrast/Opacity、bounded text width、placeholder-fade Opacity、bounded launch-origin layer origin与root Particle八字段先原子求值再供renderer/simulator消费；QuickJS scalar target 在同一 authored → userProperty → Timeline → SceneScript 顺序中进入 snapshot；launch-origin与direct property或Timeline发生target collision时整项launch producer失败关闭 | mixed、generic SceneScript mutation/event、unsupported、无consumer与particle child target仍保留rebuild/fail-closed；live media ingress不存在 | **B0/B4** |
| Timeline runtime | `L3` | 作者 `animation` 无损进 IR（lane/keyframe/tangent/options/combined 分组/`relative`/`previewvalue`），44 个真实 `scene.json` 的 48 处全部解析成功、0 诊断；纯函数 evaluator 按**绝对 scene time** 求值 Loop/Mirror/Single、`startpaused`、端点保持、作者 cubic Bézier handle 与 Loop wrap 闭合段，双侧禁用严格线性。编译成 typed `SceneDynamicTargetDefinition` 后经既有 per-surface transaction 写回，`SceneDynamicSource.timeline` 优先级高于 userProperty。现役 Workshop **48/48** authored host 均形成 typed binding：effect constant 23、layer alpha 5、relative layer transform 9、root particle scalar override 7、bounded text `maxwidth` 2、bounded Combined 2D camera `origin/zoom` 2；实际视觉消费仍受对应 backend/chain 准入约束，`3769688830:157` 是首条已闭合的 Tint alpha Timeline。9 条 wrap 声明消费末 front/首 back，当前 fixed13 25 bindings / 0 diagnostics。camera origin 不与 layer transform 重复计数；两成员共用 owner clock并原子进入共享 projection | 48/48 只表示现役 authored host 已编译为 typed binding，不是所有 target 都已由可执行 renderer 消费，也不是 generic Timeline：非 layer-transform/particle scalar 的 `relative`、普通 Combined、multiple camera path/3D camera 与 Animation Event 继续拒绝或未执行。handle/wrap wire 单位是合法语料 clean-room 合同，无 Windows 同相位/像素 golden；absolute CP position 另有 stock-wire/project-fixture consumer，angle 只保真 | **B4** |
| SceneScript property binding IR | `L1` | 文档级 IR 无损保存 inline `source`、owner、完整 JSON target path、`scriptproperties`、authored fallback、JSON value type与排序后的可选`wrapperKeys`，旧cache缺`wrapperKeys`仍可解码；覆盖正式取证的 scene general、object transform、layer visibility、effect visibility、effect-pass constant 五类位置，单 wrapper 的 `script + user` 冲突诊断并拒绝。独立`SceneScriptSourceEvidenceIR`遍历所有string-valued inline `script`，保留最近owner、完整target path与wrapper keys，只用于provenance/conflict rejection；nested/未知位置不会因此取得executable binding authority。作者顶层`visible/alpha`的script ownership在property resolution前另行保真；没有可执行runtime时，只有这些display-owned层及其descendants带精确诊断失败关闭，不把并存`value`当静态显示授权 | 该抑制与source evidence都只是故障隔离/拒绝证据，不执行脚本。file/module、schema-resolved Vec/value type、handle、VM、mutable shared、cursor events、display mutation 与generic lifecycle 仍缺，见 [E-AUTHORED-STRAIGHT-RGB-SCALAR-ALPHA](runtime-evidence-index.md#e-authored-straight-rgb-scalar-alpha) | **B4** |
| Fixed native text-script profiles | `L0 product owner / historical` | B18已删除七个source SHA、Workshop命名profile、property配置、greeting schedule复用与native formatter；source identity不再选择文字产品执行 | B18前报告只记录已退役实现，不证明现役owner、当前输出或视觉；当前产品合同只看下一行bounded AST，见 [E-R4-B18-FIXED-TEXT-PROFILE-RETIREMENT](runtime-evidence-index.md#e-r4-b18-fixed-text-profile-retirement) | **B4** |
| Bounded property-bound text `update(value)` subset | `L3 bounded` | inline source 解析为项目自有、无循环 AST；支持 `new Date()` 与七类 calendar getter、primitive script property、局部变量/顶层未初始化声明槽、块/`if`/`else if`、有限 primitive 数组与整数索引、`&&`/`<`、严格相等和 number↔finite numeric-string 松散相等、`+`/`%`、单元素/primitive 数组字符串转换与 string `slice`；按 token/statement/evaluation budget 执行并写入 typed text snapshot，不看样本、layer 或 source hash | 只解析唯一 exported `update`，不执行顶层 builder/initializer；块没有独立 scope，也没有完整 coercion/exception/locale、init/module/engine/owner/handle/event/timer/任意调用。`clockWithPeriod`、greeting及其他未准入语法不建binding并保留authored fallback，见 [E-TEXT-SCRIPT](runtime-evidence-index.md#e-text-script) | **B4** |
| Bounded property-bound Blend time-of-day subset | `L3 bounded` | 完整 wrapper 下的唯一 exported `update` 只准常数、有限四则算术、`engine.timeOfDay`、`WEMath.smoothStep` 与 `Math.max`；结果经 typed Blend multiply target 进入 per-surface snapshot，按本地 wall clock 逐帧更新，不看样本、layer 或 source hash。exact 单纹理 Blend 另支持静态 `WRITEALPHA=1`，但 alpha 本身不接受 binding | 没有 engine/WEMath module object；其他 statement/function/API/target、动态 alpha、named RT/media、多纹理、时区/DST/跨午夜长稳与 Windows 数值/像素 golden 仍缺；见 [E-EFFECT-BLEND-TRANSFORM](runtime-evidence-index.md#e-effect-blend-transform) | **B4** |
| Bounded media-playback placeholder fade | `L3 bounded；新增媒体接线 S2` | exact pass-owned effect-constant IR与严格AST结构编译为typed Opacity alpha producer；既有两样本有GPU/compositor/局部可见正证。`311115e3`后的producer-agnostic inbox另保存playback、artwork colors、title/artist及各自generation，并接入bounded color/text consumer | 新playback/colors/title/artist只由代码与自动测试证明，尚无对应fresh runtime/ROI，因此为`S2 / visible unknown`；仍无live macOS producer、status/timeline、previous、generic event/VM或Windows golden | **B4** |
| Bounded SceneScript launch/hover/click origin cohort | `L3 startup；新增交互接线 S2` | initial-false launch-origin exact13已有有界可见证据。`311115e3`另加入surface-local primary-button edge、实际camera/world-frame/quad UV hit-test、admitted master click toggle与bounded hover-origin projection | 新click/hover不是通用SceneScript event，且没有fresh可见证据；不执行ECMAScript/module、mutable shared、generic cursor callback或`3768229922` head-click 16层文字toggle | **B4** |
| Fixed native SceneScript 64-band Audio Bars profiles | `L0 product owner / historical` | B21已删除两个raw source profile的compiler/plan/verification、native geometry/renderer及全部产品接线；wrapper source仍只保真，不执行 | 通用VM、`registerAudioBuffers`、`AudioBuffers`、`createLayer`、`ILayer`与dynamic-layer lifecycle均未实现；旧64-draw报告只作历史，见 [E-SCENESCRIPT-AUDIO-BARS](runtime-evidence-index.md#e-scenescript-audio-bars) | **B4** |
| Fixed native texture-animation profiles | `L0 product owner / historical` | B19已删除delayed-loop/time-of-day两个source SHA、compiler、playback plan与专用clock；TextureAnimation SceneScript仍保真但不执行 | B19前报告只记录已退役实现，不证明现役profile owner、当前时序或视觉；普通TEX atlas/multi-image autoplay是独立资源能力，见 [E-R4-B19-FIXED-TEXTURE-ANIMATION-PROFILE-RETIREMENT](runtime-evidence-index.md#e-r4-b19-fixed-texture-animation-profile-retirement) | **B4** |
| Generic SceneScript VM/API | `L1 bounded scalar；S4 bounded visible` | QuickJS-NG 固定 revision `bbe0480a68c664d7737eecde260fd47224b2c0d6` 已作为 per-scene runtime/context 接入；pass-owned numeric `constantshadervalues` 的唯一 scalar binding 可执行 `init/update`，共享 domain 内 owner 按 generation 隔离，heap/stack/interrupt budget、exception、bad return、disabled、stale owner 均 typed fail-closed；host 结果进入现有 frame snapshot，派生真实 `3768724269` 正例记录 `0.3 -> 0.4`，异常负例记录 `exception / previous-current`，两者均沿 graph/compositor/next-frame 闭合，route 仍为 `prefer-generic`，bounded Swift owner 仍是 fallback/oracle | file/module loader、global/host API、Vec/bool/string/mutation、事件/engine/handles、per-surface instance、完整 authored corpus 与官方 parity 仍缺；正反 fixture 只改一个已有 scalar source，不能提升为 generic API 或 owner migration | **B4** |
| Current pointer | `L3` | view-normalized -> scene world 与 axis-aligned authored layer UV 极窄子集；parallax/受限 effect 消费 | parent/rotation/scale/parallax 逆变换与 effect/control-point/script golden | B0/B4 |
| Previous pointer | `L2` | Frame Context 保存，但 renderer 未消费 | shader built-in 与事件 delta | B0/B4 |
| Pointer buttons/events | `L2 bounded wiring；generic L0` | AppKit mouse down/up已更新per-surface primary-button state；bounded launch-origin master可在button edge进行camera/world/UV hit-test并toggle cohort，hover另有bounded projection。当前只由代码与自动测试证明 | generic down/up/click/enter/leave有序event queue、multi-button、capture/drag、VM bridge与fresh visible evidence | B0/B4 |
| Audio declarations | `L3` | effect 侧 `AUDIOPROCESSING` combo 与五个 audio 常量已由共享准入层消费；exact Workshop `2084198056/Simple_Audio_Bars` 的 16/32/64 数组与三个严格 combo tuple 有独立 consumer；粒子侧 audio schema 保真解析并由 bounded emitter/initializer/operator 子集消费 | 其他 Particle profile、Workshop audio shader、SceneScript `AudioBuffers` bridge 和未准入 combo/指纹继续失败关闭 | B0/B4 |
| Audio frame input | `L3` | 16/32/64 频段 × left/right host-shared 快照：系统音频 tap -> 单次 FFT 的 `SystemAudioSceneSpectrumAnalyzer` -> `SceneAudioSpectrumInbox` -> `SceneFrameContext`，host 每帧采样一次广播给所有 surface；采集由 consumer 存在性驱动，无 consumer/暂停/锁屏/休眠即停采并归零 | stock及普通/Workshop Effect profiles消费16/32/64；bounded Particle Audio Response消费16档且同帧fixed steps复用同一snapshot。B21已删除fixed native property-script consumer。频段边界、归一化和平滑是工程选择，非官方合同；没有SceneScript `AudioBuffers` bridge/object或通用average | B0/B4 |
| Resolved-material authored audio arrays | `L3 bounded` | 公共 frontend/Program ABI 保留 `float g_AudioSpectrum{16|32|64}{Left|Right}[N]` 的数组 shape/byte size，并从同一 FrameContext snapshot 经 host schema/finalizer 编入 uniform bytes；Program variant 的实际 host-uniform consumer驱动音频 demand。`3122339805:64`以ordinary source、`2938612768:563`与relocated `3299228616:151`以有界composition main-target source进入resolved-material GPU/compositor/next-frame事务；前两者证明bottom柱高，后者证明16-band left/right上下双向逐条增长及Program后zero-distortion Fisheye有序消费。未知数组、非法长度/类型、跨stage冲突、不完整envelope与不确定utility shape保持失败关闭，见 [E-R4-2-AUDIO-ARRAY](runtime-evidence-index.md#e-r4-2-audio-array) | 只证明当前可静态准入的Simple Audio Bars Program子集；不支持任意uniform array、通用audio shader、任意utility/dependency或SceneScript `AudioBuffers`，也不证明官方FFT数值或Windows像素等价。其他dedicated audio profile owner尚未全部撤权 | B0/B4 |
| 内嵌视频纹理 | `L3 bounded` | TEX 内嵌 MP4 image-layer 以共享 `SceneFrameTiming` 映射 item time；launch-scoped registry 按 layer/source/device 复用 provider，surface rebuild 保留连续 item time，pause 保帧、resume 不补暂停时长，按成功 publication 增 content generation，同 frame 去重并拒绝 stale/mismatched publication，stop 释放 player/output/cache/临时文件；真实样本证明启动、连续非黑画面和 stop | seek、真实系统 pause/screen hot-plug 运行门、loop 首帧/首帧黑场策略、codec/device-loss 恢复、variant/generic material consumer 与 Windows A/V parity | B1 |
| 系统媒体 identity | `L3 bounded current cover；metadata/colors S2` | `$mediaThumbnail`具有显式generation publication、严格Blend source replacement与通用visibility binding；current-only store保留pending last-ready、stale/cancel与clear/failure fallback。inbox现另保存playback、artwork colors、title/artist三套typed generation，并有bounded consumer代码 | 新metadata/colors/playback无fresh runtime证据；仍缺live platform producer、status/timeline、generic event/VM、previous/transition/variant | B1/B4 |
| Particle runtime | `L3` | 作者 Sprite、常见组件、bounded 16-band Audio Response、bounded classic Vortex、bounded Cap Velocity、最多 64 粒子的 zero-separation Boids alignment/cohesion、root/child 共用的 bounded Sprite Trail、static/spawn/death/follow Sprite profile 共用的 bounded uniform screen-plane child scale、严格单 renderer/Screen 的 bounded Rope 与 RopeTrail、静态同空间 plain-image Layer Image、Sphere/Box/Layer Image 共用的 bounded initial emitter delay、root Sphere/Box 的仅 rate Random periodic active/delay schedule、有限非负有序的 emitter speed range、bounded Directions/Sphere Sign、root Sphere/Box emitter 的 bounded static CP-angle frame，以及 exact pointer-lock CP 1...7 驱动的 bounded local Control Point Force。Particle audio 共用 frame snapshot 与 evaluator：rate-only Sphere/Box 乘 rate，Turbulent Velocity Random/Turbulence 乘 phase，底层已准入的 classic Vortex 乘 speed；invalid/unsupported form fail closed。root `instanceoverride` 另可由 direct User Property 驱动 `alpha/size/lifetime/rate/speed/count/brightness/colorn`，并由 absolute scalar Timeline 驱动前七项；每步 snapshot 覆盖 static fallback，只影响发射与新生粒子。initial delay 在 fixed-step 边界只消费剩余 active slice，duration 从 delay 后计时；classic Vortex 按 v1 axis/distance/speed 与 standard/infinite-axis flag 做项目自有 fixed-step 切向执行；Cap Velocity 按作者 max speed 与 normalized-age blend 移除超速比例且保持方向；Boids 用不可变 fixed-step 快照避免数组顺序偏差；Sprite Trail 允许省略/`null` length 并使用项目默认 `1`；child scale 只准正有限 X=Y 与三轴 `1/1024...1024`，同步缩放 local position/size/velocity/trail stretch；Rope 支持 `0...7` subdivision、有限 UV scale、equal-lifetime smoothing、时间 scrolling 与隔离 child topology。普通 Sprite 消费可信单 sequence sidecar nominal frame aspect，缺失时按 raw TEX axes × physical pixels 回退，root/child 共用；custom/missing shader、显式空 renderer 与未准入声明继续 fail closed | dynamic override 的 script、source conflict、direct color、relative/Combined、child inheritance 与存量粒子追溯更新仍缺；child/Layer Image/未准入 operator audio 与 emitter delay/duration/periodic/burst audio 仍缺；child 非零 angles、event offset、XY 非均匀/镜像、Rope/nested/world-space-movement scale 与 Windows transform golden 仍缺；initial delay 没有 Windows fixed-step/组合 timing golden；classic Vortex 的 CP/center force/`vortex_v2` ring 与 Windows audio/数值/轨迹 golden 仍缺；Cap Velocity 的 Windows blend/clamp 数值和轨迹 golden 仍缺；Boids separation、speed clamp、大群 spatial index 和 WE 轨迹公式仍缺；periodic burst/max-per-period、Layer Image 动态/text/puppet source、dynamic CP angle、previous pointer、其他 CP consumer、adjusted/world/perspective、Rope animated texture/root world/multiple renderer、精确 ribbon join、通用 RopeTrail subdivision/UV、Lighting、custom shader executor 和 Windows golden 仍缺失 | **B4** |
| Text/Font runtime | `L3 bounded；media text S2` | CoreText 静态栅格、direct property 动态重栅格、bounded property-bound `Date` text update subset和部分font/pointsize/padding/scale；动态内容在`limitwidth=false`时按intrinsic size扩框并贯穿capture/effect/final quad。typed title/artist media profile已接入代码 | media title/artist没有fresh runtime/ROI，不能称可见；完整SceneScript/media text、Windows baseline/fallback、outline/shadow/effect仍缺 | B4/B5 |
| Camera Parallax | `L3` | 仅作者开启且非零 depth 时启用，含层级传播/阻断；与Scene Camera Shake共用同一帧相机身份但保持独立算法 | WE 数值/时序 golden、zoom、3D camera | B5 |
| Scene Camera Shake | `L3 bounded` | 作者显式enable、默认值与有限editor-range参数进入独立descriptor/evaluator；当前只准有效2D orthographic Scene，以absolute scene time生成确定性XY camera displacement。base/path与shake形成唯一frame camera，shared view、parallax、pointer、正交层和下游perspective particle都消费该状态；User Property通过rebuild生效。见[E-CAMERA-SHAKE](runtime-evidence-index.md#e-camera-shake) | 真正perspective Scene的XYZ分支、3D camera、SceneScript same-frame setter、无重建live property、官方pause/seek事件策略、Windows同相位数值/像素、多屏长稳与整景视觉等价 | B5 |
| User Properties | `L3` | 独立窗口、条件、持久化、PNG/JPEG `sceneTexture`；layer alpha、纯 solid color、direct text、strict Local Contrast/Opacity 与受限 X-Ray target 已无重建 live 更新；同一 property texture 可按消费者建立 premultiplied generic/Blend candidate，以及相互独立的 X-Ray straight-albedo blend 与 preserved-channel halo identity；X-Ray opacity 是 authored `.mask` texture，没有 property map。exact legacy Blend slot 1 的 property candidate 优先，缺失时回退 authored asset，候选与 UV/sampler/format 同代进入 renderer；Simple Audio Bars `Bar Color` 与relocated 16-band `Opacity`已由typed binding/snapshot进入统一Program GPU consumer，旧Simple专用compiler已删除 | Audio Bars 尚无真实 live override 门；unsupported/mixed/SceneScript bindings、Texture Variants、shortcut、跨重启 UI 门；精确 census 见 runtime-input 专项表 | **B0/B1** |
| Typed texture provider | `L3` | layer/named/property identity、status/fallback；静态 resource generation 与 named frame epoch 已分离；本机普通文件 generation 还携带 device/inode/ctime，cache hit、candidate、TEX 与 video source publication 均复核完整 revision，metadata 不可得时不发布；direct text、embedded MP4 与 bounded current media cover 使用显式 content generation publication，frame registry 拒绝 stale generation、同代换纹理和 publication/texture 不匹配；current media cover 新 generation 还会协作取消旧 request，并在 decode/upload 边界停止过时工作。exact static-file/current-media publication atom现可在route精确`.unclaimed`、不存在已授权/可执行typed dependency、source-copy、frame target或final-alpha、style中性且有限authored quad与viewport有正面积交集时，仅将source按作者几何交给普通final layer compose；coverage/opacity资源默认阻断，只有exact stock Pulse单pass在`PULSEALPHA=0`时可把其mask证明为被跳过effect的局部输入。`PULSEALPHA=1`有无loaded mask、非法pass/path/combo及任一typed Opacity entry仍阻断；本批另证严格stock identity准入的Water Waves单passslot-1 mask（`TIMEOFFSET/PERSPECTIVE/DUALWAVES`缺省/0）只是被跳过effect的位移输入，不是独立opacity/final-alpha law，也不证明effect输出与raw source逐像素coverage恒等，因此不再单独阻断consumer source。X-Ray consumer收窄由current stock与exact historical stock两条公共资源身份共同约束：每条身份都原子核对完整typed definition variant、唯一typed material pass、root-stage canonical identity及VFS sourceGraph dependency/唯一stock `common_blending` include，内容digest只证明登记资源未被替换，不按sample/layer/hash选择能力；historical身份只授权consumer base-source passthrough，不取得current X-Ray execution owner。只有provider真实存在且全部跨层输入精确一致时才豁免consumer端，provider端仍一律阻断；可见authored跨层edge继续同时阻断static-file provider/consumer，hidden/self不阻断，current-media保留既有独立authority。该路径记录`degraded-layer-source-passthrough`并使benchmark保持NON-PASS。B20已删除previous provider与gradient transition产品面。Water Flow flow/phase、Standard Blur mask、static plain/effectful base image、bounded REFRACT normal 与 Depth Parallax R8 depth 已用共享 typed candidate 原子携带 generation、purpose、actual physical/mapped、UV、sampler 与 pixel format；静态 base candidate 无论 direct、inline 还是 offscreen/authored-effect 路由，都在最终 GPU split 复核 texture identity/purpose/UV/sampler/format；其上新增公开 authored `0...7` 的共享 slot-binding 原子，Shake 1/2/3、Foliage Sway 1/2、Water Ripple 1/2、Depth Parallax 1 与 exact legacy Blend 1 共九个 bounded 槽进一步原子携带 slot、identity/generation、purpose、完整 UV、sampler、format、mip/resolution/texel；Blend 的 PNG/JPEG property 与 authored TEX 共用最终 ready candidate 选择，已选 candidate 不合合同时失败关闭；TEXB0003 embedded image 按 payload-class 尺寸合同准入 | source-stage降级不执行/发布effect output，不消费authored dependency，也不证明generic effectful/nested/child provider；fresh最终源码运行仍为 **0/2 NON-PASS**：`2241938645:68`只证明基础静态source高度接近官方构图，`1636394814:195`只证明historical X-Ray consumer自身基础source恢复且裁切/效果仍不等价，`1636394814:24`（provider）继续失败关闭；真实route只有shared frame completion，没有per-route publication/compositor/next-frame证明。live platform media producer、previous/transition/variant、其余 provider 的通用异步取消、单次 ImageIO 调用抢占、跨网络/粗粒度 metadata 的 content digest、其余 Effect 与 generic material 的 slot population/readiness、sprite rotation、BC5 与 clamp-border parity仍未闭合。见[E-LAYER-SOURCE-PASSTHROUGH](runtime-evidence-index.md#e-layer-source-passthrough) | **B1** |
| EffectDefinition/Material IR | `L2` | definition/pass/RT/material/slot hole/combo/constant可保留并建图，fixed 8-slot Template/Program原子携带active reflection、publication、uniform、state与color contract；presence combo/default仍分离。launch以`.ready/.absent/.pending/.unavailable` typed asset状态复用frame候选优先级，并仅对`effect.input=.layerSource`的material用同一content-only颜色推导核验renderer-owned premultiplied capture。公共frontend现可证明唯一sample的straight-RGB/sampled-alpha-factor子集，以及同一color slot上的bounded whole-RGBA affine filter并以straight UNorm边界输出；历史exact framebuffer alias使`1553008362`的Water Waves→Shake×3闭合，`3768724269:163` Rounded Mask与`3290491250:116` Sharpen也各自闭合GPU→publication→compositor→next-frame与固定ROI | 完整schema、其他editor alias、普通未证`effectOutput`/多stage与internal FBO颜色传播、dynamic provider/user content固定点、condition/function、通用Metal state、alpha replacement/additive/conditional、多个color slot/component mutation/dynamic division、任意矩阵及其他未证purpose/color仍缺；三项有界正证不表示generic material、整样本或官方视觉等价 | B2 |
| Effect execution evidence | `L3` | descriptor admission、static runtime disposition、exact/aggregate CPU invocation、route operation 与 shared-frame command-buffer status 分轴；完整 `EffectKey`、owner/route、sticky success/failure 和 exact/aggregate eligible-observed-gap 守恒进入 Debug report。execution demand 由同代有效 disposition 推导，真正空 effect/纯 inactive 可合法无日志，missing/invalid/unknown 继续失败关闭；矩阵只锁 7 个跨运行稳定字段 | shared command-buffer 完成不是逐 stage GPU 成功；未观察 gap 不伪造成失败或成功；该合同不证明视觉、官方算法、长期性能或未准入 stage | B3 |
| Bounded effect executors | `L3` | complete-chain-only基础上逐stage Program-first；Program失败段只有typed pair、logical-target或strict history-target完整闭合时才可接管。普通Program可执行单effect、至少两material、0 authored target/command/history/background/dependency/condition的layer-local full-frame compose，非末node全部`compose:true`、末node nil/false；白名单pre-encode visual failure在whole-effect边界恢复stage-entry previous current并丢弃未编码prefix。B8 raw full-frame Precise adapter已由共享normalized-sample-sum Program owner取代并删除；B9/B11/B13分别保留各自typed logical-target、captured-main与worleyColorV1 owner。B12的Clipping external-primary late-execute只保留为历史provenance；现役classic primary binding直接进入普通MaterialProgram slot与GraphExecutor，dedicated/fallback均为0，旧Clipping专用产品与测试owner已删除，默认route为`generic-only`。现役strict history-target只授权完整Cursor Ripple 3-pass/2-target typed plan。B15-B22完成其他产品owner撤权，B23删除旧whole-chain/standalone/frame-batch/telemetry surface，B24再删除shader fallback graph/provenance；GraphExecutor per-stage typed backend与完整VFS Program路径保持 | 其他compose、Refraction/scene background、generic/full-frame-compose captured-main、generic unique/history、任意named texture、secondary/multiple dependency、非unit named-provider constants、其他Procedural Noise与未迁移shape失败关闭；resource/target/dependency/runtime encode/state/lifecycle失败不降级。quad source仍以resolved `transparentDirectDraw`初始化透明pair并由通用GraphExecutor写effect output；`SceneLightShaftsLayerRenderer`只承担模型放置与唯一compositor桥接，effect命名carrier尚未通用化。normalized sample-sum owner migration与bounded layer-local compose都不开放其他history/dependency topology或Windows数值/像素等价；B22 full45 45/45与fixed13 12/13仍是历史formal跨样本基线 | B3/B4 |
| Legacy Workshop Procedural Noise | `L3 executed-degraded` | exact `2924967132` definition/material/raw+canonical shader、静态常量、`COMBINE=0`/`WAVE=0`、`worleyColorV1`与稀疏 slot 3 typed texture；B13把单 backward dependency composition的primary provider冻结为typed external-primary binding，provider current-frame capture/ready后由统一GraphExecutor执行项目独立的时变横向暗云带近似，旧authored whole-chain owner归零。详见 [Effect 执行覆盖表](effect-execution-coverage.md)、[E-EFFECT-PROCEDURAL-NOISE](runtime-evidence-index.md#e-effect-procedural-noise) 与 [E-R4-B13-EXTERNAL-PRIMARY-PROCEDURAL-NOISE](runtime-evidence-index.md#e-r4-b13-external-primary-procedural-noise) | 其他 Workshop ID/profile、dynamic binding、多 dependency、generic Procedural Noise 数学与 Windows 像素/动态等价；B13单例不升级整族 | B2/B3/B4 |
| Current-frame capture | `L3` | bounded完整utility capture；普通 base capture 仍要求无 child/dependency，`2938612768:563`已把该source按作者触发点交给统一resolved executor。Clipping 的B12专用 late-execute只作历史provenance；现役classic primary `.resolvedMaterial` binding在同一consumer/provider/effect/pass/slot/variant/frame identity下预留texture，provider按作者顺序发布current-ready，GraphExecutor消费同一epoch/object。bounded hidden-image effectful provider不再发布base capture，而是把GraphExecutor final复制到独立primary reservation，再由同一registry发布给下一层。worleyColorV1保留其独立typed owner；旧`captureAfterChildrenXRayPrefix`及Clipping专用capture产品链均已删除 | child/non-image/project/fullscreen provider、secondary、multi-dependency/reference、forward/cycle执行、extent/format/mask、SceneScript/dynamic alpha、scene-background，以及缺失/过期/错object的current-frame publication | B2/B3 |
| Named primary target | `L3 bounded Program carrier` | bounded `_a` producer/consumer可执行；classic primary Clipping route已完成`generic-only / owner-migration-complete`，binding固定consumer/provider/variant/完整slot/blend，reservation与ready publication保持同一texture identity，普通MaterialProgram slot 1与GraphExecutor唯一持权，旧专用owner/test已删除。同一provider双consumer、同一consumer多effect/pass reference与两个独立provider各一consumer已有独立fresh `slice-visible`正反证。hidden-image effectful provider的普通graph final另可复制到独立named reservation并形成两级provider链，中间provider明确不持有compositor。`MWX_SCENE_NAMED_PROVIDER_ROUTE=disable-generic`可原子撤销binding并产生typed consumer/provider issue，但不会恢复旧链。worleyColorV1保留其独立typed external-primary owner | 任意named texture、secondary、provider/slot/blend/epoch/object mismatch、forward/cycle execution、child/non-image、multi-dependency/reference、arbitrary depth、history/resize/device-loss与非unit authored constant仍缺；本局部owner迁移不外推这些能力 | B2 |
| Named secondary identity | `L2 typed negative` | registry 区分完整 variant；`_b`在dependency classification得到typed `unsupportedVariant`且真实负门保持零Program/GraphExecutor执行和safe compositor output，仍无secondary producer/consumer flow | secondary 数据流、copy/swap/history与官方语义 | B2 |
| Generic FBO command graph | `L2` | target/bind/compose/copy/swap/condition/function 可保留或 blocker；FBO extent、clear、target/lifetime table、bounded copy/swap/history与Precise Blur material-command interleave已有各自严格门。公共 `r8 -> .r8Unorm` Metal-storage target基础包含typed plan、1 logical B/texel预算、table/persistent allocation/lease、format-aware history账本与exact-format resource clear/copy；其上新增独立于TEX `authoredFormat`的`.scalarRedUnorm` graph content、capability携带的exact attachment kind、R8 red-only write mask、typed publication，以及launch envelope中所有实际消费该slot的已编译variant的direct `.r` use证明。exact authored `uvs` absent/repeat已进入target、allocation、publication与Program sampling identity；项目GPU门及真实`3765760121:73`两material graph共同闭合`red-defined Program写入 -> R8 repeat publication -> 后续Program显式.r -> RGBA terminal`。same-descriptor authored swap另由项目正反fixture闭合后段logical identity可见性，并在extent expression、format、address、unique或clear不匹配时于claim前typed硬拒绝。普通无typed authority的R8 attachment仍拒绝。Precise raw-compose与ordinary bounded layer-local compose保留0 authored/logical RT并由共享Program/GraphExecutor使用layer-local two-member pair；Precise显式authored-intermediate则以普通GraphTargets执行full RGBA FBO，两pass同样进入共享normalized-sample-sum Program，旧`.preciseGaussian`产品owner已撤权 | 整个Generic FBO家族仍是`L2`；R8只授权无clear、非unique、单writer、至少一个later direct-`.r` reader、无copy/swap的bounded profile，swap只授权同帧、同descriptor、单effect的一次logical permutation。Precise显式FBO只授权当前两pass、history-free、full RGBA、static scale与已证source/variant/resource envelope；真实MASK/BLURALPHA/dynamic scale不在内。`.g`/whole-vector/其他alias、任一混合variant、conditional/component/multiple writer、clear/history、多writer、无consumer、其他command、未知UV与其他格式均失败关闭；还缺load/store、其余R/RG/float格式、其他Glitter revision/topology、FBO/background/dependency compose、Refraction/scene-background capture、condition/function、跨帧logical swap和完整生命周期 | B2 |
| History RT | `L3 bounded` | 既有 `unique:true` history seed/clear 仍成立；Cursor Ripple 严格白名单允许 exact `_rt_EightBuffer1/2` 非 unique 读前写，两个 `fit 256|512` RGBA8 target 在跨帧 table 中轮转。launch-scoped execution state是唯一首次初始化owner，rehydrate后的table不再二次clear；严格Cursor Ripple history adapter由统一GraphExecutor执行。shared executor invalidation现先typed终止旧generation并清空旧history residency，下一transaction以`history=seeded / reset=executor-invalidation`建立新generation，后继恢复COW。正式scene-switch re-launch创建新的coordinator/runtime identity与独立pool；正式surface stop则先把surface/provider/context降到0，再由后续launch从空宿主创建独立runtime/pool，两类旧/new generation都各自从initial seed进入COW且physical pool不相交。apply/simulate与其余scene consumer共用host admission后的simulation delta，不叠加effect-local固定1/30秒cap。项目自有GPU门验证Cursor Ripple传播/阻断/位移；fresh Motion Blur派生fixture分别闭合executor-invalidation reset/recovery、同输入scene-switch rebuild及`1→0→1→0` surface-stop/relaunch | 只开放严格Cursor Ripple typed plan、一个Motion Blur-shaped executor-invalidation原子、一个同输入正式re-launch原子及一个同输入surface-stop/relaunch原子，不开放按拓扑猜测的通用history；device loss、任意跨Scene作者语义、点击注入、pause/seek固定步进、多surface、通用跨帧copy/swap、其他history effect、Windows动态/像素golden与官方同相位对照仍缺 | B2 |
| Scene compatibility/source preparation | `L2` | project/scene version 分别以 declared/missing/invalid/out-of-range fact 与 provenance 保存，不合成 effective version；`SceneResourceView` 之上建立 package/loose/stock 候选 source graph、include edge/content digest、active source map/schema 与 prepared/variant identity。preparation 作为中性 Program 前置能力，不独立授权 GPU owner；package -> loose -> stock 是项目 VFS policy，不是已证明的官方全版本默认 | compatibility default/patch rule、verified backend/version/platform/texture-format provider、跨语言 prelude、完整 annotation/default，以及更多能与 slot/state/color 一起进入 bounded Program 的 source 形态 | B2 |
| Authored shader path/source identity | `L3 executed-degraded` | material path、stage source/raw/canonical 与 ShaderContract identity 保真；中性 preparation/frontend 对 bounded 单 effect/单 material 形态形成 active source/reflection、static/host/dynamic uniform、texture schema 与 color/state contract，只有完整原子 Program 才能由 `resolved-material` owner 准入并经统一 graph executor 执行。三组 exact Scroll 与早期单 pass GPU fixture 只保留为历史 oracle，不再推断产品 owner 或授权旧 plan/pipeline/renderer 可达 | include/combo 仍主要是 `L2` preparation；未证 sampler purpose、复杂 render state、FBO/多 pass、更多语法、arbitrary shader 与 Windows golden 继续关闭 | B2 |
| Shader source/include/annotation/declaration contract | `L2` | 完整 source/include/annotation/declaration 保真；独立 source graph、有界 include/directive、object/function-like macro evaluator、typed `[COMBO]` 与 sampler readiness/requirement schema、有界 fixed-point、active source/schema/source map/dependency/variant digest 和 unresolved input/output color carrier 已落地；已证 slot/provider、active reflection、host/static/dynamic uniform、state 与保守 color transfer 原子形成 Template/Program，bounded 子集由 resolved executor 消费 | verified environment provider、跨语言 macro/prelude、完整 annotation default/editor schema、未证 purpose/state/color、任意 sampler/render-state/multi-pass consumer 仍缺；preparation success 不能单独升级 GPU 准入 | B2 |
| Arbitrary custom shader execution | `L0` | 上述严格 framebuffer-only 单 pass 子集不等于 arbitrary shader | 通用受控翻译/映射、安全、资源分级与产品门 | P3 |
| Puppet asset identity | `L2` | MDLV0021/0023 mesh、受限 MDLV0023 + MDLS0004 + MDAT0001，以及三来源 MDLA0006/full-TRS/80+84-byte skin weights 已交叉核验并 fail closed；v25 保存 authored animation layers | 更多 MDL/MDLA 版本、完整辅助轨道与超出已验证形状的数据 | P2 |
| Puppet runtime | `L3 executed-degraded` | bind-pose 重组（`8bac86e`）、静态 attachment（`49ee89a`）、严格单 clip（`f1ee79b`），以及 bind-referenced、驱动 bone 集两两不相交的 bounded additive 组合与 typed visibility（`0892e74b`）；冲突或畸形声明整层回退 bind pose | 插值、非 loop mode、非 1 rate/blend、重叠 bone mixing/权重、动画 attachment follow、constraint/IK/physics/events、Windows golden | P2 |
| Bounded standalone volumetric `lspot` | `L3 bounded` | 无 parent/child/effect/dependency/script、`castvolumetrics=true`/`solid=true`、单 relative Mirror angles Timeline 的 2D projector cone；作者 color/intensity/radius/cone/density/exponents 进入 premultiplied additive direct draw | 不执行 normal/PBR lighting、shadow、projection image/video/effect provider、point/tube/directional、动态脚本/audio/cursor 或通用四灯合成；见 [E-SPOT-LIGHT](runtime-evidence-index.md#e-spot-light) | Advanced |
| 2D lighting/Scene HDR | `L0` | 上述 standalone `lspot` 只画投影体，不使 image material 响应光照；layer Bloom 近似也不等于官方 lighting/HDR pipeline | PBR maps、author-enabled lit material、ambient、shadow/reflection、scene post | P2 |
| 3D model/camera/physics | `L0` | 无 Scene 3D runtime | model/node/material/skeleton/attachment/camera/physics | P3 |
| RGB device integration | `L0` | 无 Scene RGB provider/output | macOS 策略、授权和 fail-closed | P3 |
| Debug PNG readback | `L2` | benchmark 可生成 GPU 截图，并按 Steam preview 比例中心裁切、输出非阻断分项指标和并排图 | 只允许同一样本跨提交比较；不能跨样本排名、替代 Windows golden 或标成 offline bake | P3 |
| Offline bake | `L0` | 无固定步进产品 adapter | fixed clock/seed、provider replay、编码器 | P3 |
| Stop/switch lifecycle | `L3` | surface=0 和部分资源释放门 | VM/provider/GPU 细粒度计数、系统暂停与长稳 | B0-B4 |
| Performance budgets | `L0` | 无统一 CPU/GPU/显存/帧时长期阈值 | 30 分钟交互、2 小时 soak、多屏和压力门 | B0-B4 |

R3 起，本表旧 bounded executor 摘要中“property 缺失时作者 fallback”的简写统一收窄为：只有对应 exact provider identity 被显式发布为 `absent` 才表示用户未选择并允许作者候选；字典 missing、pending、unavailable、incomplete ready 或 publication identity mismatch 均失败关闭。该修正不把 R3 Program 抬为 GPU consumer。

## 4. 官方 Effect 覆盖摘要

官方用户可见 Effect 的作者条件、执行通道、公共依赖、当前证据和下一验收门统一维护在 [官方 Effect 执行覆盖表](effect-execution-coverage.md)。该表当前还混有 Workshop-specific 与内部补充行，机械读取第2-7节得到50个能力行，无法用旧的`45项：L1=16/L2=3/L3=26`重新守恒；在表格显式分开官方45项与补充行之前，禁止使用聚合等级数字规划或宣传。每个`L3`仍只表示该行写明的 bounded/exact 子集，当前没有 Effect 可据此升级为官方 parity。

## 5. Particle 子系统覆盖

逐 initializer、operator、renderer、control point 和 child 类型的证据见 [粒子组件覆盖表](particle-component-coverage.md)。本节只保留系统级摘要，不能替代逐项表。

| 官方组件 | 当前级别 | 当前边界 | 升级门 |
|---|---|---|---|
| General 字段 IR | `L2` | 常见 material、maxcount、starttime、world-space/perspective/frame-blend 与五种 author allow-override system flags 可保留 | 其余 General/material variant 与动态字段 |
| General 执行子集 | `L3` | max count、prewarm、perspective、frame blend 与 color/speed/count/lifetime/size override gate 可消费；root direct User Property / absolute Timeline 可按受限字段逐步覆盖作者 fallback | script/conflict/direct color/relative/Combined、child/存量粒子追溯、prewarm cap 与 Windows 数值门 |
| Emitter schedule | `L3` | rate、instantaneous、duration；raw bit 2 的 rate emission 按每个 emitter、每次 render callback 最多一个；Sphere/Box/Layer Image 共用 bounded initial delay；严格 `flags=4`、仅 rate Sphere/Box 的确定性 Random periodic active/delay 窗口可与合法 initial delay 组合 | one-per-frame 的 30/60/120 FPS Windows count/Rope topology、initial-delay Windows timing、periodic burst/max-per-period、audio/override 组合和 WE 时间/RNG golden |
| Emitter launch speed | `L3` | finite nonnegative ordered `speedmin...speedmax`；max-only 以 0 为下界，上界项目预算 1,000,000；无效范围零发射并诊断 | 零半径、更多 emitter/consumer 与 Windows fixed-seed velocity/trajectory golden |
| Emitter shape controls | `L3` | Directions 只准 scalar 或完整 finite 三轴，绝对值不超过项目 multiplier 预算 1,000,000；零轴停在 origin。Sign 只准 Sphere 且每轴为 `-1/0/1`；其余声明零发射并诊断 | 更多轴组合、负 Directions 官方语义与 Windows fixed-seed position distribution golden |
| Sphere Random emitter | `L3` | 可执行子集 | 全参数、distribution golden |
| Box Random emitter | `L3` | zero/absent `distancemin` 的各轴按 `±abs(max)` 围绕 origin 采样；非零 min/max 保留作者区间路径；bounded Directions 可逐轴乘法，Box Sign 失败关闭 | 更多轴组合、反向范围、负 Directions 官方语义与 Windows distribution golden |
| Layer Image emitter | `L3 bounded` | 仅唯一 typed `emitterimage` index 0、静态同 parent/同 transform 的 center-aligned plain image；shared/managed RGBA8/BGRA8 单层纹理按非零 alpha 像素中心采样 | text/puppet、动态/effect source、颜色/运动继承、bitmap 更新与 Windows golden |
| 其他 emitter | `L1` | 名称可诊断，字段和执行不足 | 独立 fixture 和作者条件 |
| 已接 initializer 子集 | `L3` | lifetime/size/velocity/color/alpha/angular velocity 常见路径；finite nonnegative exponent 以 `pow(U,e)` 施加到 scalar、vector 各轴和 color 共用插值因子；event spawn/death/follow child 的省略/`setcolor` Inherit Initial Value 可复制有限 parent color snapshot | 其他 event channel、默认/非法/极值分布、WE RNG/seed golden |
| Bounded HSV Color Random | `L3 bounded` | 七个显式 direct-number wire、normalized ordered HSV ranges 与 `1...1024` hue steps；创建时按 authored order 执行项目自有离散 hue、连续 saturation/value 和 HSV→RGB 近似，有效 instance color override 冲突失败关闭 | 官方 default、instance-color 依赖、RNG/端点、linear-vs-sRGB 与 Windows 数值/像素 golden |
| Rotation Random initializer | `L2` | 字段与 simulation 分支已接线；无最终 rotation 断言 | renderer orientation 数值门 |
| 其他未接 initializer | `L1` | control-point/remap 等可诊断或字段不足 | 逐项 fixture 与创建时语义 |
| 已接 operator 子集 | `L3 bounded` | movement/angular、alpha/size/color change、部分 oscillate、Cap Velocity、bounded Control Point Force、Turbulence、classic v1 Vortex、event-follow 的省略/`setcolor` Inherit Value，以及最多 64 粒子的 zero-separation Boids alignment/cohesion；合法 Turbulence/Vortex audio profile 分别消费 phase/speed，event color 每步读当前 parent state且不重启 child emission，Boids 使用不可变 fixed-step snapshot；未准入字段或组合继续 fail closed | 其他 event channel/type、Cap Velocity Windows blend/clamp、Control Point Force falloff/world/perspective、classic Vortex CP/center-force、`vortex_v2`、Boids separation/speed clamp/spatial index、默认 phase/system seed、curve 与 Windows 数值/轨迹门 |
| Turbulence operator | `L3 bounded` | 按作者顺序消费 mask/phase/scale/time scale/speed/blend/fixed dt/static speed override，非有限写入失败关闭；合法 Audio Response 以项目 clean-room 的 `1 + response` 调制 phase，非法或未准入 profile 继续局部失败关闭 | Windows 固定 seed、非零音频轨迹/像素 golden；官方 phase、幅度、默认值和数值公式仍未证明 |
| 其他未接 force/operator | `L1` | Maintain Distance、Reduce Movement Near Control Point、Remap、Collision、`vortex_v2` 及未准入 classic Vortex/Control Point Force 扩展明确 unsupported | 双 control-point/world-space 力场、typed channel、collision solver，以及各 bounded operator 未覆盖的字段与组合 |
| Sprite renderer | `L3` | 作者纹理和程序化静态遮罩子集；TEX filter/address/mip 进入真实 Metal sampler，UV>1 repeat、minification 与单 mip 均有 GPU 门 | clamp-border 精确值、全 material/blend/lighting/atlas 与像素 golden |
| Sprite Trail | `L3` | root/strict child 共用单 quad stretch；显式 length 按有限非负值执行，省略/`null` 采用项目默认 `1`，min/max 缺省为 `1/1`；malformed、非有限、负值与反序范围失败关闭 | 默认 `1` 是 stock corpus + 公开 orientation-only 语义约束下的 clean-room 合同；orientation/单位/atlas/曲线与 Windows 像素精度仍缺 |
| Rope declaration | `L3` | typed kind/字段进入严格 profile；单 renderer、Screen、静态纹理、2...512 maxcount，准入 subdivision `0...7`、有限 UV scale、smoothing/scroll；strict child 另允许 bounded bit-2 one-per-frame emitter 与透明 unused parent CP mapping | `segments`、length/fades、root world-space、animated texture、multiple renderer、active/unknown CP mapping 继续拒绝 |
| Rope execution | `L3` | 按单调 particle ID 的出生顺序连接相邻 live 粒子；位置用独立 Catmull-Rom/Hermite 曲线细分，端点 size/color/alpha 线性插值，支持 UV scale、equal-lifetime smoothing 和 simulation-time scrolling；child system 独立建链并保留 birth-time world origin，root/child 生成段预算分别为 4,096/8,192 | 独立 quad 的急弯接缝仍可见；scroll rate、curve/join、死亡拓扑/暂停恢复没有 Windows trajectory/pixel golden，root world-space 与 animated texture 未开放 |
| Rope Trail strict execution | `L3` | 严格 Screen 单 renderer 子集以有界历史生成 quad 段；非准入字段、组合和预算全部 fail closed | 官方数值/UV/接缝/死亡尾迹/暂停恢复语义与 Windows golden |
| Static control-point subset | `L3` | root Sphere/Box emitter 只按 authored ID 0...7 绑定 static definition offset/instance position；完整 finite static definition/instance angle 以项目自有 X→Y→Z frame 旋转 local origin、shape 与 initializer velocity，instance 当前值替换默认角。显式 declaration identity 缺失/重复/越界、malformed/动态 angle 或引用越界即零发射。depth-one static child 可在 exact raw-copy profile 下复制 finite static parent CP position | dynamic angle、adjusted/world/operator/child angle consumer、angle copy 与 Windows golden |
| Dynamic control-point declaration | `L2` | CP position/angles nested Timeline 进入 typed IR/target；user/script/pointer 仍只诊断 | 其他 binding producer 与坐标空间 IR |
| Dynamic control-point execution | `L3 bounded` | 仅 absolute Timeline position 经 per-surface snapshot 每帧驱动 root emitter；缺值回退 static | angles、relative、property/script/pointer、operator/child 与 cross-space golden |
| Child asset graph | `L3` | 可递归发现并为 strict child 建立 runtime template 至层级 2：depth-one 每声明一个 template，depth-two 仅 event 触发且按 parent asset path 去重展开一次；missing/cycle/depth-three/nested-static fail closed | runtime cycle lifecycle、depth-two static 语义与 teardown 压力门 |
| Child execution/events | `L3` | deterministic birth/natural-death queue；strict static/default-static child 在有限 authored local origin 创建一次，允许 depth-one exact raw parent CP position copy与 bounded uniform screen-plane scale，仍要求零 angles、probability=1，且目标定义自身含 event children 时不再被 nested 阻断；`eventspawn`、natural-`eventdeath` 与 no-CP `eventfollow` strict Sprite child 独立模拟、绘制、跟随和回收，并共用相同 bounded scale frame；event child 可在创建时复制有限 parent color snapshot，follow 还可逐步跟随当前 parent color，depth-two 复用 typed parent state；depth-two event child 以 (parent system, 粒子) 为 owner、事件跨层按帧串行传播；spawn/death 系统的 rate 发射窗口有界（authored duration 优先，否则以 child 粒子最大寿命为本地近似窗口），rate-only event child 不再无限累积；每 system 1,024 粒子，每 root runtime 每深度 64 systems、跨两层 128 systems/131,072 capacity | nonzero angles、event offset、XY 非均匀/镜像、Rope/nested/world-space-movement scale、adjusted/dynamic CP copy、collision/delete、size/alpha 等其他 value channel、depth-two static、stop/switch 压力门 |
| Built-in textures | `L3` | 20 个精确 key；`particle/fire/fire1`、`particle/light/light_shafts_0`、Flare 三纹理、`particle/nature/snow` 与 `particle/smoke/smoke2` 等为项目自建确定性遮罩，不等于官方资产 | `rain_drops_sheet` 等剩余高频 key、atlas metadata、多纹理 material |
| World-space declaration | `L3` | General、Movement 与 Renderer flag 分开保真和准入；不再因 renderer-only world flag 拒绝整层 | Control Point world-space 仍只有声明 |
| World-space execution | `L3` | 静态可逆 layer/ancestor world frame 下，General 固定 birth origin、Movement 逆变换作者 velocity/gravity、Renderer orientation 不继承 system rotation/scale；inline script、transform Timeline/诊断、有效 parallax、循环/奇异 frame 失败关闭 | 动态 parent/Timeline、multi-screen/camera 与 Windows 数值/像素 golden |
| Collision declaration | `L1` | 可诊断但无 solver | shape/depth/response/event IR |
| Collision execution | `L0` | 无 solver | fixed step 与 event dispatch |
| Audio-response declaration | `L3 bounded` | 五字段按粒子 schema 保真进 IR，emitter、Turbulent Velocity Random、Turbulence 与 classic v1 Vortex 共用 typed 声明和准入；只有底层 component profile 与 audio profile 同时合法时才建立 consumer，其他声明明确记录 `audioResponseIgnored` 并局部失败关闭 | 其他 emitter/initializer/operator/child audio、未知字段/组合与官方默认值；声明或 frame snapshot 本身不外推执行 |
| Audio-response execution | `L3 bounded` | 共享 16-band evaluator 消费同帧 host snapshot；当前只为 root Sphere/Box rate emitter、Turbulent Velocity Random/Turbulence phase 与底层已合法的 classic v1 Vortex speed 执行。mean、bounds normalization、默认值、`1 + response` phase 与 speed/rate scale 均为项目 clean-room 近似 | 合法 Windows 非零频谱 count/trajectory/pixel golden、官方数值/默认/生命周期，以及其他 component/child/audio 组合；不得称官方 parity |
| Sprite Sheet | `L3` | Sequence/Random frame/frame blend 子集可执行；repeat sampler 不再把超 1 UV 拉成边缘条；可信单 sequence sidecar nominal aspect 优先，raw TEX axes 像素长度回退，并随 current/next frame blend 插值普通 Sprite 几何 | trim pivot、多 sequence/multi-image 异尺寸、loop/edge、多纹理 material 与 Windows atlas golden |
| Static instance overrides | `L3` | alpha/size/lifetime/rate/speed/count/brightness/normalizedColor 有运行断言，五种 General gate 有逐项 allow/deny 门 | direct color、完整类型/range 与 Windows 状态门 |
| Direct color/control-point override | `L2` | CP position 的 static/absolute-Timeline root-emitter 子集已有数值门；static CP angle 的 bounded root Sphere/Box emitter consumer 也有正反门。本聚合行仍受 direct color、dynamic angle 与其他 consumer 无执行门限制 | direct color、dynamic CP angle 与其他 consumer 断言 |
| Dynamic instance overrides | `L3 bounded` | root direct User Property 已执行七个 scalar + `colorn`，absolute Timeline 已执行七个 scalar；每步按 layer 取 typed snapshot 并覆盖 static fallback，CP absolute position consumer 保持；static CP angle 只消费静态当前值。真实 live/Timeline 两门均无 sample/layer 分支 | script、冲突来源、direct color、relative/Combined、dynamic CP angle consumer、child inheritance、存量粒子追溯语义与 Windows golden |
| Material/blend | `L3` | `genericparticle` slot 0、additive/translucent 子集；共享 typed state 只准入 `disabled/disabled/nocull` 与 alpha unspecified/default，未知 blend/state 产生 `unsupportedBlendMode`/`unsupportedRenderState`，root 不执行、child graph 拒绝，不再静默回退 translucent；color TEX sampler 显式传到 renderer，strict REFRACT 的 slot 0 使用 `.straightAlbedo`、slot 1 使用 `.normal` typed identity，各自使用独立 sampler，background 固定 linear-clamp；同一 authored 文件只有在已上传 texture 的宽高和 mip 数仍与 container 完全一致时才可跨两种 purpose 复用，发生 albedo raster/resize 时拒绝把颜色纹理静默当 normal；普通 color/additive 的既有 premultiply 合同不变。项目自建 GPU 门已锁定 format-4 packed normal 的 CPU decode/native BC 两路径与 byte-equivalent format-0 RGBA 位移一致，并锁定 translucent/additive 的 fractional albedo × particle alpha coverage 只作用一次 | `alphawriting=default` 目前只是 bounded admission，不控制固定粒子 pipeline 的 Metal write mask；unsupported custom shader 仍可能误走自有 pipeline；官方/Windows DXT5n 恢复公式与法线数值/像素 golden、多纹理/combo、其他 render state、HDR/Lighting/Cutout、clamp-border 与 Windows 像素标定仍缺 |
| Fixed step/seed/maxcount | `L3` | particle fixed simulation step、deterministic seed、maxcount 有运行门；共享 clock 已把受控 simulation delta 送入 particle runtime 并记录 dropped-time | 真实 pause/sleep、seek/history、离线 fixed-clock 等价与 WE 数值 golden |
| Turbulent velocity initializer | `L3` | 消费 forward/right/up、phase、scale、time、speed range；finite offset 作为 forward→tangent 平面方向旋转，`scale=0` 仍保留 offset；合法 Audio Response 以 `1 + response` 乘 phase，作者 phase 0 仍无效，非法 profile 继续 fail closed | right/up/noise mapping、offset 弧度与 audio 幅度均属 clean-room；Windows 固定 seed/audio 数值、轨迹与视觉 golden |
| Delta clamp/prewarm cap | `L2` | delta 子项已由共享 clock 提供 raw/simulation/dropped/discontinuity、1 秒长帧数值 fixture 与 telemetry；聚合等级仍受 prewarm cap 缺定向预算断言限制 | 真实长卡顿/不同 FPS 与高 prewarm 压力门 |

最近完整矩阵的可见 root 粒子为 `129/130`；唯一未加载 root 是 `2974757317:85705` 的显式空 `renderer: []`，它沿 `missingSpriteRenderer` 失败关闭。初始 REFRACT root batch 为 14；RopeTrail 三个目标样本为 `3299228616=7/7`、`3743305891=1/1`、`3770444459=5/5`，后者 REFRACT 为 4。这不等于粒子组件完整兼容。晚于该完整门的公共能力分别由独立定向门证明：`5cc7ee37` 对 `2131872317` / `2419444134` / `3299228616` 执行 bounded Particle Audio Response；`2419444134` 缺底层参数的 Vortex 仍失败关闭；`3770444459` 的 zero-separation Boids 由 `1fd08811` 执行、bounded static CP-angle emitter frame 由 `7cce5fde` 执行，两条 dripping-water splash/refract Rope child 由 `319214c5` 执行，三组 water-impact droplets child 的 omitted-length Sprite Trail 诊断由 `36b9775a` 消除，root direct User Property / absolute scalar Timeline instance override 由 `32e3a928` 对 `2902406982` / `2998757800` 执行。未准入 dynamic source、Boids/Rope/Trail/audio 形态、动态 world-space、Lighting 等 unsupported component 仍在。`2131872317` 的 event-death child REFRACT 继续由独立 7 秒延迟 runtime 门证明，child 不增加 root loaded-layer 计数。上述数字只度量对应矩阵实际加载的 root layer，不代表后续粒子扩展已经进入 full45，也不代表 Windows 视觉等价。

`1fd08811` 把 Boids 从纯名称诊断升级为 typed、预算化的公共执行：只接受最多 64 粒子、flags 0、finite bounded neighbor/alignment/cohesion、显式 `separationfactor=0` 且无 audio/malformed/未知字段；每步从不可变状态快照计算邻域均值，数组倒序不改变按-ID 结果。真实 `3770444459` 的 layer 48 与官方 stock `previewdrippingwater` 都记录 `boidsBounded:boids`，最终隔离报告分别为 particle `5/5` 与 `1/1`、failed frame/sample residue 均为 0；stock ready/after 可见双侧滴水与连续下落相位。分离力、250 粒子官方预览、speed clamp 与 spatial index 继续失败关闭；没有 Windows fixed-seed trajectory/pixel golden，当前数值只能表述为项目 clean-room 近似。

`36b9775a` 修复 Sprite Trail 把省略 `length` 的合法 stock 形态误判为 unsupported：共享 plan 对 omitted/`null` 使用项目默认 `1`，root 与 child 都传入 parser 的 malformed 状态，已填写但无效的字段继续沿 `trailRendererUnsupported` 失败关闭。`3770444459` 的 8 秒固定步长 harness 在 layers 239/245/248 均观察到 `water_impact_droplets.json`，各层峰值 9 instances，stretch `1.384857...2.0`，对应 child diagnostic 为 0。最终签名 App 的真实报告 `.codex/scene-particle-spritetrail-default-child-20260802-v1/report.json` 与官方 stock `previewwaterimpact` 报告 `.codex/scene-particle-spritetrail-default-stock-20260802-v1/report.json` 均 **1/1 PASS**：前者 particle `5/5`、changed ratio `0.340743`，后者 particle `1/1`、changed ratio `0.738958`，两者 failed frame/drawable miss/sample residue 均为 0。stock 多相位截图可见水花轨迹运动；没有 Windows 同相位 golden，不能据此宣称官方缺省数值、密度、轨迹或像素等价。本批未跑 fixed13/full45。

`8b06538d` 修复 strict REFRACT 的 BC3 purpose 丢失；`d432d4d5` 再把 slot 0/1 分别绑定 `.straightAlbedo` / `.normal`，并以自建 GPU fixture 锁定单 image CPU BC3 decode、多 image native BC3 直传与 byte-equivalent format-0 RGBA 的位移一致、G/A swapped 与 neutral normal 负对照，以及 translucent/additive 的 coverage 单次作用。当前工作区又要求同源复用前 texture 宽高/mip 数与 authored container 完全一致，避免 X-Ray 所需的 opaque albedo resize 扩散为 normal data resize。`3770444459` 的 8 秒固定步长历史门确认 layers 239/245/248 分别有 72/77/56 个非空 REFRACT 活动帧；当前隔离签名 App 门仍为 5/5 particle、REFRACT 4、failed frame/drawable miss 0。等级保持 `L3`：这只闭合项目内部存储/合成合同；当前没有官方/Windows DXT5n 恢复公式、法线位移 golden，也没有证明水花密度、生命周期或像素等价。

`40768dfc` 为 child definition 保留 `parentcontrolpoint` 与 raw-copy flag，并只准 depth-one static、CP `0...7`、唯一 finite static root source、零 child offset/angles 的 exact raw-copy profile。动态、event/nested、adjusted、malformed 与越界形态均 fail closed。隔离真实 `3770444459` 的 layer 48 记录 `rawParentControlPointCopyBounded:mappings=2`，5/5 particle、425/425 completed frame、failed frame/drawable miss 0；全景截图正常且有运动，但不能隔离滴水位置、angles 或证明 Windows parity。

`c6fc9c24` 修复 root emitter 把 authored CP identity 误当数组下标优先的问题，并在 emitter consumer 边界只准所有显式 declaration 都有唯一 0...7 identity；合法但未声明的 CP 仍使用 system zero，缺失/重复/越界 identity 与越界 source 均零发射并记录 `controlPointEmitterUnsupported`。隔离真实 `3770444459` 的 layer 48 记录 `controlPointEmitterBounded:sources=2`，particle 5/5、submitted/completed/failed `426/425/0`、drawable miss/sample residue `0/0`；ready/after 为完整彩色场景、changed ratio `0.301361`。这只证明真实多 emitter 链路与整景稳定，不隔离 CP angles、坐标转换、滴水轨迹或 Windows parity；未跑 fixed13/full45。

`938630a0` 把 emitter speed 从会静默交换反向边界的通用 range helper 收窄为有限、非负、有序且上界不超过 1,000,000 的项目准入；max-only 以 0 为下界，正 min-only、负值、反向、非有限与超预算声明均零发射并记录 `emitterSpeedUnsupported`。隔离真实 `3290491250` 的 layer 49 记录 `emitterSpeedBounded:sources=1`，particle 1/1、submitted/completed/failed `425/425/0`、drawable miss/sample residue `0/0`；灰色是 authored 背景，人物、标志和移动星粒子可见。该门证明真实 speed declaration 进入运行链且整景稳定，不证明 WE 的缺省值、随机序列、轨迹或像素等价；未跑 fixed13/full45。

`ce16de08` 把 emitter shape 从“解析到就执行”的分支收窄为独立准入：Directions 只接受 scalar 或完整 finite 三轴且绝对值不超过 1,000,000 的项目 multiplier 预算，Sphere Sign 只接受每轴 `-1/0/1`；malformed、短向量、非有限、超预算、非法 Sign 与 Box Sign 均零发射并记录 `emitterShapeUnsupported`。隔离真实 `3750342273` 的 layer 52 `Snow perspective` 记录 `emitterShapeBounded:sources=1`，particle 1/1、submitted/completed/failed `420/420/0`、drawable miss/sample residue `0/0`；黑白山景、时钟与多尺度雪粒子完整可见，ready/after 雪点位置和密度变化，changed ratio `0.092006`。该门证明真实 `directions="1 0.03 1"` / `sign="0 0 1"` 进入运行链且整景稳定，不证明 WE RNG、分布、轨迹或像素等价；未跑 fixed13/full45。

`7b1d36f` 的 Turbulence 定向门为 5/5，覆盖旧 full45 日志中 19 条 `unsupportedOperator:turbulence` 的 14 条；另 3 个命中样本没有在新实现上复跑。该批没有改变 particle root loaded 计数，也没有重跑 fixed13/full45。

`d19e76b` / `d792296` / `f4e8a0c` 依次补齐 TEX sampler、常见随机分布/Box/lifetime oscillation 与实际 mip 链。分布构建对 `2470144420`、`1937925563`、`3742133044` 各 1/1；最终 mip 构建对 `2470144420`、`3742133044` 各 1/1。`2470144420` 的 Sakura 命中 repeat、Turbulent Velocity offset、size exponent 2 与 7-level mip，Stars 命中 centered Box/Oscillate Alpha、但其 `halo_6` 是作者单 mip，因此 mip 修复不应改变星点。`3742133044` 的默认可见雪花命中 RG88 多 mip 保留；隐藏樱花不计入该门。该批没有样本 ID 分支，也没有重跑 fixed13/full45。

`5911049` 把 Oscillate Position 从按秒龄逐帧积分的 velocity-like 增量改为每个粒子 normalized lifetime 的精确波形差值，并让 mask 的非 0/1 数值成为轴向幅度权重。保留 full45 提取中该 operator 命中 16 样本、38 个 root layer usage、24 个 sample-definition path；定向 `2067939514`、`3088601835`、`3299228616`、`3742133044` 为 4/4。数值门锁定 lifespan independence、1 秒与 1/60 秒 fixed-step 同时相一致、完整周期无漂移和半权重 mask；没有 Windows fixed-seed trajectory golden，不能升级为 `L4`。`2470144420` 的 Sakura/Stars 都不使用该 operator，因此本批不宣称修正其剩余花瓣/星点残差。

## 6. 动态运行系统覆盖

### 6.1 Timeline 与 SceneScript

> 当前纠偏：下表中的 **48/48** 是既有 44 个已解包 `scene.json` 快照，不是对当前 Workshop 根的实时总数。当前代码能力已经扩大为 effect constant 严格 1–4 维 typed shape，并有 vector3 representative-content 的 shared Program/GPU/compositor/next-frame 正证，以及真实 `3748311238:728` 两个 vector2 Shake consumer 的 Program/GPU/publication/compositor 与局部 frame fault 恢复证据；corpus fingerprint/schema 未在本批正式刷新，因此旧快照数字只保留其原始统计边界，不再用于声称“当前根全部枚举”。

| 能力 | 当前级别 | 升级门 |
|---|---|---|
| Particle `animation` wrapper presence | `L2` | CP position/angles 的 nested wrapper 已进正式 Timeline IR；其他 particle instance field 仍只保留 presence/诊断 |
| Timeline object/property target | `L3` | 现役 Workshop census 为 effect constant 23、layer alpha 5、relative layer transform 9、root particle scalar override 7、bounded text `maxwidth` 2、bounded camera origin/zoom 2，共 **48/48** 个 authored host typed 执行；5 条普通 transform 走共享 world-frame，4 条 `lspot` angles 保持 strict spotlight consumer，camera 两成员共用 owner clock 并原子进入 projection。CP position/angles 另有 stock-wire/project-fixture typed target，只有 absolute position 的 root-emitter consumer；不得外推 generic target |
| Keyframe/value/tangent | `L3 bounded` | 帧号/值/双侧 handle/`lockangle`/`locklength` 无损保真；缺失/`null` handle 合法，非 object 报 `invalidTangent`。evaluator 以 segment-normalized X 与 property-offset Y 消费 `start.front` / `end.back`，enabled X 越出各自 segment 或已消费 control value 非有限时整条 fail closed；一侧禁用退化到端点、双侧禁用严格线性。44 场景 178 个 enabled front/back 均满足范围，四个样本含自定义 handle；单位与曲线是 clean-room 合同，仍无 Windows 同相位 golden |
| Loop/Single | `L3` | 按绝对 scene time 求值，真实执行 loop 7、single 21；single 到末帧保持末值不回绕，loop 跨周期同相位，有单测与真实样本双证 |
| Mirror | `L3` | evaluator 与单测覆盖三角波折返；随包 6 处 relative Mirror layer transform 已形成 additive binding，其中 `2938612768` 的普通 origin 走共享 world-frame，`3768903841` 的四束 `lspot` 继续走 strict spotlight consumer；端点采样仍无 Windows 定标 |
| start paused | `L3` | 6 处执行且恒停首帧；`2067939514` 为负门——无 VM 时不存在能调 `play()` 的主体，自动播放会偏离作者意图 |
| wrap-loop | `L3 bounded` | 9 条 Workshop 声明与 2 条随包 stock 声明共同约束跨周期闭合段；只准 Loop 与 `[0,length)` keyframe，按末 `front` / 首 `back` 求值并进入所有现役 typed consumer。官方只定义编辑器自动创建平滑过渡，不公开私有 wire/数值；无 Windows 同相位 golden |
| `relative` 合成 | `L3 bounded` | 9 处 layer transform 均编译为 typed additive binding；5 条 ordinary `origin`/`angles`/`scale` 走共享 hierarchy-aware world-frame，4 条 `lspot` angles 保持 strict spotlight consumer。camera origin 另按相同 additive 合同进入 bounded Combined camera target；非 layer-transform relative 继续 fail closed。公开官方页未定义私有 wire/合成公式，当前“作者基值 + 动画偏移”合同仍是 clean-room 校准，无 Windows golden |
| Combined Animations | `L3 bounded` | 双向 key 引用已保真；唯一真实 `default` 2D camera `origin` owner + `zoom` child 组共用 owner clock、同帧求值并原子提交。坏引用、多 path、未知 camera/queue、非 reciprocal 组及普通 Combined 整组 fail closed；不代表 generic Combined |
| Animation Events | `L0` | frame crossing、loop、同 layer script dispatch |
| Property binding IR | `L1` | 文档级保存 inline source、scene/object/effect/pass owner、完整 target path、properties、authored fallback 与 JSON value type；已证五类 target 有 13-shape 自建门；nested/未知 owner 不提升，`script + user` 冲突 fail-closed |
| Fixed native text profile | `L0 product owner / historical` | 七个exact source/property profile已于B18删除；旧报告不代表现役执行，只有语法落入bounded AST的源码才可能建立typed text target |
| Bounded text `update(value)` subset | `L3 bounded` | 唯一 exported update 的无循环 AST 直接求值 primitive properties、`new Date()` getter、条件与字符串装配；三层预算，未知语法/值失败关闭 |
| Fixed native 64-band Audio Bars profile | `L0 product owner / historical` | B21已删除两个exact source/profile与64-draw renderer；旧准入和报告仅作历史，不执行JavaScript，也不创建动态layer |
| Generic file/module/schema-type/handle IR | `L0-L1` | inline binding 的 owner、完整 path 与 JSON value type 为 `L1`；file/module dependency、schema-resolved Vec/value type、runtime handle identity 仍为 `L0` |
| ECMAScript VM | `L0` | 安全隔离、确定性 budget、异常处理 |
| `init`/通用 `update` 生命周期 | `L0` | bounded text subset 只纯函数求唯一 text update，不建立脚本实例；每屏实例、同帧 mutation、stop teardown 仍缺 |
| `engine` globals/Math | `L0` | 无 host bridge 或标准 Math runtime |
| bounded text `new Date()`/getter | `L3 bounded` | 同帧 wall date 注入纯 Swift AST evaluator；Gregorian 当前时区，尚无 locale/DST/离线 clock adapter/Windows golden |
| user/cursor/audio/media events | `L0` | generation queue、顺序、异常隔离 |
| component/object/particle API | `L0` | typed handles、只允许作者目标、失效语义 |
| timer/timeout/interval | `L0` | scene-time scheduler、pause/resume/预算 |

### 6.2 User Properties

完整控件和 target 计数见 [运行输入与属性覆盖表](runtime-input-property-coverage.md)。2026-07-22 的 21 样本 census 为 424 definitions、952 bindings、195 条 conditional bindings；本轮 26 样本完整运行门没有重做这项专项 census，因此不把旧计数冒充当前全集。layer alpha 73 条已编译并由当前 image/solid/text consumer live 执行；layer color 73 条全部指向 solid，其中 25 条属于纯 color key 可 live，`3122339805:basecolor` 的 48 条因同键还含未支持目标继续重建；exact Local Contrast strength 1 条与 `2902406982` 的 stock Opacity direct binding 已由 strict consumer live 执行。SceneScript Opacity candidates 继续计入 unsupported/fail-closed，不冒充 direct binding。

| 类型/行为 | 当前级别 | 当前边界或升级门 |
|---|---|---|
| Catalog/bindings | `L3` | 2026-07-22 的 21 样本专项 census、format 22 binding program 与受控 fallback；direct text content/point-size/color、Local Contrast/Opacity live，mixed/hidden/no-consumer/SceneScript fail closed |
| `color` | `L3` | UI/持久化/solid-only live consumer；补颜色空间、non-solid 与全部 target |
| `slider` | `L3` | min/max/default/step/fraction/precision UI；layer alpha、exact Local Contrast strength 与 exact stock Opacity alpha 已 live，其他 target 依 consumer 决定重建 |
| `bool` | `L3` | 条件/部分 target；不得按名称自动启用 effect |
| `combo` | `L3` | option value/条件；补全部 authored target |
| `textinput` | `L3` | 可编辑/持久化；有效可见 direct text consumer 可无重建更新，其他 target 仍重建 |
| `texture`/`scenetexture` | `L3` | PNG/JPEG picker/bookmark/static consumer 极窄子集；补 video/variant/general material |
| `usershortcut` | `L0` | parser 当前归为 unsupported；需 macOS 授权和安全降级 |
| group/order/condition | `L3` | 独立窗口已支持；补嵌套/全条件和负向门 |
| reset/default/override | `L3` | layer alpha reset/override 可原子 live 提交；texture bookmark 或非 live key 仍重建；补跨重启 UI 门 |
| Texture Variants | `L0` | 补 schema、checkbox/combo 选择和 provider identity；脚本不得切换 |
| property update event | `L0` | typed snapshot 后再派发给 SceneScript |

### 6.3 Text、Audio、Media 与 Provider

| 能力 | 当前级别 | 当前边界或升级门 |
|---|---|---|
| 静态文字内容 | `L3` | CoreText 可见；结构样本 `79/108` |
| 字体解析/fallback | `L3` | 解析顺序 alias → 包内文件 → 客户端自带（stock）→ 缺失，路径安全判定优先于后三层。官方 `systemfont_*` 别名共 8 个（`arial`/`verdana`/`segoe`/`sansserif`/`consolas`/`comicsans`/`cambria`/`calibri`），`consolas` 缺失时退到 Menlo 保住 fixed-pitch。客户端 `assets/fonts` 的 15 个官方名字全部物理位于 `SceneStockAssets.bundle/assets/fonts`：8 个原版报 `stockBundled`；7 个替代字体按官方文件名读取，报 `stockSubstituted` + `stockFontSubstituted:<category>`。包缺失或损坏才报 `stockApproximation`。**等级仍为 `L3`**：7 个替代字形的轮廓、advance 与 kerning 不是官方字形，8 个原版也无 Windows 栅格化逐像素对照，见 [E-TEXT-FONTREF](runtime-evidence-index.md#e-text-fontref) |
| point size | `L3` | 官方 `pointsize` 是 300 DPI 磅值（`lib.sceneScript.d.ts` 的 `ITextLayer`："Size of the font in points for 300 DPI"），栅格像素字号取 `pointsize * 300 / 72`，随包 `dino_run` 两个记分标签的作者 size `780x291`/`390x145` 四个数字逐位复现（此前的 `round(pointsize * 4)` 低 4%）。字号仍被夹在 `[1, 1024]` px，作者值 ≥ 245.76 磅起偏离官方换算，这是本地纹理保护不是官方合同；数值判据来自本机只读探针而非仓库内自动门，也无 Windows 逐像素对照，见 [E-TEXT-POINTSIZE](runtime-evidence-index.md#e-text-pointsize) |
| baseline/alignment | `L2` | 基线（baseline）本身未处理，`blockalign` 未解析；对齐字段的执行边界见下一行，溢出裁剪见 overflow limits 行 |
| text alignment pivot（`horizontalalign`/`verticalalign`）与 padding | `L3` | 官方取值域 left/center/right × center/top/bottom 全部参与 quad pivot；作者 `size` 是已含 `padding` 的外框，边对齐把去掉 padding 后的内容边钉到 origin，center 保持几何中心，缺省与未知值退回中心；同一组字段继续喂 CoreText。动态内容仅在 `limitwidth=false` 时取不小于作者外框的 intrinsic+padding size，并把同一 render size 传给 provider capture、effect/FBO 与最终 quad，见 [E-TEXT-PIVOT](runtime-evidence-index.md#e-text-pivot) / [E-TEXT-SCRIPT](runtime-evidence-index.md#e-text-script) |
| screen anchor（text layer） | `L3` | 官方 10 个取值全部解析并每帧折进 layer 平移，`none`/缺省/未知取值不偏移；离屏 GPU 门下 21:9 的 `dino_run` 记分标签从 0 覆盖像素回到与 16:9 完全一致的 396 px 右上角；只有 text layer 携带该字段，Windows 像素标定见 [E-TEXT-ANCHOR](runtime-evidence-index.md#e-text-anchor) |
| overflow limits（`limitrows`/`maxrows`/`limitwidth`/`maxwidth`/`limituseellipsis`） | `L3` | 五个字段随 interpretation v29 进 text IR 并由 CoreText 栅格化消费；两个数值只在对应开关打开时生效。`limitwidth=true` 的动态文字继续遵守作者/`maxwidth` 限制；`limitwidth=false` 的动态内容按 intrinsic size 扩到不小于作者框，避免 clock/date/day 被占位符框裁断。静态文字布局不变；官方省略号和动态外框无 Windows 逐像素对照，见 [E-TEXT-LIMITS](runtime-evidence-index.md#e-text-limits) / [E-TEXT-SCRIPT](runtime-evidence-index.md#e-text-script) |
| outline/shadow/text effects | `L1` | 字段或 effect 可保留，未形成完整绘制链 |
| property-driven text | `L3` | direct user property与bounded text update subset经per-frame typed snapshot、按layer generation/stale cancellation和动态纹理consumer；七个fixed native profile已退役，其他target仍依consumer决定重建 |
| SceneScript clock/text | `L3 bounded` | 只有唯一exported `update(value)`的公共无循环Date/string AST可按语法准入；不支持`clockWithPeriod`、greeting或其他语法时保留authored fallback。B18前七profile运行报告仅为历史；完整VM、timeOfDay/engine、init/event/handle/module仍为`L0` |
| Audio declaration | `L3` | effect 与粒子两套 schema 分别保真解析；两者字段名不同，粒子无 `audioamount` |
| Audio 16 bins | `L3` | left/right host-shared 快照，静音/无权限/停采集稳定归零，可注入；当前 Scene 分析器以 -60 dB 下限扩大相邻条幅度差，频段、下限与归一化仍为工程选择 |
| Audio 32/64 bins | `L3` bounded | left/right host-shared snapshot与同次FFT已接；供统一Program中的Simple Audio Bars 32/64-band作者shader。当前`2938612768:563`默认32档与`3122339805:64`显式64档数组均由统一Program实际编码并取得非零可见柱高变化；旧Simple专用consumer及fixed SceneScript Audio Bars consumer均已删除，其他Workshop/SceneScript不外推 |
| Audio effect consumer | `L3` | stock Shake（`whitePhaseFallback`/`timeOffsetCombo`）、exact old-editor Shake（`legacyUnconditionalPhase`）、stock Pulse（`stock2842`，并接受同一精确旧版 profile 缺省 `replacementkey`）继续使用各自bounded consumer；可静态证明的`3122339805:64` ordinary、`2938612768:563` composition与`3299228616:151` relocated Simple Audio Bars正例均由Program消费16/32/64 host数组，后者保留上下双向及后续zero-distortion Fisheye顺序。旧Simple专用consumer已删除，其他Simple combo/topology必须形成完整Program或失败关闭。其余legacy指纹、其他Workshop audio shader、未支持combo与SceneScript binding fail closed |
| Audio particle consumer | `L3 bounded；新增script projection S2` | 既有16-band snapshot已由bounded emitter rate、initializer/operator phase 与classic Vortex speed消费，精确边界见5节Audio-response execution；`311115e3`另接入bounded audio-scaled particle rate/layer scale，但无fresh runtime证据 |
| Fixed native property-script audio bars | `L0 product owner / historical` | B21已删除两个strict source profile、逐bin average、单纹理64-draw与产品route；不产生JS object或Scene layer handle，旧报告只作历史 |
| Generic SceneScript audio API | `L0` | 无 VM、`registerAudioBuffers`、`AudioBuffers` object/array identity 或订阅 lifecycle |
| Sound layer | `L0` | `3743305891` authored FLAC 尚未解码/播放，也未成为 Scene 频谱输入；当前 system tap 排除本进程。需补 sound content IR、提取/播放、volume、生命周期与自有频谱合同 |
| Embedded MP4 frame | `L3` | image-layer 子集；不同于系统媒体 provider |
| Media playback/colors/title/artist | `L2 wired` | producer-agnostic inbox、独立generation与bounded color/text/playback consumer已接线；本轮只由代码和未重跑的自动测试证明，visible unknown |
| Media status/album/timeline/live producer | `L0` | injectable atomic snapshot、platform lifecycle、missing-field语义与generic VM/event consumer |
| Media thumbnail identity | `L3 bounded current cover` | current `$mediaThumbnail`经frame registry显式generation消费；replacement pending保留last-ready generation，ready后原子换代。B20已删除previous identity/publication与transition输出；见 [E-MEDIA-THUMBNAIL](runtime-evidence-index.md#e-media-thumbnail) |
| Media thumbnail producer/consumer | `L3 bounded current cover` | producer-agnostic current-only inbox、16 MiB输入预算、最长边256异步decode、旧request协作取消/stale completion拒绝、pending last-ready、clear/decode-failure最终作者fallback，以及严格normal/full-strength Blend current source/visibility consumer；单次ImageIO调用不可抢占，且无live platform producer、generic event/SceneScript、previous/transition/variant |
| Layer/named target provider | `L3 bounded` | classic primary named-provider由共享`.resolvedMaterial` binding声明primary backward composition provider，frame reservation和provider capture复用同一MTLTexture，只有成功capture才发布ready，普通MaterialProgram slot 1与GraphExecutor在同一epoch/object消费；同一provider被两个consumer共享、两个独立provider各一consumer的`9000000700` fresh atom均达到`S4 slice-visible`，但只覆盖登记的bounded primary topologies。该profile已`generic-only`并删除旧Clipping owner。bounded hidden-image effectful closure则保持独立named reservation，把provider GraphExecutor final按command order复制后沿同一registry发布，并让中间provider终态保持`compositor=false`。worleyColorV1仍由B13独立typed owner执行，`3768903841`的provider 42→consumer 49沿其合同运行。R3 registry继续保留exact graph identity与ready/incomplete/absent/pending/unavailable/missing区分；effectOutput/FBO与secondary、forward/cycle execution、child/non-image、multi-dependency/reference、arbitrary depth及history/resize/device-loss仍未形成通用publication |
| Named secondary identity | `L2` | registry 已区分完整 `_b` variant |
| Named secondary producer/consumer | `L1` | executor 识别后拒绝；需真实数据流 |
| Property file provider | `L3 bounded` | PNG/JPEG 静态 consumer 极窄子集；R3 material/Blend 只在 exact identity 被显式发布为 absent 时回退作者候选，missing/pending/unavailable/incomplete 全部失败关闭；仍缺通用 GPU consumer、video/variant |
| Video/system provider identity | `L2 carrier` | R3 Template/registry保留exact system demand/publication state；purpose冲突、publication不完整或lifecycle未证时为unavailable，不伪造absent |
| Video/system generic producer | `L0` | 尚无通用 provider lifecycle |
| Generic material slots 0...7 | `L3 bounded` | Template 固定保留 8 项及 hole，Program 在同一 snapshot 原子携带 provider publication、active variant/reflection、uniform/state/color 与 exact/semantic identity；R4 bounded 子集已由 resolved pass encoder 和统一 graph executor 完成 GPU/compositor/next-frame 闭环，未证 slot/purpose/readiness 仍失败关闭 |

## 7. 高级对象与输出覆盖

逐 Puppet、Model、Lighting、产品预算、RGB 和 offline 项见 [高级对象覆盖表](advanced-object-coverage.md)。

| 能力族 | 当前级别 | 最小可用门 |
|---|---|---|
| Puppet asset identity | `L2` | 更多 MDL/MDLA 版本、辅助轨道和完整资源图 |
| Puppet mesh/bones/weights runtime | `L3` bind pose + 静态 attachment + 严格单 clip / disjoint-bone additive CPU LBS | 插值、冲突 mixing/权重、GPU skinning、动态 attachment follow、层级/遮罩 |
| Puppet spring/rigid/rope/wind | `L0` | fixed timestep solver、events、确定性 golden |
| 2D PBR maps | `L0` | normal/roughness/metalness/emissive slot 与 color space |
| Bounded standalone volumetric `lspot` | `L3 bounded` | 四束 authored cone 已按 source order、world origin 与 relative Mirror angle Timeline direct draw；无 lit-material interaction |
| Generic point/spot/tube/directional light | `L0` | typed 四灯 IR、author-enabled material、ambient、provider 与完整排序/坐标链 |
| Generic lit-material shadow/reflection/light volume | `L0` | standalone `lspot` cone 不含 material interaction；仍需 RT graph、depth/occlusion、预算和像素门 |
| Official Scene Bloom target identity | `L1` | typed scene target 已定义；binding 尚未分类 |
| Official Scene HDR/Bloom post runtime | `L0` | HDR targets、tone mapping、scene ordering |
| Workshop layer Bloom approximation | `L3` | 受限 layer effect pipeline；不得冒充 Scene post |
| 3D model/node/material | `L0` | asset loader、scene graph、camera、PBR material |
| Skeleton/attachment/animation | `L0` | animation evaluator、skin/attachment 生命周期 |
| 3D physics | `L0` | fixed timestep、collision、determinism |
| Custom shader reference/path/source identity | `L3 executed-degraded` bounded subset | material path 与 authored stage source/raw/canonical identity 已保存；独立 source graph 保留候选、依赖与 provenance，active source/frontend、slot/provider、uniform/state/color 再原子形成 Program identity。bounded Program 只由 resolved-material graph executor 执行；三组 exact Scroll 与早期 GPU fixture 仅是历史 oracle，不再形成第二个产品 owner，也不代表任意路径/source可执行 |
| ShaderContract/source preparation | `L2` | source/include/annotation/declaration/stage已保留；bounded include/directive evaluator、typed combo/readiness schema、active source map/digest与unresolved color carrier已落地。R3的Template/Program进一步原子承载8槽provider/default、reflection、uniform/state与保守resolved color子集；跨语言prelude、verified environment provider、未证purpose/state/color与GPU consumer仍缺 |
| Resolved material Template/Program | `L3 bounded` | production raw graph形成固定8槽Template；同代frame/resource/dynamic snapshot形成exact/semantic Program identity。shader compiler先消费exact source-key artifact，route由prepared source color fact、active sampler、typed texture/graph-format与stage-active uniform fact共同决定。source-proven scalar interpolation、opaque scalar output、straight-alpha R8 signal、source-independent premultiplied output、internal-framebuffer passthrough、normalized sample-sum、graph-input alpha attenuation、color blend、conditional straight union、双stage active-uniform graph-input passthrough、active sampler恰为graph-input加defaulted opacity-mask auxiliary的stage-uniform straight-alpha-preserving、无auxiliary且active sampler精确为唯一graph-input的stage-uniform straight-alpha-preserving、同一graph-input上具完整stereo audio数组与安全局部化mutable fragment varying的straight-alpha stage-uniform、唯一sampler alpha mutation、same-slot RGB channel reconstruction与auxiliary RGB blend alpha-preserving十六个profile缺省`generic-only`；更宽的graph-input straight-alpha与straight-alpha-preserving因真实compiler language反例现为`prefer-generic`，其余ordinary/provider-backed profile也保持`prefer-generic`。alpha-attenuation另要求exact previous-current、regular source sampler、只含opacity-mask auxiliary且active sampler全集与源码事实一致；named/internal framebuffer、history、provider、其他effect output、混合candidate及auxiliary graph texture均拒绝。linked reflection按实际stage member过滤共享UBO超集；launch envelope预编译reachable variant与Metal pipeline，frame只匹配冻结entry，并把output storage、graph texture format与active graph slot纳入缓存不变量。Program output contract正交区分`.color`与`.scalarRedUnorm`；V1 material/copy/history、named-provider、R8 repeat与bounded layer-local compose继续走同一GraphExecutor，eligible visual preparation/finalization失败只恢复whole-effect previous current。classic primary named-provider、source-proven scalar interpolation、source-independent premultiplied output、internal-framebuffer passthrough、normalized sample-sum、stage-uniform graph-input passthrough、stage-uniform straight-alpha-preserving、无auxiliary stage-uniform straight-alpha-preserving、audio stage-uniform straight-alpha、alpha-attenuation、color blend、conditional straight union、唯一sampler alpha mutation、same-slot RGB channel reconstruction、auxiliary RGB blend alpha-preserving及R8 two-node两个profile已完成owner migration；两个宽泛graph-input alpha profile仍仅保留bounded `slice-visible / prefer-generic`。sampler identity、internal target/active pass/cross-variant divergence等不安全错误仍hard reject，typed visual fallback不执行未证明script或伪造provider/uniform/texture。证据见[E-V1-GENERIC-SCALAR-INTERPOLATION](runtime-evidence-index.md#e-v1-generic-scalar-interpolation)、[E-V1-GRAPH-INPUT-STRAIGHT-ALPHA](runtime-evidence-index.md#e-v1-graph-input-straight-alpha)、[E-V1-STAGE-UNIFORM-GRAPH-INPUT](runtime-evidence-index.md#e-v1-stage-uniform-graph-input)、[E-V1-FBO-UV-ADDRESS-REPEAT](runtime-evidence-index.md#e-v1-fbo-uv-address-repeat)、[E-EFFECT-OPACITY](runtime-evidence-index.md#e-effect-opacity)、[E-EFFECT-WORKSHOP-SHIFT-HUE](runtime-evidence-index.md#e-effect-workshop-shift-hue)、[E-EFFECT-WORKSHOP-GRADIENT](runtime-evidence-index.md#e-effect-workshop-gradient)、[E-EFFECT-WORKSHOP-ENHANCED-AUDIO-BARS](runtime-evidence-index.md#e-effect-workshop-enhanced-audio-bars)、[E-V1-NARROW-GRAPH-INPUT-ALPHA-OWNERS](runtime-evidence-index.md#e-v1-narrow-graph-input-alpha-owners)、[E-V1-AUXILIARY-RGB-BLEND-OWNER](runtime-evidence-index.md#e-v1-auxiliary-rgb-blend-owner)与[E-V1-ORDINARY-SHADER-REGRESSION-RECOVERY](runtime-evidence-index.md#e-v1-ordinary-shader-regression-recovery) | bundled worker/Program仍仅闭合登记子集。Motion Blur accumulation/combine、layer 187双Foliage stage、strict alpha-attenuation、Shift Hue、Workshop Gradient、Enhanced Audio Bars与R8 two-node prepare/combine保留各自`generic-only / genericCompilerArtifact`证据；真实`3765760121:96`与隔离派生`9000000622:112`现分别由两个窄profile以`generic-only`持有；共享宽泛profile仍为`prefer-generic`。`3767460992` mixed chain现为26/26 active effect全部shared Program、0 dedicated。Shift Hue与Workshop Gradient当前窄profile的默认generic artifact、坏artifact effect-local previous-current与`disable-generic` shared frontend回滚均已闭合；Enhanced Audio Bars同样闭合默认generic artifact与坏artifact局部previous-current，但显式`disable-generic`只证明安全撤route而非视觉等价frontend回滚；其他effect-output graph reference、cross-layer/provider-backed、compose首node与ordinary profile仍可有typed `boundedSwift` fallback。更多language/state/resource/multi-pass/FBO/history/dependency、Fast Suite、V1其余owner migration与官方parity仍缺，不得写成arbitrary shader/material、V1完成或整个family `generic-only` |
| Custom shader execution | bounded subset `L3 executed-degraded`；arbitrary `L0` | 现有 bounded single-pass framebuffer subset可执行；2026-08-08 pre-R4-A+C full45曾记录14样本 / 57 resolved layer / 75 exact subject，其中普通Opacity、Color Key与41个Tint subject由统一Program owner持有。该历史聚合没有被本轮刷新。R4只通过公共frontend/binder/color/capability合同准入，不按sample/layer/path/hash选择；旧`.authoredShader` backend/pipeline cache/Scroll profile已撤权，三组exact Scroll只作历史oracle | include/combo、未证sampler/purpose、多pass、复杂render state、任意source/GLSL、Opacity/Tint未迁移形态、其他dedicated/legacy/utility/audio owner撤权与Windows golden仍缺；bounded Program证据不等于通用shader兼容 |
| RGB device | `L0` | macOS 产品策略、授权、设备 adapter |
| Debug PNG readback | `L2` | 可生成 benchmark 截图证据；不得标成 offline bake |
| Offline bake | `L0` | 与实时共用 IR/evaluator/render graph，固定时钟和编码输出 |

## 8. 现役路线映射

本页不再维护 B/R/Coverage-first 实施顺序。current capability、边界与待办仍由本页汇总；[Scene 兼容执行路线](../scene-compatibility-roadmap.md)只拥有 V0-V5 的执行顺序和结果声明口径，其中任何能力族摘要都只是返回本页/专项表的导航，不是第二份 current 状态：

- V0：ordinary authored shader/material 通用编译、Program、GPU 与 compositor；
- V1：ordered pass、FBO、copy/swap/compose/history/named dependency；
- V2：真正 ECMAScript VM、handle、event、timer 和 mutation lifecycle；
- V3：Particle component registry/interpreter；
- V4：properties、pointer、audio、media、text 与 provider；
- V5：Puppet、lighting/HDR、3D、RGB、offline、性能与发行。

所有后续 V1–V5 原子都不得按 Effect 名称新增 strict backend；普通作者程序优先进入通用 backend，一个 stage/quad 失败只收窄到真实 effect/依赖面。当前段位和下一 atom 只由[现役路线](../scene-compatibility-roadmap.md)选择，旧 B/R 标签仅用于追溯已有证据包。

## 9. 更新规则

1. 每次 Scene 能力提交必须更新本表对应行和精确边界；只更新开发流水账不算完成。
2. 本页系统行升级必须写明其 bounded 子集、结构/执行/可见证据和未证明边界；专项 `L0-L4` 只按该专项自己的等级定义升级，不得套用另一专题的同名阈值或机械换算为 `S0-S5`。
3. 新发现的官方能力先补 [官方页面全目录](official-page-catalog.md)、[页面能力映射](official-page-crosswalk.md) 和专题语义，再进入本表；私有字段按 [资料来源与证据索引](source-index.md) 标证据等级。
4. 当前 baseline、矩阵报告、测试总数和签名 App 身份只以 [运行证据索引](runtime-evidence-index.md) 为主答案；本表只维护系统摘要与专项入口，带日期的文档全部属于 history。语义自动门校验布局合同与相对链接，不假装同步这些动态事实。
5. 开发开始顺序：先看[现役路线](../scene-compatibility-roadmap.md)的当前可见目标，再从本表和专题合同定位首断边；不得按本表行号或旧批次标签排队，也不得凭截图写视觉特判。
6. 总表每行只允许一个本行摘要等级；若同一能力同时存在 IR、wired、executor 或 visible 子集，必须拆行或明确写成 bounded current + missing remainder，不能把不同专项的 `L`/`S` 拼成一个可比较等级。
7. 每行至少要能追溯到专项表中的代码、测试和运行证据；只有 parser 或结构时不得写成执行支持。
