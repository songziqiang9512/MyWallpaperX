# Scene Render Graph 与 Shader 覆盖矩阵

> 现役 graph-input owner 窄化：真实 `2938612768:563#effect#565` 的完整 stereo audio-array stage source 现在命中既有 `source-proven-graph-input-audio-stage-uniform-straight-alpha-no-auxiliary / generic-only`，不再被无关的 fragment varying-mutation 条件挡回宽 route；真实 `3750813609:358#effect#391` 的 straight-alpha-preserving、typed static auxiliary source 进入新增 identity-free `source-proven-graph-input-stage-uniform-straight-alpha-preserving-static-auxiliary / generic-only`。后者要求 typed static auxiliary slots 非空且精确等于其他 active textures，并拒绝 opacity mask、provider、graph target 与 scalar output。两项 default/recovery 走 generic artifact；坏 artifact 只在各 effect 发布 previous-current，profile disable 只回滚 shared `boundedSwift`，均保留 GraphTargets、GraphExecutor、GPU/publication/compositor/next-frame。现役 route census 为 **39/4/1**；没有新增 renderer、registry、property state、clock、graph/history 或 output owner。见 [E-V1-AUDIO-STATIC-AUXILIARY-GRAPH-INPUT-OWNER](runtime-evidence-index.md#e-v1-audio-static-auxiliary-graph-input-owner)。

> 现役 Standard Blur owner 更正：最后一套 dedicated effect 产品链以及只服务它的 stage plan/compiler/renderer/resource store 已删除；作者图、双 FBO、dynamic scale、target allocation/publication、completion/rollback 与最终显示继续由既有 MaterialProgram、GraphTargets、GraphExecutor 和唯一 compositor 持有。fresh 12-sample/25-reference census 为 10 Program / 13 inactive / 1 typed unsupported / 1 previous-current / 0 dedicated；真实 `3211615441` default/recovery 的两个四 material Blur 都由普通 Program 完成 GPU/publication/compositor/next-frame，profile disable只在这两个 effect 内形成 4 rejected-node previous-current，其他 graph 继续，恢复后重进 Program。dedicated 产品 family 已清零；route registry 仍有 4 个 `prefer-generic` shared fallback profile待 V1-B 审计，1 个 unowned Blur `observe-only` profile只作为显式 fail-closed 边界。没有新增或放宽 graph/FBO/command profile；详见 [E-V1-STANDARD-BLUR-DEDICATED-FAMILY-RETIREMENT](runtime-evidence-index.md#e-v1-standard-blur-dedicated-family-retirement)。

> 现役 Pulse owner 更正：旧 Pulse graph planner/runtime plan/compiler/backend、pipeline/renderer 与专用 texture loader 已成族删除；author shader preparation、typed texture/uniform publication、target/current rollback 与最终显示继续复用唯一 MaterialProgram/GraphExecutor/compositor。fresh 15-sample/36-reference route census 为0 dedicated；真实 `3769688830:21#effect#534` default/recovery进入同一Program，profile disable只保留effect-entry previous-current，三路均闭合GPU/publication/compositor/next-frame。当前唯一 dedicated family 是 Standard Blur；本批没有新增或放宽 graph/FBO/command profile。详见 [E-V1-PULSE-DEDICATED-FAMILY-RETIREMENT](runtime-evidence-index.md#e-v1-pulse-dedicated-family-retirement)。

> 现役 X-Ray owner 更正：旧专用 graph planner、runtime plan、compiler/backend、pipeline/renderer 与 texture loader 已成族删除；X-Ray authored shader、typed resources、target/publication 与最终显示继续复用唯一 MaterialProgram/GraphExecutor/compositor。property texture request 只由 MaterialProgram typed identities 驱动，stock identity verifier 仅保护 provider dependency，不是第二套 graph 或 renderer。真实 `3757555836` 的 default/disable/recovery 均保持 5 Program / 0 dedicated、GraphExecutor/GPU/publication/compositor/next-frame 完整；该批冻结时还剩Standard Blur与Pulse，后继Pulse删除后当前只剩Standard Blur。没有新增或放宽 graph/FBO/command profile；见 [E-V1-XRAY-DEDICATED-FAMILY-RETIREMENT](runtime-evidence-index.md#e-v1-xray-dedicated-family-retirement)。

> 现役 Standard Blur wrapper-metadata 后继不扩 graph、FBO 或 shader profile：两个 Gaussian `scale` consumer 与现有 direct-user Program 共用 `SceneResolvedMaterialDirectUserBindingContract`，允许未知非 producer metadata 保真，继续拒绝 semantic producer key、duplicate 与缺 required key。`9000001070` default/recovery均为4/4/0 Program / 0 dedicated；profile disable只形成GPU/publication/compositor连续的4/0/4 previous-current。route与dedicated census不变；见 [E-V1-STANDARD-BLUR-OPAQUE-WRAPPER-METADATA-OWNER](runtime-evidence-index.md#e-v1-standard-blur-opaque-wrapper-metadata-owner)。

> 现役 Pulse wrapper-metadata 后继不扩 shader profile：shared direct-user binding contract允许 `{user,value}` 携带未知、非producer语义 key，同时继续保留完整 authored keys，并让类型校验、authored range回退、scalar projection与Pulse撤权消费同一判断；`animation/script/scriptproperties`及缺required key仍拒绝。`9000001049` default/recovery均为Program / 0 dedicated，profile disable只发布GPU/publication/compositor连续的previous-current。target/FBO/command/compositor与route census不变；见 [E-V1-PULSE-OPAQUE-WRAPPER-METADATA-OWNER](runtime-evidence-index.md#e-v1-pulse-opaque-wrapper-metadata-owner)。

> 现役 historical Pulse combined RGB+alpha 边界：profile identity只允许进入分析，真正撤权继续由共享 source fact、color-transfer、canonical blend helper/mode、左右RGB multiplier、同一factor、typed auxiliary与optional opacity mask、Template/ShaderPreparation/readiness及`generic-only / fallbackOwner=none`合取决定。static与producer-present CAST3/literal-zero/`saturate(rgba)`的mask/no-mask共十二形态进入同一MaterialProgram/GraphExecutor；`saturate`精确映射为`straightAlphaUNorm`，literal-zero的`max(0,float-vector)`只在共享compiler source被静态改为同维零向量。artifact损坏或profile disable只回到effect-local previous-current且不复活dedicated。没有target/FBO/command/compositor变化；见 [E-V1-PULSE-LIVE-LITERAL-ZERO-RGB-ALPHA-OWNER](runtime-evidence-index.md#e-v1-pulse-live-literal-zero-rgb-alpha-owner)。

> 现役 Pulse staged-scalar/literal-zero 边界：同一 authored-definition → typed input → ShaderPreparation → MaterialProgram → GraphExecutor 主链现接受 exact stock `speed/amount/phase` 与 historical fragment-only `phase` 的 whole-stage definition-only source，并逐 stage 重证 wrapper/material、ABI 和作者域；live/fallback mixed source仍关闭。共享 `SceneAuthoredShaderBuiltInVectorConversion` 只把精确内建 `max(0, float-vector identifier/swizzle)`改写为同维零向量比较，拒绝`min`、`0.0`、非零、反向参数、compound/int/constructor/vector-vector/scalar-scalar与user-defined `max`。该静态source rewrite与typed producer分责，因此static与sole direct live literal-zero均可进入现有Program；其他关系/脚本/多producer边界不变。见 [E-V1-PULSE-LIVE-LITERAL-ZERO-RGB-ALPHA-OWNER](runtime-evidence-index.md#e-v1-pulse-live-literal-zero-rgb-alpha-owner)。

> 现役 Pulse authored fallback 边界：这不是新shader profile、renderer或graph owner。共享definition partition保持type/shape、finite、duplicate与逐lane Double bit pattern，direct-user wrapper统一由shared contract接纳未知非producer metadata；semantic producer key继续拒绝。旧Pulse candidate只对exact stock2842、non-audio、color-only、no-mask的已登记numeric cohort撤权，并要求whole-stage source一致、material fallback、两种readiness、active ABI/domain与既有typed-data RGB source/profile全部成立。三个低对比stock fixture的scalar/vector3/组合default与recovery均为1 authored / 1 material / 0 rejected、GPU/compositor/next-frame完成；坏artifact和profile disable均为1/0/1 typed previous-current且不进入`boundedSwift`。没有target/FBO/command/compositor变化，route仍`38/4/1`。10帧whole-frame统计不支持全屏红绿交替，但不是眼部ROI/fidelity；其他constant/source/alpha/audio/mask/script/topology仍关闭。见 [E-V1-PULSE-OPAQUE-WRAPPER-METADATA-OWNER](runtime-evidence-index.md#e-v1-pulse-opaque-wrapper-metadata-owner)。

> 现役 Standard Blur authored scalar fallback 边界：这不是新shader profile、renderer或graph owner。property compiler把loss-preserving authored definition与live instruction分开；有限单值或bit-exact equal pair形成scalar definition，只有唯一有效slider发布live producer。whole-stage gate现在允许两个Gaussian `scale` pass整stage同为sole live scalar source，或在无同key/target producer时整stage同为exact authored scalar definition source；仍逐pass重跑Template/ShaderPreparation、target/type/value/wrapper/material fallback、唯一active vertex non-array `float2` ABI与finite equal-lane default。mixed live/fallback、缺/错/多definition、错/多producer、signed-zero或其他bit mismatch与ABI漂移保留incumbent。definition-only target在dedicated保留时不进入generic fallback publication，Program-first也不能被后续generic失败抹掉。`9000000999` default/recovery为4/4/0 Program，mixed-source `9000001002`仍为4/4/0 dedicated；profile disable严格NON-PASS，raw log仅证明4/0/4 previous-current与GPU/publication/compositor/next-frame安全。profile仍`generic-only / fallbackOwner=none`，route仍`38/4/1`；不证明整个Standard Blur、ROI/fidelity、Fast/fixed/full、V1完成或V2切换。见 [E-V1-STANDARD-BLUR-AUTHORED-SCALAR-FALLBACK-OWNER](runtime-evidence-index.md#e-v1-standard-blur-authored-scalar-fallback-owner)。

> 现役 X-Ray startup-false owner 边界：这不是新的 graph/shader profile，而是 current-stock scalar candidate 对既有 safe current-pair startup activation 的精确接缝。旧 X-Ray compiler只有在完整spatial-weighted source/resource/ABI与Program proof继续成立，且descriptor authored false、exact startup target、唯一bool producer、无同层/sibling frame-driven owner时才撤权；active/static仍走原门。受控`9000000131:69#effect#131`的default/disable/recovery均 **1/1 PASS / 0 dedicated**，启动时visibility-disabled previous-current、live false→true后的Program、GPU/publication/compositor/next-frame均闭合；default/recovery为generic artifact，disable为shared bounded frontend。missing/wrong/duplicate producer、wrong target/type、active-set-only、Timeline/SceneScript、FBO/target/dependency/command、utility/child/provider与其他source/resource/ABI仍关闭。该段取代下表把startup-false pair leaf一概写成“不是owner migration”的冻结边界；只有这个exact current-stock X-Ray cohort达到bounded owner migration。见 [E-V1-XRAY-STARTUP-FALSE-SCALAR-OWNER](runtime-evidence-index.md#e-v1-xray-startup-false-scalar-owner)。

> 现役 Standard Blur startup-false owner 边界：startup-safe nonpersistent FBO 不再只停留在 V1-A correctness。共享 compile input 现在把 startup target 与 active target 分开，按 exact descriptor `visible` 生命周期互斥选择；只有作者初始 false、exact validated startup target、唯一 bool producer、无同层 frame-driven owner并再次通过完整 KERNEL0 whole-stage proof时才撤销旧 candidate。missing/wrong/multi producer、错 target、active-set-only、Timeline/SceneScript同层 owner与 unsafe topology仍返回 incumbent，X-Ray不继承该门。fresh `3211615441` default/recovery均为23 Program / 0 dedicated/fallback并完成false→true FBO Program、GPU/publication/compositor/next-frame；profile disable只让两条Blur成为exact previous-current passthrough、0 dedicated且输出链继续。该段取代下表“startup sole-key 不变 / startup FBO 关闭 / 不是 owner migration”的冻结句，不改变其余graph/target/hazard边界；见 [E-V1-STANDARD-BLUR-STARTUP-FALSE-OWNER](runtime-evidence-index.md#e-v1-standard-blur-startup-false-owner)。

> 现役 Standard Blur exact Timeline-vector2 边界：Launch 从唯一 `timelineProgram.bindings` 形成同一 typed-definition 集合并交给 dedicated admission、RuntimeCatalog 与 MaterialProgram producer catalog。两个 Gaussian `scale` consumer 只有在各自唯一 `.vector2` definition、definition authored value 与 material fallback 逐 lane bit-exact、sole Timeline contributor、exact `animation/value` wrapper、无 script、唯一 active vertex non-array `float2` ABI 全部成立时才撤销旧 candidate；缺/错/多 definition、错型/错值、额外 wrapper/script 与 ABI remainder 继续失败关闭或保留 incumbent。fresh ordinary fixture 的 default/recovery 均 **1/1 PASS / 0 dedicated**，profile disable 只让目标成为 4/0/4 previous-current 并继续 GPU/publication/compositor/next-frame。下文较早把 Timeline 整体列为 remainder 的句子只对其他 Timeline、SceneScript 与 frame-driven visibility 成立；本批没有 fresh source×Timeline cross-product，也不证明完整 V4、Standard Blur、ROI/fidelity 或 parity。route census 保持 **38/4/1**。见 [E-V1-STANDARD-BLUR-TIMELINE-VECTOR2-OWNER](runtime-evidence-index.md#e-v1-standard-blur-timeline-vector2-owner)。

> 现役 Standard Blur static-scalar 边界：共享 Template 只有在完整 authored raw token 是一个 finite scalar 且与 lowered component bit-exact 时才发布 projection provenance；MaterialProgram 只把它投影到 active reflected、non-array、typed default 为 finite equal lanes 的 `float2` consumer，exact vector2 继续保留两条作者 lane。旧 Blur candidate 撤权还须对两个 Gaussian consumer 分别重跑真实 Template、ShaderPreparation、唯一 active vertex `float2` ABI 和 scalar isotropic default；malformed/nonfinite、float3/array、missing/unequal default、Timeline/SceneScript 与未证明 dynamic 继续失败关闭或保留 incumbent。该能力不按 Blur/sample/path 选择算法，只复用现有 `source-proven-unit-previous-blurred-composite`、GraphTargets、GraphExecutor 与唯一 compositor；route census 保持 **38/4/1**。见 [E-V1-STANDARD-BLUR-STATIC-SCALAR-PROJECTION-OWNER](runtime-evidence-index.md#e-v1-standard-blur-static-scalar-projection-owner)。

> 现役 X-Ray visible provider owner：identity-free `provider-backed-graph-input-spatial-weighted-color-blend / generic-only / fallbackOwner=bounded-frontend` 只在 single-backward external-primary、pass 0 / slot 1 / primary、static scalar、无 frame-driven visibility 的 exact 形态下消费 visible image layer 的 graph-final premultiplied publication；typed compiler request/artifact 只解预乘该provider slot，其他color/data slot不变。provider publication与终端显示仍共用原compositor；legacy `MWX_SCENE_NAMED_PROVIDER_ROUTE=disable-generic`只控制旧named-provider route，不再撤销这个generic-only binding或复活X-Ray owner。真实`2896873092`五对provider→consumer的default/profile rollback/legacy disable均 **1/1 PASS / 0 dedicated**，分别命中`genericCompilerArtifact`、shared `boundedSwift`、`genericCompilerArtifact`，三路均为5个publication、5个binding、11个graph layer、0 GPU failure并闭合terminal compositor/next-frame。wrong pass/slot、secondary、forward/cycle/multi dependency、dynamic/script/Timeline与lifecycle差异仍关闭；这不是Fast/fixed/full、整个X-Ray、ROI/fidelity或parity。现役route census为 **38/4/1**，见 [E-V1-XRAY-VISIBLE-PROVIDER-OWNER](runtime-evidence-index.md#e-v1-xray-visible-provider-owner)。

> 现役证据边界：后继 fresh Developer ID App 已把 historical `directPhaseMaxClampV1` exact `CAST3(0)`、non-audio alpha-only、一个 typed opacity mask 的 cohort 闭合到既有 `source-proven-graph-input-straight-rgb-scalar-alpha / generic-only / fallbackOwner=none`。default/fresh-process restore 均 **1/1 PASS**；两个 readiness artifact 同时损坏及 profile-local `disable-generic` 的未放宽严格负门均 **0/1 NON-PASS / 12 failures**，但 raw graph 只发布 typed previous-current，GPU/publication/唯一 compositor/next-frame 继续，0 dedicated、0 `boundedSwift`。该 source/profile 与既有 mask/no-mask `[incumbent, revoked, incumbent]` 分区合取后达到 bounded `owner-migration-complete`；不扩张到 saturate、literal-zero、binding/script/contributor 或其他 source/resource/topology，也不证明 mask/Pulse ROI、fidelity 或 parity。下文“historical masked CAST3 尚缺 fresh 证据”的冻结句统一由本段取代；见 [E-V1-PULSE-CAST3-MASKED-ALPHA-OWNER](runtime-evidence-index.md#e-v1-pulse-cast3-masked-alpha-owner)。

> 终审收紧：下述 Pulse combined profile 当前只授权 canonical `BLENDMODE=9`。source helper必须由两个root return精确证明active add-clamp blend与normal fallback，source fact保留左右RGB multiplier和factor；MSL必须保持相同mode、multiplier、factor及全部typed auxiliary到factor的可达依赖。相同签名的自定义helper、mode/tint变化和detached auxiliary均失败关闭，其他blend mode不是本cohort。

> 现役证据边界：后继 fresh Developer ID App 已在同一 masked-audio 隔离 cohort 中分别闭合 `source-proven-graph-input-rgb-blend-scalar-alpha` 与 `source-proven-graph-input-straight-rgb-scalar-alpha`。default/新进程 restore 为 **2/2 PASS**；坏 exact artifact 与两个 profile 同时显式 `disable-generic` 的负门均为 **0/2 NON-PASS / 每项 7 failures**，但目标只退化为 effect-local previous-current，后缀、GPU、唯一 compositor 与 next-frame 继续，0 dedicated。两个 profile 的 source/route/Program claim 仍严格分开；只证明 exact stock2842、无 binding、一个 typed opacity mask、`AUDIOPROCESSING=3` 的 combined 与 alpha-only bounded cohort，不外推其他 blend/binding/script/contributor/resource/topology 或 Pulse ROI/fidelity/parity。本页后续“masked audio 仍待”的静态批次与表格旧句统一由本段取代。见 [E-V1-PULSE-MASKED-AUDIO-OWNER](runtime-evidence-index.md#e-v1-pulse-masked-audio-owner)。

> 现役 Pulse alpha-only owner 更正：`SceneAuthoredShaderStraightRGBScalarAlphaAnalyzer` 在既有 straight-RGB carrier、0–3 个 scalar auxiliary、唯一 alpha multiply 与 nonnegative-RGB/preserved-alpha terminal之外，允许一个严格 optional mask slot；它必须在 alpha write 后 direct-red sample，并以 exact source/transformed-carrier mix 写回。artifact lowering 重证 slot/factor/order/mix/唯一写入；`source-proven-graph-input-straight-rgb-scalar-alpha / generic-only / fallbackOwner=none` 只在 active `.opacityMask` purpose、typed auxiliary 与 graph role精确合取时接管。audio撤权只允许 exact `stock2842`；non-audio allow-list还包含`directPhaseSaturateV1`与`directPhaseMaxClampV1`，但当前source proof只让后者的exact `CAST3(0)` terminal在mask/no-mask两形态撤权，saturate与literal-zero指纹保留incumbent。可执行分区锁定`[incumbent, revoked, incumbent]`；historical no-mask、stock masked static/audio 与 historical masked CAST3 的 fresh 证据分别由 [E-V1-PULSE-STATIC-ALPHA-OWNER](runtime-evidence-index.md#e-v1-pulse-static-alpha-owner)、[E-V1-PULSE-MASKED-ALPHA-ONLY-OWNER](runtime-evidence-index.md#e-v1-pulse-masked-alpha-only-owner)、[E-V1-PULSE-MASKED-AUDIO-OWNER](runtime-evidence-index.md#e-v1-pulse-masked-audio-owner) 和 [E-V1-PULSE-CAST3-MASKED-ALPHA-OWNER](runtime-evidence-index.md#e-v1-pulse-cast3-masked-alpha-owner) 各自持有，不能互相外推。historical masked CAST3 不再缺 fresh App/坏artifact/disable/restore；binding/script、多contributor、其他 profile/resource/topology 与视觉 parity 仍未闭合。现役 route census 为 **37/4/1**。

> 现役 Pulse combined RGB + scalar-alpha owner 更正：`SceneAuthoredShaderRGBBlendScalarAlphaAnalyzer` 从 prepared fragment 证明一个 graph-input carrier 的 canonical `ApplyBlending` RGB 写回、同一个非负 scalar 再乘 alpha 与 nonnegative terminal；后继又把 scalar auxiliary 和一个可选 mask slot 分离，mask 只允许在 alpha write 后 direct-red sample，并以 exact `mix(sourceSnapshot, transformedCarrier, maskFactor)` 写回。外部 artifact lowering 重新核对 carrier/sample/helper 参数、scalar/mask slot、factor、顺序、SPIRV-Cross scalarized `x/y/z` 或 whole-RGB 写回、alpha/mask write 与唯一 terminal。`source-proven-graph-input-rgb-blend-scalar-alpha` 只在 active/typed slot、graph role 与可选 `.opacityMask` purpose 精确相等、无 provider/target/scalar output 时接管，缺省 `generic-only / fallbackOwner=none`。旧 Pulse compiler 只对 exact `stock2842`、无 binding、`PULSECOLOR=1/PULSEALPHA=1` 的 static/audio cohort撤权；alpha-only由上方 sibling straight-RGB profile持权。坏 artifact 或 profile-local disable 都仅撤销最小 effect 到 previous-current，不恢复 dedicated owner。隔离 masked-static combined 代表内容的 default/restore 均闭合目标 Program、GPU/publication/compositor/next-frame；disable按仍要求目标Program的严格门保持NON-PASS，但raw evidence证明previous-current与后缀输出继续。当前 census 仍为 **37/4/1**；combined当前余债是 binding/script/multi-contributor 及其他未证 profile/resource/topology。见 [E-V1-PULSE-MASKED-RGB-ALPHA-OWNER](runtime-evidence-index.md#e-v1-pulse-masked-rgb-alpha-owner) 与 [E-V1-PULSE-RGB-ALPHA-OWNER](runtime-evidence-index.md#e-v1-pulse-rgb-alpha-owner)。

> same-slot whole-color replacement 批次冻结时新增的是第36个缺省 `generic-only` profile：`SceneAuthoredShaderSameSlotMixAnalyzer` 从 prepared fragment 证明两个 root whole-color local 对同一 slot 的 direct sample、原 local 被 replacement local 无条件整值替换以及唯一 terminal 输出；route 再要求 active/graph-input 精确为该唯一 slot、only graph-input sampler、无 target/provider/scalar/RG/preserved output。真实 `3747492842` 的两个 no-mask/FOLLOWCURSOR Iris leaf 在 default/corrupt/disable 三路分别证明 generic artifact、effect-local previous-current 与 `boundedSwift` 回滚，GPU/publication/compositor/next-frame 和完整可见 composition 继续；conditional/member/cross-slot/bypass/extra-sampler 反例仍留在宽 route。该批 census 为 **36/4/1**；后继 Pulse combined profile把现役 census 更新为 **37/4/1**，下述更早 `35/4/1` 与“第35个”仅保留各批冻结快照。见 [E-V1-SAME-SLOT-COLOR-REPLACEMENT-OWNER](runtime-evidence-index.md#e-v1-same-slot-color-replacement-owner)。

> auxiliary RGB owner 批次没有增加 route profile：既有 `source-proven-graph-input-auxiliary-rgb-blend-alpha-preserving / generic-only` 只在两份 auxiliary RGB local 分别经过 exact `CAST3(greyscale(local))` 匹配对、其余 sample/power/blend/source-alpha terminal 合同不变且 source carrier 未传入 authored `out/inout` helper 时接管。真实 `3747492842:434#effect#545` 的作者默认 `GREYSCALE=1 / MASK=0` 因而从宽 `straight-alpha-preserving / prefer-generic` 迁入该既有窄 owner；默认 generic artifact、坏 artifact effect-local previous-current 与 profile-local shared bounded rollback 都保持 GPU/publication、Iris suffix compositor 与 next-frame。该批冻结时 census 为35/4/1；后继新增profile后现役为37/4/1。MASK、mutable carrier alias escape、非匹配/其他 RGB preprocessing、其他 blend/revision 和宽 profile 余量继续未迁移。见 [E-V1-GREYSCALE-AUXILIARY-RGB-OWNER](runtime-evidence-index.md#e-v1-greyscale-auxiliary-rgb-owner)。

> 核验日期：2026-08-27
>
> 范围：Effect definition、material、render graph、FBO 与 Wallpaper Engine shader 合同。
>
> 现役 Bloom light-map 更正：本页后续历史段落中“34 个 `generic-only` / 剩余 3 个 light-map node”的移动表述，现统一由本段取代。`source-proven-graph-input-conditional-opaque-alpha-weighted-rgb` 已作为第35个缺省 `generic-only / fallbackOwner=bounded-frontend` profile接管真实 `3747492842` 当前 MODE=0/no-mask 三图的 nodes `0/0/2`。source proof只接受两个scalar gate、1–16次同槽alpha-weighted RGB sample、exact light-map transfer与opaque terminal；typed graph input只接受`.opaque`/`.premultipliedAlpha`，generic和bounded backend都在作者边界解预乘。默认、坏artifact shared fallback与profile-local disable三路均保持三图16 authored / 16 material / 0 rejected、GPU/publication/compositor/next-frame；MODE 1、optional mask、其他revision、独立ROI/fidelity、完整Bloom family、V1与parity仍未完成。见[E-V1-BLOOM-LIGHTMAP-OWNER](runtime-evidence-index.md#e-v1-bloom-lightmap-owner)。
>
> 最新 straight-RGB/scalar-alpha owner 边界：`source-proven-graph-input-straight-rgb-scalar-alpha` 只接受一个 straight-RGB graph-input carrier、0–3 个互异 direct-red scalar auxiliary、唯一 alpha multiply 与 nonnegative-RGB/preserved-alpha terminal；零 auxiliary 只允许 scalar 来自反射已证的 uniform/linked varying。active slots、typed auxiliary 和 graph role 必须精确一致且无 provider/target/scalar output。artifact lowering 重新核对唯一 carrier sample/alias/alpha write/output，只解预乘 carrier，辅助 data sample 不做颜色转换，terminal 再预乘。该 profile 缺省 `generic-only / fallbackOwner=none`；坏 artifact 与 disable 都撤销最小 effect 并保留 previous-current，不回到 Pulse renderer。静态派生 `9000000961:23#effect#63` 与真实 `3768229922:381#effect#480/384#effect#457` 的受控 audio-alpha 暴露均进入同一 Program/GPU/publication/compositor/next-frame主链；后者 profile disable 保持0 dedicated与跨帧previous-current。它们分别不是 authored active/default静态alpha内容和 authored-default visible audio内容，也都不证明fidelity/parity。见[E-V1-PULSE-STATIC-ALPHA-OWNER](runtime-evidence-index.md#e-v1-pulse-static-alpha-owner)与[E-V1-PULSE-STOCK-AUDIO-ALPHA-OWNER](runtime-evidence-index.md#e-v1-pulse-stock-audio-alpha-owner)。
>
> direct user-property consumer 边界中，Pulse 的已登记vector/scalar cohorts均完成各自bounded owner迁移。它们统一消费shared direct-user wrapper：`user/value` required、未知非producer metadata保真、semantic producer key fail closed；撤权还要求旧plan target/fallback/domain、sole `.userProperty` contributor、无script attachment及launch-scoped producer key/target/type等值，并逐stage重证唯一non-array ABI与author domain。关系域`bounds`、其他float2、alpha或audio+binding、SceneScript/Timeline、多contributor、semantic producer wrapper、数组/ABI/shape/domain漂移继续失败关闭或保留incumbent。这些cohort复用既有`source-proven-graph-input-typed-data-rgb-filter / generic-only`，没有改变route数；现役route为38/4/1。见[E-V1-PULSE-OPAQUE-WRAPPER-METADATA-OWNER](runtime-evidence-index.md#e-v1-pulse-opaque-wrapper-metadata-owner)。
>
> 最新 typed-data/audio 边界：`source-proven-graph-input-typed-data-rgb-filter` 现不仅持有 exact static color-only Pulse，也持有 exact `stock2842` audio color-only cohort。后者仍要求同一snapshot/carrier、typed auxiliary、terminal RGB/保留alpha与`fallbackOwner=none`，并额外要求`AUDIOPROCESSING`和五个audio常量经共享admission重算后与旧plan完全一致。真实`3233141951`两项目标由普通MaterialProgram消费同帧非零stereo snapshot，经GraphExecutor完成GPU/publication/compositor/next-frame；route关闭只发布previous-current且不复活Pulse dedicated owner。stock audio alpha-only/no-mask 已由上方 scalar-alpha profile接管；color+alpha、SceneScript/Timeline、多contributor与legacy/noncanonical仍关闭或由incumbent持有。见[E-V1-PULSE-STOCK-AUDIO-COLOR-OWNER](runtime-evidence-index.md#e-v1-pulse-stock-audio-color-owner)。
>
> 结论：当前 graph 已有 loss-preserving IR、source/variant preparation、atomic Program、persistent target、GraphExecutor 和唯一 compositor；V0 ordinary authored Program与现役表中登记的V1 material/copy/history/FBO/extent/compose/condition/function/command/named-provider/visible-provider原子取得各自 bounded 证据，未登记形态继续失败关闭。公共 capability route 的现役真值由当前 switch 与上方语义段共同维护：38个profile缺省`generic-only`、4个`prefer-generic`、1个Standard Blur unowned `observe-only`隔离桶。成功artifact优先执行；`prefer-generic`失败按typed reason回到登记的shared bounded/dedicated fallback，`generic-only`默认失败只保留最小previous-current或显式登记的shared backend，`disable-generic`不得复活已撤权专用owner。两个宽泛graph-input alpha profile、ordinary/宽provider-backed scalar route与unowned Blur隔离桶仍不具有owner-migration完成声明；各窄profile（包括exact visible-provider spatial route）、当前MODE=0/no-mask Bloom ordinary nodes、named-provider classic primary及本页其他已登记cohort的完成边界只对各自source/resource/topology成立。以上结果不表示V1、整个graph/shader/effect family、官方parity或release完成；精确运行身份见[运行证据索引](runtime-evidence-index.md)。

> conditional generated-RGB 没有新增 Bloom/effect/sample/path identity 分派：prepared source 仍须证明唯一独立 alpha carrier、条件内生成 RGB、唯一 terminal `float4(generatedRGB, carrierAlpha)`、只读且无 resource/output side effect 的 helper closure，以及每个 sample slot 的 opaque RGB 或精确 R/G/B/A scalar channel use；compiler artifact与MaterialProgram继续重证sample census、carrier、terminal及逐channel typed input。V1-B后继又只在该source fact、straight-alpha-preserving carrier、active texture-slot全集、graph-input carrier、无external provider/scalar output全部一致时命中独立`source-proven-graph-input-conditional-generated-rgb-preserved-alpha / generic-only / fallbackOwner=bounded-frontend`；宽preserving profile仍为`prefer-generic`。真实`3747492842`三项同构Bloom的terminal node默认/恢复由同一generic artifact执行，三个16-node graph均16 material / 0 rejected、GPU完成，第三项经Iris suffix到terminal compositor。坏artifact默认不启用bounded frontend，而把各完整graph原子回到entry previous-current；profile-local`disable-generic`才把三个terminal node切到shared`boundedSwift`并保持graph完整执行。四路严格selection仍因旧effect-count、typed script attachment与聚合期待为NON-PASS；完成的是terminal窄profile的bounded owner migration，不是后继已迁移的24个Gaussian、9个up-sample、9个down-sample与当前3个light-map node之外的其他Bloom revision/family、独立ROI/fidelity、V1或parity。现役owner由后继[E-V1-BLOOM-LIGHTMAP-OWNER](runtime-evidence-index.md#e-v1-bloom-lightmap-owner)、[E-V1-BLOOM-DOWNSAMPLE-OWNER](runtime-evidence-index.md#e-v1-bloom-downsample-owner)裁决；本段仍见[E-V1-CONDITIONAL-GENERATED-RGB-OWNER](runtime-evidence-index.md#e-v1-conditional-generated-rgb-owner)与原correctness锚点[E-V1-CONDITIONAL-GENERATED-RGB-BLOOM](runtime-evidence-index.md#e-v1-conditional-generated-rgb-bloom)。

> Bloom Gaussian X/Y loop-average 后继不按 Bloom/effect/sample/layer/path/hash 选路。`source-proven-graph-target-opaque-loop-sample-average`只接受单fragment `main`、两个互异正scalar gate、静态对称整数循环半径1–16、沿单轴的同槽RGB采样、精确`2r+1`归一化和literal opaque alpha；route再要求active/graph-input/graph-target同一source slot且无external provider/scalar/RG/preserved output。真实`3747492842`三项Bloom的24个Gaussian node（每图8个）默认、legacy-global-disable与恢复均命中两个source-key generic artifact；同时损坏两个artifact时，每图首个horizontal node成为首断点并原子回到entry previous-current，Iris suffix继续；exact profile-local disable才让24个node全部回滚shared `boundedSwift`。五路均exit0、无timeout/failed frame，严格仍为NON-PASS；这只建立该identity-free profile的bounded owner migration。后继up-sample与down-sample profile又各迁移9个node，light-map profile再迁移当前3个node；仍不构成完整graph/family、ROI/fidelity、Fast/fixed/full、V1或parity。见[E-V1-BLOOM-LIGHTMAP-OWNER](runtime-evidence-index.md#e-v1-bloom-lightmap-owner)、[E-V1-BLOOM-DOWNSAMPLE-OWNER](runtime-evidence-index.md#e-v1-bloom-downsample-owner)、[E-V1-BLOOM-UPSAMPLE-OWNER](runtime-evidence-index.md#e-v1-bloom-upsample-owner)与[E-V1-BLOOM-GAUSSIAN-LOOP-OWNER](runtime-evidence-index.md#e-v1-bloom-gaussian-loop-owner)。

> Bloom up-sample 后继同样不按 Bloom/effect/sample/layer/path/hash 选路。`source-proven-graph-target-opaque-static-sample-average`只接受单fragment `main`、2–16次同槽静态RGB/XYZ采样、单一vec3累加、sample/use守恒、精确literal倒数归一化与opaque alpha；helper/control-flow、副作用、嵌套texture sample、混槽、粗略权重或输出漂移均拒绝。route再要求opaque transfer、active/graph-input/graph-target/source slot相同且无external provider/scalar/RG/preserved output。真实`3747492842`三项Bloom的9个up-sample node在default、legacy-global-disable与recovery均命中同一generic artifact；损坏artifact时，每图首个up-sample node成为首断点并原子回到entry previous-current，不发布内部target，Iris suffix继续；exact profile-local disable才让9个node回滚shared`boundedSwift`。五路严格为NON-PASS但无failed frame；这只建立该identity-free profile的bounded owner migration；后继down-sample owner又迁移9个node，light-map owner再迁移当前3个node。见[E-V1-BLOOM-LIGHTMAP-OWNER](runtime-evidence-index.md#e-v1-bloom-lightmap-owner)、[E-V1-BLOOM-DOWNSAMPLE-OWNER](runtime-evidence-index.md#e-v1-bloom-downsample-owner)与[E-V1-BLOOM-UPSAMPLE-OWNER](runtime-evidence-index.md#e-v1-bloom-upsample-owner)。

> Bloom down-sample 后继同样不按 Bloom/effect/sample/layer/path/hash 选路。`source-proven-graph-target-opaque-alpha-weighted-loop-average`只接受单fragment `main`中1–16次同槽采样，要求RGB逐次乘同一sample alpha、权重累加同一alpha、正epsilon归一化、literal opaque alpha，以及sample/index/use守恒；共享canonicalizer展开后的有序literal block序列由同一analyzer重证。route再要求active/graph-input/graph-target/source slot相同且无external provider/scalar/RG/preserved output。compiled variant携带typed opaque slot；launch要求作者顺序中同identity上游Program已证明opaque output，frame finalizer再要求实际graph publication为typed `.opaque`，premultiplied/data/missing/unresolved均失败关闭。真实`3747492842`三项Bloom的9个down-sample node在default、legacy-global-disable与recovery均命中同一generic artifact；损坏artifact时，每图首个down-sample node `3/3/5`成为首断点并原子回到entry previous-current，不发布内部target，Iris suffix继续；exact profile-local disable才让9个node回滚shared`boundedSwift`。五路严格为NON-PASS但无failed frame；这只建立该identity-free profile的bounded owner migration；后继light-map profile已迁移当前3个ordinary node，仍不是完整graph/family、ROI/fidelity、Fast/fixed/full、V1或parity。见[E-V1-BLOOM-LIGHTMAP-OWNER](runtime-evidence-index.md#e-v1-bloom-lightmap-owner)、[E-V1-BLOOM-DOWNSAMPLE-OWNER](runtime-evidence-index.md#e-v1-bloom-downsample-owner)。

> 最新 V1-B 后继没有改变 VHS same-alpha reconstructed-RGB 的 source/compiler/input 合同，只把 identity-free `source-proven-graph-input-same-alpha-reconstructed-rgb-data-filter` 从 `prefer-generic` 原子切到 `generic-only / fallbackOwner=bounded-frontend`。source 仍只接受无控制流且不 shadow `mix/lerp` builtin 的 `main`、同一 source slot 的 snapshot/RGB reconstruction、snapshot alpha、后续 RGB-only mutation、唯一 terminal scalar mix与只读 helper closure；其他 slot 必须按逐槽投影作为 typed `.data`，compiler 继续拒绝 alias/helper sample、投影/count、alpha与terminal drift。真实静态`2419444134:23#effect#216`默认与恢复都进入Program/GPU/compositor/next-frame；目标artifact publication缺失时默认只发布effect-local previous-current，显式`disable-generic`才回滚shared `boundedSwift`。真实`3747492842:173#effect#183`仍停在`material-dynamic-uniform-script-attachment-unproven`。完成的是该profile的bounded owner migration，不是VHS family、独立ROI/fidelity或parity；见[E-V1-SAME-ALPHA-RECONSTRUCTED-RGB-OWNER](runtime-evidence-index.md#e-v1-same-alpha-reconstructed-rgb-owner)与原correctness锚点[E-V1-VHS-SAME-ALPHA-RECONSTRUCTED-RGB](runtime-evidence-index.md#e-v1-vhs-same-alpha-reconstructed-rgb)。

> 现役阶段纠正：上述 typed script attachment 是 V2/V4 producer debt，不是 V1-A correctness atom。startup-inactive safe nonpersistent FBO 后继已经让 exact 单 effect、nonunique/nonpersistent target、无 clear/condition/function/compose、读写受支配、command storage descriptor 相容且 terminal 消费 FBO 的图在作者初始关闭时保留 authored order；同 key 的完整 bool visibility fan-out 可先发布 activation previous-current，再于 live true 后进入原 MaterialProgram/GraphTargets/GraphExecutor。真实 `3211615441` 的两条 Blur FBO 与两条 VHS suffix 在 `blur=false→true` 中完成 GPU/publication/compositor/next-frame，0 dedicated/fallback。项目正反门另锁定 renamed material/swap/copy 未见组合，以及 history read-before-write 和 copy extent mismatch 拒绝。专项表当前不再登记可独立闭合的 V1-A atom，V1-A 完成门通过，主线切到 V1-B；未登记图继续 fail closed。见 [E-V1-STARTUP-INACTIVE-SAFE-FBO](runtime-evidence-index.md#e-v1-startup-inactive-safe-fbo)。

> 该后继结论取代下表 `already-admitted nonpersistent FBO` 与 `startup-false current-pair leaf` 两行中“startup sole-key 不变 / startup FBO 全部关闭”的冻结边界；两行其余 active-owner 与 pair-leaf 证据仍有效。

> standalone overlay-alpha Blend 不新增效果算法：既有 source analyzer 必须证明 slot 0 是 previous-current color carrier、distinct overlay alpha 同时参与 blend weight 与 terminal output alpha，active graph/source slot全集精确一致且没有provider、target或scalar output，才登记 `source-proven-graph-input-overlay-alpha-blend / generic-only`。artifact失败与显式关闭都只使用共享 bounded frontend/previous-current；旧 Blend planner/plan/compiler/backend/pipeline/renderer/texture loader/store 已删除。真实 `2134765860` 的default/rollback/recovery闭合目标graph、GPU、terminal compositor与next-frame，且三路旧Blend owner observation为0；整体26条既有matrix failure不冒充suite PASS或视觉parity。见[E-V1-STANDALONE-BLEND-OWNER-RETIREMENT](runtime-evidence-index.md#e-v1-standalone-blend-owner-retirement)。
>
> 上述route还要求source fact携带distinct overlay slot，且active `typedStaticDataAuxiliarySlots`精确等于该唯一slot；untyped、premultiplied-color/wrong-purpose、wrong-slot与额外auxiliary不得取得`generic-only`。该门由fresh-context终审发现初版遗漏后补齐，正反fixture和最终签名App三路运行均在修正后重跑。

> stock Radial God Rays只扩展既有`source-proven-graph-target-independent-signal-accumulator`，没有建立按God Rays identity准入的generic profile。source analyzer对direct-main形态要求整个fragment只有main中的这一处静态`2...64` loop、helper内没有额外`for/while/do`，并证明唯一whole-vector sampler、零RGBA carrier、精确`index/(bound-1)`累计、循环后唯一RGB tint，以及terminal RGB/alpha相同factor与alpha clamp/saturate。source loop work进入exact request/cache key；external artifact contract从实际MSL重新证明entry/color0、相同static loop work、slot/sample、weight、SPIRV-Cross packed或分量lowering、carrier闭包和terminal alpha，丢循环或work漂移不能降级到普通preserving fallback。改名、不同slot及两种MSL lowering为正例；额外sampler、helper动态循环、动态bound、负/超1权重、不同alpha factor、无clamp、branch、额外carrier写/escape及source-artifact漂移均typed拒绝。真实`3028090166:13#effect#335`的五节点graph由producer→accumulator→normalized sum×2→compositing普通Program执行，两张`1024x605/rgbaBackbuffer` target、GPU/publication/compositor/next-frame成立；坏cast artifact只让整个effect发布previous-current，显式route回滚只替换cast为shared `boundedSwift`。该stock cohort冻结时仍单独保留legacy Directional，后继V1-B cohort已用source-proven sampler channel、identifier loop bound、typed RGBA8-UNorm attachment与selected graph candidate事实把真实`1937925563`的13个五节点graph交给producer→UNorm accumulator→normalized sum×2→compositing共享Program，并删除整条God Rays固定MSL产品链。精确证据见[E-V1-STOCK-RADIAL-GODRAYS-SHARED-OWNER](runtime-evidence-index.md#e-v1-stock-radial-godrays-shared-owner)与[E-V1-LEGACY-DIRECTIONAL-GODRAYS-SHARED-OWNER](runtime-evidence-index.md#e-v1-legacy-directional-godrays-shared-owner)。

> graph-input straight-alpha 的profile仍由prepared source color fact与active typed graph slot共同决定：显式graph binding、material graph-input alias和现役`implicitFramebufferSlots`规范化都进入launch授权，Program publication在execution侧复核同一graph identity。当前默认route为`prefer-generic`；合法artifact继续取得`genericCompilerArtifact`执行权，compiler/artifact失败以typed reason允许shared bounded/dedicated fallback。只有显式`disable-generic`会整体回滚该route。它不是Fisheye名称特判，也不开放external provider、任意颜色合同或整个shader family。

> single-sampler alpha mutation 子profile不改变宽泛 straight-alpha 的`prefer-generic`边界：prepared source还必须证明唯一regular graph-input sampler、同一typed source slot、无auxiliary/provider/target/scalar output，且sampled alpha在terminal return前恰有一次无条件改写。artifact v2 独立复核唯一sample/alpha write/terminal carrier后统一做sample unpremultiply与terminal premultiply，因此alpha factor读取sampled RGB的Color Key与只读取UV的Fisheye不再走语义条件不同的whole-vector快捷式。真实`3765760121:96`与真实`3767460992`四个stock/Workshop Color Key consumer均命中`source-proven-graph-input-single-sampler-alpha-mutation / generic-only / genericCompilerArtifact`；坏artifact只发布对应effect的previous-current，`disable-generic`只回滚shared `boundedSwift`。它不按Fisheye/Color Key/effect/sample/layer/path/hash准入，也不外推其他sampler/control-flow、整个family或官方parity；精确证据见[E-V1-NARROW-GRAPH-INPUT-ALPHA-OWNERS](runtime-evidence-index.md#e-v1-narrow-graph-input-alpha-owners)。

> stage-uniform graph-input profile另由实际stage member消费划分linked reflection的共享UBO字段，并要求双stage各有source-proven active非sampleruniform；它不是Foliage名称准入。默认坏artifact不会恢复旧专用owner，`disable-generic`只回滚共享bounded frontend。该profile已完成Foliage旧产品owner删除，但没有开放单stage/no-uniform、external provider、任意graph input或整个Foliage/shader family。
>
> straight-alpha-preserving graph-input profile由prepared source证明同一typed graph slot的多次RGBA sample、sampled `.w` alpha与composed RGB output；artifact统一执行unpremultiply/premultiply boundary。16/32/64 stereo scalar audio array、对应动态frequency loop与SPIRV-Cross scalar-array `.x`清理由共享ABI/budget规则处理，不读取Chromatic、effect/path/sample identity。当前默认route也为`prefer-generic`：shared compiler补齐typed `mix` vector-width normalization后Hue Shift已接受，但其他命中形态仍可使用带原因的bounded fallback。fresh真实门见[E-V1-GRAPH-INPUT-STRAIGHT-ALPHA-PRESERVING](runtime-evidence-index.md#e-v1-graph-input-straight-alpha-preserving)与[E-V1-ORDINARY-SHADER-REGRESSION-RECOVERY](runtime-evidence-index.md#e-v1-ordinary-shader-regression-recovery)；当前没有owner-migration、独立ROI、官方像素parity或全局dedicated owner清零结论。

> same-slot channel reconstruction 子profile不改变宽泛 straight-alpha-preserving 的`prefer-generic`边界：prepared source只接受同一typed graph slot的base与一个或多个distinct shifted RGBA sample，每个shifted sample只写回匹配的唯一RGB分量，base alpha保持不变，且无hidden sample、alpha write、control flow、auxiliary/provider/target/scalar output；artifact lowering独立复核同一shape。当前真实`3779026256`资源树派生、仅移除未证明visibility script并隔离单effect的`9000000622:112`默认命中`generic-only / genericCompilerArtifact`，坏artifact局部previous-current，`disable-generic`回滚shared `boundedSwift`。它不按Chromatic/effect/sample/layer/path/hash准入；派生fixture不是原始真实样本、Fast/fixed/full或官方对照。精确证据见[E-V1-NARROW-GRAPH-INPUT-ALPHA-OWNERS](runtime-evidence-index.md#e-v1-narrow-graph-input-alpha-owners)。
>
> auxiliary RGB blend alpha-preserving 子profile同样不放宽宽泛profile：prepared fragment只能有一个graph-input source carrier和一个distinct auxiliary RGB slot；direct form必须是两个全fragment sample加scalar-uniform `mix`，当前processed form必须是同一auxiliary slot的两个swizzled sample、`saturate(product)`、uniform `pow`、scalar-uniform alias与literal-mode `ApplyBlending`，两者都只能把RGB写回source并把原sample alpha原样送到唯一terminal output。隐藏helper sample、source/auxiliary同slot、alpha write、非uniform blend、provider/target/scalar output均不能命中。真实`3767460992:20#effect#702`当前命中`source-proven-graph-input-auxiliary-rgb-blend-alpha-preserving / generic-only / genericCompilerArtifact`；坏exact artifact只让该effect发布previous-current并继续compositor/next-frame，profile-local`disable-generic`才回滚shared `boundedSwift`。它不按Film Grain/effect/sample/layer/path/hash准入，也不外推其他noise、mask、blend/revision或官方像素parity；精确证据见[E-V1-AUXILIARY-RGB-BLEND-OWNER](runtime-evidence-index.md#e-v1-auxiliary-rgb-blend-owner)。
>
> stage-uniform straight-alpha-preserving 子profile不改变上述宽泛profile的`prefer-generic`边界：它额外要求唯一graph input、无target/provider/scalar output、active sampler全集恰为唯一regular graph-input与唯一typed defaulted opacity-mask auxiliary，并要求vertex/fragment均有active非sampleruniform。真实`3767460992`两处Shift Hue命中该窄profile并完成`generic-only` owner迁移；坏artifact只保留effect-local previous-current，`disable-generic`只回滚shared `boundedSwift`。它不按Shift Hue/effect/sample/path/hash准入，也不外推其他mask/schema/revision或官方parity。
>
> 无auxiliary的stage-uniform straight-alpha-preserving子profile要求相同color/graph/uniform边界，但active sampler全集必须精确等于唯一regular graph input。真实`3767460992:945#effect#952`的Workshop Gradient在共享canonicalizer把双stage同形oversized varying array证明并收敛到相同literal prefix后命中该profile；dynamic bound、控制流退出、动态/越prefix引用保持失败关闭。默认坏artifact只保留effect-local previous-current并继续后缀，`disable-generic`只回滚shared `boundedSwift`；旧Gradient产品owner已删除。它不按Gradient/effect/sample/path/hash准入，也不外推任意array/loop、其他revision或官方parity。
>
> 同一无auxiliary profile现又覆盖当前真实 Color Grading `TOOLS=2` source：共享 backend canonicalizer 在varying处理之前，以lexer/syntax的静态类型事实把 built-in `mix`/`lerp` 的简单floating scalar端点显式广播为另一端的`vec2/3/4`宽度，解决固定glslang对scalar/vector endpoint不接受的问题；compound scalar、user-defined overload、`int/uint` literal或swizzle和歧义类型不改写。真实`3768903841:263#effect#275`默认/恢复沿`generic-only / genericCompilerArtifact`形成1 material并完成GPU/publication/compositor/next-frame，坏artifact只发布该effect的previous-current，显式`disable-generic`回滚shared `boundedSwift`且仍走同一MaterialProgram/GraphExecutor。旧Color Grading planner/plan/backend/pipeline/renderer/repository slot与专用benchmark/matrix count已删除；该事实不按effect/sample/layer/path/hash准入，也不外推其他TOOLS/combo/binding、独立ROI或官方像素parity。精确证据见[E-V1-COLOR-GRADING-SHARED-OWNER](runtime-evidence-index.md#e-v1-color-grading-shared-owner)。
>
> 同一无auxiliary profile还闭合了modern与legacy Procedural Noise当前bounded source。共享stage-uniform analyzer显式消费ShaderPreparation已经证明的runtime-loop bound；straight-alpha lowerer在常规单write之外，只接受单一color sampler、2...8个完整whole-output赋值、唯一terminal return、所有sample同slot且无其他texture reference的union，再统一插入unpremultiply/premultiply。external artifact对同一contract做独立MSL复核；legacy artifact失败时只记录typed `shared-backend-fallback`并交给同一profile的shared bounded frontend，产品执行权没有转给dedicated renderer。legacy slot-3 hidden solid又由结构型`.solidLayer` binding复用现役named target publication，不读取Procedural/effect/sample/layer/path/hash、combo或常量选择算法。旧Procedural planner/profile/plan、fixed MSL pipeline/renderer与compiler/backend carrier已删除；精确证据见[E-V1-PROCEDURAL-NOISE-SHARED-OWNER](runtime-evidence-index.md#e-v1-procedural-noise-shared-owner)。
>
> audio/stage-uniform straight-alpha 无auxiliary子profile不扩大宽泛straight-alpha：它只接受唯一typed graph input、无target/provider/scalar output、active sampler全集精确为该input、双stage active uniform、同一16/32/64档完整left/right scalar audio数组，以及至少一个由共享normalizer安全局部化的non-array mutable fragment varying。共享artifact只对反射证明的audio数组动态索引清除SPIRV-Cross伪`.x`，并把source-proven straight-alpha输出放进统一unpremultiply/premultiply边界；作者uniform key的显著尾随空白继续保留。真实`3767460992:945#effect#950`默认命中`generic-only / genericCompilerArtifact`并完成GPU/publication/suffix/compositor/next-frame；坏artifact只保留effect-local previous-current，旧Enhanced Audio Bars产品owner已删除。显式`disable-generic`只证明安全撤route与previous-current，不是视觉等价的bounded frontend回滚。它不按Audio Bars/effect/sample/path/hash准入，也不外推任意audio shader、varying mutation、SceneScript `AudioBuffers`或官方parity。
>
> alpha-attenuation 是从宽泛 straight-alpha 中收窄出的独立 profile：prepared fragment必须证明一个direct source sample保持RGB且alpha仅乘有限scalar graph，execution admission再要求source是当前effect的exact unnamed previous-current、source sampler为regular、辅助slot全为非graph opacity mask，且active sampler全集与源码事实一致。named/internal framebuffer、history、provider、其他effect output、混合fallback candidate、错sampler mode、额外active sampler与auxiliary graph input均拒绝。该profile缺省`generic-only`，旧Opacity planner/backend/pipeline/renderer/loader已删除；默认坏artifact走effect-local previous-current passthrough，只有`disable-generic`回滚shared `boundedSwift`。它不按Opacity名称准入，也不升级更宽的straight-alpha profile或整个shader/effect family。
>
> typed-data RGB filter 现覆盖一组不按 Pulse/effect/sample/layer/path/hash准入的 snapshot-carrier terminal transform：authored source必须whole-sample唯一color source并保存snapshot，以snapshot初始化唯一carrier，只做一次无条件RGB写；terminal只允许carrier identity、唯一whole-vector saturate或精确`max(0, rgb)`且保留原alpha。artifact lowering重证snapshot→carrier→RGB write→可选scalar mix→terminal dataflow，不能以sampler census代替。active graph input、typed data auxiliary、sampler/readiness与`fallbackOwner=none`全部一致时，`source-proven-graph-input-typed-data-rgb-filter / generic-only`持有输出。真实静态、stock audio color-only及`1937925563:13#effect#321` exact direct user-property vector3 tint均进入普通Program/GraphExecutor；direct property又加入具显式作者范围的fragment-only scalar、same-domain双stage `speed/amount`、stage-indexed stock `phase`与historical fragment-only `phase` owner cohort，要求旧plan binding、sole user contributor、exact wrapper/fallback/domain及launch producer key/target/type等值。fragment-only项重证唯一active fragment ABI；双stage项重证同一material key的exact `{vertex,fragment}` active set与每stage唯一non-array `float`。`speed/amount`逐stage同域，stock `phase`分别重证vertex `0...1`与fragment `0...6.282`，historical `phase`只重证fragment `0...6.282`。共享finalizer逐stage执行author-range检查与fallback；缺stage、额外consumer、同stage重复、array/type/range漂移均保留incumbent。越界值从snapshot authored lane恢复；缺失producer只允许exact authored target继续Program，不伪造live state，producer不等且旧plan可接纳时旧compiler不撤权。最终profile-local `disable-generic`只保留目标entry previous-current，0 dedicated，后缀继续compositor/next-frame；因此上述producer-present scalar达到各自bounded owner-migration-complete。无效property kind导致的wrapper shape漂移另由finalizer局部拒绝，不扩大撤权范围。关系域`bounds`、其他float2、alpha或audio+binding、color+alpha、masked alpha、SceneScript/Timeline、多contributor、wrapper/ABI/domain漂移、noncanonical及其他Pulse余量仍由incumbent或现役fail-closed边界隔离。精确证据见[E-V1-PULSE-HISTORICAL-FRAGMENT-PHASE-USER-PROPERTY-OWNER](runtime-evidence-index.md#e-v1-pulse-historical-fragment-phase-user-property-owner)、[E-V1-PULSE-STAGE-INDEXED-PHASE-USER-PROPERTY-OWNER](runtime-evidence-index.md#e-v1-pulse-stage-indexed-phase-user-property-owner)、[E-V1-PULSE-CROSS-STAGE-SCALAR-USER-PROPERTY-OWNER](runtime-evidence-index.md#e-v1-pulse-cross-stage-scalar-user-property-owner)、[E-V1-PULSE-FRAGMENT-SCALAR-USER-PROPERTY-OWNER](runtime-evidence-index.md#e-v1-pulse-fragment-scalar-user-property-owner)、[E-V1-PULSE-TYPED-USER-PROPERTY-OWNER](runtime-evidence-index.md#e-v1-pulse-typed-user-property-owner)、[E-V1-PULSE-STOCK-STATIC-OWNER](runtime-evidence-index.md#e-v1-pulse-stock-static-owner)、[E-V1-PULSE-STATIC-TYPED-DATA-OWNER](runtime-evidence-index.md#e-v1-pulse-static-typed-data-owner)与[E-V1-PULSE-STATIC-MAX-CLAMP-OWNER](runtime-evidence-index.md#e-v1-pulse-static-max-clamp-owner)。
>
> straight-RGB/scalar-alpha profile 与 typed-data RGB filter 分开：source analyzer必须证明RGB除nonnegative clamp外保持不变，alpha只由bounded scalar graph相乘；0–3个辅助纹理仅作互异 direct-red data input，零辅助时scalar必须来自已反射uniform/linked varying。它另允许一个与 carrier/scalar auxiliary互异的 optional mask slot，但只接受 alpha write 后唯一 direct-red sample 与 exact source/transformed-carrier mix。route要求 scalar auxiliary精确等于typed static data全集，mask slot精确为active `.opacityMask`；SPIRV-Cross artifact verifier重证唯一carrier/alias、仅`.w *= factor`写入、mask factor/order/mix与exact nonnegative-RGB/preserved-alpha terminal。carrier/sample/output次序、引用数或sample全集漂移都拒绝。静态派生、stock audio-alpha no-mask、fresh masked-static与后继fresh masked-audio alpha-only均闭合相应compiler artifact、Program、GPU/publication/compositor/next-frame；坏digest或`disable-generic`只发布previous-current且0 dedicated。该profile不按Pulse/effect/sample/path/hash准入；不开放combined color+alpha、binding或任意scalar graph。精确audio边界见[E-V1-PULSE-MASKED-AUDIO-OWNER](runtime-evidence-index.md#e-v1-pulse-masked-audio-owner)。
>
> spatial-weighted color blend 是独立的 `generic-only / fallbackOwner=bounded-frontend` 窄profile，不放宽两个既有宽泛graph-input alpha profile。prepared source必须证明唯一previous-current carrier、straight-color whole sample、preserved `.ra` sample、可选direct-red mask、resolved normal blend、权重数据流与source-alpha守恒；carrier的额外引用、复合写、递增、inout逃逸和隐藏sample均拒绝。ShaderSchema把source proof投影为`.straightAlbedo / .preservedChannels / optional .mask`前，还要求mode/material、当前asset、registered default与lower candidate chain无冲突；annotation default integer combo只在当前prepared variant重算。profile要求active slots至少三项、typed auxiliary精确等于除source以外的全集、graph input精确为source且无target/provider/scalar output。默认缺失/坏artifact关闭共享bounded接管并发布effect-entry previous-current；profile-local `disable-generic`才回滚shared `boundedSwift`。真实`1989767609:13#effect#30`的default/corrupt/disable三路已闭合generic artifact、effect-local passthrough、共享rollback、0 dedicated、GPU/publication/唯一compositor/next-frame，自动门另锁定wrong typed-slot与同形bounded frontend可执行。该结构profile达到bounded owner migration；X-Ray旧owner只持未命中与dynamic余量，不表示完整family；见[E-V1-XRAY-SPATIAL-WEIGHTED-OWNER](runtime-evidence-index.md#e-v1-xray-spatial-weighted-owner)。
>
> 后继共享资源原子只允许effective TEXB0003、完整mip/payload与decoded extent证明JPEG、authored physical=mapped且CGImage无alpha的embedded source在`.straightAlbedo`用途下按既有texture budget缩放；候选记录实际Metal extent并使用identity UV。authored padding、TEXB0004 metadata伪装及其他preserved/data用途仍要求精确物理extent。该资源冻结中，真实`3757555836:69#effect#131`的live-size current-stock X-Ray取得10/10 asset readiness，并以当时的`prefer-generic` route与四段普通suffix连续经MaterialProgram/GraphExecutor到达GPU、publication、唯一compositor与next-frame；当前同一结构profile已由后继owner证据切为`generic-only`。当时把`3747492842`记为dynamic visibility断点；shared activation随后把冻结时首断点纠正为composition utility topology，最新captured-main correctness atom又闭合其bounded opaque subtree并把现役首断点前移到Bloom color contract。见[E-V1-XRAY-OPAQUE-JPEG-ASSET-PUBLICATION](runtime-evidence-index.md#e-v1-xray-opaque-jpeg-asset-publication)、[E-V1-XRAY-SPATIAL-WEIGHTED-OWNER](runtime-evidence-index.md#e-v1-xray-spatial-weighted-owner)、[E-V1-XRAY-SHARED-STAGE-ACTIVATION-OWNER](runtime-evidence-index.md#e-v1-xray-shared-stage-activation-owner)与[E-V1-COMPOSITION-SUBTREE-CAPTURE](runtime-evidence-index.md#e-v1-composition-subtree-capture)。
>
> 2026-08-28 的 authored scalar fallback 后继仍不新增 shader profile，也不按 X-Ray/sample identity选算法，而是复用 shared loss-preserving definition publication。`size/multiply` 每项可独立为 exact finite static、sole producer的direct-user scalar，或在同 key/target 没有任何 producer时为唯一 scalar finite bit-exact definition-only authored fallback；direct-user shape统一由 shared contract要求`user/value`并保留未知非producer metadata，`animation/script/scriptproperties`、missing/duplicate key、mixed source、错target/key、多producer、type/nonfinite/signed-zero/ABI/range漂移继续失败关闭。`9000000132`既有default/disable/recovery证据保持；extra-wrapper `9000000133`的后继default/disable/fresh recovery现均严格 **1/1 PASS / Program owner / 0 dedicated**，分别命中generic artifact/shared bounded frontend/generic artifact，三路GraphExecutor `9/9/9`、0 failure并闭合GPU/publication/terminal compositor/next-frame。定向眼部坐标探针变化保持局部、未见整屏红绿/黑屏/层丢失，但不证明眼部发光fidelity、完整X-Ray或V1完成。见[E-V1-XRAY-OPAQUE-WRAPPER-METADATA-OWNER](runtime-evidence-index.md#e-v1-xray-opaque-wrapper-metadata-owner)。

> 2026-08-27 的 current-stock scalar 后继没有新增 shader profile，也不按 X-Ray/sample identity选算法；它只删除既有 spatial-weighted `generic-only` Program前的“至少一个dynamic producer”人为阻断。该批冻结时只证明exact finite static或sole live scalar；现役definition-only authored fallback由上段后继持有。`size`仍须为active vertex `float [0,1]`，`multiply`须为active fragment `float [0,10]`，每个texture readiness variant都必须重过完整source/typed auxiliary/graph input/no-target/no-provider合同。exact direct-bool visibility只有在shared activation已经证明同一target时可同行，frame-driven Timeline/SceneScript owner仍不撤权。受控可执行partition门在stock-identity seam下用同一显式static fixture证明实际planner主体先接纳、精确产品X-Ray compiler extension再以typed reason撤权；producer/visibility反例返回incumbent，ABI/source反例至少关闭撤权且完整生产identity可更早fail closed。真实`1989767609:13#effect#30`只显式authored `size`，所以旧planner在新gate前已拒绝；它的default、profile-local disable与restore只证明static-resolved shared profile均为1 Program / 0 dedicated并闭合GPU/publication/terminal compositor/next-frame，坏artifact严格NON-PASS且raw frame 0/next-frame只发布entry previous-current。旧`3757555836:69#effect#131`继续证明live-size direct producer子集；这些证据都不升级为X-Ray family、独立ROI或fidelity。见[E-V1-XRAY-DIRECT-SCALAR-OWNER](runtime-evidence-index.md#e-v1-xray-direct-scalar-owner)与[E-V1-XRAY-STATIC-SCALAR-OWNER](runtime-evidence-index.md#e-v1-xray-static-scalar-owner)。

> 后继shared activation不改变shader profile或graph ownership，只在已经完整准入的single-current-pair leaf进入material preparation前消费同一per-surface snapshot与pointer provider。policy准入要求单effect/单material、`dependencyOwnership == .none`、无logical target/history/clear/condition/compose；exact bool visibility须有sole producer与authored fallback，pointer gating须由全部launch variants的prepared source/reflection共同证明当前spatial-weighted Program真实消费pointer host uniform，`size`只接受exact static或sole direct scalar及`0.001` identity阈值。动态`size`先使用Program相同的authored range规则：范围外user值恢复authored lane，只有有效值低于阈值才inactive。inactive不会生成假Program或第二route，而由GraphExecutor把entry current原子复制到effect output；它与visual failure使用相同安全pair primitive，但以独立`activation-passthrough:*` telemetry记账且不增加fallback count。controlled `9000000131`的pointer、live visibility、size-zero和shared bounded rollback四门均完成GPU/publication/compositor/next-frame；size-zero只满足显式activation expectation，不能满足普通graph execution。该冻结合同当时不开放startup-disabled graph；现役后继只按下述 exact sole-bool safe pair-leaf 开放，不反向扩大 pointer/scalar、Timeline/SceneScript、utility/multi-effect/dependency或target/history topology；见[E-V1-XRAY-SHARED-STAGE-ACTIVATION-OWNER](runtime-evidence-index.md#e-v1-xray-shared-stage-activation-owner)。
>
> 2026-08-26 的后继只扩大 exact bool visibility，不扩大 pointer/scalar：already-admitted 单 effect FBO graph必须复用现役 `visualFailureFramebufferTopologyMayPassthrough`，因此 target 必须 nonunique/nonpersistent、无clear/condition，节点无condition/compose，读由同 effect 先前写支配，target全集被消费且只有一个terminal full-frame output；同时仍要求`dependencyOwnership == .none`与无clear function。inactive沿GraphExecutor现役FBO previous-current原子路径完成GPU/publication，未建立新graph/target/history/compositor owner。真实`3211615441`两个四pass/两target Standard Blur在live `blur=false`后各产生4个typed activation reject并保留后缀terminal compositor；owner/route census不变。startup-disabled、Timeline/SceneScript、unique/history、dependency、clear/condition/function/compose和任意FBO仍关闭；见[E-V1-FBO-STAGE-ACTIVATION](runtime-evidence-index.md#e-v1-fbo-stage-activation)。

> 紧随其后的 owner 后继不改变上述 graph/activation 合同，只把两个已闭合的 active ordinary-root Blur target 从 Standard Blur candidate 中精确撤权。startup-false 继续使用全局 sole-key view；active owner gate使用完整校验的 target-local direct-bool view，允许同一 key fan-out 到多个全为 bool `effectVisibility` 的 exact target，再与共享 ordinary-root topology admission求交。frame-driven Timeline/SceneScript、mixed sibling、rebuild key、utility/parent/child/dependency/provider/graph-output及任一不安全 target仍保留 incumbent。默认、profile-local disable与清除恢复三路均为0 dedicated，shared Program/FBO或 effect-local previous-current继续GPU/publication/compositor/next-frame；runtime不提供compiler-preview candidate计数，候选撤权由当前compiler guard和可执行compile-input partition门证明。最高结论只覆盖`3211615441:24#effect#613`与`26#effect#257`的bounded `generic-only / owner-migration`；见[E-V1-STANDARD-BLUR-DIRECT-BOOL-VISIBILITY-OWNER](runtime-evidence-index.md#e-v1-standard-blur-direct-bool-visibility-owner)。

> 同日的 startup-false 后继仍不开放 FBO：property binding只暴露全局sole valid、bool definition/instruction/authored fallback且非rebuild的exact target；planner只把 authored-false、ordinary image/solid/text、无parent/child/utility/cross-layer provider-consumer、无target/blocker、单material、exact current-pair、无command/condition/compose的leaf放回作者顺序。Program与activation policy同时存在才可live执行；Program不可用时走独立`initially-inactive-property-stage-passthrough` member，它复用activation copy但不计visual failure/fallback或CPU exact owner，也没有live consumer。真实`3767460992`中5项startup-false safe leaf被登记，Film Grain `17#effect#725`在同一surface/window由`sparklyonoff=true`从typed inactive切到普通Program并有next-frame；另3项保持inactive-only。见[E-V1-STARTUP-FALSE-EFFECT-VISIBILITY](runtime-evidence-index.md#e-v1-startup-false-effect-visibility)。
>
> Standard Blur KERNEL0 当前由 whole-stage 结构合取授权共享执行：node 0 为 `source-proven-graph-input-alpha-weighted-sample-average / generic-only`，node 1/2 为 `source-proven-normalized-sample-sum / generic-only`，node 3 为 `source-proven-unit-previous-blurred-composite / generic-only / fallbackOwner=none`，四者仍只形成一个 MaterialProgram/GraphExecutor product。node 3 可无 mask，或消费一个 distinct direct-red sample、typed `.opacityMask`、静态 authored asset candidate。node 1/2 scale 可为静态值、同一个sole `{user,value}` live scalar producer，或在无同key/target producer时各自保留exact authored scalar definition；live与definition-only都要求两个pass整stage同源，只有作者fallback与shader `float2` default为finite单值/equal-lane pair、无Timeline/SceneScript并通过逐consumer ABI预检时，通用MaterialProgram才投影为`(x,x)`。captured-main没有新增shader profile：composition/project/fullscreen utility须content/kind一致且无child/dependency；flags为`(false,false)`或`copyBackground=true && passthrough=true`时均可复用上述static/user-scalar cohort，copy+passthrough scalar又可与同一typed static mask fact组合，mixed pair仍保留incumbent。lower named-provider provenance只允许在exact `previous -> effect.input`之前保留一个同层`.primary/.instance`候选，内部blurred槽仍须graph-only，graph absent/unavailable不得回退到provider。产品 Standard Blur compiler从共享compile input分别检查frame-driven owner与validated effect-local direct-bool target；Timeline/SceneScript及任一未通过共享ordinary-root admission的user-property target保留incumbent，只有安全direct-bool target与完整whole-stage proof同时成立时才typed撤权。既有无-mask/masked/static/live、captured-main与direct-bool证据继续有效；`9000000999`又闭合definition-only default/recovery Program，`9000001002`锁定mixed live/fallback保留dedicated，profile disable只保留typed previous-current且不恢复dedicated owner。这里达到上述窄profile与exact authored-scalar fallback cohort的`S4 owner-migration`，不是整个Standard Blur/Bokeh family、官方pixel parity或V1完成；详见 [E-V1-ALPHA-WEIGHTED-SAMPLE-AVERAGE-OWNER](runtime-evidence-index.md#e-v1-alpha-weighted-sample-average-owner)、[E-V1-STANDARD-BLUR-MASKED-OWNER](runtime-evidence-index.md#e-v1-standard-blur-masked-owner)、[E-V1-STANDARD-BLUR-USER-SCALAR-SPLAT-OWNER](runtime-evidence-index.md#e-v1-standard-blur-user-scalar-splat-owner)、[E-V1-STANDARD-BLUR-AUTHORED-SCALAR-FALLBACK-OWNER](runtime-evidence-index.md#e-v1-standard-blur-authored-scalar-fallback-owner)、[E-V1-STANDARD-BLUR-CAPTURED-MAIN-OWNER](runtime-evidence-index.md#e-v1-standard-blur-captured-main-owner)与[E-V1-STANDARD-BLUR-DIRECT-BOOL-VISIBILITY-OWNER](runtime-evidence-index.md#e-v1-standard-blur-direct-bool-visibility-owner)。
>
> Standard Blur scalar 撤权还有一层产品 owner 预检：两个 Gaussian material 都必须从真实 Template/ShaderPreparation 重新证明唯一 `scale` mapping、非数组 vertex `float2` ABI、exact `{user,value}` wrapper 与 equal-lane typed default。live形态还要求同一个sole property producer；definition-only形态要求没有同property key或同target producer，并以唯一scalar definition逐pass bit-exact匹配material fallback。mixed来源、`float3`、数组、缺省或不等默认、definition/producer identity漂移都在撤权前关闭并保留 dedicated candidate；terminal composite proof不能单独授权整个四节点 owner handoff。
>
> scale-side exact Timeline vector2 现在是与 user-scalar 投影分离的 typed cohort：它保留两条作者 lane，不要求 shader default equal-lane，也不做 scalar splat。现役产品只认 typed definition 与 material fallback 的逐 lane bit-exact join；旧 standalone harness 在 definitions 为空时仍有 identity-only compatibility 分支，这是 V4/test debt，不是 Launch 可达产品路径。
>
> 当前第二个 V1 原子闭合classic primary named cross-layer carrier：`_rt_imageLayerComposite_<provider>_a` 经 typed publication 以 `.premultipliedColor` 进入普通 MaterialProgram slot，GraphExecutor 在同一 provider/consumer/effect/pass/slot/variant/frame identity 下消费并到达 compositor/next-frame；其后owner migration删除了旧Clipping planner/contract/execution plan/renderer与实现耦合测试owner。后续 atom 又证明同一 primary provider 可由两个 consumer 共享一次 publication，两个独立 primary provider也可各自服务一个consumer、同一consumer的两个ordinary effect/pass reference可共享一个reservation/publication。当前再开放一个独立的 hidden-image provider closure：effectful provider 的 GraphExecutor final 复制到独立 primary named reservation 后供下游读取，中间 provider 不持有 compositor。基础`generic-only`profile仍由登记的primary backward provider、slot 1、无user override、blend 0和constants absent-or-unit定义。`_b`为typed `unsupportedVariant`，非unit constant为typed `unsupportedConsumer`；forward/cycle、child/non-image、multi-dependency/reference、history与任意named texture仍未闭合。bounded frontend另只对可静态证明为component-wise的`float2/float3` `+ - * /`赋值表达式缩窄更宽裸vector operand；其他复合形态仍失败关闭。

> V1 forward-unavailable failure-radius原子只处理一个不能在当前frame order取得的primary `_a`输入，不执行、捕获或重排forward provider：consumer必须只有唯一外部primary reference，provider真实存在且位于consumer之后，reference精确映射唯一active effect/pass，受影响effect必须是无FBO/command/condition/compose的single ordinary material，graph无blocker。满足时该effect以typed `dependency-stage-reference-unavailable`复制previous-current，suffix仍由MaterialProgram/GraphExecutor继续；token绑定的frame target plan明确记录是否真实消费external primary，旧dependency plan不能再次把该exact layer误判成必须binding。真实`2419444134:27`因此只跳过Blend并让Water Waves与四个Shake进入Program、GPU/compositor/next-frame。该atom冻结时真实`2241938645:68`的secondary self-reference仍失败关闭；后继atom只收窄其失败半径。该门不表示forward provider execution、secondary/self/cycle、multiple provider/reference、FBO/history或任意dependency failure已支持，精确证据见[E-V1-FORWARD-DEPENDENCY-EFFECT-LOCAL-FAILURE](runtime-evidence-index.md#e-v1-forward-dependency-effect-local-failure)。

> V1 secondary-self unavailable failure-radius原子不执行secondary数据流：它只识别唯一一个self reference精确归属active effect/pass/slot且graph slot映射effect-entry input的情形，再复用ordinary FBO visual-failure topology，要求无clear/unique/history/condition/command/compose/blocker、target read均被更早write支配且唯一terminal output。满足时整个effect以typed `dependency-stage-secondary-reference-unavailable`丢弃全部material与未提交target state并发布previous-current，suffix继续；真实`2241938645:68`只跳过5-node Shine `effect=6`，Water Waves/Shake `effect=7...9`在首帧与next-frame继续GPU completed，terminal `effect=9`仍由唯一compositor消费。错slot mapping和unique/history同形图继续claim前拒绝。该atom没有创建`_b` producer/consumer、route开关或owner迁移，也不开放其他secondary/self/dependency/FBO形态；精确证据见[E-V1-SECONDARY-SELF-DEPENDENCY-EFFECT-LOCAL-FAILURE](runtime-evidence-index.md#e-v1-secondary-self-dependency-effect-local-failure)。

> R0-R5/B0-B25 只作 sealed evidence，不决定本页顺序；迁移记录统一查[历史索引](../../history/README.md)。本页只维护当前 primitive、执行边界和升级条件，现役顺序只看[Scene 兼容执行路线](../scene-compatibility-roadmap.md)。

> 后续 V0 证据又把 launch-time `material-variant-envelope-color-contract`、static uniform schema、纯多value-contributor歧义、唯一已证value contributor附带单个未证明script attachment、唯一value contributor的纯producer缺席、multi-contributor中一个或全部producer缺席、零value-contributor但有authored fallback的单个未证明script attachment、frame-time `uniform/dynamicUniformBindingInvalid` / `uniform/staticUniformBindingInvalid`，authored declaration 覆盖 host-owned field 的 `uniform/hostUniformDeclarationConflict`、alias/field双declaration的`uniform/uniformDeclarationConflict`，以及作者sampler schema失败的`material-variant-envelope-sampler-schema`依次加入同一typed visual fallback；它们只复制previous current、继续suffix，不把失败effect、未证明script、authored fallback、缺失producer、缺失static/host值、冲突declaration或未知sampler mode记为执行成功，也不从仍可用的多个contributor中选值或伪造texture。`activeUniformSchemaMissing`、真正host input编码、launch-time schema `uniformBindingInvalid`、无authored fallback的零contributor、同property key异target、重复/混合script attachment、texture/resource/target/state/lifecycle/runtime encode仍硬拒绝。六类新sampler binding/pass/target/divergence code同样继续硬拒绝，不能因provenance已精确就读取为visual fallback。

> launch-envelope owner 收敛又删除了旧 Finalizer/TextureResolver 隐式 cache 入口和 frame-time source-schema/variant compiler owner；未预编译 key 与 launch/frame identity 漂移现在原子拒绝，不会用 frame compilation 掩盖 launch 缺口。真实 `3767460992` 前后 22 个 Program subject 与 7 层 graph/GPU/compositor/next-frame 集合一致；这是 targeted no-regression，不是新增效果支持。

> sampler binding provenance 原子进一步让launch compile、frame selection与texture resolver共用同一binding验证：order、duplicate slot与slot/name identity在字典构造前typed拒绝，internal target、active `[PASS]`与cross-variant divergence各保留独立preparation code。真实`3767460992`的22个Program disposition、52个稳定transaction与7层GPU/compositor/next-frame集合保持不变；这是provenance/no-regression证据，不是视觉能力、Fast/full或官方一致。

> V0最终可见原子见[E-V0-CHROMATIC-AUTHORED-PROGRAM](runtime-evidence-index.md#e-v0-chromatic-authored-program)：预登记directional ROI的22/22帧均满足`R < 0 < G < B`，author-disabled control不执行，未证明combo-0只局部passthrough并保留GPU/compositor/next-frame。它封存此前sampler divergence的“下一V0门”表述，不放宽该hard rejection。

> V1首个公共可见原子见[E-V1-MATERIAL-COPY-HISTORY](runtime-evidence-index.md#e-v1-material-copy-history)：真实资源树派生的一个ordinary effect以2 material node、1 copy node、2 target、0 blocker形成291/291 terminal graph success；author-disabled不执行，非法copy target在admission失败关闭且不发布partial graph output。它只证明这一条unique history/FBO topology，不外推swap/compose/function/condition、其他history family或cross-layer dependency。
>
> V1普通FBO视觉失败原子见[E-V1-ORDINARY-FBO-VISUAL-FAILURE-PASSTHROUGH](runtime-evidence-index.md#e-v1-ordinary-fbo-visual-failure-passthrough)：exact单effect、2 ordinary material、1 ordinary FBO，且无clear/unique/condition/command/history/dependency/compose时，eligible frontend/uniform/optional texture失败现在跳过整个effect、丢弃未提交target mapping/resources并由GraphExecutor发布previous-current；下一帧重新解析并恢复普通graph。format仍由既有target/attachment admission决定，Metal正反门覆盖RGBA与已准入R8形态；fresh真实资源派生可见门只使用RGBA并闭合GPU/publication/compositor/next-frame，frame-1故障注入的严格报告保持NON-PASS但raw graph observation证明2 authored node全拒绝、0 material执行、空target state、安全输出完成，frame 2恢复2 material/1 target。route仍为`prefer-generic`；target/ABI/generation/hazard/lifecycle/runtime encode与所有未登记topology继续硬拒绝，不表示owner migration、任意FBO、Fast/fixed/full或官方parity。

> V1 ordered RGBA multi-writer 可见原子见[E-V1-ORDERED-RGBA-MULTIWRITER](runtime-evidence-index.md#e-v1-ordered-rgba-multiwriter)：项目自有的单effect代表graph依次以三个普通Program执行`effect input → target`、`constant blue → same target`、`same target → effect output`，两次写入的同一非unique `rgba_backbuffer` target由第二writer决定最终全蓝画面，并在首帧/next-frame都到达GPU completion、publication和唯一compositor。第二Program在launch-time被frontend拒绝时，同一共享failure topology丢弃三个node和target prefix，以typed `material-variant-envelope-frontend`发布完整previous-current；恢复运行与正门逐像素相同。故障报告因未执行exact target graph明确为NON-PASS；raw observation才是passthrough/compositor安全事实。该atom不改变`prefer-generic` route或owner，不开放history/unique/clear/command/condition/function/dependency、其他format、任意multi-writer或官方结果。

> V1 RG88 preserved-channel carrier 原子见[E-V1-RG88-PRESERVED-CHANNEL-CARRIER](runtime-evidence-index.md#e-v1-rg88-preserved-channel-carrier)：同一GraphTargets/MaterialProgram/GraphExecutor链把`rg88`映射为`rg8Unorm`、2 logical B/texel、R/G write mask和typed `.redGreenUnorm/.preservedChannels` publication；只有所有variant对该graph slot做直接`.rg`/MSL `.xy`投影时才准入。项目自有两Program包把`(0.2,0.8)`写入nonunique/no-clear target并重建终端RGBA，首帧/next-frame的target descriptor为`2048x1152/rg88:1`且可见输出逐像素为`51/204/0`；改为只读`.r`时以`rg88-red-green-graph-unproven`在claim前关闭并显示安全layer source，恢复包与正门逐像素相同。当批真实Bokeh只证明作者声明了RG88；后继varying-loop与preserved-alpha RGB filter原子现已让真实资源派生isolated Bokeh达到7 authored/7 material/0 rejected，但不反向扩大本RG88合同。本原子仍不开放clear/unique/history/command/multi-writer、其他投影/format、owner migration、V1完成或官方parity。

> V1 preserved-channel authored-clear seed 原子见[E-V1-PRESERVED-CHANNEL-CLEAR-SEED](runtime-evidence-index.md#e-v1-preserved-channel-clear-seed)：共享capability在该证据冻结时只对single-effect、nonunique、single-writer、无target command的R8/RG88/R16F/RG1616F exact carrier开放合法`initialClear`与first-read-before-write；consumer仍须分别是direct `.r`/`.rg`，且GraphExecutor在首次创建或重建时先提交initialize intent，后续帧复用previous-current而不重复clear。项目自有RG1616F A→B→A三Program包首帧由clear seed形成精确`RGB 223/38/13`，next-frame无initialize且逐像素相同；无clear同形反例在claim前typed关闭并保留layer source，恢复与正门逐像素相同。该冻结原子不开放unique、copy/swap、其他command、multi-writer、alias/whole/mixed projection和无clear first-read；后继unique边界只按下一项exact history atom开放。这不是Advanced Fluid执行、owner migration、V1完成或官方parity。

> V1 preserved-channel unique history 原子见[E-V1-PRESERVED-CHANNEL-UNIQUE-HISTORY](runtime-evidence-index.md#e-v1-preserved-channel-unique-history)：四种storage只有在single-writer/no-command图实际read-before-write且consumer分别为direct `.r`/`.rg`时，`unique:true`才复用现有GraphTargets persistent history。独立Metal门锁定四种typed attachment/publication、首帧seed、next-frame、wrong-descriptor rehydrate硬拒绝并保持previous-current、exact rehydrate、reset与恢复。真实资源派生RG1616F正式App正/恢复分别422/418次terminal success、首帧`seeded`、次帧`reused + historyRehydrateCopyCount=1`，反门claim/encode/GPU graph为0且安全passthrough；正/恢复截图逐字节相同并精确`RGB 223/38/13`。后继[E-V1-PRESERVED-CHANNEL-FRAME-LOCAL-UNIQUE](runtime-evidence-index.md#e-v1-preserved-channel-frame-local-unique)已经把single-writer/no-command/no-clear且先写后读的`unique:true`图明确归为frame-local，不取得persistent history；same-node read/write、copy/swap、其他command、multi-writer、alias/whole/mixed projection与更宽unique/history生命周期仍关闭。这不是Advanced Fluid执行、owner migration、V1完成或官方parity。

> V1 preserved-channel feedback、channel、ordered、frame-local与independent-signal后继分别见[E-V1-PRESERVED-CHANNEL-FEEDBACK-SWAP](runtime-evidence-index.md#e-v1-preserved-channel-feedback-swap)、[E-V1-PRESERVED-CHANNEL-COMPONENT-WHOLE-VECTOR](runtime-evidence-index.md#e-v1-preserved-channel-component-whole-vector)、[E-V1-PRESERVED-CHANNEL-ORDERED-FEEDBACK](runtime-evidence-index.md#e-v1-preserved-channel-ordered-feedback)、[E-V1-PRESERVED-CHANNEL-FRAME-LOCAL-UNIQUE](runtime-evidence-index.md#e-v1-preserved-channel-frame-local-unique)和[E-V1-INDEPENDENT-SIGNAL-FEEDBACK](runtime-evidence-index.md#e-v1-independent-signal-feedback)：现役loss-preserving IR保留`.r/.x`、`.g/.y`、`.rg/.xy`与complete-vector sample，typed regular RG接受R/G subset或whole vector，typed regular scalar R接受red-only或whole vector；`.g/.rg`对scalar、unproven与非typed resource仍关闭。ordered pair与ordinary frame-local unique保持各自严格生命周期；RGBA independent-signal只对transparent seed、唯一preserving update、唯一terminal composite/swap的exact pair开放。fresh真实Fluid完整19-node graph现已执行17 material + 2 swap、0 rejected，并取得GPU/publication/compositor/next-frame；targeted样本的0/1另含三个accepted-graph exact-backend effect缺口与layer533 `unclaimed-visible-effects` route failure。该结果不是Fluid隔离ROI、owner migration、V1完成或官方parity。

> V1 LightingV1 zero-source `#require` 原子见[E-V1-LIGHTINGV1-ZERO-SOURCE-REQUIRE](runtime-evidence-index.md#e-v1-lightingv1-zero-source-require)：官方公开合同没有公开`#require`或module ABI；当前只按version-bounded中性高层合同与项目更严格策略，让active exact `LightingV1`在指令位置typed整数`LIGHTING == 0`时生成zero-source typed provenance，并把该identity送入prepared digest、cache key和Program。它不注入stub/source line/官方shader。active nonzero/missing/noninteger/unknown/case mismatch与语法错误typed关闭；inactive合法directive零resolution、inactive malformed仍syntax reject；combo冲突保留更早typed schema rejection。项目自有普通Program已取得精确ROI、GPU/publication/compositor/next-frame的正→反→恢复证据。后继[E-V1-SAMPLER-DEFAULT-PURPOSE](runtime-evidence-index.md#e-v1-sampler-default-purpose)在该证据冻结时让真实Advanced Fluid node 17 slot 3越过texture-purpose，direct后继为animated authored asset unavailable所形成的`texture/textureBindingInvalid`，当时整体route仍先拒于function/condition且Fluid authored GPU为0；最新[E-V1-CONDITIONAL-FBO-COMMAND-CONTRACT](runtime-evidence-index.md#e-v1-conditional-fbo-command-contract)已使默认`LIGHTING=0`分支执行19 authored / 17 material / 2 swap并闭合GPU/publication/compositor/next-frame，`LIGHTING=1`仍由独立nonzero module ABI与animated sampler/resource合同在graph claim前拒绝。非零ABI、完整module family、Fluid true可见、官方parity与V1完成均未开放。

> V1 bounded small varying-array loop 原子见[E-V1-BOUNDED-SMALL-VARYING-ARRAY-LOOP](runtime-evidence-index.md#e-v1-bounded-small-varying-array-loop)：共享canonicalizer现在把双stage同名同型、count `1...128`的linked varying array纳入静态循环证明；只有初值0、静态`1...16`次、无nested loop或控制流退出/loop变量额外写，且每个被索引array都有`iterations <= count`时才展开为literal index。大于16的array仍需两stage同一literal prefix才压缩，小数组保持原声明shape。项目自有`vec2[4]`四次采样Program在首帧/next-frame输出逐像素`64/128/0/255`；dynamic-bound反例只做effect-local previous-current，恢复与正门逐像素相同。真实Bokeh探测已越过`v_TexCoord[i]`，但七个node仍先以`color/colorContractUnproven / shader-color-transfer-unresolved`拒绝。本原子不开放动态bound/index、越界、任意array/loop、Bokeh执行、owner migration、V1完成或官方parity。

> V1普通FBO command-graph视觉失败原子见[E-V1-ORDINARY-FBO-COMMAND-VISUAL-FAILURE-PASSTHROUGH](runtime-evidence-index.md#e-v1-ordinary-fbo-command-visual-failure-passthrough)：共享predicate允许single-effect ordinary material/copy/swap链，但要求无clear/unique/read-before-write history/condition/function/dependency/compose、每次target read都被同effect更早的作者write支配、material ordinal递增、所有target初始化/消费且唯一terminal full-frame output。eligible visual preparation失败丢弃整个prepared material/command prefix和未提交target state，再发布effect-entry previous-current；完整性错误仍硬拒绝。项目Metal门覆盖未见mixed chain，fresh真实资源派生RGBA门以2 material/2 copy/2 target闭合正常GPU/publication/compositor/next-frame，故障帧为4 authored/0 material/0 copy/4 rejected且下一帧恢复。严格故障报告明确NON-PASS；route仍为`prefer-generic`，不表示history lifecycle、condition/function、dependency、任意command/FBO或owner migration完成。
>
> [E-V1-PERSISTENT-HISTORY-PREENCODE-VISUAL-ROLLBACK](runtime-evidence-index.md#e-v1-persistent-history-preencode-visual-rollback)把上述失败隔离从旧ordinary拓扑predicate移到“已被共享target planner准入、且尚未编码任何authored command”的single-effect内部FBO边界。condition-pruned后的clear、unique、write-only target、copy/swap和合法history不再因形状本身阻止eligible Program/finalizer/pipeline视觉失败局部回滚；首帧/重置无已提交history时清空target state，稳定allocation保留上一已提交mapping/token/generation，fresh allocation只保留rehydrated history，candidate initialization/material/copy/swap及generation全部丢弃。纯状态门覆盖首帧、稳定、fresh和畸形mapping，production GraphExecutor + Metal门以RG1616F unique-history闭合typed uniform-schema failure、GPU previous-current与下一帧恢复；它只到`S3 executable`，没有新增fresh App/compositor证据。现役后继[E-V1-EXTERNAL-PRIMARY-PREENCODE-VISUAL-PASSTHROUGH](runtime-evidence-index.md#e-v1-external-primary-preencode-visual-passthrough)又只对pair-only、无logical target、single-effect、exact consumer/pass/slot且provider reservation/publication已经ready的`.externalPrimary`开放同一allowlisted Program/finalizer/pipeline局部门，并让外部slot ownership继续参与stage conservation。真实派生三段provider chain的故障运行证明三个consumer分别发布effect-entry current、suffix继续、provider capture/binding保留、GPU/compositor/next-frame完整，恢复后回到ordinary Program。external+FBO、function clear、provider缺席/错配以及所有target/ABI/generation/epoch/hazard/lifecycle/runtime encode完整性错误继续硬拒绝。该外部primary包冻结时曾把下一步记为V1-B；后继composition-subtree与conditional generated-RGB真实运行重新暴露并闭合两个V1-A atom，当前V1-A首断点已前移到VHS，不能用本包的冻结结论跳到V1-B或V2。
>
> V1 Depth Parallax bounded owner迁移见[E-V1-DEPTH-PARALLAX-SHARED-OWNER](runtime-evidence-index.md#e-v1-depth-parallax-shared-owner)：共享external compiler与产品normalizer统一补齐`CAST3X3 -> mat3`、active sampler texture-transform ABI、无authored-helper use的unchanged sample alias passthrough color fact，以及当前函数根作用域唯一immutable finite-integral float bound的dynamic-early-exit loop预算；index/上界不得传给authored helper，含loop helper只可由`main`唯一直接且非loop-nested调用，uniform、异函数同名decoy、mutable/nonintegral bound、`||`、index参与第二条件、body写index、重复/间接helper call或lexical nested loop均关闭。真实`3766387484:17#effect#23`命中stage-uniform passthrough `generic-only / genericCompilerArtifact`并保持前后缀、GPU/publication/compositor/next-frame；坏artifact只保留effect-entry current，显式disable回滚shared bounded frontend。旧Depth planner/profile/compiler/backend、专用loader/pipeline/renderer/repository与耦合测试已删除。该结论只覆盖旧owner实际执行的bounded Q1 source/topology，不外推当前corpus另15个未由旧owner执行的shape、Q2 DXT1可见位移/fidelity、独立Depth ROI、完整family、V1完成或官方parity。
>
> V1 exact-effect lifecycle attribution原子见[E-V1-EXACT-EFFECT-LIFECYCLE-ATTRIBUTION](runtime-evidence-index.md#e-v1-exact-effect-lifecycle-attribution)：graph observation本来已经携带runtime、layer、effect与descriptor identity，但benchmark曾只按runtime+layer串联allocation/history transition，导致同层ordinary effect与persistent-history effect相互污染并产生假COW/identity红项。现役gate按完整effect identity独立审计，同时保留旧无effect字段日志的legacy scope。该包fresh原始`3767343314:17`同层Sharpen、Water Ripple、Cursor Ripple共1233个transaction全部terminal success，其中Cursor Ripple 410次COW合法；它只纠正证据归属，不改变当时产品owner。Water Ripple随后由[E-V1-WATER-RIPPLE-GRAPH-OWNER](runtime-evidence-index.md#e-v1-water-ripple-graph-owner)迁到普通Program；Cursor Ripple再由[E-V1-CURSOR-RIPPLE-SHARED-FEEDBACK-OWNER](runtime-evidence-index.md#e-v1-cursor-ripple-shared-feedback-owner)迁到结构型shared feedback/history primitive、preserved-RGBA Program与统一pointer host schema。两套专用类型族均已删除；现役同层3个effect全部`admitted-generic / program`，其后两个state Program已由[E-V1-PRESERVED-RGBA-STATE-TRANSFORM-OWNER](runtime-evidence-index.md#e-v1-preserved-rgba-state-transform-owner)迁到source/typed-input驱动的`generic-only / genericCompilerArtifact`；不能据此宣称任意history、独立视觉parity或V1完成。

> V1 shared two-target feedback owner见[E-V1-CURSOR-RIPPLE-SHARED-FEEDBACK-OWNER](runtime-evidence-index.md#e-v1-cursor-ripple-shared-feedback-owner)：`SceneGraphRenderTargetPlan`只按单effect、3个ordinary material、2个相同descriptor的nonunique/no-clear RGBA8 target及`seed → A → seed`、`seed + effectInput → effectOutput`依赖授予history seed；target/effect/path/name/hash都不参与准入。两个state attachment以`.preservedRGBAUnorm`写全四通道并发布`.data/.preservedChannels`，rehydrate保持content kind；任一Program还须独立证明whole-output与regular graph sampler。fresh原始`3767343314:17#effect#234`默认/`disable-generic`均1/1 PASS且dedicated/fallback=0，旧Cursor planner/profile/pipeline/renderer/loader/backend已删除。后继preserved-RGBA profile已把两个state Program的普通compiler route迁到`generic-only`，并在坏artifact下用typed discarded-history publication把失败限制在单effect；这个公共primitive仍不开放其他节点数、descriptor/format、command/condition/function/compose、跨effect target、任意history或现役Cursor隔离ROI。

> V1 preserved-RGBA state-transform compiler owner见[E-V1-PRESERVED-RGBA-STATE-TRANSFORM-OWNER](runtime-evidence-index.md#e-v1-preserved-rgba-state-transform-owner)：公共artifact builder从authored fragment source证明definite whole output，route只接受`.preservedRGBAUnorm`、exact一个regular graph target、无external/其他auxiliary或至多一个typed opacity mask；不使用Cursor/effect/sample/path/hash。原始`3767343314`的两个state Program与painted-mask readiness variant缺省/恢复均为`generic-only / genericCompilerArtifact`，显式disable才回滚shared bounded frontend。坏exact artifact继续按严格matrix为NON-PASS，但共享executor只丢弃该effect本帧未发布history，保留submission pin且不生成history pin，前缀、GPU、compositor与next-frame继续；missing/extra/conflicting token、错effect及伪造非空transaction/resource仍硬拒绝。
>
> V1 dependency-subgraph capture failure原子见[E-V1-DEPENDENCY-SUBGRAPH-CAPTURE-FAILURE-CONTAINMENT](runtime-evidence-index.md#e-v1-dependency-subgraph-capture-failure-containment)：unified preparation已提交exact external-primary reservation、但同帧ordinary provider capture没有publication时，runtime只撤销对应无history consumer transaction并释放其allocation，再按最后已composite tail重算独立successor；另一provider→consumer分支在同一command buffer继续GPU/publication/compositor，故障分支下一帧恢复。缺reservation、epoch/provider/kind/object漂移与history-bearing transaction继续整帧失败关闭，不会借ordinary miss路径伪造资源。Debug故障选择器只接受排序provider ordinal且一次性触发，不进入Release或产品视觉分派。classic primary named-provider仍是既有`generic-only / owner-migration-complete`；本原子只收窄classic单consumer failure radius，不开放secondary/multiple dependency、任意resource/lifecycle失败或官方parity。bounded nested/effectful closure的同类pre-encode capture miss由后继[E-V1-NESTED-DEPENDENCY-CASCADE-FAILURE-CONTAINMENT](runtime-evidence-index.md#e-v1-nested-dependency-cascade-failure-containment)独立闭合。
>
> V1 persistent-history executor invalidation原子见[E-V1-PERSISTENT-HISTORY-EXECUTOR-INVALIDATION-RECOVERY](runtime-evidence-index.md#e-v1-persistent-history-executor-invalidation-recovery)：invalidation仍是hard integrity boundary，旧reset generation的未完成transaction以typed `executor-invalidation`终止且旧history residency被清空；同一共享coordinator的新generation首transaction现在保留该reset cause，以`history=seeded`、零rehydrate重新进入GPU/publication/compositor，随后才恢复`history-copy-on-write`。Debug trigger只按时间触发现有surface runtime，不读取内容identity。故障报告因预期failed observation保持NON-PASS；它证明有界reset/recovery和安全可见连续性，不开放device loss、scene switch、surface stop、seek、系统事件pause、多surface、任意history topology、owner migration或官方parity。

> V1 persistent-history scene-switch rebuild原子见[E-V1-PERSISTENT-HISTORY-SCENE-SWITCH-RECOVERY](runtime-evidence-index.md#e-v1-persistent-history-scene-switch-recovery)：正式host re-launch创建新coordinator、runtime-instance identity与独立physical pool，旧runtime的transaction/allocation计数不会与新runtime混为一个生命周期；benchmark按runtime scope保持generation/mapping/history严格审计，而不是放宽单调性。隔离同输入的两个runtime各自从initial seed进入COW、pool UUID不相交，surface `1→1→0`，GPU/publication/compositor/next-frame与三时点可见构图闭合。它只证明bounded同输入rebuild safety，不开放其他不同topology/provider/format/SceneScript/particle state、device loss、surface stop、seek、系统事件pause、多surface、owner migration或官方parity。

> V1 persistent-history different-scene rebuild原子见[E-V1-PERSISTENT-HISTORY-DIFFERENT-SCENE-SWITCH](runtime-evidence-index.md#e-v1-persistent-history-different-scene-switch)：Debug alternate-input只接受隔离root并调用正式host launch；新Scene保留`533/557` identity和普通persistent topology，但改变camera、clear、layer alpha/color/transform与rate。旧/new runtime各自从initial seed进入COW，184/494个成功atom、pool UUID不相交，合计678/678 terminal success、676次COW、surface `1→1→0`；ready浅灰与after深蓝完整构图证明新authored state实际进入产品输出。它只闭合same-identity/different-authored-state反例，没有切换瞬间截图，不开放任意不同Scene/topology、device loss、seek、多surface、owner migration或官方parity。

> V1 persistent-history different-target-topology switch原子见[E-V1-PERSISTENT-HISTORY-DIFFERENT-TOPOLOGY-SWITCH](runtime-evidence-index.md#e-v1-persistent-history-different-topology-switch)：相同`533/557/558,57` identity从全尺寸`_rt_FullCompoBuffer1/2`切换到新logical target pair与`scale:2` descriptor，仍只使用正式host launch和共享GraphExecutor/GraphTargets/pool/compositor。旧/new runtime各自从initial seed进入COW，181/478个成功atom；descriptor为`2048x1152:2 → 1024x576:2`，descriptor/mapping hash、runtime identity与pool UUID均不相同，659/659 terminal success、657次COW且可见新构图。当前产品链无需修复；该证据只闭合一个bounded RGBA/BGRA、2 material + 1 copy topology switch，没有切换瞬间截图，不开放任意history topology/format/provider、device loss、seek、多surface、owner migration或官方parity。

> V1 persistent-history surface-stop/relaunch原子见[E-V1-PERSISTENT-HISTORY-SURFACE-STOP-RELAUNCH](runtime-evidence-index.md#e-v1-persistent-history-surface-stop-relaunch)：正式host stop先让旧surface runtime以`.surfaceStop`失效并把surface/provider/context降为0，后续正式launch才从空宿主创建新runtime与独立physical pool。隔离同输入的两个runtime各自从initial seed进入COW、pool UUID不相交，surface完整序列为`1→0→1→0`，GPU/publication/compositor/next-frame与relaunch后三时点可见构图闭合。intentional stop间隙没有surface，故不声称无黑屏连续性；该原子不开放任意跨Scene作者语义、device loss、seek、系统事件pause、多surface、owner migration或官方parity。

> V1 persistent-history pause/resume retention原子见[E-V1-PERSISTENT-HISTORY-PAUSE-RESUME](runtime-evidence-index.md#e-v1-persistent-history-pause-resume)：正式全局播放控制只冻结SceneClock并停/启同一frame driver，surface在pause前后保持1；一个pause前已提交transaction正常排空后约4.194秒没有新graph transaction，resume首transaction沿同一runtime与physical pool从allocation/mapping 181推进到182并继续`history-copy-on-write`，没有initial seed/reset或第二owner。664/664 transaction成功且resume后构图完整；没有暂停期截图证据。本原子不开放系统focus/sleep/lock事件、seek/discontinuity、device loss、多surface、任意history topology、owner migration或官方parity。
>
> V1 named-provider 公共可见原子及owner migration见[E-V1-CROSS-LAYER-NAMED-PROVIDER](runtime-evidence-index.md#e-v1-cross-layer-named-provider)：真实资源树派生的classic `BLENDMODE=5`与未见组合`Opacity=1`都经普通Program/GraphExecutor闭合GPU/publication/compositor/next-frame；secondary `_b`、nonunit `Opacity=0.5`和route-disabled分别形成typed零执行反例。旧Clipping产品owner已删除，但该结果只证明登记的classic primary profile；two-consumer共享、两个独立provider各一consumer及单consumer多reference另由独立证据包闭合，仍不外推任意named target、secondary、cycle、nested/effectful provider、整个family或V1完成。
>
> V1第三个公共可见原子见[E-V1-FBO-UV-ADDRESS-REPEAT](runtime-evidence-index.md#e-v1-fbo-uv-address-repeat)：exact absent/repeat target address、R8 scalar writer、typed publication与direct-red consumer由项目GPU正反门闭合；隔离真实`3765760121:73`的ordinary two-material graph达到GPU completion、terminal compositor、next-frame与预声明局部非均匀周期ROI。后续独立owner批次以prepared source事实、active sampler与graph-owned R8 binding fact定义两个非样本profile，使两node默认`generic-only / genericCompilerArtifact`；artifact故障局部回退layer source且不恢复旧owner，`disable-generic`可显式回滚。该profile已达owner migration，但不外推arbitrary R8/UV、Glitter family、Fast Suite、官方parity或V1完成。
>
> V1第四个公共可见原子见[E-V1-GENERIC-LAYER-LOCAL-COMPOSE](runtime-evidence-index.md#e-v1-generic-layer-local-compose)：单effect、至少两ordinary material、0 authored target/command/history/background/dependency/condition的bounded full-frame链，只有所有非末node为boolean `compose:true`、末node nil/false时才用pair轮转。两个非`dedicated:` Program已闭合GPU/publication/compositor/next-frame与首/次Program preparation failure的whole-effect rollback；同一shape的launch-time visual contract/dynamic-owner失败形成typed whole-effect passthrough并保留suffix，history/FBO反例继续硬拒绝。2026-08-21 又把同slot normalized positive sample-sum source profile迁为`generic-only`：真实`2802243144`三条raw Precise compose与[显式authored-intermediate/FBO cohort](runtime-evidence-index.md#e-v1-precise-authored-intermediate-owner)均由共享MaterialProgram/GraphTargets/GraphExecutor执行；旧raw adapter及`.preciseGaussian` planner/compiler/backend/pipeline/renderer、运行计划与direct-test oracle均已删除。该结论不外推其他compose/FBO、真实MASK/BLURALPHA、Refraction、scene background、Fast Suite或官方parity。
>
> V1第五个公共可见原子见[E-V1-SCENE-BACKGROUND-COMPOSE](runtime-evidence-index.md#e-v1-scene-background-compose)：exact case-insensitive `_rt_FullFrameBuffer` 作为renderer-owned same-frame main-target provider进入严格two-node compose shape，独立于layer source/current、effect FBO/history、named publication和final compositor。项目自有正例闭合Program、GPU/publication/compositor/next-frame；三node反例typed拒绝并回到与no-effect control像素一致的base output。不外推stock Refraction、normal/mask、其他background/dependency compose、owner migration、Fast Suite或官方parity。
>
> V1第六个公共可见原子见[E-V1-SAME-DESCRIPTOR-SWAP](runtime-evidence-index.md#e-v1-same-descriptor-swap)：项目自有ordinary `material -> material -> swap -> material` graph以两个同descriptor declared FBO交换logical mapping，后段material使整帧输出与no-swap control形成明确红/蓝差异。extent expression、format、address、unique或clear不匹配时，launch admission以typed `swap-target-descriptor-incompatible`在claim/allocation/encode/publication前拒绝，并回到与author-disabled base逐像素一致的layer source。它不外推跨帧mapping/history、Fluid、condition/function、其他format、owner migration、Fast Suite或官方parity。
>
> V1第七个公共可见原子见[E-V1-SHADER-DEFAULT-ZERO-CONDITION](runtime-evidence-index.md#e-v1-shader-default-zero-condition)：condition admission在完整graph effect、descriptor layer/effect、material path/ID与唯一双stage authored shader contract identity下，只接收词法无条件、default为0且options允许0的authored combo。显式material/instance值仍先于implicit zero；nonzero、readiness、format、disabled、branch-only、同键条件冲突、malformed、重复identity/stage与probe overflow全部拒绝。项目自有正反fixture闭合red/blue visible output和像素相同rollback；该证据冻结时Fluid只作为NON-PASS相邻定位，下一首断点为function invocation，不是Fluid执行证据。后继function、feedback与[E-V1-CONDITIONAL-FBO-COMMAND-CONTRACT](runtime-evidence-index.md#e-v1-conditional-fbo-command-contract)已经让默认`LIGHTING=0`分支执行完整19-node graph；不能继续把旧首断点或旧GPU为0写成现役事实。
>
> V1第八个 function atom见[E-V1-MATERIAL-FUNCTION-INVOCATION](runtime-evidence-index.md#e-v1-material-function-invocation)：definition registry 不再在 admission 被静默丢弃，也不自动 clear；显式 request 经过 effect/name/epoch/lease 校验后，clear intent 参与 target generation、publication 与 rollback，后续 ordinary material 可读取 clear 后内容。该 contract harness 不是 App/SceneScript bridge、Fluid 支持或官方时序 parity 证据。

> 新增 V1 mixed command atom见[E-V1-MIXED-COPY-SWAP-COMMAND-CHAIN](runtime-evidence-index.md#e-v1-mixed-copy-swap-command-chain)：真实 Motion Blur 派生 graph 在同一 descriptor-identical target pair 上按作者顺序执行 copy 后 swap；未声明 swap target 在 admission 前关闭并保留 layer-source passthrough。该结果不外推任意 mixed command、跨帧 mapping/history lifecycle 或 dependency family。

> 新增 V1 condition/command 组合 atom见[E-V1-CONDITIONED-MIXED-COMMAND-PRUNING](runtime-evidence-index.md#e-v1-conditioned-mixed-command-pruning)：显式 typed combo 为 1 时执行 copy→swap，为 0 时只剪掉 swap 并保留同一 graph 的合法 copy、GPU/publication/compositor/next-frame。它不外推 conditional FBO/binding/function/dependency 或任意表达式/provider。

> 新增 V1 reverse mixed command atom见[E-V1-MIXED-SWAP-COPY-COMMAND-CHAIN](runtime-evidence-index.md#e-v1-mixed-swap-copy-command-chain)：同一 descriptor-identical target pair 由首 material seed 后按作者顺序执行 swap→copy，末 material 读取 copy 后 logical target；非法 swap target 在 admission 前关闭并保留 layer-source passthrough。该结果不外推其他 target seed、跨帧 mapping/history lifecycle、dependency family 或 arbitrary mixed ordering。

> 新增 V1 primary provider multiple-consumer atom见[E-V1-PRIMARY-NAMED-PROVIDER-MULTIPLE-CONSUMERS](runtime-evidence-index.md#e-v1-primary-named-provider-multiple-consumers)：同一个 hidden composition provider 的一次 primary publication 被两个普通 MaterialProgram consumer 在同一 frame epoch/object 下消费，两个 graph 均闭合 GPU/publication/compositor/next-frame；secondary `_b` 与 route-disabled 反例均零 graph 执行并保留安全输出。它只覆盖单一 backward provider、两个 consumer、slot 1、无 user override、blend 0、unit constants，不外推单 consumer 多 reference、多个 provider、nested/effectful provider、owner migration或整个dependency family。two-provider atom另见[E-V1-TWO-PRIMARY-PROVIDERS](runtime-evidence-index.md#e-v1-two-primary-providers)，两个独立 primary publication 各自服务一个 consumer；cycle 另以真实 `9000000700` 反向依赖 fixture 取得 `cyclicDependency`、零 target/binding/graph 执行和安全帧，作为明确硬拒绝边界。

> 新增 V1 single-consumer multi-reference atom见[E-V1-SINGLE-CONSUMER-MULTI-REFERENCE](runtime-evidence-index.md#e-v1-single-consumer-multi-reference)：同一 primary provider 的一次 reservation/publication 被同一 consumer 的两个 ordinary authored effect/pass reference 共享，两个 Program stage 均闭合 GPU/publication/compositor/next-frame；secondary 与 route-disabled 反例均零 graph 执行并保留安全输出。该 profile 只开放 primary、slot 1、无 user override、blend 0、unit constants，不外推 secondary、mismatch、nested/effectful provider、其他 slot 或整个 dependency family。

> 新增 V1 nested/effectful named-provider atom见[E-V1-NESTED-EFFECTFUL-NAMED-PROVIDER](runtime-evidence-index.md#e-v1-nested-effectful-named-provider)：可见 image root 反向可达的 hidden image provider closure 可逐层先执行普通 Program/GraphExecutor，再把 graph final blit 到独立 primary named reservation。三层正例 `92 -> 104 -> 1104` 的中间 `92/104` 只 `output-consumed`/named publication，compositor集合仅`1104`；缺失末端provider与显式route-disabled均级联撤销整条不可满足closure、零graph execution并保留安全输出。只开放primary/backward/acyclic/single-dependency hidden-image链，不外推secondary、forward/cycle、child/non-image、multi-dependency/reference、history、resize/device-loss或任意深度。

> 后继 V1 nested dependency cascade failure原子见[E-V1-NESTED-DEPENDENCY-CASCADE-FAILURE-CONTAINMENT](runtime-evidence-index.md#e-v1-nested-dependency-cascade-failure-containment)：renderer在消费resolved-material claim前先解析external-primary输入，使ordinary raw provider capture miss可以沿既有未编码、无history ledger局部撤销门逐层级联，而不是因首个effectful provider已消费claim扩大为整帧失败。真实派生`91 -> 92 -> 104 -> 1104`故障帧对`92/104/1104`记录3次typed local rejection，独立Program layer`1200`仍同帧GPU/publication/compositor；下一帧四层全部恢复，compositor集合保持`[1104,1200]`。该结果不改变route/owner，只覆盖primary/backward/acyclic/single-dependency/no-history/pre-encode ordinary capture miss；encode后的publication failure、identity/epoch/generation/object/ABI/hazard/lifecycle、history、secondary/multi-dependency/reference继续硬拒绝或未闭合。

> 新增 V1 authored FBO scale-extent atom见[E-V1-AUTHORED-FBO-SCALE-EXTENT](runtime-evidence-index.md#e-v1-authored-fbo-scale-extent)：真实 Motion Blur 派生 graph 的 `3840x2160` authored surface 先按 full-frame policy 形成 `2048x1152` input，再把两个 `scale:2` RGBA target解析为`1024x576/rgbaBackbuffer`，descriptor identity贯穿actual frame plan、allocation/publication、GPU/compositor/next-frame与resize/restore telemetry。把copy-connected第二target改为`scale:1`时，`1x1` launch probe仍会同时clamp为`1x1`，但actual frame plan以typed `frame-target-plan-unsupported-target-descriptor`只对该layer局部降级并保留安全输出。该结果只到`slice-visible / bounded authored FBO scale-extent chain / prefer-generic`，不外推fit、arbitrary absolute/composed extent、persistent lifecycle、owner migration、Fast/fixed/full或官方parity。

> 新增 V1 authored FBO fit-extent atom见[E-V1-AUTHORED-FBO-FIT-EXTENT](runtime-evidence-index.md#e-v1-authored-fbo-fit-extent)：同一真实 Motion Blur 资源树派生 ordinary graph 把两个 target 改为 corpus 已观察过的 authored `fit:256` spelling，actual `2048x1152` input 解析为两个 `256x144/rgbaBackbuffer` target，并沿既有 allocation/publication/GraphExecutor/compositor/next-frame 闭合。把第二 target 改为 `fit:128` 时，`1x1` probe 仍 accepted，actual frame 以 typed `frame-target-plan-unsupported-target-descriptor` 局部降级且安全底图继续完成；该批当时只把`fit:256 + scale:2 -> 128x72`锁为unseen harness，现役真实闭环见下一条。该结果只到 `slice-visible / bounded authored FBO fit-extent chain / prefer-generic`，该批本身不外推 arbitrary fit、absolute/composed extent、Fluid、persistent lifecycle、owner migration、Fast/fixed/full或官方 parity。

> 新增 V1 authored FBO fit-scale composed-extent atom见[E-V1-AUTHORED-FBO-FIT-SCALE-EXTENT](runtime-evidence-index.md#e-v1-authored-fbo-fit-scale-extent)：同一 ordinary 2-material/3-copy representative 的两张target按公共resolver先`fit:256`再`scale:2`，actual `2048x1152` input解析为`128x72/rgbaBackbuffer:2`并闭合allocation/publication/GPU/compositor/next-frame。第二target改为`scale:1`时actual frame以typed descriptor mismatch局部拒绝；恢复fit-only输入后重新得到`256x144`并执行。该结果只到`slice-visible / bounded authored FBO fit-then-scale extent chain / prefer-generic`，不开放arbitrary composed extent、其他format/topology、owner migration、Fast/fixed/full或官方parity。

> 新增 V1 authored FBO absolute-extent atom见[E-V1-AUTHORED-FBO-ABSOLUTE-EXTENT](runtime-evidence-index.md#e-v1-authored-fbo-absolute-extent)：corpus snapshot 对真实 `3765760121` 记录一次 `width:256 / height:256` authored target，只证明合法 spelling；当前正门仍使用 Motion Blur 资源树派生 ordinary graph，把两个 RGBA target 写成 `width:640 / height:360` 并闭合 actual plan、allocation/lease、GPU/publication/compositor/next-frame。第二 target 改为 `320x180` 时，input-independent storage mismatch 以 typed `frame-target-plan-unsupported-target-descriptor` 在 claim/encode/GPU 前局部降级，安全底图继续完成；该批当时只把单轴 `width:640 -> 640x1152` 锁为 unseen harness，现役App闭环见下一条。该结果只到 `slice-visible / bounded authored FBO absolute-extent chain / prefer-generic`，该批本身不外推 arbitrary absolute、single-axis/composed extent、Fluid、persistent lifecycle、owner migration、Fast/fixed/full或官方 parity。

> 新增 V1 authored FBO single-axis extent atom见[E-V1-AUTHORED-FBO-SINGLE-AXIS-EXTENT](runtime-evidence-index.md#e-v1-authored-fbo-single-axis-extent)：同一 ordinary representative 的两张target只声明`width:640`，公共resolver保留actual input height并形成`640x1152/rgbaBackbuffer:2`，闭合allocation/publication/GPU/compositor/next-frame；第二target改为`width:320`时typed局部拒绝，恢复双轴`640x360`后重新执行。该结果只到`slice-visible / bounded authored FBO single-axis extent chain / prefer-generic`，不开放height-only、arbitrary single-axis/composed extent、其他format/topology、owner migration、Fast/fixed/full或官方parity。

> 新增 V1 authored FBO non-enlarging extent cohort见[E-V1-AUTHORED-FBO-NONENLARGING-EXTENTS](runtime-evidence-index.md#e-v1-authored-fbo-nonenlarging-extents)：同一真实资源派生ordinary Motion Blur consumer分别把两张RGBA target声明为`height:360`、`scale:4`与`width:1000 + fit:600 + scale:2`，actual `2048x1152` input形成`2048x360`、`512x288`与`260x300`，三路均闭合shared Program/GraphExecutor、allocation/publication、GPU/compositor/next-frame。三种第二target mismatch均在encode前以typed descriptor reason局部拒绝并保留安全frame。该cohort不开放`scale<1`、min-1、arbitrary single-axis/composed extent、其他format/topology、owner migration、Fast/fixed/full或官方parity。

> 新增 V1 authored FBO upscale extent atom见[E-V1-AUTHORED-FBO-UPSCALE-EXTENT](runtime-evidence-index.md#e-v1-authored-fbo-upscale-extent)：同一隔离ordinary Motion Blur consumer把两张RGBA target声明为`scale:0.5`，actual `2048x1152` input形成`4096x2304/rgbaBackbuffer:2`，在现役transaction budget内完成421/421 terminal graph success、420次history COW、GPU/publication/compositor/next-frame。只把第二target恢复为`scale:1`时以typed descriptor mismatch在claim/encode/GPU前局部拒绝并保留安全frame。该结果只到`slice-visible / bounded authored FBO upscale extent chain / prefer-generic`，不开放min-1、arbitrary extent/format/topology/lifecycle、owner migration、Fast/fixed/full或官方parity。

> 新增 V1 authored FBO minimum extent atom见[E-V1-AUTHORED-FBO-MINIMUM-EXTENT](runtime-evidence-index.md#e-v1-authored-fbo-minimum-extent)：同一隔离ordinary Motion Blur consumer把两张RGBA target声明为`scale:10000`，actual `2048x1152` input经floor/min-1形成`1x1/rgbaBackbuffer:2`，完成365/365 terminal graph success、364次history COW、GPU/publication/compositor/next-frame。只把第二target改为`scale:1000`时，launch `1x1` probe仍同为`1x1`，actual frame才形成`1x1`对`2x1`并以typed descriptor mismatch在claim/encode/GPU前局部拒绝、保留205/205安全frame。该结果只到`slice-visible / bounded authored FBO minimum extent chain / prefer-generic`，不开放其他arbitrary extent/format/topology/lifecycle、owner migration、Fast/fixed/full或官方parity。

> 新增 V1 authored FBO input-extent allocation-lifecycle atom见[E-V1-AUTHORED-FBO-INPUT-EXTENT-LIFECYCLE](runtime-evidence-index.md#e-v1-authored-fbo-input-extent-lifecycle)：solid projected capture 在 `1.0 → 0.25 → 1.0` 下让 graph input 形成 `2048x1152 → 963x542 → 2048x1152`。两个 `scale:2` target 随 input 改为 `1024x576 → 481x271 → 1024x576`，reprepare seed 新 history 并显式丢弃旧内容；未见的双 `640x360` absolute target保持descriptor不变，reprepare复用并复制 1 个 readable history closure。stable same-plan frame全部记为`history-copy-on-write`，不是resize churn；mixed scale/absolute只在中间尺寸typed fallback，恢复后同层graph再成功。三份fresh report均PASS并闭合GPU/publication/compositor/next-frame，但截图只有non-black/advisory且无motion，不是视觉fidelity或官方parity。该结果只到`slice-visible / bounded authored FBO graph-input extent allocation lifecycle / prefer-generic`，不外推arbitrary lifecycle、load/store/seek/stop、多surface、owner migration、Fast/fixed/full、性能/长稳或release。

## 1. 证据与使用方式

本页把以下资料压成一张当前能力矩阵，不替代原始语义文档，也不作为逐行任务队列：

- [Scene 格式与 Render Graph](scene-format-and-render-graph.md)：字段、资源身份、pass 顺序和目标执行合同；
- [Effects 语义全集](effects-reference.md)：45 个公开 effect 的输入、pass/RT 和正确空间；
- [官方页面全目录](official-page-catalog.md)：官方 Shader/Effects 页面无遗漏入口；
- [运行证据索引](runtime-evidence-index.md)：IR、ShaderContract、strict Blur、Local Contrast shared Program owner、conditional straight-union owner migration与GPU证据包；
- [资料来源与证据索引](source-index.md)：官方、样本、第三方和当前运行证据的可信边界。

这里的等级只描述 **MyWallpaperX 当前实现成熟度**，不是官方语义的可信等级，也不是某个 effect 的视觉相似度。

### 1.1 `linux-wallpaperengine` 第三方结构复核边界

固定 revision `b016d7d1fdcf4e5fd2f9c9fa420a8aaa07fee02d` 没有附带 stock effect/material/shader assets，运行时要求用户提供官方 Wallpaper Engine assets。因此它不能核验 45 个 effect 的逐参数和算法；本页只用它交叉检查 generic contract，并保留以下未进入官方 A 级表的候选：

- `ShaderUnit.cpp:77-94` 的处理顺序是 include -> `#require` -> variables；`#require` 只识别 `LightingV1`，生成的 `PerformLighting_V1` 固定返回零（`ShaderUnit.cpp:313-376`）。这只是`third-party-reference-pattern`：证明私有 shader 中可能存在 module directive，不证明该 stub、module schema或官方ABI正确，也不是现役zero-source实现输入；当前实现不复制或生成该stub。
- 它的 annotation/combo/sampler 解析依赖 regex/string，sampler slot 只提取单个数字且 combo 只接受整数（`ShaderUnit.cpp:442-543`）；不得作为未来 frontend 的实现模板。
- `CPass.cpp:824-890` 还尝试绑定 `g_TextureReductionScale`、`g_Brightness`、`g_UserAlpha`、`g_CompositeColor`、`g_LightAmbientColor`、`g_LightSkylightColor` 和 `g_NormalModelMatrix`。这些名称只属于 `third-party-reference-pattern` runtime 候选，不追加到官方 Variables 合同；须在合法输入和官方行为中另行确认声明、类型与作用域。
- 同一 binder 把 Left/Right 16/32/64 spectrum 都指向同一 mono 数组，并把部分 effect projection/normal matrix 简化为 identity 或现有 matrix。它只能帮助发现需要的 binder surface，不能提供 stereo、matrix 或 lighting 数值 golden。

| 等级 | 唯一含义 |
|---|---|
| `L0` | 当前代码没有识别或承载该语义；只有文档需求不算实现。 |
| `L1` | 能发现资源、路径、blob、raw token 或给出诊断，但没有可执行的结构化语义。 |
| `L2` | 已进入 typed/loss-preserving IR 或 typed host carrier，并有结构/失败关闭测试；没有 GPU 执行承诺。 |
| `L3` | 至少一个**明确注册的 strict executor 子集**精确消费该语义，并有正向、负向及运行/GPU 证据；只对表中边界负责。 |
| `L4` | generic runtime 能直接按 authored graph/shader 合同执行该语义，不依赖 effect ID、shader path 或固定 profile 手写分派，并覆盖资源生命周期和回归矩阵。 |

每行只能有一个等级。**通用 primitive 取全部已知 authored 形态的共同最低状态**：某个 strict profile 能执行，只能在该 profile 自己的行和 [Effect 执行覆盖表](effect-execution-coverage.md) 记 `L3`，不能反向把通用 definition、pass、FBO、slot、combo、uniform 或 render state 抬到 `L3`。辅助设施存在也不能把官方语义抬级。

**本表的 `L4` 与其他覆盖表的 `L4` 不是同一坐标轴。** [覆盖台账](coverage-ledger.md)、[Effect 执行覆盖表](effect-execution-coverage.md)、[粒子组件覆盖表](particle-component-coverage.md)、[SceneScript API 覆盖表](scenescript-api-coverage.md) 的 `L4` 都指「已与 Windows Wallpaper Engine golden 或官方行为核验」，是**验证轴**；本表的 `L4` 指「generic runtime 不靠固定 profile 分派即可执行 authored 合同」，是**实现通用性轴**。因此本表某行达到 `L4` **不等于**该语义已取得像素 parity，反之靠 strict profile 取得 golden 也不会把本表抬到 `L4`。引用等级时必须带上出处表名。

已删除的派生解释格式曾记录 graph/shader 侧增量：v22 在既有 ShaderContract/binding program 和 exact stock Opacity 合同基础上保存 direct text content/point-size/color binding program，v23 增加 layer `puppetMeshPath`。此后 v24 增加 Puppet attachment bind frame（`attachmentName`/`boneIndex`/`modelBindFrameColumnMajor`）、v25 增加 `puppetAnimationLayers`（MDLA clip 的 `animationID`/`additive`/`blend*`/`rate`/`visible`/`visibilityBinding`）——**这两次都是 Puppet 通道的字段，没有改变 graph/shader 侧合同**。v26 增加 material pass `alphaWriting` 与 `userShaderValues`，v27 增加 layer `brightness`，v28 增加 text layer `anchor`，v29 增加 text overflow 字段；这些版本号现在只作字段落地历史。`a77b875` 起 production host 直接持有内存 `SceneRuntimeInput`，Debug runtime evidence schema 1 与播放输入解耦。execution catalog 仍在启动时从 authored graph 与 ShaderContract 规划；Opacity direct alpha、Local Contrast strength、受限动态文本与exact X-Ray visibility target可live消费per-surface snapshot。其他user-property effect visibility在缺少完整typed consumer时原子拒绝并由service重启Scene；Timeline/generic SceneScript effect visibility与SceneScript alpha、其他动态 effect 参数、Shadow 常量、通用 shader uniform仍未live。

当前基线 commit 与 schema 版本号以 [运行证据索引](runtime-evidence-index.md) 和 [覆盖台账](coverage-ledger.md) 为准；本表不复制，避免随代码演进失真。

## 2. 四条实现通道

> 当前 census 更正（2026-08-27）：现役 route registry 为 **38 个缺省 `generic-only` profile、4 个 `prefer-generic` 与 1 个 `observe-only`**。新增的`provider-backed-graph-input-spatial-weighted-color-blend`只对上方exact visible-provider contract持权；Pulse scalar owner cohorts复用既有typed-data RGB profile。两个宽 graph-input alpha profile、ordinary/宽provider-backed scalar route与唯一 Standard Blur unowned observe-only隔离桶的准入、rollback和证据边界不变。

| 通道 | 当前含义 | 当前事实 |
|---|---|---|
| IR | 读取并保真保存 definition/material/shader source contract，再编译资源身份、节点、slot 和 blocker | [SceneEffectDefinition.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneEffectDefinition.swift)、[SceneAuthoredEffectRenderPlan.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/AuthoredGraph/SceneAuthoredEffectRenderPlan.swift)、[SceneShaderContract.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderContract/SceneShaderContract.swift)、[SceneAuthoredEffectRenderPlanner.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/AuthoredGraph/SceneAuthoredEffectRenderPlanner.swift) |
| bounded shader preparation | 从同一 `SceneResourceView` 构建独立 source graph，以 declared project/scene version provenance、material combo、sampler readiness 和有界 fixed-point 形成 prepared source、active schema/source map/dependency/variant identity | package -> loose -> stock 是 MyWallpaperX 当前只读 VFS policy，不是已经证明的官方全版本默认；host-owned backend/version/platform/texture-format define 无 provider 时失败关闭。preparation 是中性 Program 前置能力，只有 slot/provider/state/color 与 frontend 全部闭合后才由 resolved-material owner 准入；preparation success 本身不授权 GPU 执行，见 [E-SHADER-PREPARATION](runtime-evidence-index.md#e-shader-preparation) |
| atomic material program | production raw graph编译固定8槽Template，再从一个immutable frame/resource/dynamic snapshot解析provider、active variant/reflection、uniform/state/color并生成semantic/exact Program identity | R3 one-shot audit 仍是 `gpuEncoded=0` 的历史地基；R4 已由 capability 直接准入 bounded Program，统一 executor 负责 GPU 编码、terminal publication、completion/rollback 与 next-frame evidence。只有 explicit absent 允许低优先级候选，其他未证 purpose/state/color 仍 fail closed，见 [E-MATERIAL-PROGRAM](runtime-evidence-index.md#e-material-program) 与 [E-R4-1-STRAIGHT-ALPHA](runtime-evidence-index.md#e-r4-1-straight-alpha) |
| generic runtime | 可复用的 frame context、texture registry、offscreen allocation、Metal encoder 等基础设施 | 它们提供时钟、pointer、尺寸、纹理和命令编码能力，但**不会自动解释 authored graph node 或 shader source** |
| generic authored frontend（profile-scoped route） | 固定工具链把 exact prepared-source key 编译为反射化 MSL Program artifact；共享 authored-source analyzer先拥有可证明的material color fact，compiler backend只翻译语言、stage link、reflection与MSL，consumer拒绝与已证明源码事实矛盾的artifact。linked reflection的共享UBO超集按实际stage member消费过滤，artifact field携带`vertex`/`fragment` stage，Program只要求对应stage作者uniform，共享host field仍跨stage。当前缺省`generic-only` profile只由prepared source、active sampler、typed graph/provider/output与stage-uniform事实组成：scalar interpolation、opaque scalar output、straight-alpha R8 signal、raw RG8 scalar-splat output、preserved-RGBA state transform、source-independent premultiplied output、graph-input independent-signal producer、graph-target independent-signal accumulator、RGBA8-UNorm graph-target independent-signal accumulator、graph-input independent-signal compositing、internal-framebuffer passthrough、normalized sample-sum、graph-target opaque loop sample-average、graph-target opaque static sample-average、graph-target opaque alpha-weighted loop average、graph-input conditional opaque alpha-weighted RGB、alpha-weighted sample average、preserved-alpha RGB filter、unit previous/blurred composite、graph-input alpha attenuation、color blend、spatial-weighted color blend、conditional straight union、stage-uniform passthrough、两种stage-uniform straight-alpha-preserving、audio/stage-uniform straight-alpha、single-sampler alpha mutation、same-slot channel reconstruction、auxiliary RGB blend alpha-preserving、typed-data RGB filter、same-alpha reconstructed-RGB data filter、conditional generated-RGB preserved-alpha、straight-RGB scalar-alpha与overlay-alpha blend。两个宽graph-input alpha profile及ordinary/provider-backed仍为`prefer-generic` | Motion Blur accumulation/combine、R8 two-node、真实`3765760121:187`双Foliage stage、三个exact Shake source/profile、`3767460992`的Shift Hue/Workshop Gradient/Enhanced Audio Bars、真实`3765760121:96` Fisheye-like与隔离派生`9000000622:112` Chromatic-like、真实`3767460992:20#effect#702` auxiliary RGB blend、真实资源派生Bokeh node1、nodes2–6、原始Cursor两个state Program、stock Radial与legacy Directional God Rays、exact static/KERNEL0 Standard Blur，以及真实`3747492842`三项Bloom terminal conditional generated-RGB、24个Gaussian X/Y loop node、9个down-sample node、9个up-sample node与3个light-map node均沿Program/GraphExecutor完成各自bounded GPU/publication/compositor/next-frame并命中非样本窄profile。down-sample的launch以作者顺序上游Program opaque fact准入，frame finalization再要求source slot实际graph publication为typed `.opaque`，拒绝premultiplied/data/missing/unresolved输入；light-map只接受typed `.opaque`/`.premultipliedAlpha`并由两套共享backend在作者边界解预乘后生成opaque output。已迁移profile的坏artifact不恢复旧专用owner；conditional generated-RGB坏artifact把整个16-node effect回到entry previous-current且不发布内部target；Gaussian、down-sample或up-sample artifact损坏时每图对应首断点同样撤销完整effect；light-map坏artifact与exact profile-local`disable-generic`都只切到shared bounded frontend。single-current-pair的self-owned graph-internal dependency在ownership conservation通过时可effect-local previous-current，external-primary仍拒绝。wrong-slot、active-slot不等、external-provider、隐藏sample、alpha write、control flow、单stage/no-active-uniform与非graph-input反例保持普通route或失败关闭。更广dialect、两个宽alpha profile、其他Bloom revision、MODE 1/mask、发行依赖与其余owner debt仍未闭合，见 [E-V1-BLOOM-LIGHTMAP-OWNER](runtime-evidence-index.md#e-v1-bloom-lightmap-owner)、[E-V1-BLOOM-DOWNSAMPLE-OWNER](runtime-evidence-index.md#e-v1-bloom-downsample-owner)、[E-V1-BLOOM-UPSAMPLE-OWNER](runtime-evidence-index.md#e-v1-bloom-upsample-owner)、[E-V1-BLOOM-GAUSSIAN-LOOP-OWNER](runtime-evidence-index.md#e-v1-bloom-gaussian-loop-owner)、 [E-V0-GENERIC-SHADER-PROGRAM-SLICE](runtime-evidence-index.md#e-v0-generic-shader-program-slice)、[E-V1-GENERIC-SCALAR-INTERPOLATION](runtime-evidence-index.md#e-v1-generic-scalar-interpolation)、[E-V1-NARROW-GRAPH-INPUT-ALPHA-OWNERS](runtime-evidence-index.md#e-v1-narrow-graph-input-alpha-owners)、[E-V1-AUXILIARY-RGB-BLEND-OWNER](runtime-evidence-index.md#e-v1-auxiliary-rgb-blend-owner)、[E-V1-STAGE-UNIFORM-GRAPH-INPUT](runtime-evidence-index.md#e-v1-stage-uniform-graph-input)、[E-V1-CONDITIONAL-GENERATED-RGB-OWNER](runtime-evidence-index.md#e-v1-conditional-generated-rgb-owner)、[E-EFFECT-SHAKE](runtime-evidence-index.md#e-effect-shake)、[E-EFFECT-WORKSHOP-GRADIENT](runtime-evidence-index.md#e-effect-workshop-gradient)、[E-EFFECT-WORKSHOP-ENHANCED-AUDIO-BARS](runtime-evidence-index.md#e-effect-workshop-enhanced-audio-bars)、[E-V1-PRESERVED-ALPHA-RGB-FILTER-OWNER](runtime-evidence-index.md#e-v1-preserved-alpha-rgb-filter-owner)、[E-V1-RED-GREEN-UNORM-SCALAR-SPLAT-OWNER](runtime-evidence-index.md#e-v1-red-green-unorm-scalar-splat-owner)、[E-V1-PRESERVED-RGBA-STATE-TRANSFORM-OWNER](runtime-evidence-index.md#e-v1-preserved-rgba-state-transform-owner)、[E-V1-STANDARD-BLUR-UNIT-COMPOSITE-OWNER](runtime-evidence-index.md#e-v1-standard-blur-unit-composite-owner)和[E-V1-STANDARD-BLUR-MASKED-OWNER](runtime-evidence-index.md#e-v1-standard-blur-masked-owner) |
| strict executor | 先匹配完整graph/material/profile，再调用独立手写Metal backend；任何未知shape/state/combo均拒绝 | 目前只保留仍有现役owner的typed leaf，例如4-pass stock Blur remainder；2-pass Blur Precise的strict planner/compiler/backend/pipeline/renderer、运行计划与direct-test oracle均已物理删除，产品只走shared MaterialProgram/GraphExecutor，退役门见[E-V1-PRECISE-DEDICATED-FAMILY-RETIREMENT](runtime-evidence-index.md#e-v1-precise-dedicated-family-retirement)。Workshop Shadow与stock Local Contrast strict leaf均已撤权并删除，后者的exact四节点图由共享MaterialProgram/GraphTargets/GraphExecutor以generic-only执行。统一`SceneResolvedMaterialGraphExecutor`继续负责作者顺序、stage transaction与末段发布；[per-stage typed backend](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/EffectExecution/SceneEffectStageRenderer.swift)只保留仍有现役owner的executor内部能力，不提供whole-chain或standalone产品入口 |
| bounded authored frontend | 把已准备的 bounded authored source 翻译为 typed Metal frontend，并与 texture/provider、uniform、state、color 原子形成 Program | 当前只接受已证静态控制流、uniform layout、framebuffer/texture schema 与 render-state 子集；global uniform/varying/attribute只有在排除function parameter和lexical local shadow后才进入ABI/Metal重写。Chromatic另证明same-slot channel reconstruction、direct floating-vector coordinate `.xy`及乘除链scalar `.x`子集；赋给`float2/float3`的表达式只有在parser可静态证明为component-wise identifier/swizzle/number/grouping与`+ - * /`组合时，才把更宽裸vector operand缩窄为`.xy/.xyz`。V1原子按vertex source是否实际引用`g_ModelViewProjectionMatrix`分别提供target-pixel或NDC/clip-space `a_Position`，并只对direct sampled color leaf与scalar `mix`组成的bounded graph登记有序slot集合。共享backend canonicalizer对双stage同名同型、count `1...128`的linked varying array只在静态`1...16`次、无控制流退出/nested-loop/loop变量额外写且每个循环索引目标满足`iterations <= count`时展开为literal index；原count大于16时还要求所有use可化为两stage相同literal prefix才同步收敛，小数组不压缩声明shape，原prepared source不改写。dynamic bound/index、越界引用和shape mismatch保持失败关闭。ordinary compiler artifact另允许float induction从0开始、canonical `++`、当前函数根作用域唯一immutable finite-integral `1...64`上界且至少一个dynamic `&&`早退条件的循环；index与上界不得传给authored helper，含loop helper只准由`main`唯一直接且非loop-nested调用。第二条件引用index、uniform/异函数decoy/mutable/nonintegral bound、`||`、body改写index、重复/间接helper call、lexical nested loop与vertex+fragment累计work超过256均失败关闭。user function、一般array/index、未知类型、显式过宽swizzle、widening、vector mix weight、color mutation和不同resolved color representation保持原语义或失败关闭。完整Program由resolved pass encoder和统一graph executor执行；未知语法、动态/递归控制流、未证sampler/purpose/state/color及其他FBO/multi-pass topology失败关闭 |

`SceneMetalPipeline.swift` 中仍存的Water/Cursor等手写MSL是legacy/profile approximation；Shake与Foliage专用MSL/pipeline/renderer均已随各自owner迁移删除。其余手写实现只证明宿主能上传纹理、time、pointer和矩阵，不证明WE shader source、annotation、macro或built-in名称已经兼容。R4已撤销独立authored-shader planner/backend/pipeline/renderer与exact Scroll profile的产品执行权；现役authored source只可经[中性 preparation](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderPreparation/SceneAuthoredShaderPreparation.swift)、atomic Program与[resolved-material pass encoder](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/EffectExecution/SceneResolvedMaterialPassEncoder.swift)进入统一graph executor。`3141421197`与三组exact Scroll的旧GPU门只保留为历史oracle，不再授权旧owner可达，也不能授权放宽其他shader或表述为任意真实样本已解锁。

## 3. Render Graph 覆盖

历史提交`136d35c`新增的Local Contrast strict profile只接受`effects/localcontrast/effect.json`的单effect stage：4个material pass、2个非unique的quarter-size `rgba8888` FBO、`KERNEL=0`、`GREYSCALE=0`、`MASK=0`、Gaussian `scale=(1,1)`（显式值或该stock profile的缺省值），以及`0...5`、缺省为`1`的`strength`。该旧准入同时核对exact shader identity/canonical/stage/raw/source hash并执行仓内手写MSL；其planner/backend/renderer现已删除，不再是产品route。现役exact stock图由identity-free source facts、普通MaterialProgram、GraphTargets与GraphExecutor执行，`strength`仍由同一property binding program和per-surface snapshot作为typed effect constant供给；见[E-V1-LOCAL-CONTRAST-SHARED-OWNER](runtime-evidence-index.md#e-v1-local-contrast-shared-owner)。

现役shared profile仍不接受mask、greyscale、非默认kernel/Gaussian scale、`HLSL_SM30`偏移或其他Local Contrast shape；包含unsupported sibling的mixed chain也没有由本批升级。当前没有合法Windows/官方像素golden，也没有预登记Local Contrast独立ROI；旧手写Metal数值与历史画面变化率只能作provenance，不能替代现役generic Program的source/artifact、GPU/publication/compositor/next-frame与官方对照门。

`809b75e` 的 exact Workshop Shadow planner/pipeline/renderer是历史strict owner：它曾以definition/material/ShaderContract fingerprint准入单pass并执行仓内手写MSL。当前该owner与耦合测试均已删除；现役产品只在公共源码分析器证明conditional straight union、typed graph-input slot与完整Program后，经generic artifact和统一GraphExecutor执行。旧fingerprint与手写MSL只保留为历史provenance，不能重新授权产品route，也不能证明WE像素等价。

`b541867` 的 scheduler 验证全局 effect/node/target identity、layer source -> effect output 连续性和最终输出，并先为整链建立 plan/table、预算与 LRU 候选，成功后一次提交。GPU 在同一 offscreen command buffer 依序执行 strict backend。普通 effect 继续按最长边 2048 分档；受限 authored shader 按 drawable 上的图层投影像素执行，最长边上限 4096，pool 驻留预算 128 MiB。`69cc7f01` 让 preflight 与 allocation 共用同一 extent resolver：先用显式 `width`/`height` 覆盖对应轴，再执行不放大的 `fit`、`scale` divisor 与 floor/min-1；单轴、组合形态及 `scale < 1` 放大都按实际 2048² 参考面积计费，非法值直接拒绝。`c73aa0b0`最初只开放零clear；现役logical descriptor已接受string/array形式的四个finite `0...1` authored clear，并在table首次创建/重建后与history seed一并执行一次初始化，后续帧消费previous-current；越界、非有限、缺lane或畸形clear仍拒绝。该 128 MiB resident transaction 门不是瞬时驱动 VRAM 硬上限。除已登记的无target layer-local bounded compose外，其他compose、condition/function、跨帧 logical swap 与其他 history consumer 仍不执行，也不让下表 generic primitive 整体升级到 `L3`。

| 语义项 | 等级 | IR | generic runtime | strict executor | 代码/测试证据 | 执行边界 | 下一门 |
|---|---|---|---|---|---|---|---|
| `EffectDefinition` | `L2` | JSON5 loader 保留 metadata、dependencies、FBO、ordered pass、functions/gizmos、extra fields 和 unknown paths | 没有按任意 definition 驱动 GPU 的执行循环 | 现役 bounded strict profiles 从同一 authored plan 进入执行，未知字段/blocker 一律拒绝 | [定义 loader](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneEffectDefinition.swift)、[定义测试](../../../script/tests/test_scene_effect_definition.py)、[E-EFFECT-IR](runtime-evidence-index.md#e-effect-ir)、[E-EFFECT-WATER-MOTION](runtime-evidence-index.md#e-effect-water-motion) | 现役完整 strict profile 不代表通用 definition 已执行；该 primitive 仍停在 IR | 建立 graph executor capability registry；每种 node/resource semantic 显式声明并逐项放行 |
| ordered pass / material ordinal | `L2 bounded condition-aware graph` | definition pass顺序、definition index、material ordinal与instance pass index分离 | `SceneGraphAdmissionCompiler`先在target allocation/Program preparation前按typed combo provider剪掉false target/pass/binding/node；`SceneGraphExecutionState`再按nodeIndex把material/copy/swap降低为带logical mapping before/after的事务，统一GraphExecutor编码并在GPU completion后提交publication。`PreparedStage`冻结ordered commands、transition、publications与effect output；bounded layer-local compose继续在ordinary Program间轮转pair，eligible普通FBO command graph的pre-encode visual failure丢弃整个material/command prefix并恢复effect-entry snapshot | 既有condition、multi-copy、copy→swap、swap→copy与dependency门保持；新增项目自有同图双分支把同一static integer condition同时放在FBO、producer pass与consumer binding，false分支执行5 authored / 3 material / 1 copy / 1 swap与2 target，true分支执行6 / 4 / 1 / 1与3 target，frame 0和next-frame均经GPU/publication/terminal compositor闭合。隔离真实Fluid默认false分支另执行19 authored / 17 material / 2 swap与8 target；其nonzero Lighting true分支仍被独立module/resource能力拒绝 | [condition admission](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/AuthoredGraph/SceneGraphConditionAdmission.swift)、[execution state](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/GraphTargets/SceneGraphExecutionState.swift)、[resolved executor](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/EffectExecution/SceneResolvedMaterialGraphExecutor.swift)、[conditional FBO command gate](../../../script/tests/test_scene_graph_conditional_fbo_command_contract.py)、[E-V1-CONDITIONAL-FBO-COMMAND-CONTRACT](runtime-evidence-index.md#e-v1-conditional-fbo-command-contract)、[E-V1-ORDERED-MULTI-COPY-COMMAND-CHAIN](runtime-evidence-index.md#e-v1-ordered-multi-copy-command-chain)、[E-V1-MIXED-COPY-SWAP-COMMAND-CHAIN](runtime-evidence-index.md#e-v1-mixed-copy-swap-command-chain)、[E-V1-MIXED-SWAP-COPY-COMMAND-CHAIN](runtime-evidence-index.md#e-v1-mixed-swap-copy-command-chain)、[E-V1-ORDINARY-FBO-COMMAND-VISUAL-FAILURE-PASSTHROUGH](runtime-evidence-index.md#e-v1-ordinary-fbo-command-visual-failure-passthrough)、[E-V1-NESTED-DEPENDENCY-CASCADE-FAILURE-CONTAINMENT](runtime-evidence-index.md#e-v1-nested-dependency-cascade-failure-containment) | 只证明launch-static、exact integer provider下一个同条件FBO/producer/binding原子、layer-local compose、登记的copy/swap顺序与bounded dependency失败半径；其他condition语法/provider、逐帧切换、function、condition+history/dependency、encoded publication failure、secondary/multi-dependency/reference及resource/target/runtime hard failure仍未执行 | 继续以其他真实依赖子图中段失败与persistent history topology/lifecycle验证顺序和回滚 |
| `Material` | `L3 bounded program execution` | 固定8槽、candidate provenance、combo/uniform/state进入Template/Program；presence combo与default仍分离。公共frontend能证明bounded straight-color/scalar-alpha/whole-color filter；MaterialProgram从per-surface snapshot消费exact direct user-property scalar/vector3 uniform，但只在sole contributor、exact wrapper/fallback/domain、launch producer key/target/type与expected stage ABI均成立时获准。fragment-only项（含historical `phase`）要求唯一active fragment consumer；`speed/amount`与stock `phase`要求exact `{vertex,fragment}` set且每stage唯一non-array `float`。stock `phase`按stage分别核对vertex `0...1`与fragment `0...6.282`，historical `phase`只核对fragment `0...6.282`。snapshot另保留authored lane，finalizer逐consumer stage执行range/fallback | registry精确区分graph/asset/property/system state；统一executor完成GPU/publication/compositor/next-frame。`3768724269:163` Rounded Mask、`3290491250:116` Sharpen、`3768229922`七个Pulse layer及`1937925563:13#effect#321`均有各自首帧、exact publication、terminal compositor与next-frame证据；既有真实vector3及受控fragment/same-domain scalar门继续成立，受控`9000000968`证明stock stage-indexed `phase`，基于真实`2241938645`的受控`9000000969`另证明historical fragment-only `phase`默认/恢复进入Program、profile-local rollback只保留目标previous-current且后缀继续 | 未认领effect继续失败关闭；typed VFS graph与package/loose/stock provenance保留；普通`effectOutput`后续stage、dynamic provider与internal FBO仍由frame精确门约束 | [straight RGB/alpha factor analyzer](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderFrontend/SceneAuthoredShaderStraightRGBAlphaFactorAnalyzer.swift)、[scalar-alpha analyzer](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderFrontend/SceneAuthoredShaderStraightRGBScalarAlphaAnalyzer.swift)、[whole-color filter analyzer](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderFrontend/SceneAuthoredShaderStraightWholeColorFilterAnalyzer.swift)、[layerSource launch color gate](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/MaterialProgram/SceneResolvedMaterialTextureResolver+LaunchColor.swift)、[Program color derivation](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/MaterialProgram/SceneResolvedMaterialProgram+ColorDerivation.swift)、[E-AUTHORED-STRAIGHT-RGB-ALPHA-FACTOR](runtime-evidence-index.md#e-authored-straight-rgb-alpha-factor)、[E-AUTHORED-STRAIGHT-RGB-SCALAR-ALPHA](runtime-evidence-index.md#e-authored-straight-rgb-scalar-alpha)、[E-AUTHORED-STRAIGHT-WHOLE-COLOR-UNORM](runtime-evidence-index.md#e-authored-straight-whole-color-unorm)、[E-V1-PULSE-HISTORICAL-FRAGMENT-PHASE-USER-PROPERTY-OWNER](runtime-evidence-index.md#e-v1-pulse-historical-fragment-phase-user-property-owner)、[E-V1-PULSE-STAGE-INDEXED-PHASE-USER-PROPERTY-OWNER](runtime-evidence-index.md#e-v1-pulse-stage-indexed-phase-user-property-owner)、[E-V1-PULSE-CROSS-STAGE-SCALAR-USER-PROPERTY-OWNER](runtime-evidence-index.md#e-v1-pulse-cross-stage-scalar-user-property-owner)、[E-V1-PULSE-FRAGMENT-SCALAR-USER-PROPERTY-OWNER](runtime-evidence-index.md#e-v1-pulse-fragment-scalar-user-property-owner)、[E-V1-PULSE-TYPED-USER-PROPERTY-OWNER](runtime-evidence-index.md#e-v1-pulse-typed-user-property-owner) | producer-present fragment scalar、same-domain `speed/amount`、stage-indexed stock `phase`与historical fragment-only `phase`均为bounded owner-migration-complete；缺失producer保留作者值，producer不等且旧plan可接纳时不撤权，无效kind/shape则局部previous-current。关系域`bounds`、其他float2、alpha或audio+binding、SceneScript/Timeline、多contributor、wrapper/ABI/domain漂移、普通dynamic provider、internal FBO颜色传播、alpha replacement/additive/conditional、多个color slot及其他未证helper/matrix仍关闭 | bounded真实正证不升级arbitrary material或整样本；受控scalar fixture不是authored scalar corpus、官方ROI/fidelity、Windows golden或视觉等价 |
| FBO declaration | `L2` | effect-scoped identity、独立 `width`/`height`/`fit`/`scale`、format、unique、clear、UV、condition进入`RenderTarget`；当前typed storage登记RGBA、R8、RG88、R16F与RG1616F | target plan/table解析extent、lifetime、budget与allocation；exact authored `uvs` absent/repeat分别进入`.clampToEdge/.repeatWrap` descriptor；RG88/RG1616F分别锁定`.rg8Unorm/.rg16Float`、R/G write mask与typed `.redGreen* / .preservedChannels` publication，consumer可按launch-envelope守恒使用direct R/G component subset或独立`wholeVector`事实，Metal对未存B/A提供确定性`0/1`；R8/R16F分别锁定`.r8Unorm/.r16Float`、R write mask与typed `.scalarRed*` publication，consumer只接受`redOnly / wholeVector`，`.g/.rg`与unproven关闭。四种preserved-channel storage在合法`initialClear`时允许first-read-before-write，initialize只发生于首次创建/重建，后续帧消费previous-current；exact R16F ordered pair另只接受same effect/descriptor、unique+clear、无command、偶数且严格交替writer、首读history、末写history与逐read prior-write/channel证明；ordinary unique同帧形态只接受single-writer/no-command/no-clear且writer先于typed later reader，并保持frame-local而不建立history。RGBA independent-signal pair则只接受same descriptor、unique+透明clear、一个preserving update、一个terminal composite和terminal swap，并从initialize起保存`.independentAlphaSignal` | strict Blur继续消费既有FBO；stock Local Contrast的两张quarter RGBA target现由普通Program/GraphTargets/GraphExecutor消费。项目GPU门与真实`3765760121:73`证明R8 absent/repeat；RG88/RG1616F门依次闭合component subset、whole-vector、clear/history与exact feedback swap。未见R16F ordered门闭合frame 0/next-frame GPU、typed frame/persistent publication与scratch非持久；四format frame-local门又证明首帧/next-frame只有material intents且无persistent resource。新的未见RGBA pair在同一executor/lease跨frame累积，真实Fluid完整执行17 material + 2 swap、0 rejected并进入GPU/publication/compositor/next-frame；十种RGBA extent与既有lifecycle证据继续有效 | [extent/address resolver](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/GraphTargets/SceneGraphRenderTargetPlan+Extent.swift)、[target plan](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/GraphTargets/SceneGraphRenderTargetPlan.swift)、[typed attachment storage](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/EffectExecution/SceneResolvedMaterialAttachmentStorage.swift)、[independent-signal feedback tests](../../../script/tests/test_scene_independent_signal_feedback.py)、[E-V1-INDEPENDENT-SIGNAL-FEEDBACK](runtime-evidence-index.md#e-v1-independent-signal-feedback)、[E-V1-PRESERVED-CHANNEL-FRAME-LOCAL-UNIQUE](runtime-evidence-index.md#e-v1-preserved-channel-frame-local-unique)、[E-V1-PRESERVED-CHANNEL-ORDERED-FEEDBACK](runtime-evidence-index.md#e-v1-preserved-channel-ordered-feedback)、[E-V1-PRESERVED-CHANNEL-COMPONENT-WHOLE-VECTOR](runtime-evidence-index.md#e-v1-preserved-channel-component-whole-vector)、[E-V1-FBO-UV-ADDRESS-REPEAT](runtime-evidence-index.md#e-v1-fbo-uv-address-repeat)、[E-V1-AUTHORED-FBO-INPUT-EXTENT-LIFECYCLE](runtime-evidence-index.md#e-v1-authored-fbo-input-extent-lifecycle) | bounded formats、extent与各exact lifecycle仍不开放未知UV、arbitrary clear/history/copy/swap/multiwriter、无consumer、其他format、arbitrary extent或通用load/store/reset；independent-signal pair只授权一个严格拓扑，nonzero clear、copy、额外reader、wrong composite和冲突identity关闭 | 9 RT / 8 history其余resize/seek/device-loss/多surface/预算、其他command/dependency/compose/target、SceneScript bridge、emitters与更宽局部失败边界仍未闭合；真实Fluid执行不等于隔离ROI、官方parity、owner migration或V1完成 |
| `target` | `L3 bounded effect-scoped + primary named + scene-background identity` | 区分layer source、effect output、effect-scoped framebuffer、primary named layer target、same-frame scene background和unresolved；同名FBO不跨effect共享，named namespace不与effect-scoped table或scene background alias | effect table按完整identity映射且不alias，每个surface由独立pool隔离；V1首个公共原子使两个effect-scoped target进入persistent allocation、copy/history publication与下一帧读取。classic primary named profile在独立namespace按frame预留 `_rt_imageLayerComposite_<provider>_a`并默认`generic-only`；同一provider的ready publication可由两个consumer或同一consumer的多个 ordinary effect/pass reference共享，两个独立provider也可各自建立reservation。exact external-primary `imageLayerBlend`还要求single backward image provider、pass 0 / slot 1、blend 0与唯一reference，并且当前只由ordinary MaterialProgram / GraphExecutor持有产品输出；`.blend` dedicated不再是该profile的fallback或ownership match。slot-3 hidden static solid另由结构型`.solidLayer` binding准入一个visible effect/pass 0、slot 0...2 hole、slot 3唯一primary `_a`且无user override的consumer，并要求更早provider为hidden、无utility/effect/child的static solid；它复用同一reservation/capture/publication与epoch/object复核，不读取effect/sample/layer/path/hash、combo或常量选择算法。bounded nested/effectful closure把hidden provider graph final blit到独立named reservation，避免graph-pool复用形成consumer读写alias；其pre-encode ordinary capture miss会逐层撤销未claim、无history的真实依赖ledger，不污染独立successor。exact `_rt_FullFrameBuffer`由renderer只为accepted strict claim创建epoch-qualified `.sceneBackground(consumerLayerID:)`；cycle identity在binding前硬拒绝 | Motion Blur atom消费`current/history`；classic/Opacity1、multiple-consumer、multi-reference与two-provider fixture闭合primary publication。final Image Blend owner-migration fixture `91 -> 92 -> 104 -> 1104`使3个exact external-primary Blend都成为ordinary Program，正门`dedicated/fallback=0`、capture `[91,92,104]`、binding `[92,104,1104]`；raw-provider capture miss只局部拒绝该链，独立`1200`同帧composite且下一帧全链恢复。`disable-generic`让终端`1104`typed unsupported且仍不恢复dedicated；独立`1200`保持Program/compositor。scene-background representative读取同一main target；cycle fixture记录`cyclicDependency`且安全帧保留 | [target identity](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/AuthoredGraph/SceneAuthoredEffectRenderPlan.swift)、[target plan](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/GraphTargets/SceneGraphRenderTargetPlan.swift)、[provider publication](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/SceneTextureProviderPublication.swift)、[dependency runtime](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/LayerDependencies/SceneDependencyFrameRuntime.swift)、[copy/history GPU gate](../../../script/tests/test_scene_material_copy_history_rendering.py)、[E-V1-MATERIAL-COPY-HISTORY](runtime-evidence-index.md#e-v1-material-copy-history)、[E-V1-CROSS-LAYER-NAMED-PROVIDER](runtime-evidence-index.md#e-v1-cross-layer-named-provider)、[E-V1-PRIMARY-NAMED-PROVIDER-MULTIPLE-CONSUMERS](runtime-evidence-index.md#e-v1-primary-named-provider-multiple-consumers)、[E-V1-SINGLE-CONSUMER-MULTI-REFERENCE](runtime-evidence-index.md#e-v1-single-consumer-multi-reference)、[E-V1-TWO-PRIMARY-PROVIDERS](runtime-evidence-index.md#e-v1-two-primary-providers)、[E-V1-NESTED-EFFECTFUL-NAMED-PROVIDER](runtime-evidence-index.md#e-v1-nested-effectful-named-provider)、[E-V1-NESTED-DEPENDENCY-CASCADE-FAILURE-CONTAINMENT](runtime-evidence-index.md#e-v1-nested-dependency-cascade-failure-containment)、[E-V1-EXTERNAL-PRIMARY-IMAGE-BLEND-OWNER-MIGRATION](runtime-evidence-index.md#e-v1-external-primary-image-blend-owner-migration)、[E-V1-PROCEDURAL-NOISE-SHARED-OWNER](runtime-evidence-index.md#e-v1-procedural-noise-shared-owner)、[E-V1-SCENE-BACKGROUND-COMPOSE](runtime-evidence-index.md#e-v1-scene-background-compose) | 只证明登记的effect-scoped topology、primary named profile及其bounded shared/nested shapes、exact `imageLayerBlend` owner migration、pre-encode ordinary capture failure containment与strict same-frame scene-background carrier；standalone Blend专用产品owner已由独立后继cohort退役，更宽Blend source/provider/dynamic形态仍是共享Program/frontend能力债；旧`worleyColorV1`已退役，slot-3 solid只保留上述bounded结构，cycle明确硬拒绝。secondary、forward/cycle execution、child/non-image、multi-dependency/reference、arbitrary depth、history/resize/device-loss、encode-after publication failure、Refraction normal/mask与多writer仍未授权 | 新topology继续复用现役identity/table/publication owner并逐项闭合generation与teardown，不新建旁路资源表 |
| `previous` / framebuffer material alias | `L3 bounded program` | 每个 effect 开始时固定为 chain input；同一 effect 内不会随 pass target 前进。现代`framebuffer`/`previous`及historical exact `ui_editor_properties_framebuffer`都可表达当前effect input，但历史token仅在regular hidden `g_Texture0` slot 0成立；`_rt_FullFrameBuffer`是独立scene-background provider，不是previous alias | ordered strict stage programs已把前一effect output作为下一effect input；公共Program路径进一步让`1553008362`的Water Waves消费layerSource、三段Shake逐段消费前一effectOutput，并保持typed publication identity | 两个Blur与Local Contrast验证authored `previous`；Workshop Shadow现由conditional straight-union共享profile锁定stage推进；historical alias synthetic正反门及155真实四段链证明相似editor key、label-only、缺hidden/错槽不会误准入 | [sampler schema](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/MaterialProgram/SceneResolvedMaterialShaderSchema+SamplerPurpose.swift)、[capability tests](../../../script/tests/test_scene_resolved_material_execution_capability.py)、[E-EFFECT-CHAIN](runtime-evidence-index.md#e-effect-chain)、[E-HISTORICAL-FRAMEBUFFER-MATERIAL-ALIAS](runtime-evidence-index.md#e-historical-framebuffer-material-alias) | historical alias只闭合当前effect input identity；scene background只闭合独立strict carrier。original、其他full-frame alias、history、其他editor key与`1636394814`后继mixed-chain blocker仍未接 | 为剩余source kind建立显式identity/provider，不以label、路径或当前framebuffer猜语义 |
| `bind` | `L3 bounded atomic program` | sparse index、authored name、conditions和exact texture identity均保留；Program冻结publication generation/purpose/content/physical/mapped/UV/sampler与active variant | graph/asset/user/system source已有typed demand/readiness；bounded R8 repeat publication进入direct-red consumer；exact `_rt_FullFrameBuffer`在strict shape中映射typed same-frame scene-background demand | 既有Program binding正门继续成立；项目GPU门与真实两node graph闭合bounded internal FBO consumer，scene-background representative另闭合slot 1、frame epoch/object identity与premultiplied color consumer | [Program texture resolver](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/MaterialProgram/SceneResolvedMaterialTextureResolver.swift)、[frame registry](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/SceneFrameTextureRegistry.swift)、[provider publication](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/SceneTextureProviderPublication.swift)、[scene-background gate](../../../script/tests/test_scene_background_material_provider.py)、[E-V1-FBO-UV-ADDRESS-REPEAT](runtime-evidence-index.md#e-v1-fbo-uv-address-repeat)、[E-V1-SCENE-BACKGROUND-COMPOSE](runtime-evidence-index.md#e-v1-scene-background-compose) | 只授权登记的R8 red-only topology与strict scene-background carrier；其他FBO/command publication、Refraction normal/mask、regular asset purpose与dynamic lifecycle仍缺 | 新consumer继续复用同一binding owner，不在renderer重选候选或猜purpose |
| `compose` | `L3 bounded layer-local + scene-background carrier` | raw value保留在node；非boolean继续block。layer-local门接受单effect、至少两ordinary material、0 authored FBO/command/history/background/dependency/condition、所有binding读effect input、所有node写effect output、非末node全部`compose:true`且末node nil/false。scene-background门更窄：exactly two node、首node以slot 1读取`_rt_FullFrameBuffer`并compose、次node不compose、一次transition/two writes/same pair member、dependency owner`.none` | GraphExecutor以stage-local pair推进ordinary Program；同slot normalized sample-sum由公共source proof选择generic artifact或显式`disable-generic`的shared bounded frontend，不再选择Precise专用算法，并已覆盖raw compose与显式`effect input -> authored RGBA FBO -> effect output`两类Precise revision。scene-background claim关闭main pass并通过`withReadableTarget`执行offscreen Program，随后恢复main pass；accepted claim才使CAMetalLayer可读 | layer-local representative闭合pair与首/次Program preparation rollback；真实`2802243144`三条raw Precise compose及`3765760121:68/76/82`三条显式FBO Precise在默认与shared rollback两路均闭合GPU/publication/compositor/next-frame、0 dedicated/fallback；scene-background representative相对control改变87.896825%像素，三node反例零claim/encode/GPU且像素回到control | [executor](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/EffectExecution/SceneResolvedMaterialGraphExecutor.swift)、[normalized sum proof](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderFrontend/SceneAuthoredShaderNormalizedSampleSumAnalyzer.swift)、[scene-background gate](../../../script/tests/test_scene_background_material_provider.py)、[E-V1-GENERIC-LAYER-LOCAL-COMPOSE](runtime-evidence-index.md#e-v1-generic-layer-local-compose)、[E-V1-PRECISE-AUTHORED-INTERMEDIATE-OWNER](runtime-evidence-index.md#e-v1-precise-authored-intermediate-owner)、[E-V1-SCENE-BACKGROUND-COMPOSE](runtime-evidence-index.md#e-v1-scene-background-compose) | single-node、畸形compose、其他FBO/command/history、其他background/dependency、Refraction normal/mask继续失败关闭；Precise真实MASK/BLURALPHA/dynamic scale与官方对照未完成 | 下一独立compose/FBO atom继续闭合其他真实background/dependency或command topology；不得从carrier外推stock effect或视觉算法 |
| `copy` | `L3 bounded command` | 独立 command node，保留 source/target，校验 storage compatibility；不占 material ordinal | `SceneGraphExecutionState`按authored nodeIndex生成copy intent并携带logical mapping，GraphExecutor在同一command buffer中编码Metal copy；已闭合单copy unique-history、同 effect 双copy、descriptor-identical copy→swap、seeded swap→copy及conditional FBO suffix，command均保留authored order、target lifetime、storage identity并在GPU completion后提交publication | production GPU fixture以4x3不同像素、rate `0.25/0.75`和三帧recurrence证明source capture、copy、pending-history defer与后续material消费；派生真实 Motion Blur 双copy atom形成2 material + 2 copy并取得416/416 terminal graph success，copy→swap形成2 material + 1 copy + 1 swap并取得415/415，seeded swap→copy形成2 material + 1 swap + 1 copy并取得430/430；conditional FBO双分支继续把同一copy稳定保留到frame 0/next-frame | [command planning](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/AuthoredGraph/SceneAuthoredEffectRenderPlanner.swift)、[execution state](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/GraphTargets/SceneGraphExecutionState.swift)、[copy/history GPU gate](../../../script/tests/test_scene_material_copy_history_rendering.py)、[conditional FBO command gate](../../../script/tests/test_scene_graph_conditional_fbo_command_contract.py)、[E-V1-CONDITIONAL-FBO-COMMAND-CONTRACT](runtime-evidence-index.md#e-v1-conditional-fbo-command-contract)、[E-V1-MATERIAL-COPY-HISTORY](runtime-evidence-index.md#e-v1-material-copy-history)、[E-V1-ORDERED-MULTI-COPY-COMMAND-CHAIN](runtime-evidence-index.md#e-v1-ordered-multi-copy-command-chain)、[E-V1-MIXED-COPY-SWAP-COMMAND-CHAIN](runtime-evidence-index.md#e-v1-mixed-copy-swap-command-chain)、[E-V1-MIXED-SWAP-COPY-COMMAND-CHAIN](runtime-evidence-index.md#e-v1-mixed-swap-copy-command-chain)、[E-V1-PERSISTENT-HISTORY-DIFFERENT-TOPOLOGY-SWITCH](runtime-evidence-index.md#e-v1-persistent-history-different-topology-switch) | 只证明同extent/format、单effect、登记的单/双copy、两个descriptor-identical mixed chain、一个static conditional prefix与一个bounded unique-history topology switch；非法target在admission拒绝。其他different-topology/format/provider switch、seek/device loss/multi-surface、跨family、多copy其他组合、swap/compose/function/dynamic condition与官方时序仍未证 | 逐项扩展生命周期事件与新 mixed command combination fixture；任何storage/identity/hazard不确定继续hard reject |
| `swap` | `L3 bounded command` | 独立 command node；ordinary command要求两个declared FBO的normalized extent expression、format、address、unique与clear完全相同；preserved-channel另有exact same-descriptor、unique+clear、typed reader/writer且terminal-only的feedback pair；static condition只能在admission后留下资源完整的command suffix | `SceneGraphExecutionState`在同一事务中交换logical identity mapping，后段material读取交换后的texture；feedback pair复用既有GraphTargets persistent mapping/history、rehydration、reset、completion和rollback，未新增swap state owner；不兼容或剪枝后悬空时在claim前typed拒绝 | ordinary三material正例相对no-swap control使全帧从红变蓝，extent/format/unique/clear反例关闭；seeded Motion Blur证明swap→copy顺序；conditional FBO双分支证明prefix target/producer/binding被剪枝后同一swap及terminal read仍执行；未见RG88 feedback pair与真实Fluid默认false分支继续闭合GPU/publication/compositor/next-frame | [descriptor compatibility](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/GraphTargets/SceneGraphRenderTargetPlan+Extent.swift)、[preserved pair admission](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/EffectExecution/SceneResolvedMaterialExecutionCapability+PreservedChannels.swift)、[execution state](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/GraphTargets/SceneGraphExecutionState.swift)、[conditional FBO command gate](../../../script/tests/test_scene_graph_conditional_fbo_command_contract.py)、[E-V1-CONDITIONAL-FBO-COMMAND-CONTRACT](runtime-evidence-index.md#e-v1-conditional-fbo-command-contract)、[E-V1-SAME-DESCRIPTOR-SWAP](runtime-evidence-index.md#e-v1-same-descriptor-swap)、[E-V1-MIXED-SWAP-COPY-COMMAND-CHAIN](runtime-evidence-index.md#e-v1-mixed-swap-copy-command-chain)、[E-V1-PRESERVED-CHANNEL-FEEDBACK-SWAP](runtime-evidence-index.md#e-v1-preserved-channel-feedback-swap)、[E-V1-INDEPENDENT-SIGNAL-FEEDBACK](runtime-evidence-index.md#e-v1-independent-signal-feedback) | 只证明ordinary同帧bounded permutation/order、一个static conditional prefix、一个exact preserved-channel跨帧pair与一个exact independent-signal terminal swap topology；arbitrary mapping persistence、其他descriptor/format/condition/command组合仍未授权 | 后续command组合独立闭合；任何descriptor/identity/hazard不确定继续hard reject |
| `condition` | `L3 bounded static graph admission` | FBO/pass/bind各位置保留raw `SceneJSONValue`；显式material/instance integer combo provider与stable shader default-zero evidence可在allocation/Program preparation前共同剪枝false target/pass/bind，保留原nodeIndex/definition index/material ordinal空洞 | `SceneGraphConditionAdmission`/`SceneGraphAdmissionCompiler`只消费typed provider并先形成一个immutable admitted graph；悬空pruned resource以`prunedResourceReferenced`、冲突provider以`conflictingComboProviders`硬拒绝。admitted graph随后只进入既有target plan、`SceneGraphExecutionState`与GraphExecutor，不执行逐帧表达式或建立第二套condition owner | 项目自有6-node/3-FBO图在同一condition的false分支执行5 authored / 3 material / 1 copy / 1 swap与2 target，true分支执行6 / 4 / 1 / 1与3 target；两路frame 0/next-frame均GPU/publication/terminal compositor成功，预登记ROI分别精确红/蓝。隔离真实Fluid默认false分支另执行19 authored / 17 material / 2 swap与8 target；true分支被独立Lighting/resource能力fail closed | [definition IR](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneEffectDefinition.swift)、[condition admission](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/AuthoredGraph/SceneGraphConditionAdmission.swift)、[graph compiler](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/AuthoredGraph/SceneGraphAdmissionCompiler.swift)、[conditional FBO command gate](../../../script/tests/test_scene_graph_conditional_fbo_command_contract.py)、[E-V1-CONDITIONAL-FBO-COMMAND-CONTRACT](runtime-evidence-index.md#e-v1-conditional-fbo-command-contract)、[E-V1-SHADER-DEFAULT-ZERO-CONDITION](runtime-evidence-index.md#e-v1-shader-default-zero-condition)、[E-V1-EXPLICIT-CONDITION-PRUNING](runtime-evidence-index.md#e-v1-explicit-condition-pruning) | 只支持已登记launch-static integer/default-zero provider和资源完整的target/pass/binding组合；任意syntax/provider、dynamic switch、condition+function/history/dependency、SceneScript expression及官方逐帧variable semantics仍关闭 | 下一condition扩面必须由新的公共typed provider/组合、资源剪枝、局部失败和真实consumer门共同驱动，不按effect名增加分支 |
| `function` | `L2 bounded typed request` | definition 顶层 functions 原样保留；registry-defined `action=clear` 可由 frame-scoped typed request 驱动同一 GraphExecutor target generation | bounded QuickJS-NG `SceneScriptScalarProgram`已能把owner-local mutation投影为typed request，经frame context/preflight进入GraphExecutor；unknown/stale/lease/encode-abandon局部失败，未提供通用`IEffect` handle/API | 21 focused tests锁有序clear intent、generation、stale/unknown与rollback；派生真实App fixture `9000000800...0803`分别闭合positive、unknown/no-call/invalid-effect局部fallback，无关suffix继续GPU/publication/compositor/next-frame | [definition loader](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneEffectDefinition.swift)、[function request](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/AuthoredGraph/SceneGraphAdmissionCompiler.swift)、[E-V1-MATERIAL-FUNCTION-INVOCATION](runtime-evidence-index.md#e-v1-material-function-invocation) | 不执行任意 function body、通用SceneScript effect handle/API、float/utility target或其他资源副作用；仍不是 Fluid/V1 完成 | 在V2扩展真实VM的通用`getEffect(...).executeMaterialFunction(...)` host API，再跑Fluid与生命周期正反门 |
| extent (`width`/`height`/`fit`/`scale`) | `L3 bounded scale + fit + fit-then-scale + dual/single-axis absolute + non-enlarging composed + upscale divisor + minimum clamp；generic carrier L2` | 零操作、单操作与多操作分别规范化为 `input`、`scale`、`fit`、`absolute`、`composed`；四个 authored 字段分别保留，非数字、非有限或 `<=0` 为 `unsupported` | 同一 `pixelExtent` 先以显式 width/height 覆盖对应输入轴，fit 只缩小并保持当前比例，再除以 scale，最后 floor/min-1；actual frame plan与preflight共用resolver，logical binding telemetry要求before/after descriptor完整、正extent、格式与logical identity稳定；complete plan signature包含actual input和target descriptor | 真实`3749463715`资源树派生graph已让两张同descriptor target分别闭合`scale:2 -> 1024x576`、`fit:256 -> 256x144`、`fit:256+scale:2 -> 128x72`、双轴absolute `640x360`、单轴`width:640 -> 640x1152`、单轴`height:360 -> 2048x360`、`scale:4 -> 512x288`、`width:1000+fit:600+scale:2 -> 260x300`、upscale `scale:0.5 -> 4096x2304`与minimum clamp `scale:10000 -> 1x1`，均完成GPU/publication/compositor/next-frame。upscale链进一步完成420次history COW，minimum链完成364次history COW；A→B→A lifecycle另证明input改变时scale descriptor随之重建并discard旧history，双轴absolute descriptor保持并复制readable history；十种copy-connected mismatch均以typed `unsupported-target-descriptor`局部拒绝，安全frame继续完成 | [IR/resolver](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/AuthoredGraph/SceneAuthoredEffectRenderPlanner+Resolution.swift)、[pixel extent](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/GraphTargets/SceneGraphRenderTargetPlan+Extent.swift)、[persistent pool](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/GraphTargets/SceneOffscreenTexturePool.swift)、[observation contract](../../../MyWallpaperX/Core/SteamWorkshopScene/Runtime/SceneGraphExecutionObservation.swift)、[target plan tests](../../../script/tests/test_scene_graph_render_target_plan.py)、[pool transaction tests](../../../script/tests/test_scene_offscreen_texture_pool.py)、[non-enlarging cohort tests](../../../script/tests/test_scene_nonenlarging_extent_cohort.py)、[E-V1-AUTHORED-FBO-SCALE-EXTENT](runtime-evidence-index.md#e-v1-authored-fbo-scale-extent)、[E-V1-AUTHORED-FBO-FIT-EXTENT](runtime-evidence-index.md#e-v1-authored-fbo-fit-extent)、[E-V1-AUTHORED-FBO-FIT-SCALE-EXTENT](runtime-evidence-index.md#e-v1-authored-fbo-fit-scale-extent)、[E-V1-AUTHORED-FBO-ABSOLUTE-EXTENT](runtime-evidence-index.md#e-v1-authored-fbo-absolute-extent)、[E-V1-AUTHORED-FBO-SINGLE-AXIS-EXTENT](runtime-evidence-index.md#e-v1-authored-fbo-single-axis-extent)、[E-V1-AUTHORED-FBO-NONENLARGING-EXTENTS](runtime-evidence-index.md#e-v1-authored-fbo-nonenlarging-extents)、[E-V1-AUTHORED-FBO-UPSCALE-EXTENT](runtime-evidence-index.md#e-v1-authored-fbo-upscale-extent)、[E-V1-AUTHORED-FBO-MINIMUM-EXTENT](runtime-evidence-index.md#e-v1-authored-fbo-minimum-extent)、[E-V1-AUTHORED-FBO-INPUT-EXTENT-LIFECYCLE](runtime-evidence-index.md#e-v1-authored-fbo-input-extent-lifecycle) | `L3`只覆盖上述十个同descriptor RGBA bounded copy chains及一条solid-capture input resize lifecycle；其他absolute/single-axis/composed仍只有unseen harness。arbitrary fit/absolute/composed、其他format/load-store/arbitrary lifecycle与官方数值parity仍未闭合 | 后续extent/lifecycle atom须使用合法consumer与actual allocation/publication闭合新的作者组合；未知storage/identity/hazard继续hard reject |
| `format` | `L2` | `r8`、`rg88`、`r16f`、`rg1616f`、`rgba8888`、`rgba_backbuffer`可进入typed target plan；其他已识别格式继续失败关闭 | R8/R16F使用`.r8Unorm/.r16Float`、`.scalarRed*`与R write mask；typed regular scalar consumer按所有active variant守恒接受`redOnly / wholeVector`，`.g/.rg`与unproven关闭。RG88/RG1616F使用`.rg8Unorm/.rg16Float`、`.redGreen*`、R/G write mask与`.preservedChannels` publication，consumer接受R/G subset或whole-vector；Metal对未存component作确定性默认扩展。RGBA通常按color/preserved-data初始化；只有完整feedback shape能把两张target覆盖为`.independentAlphaSignal`。所有Program与attachment必须精确匹配，repeat sampling随publication冻结 | 既有四种storage门保持；未见R16F ordered pair以4个writer完成frame 0/next-frame GPU、typed frame/persistent publication与scratch非持久。新增RGBA signal pair从transparent clear到next-frame保持独立alpha并在真实Fluid中服务完整graph；nonzero clear与wrong composite关闭 | [format mapping](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/GraphTargets/SceneGraphRenderTargetFormat.swift)、[attachment storage](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/EffectExecution/SceneResolvedMaterialAttachmentStorage.swift)、[publication](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/GraphTargets/SceneGraphRenderTargetLease+Publication.swift)、[independent-signal feedback tests](../../../script/tests/test_scene_independent_signal_feedback.py)、[E-V1-INDEPENDENT-SIGNAL-FEEDBACK](runtime-evidence-index.md#e-v1-independent-signal-feedback)、[E-V1-PRESERVED-CHANNEL-ORDERED-FEEDBACK](runtime-evidence-index.md#e-v1-preserved-channel-ordered-feedback)、[E-V1-PRESERVED-CHANNEL-COMPONENT-WHOLE-VECTOR](runtime-evidence-index.md#e-v1-preserved-channel-component-whole-vector) | R8/R16F不准green/pair/unproven；whole-vector只证明typed single-component默认扩展，不把graph resource改成color。显式B/RGB/permutation、unknown/vertex use、same-node read/write、未登记copy/swap/multiwriter/command、其他格式或官方数值parity不开放 | 每个新format/topology仍须独立证明typed initialization、consumer、publication/completion、rollback与反例；真实Fluid执行不开放arbitrary RGBA signal target |
| `clear` | `L3 bounded typed initialization` | string/array 四分量经 finite + `[0,1]` 校验进入 typed target descriptor；`clear` 与 history seed/function clear 仍区分 | 普通RGBA与R8/RG88/R16F/RG1616F target的首次read/initialization由`SceneGraphResourcePassEncoder`按typed storage编码，publication/content representation与后续MaterialProgram读取保持同一identity；preserved-channel clear通常只授权single-writer/no-command/typed consumer，exact ordered pair则另要求双target均authored clear并由首读history/末写history守恒；首次创建/重建后不重复clear | 既有RGBA、RG1616F clear-seed与surface resize证据保持；新增R16F ordered pair在frame 0有initialize、next-frame无initialize且输出稳定，缺任一clear在claim前拒绝。见[E-V1-AUTHORED-RGBA-CLEAR](runtime-evidence-index.md#e-v1-authored-rgba-clear)、[E-V1-PRESERVED-CHANNEL-CLEAR-SEED](runtime-evidence-index.md#e-v1-preserved-channel-clear-seed)与[E-V1-PRESERVED-CHANNEL-ORDERED-FEEDBACK](runtime-evidence-index.md#e-v1-preserved-channel-ordered-feedback) | 不外推arbitrary clear语法、未登记格式/通道、任意unique/history/command/multi-writer、target extent、seek/stop或官方数值parity；ordered pair是严格拓扑例外。history rehydrate每帧新allocation的`allocation-reprepare`诊断不能当作resize事件计数 | 按独立atom继续验证更宽reset/load-store/lifecycle；任何identity/hazard不确定继续硬拒绝 |
| `unique` | `L3 bounded history/frame-local identity` | bool声明和effect-scoped identity已保留，非bool阻断；`unique`本身不自动等同history，persistence仍由read/write/copy数据流与target plan共同决定 | 已准入的read-before-write unique target取得独立persistent allocation/history seed；四种preserved storage保留single-writer/no-command/typed consumer合同。exact ordered pair允许两张same-descriptor unique+clear target严格交替writer；ordinary single-writer/no-command/no-clear且write-before-later-read的unique target只取得frame-local allocation/publication。independent-signal RGBA pair另要求same descriptor、透明clear、唯一preserving update、唯一terminal composite/swap；两张target都沿既有persistent mapping跨frame保持typed signal。publication/completion、pending generation、invalidation、scene switch、stop与pause/resume合同不变 | 既有history/lifecycle证据保持；R16F ordered pair闭合frame 0/next-frame与scratch非持久；四format frame-local门锁定无history；新RGBA pair在未见门跨frame累积，真实Fluid从frame0到frame138反复GPU/publication/compositor成功 | [definition/graph IR](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/AuthoredGraph/SceneAuthoredEffectRenderPlan.swift)、[preserved history gate](../../../script/tests/test_scene_preserved_channel_history.py)、[frame-local unique gate](../../../script/tests/test_scene_preserved_channel_frame_local_unique.py)、[ordered feedback gate](../../../script/tests/test_scene_preserved_channel_ordered_feedback.py)、[independent-signal feedback gate](../../../script/tests/test_scene_independent_signal_feedback.py)、[runtime lifecycle gate](../../../script/tests/test_scene_resolved_material_runtime_bridge.py)、[E-V1-INDEPENDENT-SIGNAL-FEEDBACK](runtime-evidence-index.md#e-v1-independent-signal-feedback)、[E-V1-PRESERVED-CHANNEL-UNIQUE-HISTORY](runtime-evidence-index.md#e-v1-preserved-channel-unique-history)、[E-V1-PERSISTENT-HISTORY-EXECUTOR-INVALIDATION-RECOVERY](runtime-evidence-index.md#e-v1-persistent-history-executor-invalidation-recovery) | 只对已登记exact history、frame-local或signal-pair形态成立；same-node、未登记command/consumer/multiwriter继续关闭。不能把任意`unique:true`、同名target、device loss、其他topology/provider/format/SceneScript/particle state、seek/discontinuity、系统事件pause、多surface或跨effect sharing读成已支持 | 每个新persistence topology仍须证明identity、初始化、publication generation、reset/teardown与局部失败边界；本批真实extent变化的reprepare/reseed观察不等于完整resize门 |
| FBO `UV` / wrap | `L3 bounded` | authored absent映射`.clampToEdge`，exact`"repeat"`映射`.repeatWrap`；未知及非string值失败关闭 | address mode进入target descriptor、execution/allocation identity与typed publication；sampler分别为normalized linear clamp或S/T linear repeat | RGBA/R8越界GPU probes证明clamp/repeat方向，真实R8 two-node graph取得bounded可见正证 | [address resolver](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/GraphTargets/SceneGraphRenderTargetPlan+Extent.swift)、[address tests](../../../script/tests/test_scene_graph_target_address_mode.py)、[publication tests](../../../script/tests/test_scene_graph_texture_publication.py)、[E-V1-FBO-UV-ADDRESS-REPEAT](runtime-evidence-index.md#e-v1-fbo-uv-address-repeat) | 不开放未知UV、filter/edge/rounding、history/swap sampling identity或官方客户端行为；不能从本项外推arbitrary FBO | 每个新command/lifecycle topology继续证明sampling identity守恒 |

表内的“多writer未授权”均指除上述exact原子以外的任意writer数、顺序、consumer图、format和lifecycle；已登记的特例只是单effect、一张nonunique/no-clear `rgba_backbuffer`、两个有序writer和一个terminal consumer，其证据与失败半径不能外推为整个FBO/multi-writer family。


> 当前Fluid执行事实更正：上表与下方各冻结段中旧node/resource/color拒绝只保留时间有界事实。现役independent-signal feedback合同已把两张透明seed RGBA target从初始化到publication标为`.independentAlphaSignal`；fresh真实`3749463715`仍为0/1 NON-PASS、exit0、无timeout，但Fluid `560#effect#617`已以`resolved-material-graph / program`完成17 material + 2 swap、0 rejected，并在frame0到frame138取得GPU、exact publication、terminal compositor和next-frame。accepted graph exact-backend缺口是layer464/1106 Gradient Opacity static-uniform binding与layer536 test_shader frontend；整样本还单列layer533 `unclaimed-effect-product-authority / unclaimed-visible-effects` route-operation failure，两类都不是Fluid graph拒绝。该结果证明bounded Fluid执行，不证明Fluid隔离ROI、官方像素parity、owner migration、V1完成或V2切换；详见[E-V1-INDEPENDENT-SIGNAL-FEEDBACK](runtime-evidence-index.md#e-v1-independent-signal-feedback)。

> V1 independent-signal compositing合同：共享analyzer不看effect/material/sample identity，只在exact signal whole RGBA sample、exact previous-color direct sample与末尾`ApplyBlending(mode 1...32, previous.rgb, signal.rgb, factor[exactly one signal.a])`、combined alpha `saturate(previous.a + signal.a)`、whole output连续成立时发布`.independentAlphaSignalCompositing(signalSlot:colorSlot:)`。signal prefix只能member-local使用；wrong base/alpha、carrier escape/replacement、post-tail mutation、shadowed saturate与同slot/重复whole sample全部关闭。MaterialProgram要求signal slot为`.independentAlphaSignal`、previous color为opaque/premultiplied且全部其余texture事实为scalar/data，并把output定为`.premultipliedAlpha`；auxiliary color拒绝。bundled `effects/fluidsimulation_combine` default variant经真实preprocess得到`signal-composite:0:1`并完成bounded Metal compilation；production PassEncoder 2x2 GPU门把`204/51/26/128` signal与`26/51/76/255` previous合成为四像素`115/51/51/255`，坏combined-alpha tail在Program assembly前拒绝。最终checkpoint为51 modules / 166.2 s / ALL OK，code health 864 Swift / 96 warnings / PASS，Developer ID Debug BUILD SUCCEEDED且closure complete。最高仅为`S3 executable Program/GPU correctness`；未证明GraphTargets publication、compositor、next-frame、完整Fluid graph或official pixel parity。

> V1 independent-signal feedback target合同：共享graph classifier不读取effect/material/sample/path/hash，只接受一个blocker-free effect中的两张same-descriptor、`unique:true`、transparent-clear RGBA target；唯一update必须以全variant守恒的signal-preserving transfer从history写scratch，唯一terminal consumer必须以signal-compositing transfer读取scratch与effect input，随后只有一个terminal swap且无post-swap pair access。LayerCapability冻结两个identity的`.independentAlphaSignal`，GraphExecutor初始化、persistent logical mapping、publication、generation、completion、rollback与compositor仍复用现有owner。未见production Metal门跨frame累积；nonzero clear、copy、额外reader与wrong composite关闭。真实Fluid完整graph执行见[E-V1-INDEPENDENT-SIGNAL-FEEDBACK](runtime-evidence-index.md#e-v1-independent-signal-feedback)。

## 4. 官方 Shader 六页合同

以下六个 anchor 与 [官方逐页映射](official-page-map.md) 一一对应。这里记录官方公开语义，不把编辑器操作步骤误算成运行时能力。

<a id="op-shader-overview"></a>
### 4.1 Overview

| 官方合同 | 当前级别 | 当前边界 / 实现门 |
|---|---|---|
| effect shader 使用类 GLSL source，并在需要时翻译为 HLSL | `L3 子集` | ShaderContract 保真后，中性 preparation/frontend 可把 bounded framebuffer/单源颜色、host/dynamic uniform 与 texture schema 编译成完整 Program；现役 GPU owner 是 resolved-material graph executor。三组 exact Scroll 只作历史 oracle，不再通过 profile 推断独立 owner；include/combo、任意 material texture、复杂 state/FBO/多 pass 与 arbitrary source 仍不能外推为官方完整兼容。见 [E-SHADER-PREPARATION](runtime-evidence-index.md#e-shader-preparation) 与 [E-AUTHORED-SHADER](runtime-evidence-index.md#e-authored-shader)。 |
| 官方向 effect shader 提供向后兼容承诺 | `L0` | MyWallpaperX 只有 bounded Program/executor 子集，尚不能承接官方的通用向后兼容承诺；剩余 strict MSL 也不能冒充 source compatibility。 |
| 3D 自定义 shader 必须兼容 `generic2.vert` / `generic2.frag` | `L0` | 无 3D model/runtime，也未取得这两个 stock contract 的合法本地 schema。 |
| 不应替换 particle 等 system shader；官方不保证其向后兼容 | `L0` | 产品门：即使未来能读取，也必须默认拒绝 system-shader override，不能把它纳入首期 arbitrary shader。 |

<a id="op-shader-syntax"></a>
### 4.2 Syntax

官方公开syntax资料只覆盖自定义preprocessor、macro与conditional，没有公开`#require`或module ABI，所以下表不把它伪列为official-public-contract。MyWallpaperX当前另有一个严格项目profile：active exact `LightingV1`只在directive-position `LIGHTING`为typed整数0时形成zero-source typed module resolution；其他active module/值/语法失败关闭，inactive只按项目策略做syntax-only验证。目标、来源与证据上限见[E-V1-LIGHTINGV1-ZERO-SOURCE-REQUIRE](runtime-evidence-index.md#e-v1-lightingv1-zero-source-require)。

| 官方合同 | 当前级别 | 当前边界 / 实现门 |
|---|---|---|
| custom preprocessor 支持布尔/逻辑条件，但不是完整 C preprocessor；官网移动端指南不承诺 `#elif` | `L2 bounded preparation` | 有界 evaluator 支持 object-like `#define/#undef`、普通/零参数 function-like macro、嵌套调用、宏实参、object alias、`#if/#ifdef/#ifndef/#elif/#else/#endif`、按 relational 高于 equality 的整数/布尔表达式与 include 展开；`#elif` 只按同一 conditional frame 的首个命中分支执行，unmatched/empty/after-else 失败关闭。macro expansion 识别 code/string/character/line-comment/block-comment，selected/host macro 保留 defined/undefined 所有权。只让 active 行贡献诊断和 metadata，并保留逐行 source map、依赖 hash、输入/输出/深度/token/字节预算。variadic、stringize/paste、递归、引号、跨行 replacement、未知/畸形 directive、directive-line annotation、循环/缺失/冲突 include 均失败关闭；combo/environment 不能注入 token sequence。该语料兼容子集不改变官网对移动端作者写法的限制，也不自动扩大 GPU admission。 |
| 环境 define：`GLSL`、`HLSL`、`HLSL_SM40`、`HLSL_GS40` | `L1 recognized/fail-closed` | 名称已归为 host-owned backend-language requirement 并进入稳定诊断边界；Metal backend 尚无已验证 provider，material/authored source 不能伪造这些值，因此命中时仍失败关闭，也不形成可执行 environment variant。 |
| 跨语言函数：`texSample2D`、`texSample2DLod`、`mix`、`frac`、`saturate`、`atan2`、`ddx`、`ddy` | `L3 bounded subset` | frontend已逐符号翻译真实Program所需的sampling、mix/frac、常用数学与有界helper；external compiler输入对每个active sampler生成同一slot的两项texture-transform uniform与coordinate函数，`texSample2D`/`texture2D`及LOD入口统一消费mapped/padded/animated UV，inactive sampler不扩ABI。built-in `texSample2DLod`只接受直接sampler identifier、既有可编译UV表达式与单个number或唯一静态可证float identifier，并在vertex/fragment stage生成Metal `sample(..., level(...))`；user-defined同名函数不重写，错参数数、间接/未知sampler、vector/matrix/compound或歧义LOD继续以`unsupportedSampler`失败关闭。`mix/lerp`只在确认为built-in、前两参可由有界typed parser全量消费且weight为scalar或同宽向量时才缩到共同最小宽度。parser接受直接identifier/swizzle/number/grouping、`*`/`/`最小宽度链及已同宽的`+/-`包络，只缩窄可直接证明的较宽叶子；user-defined、call/constructor/sample/index/ternary、整数/matrix、错宽weight及compound较宽侧不猜测并继续失败关闭。`inverse`只接受静态typed `mat3`并翻译到项目自有3×3 cofactor helper，其他matrix/scalar/vector形态在compile前失败关闭。未实现符号仍拒绝，不能把整行函数集合读成全支持。 |
| 构造/转换：`CAST2`、`CAST3`、`CAST4`、`CAST3X3` | `L2/L3 bounded` | macro/prelude与typed constructor已有受限处理；产品normalizer与external compiler worker现在都把`CAST3X3(x)`精确映射为GLSL `mat3(x)`，由后端负责合法4x4→3x3构造与链接；HLSL浮点向量隐式缩窄只对静态可证乘除链、赋值/参数目标，以及上述built-in `mix/lerp`有界typed实参开放；函数参数只接受简单value-type `inout name`并翻译为Metal thread reference。程序域静态`const`转换为Metal `constant` address-space，函数局部`const`不变，runtime-dependent或mutable global仍由既有声明/Metal门拒绝。数组/畸形`inout`、user-defined调用、未证复合参数、其他隐式转换与未证CAST形态失败关闭。 |
| HLSL-style float modulo operator `%` | `L3 bounded statement` | 仅简单标量声明赋值、目标与左右操作数均可静态证明为float/int/uint且至少一侧为float时，显式翻译为`fmod`后按目标构造；纯整数`%`保持原生。括号复合左值、向量、表达式链、歧义声明与其他目标继续由Metal拒绝，不做语料/path分派。 |
| legacy stage IO：`attribute`、`varying`、`uniform`、`gl_FragColor` | `L3 bounded 2D effect` | frontend已对2D image-effect的position/texcoord、typed varying、stage-qualified uniform ABI与单fragment output做link并由Program执行；`a_Position`按vertex source是否实际引用`g_ModelViewProjectionMatrix`分别接收target pixels或NDC/clip-space，4x3 direct-clip GPU门锁定逐像素保持；其他layout/interpolation/MRT关闭。 |
| image/text vertex 只用 `a_Position`、`a_TexCoord`；完整输入集合还包括 `a_PositionVec4`、`a_Normal`、`a_Tangent4`、`a_TexCoordVec3/Vec4`、`a_TexCoordC1...C5` 的 vec2/vec3/vec4 变体及 `a_Color` | `L0` | 自有 quad/particle layout 不算 authored attribute；需按对象类型注册 vertex source。 |

<a id="op-shader-variables"></a>
### 4.3 Variables

| 官方合同 | 当前级别 | 当前边界 / 实现门 |
|---|---|---|
| `[COMBO]` checkbox/options：`material`、`combo`、`type`、`default`、`options` | `L2 bounded preparation` | exact case-sensitive `[COMBO]` 已形成 typed schema；annotation 数值以 Decimal-first/exact Int64 IR 解析，Template只保留material显式值，active schema在下游按“显式优先，否则有效且一致default”形成统一macro environment与provenance；options无论最终取显式/default都完整验证。JSON `require/requireany`保真为authoring/editor关系metadata，不参与player compile-map剪枝；sampler readiness/format的resource requirement另经bounded fixed point形成active variant。底座只含词法无条件 root/include schema，最多8个条件候选会穷举所有非空子集；多个stable prepared signature报`active-schema-ambiguous`，候选数或估算累计工作超过256 MiB报`active-schema-audit-budget`。严格JSON失败的metadata保留raw text与`malformedAnnotation`诊断且不形成typed record；只有声明外comment-only `[COMBO]`能证明root stage存在词法无条件、同名且除坏`options`外顶层字段完全相同的合法record时才跳过，附着声明、conditional/conflicting/missing counterpart、所需default缺失/冲突schema及其他fatal diagnostic仍失败关闭。disabled marker与坏最终schema失败关闭，prepared variant尚不授权GPU permutation。 |
| General：`g_Time`、`g_Daytime`、`g_Frametime`、current/last pointer、texel/half-texel、screen、alpha/color/color4、parallax | `L3 bounded` | Program ABI 已按官方名称编码 time/daytime/frametime、current/last pointer、screen、texel/half-texel 与 MVP 的已证子集；alpha/color/color4、parallax、完整单位/作用域和更新频率仍未闭合，逐项状态见下表。 |
| View：eye/view/orientation 三轴、model/inverse、view-projection、model-view-projection/inverse | `L2` | 部分自有矩阵存在；缺官方坐标系、转置、inverse 和 object/surface scope 合同。 |
| Effect/Layer：effect model/MVP/inverse、effect texture projection/inverse、layer model | `L3 bounded 2D effect/layer matrices` | Program host schema已对当前2D effect提供effect texture projection及inverse；另以exact `float4x4 g_LayerModelMatrix`消费同帧`worldFramesByLayerID`，缺失即失败关闭。Iris/Depth/Light Shafts消费前者，`3768724269:163` rounded-mask消费后者并取得GPU/publication/compositor/next-frame与ROI正证。其他effect model/MVP/inverse、geometry-size/pivot组合、任意3D空间及官方/Windows矩阵逐值仍未闭合。 |
| 每个 `T0...T7`：`g_TextureNResolution.xy` physical / `.zw` mapped、Rotation、Translation | `L3 bounded` | 共享 slot-binding atom 固定表达 authored `0...7` 与 hole，并原子携带 candidate identity/generation、purpose、physical/mapped、完整 UV、sampler、format、mip/resolution/texel；完整 Program 可按 active slot 把 `g_TextureNResolution` 编入 uniform bytes 并由 resolved executor 消费。Rotation/Translation、未证 purpose/readiness、复杂 UV 与任意 sampler 仍拒绝。 |
| Audio：16/32/64 bin、left/right、低到高、正值且不归一化 | `L3 bounded` | 同一次 FFT 已建立 16/32/64 left/right host snapshot，静音/暂停/无 consumer 有明确策略；六组 `g_AudioSpectrum{16|32|64}{Left|Right}` 数组已进入 bounded Program ABI，三个Simple Audio Bars、Workshop Audio Hue、Enhanced Audio Bars、三个exact Shake source/profile以及exact `stock2842` audio color-only与alpha-only Pulse均由实际Program consumer驱动采集。Pulse alpha-only与combined各允许一个exact optional opacity mask；fresh masked-audio default/restore已分别让两个 sibling profile进入audio-host-ABI Program并闭合frame0/next-frame GPU、publication、compositor，坏artifact与disable只发布目标previous-current且0 dedicated。同次运行的host log在frame30及以后记录非零且变化的stereo snapshot，但保留的目标effect transaction只有frame0/next-frame、没有late-frame identity/uniform join，因此不声称目标Program已被直接证明消费非零快照。historical CAST3 masked alpha-only是non-audio独立cohort，现也已由fresh default/corrupt/disable/restore闭合；它不增加audio能力。Pulse binding/SceneScript/Timeline、多contributor、其他legacy/noncanonical及其他未迁移形态仍保留bounded consumer或失败关闭。Enhanced只迁移一个同时具graph-input/stage-uniform/mutable-varying证明的窄profile，Shake也只迁移三个exact source/profile；尚无通用 audio shader 准入、SceneScript `AudioBuffers` 或官方数值 parity。见 [E-AUDIO-INPUT](runtime-evidence-index.md#e-audio-input)、[E-AUDIO-EFFECT](runtime-evidence-index.md#e-audio-effect)、[E-EFFECT-WORKSHOP-ENHANCED-AUDIO-BARS](runtime-evidence-index.md#e-effect-workshop-enhanced-audio-bars)、[E-V1-PULSE-STOCK-AUDIO-COLOR-OWNER](runtime-evidence-index.md#e-v1-pulse-stock-audio-color-owner)、[E-V1-PULSE-STOCK-AUDIO-ALPHA-OWNER](runtime-evidence-index.md#e-v1-pulse-stock-audio-alpha-owner)、[E-V1-PULSE-MASKED-AUDIO-OWNER](runtime-evidence-index.md#e-v1-pulse-masked-audio-owner)、[E-V1-PULSE-CAST3-MASKED-ALPHA-OWNER](runtime-evidence-index.md#e-v1-pulse-cast3-masked-alpha-owner)与[E-V1-PULSE-MASKED-ALPHA-ONLY-OWNER](runtime-evidence-index.md#e-v1-pulse-masked-alpha-only-owner)。 |
| sampler `g_Texture0...7` annotation：`material`、`label`、`hidden`、`default`、`mode`、`combo`、`paintdefaultcolor` | `L3 bounded typed graph-input subset；其余metadata L2` | sampler declaration同行的`combo/require/requireany`已形成独立的resource readiness/format typed schema并保持slot`0...7`；这类require仍参与项目bounded resource fixed point，不受authored `[COMBO]` editor metadata结论影响。`material`、`hidden`、已登记`mode`、typed `default`与presence `combo`已有bounded projection；explicit material alias、implicit missing alias与dormant unresolved material key现在统一形成typed graph-input source-slot fact并保留provenance。dormant形态要求exact unnamed effect input、所有variant唯一active regular color carrier、hidden+非空authored key但template exact slot nil、无任意candidate/default/readiness fallback或explicit graph binding，并由source/frontend共同证明同slot previous/current color及alpha conservation；key、label、hidden单独或任意相似alias都不授权graph，slot/name/mode/purpose/variant分歧继续拒绝。authored candidate与shader default优先于typed fact；高优先级pending/missing/unavailable、wrong purpose/publication/generation不回落到推断current。作者annotation/declaration或`formatcombo`无法形成launch schema时产生`authoredSamplerSchemaInvalid`，只有exact single-current-pair leaf可复制previous current并继续suffix，不选择或伪造texture。reachable schema 与 compiled variants 只由 launch envelope 冻结并供 frame selection 消费；`RuntimeCatalog` 同形读取只用于 launch demand discovery，不是 execution owner。其余editor/paint metadata仍只是loss-preserving JSON，不能当成完整sampler annotation AST。binding order/duplicate slot/slot-name identity由共享验证分别返回`samplerBindingOrderInvalid`/`samplerBindingDuplicateSlot`/`samplerBindingIdentityMismatch`，active `[PASS]`、internal target与cross-variant divergence分别返回`activePassUnsupported`/`samplerInternalTargetUnsupported`/`samplerVariantSchemaDivergence`；六者继续映射`material-variant-envelope-invariant`并硬拒绝，不在passthrough allowlist。证据见[E-V0-SAMPLER-BINDING-PROVENANCE](runtime-evidence-index.md#e-v0-sampler-binding-provenance)、[E-V0-LAUNCH-ENVELOPE-OWNER-CONSOLIDATION](runtime-evidence-index.md#e-v0-launch-envelope-owner-consolidation)、[E-V0-AUTHORED-SAMPLER-SCHEMA-FALLBACK](runtime-evidence-index.md#e-v0-authored-sampler-schema-fallback)与[E-V1-DORMANT-UNRESOLVED-MATERIAL-GRAPH-INPUT](runtime-evidence-index.md#e-v1-dormant-unresolved-material-graph-input)。 |
| paint mode：`opacitymask`、`rgbmask`、`flowmask`；flow idle 以 127/255 为中心；optional texture 仅在实际绑定时定义 combo | `L2 readiness decision only` | optional texture combo 只有在同代 slot readiness 明确可用/不可用时才产生带 slot provenance 的 variant；bounded Program 会原子消费已证 candidate generation，readiness 缺失保持 unknown 并失败关闭。通用 mask 解码、paint metadata 与更多 purpose 仍未闭合。 |
| user uniform：slider (`default/range`)、color (`type=color`)、UV (`position=true`)；名字应以 `u_` 开头 | `L3 bounded` | reflected layout 已保存 constant 与 typed `shaderValue` dynamic uniform；既有equal-lane scalar→float2窄投影保持独立，另有exact direct vector3 color binding可从显式`range`或`type=color`得到author domain、拒绝越界live值并编码作者fallback。完整 editor schema、其他default/range、UV position、任意 user/script binding 与多 producer 冲突仍关闭。 |

<a id="op-shader-headers"></a>
### 4.4 Headers

| 官方模块 | 公开合同 | 当前级别 / 实现门 |
|---|---|---|
| `common.h` | `M_PI`、`M_PI_HALF`、`M_PI_2`、`SQRT_2`、`SQRT_3`；`hsv2rgb`、`rgb2hsv`、`rotateVec2`、`greyscale` | `L0`：R2 source graph 可定位并展开 header 字节，但没有这些常量/函数的 typed 跨语言语义、数值 golden 或 GPU consumer；资源准备不能抬高具体 helper 能力。 |
| `common_fragment.h` | `DecompressNormal`、`DecompressNormalWithMask`、`ConvertSampleR8`、`ConvertTexture0Format` | `L0`：本批只以typed graph scalar content与显式direct `.r` consumer闭合Metal R8原子，不实现、替代或绕过`ConvertSampleR8`/`ConvertTexture0Format`；format helper、非red分量、缺省GBA与跨后端通道适配仍未开放。 |
| `common_vertex.h` | 两种 `BuildTangentSpace` | `L0`：依赖完整 vertex attribute、model transform 与 handedness。 |
| `common_blending.h` | `BLENDMODE` imageblending combo + `ApplyBlending(BLENDMODE, colorA, colorB, blend)`；首参为兼容性保留 | `L3 bounded`：0…32 模式由共享`SceneBlendModeShaderSource`在GPU上对独立numpy oracle执行198个数值case，含mode 5/10忽略opacity、mode 31为加法而非mix、mode 0强制alpha=1；`source-proven-graph-input-color-blend`另由prepared authored source事实准入stock mask multiply、legacy mask override和mode 0 alpha形态，并经普通Program/GraphExecutor的mapped/mip R8 mask门。旧Tint专用planner/profile/pipeline/renderer/loader不再有产品或测试owner；layer color blend仍是独立结构性compositor步骤。共享primitive数学门不等同于generic compiler对任意真实header revision的完整产物数值门，因此不授权其他Effect、任意include/helper/revision或Windows像素等价。 |

<a id="op-shader-mobile"></a>
### 4.5 Mobile

| 官方合同 | 当前级别 | 当前边界 / 实现门 |
|---|---|---|
| 目标为 DirectX 11 HLSL 4.0 与 OpenGL ES GLSL 3.00 ES；官方会自动改写 source | `L0` | MyWallpaperX 目标是 Metal，不宣称移动端编译 parity；但 source parser 必须保留这些条件分支。 |
| 类型 alias：`vec2/3/4`、`mat2/3/4x3/4`；函数 alias 另含 `mul`、`mod`、`fmod` | `L0` | 与 Syntax 中 alias 一并进入可版本化 symbol table。 |
| 整数字面量默认尝试改成 float；`WEMOBILE_DISABLE_INTEGER_CONVERSION` 可关闭 | `L0` | 不能靠文本替换；需 token-aware rewrite 和 before/after fixture。 |
| 平台分支用独立 `#ifdef HLSL` / `#ifdef GLSL`；`#elif` 不支持，`#else` 对未来 API 不稳 | `L0` | preprocessor 需与官方限制一致并输出明确诊断。 |
| 移动端编译失败时移除所属 effect，壁纸继续运行 | `L0` | MyWallpaperX尚未实现官方移动端的per-effect移除合同。当前只有更窄的source-stage故障隔离降级：route精确为`.unclaimed`，不存在已授权/可执行typed dependency、source-copy、frame target或final-alpha，也没有已加载mask或其他非中性合成责任，且exact static-file或current-media atom与authored quad全部闭合时，仅把source交给普通final layer compose。它不执行或发布effect output，记录`degraded-layer-source-passthrough`并使benchmark保持NON-PASS，不得用来提升本行等级或声称官方行为。 |

<a id="op-shader-desaturation"></a>
### 4.6 Desaturation Tutorial

官方教程给出了首个 generic shader executor 的最小合法 fixture：hidden framebuffer `g_Texture0`、`v_TexCoord`、`texSample2D`、`CAST3(dot(...))`、`gl_FragColor`，再加入 `u_DesaturationAmount` slider 与 `mix`。当前为 `L0`：现有色彩近似/手写 pipeline 没有执行这份 authored source。只有这份 fixture 能经 source parse、annotation、material/slot、variant、uniform upload、GPU 编译和像素门端到端通过后，才能把对应 primitive 升级。

## 5. 当前 Shader primitive 覆盖

Standard Blur 的 static/KERNEL0 whole-stage topology 只提供候选范围；产品撤权还必须从真实 terminal material 重跑 shader-contract、Template/ShaderPreparation、canonical source、active sampler/runtime loop、共享 unit-composite eligibility 与 color-transfer proof。source/profile 不合格的 strict remainder 不会被 topology-only 误撤。

2026-08-20 的 typed Timeline effect-vector consumer 已把 `.effectConstant` 动态 definition 从固定 scalar 改为严格 1–4 维 shape：producer 对完整 raw token、有限值和 lane 数负责，Program reflection/统一 encoder 仍拥有最终 field 类型校验。两帧 vector3 普通值变化只改变 uniform bytes，不改变 semantic identity 或 prepared shader cache key；合法 vector2 snapshot 写入 reflected vector3 field 仍返回 `dynamicUniformBindingInvalid`。representative Tint color 正门经 shared Program/GraphExecutor 可见，lane mismatch 只局部 passthrough，profile-local `disable-generic` 仍回到 shared `boundedSwift` frontend；真实 `3748311238:728` 的两个 vector2 Shake `friction` consumer 也进入普通 Program/GPU/publication/compositor，frame fault 只局部复制 previous current 并恢复原 Program；见 [E-V1-TYPED-TIMELINE-EFFECT-VECTOR](runtime-evidence-index.md#e-v1-typed-timeline-effect-vector)与[E-TIMELINE](runtime-evidence-index.md#e-timeline)。该 Timeline 原子本身没有撤销旧 owner；Opacity、Tint与三个exact Shake source/profile分别由后续独立owner门撤权。Timeline 证据不外推其余 vector occurrence、整个 effect family、owner migration 或 parity。

> 2026-08-22 的single-image animated material atlas原子已取代下方Lighting段落中记录的旧resource冻结断点；详见[E-V1-SINGLE-IMAGE-ANIMATED-MATERIAL-ATLAS](runtime-evidence-index.md#e-v1-single-image-animated-material-atlas)。catalog只保存immutable definitions，surface-scoped FrameProvider让同一GPU texture/file lifecycle按scene time发布frame UV与单调content generation；frame duration只准finite nonnegative，作者值0按既有sprite autoplay规则规范为effective `1/60 s`。每个active slot的mandatory host transform ABI由boundedSwift与generic实际sample consumer共享，static identity像素等价，mapped/padded static也走同一UV correctness。项目Developer ID App正/恢复闭合actual Program/GPU/publication/compositor/next-frame与四个预登记ROI簇；非法geometry或negative/nonfinite/aggregate-overflow duration反门的benchmark aggregate仍为NON-PASS，但原始typed ordinary-effect previous-current/GPU/compositor/next-frame链成立。后继Program attribution、graph-level source conservation、dominating output initialization、independent RGBA carrier与dormant source binding classifier已依次越过真实Fluid的node0、node14与node15旧断点；该段冻结时首断点为node17 `fluidsimulation_combine.json#0`的`color/colorContractUnproven / shader-color-transfer-unresolved`，现已由[E-V1-INDEPENDENT-SIGNAL-COMPOSITING](runtime-evidence-index.md#e-v1-independent-signal-compositing)越过。该冻结probe仍NON-PASS、Fluid target Program/GPU仍0、outer仍`r5-no-runtime-owner`；非Fluid执行/可见、Fast/fixed/full、官方parity、owner migration、V1完成或release。

> Transform pass vector2 是当前 uniform producer 的明确边界，不是 static identity 准入问题。**目标合同**要求作者 `{script,user:null,value}` scale 由真实 typed SceneScript producer写入同一 surface snapshot。**当前事实**是 classifier 将其保存为零 value contributor + `.unproven` attachment，launch gate返回`material-dynamic-uniform-script-attachment-unproven`；当前SceneScript scalar producer仅接pass-owned number且properties为空，property-vector producer仅接object-owned layer origin/scale vector3，audio-scaled producer仅接layer scale vector3或particle rate scalar，均不产pass-owned`.effectConstant` vector2。严格static identity source可由`source-proven-graph-input-stage-uniform-passthrough / generic-only`执行，但真实`2067939514`的25项scale全带上述binding，static identity实例为0。旧`.transform` pair leaf只复制previous-current、不执行数学，现已连同planner/plan/profile/backend/renderer删除；7个active stage在fresh App中均以typed reason进入共享effect-local previous-current，首帧/next-frame exact publication、GPU与terminal compositor继续，dedicated为0。**偏差债务**仍是在V2/V4 producer边界闭合vector2 identity/generation/epoch/publication与Program消费；owner删除没有把fallback升级成Transform执行。详见[E-V1-TRANSFORM-TYPED-VECTOR2-PRODUCER-BLOCKER](runtime-evidence-index.md#e-v1-transform-typed-vector2-producer-blocker)。该证据只证明重复identity-copy owner撤权与失败半径，不证明dynamic scale、Transform数学、独立ROI/fidelity或官方parity。

| 语义项 | 等级 | IR / host carrier | generic runtime | strict executor | 代码/测试证据 | 执行边界 | 下一门 |
|---|---|---|---|---|---|---|---|
| startup-inactive safe nonpersistent FBO graph 的 exact bool visibility admission | `L3 bounded / S4 V1-A correctness` | startup planner消费完整校验的effect-local direct-bool view；同key全部sibling必须为bool `effectVisibility`、具bool authored fallback且非rebuild。共享topology只接受exact单effect、同effect-scoped nonempty FBO、nonunique/nonpersistent、无blocker/clear/condition/function/compose，所有target读由先前作者写支配，material ordinal递增，copy/swap authored storage descriptor不因`1x1` probe误等价，全部target被初始化/消费且末node读取FBO后写effect output | inactive不建立假target/history或第二Program，而由GraphExecutor发布effect-entry previous-current；live true后同一authored graph才建立既有GraphTargets并执行MaterialProgram。persistent/unique/history/read-before-write、descriptor mismatch、dependency/provider、mixed/nonbool/rebuild与无Program consumer继续关闭 | 真实`3211615441`以`blur=false`启动：两条Blur FBO和两条VHS suffix共4 target均`property-inactive / admitted-generic / complete`，frame0/1 activation passthrough；live `blur=true`在同一surface/window accepted，frame63两Blur各4 authored / 4 material / 0 rejected并发布`321x512`与`306x512` RGBA双target，VHS suffix继续Program与terminal compositor，frame64 next-frame继续。全景23 generic / 0 dedicated / 0 fallback，GraphExecutor 246/246/246、0 failure，78/78 frame完成 | [shared topology](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/GraphTargets/SceneGraphRenderTargetPlan+Extent.swift)、[authored planner](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/AuthoredGraph/SceneAuthoredEffectRenderPlanner.swift)、[runtime input](../../../MyWallpaperX/Core/SteamWorkshopScene/Runtime/SceneRuntimeInput.swift)、[planner gate](../../../script/tests/test_scene_effect_render_graph.py)、[runtime input gate](../../../script/tests/test_scene_runtime_input.py)、[E-V1-STARTUP-INACTIVE-SAFE-FBO](runtime-evidence-index.md#e-v1-startup-inactive-safe-fbo) | 只证明bounded startup activation与previous-current安全，不新增owner迁移，也不外推任意FBO/command、独立Blur/VHS ROI/fidelity、Fast/fixed/full、整个V1或V2。V1-A完成来自本行与既有专项原子的合取，未登记shape继续失败关闭 | 转入V1-B逐profile审计与撤权；typed script attachment留在V2/V4 |
| already-admitted nonpersistent FBO graph 的 typed bool stage activation | `L3 bounded / S4 V1-A correctness + exact owner-migration` | 不新准入graph；只在单effect、`dependencyOwnership == .none`、无clear function、sole bool user-property visibility与authored fallback成立时，复用既有FBO visual-failure predicate。target必须nonunique/nonpersistent且无clear/condition，node无condition/compose，FBO读由先前同effect写支配，target全集被消费并只有一个terminal full-frame output。active owner转移另要求完整校验的target-local direct-bool view与ordinary-root topology gate；startup sole-key view不变 | GraphExecutor把inactive解释为typed `activation-passthrough`，原子保留effect-entry current并复用既有target/publication/completion/compositor；pointer/identity scalar仍只属于pair leaf。Standard Blur compiler只对同时通过whole-stage proof的validated active direct-bool target撤candidate；frame-driven、mixed sibling、rebuild key、utility/parent/child/dependency/provider/graph-output、startup-disabled、history/unique、clear/condition/function/compose与任意其他FBO保持关闭 | 真实`3211615441:24#effect#613`和`26#effect#257`在默认及恢复时均为Program owner、23 generic / 0 dedicated / 0 fallback；live `blur=false`后各4/0/4 typed activation reject、GPU completed、publication保持同allocation/mapping，同key VHS suffix继续terminal compositor。profile-local disable只使两项effect-local previous-current且仍0 dedicated；默认与恢复各1/1 PASS，rollback按主链要求诚实NON-PASS | [property binding program](../../../MyWallpaperX/Core/SteamWorkshopScene/Properties/ScenePropertyBindingProgram.swift)、[route admission](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/EffectExecution/SceneResolvedMaterialExecutionCapabilityAdmission.swift)、[compile input](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/EffectCompilation/SceneEffectStageCompileModel.swift)、[stage activation policy](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/EffectExecution/SceneResolvedMaterialExecutionCapability+Stages.swift)、[FBO topology predicate](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/EffectExecution/SceneResolvedMaterialVisualFailureTopology.swift)、[GPU gate](../../../script/tests/test_scene_resolved_material_fbo_stage_activation.py)、[E-V1-FBO-STAGE-ACTIVATION](runtime-evidence-index.md#e-v1-fbo-stage-activation)、[E-V1-STANDARD-BLUR-DIRECT-BOOL-VISIBILITY-OWNER](runtime-evidence-index.md#e-v1-standard-blur-direct-bool-visibility-owner) | 两个exact Blur identity达到bounded `generic-only / owner-migration`；runtime无compiler-preview telemetry，candidate撤权由compiler guard和可执行partition门证明。不是整个Standard Blur family、任意FBO visibility、独立Blur ROI/fidelity、V1-A/V1完成或V2切换 | V1-A已由后继startup-inactive safe FBO原子完成；本行边界冻结有效，现役转入V1-B，脚本附件留给V2/V4且不静态化 |
| startup-false current-pair leaf 的 exact bool visibility admission | `L3 bounded / S4 V1-A correctness；exact X-Ray owner-migration exception` | binding program只输出全局sole valid、bool instruction/definition/authored fallback、非rebuild的exact target；planner只保留ordinary image/solid/text、无parent/child/utility/cross-layer provider-consumer、无logical target/blocker、单material node、exact effect input/output pair、无command/condition/compose的authored-false effect，并重建其active sibling chain | Program-backed stage取得同一activation policy后可frame-local执行；Program不可用或缺同target policy时只形成显式`.initiallyInactivePassthrough` member，经activation copy保留previous-current，不算visual failure/fallback、不形成CPU exact owner，也不复活dedicated owner。无Program stage没有live consumer | 旧correctness cohort真实`3767460992`为1/1 PASS；5项startup-false safe leaf守恒登记（2 Program-backed / 3 inactive-only），本身不构成owner migration。现役后继受控`9000000131:69#effect#131`另以default/disable/recovery三路闭合exact current-stock X-Ray startup lifecycle candidate撤权、0 dedicated及GPU/publication/compositor/next-frame | [binding target](../../../MyWallpaperX/Core/SteamWorkshopScene/Properties/ScenePropertyBindingProgram.swift)、[authored planner](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/AuthoredGraph/SceneAuthoredEffectRenderPlanner.swift)、[Program-first stages](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/EffectExecution/SceneResolvedMaterialExecutionCapability+ProgramFirstStages.swift)、[planner gate](../../../script/tests/test_scene_effect_render_graph.py)、[capability gate](../../../script/tests/test_scene_resolved_material_execution_capability.py)、[E-V1-STARTUP-FALSE-EFFECT-VISIBILITY](runtime-evidence-index.md#e-v1-startup-false-effect-visibility)、[E-V1-XRAY-STARTUP-FALSE-SCALAR-OWNER](runtime-evidence-index.md#e-v1-xray-startup-false-scalar-owner) | 不开放startup-false FBO、target/history/dependency/command、Timeline/SceneScript、mixed producer、utility/parent/child/named-provider/multi-effect或无Program live activation；旧correctness cohort不是owner migration，唯一现役例外是上述exact current-stock X-Ray cohort；不证明Film Grain ROI/fidelity、V1完成或V2切换 | V1-A已由后继startup-inactive safe FBO原子完成；现役转入V1-B，无Program producer/VM能力按V2/V4闭合且不以inactive passthrough冒充执行 |
| texture sample host transform ABI | `L3 bounded / S4 project fixture` | 每个active reflected texture slot固定两项canonical `float4` host field，原子表示candidate `origin/xAxis/yAxis`；字段名为reserved internal ABI，authored同名声明在bounded/generic frontend都typed拒绝。identity transform保持static像素等价，mapped/padded static与已解析single-image animated atlas共享同一candidate UV合同 | Program finalizer/uniform encoder每帧写入exact bytes/identity，transform值变化不重编Metal pipeline；boundedSwift emitter与generic normalizer/worker artifact都对vertex/fragment、direct/helper及已支持LOD的每次`texSample2D/texture2D`计算`origin + xAxis*u + yAxis*v`。worker按active texture binding复核每槽两项float4并跨stage合成stage=nil；Swift artifact decoder继续强制missing/malformed/stage drift拒绝，cache request namespace为v5 | 项目自有static mapped/padded及synthetic atlas两backend Metal color/ROI门已闭合；Developer ID App正/恢复均进入actual Program、GraphExecutor GPU/publication/compositor/next-frame并形成四个预登记ROI簇。invalid animated frame metadata只在ordinary single effect边界typed previous-current，aggregate仍为NON-PASS；真实Fluid仍0 CPU/Program/GPU | [bounded ABI](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderFrontend/SceneAuthoredShaderTextureTransformABI.swift)、[generic sampling normalizer](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderPreparation/SceneGenericShaderTextureSamplingNormalizer.swift)、[uniform encoder](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/MaterialProgram/SceneResolvedMaterialUniformEncoder.swift)、[worker](../../../script/scene_shader_compiler_artifact.py)、[texture transform gate](../../../script/tests/test_scene_material_texture_transform.py)、[real compiler gate](../../../script/tests/test_scene_generic_shader_texture_transform_compiler.py)、[E-V1-SINGLE-IMAGE-ANIMATED-MATERIAL-ATLAS](runtime-evidence-index.md#e-v1-single-image-animated-material-atlas) | 只开放通用UV correctness与single-image finite axis-aligned/in-bounds frame shape；rotated/trimmed/fractional/out-of-range、multi-image material atlas、dynamic replacement与官方timing/pixel parity仍缺。identity/purpose/publication/ABI/generation/stale/target/OOM等完整性错误不soften | 后继只按新的通用frame shape或consumer语义扩展同一ABI；不得按provider/path/sample分支，不新建第二registry/clock/renderer |
| GLSL-like source | `L3 bounded` | ShaderContract 安全保留完整 UTF-8 source、raw SHA-256、stage path/kind 与 canonical identity | frontend 对静态有界控制流和受支持 stage/uniform 子集做 typed link、翻译；preparation、reflection、texture/provider/state/color 必须原子形成 Program 后才可准入 | bounded Program 由 resolved-material pass encoder 编译并在统一 graph executor 执行；旧独立 authored-shader 与 exact Scroll owner 已撤权，Blur 等尚未迁移的 strict profile 仍调用手写 MSL | [contract](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderContract/SceneShaderContract.swift)、[frontend](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderFrontend/SceneAuthoredShaderFrontend.swift)、[preparation](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderPreparation/SceneAuthoredShaderPreparation.swift)、[pass encoder](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/EffectExecution/SceneResolvedMaterialPassEncoder.swift)、[frontend tests](../../../script/tests/test_scene_authored_shader_frontend.py)、[resolved execution tests](../../../script/tests/test_scene_resolved_material_execution_capability.py)、[E-AUTHORED-SHADER](runtime-evidence-index.md#e-authored-shader) | bounded Program 子集不等于通用 GLSL；未注解 sampler、未证 helper、动态控制流、更多语法/state 与缺失 provider 继续拒绝；旧 GPU 门只是历史 oracle | V0 让上游通用 backend 复用现有 source/Program ABI；Swift frontend 降为 bounded fallback、diagnostic/oracle，不再逐语义扩成完整语言 |
| HLSL / DirectX blob | `L1` | 能发现 `blobssm/*.dxs` 并报告数量 | 无 DXIL/DXBC 反射、HLSL frontend 或 Metal translation | 无 strict HLSL executor | [blob 分类](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/SceneResourceIndex.swift)、[未实现诊断](../../../MyWallpaperX/Core/SteamWorkshopScene/Runtime/SceneDiagnostics.swift) | 不运行或捆绑不可信 `dxc`；blob 存在只算诊断 | 先确定合法 source 优先级和 blob metadata；任何编译器路线另做签名、许可、缓存和 sandbox 设计 |
| preprocessor | `L3 bounded module slice / S4 project fixture` | directive、条件表达式、object/function macro、include edge、active annotation/declaration、source map与typed module dependency均由同一State承载；module provenance保存identity、stage、作者path/line、ordinal、directive-position规范化macro digest与outcome | 有界展开形成file dependency与独立module dependency digest；exact `LightingV1 + typed LIGHTING=0`零source贡献但identity进入prepared/cache/Program。object/function macro表排序归一，不受遍历偶然顺序影响；任一语义值变化必须改digest | 普通Program `9000000930`正门已让module-bearing Program进入GPU/publication/compositor/next-frame；LIGHTING=1在authored preparation关闭，outer previous-current GPU单列；真实Fluid combine standalone preparation accepted但整体Fluid仍零authored GPU | [preprocessor](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderPreparation/SceneShaderPreprocessor.swift)、[module resolution](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderPreparation/SceneShaderVariantEnvironment+ModuleResolution.swift)、[module tests](../../../script/tests/test_scene_shader_module_resolution.py)、[E-V1-LIGHTINGV1-ZERO-SOURCE-REQUIRE](runtime-evidence-index.md#e-v1-lightingv1-zero-source-require) | 不生成stub/source line/官方shader；active missing/nonzero/noninteger/unknown/case/syntax均typed reject，inactive合法零resolution、malformed仍reject；combo conflict仍由更早schema gate拒绝。preparation/Program PASS不等于任意Metal/module GPU支持 | 非零ABI/source/语义、unknown module、完整Lighting、官方动态golden与Fluid graph继续保持unsupported；不从本slice扩张module registry |
| 跨语言宏 / helper translation | `L3 bounded` | source token、active helper、stage declaration与typed syntax unit可保留；frontend逐符号翻译已证 `texSample2D`/`texture2D`、`mix`、`frac`、常用数学、`mul`、`mat3 inverse`及有界构造，并解析简单value-type `inout` | 只对静态有界控制流、可唯一解析类型与已登记helper形成Metal source；浮点向量隐式缩窄支持已证乘除链、赋值/函数参数目标，以及赋给`float2/float3`时可静态证明为component-wise的`+ - * /`表达式中更宽裸vector operand；user function、array/index、未知类型与显式过宽swizzle不猜测。float `%`仅支持整句简单标量声明赋值并显式转`fmod`，其他转换、compound/vector modulo、matrix宽度与数组/畸形`inout`失败关闭 | Film Grain、`3766387484`九段链、layer 80 Light Shafts与`3299228616:151` relocated Audio Bars已执行authored helper/source；未迁移strict MSL仍绕过此能力 | [frontend](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderFrontend/SceneAuthoredShaderFrontend.swift)、[function semantics](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderFrontend/SceneAuthoredShaderFunctionSemantics.swift)、[vector conversion](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderFrontend/SceneAuthoredShaderVectorConversion.swift)、[frontend tests](../../../script/tests/test_scene_authored_shader_frontend.py)、[E-R4-3-AUTHORED-PROGRAM-CHAIN](runtime-evidence-index.md#e-r4-3-authored-program-chain)、[E-WORKSHOP-SIMPLE-AUDIO-BARS](runtime-evidence-index.md#e-workshop-simple-audio-bars) | bounded符号集合与少量HLSL转换形态不等于通用GLSL/HLSL兼容；歧义类型、动态/递归控制流和未知helper关闭 | 语言解析、类型与翻译移交 V0 通用 backend；Swift 只保留小型 dialect normalization、host prelude、诊断和已有正反 oracle，不按 effect/path 扩 translator |
| `combo` / permutation | `L2；Opacity/Light Shafts/Standard Blur bounded L3` | material/instance explicit值、exact active `[COMBO]` default/options与sampler readiness/format schema形成带provenance的resolution；authored `require/requireany`只保真为editor metadata | normalized variant key、active source/schema与resource bounded fixed-point已有；同一resource snapshot以最终readiness重算并把active variant/reflection写进Program identity。Template保持explicit-only；schema seed通常只消费根级无条件metadata，唯一source-proven例外是一个sampler声明及其texture-readiness annotation同处单层根级正向`#if NAME`、`#if NAME == 1`或`#if 1 == NAME`，且combo同名、无requirements/require-any、嵌套/`elif`/`else`；它只让readiness进入fixed-point，真实slot状态仍选择最终分支。resolver再验证全部active同名声明，显式值优先，否则要求每条声明都有同一个有效default，再把最终值同时供给条件、definedness、普通code与variant/prepared identity。missing-needed、冲突、畸形、options不匹配均失败关闭 | 只有其余Program合同同时闭合的bounded variant可进入resolved executor；V0 exact source-key backend 已让真实 Opacity mask-present/texture-absent 两种 prepared variant 都进入 generic Program，absent variant 不绑定或伪造死 slot，二者各有 distinct ROI 与坏 artifact 回滚。Light Shafts及既有bounded Opacity也消费同一Program能力；Standard Blur 的 exact KERNEL0 whole-stage 当前同时覆盖 MASK=0 与 MASK=1：MASK=1 只接受 distinct direct-red sample、typed `.opacityMask`、静态 authored asset candidate，并把同代候选作为普通 Program texture atom；scale可以是静态值或严格equal-lane user-property scalar。Pulse static snapshot-carrier另以该self-gated readiness例外闭合mask absent/present两个variant。pending/missing/wrong-purpose/动态texture候选、KERNEL1/2、其他dynamic producer/vector shape、nonunit与其他variant仍由strict remainder或fail-closed边界处理 | [material resolver](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredMaterialResolver.swift)、[variant resolver](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderPreparation/SceneShaderVariantResolver.swift)、[preprocessor tests](../../../script/tests/test_scene_shader_preprocessor.py)、[E-V0-GENERIC-SHADER-PROGRAM-SLICE](runtime-evidence-index.md#e-v0-generic-shader-program-slice)、[E-SHADER-PREPARATION](runtime-evidence-index.md#e-shader-preparation)、[E-MATERIAL-PROGRAM](runtime-evidence-index.md#e-material-program)、[E-V1-STANDARD-BLUR-MASKED-OWNER](runtime-evidence-index.md#e-v1-standard-blur-masked-owner)、[E-V1-STANDARD-BLUR-USER-SCALAR-SPLAT-OWNER](runtime-evidence-index.md#e-v1-standard-blur-user-scalar-splat-owner)、[E-V1-PULSE-STATIC-TYPED-DATA-OWNER](runtime-evidence-index.md#e-v1-pulse-static-typed-data-owner) | 两种Opacity variant、Standard Blur的exact MASK=0/1 KERNEL0与Pulse static self-gated mask variants都不证明其他optional/default/combo；preparation success不等于GPU permutation，purpose缺失/歧义、host-owned define、unknown ordinary identifier或unsupported mixed chain均失败关闭 | 继续扩大经证实的variant/provider/state子集，并在默认route、fallback metric和回滚闭合后逐项撤销旧owner |
| shader annotation | `L2 bounded schema slice` | marker、行号、raw/structured JSON与exact numeric保真；exact combo/default/options、sampler readiness/resource requirement、公开`opacitymask/rgbmask/flowmask` mode及`material` lookup key进入active schema | 只让active非directive行参与Program schema；Template仍只保存material显式值，annotation default在下游compile environment生效。`material`只是key，不是purpose，numeric `format`不证明normal/depth；authored `require/requireany`属于editor关系，不删除player声明。active/reachable regular sampler若自身无purpose，可由typed asset default为同slot的authored asset override建立purpose；candidate已登记冲突role时拒绝，untyped/internal/inactive/unreachable不继承。default只提供metadata，不能替代pending/missing/unavailable/wrong-publication/generation的author candidate，exact demand/publication/selection与Program identity继续属于override。逐项登记的stock语义仍由registry决定，不按文件名或样本放宽 | Film Grain stock/no-mask与当前Light Shafts正例已让各自exact default进入Program；项目自有static unregistered override已完成signed App正→untyped-default反→byte-identical恢复，真实Advanced Fluid node 17 slot 3不再因purpose拒绝 | [contract loader](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderContract/SceneShaderContractLoader.swift)、[material schema](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/MaterialProgram/SceneResolvedMaterialShaderSchema.swift)、[purpose resolver](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/MaterialProgram/SceneResolvedMaterialShaderSchema+SamplerPurpose.swift)、[stock registry](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/SceneStockTextureSemanticRegistry.swift)、[sampler-purpose gate](../../../script/tests/test_scene_sampler_default_purpose.py)、[E-V1-SAMPLER-DEFAULT-PURPOSE](runtime-evidence-index.md#e-v1-sampler-default-purpose) | typed slice不是完整editor/runtime schema；conflicting/untyped default继续关闭，不开放filename/path pattern、default runtime fallback、animated loader/lifecycle或arbitrary purpose | 新增 mode/default/stock identity 必须进入完整 Program schema，并有公开或 clean-room 语义与正反门；旧 R4 标签不再决定顺序 |
| uniform / `constantshadervalues` | `L3 bounded；generic carrier L2` | number/vector/string/binding raw value与components保留，Template区分static、property、Timeline与已证明SceneScript value producer；`DynamicUniformScriptAttachment`把已证明value contributor与中性script attachment分开，`.unproven`明确表示该脚本仍可能产值而不是control-only；作者名与stage独立保存 | Program按active reflection编码host/static/dynamic bytes；stage ABI、host schema与producer冲突门保持。exact single-current-pair leaf可在launch遇到producer/target全可证、无script attachment且value contributor多于一个的歧义，唯一已证value contributor附带单个`.unproven` attachment，唯一value contributor且attachment为空但对应producer完全缺席，多个value contributor且attachment为空、无同key异target mismatch但一个或全部producer完全缺席，或零个已证明value contributor、authored fallback存在且attachment精确为单个`.unproven`时，做previous-current GPU copy并继续suffix；这些门不执行或分类script，不把authored fallback当作动态结果，不从多个owner中选择值，也不伪造producer。frame-time failure精确为`dynamicUniformBindingInvalid`或`staticUniformBindingInvalid`时也可走同一copy；后者只在active schema已有效但static default/explicit值不可编码时产生，不伪造static值。作者 declaration 命中 host-owned field 时精确为`hostUniformDeclarationConflict`并可走同一copy；同一reflected field同时命中alias/field authored key时精确为`uniformDeclarationConflict`并可走同一copy，不选择任一值；active schema缺席精确为`activeUniformSchemaMissing`，与declaration为空但`encodeHost`失败的`hostUniformBindingInvalid`都继续硬拒绝。launch-time schema `uniformBindingInvalid`、无authored fallback的零contributor、同property key异target、重复/混合script attachment、frame/resource/invariant及runtime encode仍硬拒绝 | bounded Program与adapter都由统一executor编码；普通/Workshop Effect Audio Bars均通过host uniform消费共享频谱，既有typed property/Timeline/SceneScript producer只在各自已证范围写入snapshot。派生真实内容分别证明多contributor时不选择值、未证明script不执行、单/多contributor的producer缺席时不发明动态值、零value contributor加script时只保留previous current与suffix、static值缺失时只跳过精确失败effect，host declaration冲突时不覆盖host值，以及alias/field declaration冲突时不选择值；分别删除authored host declaration或多余alias后恢复三个普通Program，正门与回滚截图逐像素相同。Debug隔离fault另证明一次dynamic value缺失后suffix/compositor同帧继续、下一帧恢复普通Program；route仍为`prefer-generic`且旧owner未撤权 | [host schema](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/MaterialProgram/SceneResolvedMaterialHostUniformSchema.swift)、[Program finalizer](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/MaterialProgram/SceneResolvedMaterialProgramFinalizer.swift)、[script classifier](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/MaterialProgram/SceneResolvedMaterialScriptBindingClassifier.swift)、[resolved live target](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/EffectExecution/SceneResolvedMaterialExecutionCapability.swift)、[E-V0-UNIFORM-DECLARATION-CONFLICT-FALLBACK](runtime-evidence-index.md#e-v0-uniform-declaration-conflict-fallback)、[E-V0-HOST-UNIFORM-DECLARATION-CONFLICT-FALLBACK](runtime-evidence-index.md#e-v0-host-uniform-declaration-conflict-fallback)、[E-V0-STATIC-UNIFORM-FINALIZER-FALLBACK](runtime-evidence-index.md#e-v0-static-uniform-finalizer-fallback)、[E-V0-DYNAMIC-UNIFORM-CONTRIBUTOR-PRODUCER-UNAVAILABLE](runtime-evidence-index.md#e-v0-dynamic-uniform-contributor-producer-unavailable)、[E-V0-DYNAMIC-UNIFORM-SCRIPT-ATTACHMENT-UNPROVEN](runtime-evidence-index.md#e-v0-dynamic-uniform-script-attachment-unproven)、[E-V0-DYNAMIC-UNIFORM-PRODUCER-UNAVAILABLE](runtime-evidence-index.md#e-v0-dynamic-uniform-producer-unavailable)、[E-V0-DYNAMIC-UNIFORM-CONTROL-POLICY（历史）](runtime-evidence-index.md#e-v0-dynamic-uniform-control-policy)、[E-V0-DYNAMIC-UNIFORM-CONTRIBUTOR-POLICY](runtime-evidence-index.md#e-v0-dynamic-uniform-contributor-policy)、[E-V0-DYNAMIC-UNIFORM-FRAME-FALLBACK](runtime-evidence-index.md#e-v0-dynamic-uniform-frame-fallback) | exact reflected stereo audio scalar array仅在已登记Program ABI内支持；其他uniform array、SceneScript VM、更多dynamic producer、`activeUniformSchemaMissing`、真正host input错误局部化、launch-time schema失败、无authored fallback的零contributor、同property key异target、重复/混合script attachment与其余官方built-in单位未完成；普通authored Blend不外推generic dependency | V0可见门已闭合；该sampler divergence仍hard reject，当前V1不借uniform fallback放宽graph/FBO边界。真实SceneScript value/control/lifecycle语义继续由V2 VM闭合，每个新dynamic/host来源仍须同批补schema、snapshot、ABI冲突、GPU门与被替代owner removal |
| include / header | `L3 bounded` | 独立 source graph 保存 package/loose/stock 候选、provenance、冲突、edge 与内容 hash，不改旧 raw stage projection | 有界 include expansion、dependency graph/hash、cycle/missing/conflict、source map 与 cache identity 已有；Program 可重编已证 active source | Film Grain stock/no-mask 正例已让 `common.h`/`common_blending.h` 的 active source/helper 经 frontend、Program 与统一 executor 编码；未知 include/helper、其他 revision 和未证 state/resource/color仍拒绝 | [source graph](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderContract/SceneShaderSourceGraph.swift)、[source resolver](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/SceneShaderSourceResolver.swift)、[preprocessor](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderPreparation/SceneShaderPreprocessor.swift)、[E-SHADER-PREPARATION](runtime-evidence-index.md#e-shader-preparation)、[E-MATERIAL-PROGRAM](runtime-evidence-index.md#e-material-program)、[E-EFFECT-FILM-GRAIN](runtime-evidence-index.md#e-effect-film-grain) | package -> loose -> stock 是项目 VFS policy；一个 exact Program 正例不等于任意 helper translation 或 GPU 支持 | 新 header/helper 先进入通用 backend 的 VFS/prelude/diagnostic 路线；旧 projection/adapter 已完成的 R5 历史不再作为后续前置 |
| vertex attribute | `L3 bounded 2D quad` | authored attribute declaration、type/name/array shape、stage与行号已保存 | frontend把已证`a_Position`/`a_TexCoord` image-effect layout链接到统一full-target quad；实际引用`g_ModelViewProjectionMatrix`的source取得target-pixel position，不引用时取得NDC/clip-space，generic normalizer与bounded Metal wrapper使用同一source-driven合同；其他attribute拒绝 | `3766387484`九段MVP链与`3749463715`派生direct-clip链都由Program执行；4x3不同像素GPU门证明direct-clip没有坍缩或重排 | [contract loader](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderContract/SceneShaderContractLoader.swift)、[bounded Metal source](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderFrontend/SceneAuthoredShaderMetalSource.swift)、[generic normalizer](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderPreparation/SceneGenericShaderSourceNormalizer.swift)、[E-V1-MATERIAL-COPY-HISTORY](runtime-evidence-index.md#e-v1-material-copy-history) | 仅证明2D effect quad的两种position contract，不开放normal/tangent/color、多texcoord、3D/particle/custom layout或官方矩阵数值parity | 按对象draw packet逐类注册layout并补stage-link/GPU方向门 |
| varying / fragment output | `L3 bounded` | authored varying与`gl_FragColor` type/name/stage被typed frontend链接；fragment output另保存`.redDefined/.unproven`事实 | scalar fact只接受active `main`中root-level、无条件、唯一whole `gl_FragColor`写入，并进入artifact、Program与identity；它不授予RGBA color contract | source-only、Finalizer、PassEncoder与GraphExecutor GPU门闭合scalar writer正例；conditional、multiple、component-only、early-return、discard与helper-output不授权scalar attachment | [fragment output analyzer](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderFrontend/SceneAuthoredShaderFragmentOutputAnalyzer.swift)、[frontend tests](../../../script/tests/test_scene_shader_color_contract.py)、[E-V1-FBO-UV-ADDRESS-REPEAT](runtime-evidence-index.md#e-v1-fbo-uv-address-repeat) | 只证明唯一whole-output bounded shape；component/path-complete CFG、MRT、未知颜色空间与官方pixel parity仍缺 | 新writer形态先建立公共CFG/dataflow证明并保持RGBA反门 |
| raw RG8 scalar-splat output with typed scalar input | `L3 bounded / S4 owner-migration` | `.redGreenUnorm`形成独立`red-green-unorm` request/output ABI；prepared fragment只在`main`中唯一、无条件写`CAST4/vec4/float4(scalar)`时获准，scalar只能是有限字面量或当前`main`的local float。active输入必须为空、物理R8/R16F，或sampler schema明确为`opacityMask`/`depth` | compiler artifact保持raw MSL，不走color premultiply/transfer lowering，并发布`red-green-unorm-data`与`redDefined`；request key隔离color/raw artifact。route拒绝external provider、graph input/target、普通未知格式、RG/彩色输入与helper/间接输出；缺省`generic-only`，显式disable才允许shared bounded rollback | 项目正反门覆盖constant/local scalar、间接输出、vector输出、未知semantic、request-key分离与typed sampler/physical format。真实资源派生isolated Bokeh node1的MODE=0 mask/no-mask variant默认/恢复均由`genericCompilerArtifact`执行，七节点effect为7 authored / 7 material / 0 rejected并完成RG88 target、GPU、publication、suffix、唯一compositor与next-frame；坏exact artifact得到7 rejected previous-current而suffix继续；disable以boundedSwift 7/7通过 | [scalar-splat analyzer](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderFrontend/SceneAuthoredShaderColorTransferAnalyzer+Syntax.swift)、[typed sampler purpose](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/MaterialProgram/SceneResolvedMaterialShaderSchema+SamplerPurpose.swift)、[output ABI](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/MaterialProgram/SceneResolvedMaterialGenericShaderProgramArtifact.swift)、[artifact builder](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderPreparation/SceneGenericShaderArtifactBuilder.swift)、[E-V1-RED-GREEN-UNORM-SCALAR-SPLAT-OWNER](runtime-evidence-index.md#e-v1-red-green-unorm-scalar-splat-owner) | 不开放MODE=1的`(-depth,+depth)`、RG16F、任意vector/MRT/双通道数学、provider/graph输入、Bokeh独立ROI/fidelity、手部/剑光、完整样本/Fast/fixed/full或官方parity | 当前source/typed-input profile已完成owner migration；下一cohort继续从剩余V1-B owner debt选择，不按效果名扩张 |
| shader input/output color representation | `L3 bounded resolved execution` | carrier可表达`opaque/straight-alpha/premultiplied-alpha`、有序multi-slot scalar interpolation与unresolved；公共analyzer接受已证明的单源direct sample/alpha改写、同槽channel reconstruction、source-proven conditional union，以及由direct sampled color leaf和scalar `mix`组成的bounded graph。prepared authored source能证明时，该analyzer是generic与bounded frontend共享的唯一material color fact | V0 whole-color attenuation与same-slot边界不变；`interpolatedColor`要求至少两个有序唯一slot，artifact producer/consumer都要求全部slot已绑定，consumer拒绝与源码事实矛盾的artifact。无外部provider texture的source-proven interpolation已撤销bounded产品owner；graph-input straight-alpha与straight-alpha-preserving宽profile当前为`prefer-generic`；single-sampler alpha mutation与same-slot channel reconstruction已分别拆为`generic-only`窄profile；auxiliary RGB blend alpha-preserving另拆为`generic-only`窄profile；conditional straight-union已拆为`generic-only`并撤销旧Shadow owner；active sampler全集恰为唯一regular graph input与唯一typed defaulted opacity mask auxiliary、且具双stage active uniform的straight-alpha-preserving子集另拆为`generic-only`并撤销旧Shift Hue owner；active sampler全集精确为唯一regular graph input、无auxiliary且具双stage active uniform的straight-alpha-preserving子集也拆为`generic-only`并撤销旧Gradient owner；同一graph/sampler/stage-uniform边界上另具完整stereo audio数组与安全局部化mutable fragment varying的straight-alpha子集已拆为`generic-only`并撤销旧Enhanced Audio Bars owner。两个宽泛profile成功artifact优先，失败按typed reason保留shared bounded/dedicated fallback。composed preserving只接受同一graph slot sample与明确sampled alpha；conditional union要求两个同slot sample、两个whole-color fallback、完整RGB+alpha branch write与唯一terminal return。vector weight、额外alpha/whole-vector use、RGB write、混用slot、重复/缺失声明或representation mismatch均失败关闭 | Opacity保留既有GPU/compositor/ROI证据；Fisheye-like真实`3765760121:96`与隔离派生Chromatic-like`9000000622:112`分别命中两个窄profile，并闭合默认artifact、坏artifact局部previous-current、profile-local rollback、GPU/publication/compositor/next-frame；Motion Blur迁移node保持`generic-only / genericCompilerArtifact`。当前`3767460992`中Color Key的RGB-read+单次alpha attenuation、Hue Shift的typed `mix` narrowing、Workshop Shadow conditional union、两处Shift Hue stage与Workshop Gradient当前stage与Enhanced Audio Bars均由generic artifact接受，26个active effect现为26 shared Program / 0 dedicated。single-sampler alpha mutation、same-slot channel reconstruction、auxiliary RGB blend alpha-preserving、Shift Hue、Workshop Gradient与Enhanced Audio Bars窄profile已owner migration，但不证明两个宽泛graph-input alpha profile已owner migration | [product artifact builder](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderPreparation/SceneGenericShaderArtifactBuilder.swift)、[preserving lowering](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderPreparation/SceneGenericShaderStraightAlphaPreservingLowering.swift)、[generic normalizer](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderPreparation/SceneGenericShaderSourceNormalizer.swift)、[color mix analyzer](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderFrontend/SceneAuthoredShaderColorMixGraphAnalyzer.swift)、[E-V1-GENERIC-SCALAR-INTERPOLATION](runtime-evidence-index.md#e-v1-generic-scalar-interpolation)、[E-V1-NARROW-GRAPH-INPUT-ALPHA-OWNERS](runtime-evidence-index.md#e-v1-narrow-graph-input-alpha-owners)、[E-V1-AUXILIARY-RGB-BLEND-OWNER](runtime-evidence-index.md#e-v1-auxiliary-rgb-blend-owner)、[E-V1-ORDINARY-SHADER-REGRESSION-RECOVERY](runtime-evidence-index.md#e-v1-ordinary-shader-regression-recovery)、[E-EFFECT-WORKSHOP-SHIFT-HUE](runtime-evidence-index.md#e-effect-workshop-shift-hue)、[E-EFFECT-WORKSHOP-GRADIENT](runtime-evidence-index.md#e-effect-workshop-gradient)、[E-EFFECT-WORKSHOP-ENHANCED-AUDIO-BARS](runtime-evidence-index.md#e-effect-workshop-enhanced-audio-bars) | 不证明任意shader alpha/RGB数学、任意conditional/component mutation、linear/sRGB或Windows pixel parity；两个宽泛graph-input alpha profile仍缺完整language envelope与owner迁移；Fisheye-like与Chromatic-like窄profile不外推其他sampler/slot/provider/control-flow/alpha-write形态、原始active Chromatic、独立ROI或整个family；Shift Hue、Gradient与Enhanced窄profile不外推其他mask/schema/revision、任意array/loop/audio shader/varying mutation | 继续按资源语义与作者源码事实组成共享cohort；未证形态保留可观察fallback，不外推整个shader family |
| preserved-RGBA data state transform | `L3 bounded / S4 owner-migration` | prepared authored fragment必须证明definite whole `vec4` output；attachment为`.preservedRGBAUnorm`，active sampler恰有一个regular graph target，且无external/其他auxiliary，或只多一个typed opacity mask。source fact从loss-preserving authored source取得，不依赖normalizer改写后的output符号 | `source-proven-preserved-rgba-state-transform`缺省`generic-only`，compiler artifact保持raw RGBA data、不运行color transfer；MaterialProgram/PassEncoder继续使用`.data / preservedChannels` publication与RGBA write mask。显式`disable-generic`才回滚shared bounded frontend；坏artifact撤销最小effect。首帧feedback失败另用typed discarded-history effect set保留submission pin而不发布history pin，missing/extra/conflicting token与伪造状态硬拒绝 | 原始`3767343314:17#effect#234`两个state Program及painted-mask readiness variant在恢复门全部为`genericCompilerArtifact`，终段仍为既有generic-only profile；disable与恢复均1/1 PASS。坏node3 artifact严格NON-PASS，但151/150 frame、0 failed，Sharpen/Water Ripple前缀、GPU completion、terminal compositor、next-frame及非黑动态画面继续 | [artifact builder](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderPreparation/SceneGenericShaderArtifactBuilder.swift)、[route profile](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/MaterialProgram/SceneResolvedMaterialGenericShaderRouteProfile.swift)、[typed history disposition](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/GraphTargets/SceneOffscreenTextureResidency.swift)、[E-V1-PRESERVED-RGBA-STATE-TRANSFORM-OWNER](runtime-evidence-index.md#e-v1-preserved-rgba-state-transform-owner) | 不开放任意RGBA data source/control-flow、多graph target、其他auxiliary/provider、任意feedback topology、Cursor独立点击/collision/传播/衰减ROI、官方fidelity或整个family | 继续从source/typed sampler/output事实扩展相邻data Program；不得按Cursor/effect/path身份增加profile，V1仍按剩余owner debt推进 |
| alpha-weighted same-slot sample average | `L3 bounded / S4 owner-migration` | 单fragment `main`、无control flow、同slot `1...16`次direct sample；每次严格按sample alpha累积颜色与weight，RGB只除`max(positive epsilon, weight)`，terminal alpha只除同一sample count。prepared source仍是loss-preserving truth | 公共color analyzer登记`.straightAlpha(slot)`；bounded frontend与generic artifact都在每个sample进入作者数学前unpremultiply，并只在terminal premultiply一次。generic lowering另外验证固定SPIRV-Cross accumulator/copy/component-write shape；count、hidden sample、weight、normalization、compiler drift或helper collision不一致即不发布artifact | 项目自有单Program包曾以真实Bokeh第一downsample双stage source闭合source/GPU能力，分母`4→3`反例只回退当前effect；该历史运行当时为`prefer-generic`。现役fresh Standard Blur与真实资源派生isolated Bokeh的default/recovery均命中`generic-only / genericCompilerArtifact`并到达GPU/publication/compositor/next-frame；显式disable只把alpha node切到shared bounded frontend，坏artifact撤销相关effect且不恢复dedicated owner | [source analyzer](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderFrontend/SceneAuthoredShaderAlphaWeightedSampleAverageAnalyzer.swift)、[generic lowering](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderPreparation/SceneGenericShaderStraightAlphaPreservingLowering.swift)、[artifact gate](../../../script/tests/test_scene_generic_shader_program_artifact.py)、[E-V1-ALPHA-WEIGHTED-SAMPLE-AVERAGE](runtime-evidence-index.md#e-v1-alpha-weighted-sample-average) [E-V1-ALPHA-WEIGHTED-SAMPLE-AVERAGE-OWNER](runtime-evidence-index.md#e-v1-alpha-weighted-sample-average-owner) | 不开放任意weighted filter/control flow、多slot、其他weight/normalization、Bokeh独立ROI/fidelity、整个Bokeh/Standard Blur family、Fast/fixed/full或官方parity | 继续按source-derived正反协议闭合新的weighted shape；当前profile为`generic-only`；默认坏artifact只撤销最小effect，显式`disable-generic`才使用shared bounded frontend。Bokeh nodes2–6现由后继preserved-alpha窄profile迁走，node1也由独立raw RG8 scalar-splat profile完成`generic-only`迁移；Standard Blur remainder仍按自身owner/route处理 |
| preserved-alpha RGB filter with typed data slot | `L3 bounded / S4 owner-migration` | 唯一source carrier可whole/RGB采样；可有一个distinct whole-color base slot和一个distinct `.rg/.xy` data slot；direct form保留exact whole-carrier `ApplyBlending` reconstruction，helper form只准direct `if` block中的`vec4(helper(...), carrier.a)`与reachable pure helper closure中的同source RGB sample。固定循环只在编译期可证明边界内开放 | source analyzer按slot记录whole/RGB/data sample投影；helper closure拒绝`out/inout`、global/output write、`discard`、image/atomic/barrier副作用、未登记resource call、额外slot/sample与多return；bounded Metal只对color slots解预乘，data slot不转换；generic lowering以括号配平复核nested compiler sample projection并只premultiply terminal。route另要求fact的source与color/data slot全集精确等于active graph-input slot全集；命中`source-proven-graph-input-preserved-alpha-rgb-filter / generic-only / fallbackOwner=none` | 项目自有direct四Program/FBO链保持4/4正门/恢复；真实资源派生isolated layer 17的Bokeh node0为alpha-weighted generic、node1由后继raw RG8 scalar-splat profile执行、nodes2–6为本窄profile generic artifact，完整effect为7 authored/7 material/0 rejected并让cloudmotion/tint suffix到GPU/publication/唯一compositor/next-frame。五个exact artifact均被机械损坏时首个失败即撤销整个Bokeh effect；显式disable得到相同previous-current结构结果；清除override后nodes2–6恢复相同route/backend | [direct source analyzer](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderFrontend/SceneAuthoredShaderPreservedAlphaRGBFilterAnalyzer.swift)、[helper source analyzer](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderFrontend/SceneAuthoredShaderPreservedAlphaRGBHelperFilterAnalyzer.swift)、[route profile](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/MaterialProgram/SceneResolvedMaterialGenericShaderRouteProfile.swift)、[generic lowering](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderPreparation/SceneGenericShaderStraightAlphaPreservingLowering.swift)、[loop evaluator](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderPreparation/SceneGenericShaderBoundedLoopWork.swift)、[E-V1-PRESERVED-ALPHA-RGB-FILTER](runtime-evidence-index.md#e-v1-preserved-alpha-rgb-filter)、[E-V1-PRESERVED-ALPHA-RGB-FILTER-OWNER](runtime-evidence-index.md#e-v1-preserved-alpha-rgb-filter-owner) | 不开放任意helper/filter/dataflow/control-flow、任意data swizzle、多base/data slot；raw RG8 node1由独立后继profile覆盖，仍不属于本行；Bokeh独立ROI/fidelity、原始完整样本PASS、Fast/fixed/full或官方parity仍未闭合 | 当前窄profile已完成owner迁移；宽`source-proven-graph-input-straight-alpha-preserving`仍为`prefer-generic`。不得从nodes2–6外推整个Bokeh family或新增效果名专用实现 |
| Standard Blur static / live-scalar / authored-fallback-scalar / exact Timeline-vector2 source shapes 与 shared profile owners | `L3 bounded / S4 bounded profile owner-migration` | 产品授权仍要求唯一effect、exact KERNEL0 4 material / 2 quarter-target、完整source/color-transfer/target proof。scale分为exact vector2、proven authored static scalar、sole live `{user,value}` scalar、无同key/target producer的exact authored scalar fallback或exact Timeline vector2。definition-only形态要求两个Gaussian target各自唯一scalar definition、finite单值或bit-exact equal pair、exact wrapper/material fallback与active non-array vertex `float2` equal-lane default；Timeline形态保留各自唯一typed `.vector2` definition、逐lane bit-exact fallback、sole Timeline contributor、exact `animation/value` wrapper与无script。ordinary与既有childless captured-main source cohort共用同一穷尽gate；utility仍须content/kind一致且无child/dependency/authored dependency | alpha-weighted、normalized Gaussian sum与unit previous+blurred composite继续组成唯一MaterialProgram/GraphExecutor transaction，profile保持`generic-only`且terminal `fallbackOwner=none`。live/definition scalar只在equal-lane default/fallback时做`(x,x)`投影；Timeline vector2保留作者两lane。Launch把同一typed-definition集合传给dedicated admission、RuntimeCatalog和dynamic producer catalog；Program-first保留既有incumbent，definition-only target在incumbent retained时从generic fallback publication移除。mixed source、错target/type/value、duplicate、非有限/bit mismatch、额外wrapper/script或ABI漂移在撤权前关闭 | 既有static、mask、live-scalar、captured-main、direct-bool与Timeline证据保持。fresh authored-fallback `9000000999` default/recovery均**1/1 PASS**、4/4/0、0 dedicated、GPU/publication/compositor/next-frame；mixed-source `9000001002`严格**1/1 PASS**并保留dedicated；profile disable严格**0/1 NON-PASS / 9 failures**，raw log只证明4/0/4 previous-current与输出链安全 | [whole-stage admission](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneResolvedMaterialUnitPreviousBlurredCompositeOwnerAdmission.swift)、[scalar owner admission](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/EffectCompilation/SceneEffectStageStandardBlurScalarOwnerAdmission.swift)、[fallback partition](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/EffectCompilation/SceneEffectStageAuthoredFallbackOwnerPartition.swift)、[Program finalizer](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/MaterialProgram/SceneResolvedMaterialProgramFinalizer.swift)、[Timeline producer](../../../MyWallpaperX/Core/SteamWorkshopScene/Properties/SceneTimelineTargetCompiler.swift)、[E-V1-STANDARD-BLUR-AUTHORED-SCALAR-FALLBACK-OWNER](runtime-evidence-index.md#e-v1-standard-blur-authored-scalar-fallback-owner) [E-V1-STANDARD-BLUR-TIMELINE-VECTOR2-OWNER](runtime-evidence-index.md#e-v1-standard-blur-timeline-vector2-owner) | 上述exact cohorts达到bounded owner migration。mixed live/fallback、其他Timeline/SceneScript producer、visibility Timeline、mixed contributor、child/dependency/lifecycle、未运行source组合与未通过完整source/schema/resource proof的旧planner形态仍保留incumbent。KERNEL1/2、nonunit composite和更宽topology仍fail closed；不证明整个Standard Blur、专用family删除、ROI/fidelity、Fast/fixed/full或官方parity | V1-B继续按提交后当前owner/route/首断点推进，不越阶段批量升权 |
| built-in time (`g_Time`/`g_Daytime`/`g_Frametime`) | `L3 bounded` | `SceneFrameContext` 有 monotonic scene/frame time 与 wall date，官方 Variables 页定义 `g_Time` 为程序运行秒数 | Program host-uniform schema 按 exact 名称编码；未知类型/名字失败关闭 | Film Grain stock/no-mask 正例实际以 `g_Time` 驱动两组 noise UV 并经统一 executor 编码；其他两个名称和更多 owner仍未迁移 | [frame context](../../../MyWallpaperX/Core/SteamWorkshopScene/Runtime/SceneFrameContext.swift)、[clock 测试](../../../script/tests/test_scene_frame_context.py)、[E-EFFECT-FILM-GRAIN](runtime-evidence-index.md#e-effect-film-grain) | 一个时变正例不等于 `g_Time` family parity；daytime、pause/seek/reset、跨 surface 起点与 Windows 同相位时序未闭合 | 为每个名称锁单位、更新频率、pause/seek/reset 与跨 surface lifecycle，并补确定性 clock/Windows 门 |
| built-in pointer (`g_PointerPosition*`) | `L3 bounded` | frame context独立携带current/previous、primary-button与camera parallax；effect projection正逆矩阵进入同一Program host schema。`g_PointerState`只以`authored-corpus-observation`/`MyWallpaperX-current-evidence`的bounded float4接入，z为primary down，x/y/w为0 | image-effect Program按WE UV约定编码current/last，作者vertex可用inverse matrix转到effect local；Camera Parallax关闭只归零parallax，不覆盖raw pointer。fresh Cursor feedback fixed-hover运行消费current/last并闭合shared graph；点击分量未在fresh运行中置1 | Workshop Iris已有双眼方向门；现役Cursor feedback使用同一Program host输入，不再拥有独立pointer provider | [frame context](../../../MyWallpaperX/Core/SteamWorkshopScene/Runtime/SceneFrameContext.swift)、[Program host schema](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/MaterialProgram/SceneResolvedMaterialHostUniformSchema.swift)、[E-V1-CURSOR-RIPPLE-SHARED-FEEDBACK-OWNER](runtime-evidence-index.md#e-v1-cursor-ripple-shared-feedback-owner) | 当前只闭合一个image-effect projection、hover与整层安全输出；`g_PointerState`不是已登记的`official-public-contract`，其他对象、last-pointer独立时序、click ROI和Windows坐标仍未证 | 为不同layer transform/crop/rotation、previous pointer与button事件补正反方向门 |
| built-in audio spectra | `L3 bounded` | `SceneFrameContext` 携带同次 FFT 的 16/32/64 left/right host-shared 快照；project `supportsaudioprocessing` 仍只作 capability 诊断，不参与采集判定 | R4 host schema/finalizer/uniform encoder/pass encoder 已按精确 `g_AudioSpectrum{16|32|64}{Left|Right}[N]` 名称与 float 数组形状编码，未知名/类型/长度/跨 stage 冲突 fail closed | `3122339805:64` ordinary source与`2938612768:563` composition main-target source均已形成resolved-material graph GPU/compositor/next-frame正例，并以非零fixture取得可见柱高变化；后者只接受无child/dependency的有界utility shape，其他stock/exact audio owner仍保留 | [frame context](../../../MyWallpaperX/Core/SteamWorkshopScene/Runtime/SceneFrameContext.swift)、[SceneAudioSpectrum.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Runtime/SceneAudioSpectrum.swift)、[utility preflight](../../../MyWallpaperX/Core/SteamWorkshopScene/Rendering/SceneResolvedMaterialFramePreflight.swift)、[E-R4-2-AUDIO-ARRAY](runtime-evidence-index.md#e-r4-2-audio-array) | 粒子 audio、SceneScript AudioBuffers、任意 uniform array、频段归一化、任意utility/dependency与旧 audio owner 撤权未闭合 | 继续按可静态证明的source/shape迁移旧owner；每项补GPU、compositor、next-frame与局部视觉门 |
| built-in matrices | `L3 bounded image-effect` | renderer生成MVP与effect-texture projection正逆矩阵，host schema按exact作者名/type编码；`g_ModelViewProjectionMatrixInverse`现由同代`inputs.modelViewProjection.inverse`形成typed field | Program/pass encoder对2D image-effect quad上传同代矩阵；未知名称/type/stage冲突失败关闭 | Workshop Iris与Depth authored source已消费effect projection矩阵；Chromatic visual atom实际消费inverse MVP并取得directional ROI | [matrix helpers](../../../MyWallpaperX/Core/SteamWorkshopScene/Rendering/SceneMatrix.swift)、[host schema](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/MaterialProgram/SceneResolvedMaterialHostUniformSchema.swift)、[uniform encoder](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/MaterialProgram/SceneResolvedMaterialUniformEncoder.swift)、[E-V0-CHROMATIC-AUTHORED-PROGRAM](runtime-evidence-index.md#e-v0-chromatic-authored-program)、[E-R4-3-AUTHORED-PROGRAM-CHAIN](runtime-evidence-index.md#e-r4-3-authored-program-chain) | 只闭合当前2D image-effect输入；3D、非轴对齐与Windows转置/数值约定未系统核验 | 增加rotation/crop/fit与inverse数值GPU门，再扩对象矩阵集合 |
| built-in resolution / texel size | `L3 bounded` | frame context 有 canvas/screen；typed candidate 保存 physical/mapped/transform，共享 slot binding 提供项目内 physical/mapped resolution、两类 texel size 与 mip count carrier；官方 Variables 页定义 `g_TextureNResolution.xy` 为 physical、`.zw` 为 mapped | Program uniform encoder 可从同代 texture atom按 exact slot 名编码 resolution；未绑定/未证 slot继续失败关闭 | Film Grain stock/no-mask 正例实际消费 slot 0 physical/mapped resolution计算 aspect；Water Flow的baseV1/phaseFeather现由普通Program从同代typed slot atom编码resolution/texel，已无专用carrier；Standard Blur exact static/KERNEL0 mask也从普通MaterialProgram的typed `.opacityMask` asset atom消费physical/mapped resolution，不再依赖专用mask carrier。strict Blur及未迁移Standard Blur remainder仍可保留各自现役strict carrier | [frame context](../../../MyWallpaperX/Core/SteamWorkshopScene/Runtime/SceneFrameContext.swift)、[typed candidate](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/SceneTextureCandidate.swift)、[slot binding](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/SceneTextureSlotBinding.swift)、[Program uniform encoder](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/MaterialProgram/SceneResolvedMaterialUniformEncoder.swift)、[E-TEXTURE-CANDIDATE](runtime-evidence-index.md#e-texture-candidate)、[E-EFFECT-FILM-GRAIN](runtime-evidence-index.md#e-effect-film-grain)、[E-V1-STANDARD-BLUR-MASKED-OWNER](runtime-evidence-index.md#e-v1-standard-blur-masked-owner) | 一个 slot 0 正例与一个bounded mask正例不证明全部 `g_TextureNResolution`、texel/mip、sprite transform、动态 replacement/resize、BC5 或 Windows packing | 继续让 Program texture atom驱动逐 slot resolution/texel/mip，并按正负 GPU 门迁移旧 consumer |
| render state (blend/depth/write/cull/alpha writing) | `L2` | 五项 raw 值按 material pass 保存并进入resolved node；共享compiler只把当前语料观测到的`normal/translucent/additive`、enabled/disabled depth、normal/nocull与unspecified/default/enabled alpha编译为typed state，未知或不完整值返回nil | typed state进入atomic Program与Metal compile-state identity；resolved fullscreen overwrite只接受`normal / depth disabled / depth-write disabled / nocull / alpha unspecified-or-enabled`，preserved-RGBA与color attachment都由同一pass encoder按output contract选择write mask；particle保持独立bounded admission | 当前Cursor三个作者pass直接走共享Program：两个state pass的alpha enabled与combine的unspecified都命中同一fullscreen overwrite predicate，不再有Cursor专用state profile。particle只准其既有additive/translucent、disabled depth/write、nocull与alpha unspecified/default | [typed state](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneMaterialRenderState.swift)、[template compiler](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/MaterialProgram/SceneResolvedMaterialTemplateCompiler.swift)、[Program identity](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/MaterialProgram/SceneResolvedMaterialProgramIdentity.swift)、[pass encoder](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/EffectExecution/SceneResolvedMaterialPassEncoder.swift)、[particle admission](../../../MyWallpaperX/Core/SteamWorkshopScene/Particles/SceneParticleAssetGraph.swift)、[E-AUTHORED-SHADER](runtime-evidence-index.md#e-authored-shader)、[E-V1-CURSOR-RIPPLE-SHARED-FEEDBACK-OWNER](runtime-evidence-index.md#e-v1-cursor-ripple-shared-feedback-owner) | `alphawriting=default`仍未转换为resolved write mask；blending disabled不是当前shared tuple。Ghidra静态证据只确认resolver/state injection与blend-to-variant边界，不提供Metal blend factor/operation、write mask、alpha convention或`default`语义 | generic translator/cache逐枚举增加source/destination/alpha像素门，并以Windows golden定标；不得从Ghidra推导factor |
| MyWallpaperX 手写 MSL profile（非 WE 语言语义） | `L3` | 由effect/runtime plan转成固定Swift uniform struct | Metal compiler可编译repo内MSL | B1至B9曾让typed pair、已证logical-target与exact raw Precise pair复用统一executor；B9 God Rays固定MSL path现已由两个bounded共享Program owner取代并物理退役，只保留历史provenance。B10没有新增MSL，只收敛visibility lifecycle；B11迁移bounded captured-main。B12的Clipping专用dependency-blend renderer与GPU oracle现已删除，classic primary改由作者MaterialProgram执行；B13的`worleyColorV1`固定MSL与typed专用owner现也已删除，legacy slot-3 solid改由普通作者Program和共享named publication执行 | [E-V1-LEGACY-DIRECTIONAL-GODRAYS-SHARED-OWNER](runtime-evidence-index.md#e-v1-legacy-directional-godrays-shared-owner)、历史[E-R4-B9-LEGACY-GODRAYS-LOGICAL-TARGET](runtime-evidence-index.md#e-r4-b9-legacy-godrays-logical-target)、[E-R4-B11-CAPTURED-STANDARD-BLUR-LOGICAL-TARGET](runtime-evidence-index.md#e-r4-b11-captured-standard-blur-logical-target)、[E-R4-B12-EXTERNAL-PRIMARY-CLIPPING-MASK](runtime-evidence-index.md#e-r4-b12-external-primary-clipping-mask)、[E-V1-PROCEDURAL-NOISE-SHARED-OWNER](runtime-evidence-index.md#e-v1-procedural-noise-shared-owner)、历史[E-R4-B13-EXTERNAL-PRIMARY-PROCEDURAL-NOISE](runtime-evidence-index.md#e-r4-b13-external-primary-procedural-noise)、[E-R4-B8-PRECISE-FULL-FRAME-COMPOSE](runtime-evidence-index.md#e-r4-b8-precise-full-frame-compose) | 只说明仍存在的项目自有backend可交付；已删除God Rays、Clipping与Procedural Noise实现只作历史证据，各门不证明其他topology、generic named texture或Windows WE golden | 对仍未迁移profile逐项保留可观察fallback；通用路径取得对应可见结果并稳定后按bounded capability撤权，旧R4顺序不再适用 |

现役 Program 已为 `renderSize/MVP/time/dayTime/frameTime/pointer/screen/texelSize/textureResolution` 建立 typed host-uniform carrier，并让 bounded dynamic scalar/vector3 与音频数组进入同一 Program/pass encoder；这些能力分别由历史 R3/R4 批次落地，但旧标签不再决定现役顺序。它们仍不证明所有官方名称、单位、空间、更新频率或任意 shader 兼容；未取得 GPU 正例的 built-in 行保持原等级与缺口，不得因 carrier 或 ABI 存在直接开放。

## 6. 现役能力扩展边界

1. **Shader contract IR v1（已完成）**：只读、安全保存 source/stage/include/annotation/declaration、raw/canonical hash 与诊断。当前是 line-based loss-preserving source contract，不是完整 AST；不运行外部编译器，也不声称 GPU 支持。
2. **Local Contrast shared Program owner（bounded owner migration 已完成）**：`2902406982` layers `167/177` 的exact stock 4-pass/2-quarter-RGBA图现由alpha-weighted average、normalized-sum与identity-free preserved-alpha RGB difference source contracts形成普通MaterialProgram并以`generic-only`执行；旧strict planner/backend/renderer已删除。坏node artifact只撤销当前effect到previous-current，恢复后同一request重新取得generic artifact。边界仍是mask/greyscale/nondefault kernel/scale、mixed chain、其他difference/mix source形态、独立ROI、`HLSL_SM30` parity和Windows/官方golden；精确证据见[E-V1-LOCAL-CONTRAST-SHARED-OWNER](runtime-evidence-index.md#e-v1-local-contrast-shared-owner)，旧strict证据只作历史provenance。
3. **Ordered strict effect-chain（已完成有界执行；D6 extent 为 `L2`）**：完整支持的 material-only stage 已按作者顺序消费 target/bind/read/write，同 command buffer、整链 allocation/LRU 和末段合成有原子失败门；synthetic 与真实 Workshop chain 均有正向门，现役 strict fallback 的 unsupported mixed chain 仍整体关闭。这个整链失败是当前 bounded 实现事实，不是 V0/V1 通用路径的验收不变量；新路径必须按 effect/pass/真实依赖子图局部降级并继续无关 suffix。`69cc7f01` 让 authored `width`/`height`/`fit`/`scale` 保真、allocation 与 preflight area cost 共用同一算法，但没有因此建立 generic FBO/material executor。
4. **Workshop Shadow conditional straight-union（owner已迁移）**：旧exact strict backend只保留历史provenance；现役`source-proven-graph-input-conditional-straight-union`由源码结构、唯一typed graph input与无provider/target/scalar事实准入，不含effect/sample/path/hash。真实`3767460992:945#effect#956`默认generic-only、显式回滚、坏artifact previous-current与后缀compositor均已闭合；这不是任意conditional、任意Shadow或官方像素oracle。
5. **Stock Opacity `MASK=0` + live alpha（已完成）**：exact single-pass profile 已让 alpha 经既有 binding program/per-surface snapshot 被 GPU 消费；同批覆盖 premultiplied RGBA、ordered chain、invalid/mixed rebuild、same surface/window 与 unsupported stage 原子失败。
6. **Typed backend consolidation（只按真实复用推进）**：现役 backend 共享统一 frame preflight、persistent target、GraphExecutor transaction 与 compositor；旧 chain capture、whole-chain owner 和 partial-recovery 已删除。各 pipeline 仍可保留 profile-specific uniform/target 编排；只有真实重复能由同一职责边界消除时才提取，不为行数或未来扩展预建抽象。
7. **Provider Core（静态候选与 bounded slot-binding 切片已完成，动态/通用仍待推进）**：`378e68e7` 曾让旧Water Flow flow/phase与Standard Blur mask复用同一typed candidate；现役Water Flow已删除专用loader/slot owner，作者flow与exact stock phase改由普通material asset catalog/publication与Program消费，该历史提交只作provenance；`de5f01b6` 进一步接入 static plain base image 与 bounded distinct REFRACT normal；`d87de042` 让同一 static base candidate 在 direct、inline、offscreen 与 authored-effect 路由统一经过最终 texture identity/purpose/UV/sampler/format 校验，错误 purpose 或不匹配 texture 不再静默回落 legacy frame。历史`6665cb98`建立 authored `0...7` slot-binding atom，并曾让旧Shake 1/2/3、Foliage Sway 1/2、Water Ripple 1/2七槽把texture与candidate代际/identity/purpose/UV/sampler/format原子绑定；现役Shake已删除专用loader/slot owner，改由普通Template/variant resource path消费同一共享publication，该历史原子不再授权Shake专用产品路由。`eedb3c62`接入exact legacy Blend slot 1；`b623f421`曾把Depth Parallax slot 1 R8 `.depth`接入旧专用consumer，现役Depth已删除该loader/slot owner并由ordinary material asset publication、active Program slot与同一texture-transform ABI消费，该提交只作provenance。仍存专用consumer继续在最终GPU split复核各自slot/purpose/format/UV/sampler并实际消费sampler。`7866dbc0`让本机普通文件generation加入device/inode/ctime，并在image/TEX/video读取或发布前后复核完整revision，关闭同size/恢复mtime rewrite与metadata全零identity。另一个bounded primary named原子已让backward/acyclic/single-dependency hidden-image effectful provider沿普通Program/GraphExecutor执行，并把graph final复制到独立reservation后发布；其余effectful/nested形态、child provider与same-file/animated/multi-image/sprite/clamp-border REFRACT仍保持legacy或关闭。已证slot仍只接受各自已证axis-aligned/identity UV与格式。其余Effect/generic material的population/readiness、dynamic provider generation/stale cancellation、跨网络/粗粒度metadata的content digest、alpha/color encoding与graph invalidation仍未共用完整代际合同。
   本次single-image animated material原子修正了上一段的现役边界：catalog仍只拥有immutable definition；surface-scoped FrameProvider复用同一GPU texture/file lifecycle，并在frame变化或wrap时只推进唯一frame registry的content generation和candidate UV。Program每个active slot的mandatory transform ABI使identity static像素等价，并把mapped/padded static与animated frame统一送入boundedSwift/generic sample consumer。其余effectful/nested形态、child provider、multi-image material atlas、rotated/trimmed/fractional frame、dynamic replacement与sprite/clamp-border REFRACT仍保持其他owner或关闭；identity/purpose/publication/ABI/generation/stale/OOM等错误继续硬拒绝。详见[E-V1-SINGLE-IMAGE-ANIMATED-MATERIAL-ATLAS](runtime-evidence-index.md#e-v1-single-image-animated-material-atlas)。
8. **Built-in 与 render-state binder（统一框架上的能力缺口）**：generic carrier 与 observed-enum compiler保持；typed `shaderValue` scalar/vector3和16/32/64 stereo audio arrays已进入Program/pass encoder。其他官方名称/单位/空间、通用state translator/cache与像素门仍待公共能力扩展；旧owner撤权已经完成，`alphawriting=default`继续只保真、不猜映射。
9. **Shader preparation（现役底座）**：project/scene version provenance、VFS source graph、include/directive、combo/readiness、active source map/digest、unresolved color carrier与独立typed module dependency继续服务普通作者程序。exact LightingV1 zero-source slice已有项目自有Program可见正反恢复，但只授权该bounded module-resolution结果；底座结构与真实Fluid standalone preparation本身都不算Fluid可见或完整module能力。
10. **Atomic material Program（现役底座）**：固定8槽Template、provider publication、active variant/reflection、uniform、typed state与保守color transfer已进入同一snapshot/identity；bounded Program子集现在已经由统一GraphExecutor实际编码，旧`gpuEncoded=0 / 下一步R4`口径已失效。
11. **Generic shader execution（V0首个可见结果已闭合；V1已有独立profile迁移）**：exact source-key artifact已复用Program ABI，并对Water Waves、Water Flow、Depth Parallax、Light Shafts、Opacity、Fisheye、Chromatic及当前Color Key/Hue Shift/Shadow bounded shape取得各自GPU/compositor/next-frame证据。source-proven scalar interpolation、独立premultiplied output、graph-target passthrough、R8 writer/signal、stage-uniform graph-input与strict alpha-attenuation等独立profile保持`generic-only`；graph-input straight-alpha与straight-alpha-preserving因真实mixed-chain compiler反例退回`prefer-generic`，不再宣称宽泛profile owner migration。`source-proven-independent-premultiplied-output`只接受prepared active source不采样slot 0、没有graph/provider/scalar output且颜色证明为premultiplied的Program；三个真实Light Shafts stage与显式`disable-generic`回滚闭合后，其旧planner/plan/pipeline/resource loader及dedicated backend已删除。其他旧dedicated leaf只可作带typed reason的迁移fallback/oracle，不得增加effect matcher；strict alpha-attenuation现已闭合默认坏artifact的effect-local previous-current GPU/publication/compositor/next-frame反门、profile-local `disable-generic`回滚与旧Opacity owner撤权。Water Waves `reducedV2`的40个static instance所属prepared source shape现由公共stage-uniform graph-input profile持权，旧planner/profile/compiler/backend/pipeline/renderer/loader已成族删除；两个user-bound与一个四-property SceneScript wrapper从未被旧planner接纳，继续作为V2/V4 typed producer与generation/epoch/publication债务，不能固定成静态常量、改名unsupported或复活旧renderer。Water Flow的baseV1/phaseFeather与Depth Parallax已统一由stage-uniform graph-input profile持权，普通asset publication分别提供作者flow/exact stock phase与R8 `.depth`；两族旧planner/profile/compiler/backend/pipeline/renderer/loader与耦合测试均已删除，真实失败注入、shared rollback与恢复没有恢复旧owner。Depth额外只开放当前函数根作用域有静态immutable上界、index/上界不进入authored helper且loop helper由`main`唯一直接调用的float early-exit loop、`CAST3X3`与active sampler transform ABI，不外推其余family shape。其他profile的真实失败注入仍按各自owner门推进。不得把任一profile或单样本结果外推为任意shader、V1完成或官方parity。
12. **旧owner零门（稳定不变量）**：whole-chain/standalone/frame-batch与重复target/resource路径不得回引；其R/B迁移过程只从[历史索引](../../history/README.md)追溯，不参与能力优先级。
13. **扩面原则**：Motion Blur、Refraction、Fluid等只作为普通pass/FBO/history/resource fixture；修通用primitive后观察同类authored unit收益，不按效果显示名新增strict stage。

### V1 independent RGBA signal carrier 现役更正

本段取代本页前文所有把真实 Fluid node 15 写成 `shader-color-transfer-unresolved` 的现役断点口径。共享 ShaderFrontend/ShaderPreparation 现可从 identity-independent source 证明 exact-slot independent RGBA carrier，经 exact request/cache/artifact 协议进入 MaterialProgram；输入必须已有 `.independentAlphaSignal` publication，输出保持同类型，不做 straight/premultiply lowering。source helper closure 与 compiler-output MSL 均要求 exact carrier/output-use 闭包；source sample 到首次 output 之间由顺序 symbol state证明 exact uniform/varying、proven local、pure scalar grammar和 non-carrier projected-data sample，任何 shadow、whole/member/index/compound、inc/dec、未知/inout call、carrier member write/escape及 reachable recursion/cycle失败关闭。actual MSL从 normalized `MWXFragmentUniforms` exact buffer-8 entry parameter重做相同证明；direct output carrier允许 exact value或 `thread const float4&`，SPIRV-Cross staging local只允许从已经初始化的 exact color-0 output单跳进入唯一 leaf `thread const float4&` helper consumer，副作用、alias、额外 whole/color sample、未满足 projected-data contract 的 sample、额外 output/read/assignment、nested helper、multi-hop、value/non-const-ref/pointer ABI、direct/transitive cast/address escape、authored builtin shadow、constant init和protocol drift失败关闭。最终 checkpoint 为 **53 modules / 149.9 s / ALL OK**，Developer ID Debug BUILD SUCCEEDED且 `closure complete: yes`；没有新增 renderer、registry、graph、target 或 compositor owner，也没有 route 变化。

该冻结probe中的node15 source-route断点已由后继active-program classifier闭合；本节只保留independent carrier的source/artifact/Program合同。详见 [E-V1-INDEPENDENT-SIGNAL-CARRIER](runtime-evidence-index.md#e-v1-independent-signal-carrier)。

### V1 dormant captured-main binding 冻结后继

authored graph仍完整保留layer-source binding，但captured-main Program角色以precompiled launch envelope的active slot为准。只有该binding slot在每个variant的`frontendProgram.textureBindings`中都缺席，且其余graph references/target仍为同effect FBO时，Program才可作为internal-only；任一variant实际消费slot时，现有regular/no-default sampler与color-transfer source-slot合同必须证明它是exact main source。wrong layer/effect、多个source、provider、variant divergence、错target/FBO和全图零source继续失败关闭；没有第二GraphTargets、renderer、resource registry、publication或compositor owner。

项目三Program/两FBO正门与逐字节恢复均为targeted **1/1 PASS**、47/47/47 GraphExecutor claim/encode/GPU，frame0/next-frame均3 material/0 rejected、GPU/publication/terminal compositor闭合。只让dormant slot变成active non-color source的反门按预登记为 **1/1 negative-contract PASS**，实际为`not-admitted / r5-no-runtime-owner`与0 claim/encode/GPU，previous-current保留完整红蓝control。fresh真实`3749463715`为targeted **0/1 NON-PASS**、exit0、timed_out=false；旧node15 source-route拒绝消失，该批冻结时首断点前移到node17 `fluidsimulation_combine.json#0`的`color/colorContractUnproven / shader-color-transfer-unresolved`，Fluid CPU/Program/GPU仍0。该atom已由后继shared compositing合同闭合；非Fluid可见/执行、owner migration、V1完成或official parity。冻结证据见[E-V1-DORMANT-CAPTURED-MAIN-BINDING](runtime-evidence-index.md#e-v1-dormant-captured-main-binding)，现役后继见[E-V1-INDEPENDENT-SIGNAL-COMPOSITING](runtime-evidence-index.md#e-v1-independent-signal-compositing)。

### V1 effect-local texture-purpose 失败隔离

`implicitFramebufferSlots` 现在把 graph-input identity 与 auxiliary purpose 分开：slot 0 为 active regular sampler、无 material key/default/candidate，其余 active slot 均有显式非 graph authored candidate 时，可把 slot 0 识别为 previous/current；这些 candidate 只证明 auxiliary role，purpose 仍必须通过独立 Program demand。任一 auxiliary graph candidate、duplicate/conflict、inactive sampler 或 slot identity 漂移都不能命中，launch/frame 继续共用同一 selection owner，authored candidate precedence 不变。

只有无 history/dependency/compose/unsafe target 的已证 effect topology，`purposeUnproven` 才以 typed previous-current passthrough 收窄失败半径；resource/reference/identity/ABI/generation/target/hazard/lifecycle/budget/encode 仍硬拒绝。真实 `3749463715:533` 以 effect 0 passthrough + effects 1...4 Program 在 frame 0 与 next-frame 闭合 GPU/publication/terminal compositor，但不证明 Water Flow 视觉执行、整样本 PASS 或 parity；已存手部 alpha/mask 与剑光 fidelity 债务未修复。见 [E-V1-EFFECT-LOCAL-TEXTURE-PURPOSE-FALLBACK](runtime-evidence-index.md#e-v1-effect-local-texture-purpose-fallback)。

### V1 authored slot-chain typed purpose 现役更正

Program texture purpose 不再只看 selected override 的单一 asset。共享 ordinal-aware fact 只允许未登记的高优先级 instance asset 从同槽、严格更低 ordinal、无 intervening ambiguity 的 authored material asset chain 继承唯一一致的 exact registry purpose；untyped/conflicting lower、registered high conflict、graph/provider/user/intervening、wrong provenance/order 全部拒绝，且没有为 `particle/normal_pinch_rotate` 增加 registry entry。Catalog、launch 与 frame/Program selection 共用该事实；只有 explicit `.absent` 回退，其他 publication/generation/identity/purpose 错误仍硬拒绝。

真实 `3749463715:533` 的 Water Flow effect 0 已由 passthrough 晋升为 ordinary `resolved-material / program / encoded-output`，与 effects 1...4 在 frame 0...116 各完成 117 次 GPU；每帧 0 rejected，effect 4 terminal compositor consumed，layer533 在 next-frame 集合，GraphExecutor 无 local fallback。整样本仍因 layer464/1106 static-uniform binding 与 layer536 既有 passthrough 为 targeted **0/1 NON-PASS**；当前截图只支持无新增黑屏/整层/整帧丢失，不是 Water Flow ROI、视觉 parity 或 owner migration。见 [E-V1-SLOT-CHAIN-TEXTURE-PURPOSE](runtime-evidence-index.md#e-v1-slot-chain-texture-purpose)。

后继owner批次已经把这一执行切片升级为bounded owner migration：`baseV1`与`phaseFeather`共用`source-proven-graph-input-stage-uniform-passthrough / generic-only`，作者flow与exact stock phase沿普通material asset publication进入Program；Water Flow专用planner/profile/compiler/backend/pipeline/renderer/texture loader/repository/mask storage及实现耦合测试已删除。坏artifact只撤销exact effect并继续suffix，显式disable只回滚shared bounded frontend；`3028090166`默认/回滚/恢复均为1/1 PASS。`3749463715`仍因surface生命周期超时为0/1 NON-PASS，不能由其已完成的Program/GPU/publication/compositor事实改写为样本通过。见[E-V1-WATER-FLOW-GRAPH-OWNER](runtime-evidence-index.md#e-v1-water-flow-graph-owner)。

### V1 inactive resolution hole 的 typed neutral ABI

prepared source structural fact只接受missing/inactive `g_TextureNResolution`精确供给一个varying component pair，且该pair只被active slot M作为sample coordinate消费；其他sampler仅可使用disjoint varying component。dead-binding因此可以在不删除resolution uniform/coordinate statement的前提下让未被fragment引用的sampler N保持inactive。fact进入compiled variant及Program identity；Finalizer仍要求N没有任何binding/default/candidate/active sampler，M的exact resolved candidate具备正确purpose/publication/generation且mapped UV scale精确为`(1,1)`，才编码typed neutral float4 ones。physical/mapped、UV、purpose、pending、source component/use或ABI任一不精确都继续失败关闭，不会生成placeholder、alias或host resolution来源。

fresh真实`3749463715`的`1106#effect#367`与`464#effect#519`已由static-uniform passthrough进入同一ordinary `genericCompilerArtifact → MaterialProgram → GraphExecutor`链，frame0/next-frame均1 authored / 1 material / 0 rejected并完成GPU/publication，后续suffix继续到terminal compositor。整样本仍因layer536 test_shader frontend为targeted **0/1 NON-PASS**；route保持`prefer-generic`，不表示Gradient family、owner migration、视觉parity或V1完成。见[E-V1-NEUTRAL-MISSING-TEXTURE-RESOLUTION](runtime-evidence-index.md#e-v1-neutral-missing-texture-resolution)。

### V1 exact scalar runtime-loop 与 shared varying-prefix frontend

MaterialProgram现从resolved template发布`SceneAuthoredShaderExactScalarFact`：fact必须stage-qualified、唯一immutable `.staticExact`、单分量、alias无冲突，并保留uniform/producer/binding/provenance/source line、Int32 value与Float32 bits。只有finite、非负、Int32可表示且Float32 exact integral的值获准；annotation range/int/default、dynamic/live/script/property contributor、冲突或multiple producer均不形成证明。fact进入compiled variant、reachability/dead-binding、Program schema以及semantic/exact identity，不能跨不同bits、source binding或producer provenance误复用；source operator、cast form与array extent仍由compiled artifact/source identity区分。

runtime-loop admission只接受canonical `int i = lower|int(lower); i <|<= upper|int(upper); i++|++i`，并要求loop body及静态可达helper中每个直接`array[i]`都是fixed one-dimensional float array。`<`要求`0 <= lower <= upper <= minExtent`；`<=`要求`0 <= lower <= upper < minExtent`。每loop work上限256、调用展开总量4096；index/bound mutation或shadow、递归/歧义、`i+1`、array/index parameter逃逸、二维/动态index、unsupported array和overflow/预算失败均拒绝。准入不clamp、不unroll、不改作者source或控制流。

bounded frontend和generic normalizer现在共用`SceneAuthoredShaderVaryingPrefixLink`。只有同名float vector严格前缀、vertex无条件初始化fragment需要的连续前缀且fragment不读suffix时才可link；emitter/normalizer只对该typed proof显式化`.xy`或`.xyz`。`a93c2608`后的fresh runtime暴露旧proof没有覆盖direct texture-coordinate call：constructor-only guard让真实`texSample2D(g_Texture0, carrier.xy)`在两条frontend重新拒绝。现役proof复用emitter的canonical texture-sampling builtin及exact arity，只接受第2个read-only coordinate实参；重命名slot3 fixture以vertex仅写`.xy`、fragment direct sample后再whole local copy证明两路均形成prefix/slot事实。suffix/yx/zw、missing/conditional/multiple writer、fragment write、return、unknown/user/out-inout call、array/dynamic index、integer/type/shape与alias escape继续失败关闭。fresh真实`3749463715`再次越过原`dynamicLoop`与varying/compiler-normalization首断点，但`536#effect#553`在Program/GPU之前被`textureBindingInvalid slot=0`拒绝；所以这里只登记shared frontend/compiler/preflight能力，不登记可执行或可见等级。见[E-V1-EXACT-RUNTIME-LOOP-VARYING-PREFIX-FRONTEND](runtime-evidence-index.md#e-v1-exact-runtime-loop-varying-prefix-frontend)。

该冻结首断点现由canonical typed graph-input fact闭合：launch只登记logical exact effect input，frame必须等GraphExecutor已有pair input exact publication后才选择`.graph(pairStep.inputIdentity)`；Program exact/semantic identity、diagnostic provenance、color/alpha eligibility与captured-main conservation都复用同一fact。真实`3749463715:536` effect0的slot0为new dormant provenance + exact layerSource536，effect1 slot0为既有implicit provenance + exact连续effect0 output；frame0和next-frame均为两段普通Program、0 rejected、GPU/publication，effect1由terminal compositor消费。旧`textureBindingInvalid`、source/frontend fact divergence与Metal-library rejection最终均为0。selection-required报告虽为1/1 PASS，本表只登记`S3 executable / prefer-generic`；没有controlled audio、预登记test_shader ROI、官方golden、owner migration或整样本兼容结论。见[E-V1-DORMANT-UNRESOLVED-MATERIAL-GRAPH-INPUT](runtime-evidence-index.md#e-v1-dormant-unresolved-material-graph-input)。

## 7. 当前证据与更新规则

当前 App、样本、报告、matrix 和签名身份只见[运行证据索引](runtime-evidence-index.md)。本表只维护能力合同与缺口，不复制某个旧提交的测试总数、报告路径或“下一批”。

本表只使用本专题定义的 `L0-L4` 表达能力覆盖。`S0-S5` 的含义只由[运行证据索引](runtime-evidence-index.md)定义，引用时必须同时给出其精确 code/App/fixture/ROI identity；本表不重新定义 `S`，也不从 `L` 机械推导 `S`。

formal full45、fixed13、非黑截图、compile成功或旧strict profile不能替代后两级。当前优先级只看[Scene兼容执行路线](../scene-compatibility-roadmap.md)。
