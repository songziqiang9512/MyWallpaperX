# Scene Render Graph 与 Shader 覆盖矩阵

> 核验日期：2026-08-13
>
> 范围：Effect definition、material、render graph、FBO 与 Wallpaper Engine shader 合同。
>
> 结论：当前graph已有较完整的保真IR，R2/R3建立source/variant preparation与atomic `SceneResolvedMaterialProgram`，R4-A至B22把旧产品owner逐族迁入并撤销统一executor周围的并行裁决。B23-B25删除frame-batch、whole-chain/standalone、旧planner/runtime-plan/decision/inline telemetry、fallback graph/provenance、启发式资源投影和直接target旁路；产品只保留typed VFS、admission/program、frame preflight/persistent target、GraphExecutor per-stage backend与compositor。全部R4/R5 retirement gate为全局`0/0`且无例外，阶段为`R4=complete / R5=complete`。B25没有benchmark/fixed/full；B22签名App的full45 **45/45 PASS**、fixed13 **12/13 NON-PASS**与`2938612768`的`flat-border`视觉债务仍是最新跨样本里程碑。formal owner门、full45 PASS、非黑与旧ImageBlend 5/5都不表示通用shader、性能或Wallpaper Engine视觉等价。精确身份见[运行证据索引](runtime-evidence-index.md)。

> R0-R5 的逐批迁移叙事已从本现役矩阵移除；历史原因、运行身份和失败边界统一查[已完成的重构计划](../scene-render-chain-refactor-plan-2026-08-03.md)与[运行证据索引](runtime-evidence-index.md)。本页只保留当前 primitive、执行边界与下一能力门。

## 1. 证据与使用方式

本页把以下资料压成一张可执行的开发清单，不替代原始语义文档：

- [Scene 格式与 Render Graph](scene-format-and-render-graph.md)：字段、资源身份、pass 顺序和目标执行合同；
- [Effects 语义全集](effects-reference.md)：45 个公开 effect 的输入、pass/RT 和正确空间；
- [官方页面全目录](official-page-catalog.md)：官方 Shader/Effects 页面无遗漏入口；
- [运行证据索引](runtime-evidence-index.md)：IR、ShaderContract、strict Blur、strict Local Contrast、exact Workshop Shadow 与 GPU 证据包；
- [资料来源与证据索引](source-index.md)：官方、样本、第三方和当前运行证据的可信边界。

这里的等级只描述 **MyWallpaperX 当前实现成熟度**，不是官方语义的可信等级，也不是某个 effect 的视觉相似度。

### 1.1 `linux-wallpaperengine` D 级复核边界

固定 revision `b016d7d1fdcf4e5fd2f9c9fa420a8aaa07fee02d` 没有附带 stock effect/material/shader assets，运行时要求用户提供官方 Wallpaper Engine assets。因此它不能核验 45 个 effect 的逐参数和算法；本页只用它交叉检查 generic contract，并保留以下未进入官方 A 级表的候选：

- `ShaderUnit.cpp:77-94` 的处理顺序是 include -> `#require` -> variables；`#require` 只识别 `LightingV1`，生成的 `PerformLighting_V1` 固定返回零（`ShaderUnit.cpp:313-376`）。这证明私有 shader 中可能存在 module directive，不证明该 stub 或 module schema 正确。
- 它的 annotation/combo/sampler 解析依赖 regex/string，sampler slot 只提取单个数字且 combo 只接受整数（`ShaderUnit.cpp:442-543`）；不得作为未来 frontend 的实现模板。
- `CPass.cpp:824-890` 还尝试绑定 `g_TextureReductionScale`、`g_Brightness`、`g_UserAlpha`、`g_CompositeColor`、`g_LightAmbientColor`、`g_LightSkylightColor` 和 `g_NormalModelMatrix`。这些名称是 D 级 runtime 候选，不追加到官方 Variables 合同；先在合法 assets/样本 source 中确认声明、类型和作用域。
- 同一 binder 把 Left/Right 16/32/64 spectrum 都指向同一 mono 数组，并把部分 effect projection/normal matrix 简化为 identity 或现有 matrix。它只能帮助发现需要的 binder surface，不能提供 stereo、matrix 或 lighting 数值 golden。

| 等级 | 唯一含义 |
|---|---|
| `L0` | 当前代码没有识别或承载该语义；只有文档需求不算实现。 |
| `L1` | 能发现资源、路径、blob、raw token 或给出诊断，但没有可执行的结构化语义。 |
| `L2` | 已进入 typed/loss-preserving IR 或 typed host carrier，并有结构/失败关闭测试；没有 GPU 执行承诺。 |
| `L3` | 至少一个**明确注册的 strict executor 子集**精确消费该语义，并有正向、负向及运行/GPU 证据；只对表中边界负责。 |
| `L4` | generic runtime 能直接按 authored graph/shader 合同执行该语义，不依赖 effect ID、shader path 或固定 profile 手写分派，并覆盖资源生命周期和回归矩阵。 |

每行只能有一个等级。**通用 primitive 取全部已知 authored 形态的共同最低状态**：某个 strict profile 能执行，只能在该 profile 自己的行和 [Effect 执行覆盖表](effect-execution-coverage.md) 记 `L3`，不能反向把通用 definition、pass、FBO、slot、combo、uniform 或 render state 抬到 `L3`。辅助设施存在也不能把官方语义抬级。

**本表的 `L4` 与其他覆盖表的 `L4` 不是同一坐标轴。** [覆盖台账](coverage-ledger.md)、[Effect 执行覆盖表](effect-execution-coverage.md)、[粒子组件覆盖表](particle-component-coverage.md)、[SceneScript API 覆盖表](scenescript-api-coverage.md) 的 `L4` 都指「已与 Windows Wallpaper Engine golden 或官方行为核验」，是**验证轴**；本表的 `L4` 指「generic runtime 不靠固定 profile 分派即可执行 authored 合同」，是**实现通用性轴**。因此本表某行达到 `L4` **不等于**该语义已取得像素 parity，反之靠 strict profile 取得 golden 也不会把本表抬到 `L4`。引用等级时必须带上出处表名。

已删除的派生解释格式曾记录 graph/shader 侧增量：v22 在既有 ShaderContract/binding program 和 exact stock Opacity 合同基础上保存 direct text content/point-size/color binding program，v23 增加 layer `puppetMeshPath`。此后 v24 增加 Puppet attachment bind frame（`attachmentName`/`boneIndex`/`modelBindFrameColumnMajor`）、v25 增加 `puppetAnimationLayers`（MDLA clip 的 `animationID`/`additive`/`blend*`/`rate`/`visible`/`visibilityBinding`）——**这两次都是 Puppet 通道的字段，没有改变 graph/shader 侧合同**。v26 增加 material pass `alphaWriting` 与 `userShaderValues`，v27 增加 layer `brightness`，v28 增加 text layer `anchor`，v29 增加 text overflow 字段；这些版本号现在只作字段落地历史。`a77b875` 起 production host 直接持有内存 `SceneRuntimeInput`，Debug runtime evidence schema 1 与播放输入解耦。execution catalog 仍在启动时从 authored graph 与 ShaderContract 规划；Opacity direct alpha、Local Contrast strength、受限动态文本与exact X-Ray visibility target可live消费per-surface snapshot。其他user-property effect visibility在缺少完整typed consumer时原子拒绝并由service重启Scene；Timeline/generic SceneScript effect visibility与SceneScript alpha、其他动态 effect 参数、Shadow 常量、通用 shader uniform仍未live。

当前基线 commit 与 schema 版本号以 [运行证据索引](runtime-evidence-index.md) 和 [覆盖台账](coverage-ledger.md) 为准；本表不复制，避免随代码演进失真。

## 2. 四条实现通道

| 通道 | 当前含义 | 当前事实 |
|---|---|---|
| IR | 读取并保真保存 definition/material/shader source contract，再编译资源身份、节点、slot 和 blocker | [SceneEffectDefinition.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneEffectDefinition.swift)、[SceneAuthoredEffectRenderPlan.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/AuthoredGraph/SceneAuthoredEffectRenderPlan.swift)、[SceneShaderContract.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderContract/SceneShaderContract.swift)、[SceneAuthoredEffectRenderPlanner.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/AuthoredGraph/SceneAuthoredEffectRenderPlanner.swift) |
| bounded shader preparation | 从同一 `SceneResourceView` 构建独立 source graph，以 declared project/scene version provenance、material combo、sampler readiness 和有界 fixed-point 形成 prepared source、active schema/source map/dependency/variant identity | package -> loose -> stock 是 MyWallpaperX 当前只读 VFS policy，不是已经证明的官方全版本默认；host-owned backend/version/platform/texture-format define 无 provider 时失败关闭。preparation 是中性 Program 前置能力，只有 slot/provider/state/color 与 frontend 全部闭合后才由 resolved-material owner 准入；preparation success 本身不授权 GPU 执行，见 [E-SHADER-PREPARATION](runtime-evidence-index.md#e-shader-preparation) |
| atomic material program | production raw graph编译固定8槽Template，再从一个immutable frame/resource/dynamic snapshot解析provider、active variant/reflection、uniform/state/color并生成semantic/exact Program identity | R3 one-shot audit 仍是 `gpuEncoded=0` 的历史地基；R4 已由 capability 直接准入 bounded Program，统一 executor 负责 GPU 编码、terminal publication、completion/rollback 与 next-frame evidence。只有 explicit absent 允许低优先级候选，其他未证 purpose/state/color 仍 fail closed，见 [E-MATERIAL-PROGRAM](runtime-evidence-index.md#e-material-program) 与 [E-R4-1-STRAIGHT-ALPHA](runtime-evidence-index.md#e-r4-1-straight-alpha) |
| generic runtime | 可复用的 frame context、texture registry、offscreen allocation、Metal encoder 等基础设施 | 它们提供时钟、pointer、尺寸、纹理和命令编码能力，但**不会自动解释 authored graph node 或 shader source** |
| strict executor | 先匹配完整 graph/material/profile，再调用独立手写 Metal backend；任何未知 shape/state/combo 均拒绝 | 目前有 2-pass Blur Precise、4-pass stock Blur、4-pass stock Local Contrast 与 exact Workshop Shadow 单 pass；[admission catalog](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/EffectCompilation/SceneEffectAdmissionCatalog.swift)只发布完整、守恒的stage program，统一`SceneResolvedMaterialGraphExecutor`负责作者顺序、stage transaction与末段发布；[per-stage typed backend](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/EffectExecution/SceneEffectStageRenderer.swift)只保留`prepareStage`/`encodePreparedStage`/`renderStage`等executor内部能力，不再提供whole-chain或standalone产品入口。Shadow准入见[SceneAuthoredWorkshopShadowPlanner.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredWorkshopShadowPlanner.swift) |
| bounded authored frontend | 把已准备的 bounded authored source 翻译为 typed Metal frontend，并与 texture/provider、uniform、state、color 原子形成 Program | 当前只接受已证静态控制流、uniform layout、framebuffer/texture schema 与 render-state 子集；完整 Program 由 resolved-material pass encoder 和统一 graph executor 执行。三组 exact Scroll 的 hidden-slot fixture 只作历史 oracle，旧 Scroll profile/pipeline/renderer owner 已撤权；未知语法、动态/递归控制流、未证 sampler/purpose/state/color、FBO 与多 pass 失败关闭 |

`SceneMetalPipeline.swift` 中 Foliage/Shake/Water/Cursor 等手写 MSL 是 legacy/profile approximation。它们证明宿主能上传纹理、time、pointer 和矩阵，不证明 WE shader source、annotation、macro 或 built-in 名称已经兼容。R4 已撤销独立 authored-shader planner/backend/pipeline/renderer 与 exact Scroll profile 的产品执行权；现役 authored source 只可经 [中性 preparation](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderPreparation/SceneAuthoredShaderPreparation.swift)、atomic Program 与 [resolved-material pass encoder](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/EffectExecution/SceneResolvedMaterialPassEncoder.swift) 进入统一 graph executor。`3141421197` 与三组 exact Scroll 的旧 GPU 门只保留为历史 oracle，不再授权旧 owner 可达，也不能授权放宽其他 shader 或表述为任意真实样本已解锁。

## 3. Render Graph 覆盖

`136d35c` 新增的 Local Contrast strict profile 只接受 `effects/localcontrast/effect.json` 的单 effect stage：4 个 material pass、2 个非 unique 的 quarter-size `rgba8888` FBO、`KERNEL=0`、`GREYSCALE=0`、`MASK=0`、Gaussian `scale=(1,1)`（显式值或该 stock profile 的缺省值），以及 `0...5`、缺省为 `1` 的 `strength`。准入同时核对 exact shader identity/canonical/stage/raw/source hash；这些 fingerprint **只用于确认 stock contract 身份**，实际 GPU 路径仍执行仓内手写 MSL，并未编译 authored source。`strength` 是当前唯一由 property binding program -> per-surface snapshot -> GPU strict executor live 消费的该 profile uniform。

该 profile 不接受 mask、greyscale、非默认 kernel/Gaussian scale 或其他 Local Contrast shape；它只能进入每个 stage 都由 strict backend 接受的 chain，包含任一 unsupported stage 的 mixed chain 整体失败关闭。Metal 实现采用官方非 `HLSL_SM30` 分支语义，没有套用 `0.75 / resolution` 偏移；当前也没有合法 Windows WE 像素 golden，且运行日志尚不能直接证明某一帧把具体 `strength` 数值上传到了 Metal buffer。这些边界不能由 repo-local stage 数值测试或画面变化率代替。

`809b75e` 的 exact Workshop Shadow 只接受 `effects/workshop/3488490208/shadow_____________/effect.json` 的单 material stage、零 authored FBO/bind/slot、`MASK=0`、`BLENDMODE=0`、normal/nocull/depth disabled、black color，以及静态有限 alpha/border/offset；definition、material、vertex/fragment ShaderContract fingerprint 任一差异都拒绝。它复用 target table 的 synthetic input/output texture，在 ordered chain 中消费前一 stage output；仓内手写 MSL 保持 premultiplied host 边界，但 mode 0 没有合法官方 Windows 像素 oracle，因此不等于 WE 像素等价、官方 45 项 Effect、generic Shadow 或 authored shader execution。

`b541867` 的 scheduler 验证全局 effect/node/target identity、layer source -> effect output 连续性和最终输出，并先为整链建立 plan/table、预算与 LRU 候选，成功后一次提交。GPU 在同一 offscreen command buffer 依序执行 strict backend。普通 effect 继续按最长边 2048 分档；受限 authored shader 按 drawable 上的图层投影像素执行，最长边上限 4096，pool 驻留预算 128 MiB。`69cc7f01` 让 preflight 与 allocation 共用同一 extent resolver：先用显式 `width`/`height` 覆盖对应轴，再执行不放大的 `fit`、`scale` divisor 与 floor/min-1；单轴、组合形态及 `scale < 1` 放大都按实际 2048² 参考面积计费，非法值直接拒绝。`c73aa0b0` 只让 string/array 的精确 `[0,0,0,0]` clear 进入 logical target，并在 table 首次创建/重建后与 history seed 一并执行一次初始化；非零或畸形 clear 继续拒绝。该 128 MiB resident transaction 门不是瞬时驱动 VRAM 硬上限。generic compose/condition/function、跨帧 logical swap 与其他 history consumer 仍不执行，也不让下表 generic primitive 整体升级到 `L3`。

| 语义项 | 等级 | IR | generic runtime | strict executor | 代码/测试证据 | 执行边界 | 下一门 |
|---|---|---|---|---|---|---|---|
| `EffectDefinition` | `L2` | JSON5 loader 保留 metadata、dependencies、FBO、ordered pass、functions/gizmos、extra fields 和 unknown paths | 没有按任意 definition 驱动 GPU 的执行循环 | 现役 bounded strict profiles 从同一 authored plan 进入执行，未知字段/blocker 一律拒绝 | [定义 loader](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneEffectDefinition.swift)、[定义测试](../../../script/tests/test_scene_effect_definition.py)、[E-EFFECT-IR](runtime-evidence-index.md#e-effect-ir)、[E-EFFECT-WATER-MOTION](runtime-evidence-index.md#e-effect-water-motion) | 现役完整 strict profile 不代表通用 definition 已执行；该 primitive 仍停在 IR | 建立 graph executor capability registry；每种 node/resource semantic 显式声明并逐项放行 |
| ordered pass / material ordinal | `L2` | definition pass顺序、definition index、material ordinal与instance pass index分离 | unified node scheduler按nodeIndex交错material/copy/swap并把当前logical mapping传给material encoder；`SceneResolvedMaterialGraphExecutor.PreparedStage`再把一个effect的ordered commands、transition、frame/persistent publications与effect output冻结为stage-owned事务，stage N+1只消费stage N已发布output；unique history seed与已准入transparent-zero authored clear在table初始化阶段执行一次clear | 现役bounded backend均要求各自完整固定顺序；Precise Blur interleave、Shake前后、Water Flow/Opacity与重复Water Waves chain profile为受限`L3`，copy/swap不消耗material ordinal | [node scheduler](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/GraphTargets/SceneGraphNodeScheduler.swift)、[resolved executor](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/EffectExecution/SceneResolvedMaterialGraphExecutor.swift)、[admission catalog](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/EffectCompilation/SceneEffectAdmissionCatalog.swift)、[graph command runtime](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/GraphTargets/SceneGraphCommandRuntime.swift)、[target plan/table](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/GraphTargets/SceneGraphRenderTargetPlan.swift)、[E-R4-3-WATER-SHIMMER-STAGE-PIXELS](runtime-evidence-index.md#e-r4-3-water-shimmer-stage-pixels) | PreparedStage和v51只证明当前bounded chain的stage ownership/像素推进；generic topology、其他history consumer、跨帧logical swap、compose/condition/function仍未执行 | 接真实history consumer、compose，并继续以read/write hazard验证命令顺序 |
| `Material` | `L3 bounded program execution` | 固定8槽、candidate provenance、combo/uniform/state进入Template/Program；presence combo与default仍分离。launch以完整typed asset状态复用frame的“高优先级优先、仅explicit absent回退”选择，并仅对`layerSource`首段用同一content-only颜色推导核验renderer-owned premultiplied capture；该子集内可预知的颜色失败不再取得owner。公共frontend能证明唯一sample、RGB不写、alpha为`sample.a * scalar factors`的bounded straight-color形态；也能证明sampled RGB保持而alpha只由1–3个互异direct `.r` data辅助量调制的bounded scalar graph，以及同一layerSource color slot上的bounded whole-RGBA affine邻域滤镜；后者以独立`.straightAlphaUNorm(slot)`执行straight RGBA saturate后单次premultiply | registry精确区分graph/asset/property/system state；统一executor完成GPU/publication/compositor/next-frame。`3768724269:163` Rounded Mask、`3290491250:116` Sharpen及`3768229922`七个Pulse layer均有首帧、exact publication、terminal compositor、next-frame与目标ROI正证 | 未认领effect继续失败关闭；typed VFS graph与package/loose/stock provenance保留；普通`effectOutput`后续stage、dynamic provider/user source和internal FBO仍由frame精确门约束 | [straight RGB/alpha factor analyzer](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderFrontend/SceneAuthoredShaderStraightRGBAlphaFactorAnalyzer.swift)、[scalar-alpha analyzer](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderFrontend/SceneAuthoredShaderStraightRGBScalarAlphaAnalyzer.swift)、[whole-color filter analyzer](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderFrontend/SceneAuthoredShaderStraightWholeColorFilterAnalyzer.swift)、[layerSource launch color gate](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/MaterialProgram/SceneResolvedMaterialTextureResolver+LaunchColor.swift)、[Program color derivation](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/MaterialProgram/SceneResolvedMaterialProgram+ColorDerivation.swift)、[E-AUTHORED-STRAIGHT-RGB-ALPHA-FACTOR](runtime-evidence-index.md#e-authored-straight-rgb-alpha-factor)、[E-AUTHORED-STRAIGHT-RGB-SCALAR-ALPHA](runtime-evidence-index.md#e-authored-straight-rgb-scalar-alpha)、[E-AUTHORED-STRAIGHT-WHOLE-COLOR-UNORM](runtime-evidence-index.md#e-authored-straight-whole-color-unorm) | 普通`effectOutput`/多stage与internal FBO颜色传播、dynamic provider/user content fixed point、alpha replacement/additive/conditional、多个color slot、component mutation、dynamic division、current stock `PerformBlend`、其他helper/matrix与generic dependency仍关闭 | 三个公共形态的真实正证不升级arbitrary material或整样本；Pulse截图是开场相机上升阶段，display SceneScript/点击/shared mutation、Windows golden与官方视觉等价仍未闭合 |
| FBO declaration | `L2` | effect-scoped identity、独立 `width`/`height`/`fit`/`scale` 及 composed extent、format、unique、clear、UV、condition 均进入 `RenderTarget`；旧非 composed Codable 形态继续兼容 | `SceneGraphRenderTargetPlan/Table` 以 override -> fit -> scale -> floor/min-1 解析完整 extent，提供 lifetime、checked resident budget 与整组 Metal allocation；pool 已接 effect/plan/extent/format cache、LRU、原子替换和 reset，且整链先建候选后原子提交；显式 unique FBO 的读前写会记录 history seed；精确 transparent-zero authored clear 进入logical target，两者在首次创建或重建后由同一initialization transaction GPU clear，完成后普通下一帧不重复；BGRA backbuffer、RGBA8888与R8 FBO可分配，R8 current/history账本统一按1 logical B/texel，不等于device-specific physical footprint | precise/standard strict Blur与stock Local Contrast从table取得authored FBO。项目自有普通两material产品fixture进一步证明`uvs:nil`、无clear、非unique、单writer的R8 attachment只写red，以`.scalarRedUnorm/.preservedChannels`发布，并只交给launch envelope中所有实际消费该slot且可证明direct `.r`的已编译later variant，最终输出RGBA；普通无capability的R8 attachment仍拒绝。Workshop Shadow要求零authored FBO，但复用同一table的synthetic input/output texture | [clear normalization](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/GraphTargets/SceneGraphRenderTargetPlan+Clear.swift)、[extent resolver](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/GraphTargets/SceneGraphRenderTargetPlan+Extent.swift)、[target format](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/GraphTargets/SceneGraphRenderTargetFormat.swift)、[target plan](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/GraphTargets/SceneGraphRenderTargetPlan.swift)、[allocation table](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/GraphTargets/SceneGraphRenderTargetTable.swift)、[pool](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/GraphTargets/SceneOffscreenTexturePool.swift)、[graph target tests](../../../script/tests/test_scene_graph_render_target_plan.py)、[Metal table tests](../../../script/tests/test_scene_graph_render_target_table.py)、[R8 scalar graph tests](../../../script/tests/test_scene_resolved_material_graph_executor.py)、[E-GRAPH-R8-TARGET](runtime-evidence-index.md#e-graph-r8-target)、[E-GRAPH-R8-SCALAR-RED](runtime-evidence-index.md#e-graph-r8-scalar-red) | 只准string/array的精确四零值；非零、非四分量、非数字和非有限clear失败关闭。128 MiB限制提交后的pool resident transaction，不是瞬时驱动VRAM硬上限；R8 bounded atom不授权clear/history/copy/swap、错误通道、repeat、Glitter或arbitrary FBO产品准入，也不把Metal缺省GBA当语义 | 建FBO sampler/repeat与load-store合同后，再按完整stock topology验证Glitter；非零clear需先有颜色顺序、色彩空间和预乘语义门 |
| `target` | `L2` | 区分 layer source、effect output、effect-scoped framebuffer 和 unresolved；同名 FBO 不跨 effect 共享 | table 按完整 identity 映射且不 alias；每个 surface 由独立 pool 隔离，named layer target namespace 保持独立 | 现役 strict profile 已消费同一 table；God Rays 与 Shine 使用两张 scale=2 half RT，并按实际 extent 计入混合链预算；跨 effect 与 resize 不串用 | [target identity](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/AuthoredGraph/SceneAuthoredEffectRenderPlan.swift)、[target plan](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/GraphTargets/SceneGraphRenderTargetPlan.swift)、[pool 测试](../../../script/tests/test_scene_offscreen_texture_pool.py)、[GPU 测试](../../../script/tests/test_scene_framebuffer_capture.py) | 其他 authored target 不会分配或发布；named layer target 不能冒充 effect FBO/history | 让 generic scheduler 消费同一 identity/table，不新建旁路资源表 |
| `previous` / framebuffer material alias | `L3 bounded program` | 每个 effect 开始时固定为 chain input；同一 effect 内不会随 pass target 前进。现代`framebuffer`/`previous`及historical exact `ui_editor_properties_framebuffer`都可表达当前effect input，但历史token仅在regular hidden `g_Texture0` slot 0成立 | ordered strict stage programs已把前一effect output作为下一effect input；公共Program路径进一步让`1553008362`的Water Waves消费layerSource、三段Shake逐段消费前一effectOutput，并保持typed publication identity | 两个Blur与Local Contrast验证authored `previous`；Workshop Shadow锁定stage推进；historical alias synthetic正反门及155真实四段链证明相似editor key、label-only、缺hidden/错槽不会误准入 | [sampler schema](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/MaterialProgram/SceneResolvedMaterialShaderSchema+SamplerPurpose.swift)、[capability tests](../../../script/tests/test_scene_resolved_material_execution_capability.py)、[E-EFFECT-CHAIN](runtime-evidence-index.md#e-effect-chain)、[E-HISTORICAL-FRAMEBUFFER-MATERIAL-ALIAS](runtime-evidence-index.md#e-historical-framebuffer-material-alias) | historical alias只闭合当前effect input identity；original、scene background、full-frame alias、history、其他editor key与`1636394814`后继mixed-chain blocker仍未接 | 为剩余source kind建立显式identity/provider，不以label、路径或当前framebuffer猜语义 |
| `bind` | `L3 bounded atomic program` | sparse index、authored name、conditions 和 exact texture identity 均保留，slot 不压缩；Template 固定 8 槽并保存全部候选 provenance，Program 把最终 publication generation/purpose/content/physical/mapped/UV/sampler raw flags 与 active variant 同代冻结 | graph/asset/user/system source kind 已有 typed demand/readiness；只有 explicit absent 跳过较高优先级，missing/pending/unavailable/incomplete 不授权 fallback。bounded resolved Program 已直接编码被证明的 graph/asset bindings；Film Grain 正例把 slot 0 implicit framebuffer 与 slot 1 exact stock noise 的两个独立 publication atom 同时送入一个 Program | Blur/Water/Blend 等未迁移 strict consumer保持各自 GPU 正反门；Program-compatible Opacity/Tint/Film Grain 只开放各自已证 binding 子集，不把任意 material texture开放 | [binding resolver](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/AuthoredGraph/SceneAuthoredEffectRenderPlanner+Resolution.swift)、[Program texture resolver](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/MaterialProgram/SceneResolvedMaterialTextureResolver.swift)、[frame registry](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/SceneFrameTextureRegistry.swift)、[E-MATERIAL-PROGRAM](runtime-evidence-index.md#e-material-program)、[E-EFFECT-FILM-GRAIN](runtime-evidence-index.md#e-effect-film-grain) | 未证 regular asset purpose、dynamic system/video/variant lifecycle、effectOutput/FBO command publication 及其他 GPU consumer 仍缺 | R4 在唯一 executor 中直接消费 Program，不得在 renderer 重新选择候选或猜 purpose |
| `compose` | `L2` | raw value保留在node；generic `true`继续生成blocker。exact Blur Precise `[compose:true, material] -> [material]`由strict planner直接识别为0 authored/logical RT的layer-local two-member pair，不生成synthetic target | B8只消费layer source并在stage-local pair中推进；没有scene-behind-layer capture identity | 横纵pass匹配的exact `KERNEL=0/1/2` Blur Precise profile为受限`L3`；`2802243144:41/64/115`已由统一executor执行 | [planner](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/AuthoredGraph/SceneAuthoredEffectRenderPlanner.swift)、[compose 正反测试](../../../script/tests/test_scene_effect_render_graph.py)、[E-R4-B8-PRECISE-FULL-FRAME-COMPOSE](runtime-evidence-index.md#e-r4-b8-precise-full-frame-compose)、[E-GRAPH-LEGACY-COMPOSE（历史）](runtime-evidence-index.md#e-graph-legacy-compose) | generic compose、Refraction、scene background/captured-main与history未开放 | 后续若迁移Refraction，须先建立typed scene-background provider与capture时序 |
| `copy` | `L2` | 独立 command node，保留 source/target，校验 storage compatibility；不占 material ordinal | node scheduler 可在同一 command buffer 中按 authored nodeIndex 编码 Metal copy | Precise Blur 仅接受两个 descriptor 完全一致的非 unique BGRA target，并要求前后各一个 material node | [command planning](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/AuthoredGraph/SceneAuthoredEffectRenderPlanner.swift)、[node scheduler](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/GraphTargets/SceneGraphNodeScheduler.swift)、[E-GRAPH-INTERLEAVE](runtime-evidence-index.md#e-graph-interleave) | Motion Blur 的 copy 仍因完整 graph blocker 不执行，也不更新 history | 以合法真实 history consumer 闭合跨帧 copy 与 resize/switch/seek/stop |
| `swap` | `L2` | 独立 command node；要求 extent/format 相容，并额外比较 unique/UV/clear/condition | node scheduler 可在同一 command buffer 中交换 logical identity mapping，后段 material 读取交换后的 texture | Precise Blur 仅接受 destination `unique:true` 的白名单拓扑；其他 topology 拒绝 | [command compatibility](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/AuthoredGraph/SceneAuthoredEffectRenderPlanner+Resolution.swift)、[node scheduler](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/GraphTargets/SceneGraphNodeScheduler.swift)、[E-GRAPH-INTERLEAVE](runtime-evidence-index.md#e-graph-interleave) | mapping 不跨帧保存；Fluid 仍因 function/condition blocker 不执行 | 闭合跨帧 ownership、persistent lifecycle 与真实 history consumer |
| `condition` | `L2` | FBO/pass/bind 各位置保留 raw `SceneJSONValue` 并产生显式 blocker | 无表达式环境或逐帧 evaluator | 现役 strict profile 要求所有 condition 为 nil | [definition IR](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneEffectDefinition.swift)、[planner blocker](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/AuthoredGraph/SceneAuthoredEffectRenderPlanner.swift)、[graph 测试](../../../script/tests/test_scene_effect_render_graph.py) | 没有 AST、类型转换、combo/runtime variable 绑定或分支资源分析 | 从合法 fixture 建最小 condition AST/evaluator，并验证 false 分支不分配 RT/不占 material ordinal |
| `function` | `L2` | definition 顶层 functions 原样保留并阻断执行 | 无 live function executor 或 reset hook | 现役 strict profile 只接受 functions 缺失 | [definition loader](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneEffectDefinition.swift)、[保真测试](../../../script/tests/test_scene_effect_definition.py)、[planner blocker](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/AuthoredGraph/SceneAuthoredEffectRenderPlanner.swift) | Fluid reset 等函数不会运行 | 先确认 function schema、调用时机和资源副作用，再实现 resize/reset/initialization fixture |
| extent (`width`/`height`/`fit`/`scale`) | `L2` | 零操作、单操作与多操作分别规范化为 `input`、`scale`、`fit`、`absolute`、`composed`；四个 authored 字段分别保留，非数字、非有限或 `<=0` 为 `unsupported` | 同一 `pixelExtent` 先以显式 width/height 覆盖对应输入轴，fit 只缩小并保持当前比例，再除以 scale，最后 floor/min-1；preflight 以 2048² reference 调用同一算法，覆盖单轴、组合和 `scale<1` 放大 | 既有 strict profile 仍只接受各自 exact typed topology；新 composed IR 不会绕过完整 profile admission | [IR/resolver](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/AuthoredGraph/SceneAuthoredEffectRenderPlanner+Resolution.swift)、[pixel extent](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/GraphTargets/SceneGraphRenderTargetPlan+Extent.swift)、[effect graph tests](../../../script/tests/test_scene_effect_render_graph.py)、[target plan tests](../../../script/tests/test_scene_graph_render_target_plan.py)、[pool transaction tests](../../../script/tests/test_scene_offscreen_texture_pool.py)、[E-EFFECT-RT](runtime-evidence-index.md#e-effect-rt) | public extent 保真、allocation 与预算已闭合到 `L2`；尚无 strict backend 或真实样本正门消费 arbitrary composed FBO，不能表述为 generic FBO/Glitter 执行 | 以合法 consumer验证 format/sampler/load-store 与 resize 后重建，再按完整 material/pass profile 放行 |
| `format` | `L2` | 已识别 `r8`、`rg88`、`r16f`、`rg1616f`、`rgba8888`、`rgba_backbuffer`，未知值阻断；当前只有`r8`、`rgba8888`、`rgba_backbuffer`可进入typed target plan，`rg88`/`r16f`/`rg1616f`继续失败关闭 | 共用mapping精确映射`r8 -> .r8Unorm`、`rgba_backbuffer -> .bgra8Unorm`、`rgba8888 -> .rgba8Unorm`；synthetic input/output保持BGRA。R8 target的table/persistent allocator/lease与current/history logical accounting使用同一storage identity与1 logical B/texel，resource encoder只开放exact-format clear/copy；RGBA/BGRA按4 logical B/texel。R8 graph publication使用独立`.scalarRedUnorm` content与`.preservedChannels` purpose，`authoredFormat`保持nil；capability把exact attachment kind带到executor，PassEncoder对R8使用`.red` write mask，resolver/Program只接受launch envelope中所有实际消费该slot的已编译variant的immediate direct `.r` use | strict Blur执行`rgba_backbuffer`；stock Local Contrast的两个quarter target执行`rgba8888`。项目自有GPU fixture证明R8 allocation/resource-op基础，以及一条普通`Program -> R8 -> explicit .r Program -> RGBA terminal`产品原子；distinct BGRA输入的logical red 64被R8保存并在terminal得到BGRA`[0,0,64,255]`。普通无typed authority的R8 Program target、`.g`/whole/alias与混合channel envelope均拒绝 | [format gate](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/AuthoredGraph/SceneAuthoredEffectRenderPlanner+Resolution.swift)、[format mapping](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/GraphTargets/SceneGraphRenderTargetFormat.swift)、[attachment storage](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/EffectExecution/SceneResolvedMaterialAttachmentStorage.swift)、[publication](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/GraphTargets/SceneGraphRenderTargetLease+Publication.swift)、[channel analyzer](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderFrontend/SceneAuthoredShaderTextureChannelAnalyzer.swift)、[graph executor tests](../../../script/tests/test_scene_resolved_material_graph_executor.py)、[E-GRAPH-R8-TARGET](runtime-evidence-index.md#e-graph-r8-target)、[E-GRAPH-R8-SCALAR-RED](runtime-evidence-index.md#e-graph-r8-scalar-red) | bounded scalar atom不证明sampled G/B/A缺省、arbitrary swizzle/vector use、sampler wrap、stock shader、Glitter、Windows像素或跨backend等价；1 logical B/texel不是Metal heap/driver physical allocation。clear/history/copy/swap/multiwriter/no-consumer与`rg88`/`r16f`/`rg1616f`尚无对应Program atom | 闭合FBO sampler/repeat、load/store与可见输出，再按完整Glitter topology验收；其余格式逐项验证通道、精度、filter/write、budget与合法consumer |
| `clear` | `L2` | string/array 四分量被校验并保真 | 当前 offscreen pass 固定透明清屏，与 authored clear 无关 | 三个 authored-FBO profile 要求 clear 为 nil；Workshop Shadow 要求 RT 列表为空 | [clear validation](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/AuthoredGraph/SceneAuthoredEffectRenderPlanner+Resolution.swift)、[畸形值测试](../../../script/tests/test_scene_effect_render_graph.py)、[fixed clear backend](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneOffscreenEffectRenderer.swift) | 不能把“backend 总是 clear”记成 authored clear 支持；persistent RT load/reset 未定义 | 将 clear 映射到 load action 与生命周期事件，覆盖首次分配、每帧、reset 三种时机 |
| `unique` | `L2` | bool 声明和 effect-scoped identity 已保留；非 bool 阻断 | 没有按 unique 管理实例/跨帧资源；所有 authored FBO identity 本就含 effect key | 三个 authored-FBO profile 只接受 false；Workshop Shadow 要求 RT 列表为空 | [definition/graph IR](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/AuthoredGraph/SceneAuthoredEffectRenderPlan.swift)、[copy/unique 测试](../../../script/tests/test_scene_effect_render_graph.py)、[strict 负向测试](../../../script/tests/test_scene_authored_effect_execution.py) | `unique` 不等于 history，当前也不会让资源跨帧常驻 | 以 read-before-write/copy/swap/function 数据流判定 persistence，再让 unique 只控制实例隔离 |
| FBO `UV` / wrap | `L2` | raw IR识别并保留`uvs:repeat`，其他值阻断；当前typed target descriptor仍只接受`uvs:nil`，所以repeat在产品capability的`admitted-graph-structure`阶段失败关闭 | image texture/mask 有独立 UV transform，但不是 FBO sampler contract；当前graph publication仍固定`.directImageFallback`/linear-clamp | 三个既有authored-FBO profile与本批R8 scalar atom都要求uvs为nil；Workshop Shadow要求RT列表为空 | [UV gate](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/AuthoredGraph/SceneAuthoredEffectRenderPlanner.swift)、[typed target descriptor](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/GraphTargets/SceneGraphRenderTargetPlan+Extent.swift)、[R8 repeat负门](../../../script/tests/test_scene_resolved_material_graph_executor.py) | raw token被识别不等于sampler address mode已执行；不能用image texture的repeat合同外推FBO，也不能由stock字段猜filter/edge/rounding | 建typed FBO sampling identity并以UV越界的RGBA/R8 GPU fixture验证normalized S/T repeat；未知值及与history/swap不一致的sampling identity继续失败关闭 |

## 4. 官方 Shader 六页合同

以下六个 anchor 与 [官方逐页映射](official-page-map.md) 一一对应。这里记录官方公开语义，不把编辑器操作步骤误算成运行时能力。

<a id="op-shader-overview"></a>
### 4.1 Overview

| 官方合同 | 当前级别 | 当前边界 / 实现门 |
|---|---|---|
| effect shader 使用类 GLSL source，并在需要时翻译为 HLSL | `L3 子集` | ShaderContract 保真后，中性 preparation/frontend 可把 bounded framebuffer/单源颜色、host/dynamic uniform 与 texture schema 编译成完整 Program；现役 GPU owner 是 resolved-material graph executor。三组 exact Scroll 只作历史 oracle，不再通过 profile 推断独立 owner；include/combo、任意 material texture、复杂 state/FBO/多 pass 与 arbitrary source 仍不能外推为官方完整兼容。见 [E-SHADER-PREPARATION](runtime-evidence-index.md#e-shader-preparation) 与 [E-AUTHORED-SHADER](runtime-evidence-index.md#e-authored-shader)。 |
| 官方向 effect shader 提供向后兼容承诺 | `L0` | MyWallpaperX 只有 bounded Program/executor 子集，尚不能承接官方的通用向后兼容承诺；剩余 strict MSL 也不能冒充 source compatibility。 |
| 3D 自定义 shader 必须兼容 `generic2.vert` / `generic2.frag` | `L0` | 无 3D model/runtime，也未取得这两个 stock contract 的合法本地 schema。 |
| 不应替换 particle 等 system shader；官方不保证其向后兼容 | `L0` | 产品门：即使未来能读取，也必须默认拒绝 system-shader override，不能把它纳入首期 arbitrary shader。 |

<a id="op-shader-syntax"></a>
### 4.2 Syntax

| 官方合同 | 当前级别 | 当前边界 / 实现门 |
|---|---|---|
| custom preprocessor 支持布尔/逻辑条件，但不是完整 C preprocessor；官网移动端指南不承诺 `#elif` | `L2 bounded preparation` | 有界 evaluator 支持 object-like `#define/#undef`、普通/零参数 function-like macro、嵌套调用、宏实参、object alias、`#if/#ifdef/#ifndef/#elif/#else/#endif`、按 relational 高于 equality 的整数/布尔表达式与 include 展开；`#elif` 只按同一 conditional frame 的首个命中分支执行，unmatched/empty/after-else 失败关闭。macro expansion 识别 code/string/character/line-comment/block-comment，selected/host macro 保留 defined/undefined 所有权。只让 active 行贡献诊断和 metadata，并保留逐行 source map、依赖 hash、输入/输出/深度/token/字节预算。variadic、stringize/paste、递归、引号、跨行 replacement、未知/畸形 directive、directive-line annotation、循环/缺失/冲突 include 均失败关闭；combo/environment 不能注入 token sequence。该语料兼容子集不改变官网对移动端作者写法的限制，也不自动扩大 GPU admission。 |
| 环境 define：`GLSL`、`HLSL`、`HLSL_SM40`、`HLSL_GS40` | `L1 recognized/fail-closed` | 名称已归为 host-owned backend-language requirement 并进入稳定诊断边界；Metal backend 尚无已验证 provider，material/authored source 不能伪造这些值，因此命中时仍失败关闭，也不形成可执行 environment variant。 |
| 跨语言函数：`texSample2D`、`texSample2DLod`、`mix`、`frac`、`saturate`、`atan2`、`ddx`、`ddy` | `L3 bounded subset` | frontend已逐符号翻译真实Program所需的sampling、mix/frac、常用数学与有界helper。built-in `texSample2DLod`只接受直接sampler identifier、既有可编译UV表达式与单个number或唯一静态可证float identifier，并在vertex/fragment stage生成Metal `sample(..., level(...))`；user-defined同名函数不重写，错参数数、间接/未知sampler、vector/matrix/compound或歧义LOD继续以`unsupportedSampler`失败关闭。`mix/lerp`只在确认为built-in、前两参可由有界typed parser全量消费且weight为scalar或同宽向量时才缩到共同最小宽度。parser接受直接identifier/swizzle/number/grouping、`*`/`/`最小宽度链及已同宽的`+/-`包络，只缩窄可直接证明的较宽叶子；user-defined、call/constructor/sample/index/ternary、整数/matrix、错宽weight及compound较宽侧不猜测并继续失败关闭。`inverse`只接受静态typed `mat3`并翻译到项目自有3×3 cofactor helper，其他matrix/scalar/vector形态在compile前失败关闭。未实现符号仍拒绝，不能把整行函数集合读成全支持。 |
| 构造/转换：`CAST2`、`CAST3`、`CAST4`、`CAST3X3` | `L2/L3 bounded` | macro/prelude与typed constructor已有受限处理；HLSL浮点向量隐式缩窄只对静态可证乘除链、赋值/参数目标，以及上述built-in `mix/lerp`有界typed实参开放；函数参数只接受简单value-type `inout name`并翻译为Metal thread reference。程序域静态`const`转换为Metal `constant` address-space，函数局部`const`不变，runtime-dependent或mutable global仍由既有声明/Metal门拒绝。数组/畸形`inout`、user-defined调用、未证复合参数、其他隐式转换与未证CAST形态失败关闭。 |
| HLSL-style float modulo operator `%` | `L3 bounded statement` | 仅简单标量声明赋值、目标与左右操作数均可静态证明为float/int/uint且至少一侧为float时，显式翻译为`fmod`后按目标构造；纯整数`%`保持原生。括号复合左值、向量、表达式链、歧义声明与其他目标继续由Metal拒绝，不做语料/path分派。 |
| legacy stage IO：`attribute`、`varying`、`uniform`、`gl_FragColor` | `L3 bounded 2D effect` | frontend已对2D image-effect的position/texcoord、typed varying、stage-qualified uniform ABI与单fragment output做link并由Program执行；其他layout/interpolation/MRT关闭。 |
| image/text vertex 只用 `a_Position`、`a_TexCoord`；完整输入集合还包括 `a_PositionVec4`、`a_Normal`、`a_Tangent4`、`a_TexCoordVec3/Vec4`、`a_TexCoordC1...C5` 的 vec2/vec3/vec4 变体及 `a_Color` | `L0` | 自有 quad/particle layout 不算 authored attribute；需按对象类型注册 vertex source。 |

<a id="op-shader-variables"></a>
### 4.3 Variables

| 官方合同 | 当前级别 | 当前边界 / 实现门 |
|---|---|---|
| `[COMBO]` checkbox/options：`material`、`combo`、`type`、`default`、`options` | `L2 bounded preparation` | exact case-sensitive `[COMBO]` 已形成 typed schema；annotation 数值以 Decimal-first/exact Int64 IR 解析，Template只保留material显式值，active schema在下游按“显式优先，否则有效且一致default”形成统一macro environment与provenance；options无论最终取显式/default都完整验证。JSON `require/requireany`保真为authoring/editor关系metadata，不参与player compile-map剪枝；sampler readiness/format的resource requirement另经bounded fixed point形成active variant。底座只含词法无条件 root/include schema，最多8个条件候选会穷举所有非空子集；多个stable prepared signature报`active-schema-ambiguous`，候选数或估算累计工作超过256 MiB报`active-schema-audit-budget`。严格JSON失败的metadata保留raw text与`malformedAnnotation`诊断且不形成typed record；只有声明外comment-only `[COMBO]`能证明root stage存在词法无条件、同名且除坏`options`外顶层字段完全相同的合法record时才跳过，附着声明、conditional/conflicting/missing counterpart、所需default缺失/冲突schema及其他fatal diagnostic仍失败关闭。disabled marker与坏最终schema失败关闭，prepared variant尚不授权GPU permutation。 |
| General：`g_Time`、`g_Daytime`、`g_Frametime`、current/last pointer、texel/half-texel、screen、alpha/color/color4、parallax | `L3 bounded` | Program ABI 已按官方名称编码 time/daytime/frametime、current/last pointer、screen、texel/half-texel 与 MVP 的已证子集；alpha/color/color4、parallax、完整单位/作用域和更新频率仍未闭合，逐项状态见下表。 |
| View：eye/view/orientation 三轴、model/inverse、view-projection、model-view-projection/inverse | `L2` | 部分自有矩阵存在；缺官方坐标系、转置、inverse 和 object/surface scope 合同。 |
| Effect/Layer：effect model/MVP/inverse、effect texture projection/inverse、layer model | `L3 bounded 2D effect/layer matrices` | Program host schema已对当前2D effect提供effect texture projection及inverse；另以exact `float4x4 g_LayerModelMatrix`消费同帧`worldFramesByLayerID`，缺失即失败关闭。Iris/Depth/Light Shafts消费前者，`3768724269:163` rounded-mask消费后者并取得GPU/publication/compositor/next-frame与ROI正证。其他effect model/MVP/inverse、geometry-size/pivot组合、任意3D空间及官方/Windows矩阵逐值仍未闭合。 |
| 每个 `T0...T7`：`g_TextureNResolution.xy` physical / `.zw` mapped、Rotation、Translation | `L3 bounded` | 共享 slot-binding atom 固定表达 authored `0...7` 与 hole，并原子携带 candidate identity/generation、purpose、physical/mapped、完整 UV、sampler、format、mip/resolution/texel；完整 Program 可按 active slot 把 `g_TextureNResolution` 编入 uniform bytes 并由 resolved executor 消费。Rotation/Translation、未证 purpose/readiness、复杂 UV 与任意 sampler 仍拒绝。 |
| Audio：16/32/64 bin、left/right、低到高、正值且不归一化 | `L3 bounded` | 同一次 FFT 已建立 16/32/64 left/right host snapshot，静音/暂停/无 consumer 有明确策略；六组 `g_AudioSpectrum{16|32|64}{Left|Right}` 数组已进入 bounded Program ABI，三个Simple Audio Bars与Workshop Audio Hue正例均由实际Program uniform consumer驱动采集，stock Shake/Pulse及其他未迁移形态仍有各自bounded consumer。尚无通用 audio shader 准入、SceneScript `AudioBuffers` 或官方数值 parity；当前 32 Hz→16 kHz、-60 dB 映射与平滑只是工程选择，见 [E-AUDIO-INPUT](runtime-evidence-index.md#e-audio-input)。 |
| sampler `g_Texture0...7` annotation：`material`、`label`、`hidden`、`default`、`mode`、`combo`、`paintdefaultcolor` | `L2 bounded combo/readiness slice` | sampler declaration同行的`combo/require/requireany`已形成独立的resource readiness/format typed schema并保持slot`0...7`；这类require仍参与项目bounded resource fixed point，不受authored `[COMBO]` editor metadata结论影响。其余editor/default/mode/paint metadata仍只是loss-preserving JSON，不能当成完整sampler annotation AST。 |
| paint mode：`opacitymask`、`rgbmask`、`flowmask`；flow idle 以 127/255 为中心；optional texture 仅在实际绑定时定义 combo | `L2 readiness decision only` | optional texture combo 只有在同代 slot readiness 明确可用/不可用时才产生带 slot provenance 的 variant；bounded Program 会原子消费已证 candidate generation，readiness 缺失保持 unknown 并失败关闭。通用 mask 解码、paint metadata 与更多 purpose 仍未闭合。 |
| user uniform：slider (`default/range`)、color (`type=color`)、UV (`position=true`)；名字应以 `u_` 开头 | `L3 bounded` | reflected layout 已保存 constant 与 typed `shaderValue` dynamic uniform；唯一 property kind/fallback shape 的 scalar/vector3 可经 snapshot 编入 Program bytes。完整 editor schema/default/range、UV position、任意 user/script binding 与多 producer 冲突仍关闭。 |

<a id="op-shader-headers"></a>
### 4.4 Headers

| 官方模块 | 公开合同 | 当前级别 / 实现门 |
|---|---|---|
| `common.h` | `M_PI`、`M_PI_HALF`、`M_PI_2`、`SQRT_2`、`SQRT_3`；`hsv2rgb`、`rgb2hsv`、`rotateVec2`、`greyscale` | `L0`：R2 source graph 可定位并展开 header 字节，但没有这些常量/函数的 typed 跨语言语义、数值 golden 或 GPU consumer；资源准备不能抬高具体 helper 能力。 |
| `common_fragment.h` | `DecompressNormal`、`DecompressNormalWithMask`、`ConvertSampleR8`、`ConvertTexture0Format` | `L0`：本批只以typed graph scalar content与显式direct `.r` consumer闭合Metal R8原子，不实现、替代或绕过`ConvertSampleR8`/`ConvertTexture0Format`；format helper、非red分量、缺省GBA与跨后端通道适配仍未开放。 |
| `common_vertex.h` | 两种 `BuildTangentSpace` | `L0`：依赖完整 vertex attribute、model transform 与 handedness。 |
| `common_blending.h` | `BLENDMODE` imageblending combo + `ApplyBlending(BLENDMODE, colorA, colorB, blend)`；首参为兼容性保留 | `L3 bounded`：32 个模式已按官方 header 源码自研为共享 Metal shader 源（`SceneBlendModeShaderSource`），逐模式对独立 numpy 重算做过 GPU 交叉验证，含 mode 5/10 忽略 opacity、mode 31 为加法而非 mix、mode 0 强制 alpha=1 三条偏离；Program-compatible Tint 与 Film Grain stock/no-mask正例经 source graph/preparation、authored frontend、Program与resolved pass executor消费active helper。B17后旧dedicated effect backend只剩像素单测/R5 surface，无claim可见effect不会由产品planner恢复；layer color blend仍是独立于Effect权限的结构性compositor步骤。Tint、Film Grain与Blend都不是因此自动开放的generic leaf；它不授权其他Effect、任意include/helper/revision或Windows像素等价。 |

<a id="op-shader-mobile"></a>
### 4.5 Mobile

| 官方合同 | 当前级别 | 当前边界 / 实现门 |
|---|---|---|
| 目标为 DirectX 11 HLSL 4.0 与 OpenGL ES GLSL 3.00 ES；官方会自动改写 source | `L0` | MyWallpaperX 目标是 Metal，不宣称移动端编译 parity；但 source parser 必须保留这些条件分支。 |
| 类型 alias：`vec2/3/4`、`mat2/3/4x3/4`；函数 alias 另含 `mul`、`mod`、`fmod` | `L0` | 与 Syntax 中 alias 一并进入可版本化 symbol table。 |
| 整数字面量默认尝试改成 float；`WEMOBILE_DISABLE_INTEGER_CONVERSION` 可关闭 | `L0` | 不能靠文本替换；需 token-aware rewrite 和 before/after fixture。 |
| 平台分支用独立 `#ifdef HLSL` / `#ifdef GLSL`；`#elif` 不支持，`#else` 对未来 API 不稳 | `L0` | preprocessor 需与官方限制一致并输出明确诊断。 |
| 移动端编译失败时移除所属 effect，壁纸继续运行 | `L0` | 对应 MyWallpaperX 的产品策略应是 per-effect fail-closed passthrough，而非整幅 wallpaper 黑屏。 |

<a id="op-shader-desaturation"></a>
### 4.6 Desaturation Tutorial

官方教程给出了首个 generic shader executor 的最小合法 fixture：hidden framebuffer `g_Texture0`、`v_TexCoord`、`texSample2D`、`CAST3(dot(...))`、`gl_FragColor`，再加入 `u_DesaturationAmount` slider 与 `mix`。当前为 `L0`：现有色彩近似/手写 pipeline 没有执行这份 authored source。只有这份 fixture 能经 source parse、annotation、material/slot、variant、uniform upload、GPU 编译和像素门端到端通过后，才能把对应 primitive 升级。

## 5. 当前 Shader primitive 覆盖

| 语义项 | 等级 | IR / host carrier | generic runtime | strict executor | 代码/测试证据 | 执行边界 | 下一门 |
|---|---|---|---|---|---|---|---|
| GLSL-like source | `L3 bounded` | ShaderContract 安全保留完整 UTF-8 source、raw SHA-256、stage path/kind 与 canonical identity | frontend 对静态有界控制流和受支持 stage/uniform 子集做 typed link、翻译；preparation、reflection、texture/provider/state/color 必须原子形成 Program 后才可准入 | bounded Program 由 resolved-material pass encoder 编译并在统一 graph executor 执行；旧独立 authored-shader 与 exact Scroll owner 已撤权，Blur 等尚未迁移的 strict profile 仍调用手写 MSL | [contract](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderContract/SceneShaderContract.swift)、[frontend](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderFrontend/SceneAuthoredShaderFrontend.swift)、[preparation](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderPreparation/SceneAuthoredShaderPreparation.swift)、[pass encoder](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/EffectExecution/SceneResolvedMaterialPassEncoder.swift)、[frontend tests](../../../script/tests/test_scene_authored_shader_frontend.py)、[resolved execution tests](../../../script/tests/test_scene_resolved_material_execution_capability.py)、[E-AUTHORED-SHADER](runtime-evidence-index.md#e-authored-shader) | bounded Program 子集不等于通用 GLSL；未注解 sampler、未证 helper、动态控制流、更多语法/state 与缺失 provider 继续拒绝；旧 GPU 门只是历史 oracle | 逐项扩 directive/token frontend、source map 与 typed stage contract，每项保持正反 GPU 门并同批撤销被替代 owner |
| HLSL / DirectX blob | `L1` | 能发现 `blobssm/*.dxs` 并报告数量 | 无 DXIL/DXBC 反射、HLSL frontend 或 Metal translation | 无 strict HLSL executor | [blob 分类](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/SceneResourceIndex.swift)、[未实现诊断](../../../MyWallpaperX/Core/SteamWorkshopScene/Runtime/SceneDiagnostics.swift) | 不运行或捆绑不可信 `dxc`；blob 存在只算诊断 | 先确定合法 source 优先级和 blob metadata；任何编译器路线另做签名、许可、缓存和 sandbox 设计 |
| preprocessor | `L2 bounded preparation` | directive、条件表达式、object-like token sequence、bounded function-like macro、include edge、active annotation/declaration 与 source map 均为 typed carrier | 有界展开形成 dependency/variant/prepared SHA；Program 可原子消费已证明的 prepared reflection/slot/provider/state/color，host-owned 环境值未证仍失败关闭 | Film Grain与当前Light Shafts正例已消费 source graph、条件分支和active helper后进入GPU；多数其他prepared source与strict MSL owner仍不能借此迁移 | [preprocessor](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderPreparation/SceneShaderPreprocessor.swift)、[macro expander](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderPreparation/SceneShaderMacroExpansion.swift)、[variant resolver](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderPreparation/SceneShaderVariantResolver.swift)、[preprocessor tests](../../../script/tests/test_scene_shader_preprocessor.py)、[variant tests](../../../script/tests/test_scene_shader_variant_environment.py)、[E-SHADER-PREPARATION](runtime-evidence-index.md#e-shader-preparation)、[E-R4-3-AUTHORED-PROGRAM-CHAIN](runtime-evidence-index.md#e-r4-3-authored-program-chain) | preparation/Program PASS都不是任意Metal compile/GPU support；variadic、stringize/paste、递归、未知directive与未证环境define关闭 | 继续公共dependency ownership、typed texture binding与跨语言helper，闭环后才撤对应旧owner |
| 跨语言宏 / helper translation | `L3 bounded` | source token、active helper、stage declaration与typed syntax unit可保留；frontend逐符号翻译已证 `texSample2D`/`texture2D`、`mix`、`frac`、常用数学、`mul`、`mat3 inverse`及有界构造，并解析简单value-type `inout` | 只对静态有界控制流、可唯一解析类型与已登记helper形成Metal source；浮点向量隐式缩窄仅支持静态可证乘除链、赋值和函数参数目标；float `%`仅支持整句简单标量声明赋值并显式转`fmod`，其他转换、compound/vector modulo、matrix宽度与数组/畸形`inout`失败关闭 | Film Grain、`3766387484`九段链、layer 80 Light Shafts与`3299228616:151` relocated Audio Bars已执行authored helper/source；未迁移strict MSL仍绕过此能力 | [frontend](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderFrontend/SceneAuthoredShaderFrontend.swift)、[function semantics](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderFrontend/SceneAuthoredShaderFunctionSemantics.swift)、[vector conversion](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderFrontend/SceneAuthoredShaderVectorConversion.swift)、[frontend tests](../../../script/tests/test_scene_authored_shader_frontend.py)、[E-R4-3-AUTHORED-PROGRAM-CHAIN](runtime-evidence-index.md#e-r4-3-authored-program-chain)、[E-WORKSHOP-SIMPLE-AUDIO-BARS](runtime-evidence-index.md#e-workshop-simple-audio-bars) | bounded符号集合与少量HLSL转换形态不等于通用GLSL/HLSL兼容；歧义类型、动态/递归控制流和未知helper关闭 | 只按合法语料逐语义扩展并补正反GPU门，不按effect/path选择translator |
| `combo` / permutation | `L2；Opacity/Light Shafts bounded L3` | material/instance explicit值、exact active `[COMBO]` default/options与sampler readiness/format schema形成带provenance的resolution；authored `require/requireany`只保真为editor metadata | normalized variant key、active source/schema与resource bounded fixed-point已有；同一resource snapshot以最终readiness重算并把active variant/reflection写进Program identity。Template保持explicit-only；resolver先验证全部active同名声明，显式值优先，否则要求每条声明都有同一个有效default，再把最终值同时供给条件、definedness、普通code与variant/prepared identity。missing-needed、冲突、畸形、options不匹配均失败关闭 | 只有其余Program合同同时闭合的bounded variant可进入resolved executor；Opacity与当前Light Shafts正例已消费Program能力，exact stock Standard Blur以独立typed graph-stage adapter消费既有strict combo，其余Standard Blur形态仍只消费旧白名单或失败关闭 | [material resolver](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneAuthoredMaterialResolver.swift)、[variant resolver](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderPreparation/SceneShaderVariantResolver.swift)、[preprocessor tests](../../../script/tests/test_scene_shader_preprocessor.py)、[E-SHADER-PREPARATION](runtime-evidence-index.md#e-shader-preparation)、[E-MATERIAL-PROGRAM](runtime-evidence-index.md#e-material-program)、[E-R4-3-AUTHORED-PROGRAM-CHAIN](runtime-evidence-index.md#e-r4-3-authored-program-chain) | preparation success不等于GPU permutation；purpose缺失/歧义、host-owned define、unknown ordinary identifier或unsupported mixed chain均失败关闭 | 继续扩大经证实的variant/provider/state子集并同批撤销对应旧owner |
| shader annotation | `L2 bounded schema slice` | marker、行号、raw/structured JSON与exact numeric保真；exact combo/default/options、sampler readiness/resource requirement、公开`opacitymask/rgbmask/flowmask` mode及`material` lookup key进入active schema | 只让active非directive行参与Program schema；Template仍只保存material显式值，annotation default在下游compile environment生效。`material`只是key，不是purpose，numeric `format`不证明normal/depth；authored `require/requireany`属于editor关系，不删除player声明。唯一逐项登记的stock语义仍是exact `util/noise -> .noise`，声明purpose与registry冲突时unproven | Film Grain stock/no-mask与当前Light Shafts正例已让各自exact default进入Program；其他default/path不自动扩大GPU准入 | [contract loader](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderContract/SceneShaderContractLoader.swift)、[material schema](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/MaterialProgram/SceneResolvedMaterialShaderSchema.swift)、[stock registry](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/SceneStockTextureSemanticRegistry.swift)、[E-MATERIAL-PROGRAM](runtime-evidence-index.md#e-material-program)、[E-EFFECT-FILM-GRAIN](runtime-evidence-index.md#e-effect-film-grain) | typed slice不是完整editor/runtime schema；directive-line placement与普通asset purpose没有官方证据，filename/path pattern继续显式关闭 | R4只消费完整Program；新增mode/default/stock identity必须有公开或clean-room语义和正反门 |
| uniform / `constantshadervalues` | `L3 bounded；generic carrier L2` | number/vector/string/binding raw value与components保留，Template区分static、property、Timeline与SceneScript producer；作者名与stage独立保存 | Program按active reflection编码host/static/dynamic bytes；stage ABI、host schema与producer冲突门保持。B1允许Program失败段在pair-only/readiness闭合时使用typed adapter，已有authored graph整图拒绝仍中和legacy inputs；B17又使所有未认领可见effect失败关闭 | bounded Program与adapter都由统一executor编码；B12/B13只闭合两种exact cross-layer dependency。B21后fixed script/source与numeric Workshop runtime dispatch均归零，普通/Workshop Effect Audio Bars继续通过host uniform消费共享频谱；B22又撤销独立ImageBlend property execution授权，generic dependency仍保持失败关闭。产品planner引用已归零，3个正式diagnostics/disposition观察调用留在R5 surface | [host schema](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/MaterialProgram/SceneResolvedMaterialHostUniformSchema.swift)、[Program uniform encoder](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/MaterialProgram/SceneResolvedMaterialUniformEncoder.swift)、[resolved live target](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/EffectExecution/SceneResolvedMaterialExecutionCapability.swift)、[E-R4-B22-INDEPENDENT-IMAGE-BLEND-RETIREMENT](runtime-evidence-index.md#e-r4-b22-independent-image-blend-retirement) | 任意uniform array、SceneScript VM、更多dynamic producer与其余官方built-in单位未完成；普通authored Blend不外推generic dependency | 每个新dynamic/host来源同批补schema、snapshot、ABI冲突、GPU门与被替代owner removal |
| include / header | `L3 bounded` | 独立 source graph 保存 package/loose/stock 候选、provenance、冲突、edge 与内容 hash，不改旧 raw stage projection | 有界 include expansion、dependency graph/hash、cycle/missing/conflict、source map 与 cache identity 已有；Program 可重编已证 active source | Film Grain stock/no-mask 正例已让 `common.h`/`common_blending.h` 的 active source/helper 经 frontend、Program 与统一 executor 编码；未知 include/helper、其他 revision 和未证 state/resource/color仍拒绝 | [source graph](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderContract/SceneShaderSourceGraph.swift)、[source resolver](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/SceneShaderSourceResolver.swift)、[preprocessor](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderPreparation/SceneShaderPreprocessor.swift)、[E-SHADER-PREPARATION](runtime-evidence-index.md#e-shader-preparation)、[E-MATERIAL-PROGRAM](runtime-evidence-index.md#e-material-program)、[E-EFFECT-FILM-GRAIN](runtime-evidence-index.md#e-effect-film-grain) | package -> loose -> stock 是项目 VFS policy；一个 exact Program 正例不等于任意 helper translation 或 GPU 支持 | 每个新 header/helper 逐符号补 typed translation、数值门与负例；R5 退役旧 root projection 后再删除 adapter |
| vertex attribute | `L3 bounded 2D quad` | authored attribute declaration、type/name/array shape、stage与行号已保存 | frontend把已证`a_Position`/`a_TexCoord` image-effect layout链接到统一full-target quad，Program/pass encoder实际提供buffer；其他attribute拒绝 | `3766387484`九段链的authored vertex source已执行；3D/particle/custom layout仍走领域runtime或关闭 | [contract loader](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderContract/SceneShaderContractLoader.swift)、[frontend](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderFrontend/SceneAuthoredShaderFrontend.swift)、[E-R4-3-AUTHORED-PROGRAM-CHAIN](runtime-evidence-index.md#e-r4-3-authored-program-chain) | 仅证明2D effect quad子集，不开放normal/tangent/color或多texcoord布局 | 按对象draw packet逐类注册layout并补stage-link/GPU方向门 |
| varying / fragment output | `L3 bounded` | authored varying与`gl_FragColor` type/name/stage被typed frontend链接；同名跨stage uniform另用stage-qualified ABI | frontend只接受唯一可证stage link、支持的varying shape与保守color contract；未知插值/多输出拒绝 | `3766387484`九段vertex→fragment varying和最终premultiplied output均经Program执行 | [frontend](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderFrontend/SceneAuthoredShaderFrontend.swift)、[color analyzer](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderFrontend/SceneAuthoredShaderColorTransferAnalyzer.swift)、[E-R4-3-AUTHORED-PROGRAM-CHAIN](runtime-evidence-index.md#e-r4-3-authored-program-chain) | 不开放任意location/interpolation/MRT、未知颜色空间或Windows pixel parity | 扩展前先建立typed output attachment与逐像素正反门 |
| shader input/output color representation | `L3 bounded resolved execution` | carrier可表达`opaque/straight-alpha/premultiplied-alpha`与unresolved；公共 analyzer 只证明单源 root-local direct sample、可证 alpha 改写或 alpha=1 opaque construction | 其他表达式、RGB 变换、支配输出的控制流、多/未知 graph color 或不匹配 state 均 fail closed；launch capability 在 texture binding 后拒绝 unresolved color transfer | R4 resolved executor 对 bounded straight-alpha 子集在 slot 入口 unpremultiply、fragment 出口 premultiply 并完成 GPU/compositor 证据；其他 strict owner 仍保留 | [color analyzer](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/ShaderFrontend/SceneAuthoredShaderColorTransferAnalyzer.swift)、[color tests](../../../script/tests/test_scene_shader_color_contract.py)、[E-R4-1-STRAIGHT-ALPHA](runtime-evidence-index.md#e-r4-1-straight-alpha) | 不证明任意 shader alpha 数学、blend factor/write mask、linear/sRGB 或 Windows pixel parity | 以项目自有像素正反门逐形态扩展，未证形态继续 fail closed |
| built-in time (`g_Time`/`g_Daytime`/`g_Frametime`) | `L3 bounded` | `SceneFrameContext` 有 monotonic scene/frame time 与 wall date，官方 Variables 页定义 `g_Time` 为程序运行秒数 | Program host-uniform schema 按 exact 名称编码；未知类型/名字失败关闭 | Film Grain stock/no-mask 正例实际以 `g_Time` 驱动两组 noise UV 并经统一 executor 编码；其他两个名称和更多 owner仍未迁移 | [frame context](../../../MyWallpaperX/Core/SteamWorkshopScene/Runtime/SceneFrameContext.swift)、[clock 测试](../../../script/tests/test_scene_frame_context.py)、[E-EFFECT-FILM-GRAIN](runtime-evidence-index.md#e-effect-film-grain) | 一个时变正例不等于 `g_Time` family parity；daytime、pause/seek/reset、跨 surface 起点与 Windows 同相位时序未闭合 | 为每个名称锁单位、更新频率、pause/seek/reset 与跨 surface lifecycle，并补确定性 clock/Windows 门 |
| built-in pointer (`g_PointerPosition*`) | `L3 bounded` | frame context独立携带current/previous与camera parallax；effect projection正逆矩阵进入同一Program host schema | image-effect Program按WE UV约定编码current/last，作者vertex可用inverse matrix转到effect local；Camera Parallax关闭只归零parallax，不覆盖raw pointer | Workshop Iris真实source已取得双眼方向门；legacy Cursor Ripple仍消费其独立effect-local输入 | [frame context](../../../MyWallpaperX/Core/SteamWorkshopScene/Runtime/SceneFrameContext.swift)、[layer cursor geometry](../../../MyWallpaperX/Core/SteamWorkshopScene/Rendering/SceneLayerCursorGeometry.swift)、[context 测试](../../../script/tests/test_scene_frame_context.py)、[E-R4-3-AUTHORED-PROGRAM-CHAIN](runtime-evidence-index.md#e-r4-3-authored-program-chain) | 当前只闭合一个image-effect projection与hover/outside路径；其他对象、last-pointer时序和Windows坐标仍未证 | 为不同layer transform/crop/rotation与previous pointer补正反方向门 |
| built-in audio spectra | `L3 bounded` | `SceneFrameContext` 携带同次 FFT 的 16/32/64 left/right host-shared 快照；project `supportsaudioprocessing` 仍只作 capability 诊断，不参与采集判定 | R4 host schema/finalizer/uniform encoder/pass encoder 已按精确 `g_AudioSpectrum{16|32|64}{Left|Right}[N]` 名称与 float 数组形状编码，未知名/类型/长度/跨 stage 冲突 fail closed | `3122339805:64` ordinary source与`2938612768:563` composition main-target source均已形成resolved-material graph GPU/compositor/next-frame正例，并以非零fixture取得可见柱高变化；后者只接受无child/dependency的有界utility shape，其他stock/exact audio owner仍保留 | [frame context](../../../MyWallpaperX/Core/SteamWorkshopScene/Runtime/SceneFrameContext.swift)、[SceneAudioSpectrum.swift](../../../MyWallpaperX/Core/SteamWorkshopScene/Runtime/SceneAudioSpectrum.swift)、[utility preflight](../../../MyWallpaperX/Core/SteamWorkshopScene/Rendering/SceneResolvedMaterialFramePreflight.swift)、[E-R4-2-AUDIO-ARRAY](runtime-evidence-index.md#e-r4-2-audio-array) | 粒子 audio、SceneScript AudioBuffers、任意 uniform array、频段归一化、任意utility/dependency与旧 audio owner 撤权未闭合 | 继续按可静态证明的source/shape迁移旧owner；每项补GPU、compositor、next-frame与局部视觉门 |
| built-in matrices | `L3 bounded image-effect` | renderer生成MVP与effect-texture projection正逆矩阵，host schema按exact作者名/type编码 | Program/pass encoder对2D image-effect quad上传同代矩阵；未知名称/type/stage冲突失败关闭 | Workshop Iris与Depth authored source已实际消费inverse/forward矩阵并取得方向/稳定性证据 | [matrix helpers](../../../MyWallpaperX/Core/SteamWorkshopScene/Rendering/SceneMatrix.swift)、[layer cursor geometry](../../../MyWallpaperX/Core/SteamWorkshopScene/Rendering/SceneLayerCursorGeometry.swift)、[host schema](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/MaterialProgram/SceneResolvedMaterialHostUniformSchema.swift)、[E-R4-3-AUTHORED-PROGRAM-CHAIN](runtime-evidence-index.md#e-r4-3-authored-program-chain) | 只闭合当前2D effect texture空间；model/view/projection、3D、非轴对齐与Windows转置约定未系统核验 | 增加rotation/crop/fit与inverse数值GPU门，再扩对象矩阵集合 |
| built-in resolution / texel size | `L3 bounded` | frame context 有 canvas/screen；typed candidate 保存 physical/mapped/transform，共享 slot binding 提供项目内 physical/mapped resolution、两类 texel size 与 mip count carrier；官方 Variables 页定义 `g_TextureNResolution.xy` 为 physical、`.zw` 为 mapped | Program uniform encoder 可从同代 texture atom按 exact slot 名编码 resolution；未绑定/未证 slot继续失败关闭 | Film Grain stock/no-mask 正例实际消费 slot 0 physical/mapped resolution计算 aspect；strict Blur、Standard Blur mask、Water Flow 等仍各自消费专用 carrier，其中exact stock Standard Blur graph stage已由统一executor调用同一strict carrier | [frame context](../../../MyWallpaperX/Core/SteamWorkshopScene/Runtime/SceneFrameContext.swift)、[typed candidate](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/SceneTextureCandidate.swift)、[slot binding](../../../MyWallpaperX/Core/SteamWorkshopScene/Resources/SceneTextureSlotBinding.swift)、[Program uniform encoder](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/MaterialProgram/SceneResolvedMaterialUniformEncoder.swift)、[E-TEXTURE-CANDIDATE](runtime-evidence-index.md#e-texture-candidate)、[E-EFFECT-FILM-GRAIN](runtime-evidence-index.md#e-effect-film-grain) | 一个 slot 0 正例不证明全部 `g_TextureNResolution`、texel/mip、sprite transform、动态 replacement/resize、BC5 或 Windows packing | 继续让 Program texture atom驱动逐 slot resolution/texel/mip，并按正负 GPU 门迁移旧 consumer |
| render state (blend/depth/write/cull/alpha writing) | `L2` | 五项 raw 值按 material pass 保存并进入 resolved node；共享 compiler 只把当前语料观测到的 `normal/translucent/additive`、enabled/disabled depth、normal/nocull 与 unspecified/default/enabled alpha 编译为 typed state，未知或不完整值返回 nil | typed state 进入 atomic Program 与 Metal compile-state identity；resolved pass encoder 只消费完整且可证明的 state，particle 保持独立 bounded admission | resolved-material 当前只准已验证 tuple；Cursor apply/simulate 只准同 tuple 的 alpha enabled、combine 只准 unspecified；particle 只准 additive/translucent、disabled depth/write、nocull 与 alpha unspecified/default，其他值失败关闭 | [typed state](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneMaterialRenderState.swift)、[template compiler](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/MaterialProgram/SceneResolvedMaterialTemplateCompiler.swift)、[Program identity](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/MaterialProgram/SceneResolvedMaterialProgramIdentity.swift)、[pass encoder](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/EffectExecution/SceneResolvedMaterialPassEncoder.swift)、[Cursor state](../../../MyWallpaperX/Core/SteamWorkshopScene/RenderGraph/SceneCursorRippleRenderStates.swift)、[particle admission](../../../MyWallpaperX/Core/SteamWorkshopScene/Particles/SceneParticleAssetGraph.swift)、[E-AUTHORED-SHADER](runtime-evidence-index.md#e-authored-shader)、[E-EFFECT-CURSOR-RIPPLE](runtime-evidence-index.md#e-effect-cursor-ripple) | `alphawriting=default` 未转换为 write mask；exact pipeline 中的 blending disabled/`.all`/cull none 是项目自有 tuple 合同。Ghidra 静态证据只确认 resolver/state injection 与 blend-to-variant 边界，不提供 Metal blend factor/operation、write mask、alpha convention 或 `default` 语义 | generic translator/cache 逐枚举增加 source/destination/alpha 像素门，并以 Windows golden 定标；不得从 Ghidra 推导 factor |
| MyWallpaperX 手写 MSL profile（非 WE 语言语义） | `L3` | 由effect/runtime plan转成固定Swift uniform struct | Metal compiler可编译repo内MSL | B1至B9已让typed pair、已证logical-target与exact raw Precise pair复用统一executor；B10没有新增MSL，只收敛visibility lifecycle；B11迁移bounded captured-main。B12/B13同样不改Clipping Mask或worleyColorV1 shader math，只把两种exact external-primary dependency reservation/publication/execute/compositor ownership收进统一调度 | [God Rays pipeline](../../../MyWallpaperX/Core/SteamWorkshopScene/Effects/SceneGodraysPipeline.swift)、[E-R4-B9-LEGACY-GODRAYS-LOGICAL-TARGET](runtime-evidence-index.md#e-r4-b9-legacy-godrays-logical-target)、[E-R4-B11-CAPTURED-STANDARD-BLUR-LOGICAL-TARGET](runtime-evidence-index.md#e-r4-b11-captured-standard-blur-logical-target)、[E-R4-B12-EXTERNAL-PRIMARY-CLIPPING-MASK](runtime-evidence-index.md#e-r4-b12-external-primary-clipping-mask)、[E-R4-B13-EXTERNAL-PRIMARY-PROCEDURAL-NOISE](runtime-evidence-index.md#e-r4-b13-external-primary-procedural-noise)、[E-R4-B8-PRECISE-FULL-FRAME-COMPOSE](runtime-evidence-index.md#e-r4-b8-precise-full-frame-compose) | 只说明当前自有backend可交付；各门不证明其他topology、generic named texture或Windows WE golden | 继续撤销剩余R4产品权限；视觉parity留到架构收敛后 |

R3 Program为`renderSize/MVP/time/dayTime/frameTime/pointer/screen/texelSize/textureResolution`建立typed host-uniform carrier，R4 又让 bounded dynamic scalar/vector3 与音频数组进入同一 Program/pass encoder；但这仍不证明所有官方名称、单位、空间、更新频率或任意 shader 兼容。未取得 GPU 正例的 built-in 行仍保持原等级与缺口，不得因 carrier 或 ABI 存在直接开放。

## 6. 现役能力扩展边界

1. **Shader contract IR v1（已完成）**：只读、安全保存 source/stage/include/annotation/declaration、raw/canonical hash 与诊断。当前是 line-based loss-preserving source contract，不是完整 AST；不运行外部编译器，也不声称 GPU 支持。
2. **strict Local Contrast（已完成）**：`2902406982` layers `167/177` 已闭合 exact stock 4-pass/2-quarter-RGBA 单效果 profile；`2938612768` mixed chains 保持 fail closed。边界仍是 mask/greyscale/nondefault kernel/scale、mixed chain、generic shader、`HLSL_SM30` parity、直接 uniform 上传遥测和 Windows golden。
3. **Ordered strict effect-chain（已完成有界执行；D6 extent 为 `L2`）**：完整支持的 material-only stage 已按作者顺序消费 target/bind/read/write，同 command buffer、整链 allocation/LRU 和末段合成有原子失败门；synthetic 与真实 Workshop chain 均有正向门，unsupported mixed chain 仍整体关闭。`69cc7f01` 让 authored `width`/`height`/`fit`/`scale` 保真、allocation 与 preflight area cost 共用同一算法，但没有因此建立 generic FBO/material executor。
4. **Exact Workshop Shadow（已完成）**：`3724289844:20` 的 `Blur Precise -> Shadow` 已成为首条真实 strict chain。该 backend 只接受完整 definition/material/ShaderContract、normal/no-mask/default-blend/静态常量合同；它不是官方 45 项 Effect、generic Shadow 或 authored shader，且 mode 0 无官方 Windows 像素 oracle。
5. **Stock Opacity `MASK=0` + live alpha（已完成）**：exact single-pass profile 已让 alpha 经既有 binding program/per-surface snapshot 被 GPU 消费；同批覆盖 premultiplied RGBA、ordered chain、invalid/mixed rebuild、same surface/window 与 unsupported stage 原子失败。
6. **Typed backend consolidation（只按真实复用推进）**：现役 backend 共享统一 frame preflight、persistent target、GraphExecutor transaction 与 compositor；旧 chain capture、whole-chain owner 和 partial-recovery 已删除。各 pipeline 仍可保留 profile-specific uniform/target 编排；只有真实重复能由同一职责边界消除时才提取，不为行数或未来扩展预建抽象。
7. **Provider Core（静态候选与 bounded slot-binding 切片已完成，动态/通用仍待推进）**：`378e68e7` 已让 Water Flow flow/phase 与 Standard Blur mask 复用同一 typed candidate；`de5f01b6` 进一步接入 static plain base image 与 bounded distinct REFRACT normal；`d87de042` 让同一 static base candidate 在 direct、inline、offscreen 与 authored-effect 路由统一经过最终 texture identity/purpose/UV/sampler/format 校验，错误 purpose 或不匹配 texture 不再静默回落 legacy frame；`6665cb98` 建立 authored `0...7` slot-binding atom，并让 Shake 1/2/3、Foliage Sway 1/2、Water Ripple 1/2 七槽分别把 texture 与各自 candidate 的 generation/index/identity/purpose/physical/mapped/完整 UV/sampler/format/mip/resolution/texel 原子绑定；`eedb3c62` 接入 exact legacy Blend slot 1，`b623f421` 再接入 Depth Parallax slot 1 R8 `.depth`，九槽都在最终 GPU split 复核 slot/purpose/format/UV/sampler 并实际消费 sampler；`7866dbc0` 让本机普通文件 generation 加入 device/inode/ctime，并在 image/TEX/video 读取或发布前后复核完整 revision，关闭同 size/恢复 mtime rewrite 与 metadata 全零 identity。effectful/nested/child provider 与 same-file/animated/multi-image/sprite/clamp-border REFRACT 保持 legacy 或关闭；新九槽仍只接受各自已证 axis-aligned/identity UV 与格式。其余 Effect/generic material 的 population/readiness、dynamic provider generation/stale cancellation、跨网络/粗粒度 metadata 的 content digest、alpha/color encoding 与 graph invalidation 仍未共用完整代际合同。
8. **Built-in 与 render-state binder（统一框架上的能力缺口）**：generic carrier 与 observed-enum compiler保持；typed `shaderValue` scalar/vector3和16/32/64 stereo audio arrays已进入Program/pass encoder。其他官方名称/单位/空间、通用state translator/cache与像素门仍待公共能力扩展；旧owner撤权已经完成，`alphawriting=default`继续只保真、不猜映射。
9. **R2 bounded shader preparation（已完成结构门）**：declared project/scene version provenance、独立 VFS source graph、有界 include/directive evaluator、exact numeric combo/readiness schema、active-only bounded fixed-point、active source map/digest 与 unresolved color carrier 已进入共享 preparation；45 样本 census 证明 raw/canonical 与 planner compile admission 相对 R1 无扩大或丢失。该门不替换 renderer/pipeline cache，也不清除任何视觉缺口。
10. **R3 atomic material program（已完成结构门）**：production raw graph形成固定8槽Template；exact provider publication、active variant/reflection、uniform bytes、typed state与保守color transfer在同一snapshot内形成semantic/exact Program identity。45样本census、provider正反fixture、one-shot production审计和四样本性能门已闭合；`gpuEncoded=0`，不能写成generic shader execution或视觉修复。
11. **Generic shader execution（统一框架上的能力扩展）**：统一Program/graph-node executor已对bounded implicit-framebuffer/straight-alpha/dynamic-uniform子集完成真实GPU/compositor执行；后续按capability family扩展condition/function、copy/swap/compose/history、state/resource与helper。只有不依赖固定effect/shader path且通过正负、生命周期与像素矩阵后，相关行才可升`L4`。
12. **R5 删除与合并门（`complete`）**：B22完成最后一个产品owner撤权，B23-B25删除whole-chain/standalone/frame-batch、旧planner/runtime-plan/decision/telemetry、shader fallback/provenance与重复资源/target入口。稳定不变量继续统计dedicated probes、runtime backends、旧authority sites、Scene Swift文件/LOC、`<3 KiB`与`<1 KiB`文件数，并禁止旧执行权、样本分支或第二target/resource路径回引。后续只可在统一框架上扩展能力；若需要恢复旧owner，必须作为架构回归处理。
13. **按效果扩面**：Motion Blur、Refraction、Fluid、SceneScript 与更多常见 backend 都是候选，但当前暂停指定下一项。先按原始样本新增 strict stage、完整链解锁、Windows WE 对照改善和实现成本复盘；不得用显示名或视觉近似绕开 authored contract。

## 7. 本轮验证

`69cc7f01` 的 D6 extent/预算公共修复以 effect-chain、effect graph、target plan、offscreen pool 四项门分别通过 **5/5、8/8、11/11、13/13**；`7866dbc0` 的 texture candidate 与 video source 分别通过 **1/1、7/7**。最终 checkout 的 Scene **150 modules / 794 tests ALL OK**、11 skipped，代码健康 677 Swift files / 44 locked legacy / 400-line limit，`script/build_and_run.sh verify` 通过。最终签名 Debug App 对隔离 `3299228616`、`3750342273`、`3765760121` 跑 `.codex/scene-texture-fbo-targeted-20260731-v3/report.json`，**3/3 PASS**、loaded ratio 均为 1，合计 completed frame 713，failed frame、drawable miss、graph blocker 与 sample residue 均为 0；报告 SHA-256 `e75bf6d61f67b1f3d4d77cdec56d56c0d65af5ad6825256cb2c8d8fc10d5e916`。该门只证明 strict fit/scale、静态纹理读取与既有图结构无回归，没有正向消费 arbitrary composed FBO，也没有模拟文件热替换；本批没有重跑 fixed13/full45。Glitter、generic FBO/material executor、dynamic provider cancellation 与跨文件系统 content digest 仍未闭合。

本批 typed slot-binding 的最终源码以 `python3 script/run_scene_tests.py --scope scene -j 4` 跑 **145 modules ALL OK**，代码健康 662 Swift files / 44 locked legacy / 400-line limit，签名 Debug build 通过。六样本隔离门 **6/6 PASS**，Shake/Foliage Sway/Water Ripple `33/7/6`、strict stage/chain `103/26`、graph failures/failed frames/drawable miss 均为 0，六组 ready/after 非黑且有运动；人工未见黑块、单侧遮罩丢失、异常裁切或 alpha 破坏。fixed13/full45 没有在 `6665cb98` 重跑；generic material、dynamic generation、rotation/translation、clamp-border、BC5 与 Windows pixel parity 仍保持关闭或未证。

`44b56cfc` 的 typed render-state 批次以同一命令跑 **146 modules ALL OK**，代码健康 664 Swift files / 44 locked legacy / 400-line limit，签名 Debug build 通过。`.codex/scene-render-state-20260730-v1/report.json` 对三个 Cursor Ripple、一个 authored shader 和两个 particle/REFRACT 样本跑 **6/6 PASS**：loaded ratio 均为 1，Cursor `3/3`、authored shader `1/1`、particle `19/19`、REFRACT 4，graph failed layer/failed frame/drawable miss 均为 0；六组 ready/after 均非黑，人工 montage 未见新增黑屏、单边写入或粒子丢失。没有重跑 fixed13/full45；真实门没有脚本化鼠标轨迹，Cursor 动态 state-channel 写入由 4 项 GPU 门证明，不能外推为 Windows 动态或像素等价。

当前运行门、strict/resolved聚合、报告/矩阵SHA与签名App身份统一见[运行证据索引](runtime-evidence-index.md)，本页不再复制维护。现役代码支持bounded Program、typed pair/logical-target、B8 exact raw Precise full-frame pair、B9 strict Directional God Rays logical-target、B10 visibility lifecycle、B11 exact captured-main Standard Blur，以及B12/B13 exact Clipping Mask与worleyColorV1 external-primary dependency。B22撤销独立ImageBlend产品owner，B23-B25删除旧执行surface、observation、shader fallback/provenance及重复资源/target入口；typed VFS graph、普通authored Blend、layer color blend与GraphExecutor per-stage typed backend继续保留。B25未运行benchmark/fixed/full，阶段为`R4=complete / R5=complete`；B22 full45 45/45 PASS、fixed13 12/13 NON-PASS与`2938612768`的`flat-border`视觉债务不变。formal PASS、非黑与旧ImageBlend 5/5不得用于升级generic graph primitive/compose、generic named texture/secondary dependency、Refraction、generic/full-frame-compose captured-main、history、arbitrary authored shader或视觉等价。

R2 最终源码以 `python3 script/run_scene_tests.py --scope scene -j 4` 跑 **173 modules / 119.4 s / ALL OK**，Swift code health **784 files / 44 locked legacy / 400-line limit** 与 `script/build_and_run.sh verify` 通过。45 样本 preparation census 为 434 contracts / 728 material passes，同一 current harness 两次完整输出逐字节一致；R1/current planner compile admission 都是 accepted 1 / rejected 727，new/lost accepted 均为 0，prepared accepted/rejected 93/635，正式报告 SHA-256 `2e535ccceb0fa381ca909217e7f1e4a16b434b3ea2b279a57c43d64b0ebf1b5e`。隔离六样本 `.codex/scene-chain-r2-preparation-20260803-v1/report.json` 为 **6/6 PASS**、loaded ratio 均为 1、ready/after 均非黑且有运动，admission/disposition/execution validation、effect execution gap/failure 与运行 failed frame 均为 0，报告 SHA-256 `f478dac02e332d4f7e0fafd01482b539986fd69078f3cb6d53a82cae548f5d63`；preview 比较仅为 advisory、`gating=false`，两个已知样本仍分别保留 1 / 6 个 `no-backend-accepted` stage failure。以上只证明 preparation 确定性、planner 准入守恒和当前运行无结构回归，不证明局部头发、鱼群水流、烟花或任何 Windows 像素/动态等价。

R3最终源码以同一Scene全量入口跑 **179 modules / 119.2 s / ALL OK**，Swift code health **795 files / 44 locked legacy / 400-line limit**、`script/build_and_run.sh verify`与深度严格验签通过。45样本material census为1296 descriptor effects / 489 authored plans / 1400 material nodes，Template 1392 accepted / 8 rejected，Program因无runtime snapshot为0 attempted；两次结果字节一致，报告SHA-256 `394b7f8fd5f59a7d88eb307b26d7bcc739e0bb5eb58a84067bc180c36da09999`。逐帧审计的首版四样本虽结构PASS却造成严重CPU回退；改为first-active-frame、闭合最终 Image Blend legacy bypass并持久化审计后，`.codex/scene-chain-r3-material-atomic-20260804-v4/report.json`为 **4/4 PASS**、loaded ratio全1、failed frame全0，报告SHA-256 `23c6b2cc5c7c1d83db75dcfa848bb7d7234172c05ffe14578c7d658bad91c9fb`，四样本均保持目标帧率。每个样本恰有一条production audit且均`gpuEncoded=0`，但真实Program finalization accepted全为0；这证明分类稳定和unknown fail-closed，不证明真实样本Program成功。本批未跑fixed13/full45，也没有修复三项视觉缺口或证明官方/Windows等价。

R4 bounded macro preparation follow-up 的项目自有正反门覆盖 function-like/object token sequence 的展开与失败预算；Scene 全量 **190 modules / 200.5 s / ALL OK**，最终 code health **838 files / 44 locked legacy / 400-line limit** 与签名 `script/build_and_run.sh verify` 通过。preparation census 为 **116/612**、GPU admission 保持 **1/727**，报告 SHA-256 `0994b560bf288ffe56c95921137df95d01056bf3ec16b52e494ab0b32a4306a3`；material census 的 R2 variant bootstrap 为 **446/954**，Template **1392/8**、Program **0 attempted / 1400 notAttempted**，报告 SHA-256 `50011556edcd9b333888bd7d14a1cf91ecdc158b387dc97638b5717642076b10`。四样本隔离门 **4/4 PASS**，报告 SHA-256 `f66519e7c7f636f72900eeb73d34995f06f0e7c407bb3e31c281fb02507fb52e`，但 resolved graph 全部 `accepted=0`；因此没有撤旧 owner、没有提升 GPU/视觉等级，也没有运行 full45。

R4 2026-08-07 owner/lifecycle 门把 immutable bootstrap sampler preparation 收敛到 launch/cache scope，并允许 legacy frame batch 在 snapshot 后的无关 GPU completion 改变 revision 时，于 commit 锁内重验原 generation/history/shared-pair identity；reset epoch 或身份变化仍拒绝。随后 authored-shader 撤权波次删除旧 compiler/backend/plan/planner/binder/pipeline/renderer/Scroll profile，并把 generic stage 从旧 chain 守恒中移出。fresh full45 中七个样本共 22 层 / 68 stage 进入 resolved graph，accepted/exact backend/compositor/next-frame 守恒且 legacy conflict 为空；fixed13 **13/13 PASS**，full45 **44/45，非 PASS**，唯一红项仍是既有 `2067939514` 粒子合同偏差，当前 executor claimed/encoded/GPU `648/648/648`、failure `0`。这只闭合 authored-shader 产品 owner family，不提升 arbitrary shader 等级；dedicated、legacy、utility/audio owner 仍待 R4 撤权，R5 未准入。

R4 2026-08-08 ordinary Tint 波次继续沿同一 Program/capability/executor 链撤权：当时只有完整 Program 成功时，`.tint` dedicated plan 才让出 owner，失败与 mixed/unmigrated chain 仍保留 bounded fallback。该波次 fresh full45 为 **14 个样本 / 57 个 resolved layer / 75 个 exact subject**，其中 Tint **41 个 exact subject / 41 层**；executor claimed/encoded/GPU `2084/2084/2084`、failure `0`，graph validation/failed/GPU failed 为 `0/0/0`。`2902406982` 首轮 fixed13 暴露的 live property 拒绝由 capability 投影唯一 user-property target 修复，最终 fixed13 **13/13 PASS**、full45 **44/45，非 PASS**，仍只保留 `2067939514` 粒子偏差。此处 fallback 是 R4-A+C 前历史行为，现役 rejected authored graph 已中和 legacy 输入；这些数字也不是本轮 fresh full45。它不证明任意 Tint revision、helper/include、Windows 色彩/像素或 R4/R5 完成。

R4 2026-08-08 Film Grain 波次沿官方普通 image effect 职责路径补齐一个公共 resource/schema 缺口：closed registry 只登记 exact `util/noise -> .noise`，从而让旧式隐式 framebuffer 与 typed auxiliary default 在同一 Template/Program 中成立；当时 Program 成功时 `.filmGrain` dedicated plan 让出 owner，失败或 unsupported topology 仍由原 planner/pipeline/renderer fallback。正例 `3767460992:20#effect#702` 经 source graph、`common.h`/`common_blending.h`、`g_Time`、slot resolution、两个 texture atom、uniform/state/color、统一 executor 与 compositor 完整闭合；负例 `3747492842:434#effect#545` 当时为 `prefix-omitted`。该波次 fresh full45 的 resolved 总量为 **14 个样本 / 57 个 layer / 75 个 exact subject**，Film Grain只从 dedicated family 转到 resolved-material family；executor claimed/encoded/GPU `2458/2458/2458`、failure `0`。full45 **44/45，非 PASS**，唯一红项仍是 `2067939514` 的既有粒子偏差。上述 fallback/prefix recovery 已被 R4-A+C 现役完整链拒绝合同取代，数字也不是本轮 fresh full45；mask、其他 greyscale/blend/revision、任意 stock path、Windows 动态/像素及 R4/R5 仍未闭合。
